// Snyk Policies — security policy management and rule configuration — Tests
// Auto-generated by scripts/snyk-codegen — do not edit manually.
// SPDX-License-Identifier: Apache-2.0

import { assertEquals, assertExists } from "jsr:@std/assert@1";
import { createModelTestContext } from "@systeminit/swamp-testing";
import { model } from "./policies.ts";

// ---------------------------------------------------------------------------
// Model Export Structure Tests
// ---------------------------------------------------------------------------

Deno.test("policies model: has correct type", () => {
  assertEquals(model.type, "@webframp/snyk/policies");
});

Deno.test("policies model: has valid version format", () => {
  const versionPattern = /^\d{4}\.\d{2}\.\d{2}\.\d+$/;
  assertEquals(versionPattern.test(model.version), true);
});

Deno.test("policies model: has globalArguments with apiToken", () => {
  assertExists(model.globalArguments);
  const shape = model.globalArguments.shape;
  assertExists(shape.apiToken);
  assertExists(shape.orgId);
  assertExists(shape.version);
});

Deno.test("policies model: has expected methods", () => {
  assertExists(model.methods);
  assertExists(model.methods.get_org_policies);
  assertExists(model.methods.create_org_policy);
  assertExists(model.methods.get_org_policy);
  assertExists(model.methods.update_org_policy);
  assertExists(model.methods.delete_org_policy);
  assertExists(model.methods.get_org_policy_events);
});

Deno.test("policies model: has expected resources", () => {
  assertExists(model.resources);
  assertExists(model.resources["get_org_policies"]);
  assertExists(model.resources["org_policy"]);
  assertExists(model.resources["get_org_policy_events"]);
});

// ---------------------------------------------------------------------------
// Mock Snyk API Server
// ---------------------------------------------------------------------------

function startMockSnykServer(
  responses: Record<string, { data: unknown; isCollection?: boolean }>,
): { url: string; server: Deno.HttpServer } {
  const server = Deno.serve({ port: 0, onListen() {} }, (req) => {
    const url = new URL(req.url);
    const path = url.pathname;

    for (const [pattern, { data, isCollection }] of Object.entries(responses)) {
      if (path.includes(pattern)) {
        const body = isCollection
          ? {
            data: Array.isArray(data) ? data : [data],
            jsonapi: { version: "1.0" },
            links: { self: path, next: null },
          }
          : {
            data,
            jsonapi: { version: "1.0" },
            links: { self: path },
          };
        return Response.json(body, {
          headers: { "content-type": "application/vnd.api+json" },
        });
      }
    }

    return new Response(
      JSON.stringify({
        jsonapi: { version: "1.0" },
        errors: [{ status: "404", detail: "Not found" }],
      }),
      { status: 404, headers: { "content-type": "application/vnd.api+json" } },
    );
  });

  const addr = server.addr as Deno.NetAddr;
  return { url: `http://localhost:${addr.port}`, server };
}

function installFetchMock(mockUrl: string): () => void {
  const originalFetch = globalThis.fetch;
  globalThis.fetch = (input, init) => {
    const reqUrl = typeof input === "string" ? input : input.toString();
    const newUrl = reqUrl.replace("https://api.snyk.io/rest", mockUrl);
    return originalFetch(newUrl, init);
  };
  return () => {
    globalThis.fetch = originalFetch;
  };
}

Deno.test({
  name: "policies model: get_org_policies fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockItem = {
      "id": "fixture-123",
      "type": "resource",
      "attributes": {
        "action": { "data": null },
        "action_type": "ignore",
        "conditions_group": { "conditions": [], "logical_operator": "and" },
        "created_at": "2024-03-16T00:00:00Z",
        "created_by": {
          "actor_source": "snyk_user",
          "email": "test-value",
          "external_id": "test-value",
          "id": "test-value",
          "name": "test-value",
          "origin": "bitbucket_cloud",
        },
        "name": "test-value",
        "review": "pending",
        "updated_at": "2024-03-16T00:00:00Z",
      },
    };
    const { url, server } = startMockSnykServer({
      "/policies": { data: [mockItem], isCollection: true },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: {
          "apiToken": "test-token",
          "version": "2024-10-15",
          "orgId": "test-org-123",
        },
        definition: {
          id: "test-id",
          name: "test-policies",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).get_org_policies.execute({}, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "policies model: create_org_policy creates and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "id": "new-123",
      "type": "resource",
      "attributes": {
        "action": { "data": null },
        "action_type": "ignore",
        "conditions_group": { "conditions": [], "logical_operator": "and" },
        "created_at": "2024-03-16T00:00:00Z",
        "created_by": {
          "actor_source": "snyk_user",
          "email": "test-value",
          "external_id": "test-value",
          "id": "test-value",
          "name": "test-value",
          "origin": "bitbucket_cloud",
        },
        "name": "test-value",
        "review": "pending",
        "updated_at": "2024-03-16T00:00:00Z",
      },
    };
    const { url, server } = startMockSnykServer({
      "/policies": { data: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: {
          "apiToken": "test-token",
          "version": "2024-10-15",
          "orgId": "test-org-123",
        },
        definition: {
          id: "test-id",
          name: "test-policies",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).create_org_policy.execute({ "name": "test-resource" }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "policies model: get_org_policy fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "id": "fixture-123",
      "type": "resource",
      "attributes": {
        "action": { "data": null },
        "action_type": "ignore",
        "conditions_group": { "conditions": [], "logical_operator": "and" },
        "created_at": "2024-03-16T00:00:00Z",
        "created_by": {
          "actor_source": "snyk_user",
          "email": "test-value",
          "external_id": "test-value",
          "id": "test-value",
          "name": "test-value",
          "origin": "bitbucket_cloud",
        },
        "name": "test-value",
        "review": "pending",
        "updated_at": "2024-03-16T00:00:00Z",
      },
    };
    const { url, server } = startMockSnykServer({
      "/policies/test-id-123": { data: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: {
          "apiToken": "test-token",
          "version": "2024-10-15",
          "orgId": "test-org-123",
        },
        definition: {
          id: "test-id",
          name: "test-policies",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).get_org_policy.execute({ "policy_id": "test-id-123" }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "policies model: update_org_policy executes and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "id": "fixture-123",
      "type": "resource",
      "attributes": {
        "action": { "data": null },
        "action_type": "ignore",
        "conditions_group": { "conditions": [], "logical_operator": "and" },
        "created_at": "2024-03-16T00:00:00Z",
        "created_by": {
          "actor_source": "snyk_user",
          "email": "test-value",
          "external_id": "test-value",
          "id": "test-value",
          "name": "test-value",
          "origin": "bitbucket_cloud",
        },
        "name": "test-value",
        "review": "pending",
        "updated_at": "2024-03-16T00:00:00Z",
      },
    };
    const { url, server } = startMockSnykServer({
      "/policies/test-id-123": { data: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: {
          "apiToken": "test-token",
          "version": "2024-10-15",
          "orgId": "test-org-123",
        },
        definition: {
          id: "test-id",
          name: "test-policies",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).update_org_policy.execute({
        "policy_id": "test-id-123",
        "name": "test-resource",
      }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "policies model: delete_org_policy executes successfully",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockSnykServer({
      "/policies/test-id-123": {
        data: { id: "test-id-123", type: "resource" },
      },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context } = createModelTestContext({
        globalArgs: {
          "apiToken": "test-token",
          "version": "2024-10-15",
          "orgId": "test-org-123",
        },
        definition: {
          id: "test-id",
          name: "test-policies",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).delete_org_policy.execute({ "policy_id": "test-id-123" }, context);
      assertEquals(result.dataHandles.length, 0);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});
