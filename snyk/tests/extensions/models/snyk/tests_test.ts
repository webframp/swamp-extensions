// Snyk Tests — on-demand package and dependency vulnerability testing — Tests
// Auto-generated by scripts/snyk-codegen — do not edit manually.
// SPDX-License-Identifier: Apache-2.0

import {
  assertEquals,
  assertExists,
  assertStringIncludes,
} from "jsr:@std/assert@1.0.19";
import { createModelTestContext } from "@systeminit/swamp-testing";
import { model } from "./tests.ts";

// ---------------------------------------------------------------------------
// Model Export Structure Tests
// ---------------------------------------------------------------------------

Deno.test("tests model: has correct type", () => {
  assertEquals(model.type, "@webframp/snyk/tests");
});

Deno.test("tests model: has valid version format", () => {
  const versionPattern = /^\d{4}\.\d{2}\.\d{2}\.\d+$/;
  assertEquals(versionPattern.test(model.version), true);
});

Deno.test("tests model: has globalArguments with apiToken", () => {
  assertExists(model.globalArguments);
  const shape = model.globalArguments.shape;
  assertExists(shape.apiToken);
  assertExists(shape.orgId);
  assertExists(shape.version);
});

Deno.test("tests model: has expected methods", () => {
  assertExists(model.methods);
  assertExists(model.methods.create_test);
  assertExists(model.methods.get_test);
  assertExists(model.methods.list_findings);
});

Deno.test("tests model: has expected resources", () => {
  assertExists(model.resources);
  assertExists(model.resources["test"]);
  assertExists(model.resources["findings"]);
});

// ---------------------------------------------------------------------------
// Mock Snyk API Server
// ---------------------------------------------------------------------------

interface CapturedRequest {
  method: string;
  path: string;
  search: string;
  headers: Record<string, string>;
  body: unknown;
}

function startMockSnykServer(
  responses: Record<string, { data: unknown; isCollection?: boolean }>,
): { url: string; server: Deno.HttpServer; requests: CapturedRequest[] } {
  const requests: CapturedRequest[] = [];
  const server = Deno.serve({ port: 0, onListen() {} }, async (req) => {
    const url = new URL(req.url);
    const path = url.pathname;

    // Capture the request (headers, method, path, query, parsed body).
    let reqBody: unknown = null;
    if (req.body) {
      const text = await req.text();
      if (text) {
        try {
          reqBody = JSON.parse(text);
        } catch {
          reqBody = text;
        }
      }
    }
    requests.push({
      method: req.method,
      path,
      search: url.search,
      headers: Object.fromEntries(req.headers),
      body: reqBody,
    });

    for (const [pattern, { data, isCollection }] of Object.entries(responses)) {
      if (path.includes(pattern)) {
        const body = isCollection
          ? {
            data: Array.isArray(data) ? data : [data],
            jsonapi: { version: "1.0" },
            links: { self: path, next: null },
          }
          : {
            data,
            jsonapi: { version: "1.0" },
            links: { self: path },
          };
        return Response.json(body, {
          headers: { "content-type": "application/vnd.api+json" },
        });
      }
    }

    return new Response(
      JSON.stringify({
        jsonapi: { version: "1.0" },
        errors: [{ status: "404", detail: "Not found" }],
      }),
      { status: 404, headers: { "content-type": "application/vnd.api+json" } },
    );
  });

  const addr = server.addr as Deno.NetAddr;
  return { url: `http://localhost:${addr.port}`, server, requests };
}

function installFetchMock(mockUrl: string): () => void {
  const originalFetch = globalThis.fetch;
  globalThis.fetch = (input, init) => {
    const reqUrl = typeof input === "string" ? input : input.toString();
    const newUrl = reqUrl.replace("https://api.snyk.io/rest", mockUrl);
    return originalFetch(newUrl, init);
  };
  return () => {
    globalThis.fetch = originalFetch;
  };
}

Deno.test({
  name: "tests model: create_test creates and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = { "id": "new-123", "type": "resource" };
    const { url, server, requests } = startMockSnykServer({
      "/tests": { data: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: {
          "apiToken": "test-token",
          "version": "2024-10-15",
          "orgId": "test-org-123",
        },
        definition: { id: "test-id", name: "test-tests", version: 1, tags: {} },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).create_test.execute({ "name": "test-resource" }, context);
      assertEquals(result.dataHandles.length, 1);

      assertEquals(requests.length, 1);
      const req0 = requests[0];
      assertEquals(req0.method, "POST");
      assertStringIncludes(req0.path, "/tests");
      assertStringIncludes(req0.search, "version=");
      assertEquals(req0.headers["authorization"], "token test-token");
      assertEquals(req0.headers["content-type"], "application/vnd.api+json");
      assertExists(req0.body);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
      assertEquals(resources[0].specName, "test");
      assertEquals(resources[0].name, "new-123");
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "tests model: get_test fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "id": "fixture-123",
      "type": "resource",
      "attributes": {
        "components": [],
        "config": {
          "asset_name": "test-value",
          "local_policy": null,
          "monitor": true,
          "project_business_criticality": "test-value",
          "project_environment": [],
          "project_lifecycle": [],
          "project_tags": [],
          "publish_report": true,
          "scan_config": null,
          "target_name": "test-value",
          "target_reference": "test-value",
          "timeout": null,
        },
        "created_at": "2024-01-01T00:00:00Z",
        "effective_summary": {
          "count": 30,
          "count_by": {
            "result_type": { "sast": 12, "sca": 18 },
            "severity": { "critical": 2, "high": 3, "low": 15, "medium": 10 },
          },
        },
        "outcome": {
          "breached_policies": null,
          "reason": null,
          "result": null,
        },
        "raw_summary": {
          "count": 30,
          "count_by": {
            "result_type": { "sast": 12, "sca": 18 },
            "severity": { "critical": 2, "high": 3, "low": 15, "medium": 10 },
          },
        },
        "resources": [{
          "resource": {
            "content_type": "source",
            "file_patterns": ["*.go"],
            "repository_url": "https://example.com/source/repo",
            "revision_id": "abcde12345",
            "type": "upload",
          },
          "type": "base",
        }, {
          "resource": {
            "ref_id": "6BD4F5E2-D4E8-4C6B-BB0E-ECF0A70423F5",
            "reference_type": "target",
            "type": "snyk_ref",
          },
          "type": "base",
        }],
        "sdlc_stage": {},
        "state": { "errors": [], "execution": null, "warnings": [] },
        "subject": {},
        "subject_locators": [{
          "paths": ["package.json"],
          "type": "local_path",
        }],
        "test_facts": [],
        "user_public_id": "test-value",
      },
    };
    const { url, server, requests } = startMockSnykServer({
      "/tests/test-id-123": { data: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: {
          "apiToken": "test-token",
          "version": "2024-10-15",
          "orgId": "test-org-123",
        },
        definition: { id: "test-id", name: "test-tests", version: 1, tags: {} },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).get_test.execute({ "test_id": "test-id-123" }, context);
      assertEquals(result.dataHandles.length, 1);

      assertEquals(requests.length, 1);
      const req0 = requests[0];
      assertEquals(req0.method, "GET");
      assertStringIncludes(req0.path, "/tests/test-id-123");
      assertStringIncludes(req0.search, "version=");
      assertEquals(req0.headers["authorization"], "token test-token");

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
      assertEquals(resources[0].specName, "test");
      assertEquals(resources[0].name, "test-id-123");
      const data = resources[0].data as Record<string, unknown>;
      assertEquals(data.id, "fixture-123");
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "tests model: list_findings fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockItem = {
      "id": "fixture-123",
      "type": "resource",
      "attributes": {
        "cause_of_failure": true,
        "component_key": {},
        "description": "test-value",
        "evidence": [{
          "path": [{ "name": "my-app:1.0.0", "version": "log4j:2.4.1" }],
          "source": "dependency_path",
        }],
        "finding_type": {},
        "key": "test-value",
        "locations": [],
        "policy_modifications": [],
        "problems": [{ "id": "CWE-943", "source": "cwe" }, {
          "default_configuration": { "severity": "high" },
          "help": { "markdown": "help text goes here" },
          "id": "javascript/NoSqli",
          "name": "NoSqli",
          "properties": {
            "categories": ["Security"],
            "cwe": ["CWE-943"],
            "example_commit_descriptions": [],
            "example_commit_fixes": [],
            "precision": "very-high",
            "repo_dataset_size": 30,
            "tags": ["javascript", "NoSqli"],
          },
          "short_description": { "text": "NoSQL Injection" },
          "source": "snyk_code_rule",
        }],
        "rating": { "severity": null },
        "risk": { "risk_score": null },
        "suppression": {
          "created_at": "2024-01-01T00:00:00Z",
          "expires_at": "2024-01-01T00:00:00Z",
          "justification": "test-value",
          "path": [],
          "policy": null,
          "skipIfFixable": true,
          "status": null,
        },
        "title": "test-value",
        "asset_id": "test-value",
        "fix_id": "test-value",
        "org_id": "test-value",
        "policy_id": "test-value",
        "project_id": "test-value",
        "test_id": "test-value",
      },
    };
    const { url, server, requests } = startMockSnykServer({
      "/tests/test-id-123/findings": { data: [mockItem], isCollection: true },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: {
          "apiToken": "test-token",
          "version": "2024-10-15",
          "orgId": "test-org-123",
        },
        definition: { id: "test-id", name: "test-tests", version: 1, tags: {} },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).list_findings.execute({ "test_id": "test-id-123" }, context);
      assertEquals(result.dataHandles.length, 1);

      assertEquals(requests.length, 1);
      const req0 = requests[0];
      assertEquals(req0.method, "GET");
      assertStringIncludes(req0.path, "/tests/test-id-123/findings");
      assertStringIncludes(req0.search, "version=");
      assertEquals(req0.headers["authorization"], "token test-token");

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
      assertEquals(resources[0].specName, "findings");
      const data = resources[0].data as {
        items: unknown[];
        truncated: boolean;
      };
      assertEquals(Array.isArray(data.items), true);
      assertEquals(data.items.length, 1);
      assertEquals(typeof data.truncated, "boolean");
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "tests model: create_test surfaces API errors",
  sanitizeResources: false,
  fn: async () => {
    // A server that returns 500 for every request, exercising the error path.
    const originalFetch = globalThis.fetch;
    const server = Deno.serve({ port: 0, onListen() {} }, () =>
      new Response(
        JSON.stringify({
          jsonapi: { version: "1.0" },
          errors: [{ status: "500", detail: "Internal Server Error" }],
        }),
        {
          status: 500,
          headers: { "content-type": "application/vnd.api+json" },
        },
      ));
    const addr = server.addr as Deno.NetAddr;
    const mockUrl = `http://localhost:${addr.port}`;
    globalThis.fetch = (input, init) => {
      const reqUrl = typeof input === "string" ? input : input.toString();
      return originalFetch(
        reqUrl.replace("https://api.snyk.io/rest", mockUrl),
        init,
      );
    };

    try {
      const { context } = createModelTestContext({
        globalArgs: {
          "apiToken": "test-token",
          "version": "2024-10-15",
          "orgId": "test-org-123",
        },
        definition: { id: "test-id", name: "test-tests", version: 1, tags: {} },
      });

      let threw = false;
      try {
        await (model.methods as Record<
          string,
          {
            execute: (
              args: Record<string, unknown>,
              ctx: unknown,
            ) => Promise<{ dataHandles: unknown[] }>;
          }
        >).create_test.execute({ "name": "test-resource" }, context);
      } catch (_err) {
        threw = true;
      }
      assertEquals(threw, true);
    } finally {
      globalThis.fetch = originalFetch;
      await server.shutdown();
    }
  },
});
