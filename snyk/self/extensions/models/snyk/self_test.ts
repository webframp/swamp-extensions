// Snyk Self — current user context, org listing, and app management — Tests
// Auto-generated by scripts/snyk-codegen — do not edit manually.
// SPDX-License-Identifier: Apache-2.0

import {
  assertEquals,
  assertExists,
  assertStringIncludes,
} from "jsr:@std/assert@1.0.19";
import { createModelTestContext } from "@systeminit/swamp-testing";
import { model } from "./self.ts";

// ---------------------------------------------------------------------------
// Model Export Structure Tests
// ---------------------------------------------------------------------------

Deno.test("self model: has correct type", () => {
  assertEquals(model.type, "@webframp/snyk/self");
});

Deno.test("self model: has valid version format", () => {
  const versionPattern = /^\d{4}\.\d{2}\.\d{2}\.\d+$/;
  assertEquals(versionPattern.test(model.version), true);
});

Deno.test("self model: has globalArguments with apiToken", () => {
  assertExists(model.globalArguments);
  const shape = model.globalArguments.shape;
  assertExists(shape.apiToken);
  assertExists(shape.version);
});

Deno.test("self model: has expected methods", () => {
  assertExists(model.methods);
  assertExists(model.methods.get_self);
  assertExists(model.methods.get_access_requests);
  assertExists(model.methods.get_user_installed_apps);
  assertExists(model.methods.get_app_installs_for_user);
  assertExists(model.methods.delete_user_app_install_by_id);
  assertExists(model.methods.delete_revoke_user_installed_app);
  assertExists(model.methods.get_user_app_sessions);
  assertExists(model.methods.delete_revoke_user_app_session);
  assertExists(model.methods.list_personal_access_token);
  assertExists(model.methods.delete_personal_access_token);
});

Deno.test("self model: has expected resources", () => {
  assertExists(model.resources);
  assertExists(model.resources["self"]);
  assertExists(model.resources["get_access_requests"]);
  assertExists(model.resources["get_user_installed_apps"]);
  assertExists(model.resources["get_app_installs_for_user"]);
  assertExists(model.resources["get_user_app_sessions"]);
  assertExists(model.resources["personal_access_token"]);
});

// ---------------------------------------------------------------------------
// Mock Snyk API Server
// ---------------------------------------------------------------------------

interface CapturedRequest {
  method: string;
  path: string;
  search: string;
  headers: Record<string, string>;
  body: unknown;
}

function startMockSnykServer(
  responses: Record<string, { data: unknown; isCollection?: boolean }>,
): { url: string; server: Deno.HttpServer; requests: CapturedRequest[] } {
  const requests: CapturedRequest[] = [];
  const server = Deno.serve({ port: 0, onListen() {} }, async (req) => {
    const url = new URL(req.url);
    const path = url.pathname;

    // Capture the request (headers, method, path, query, parsed body).
    let reqBody: unknown = null;
    if (req.body) {
      const text = await req.text();
      if (text) {
        try {
          reqBody = JSON.parse(text);
        } catch {
          reqBody = text;
        }
      }
    }
    requests.push({
      method: req.method,
      path,
      search: url.search,
      headers: Object.fromEntries(req.headers),
      body: reqBody,
    });

    for (const [pattern, { data, isCollection }] of Object.entries(responses)) {
      if (path.includes(pattern)) {
        const body = isCollection
          ? {
            data: Array.isArray(data) ? data : [data],
            jsonapi: { version: "1.0" },
            links: { self: path, next: null },
          }
          : {
            data,
            jsonapi: { version: "1.0" },
            links: { self: path },
          };
        return Response.json(body, {
          headers: { "content-type": "application/vnd.api+json" },
        });
      }
    }

    return new Response(
      JSON.stringify({
        jsonapi: { version: "1.0" },
        errors: [{ status: "404", detail: "Not found" }],
      }),
      { status: 404, headers: { "content-type": "application/vnd.api+json" } },
    );
  });

  const addr = server.addr as Deno.NetAddr;
  return { url: `http://localhost:${addr.port}`, server, requests };
}

function installFetchMock(mockUrl: string): () => void {
  const originalFetch = globalThis.fetch;
  globalThis.fetch = (input, init) => {
    const reqUrl = typeof input === "string" ? input : input.toString();
    const newUrl = reqUrl.replace("https://api.snyk.io/rest", mockUrl);
    return originalFetch(newUrl, init);
  };
  return () => {
    globalThis.fetch = originalFetch;
  };
}

Deno.test({
  name: "self model: get_self fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "id": "fixture-123",
      "type": "resource",
      "attributes": {},
    };
    const { url, server, requests } = startMockSnykServer({
      "/self": { data: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "version": "2024-10-15" },
        definition: { id: "test-id", name: "test-self", version: 1, tags: {} },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).get_self.execute({}, context);
      assertEquals(result.dataHandles.length, 1);

      assertEquals(requests.length, 1);
      const req0 = requests[0];
      assertEquals(req0.method, "GET");
      assertStringIncludes(req0.path, "/self");
      assertStringIncludes(req0.search, "version=");
      assertEquals(req0.headers["authorization"], "token test-token");

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
      assertEquals(resources[0].specName, "self");
      assertEquals(resources[0].name, "latest");
      const data = resources[0].data as Record<string, unknown>;
      assertEquals(data.id, "fixture-123");
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "self model: get_access_requests fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockItem = {
      "id": "fixture-123",
      "type": "resource",
      "attributes": { "status": "pending" },
    };
    const { url, server, requests } = startMockSnykServer({
      "/self/access_requests": { data: [mockItem], isCollection: true },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "version": "2024-10-15" },
        definition: { id: "test-id", name: "test-self", version: 1, tags: {} },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).get_access_requests.execute({}, context);
      assertEquals(result.dataHandles.length, 1);

      assertEquals(requests.length, 1);
      const req0 = requests[0];
      assertEquals(req0.method, "GET");
      assertStringIncludes(req0.path, "/self/access_requests");
      assertStringIncludes(req0.search, "version=");
      assertEquals(req0.headers["authorization"], "token test-token");

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
      assertEquals(resources[0].specName, "get_access_requests");
      const data = resources[0].data as {
        items: unknown[];
        truncated: boolean;
      };
      assertEquals(Array.isArray(data.items), true);
      assertEquals(data.items.length, 1);
      assertEquals(typeof data.truncated, "boolean");
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "self model: delete_user_app_install_by_id executes successfully",
  sanitizeResources: false,
  fn: async () => {
    const { url, server, requests } = startMockSnykServer({
      "/self/apps/installs/test-id-123": {
        data: { id: "test-id-123", type: "resource" },
      },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "version": "2024-10-15" },
        definition: { id: "test-id", name: "test-self", version: 1, tags: {} },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).delete_user_app_install_by_id.execute({
        "install_id": "test-id-123",
      }, context);
      assertEquals(result.dataHandles.length, 0);

      assertEquals(requests.length, 1);
      const req0 = requests[0];
      assertEquals(req0.method, "DELETE");
      assertStringIncludes(req0.path, "/self/apps/installs/test-id-123");
      assertStringIncludes(req0.search, "version=");
      assertEquals(req0.headers["authorization"], "token test-token");

      const resources = getWrittenResources();
      assertEquals(resources.length, 0);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "self model: get_self surfaces API errors",
  sanitizeResources: false,
  fn: async () => {
    // A server that returns 500 for every request, exercising the error path.
    const originalFetch = globalThis.fetch;
    const server = Deno.serve({ port: 0, onListen() {} }, () =>
      new Response(
        JSON.stringify({
          jsonapi: { version: "1.0" },
          errors: [{ status: "500", detail: "Internal Server Error" }],
        }),
        {
          status: 500,
          headers: { "content-type": "application/vnd.api+json" },
        },
      ));
    const addr = server.addr as Deno.NetAddr;
    const mockUrl = `http://localhost:${addr.port}`;
    globalThis.fetch = (input, init) => {
      const reqUrl = typeof input === "string" ? input : input.toString();
      return originalFetch(
        reqUrl.replace("https://api.snyk.io/rest", mockUrl),
        init,
      );
    };

    try {
      const { context } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "version": "2024-10-15" },
        definition: { id: "test-id", name: "test-self", version: 1, tags: {} },
      });

      let threw = false;
      try {
        await (model.methods as Record<
          string,
          {
            execute: (
              args: Record<string, unknown>,
              ctx: unknown,
            ) => Promise<{ dataHandles: unknown[] }>;
          }
        >).get_self.execute({}, context);
      } catch (_err) {
        threw = true;
      }
      assertEquals(threw, true);
    } finally {
      globalThis.fetch = originalFetch;
      await server.shutdown();
    }
  },
});
