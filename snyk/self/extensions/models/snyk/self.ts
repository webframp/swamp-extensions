/**
 * Snyk Self — current user context, org listing, and app management
 *
 * Auto-generated by scripts/snyk-codegen — do not edit manually.
 *
 * @module
 */
// SPDX-License-Identifier: AGPL-3.0-or-later WITH Swamp-Extension-Exception

import { z } from "npm:zod@4.4.3";
import { snykApi, snykApiPaginated } from "./_lib/api.ts";

// =============================================================================
// Schemas
// =============================================================================

const GlobalArgsSchema = z.object({
  apiToken: z.string().meta({ sensitive: true }).describe("Snyk API token"),
  version: z.string().default("2024-10-15").describe("Snyk API version date"),
});

const GetSelfSchema = z.object({
  id: z.string().describe(
    "The Snyk ID corresponding to this user, service account or app",
  ),
  type: z.enum(["user", "service_account", "app_instance"]).optional().describe(
    "Content type.",
  ),
});

const GetAccessRequestsItemSchema = z.object({
  id: z.string().describe("The Snyk ID of the access request."),
  type: z.string().optional(),
  status: z.enum(["pending", "expired"]),
});

const GetAccessRequestsSchema = z.object({
  items: z.array(GetAccessRequestsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetUserInstalledAppsItemSchema = z.object({
  id: z.string(),
  type: z.string().optional(),
  client_id: z.string().describe("The oauth2 client id for the app."),
  context: z.enum(["tenant", "user"]).optional().describe(
    "Allow installing the app to a org/group or to a user, default tenant.",
  ),
  name: z.string().min(1).describe(
    "New name of the app to display to users during authorization flow.",
  ),
  scopes: z.array(z.string().min(1)).optional().describe(
    "The scopes this app is allowed to request during authorization.",
  ),
});

const GetUserInstalledAppsSchema = z.object({
  items: z.array(GetUserInstalledAppsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetAppInstallsForUserItemSchema = z.object({
  id: z.string(),
  type: z.string().optional(),
  client_id: z.string().optional().describe(
    "The OAuth2 client id for the app installation. Only provided for installations of non-interactive...",
  ),
  installed_at: z.string().optional().describe(
    "Timestamp at which this app was first installed at.",
  ),
  app_id: z.string().optional().describe("Related app ID"),
});

const GetAppInstallsForUserSchema = z.object({
  items: z.array(GetAppInstallsForUserItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetUserAppSessionsItemSchema = z.object({
  id: z.string(),
  type: z.string().optional(),
  created_at: z.string(),
});

const GetUserAppSessionsSchema = z.object({
  items: z.array(GetUserAppSessionsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const PersonalAccessTokenItemSchema = z.object({
  id: z.string().describe("The personal access token id"),
  type: z.enum(["personal_access_token"]).optional().describe(
    "Type of the resource.",
  ),
  created_at: z.string().describe("Date/Time when the token was created"),
  expires_at: z.string().describe("Date/Time when the token expires"),
  label: z.string().describe(
    "A human-friendly name for the personal access token",
  ),
  tenant_id: z.string().nullable().optional().describe(
    "Identifier of the tenant to which the Personal Access Token is scoped.",
  ),
});

const ListPersonalAccessTokenSchema = z.object({
  items: z.array(PersonalAccessTokenItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

// =============================================================================
// Model Definition
// =============================================================================

/** Snyk Self — current user context, org listing, and app management */
export const model = {
  type: "@webframp/snyk/self",
  version: "2026.07.19.1",
  globalArguments: GlobalArgsSchema,

  upgrades: [],

  resources: {
    "self": {
      description: "My User Details",
      schema: GetSelfSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "get_access_requests": {
      description: "Get access requests (Early Access)",
      schema: GetAccessRequestsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "get_user_installed_apps": {
      description: "Get a list of Snyk Apps that can act on your behalf",
      schema: GetUserInstalledAppsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "get_app_installs_for_user": {
      description: "Get a list of Snyk Apps installed for a user",
      schema: GetAppInstallsForUserSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "get_user_app_sessions": {
      description: "Get a list of active OAuth sessions by app ID",
      schema: GetUserAppSessionsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "personal_access_token": {
      description: "List personal access tokens",
      schema: ListPersonalAccessTokenSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
  },

  methods: {
    get_self: {
      description: "My User Details",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const result = await snykApi(
          apiToken,
          "GET",
          `/self`,
          version,
        );

        const handle = await context.writeResource("self", "latest", result);
        context.logger.info("Fetched self", {});
        return { dataHandles: [handle] };
      },
    },
    get_access_requests: {
      description: "Get access requests (Early Access)",
      arguments: z.object({
        org_id: z.string().optional().describe(
          "The IDs of the org to filter by",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await snykApiPaginated(
          apiToken,
          `/self/access_requests`,
          version,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "get_access_requests",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} get_access_requests", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    get_user_installed_apps: {
      description: "Get a list of Snyk Apps that can act on your behalf",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await snykApiPaginated(
          apiToken,
          `/self/apps`,
          version,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "get_user_installed_apps",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} get_user_installed_apps", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    get_app_installs_for_user: {
      description: "Get a list of Snyk Apps installed for a user",
      arguments: z.object({
        expand: z.string().optional().describe("Expand relationships."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await snykApiPaginated(
          apiToken,
          `/self/apps/installs`,
          version,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "get_app_installs_for_user",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} get_app_installs_for_user", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    delete_user_app_install_by_id: {
      description: "Revoke a Snyk App by install ID",
      arguments: z.object({
        install_id: z.string().describe("Install ID"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        await snykApi(
          apiToken,
          "DELETE",
          `/self/apps/installs/${args.install_id}`,
          version,
        );

        context.logger.info("Deleted resource {id}", { id: args.install_id });
        return { dataHandles: [] };
      },
    },
    delete_revoke_user_installed_app: {
      description: "Revoke a Snyk App by app ID",
      arguments: z.object({
        app_id: z.string().describe("App ID"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        await snykApi(
          apiToken,
          "DELETE",
          `/self/apps/${args.app_id}`,
          version,
        );

        context.logger.info("Deleted resource {id}", { id: args.app_id });
        return { dataHandles: [] };
      },
    },
    get_user_app_sessions: {
      description: "Get a list of active OAuth sessions by app ID",
      arguments: z.object({
        app_id: z.string().describe("App ID"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>(["app_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await snykApiPaginated(
          apiToken,
          `/self/apps/${args.app_id}/sessions`,
          version,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "get_user_app_sessions",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} get_user_app_sessions", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    delete_revoke_user_app_session: {
      description: "Revoke the Snyk App session of an active user",
      arguments: z.object({
        app_id: z.string().describe("App ID"),
        session_id: z.string().describe("Session ID"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        await snykApi(
          apiToken,
          "DELETE",
          `/self/apps/${args.app_id}/sessions/${args.session_id}`,
          version,
        );

        context.logger.info("Deleted resource {id}", { id: args.session_id });
        return { dataHandles: [] };
      },
    },
    list_personal_access_token: {
      description: "List personal access tokens",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await snykApiPaginated(
          apiToken,
          `/self/personal_access_tokens`,
          version,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "personal_access_token",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} personal_access_token", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    delete_personal_access_token: {
      description: "Deletes a personal access token",
      arguments: z.object({
        personal_access_token_id: z.string().describe(
          "The personal access token id",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        await snykApi(
          apiToken,
          "DELETE",
          `/self/personal_access_tokens/${args.personal_access_token_id}`,
          version,
        );

        context.logger.info("Deleted resource {id}", {
          id: args.personal_access_token_id,
        });
        return { dataHandles: [] };
      },
    },
  },
};
