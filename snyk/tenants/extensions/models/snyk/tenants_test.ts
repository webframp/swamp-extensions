// Snyk Tenants — tenant and organization lifecycle management — Tests
// Auto-generated by scripts/snyk-codegen — do not edit manually.
// SPDX-License-Identifier: Apache-2.0

import {
  assertEquals,
  assertExists,
  assertStringIncludes,
} from "jsr:@std/assert@1.0.19";
import { createModelTestContext } from "@systeminit/swamp-testing";
import { model } from "./tenants.ts";

// ---------------------------------------------------------------------------
// Model Export Structure Tests
// ---------------------------------------------------------------------------

Deno.test("tenants model: has correct type", () => {
  assertEquals(model.type, "@webframp/snyk/tenants");
});

Deno.test("tenants model: has valid version format", () => {
  const versionPattern = /^\d{4}\.\d{2}\.\d{2}\.\d+$/;
  assertEquals(versionPattern.test(model.version), true);
});

Deno.test("tenants model: has globalArguments with apiToken", () => {
  assertExists(model.globalArguments);
  const shape = model.globalArguments.shape;
  assertExists(shape.apiToken);
  assertExists(shape.version);
});

Deno.test("tenants model: has expected methods", () => {
  assertExists(model.methods);
  assertExists(model.methods.list_tenants);
  assertExists(model.methods.get_tenant);
  assertExists(model.methods.update_tenant);
  assertExists(model.methods.get_broker_connection_integrations);
  assertExists(model.methods.create_broker_connection_integration);
  assertExists(model.methods.delete_broker_connection_integration);
  assertExists(model.methods.list_broker_deployments_for_tenant);
  assertExists(model.methods.list_connection_contexts);
  assertExists(model.methods.get_connection_context);
  assertExists(model.methods.update_broker_context);
  assertExists(model.methods.delete_broker_context);
  assertExists(model.methods.update_broker_context_integration);
  assertExists(model.methods.delete_broker_context_integration);
  assertExists(model.methods.list_broker_deployments);
  assertExists(model.methods.create_broker_deployment);
  assertExists(model.methods.update_broker_deployment);
  assertExists(model.methods.delete_broker_deployment);
  assertExists(model.methods.list_broker_connections);
  assertExists(model.methods.create_broker_connection);
  assertExists(model.methods.delete_broker_connections);
  assertExists(model.methods.get_broker_connection);
  assertExists(model.methods.update_broker_connection);
  assertExists(model.methods.delete_broker_connection);
  assertExists(model.methods.list_broker_orgs_for_bulk_migration);
  assertExists(model.methods.create_broker_orgs_for_bulk_migration);
  assertExists(model.methods.list_deployment_contexts);
  assertExists(model.methods.create_broker_context);
  assertExists(model.methods.list_deployment_credentials);
  assertExists(model.methods.create_deployment_credential);
  assertExists(model.methods.get_deployment_credential);
  assertExists(model.methods.update_deployment_credential);
  assertExists(model.methods.delete_deployment_credential);
  assertExists(model.methods.list_assets_tenant);
  assertExists(model.methods.update_assets_bulk_tenant);
  assertExists(model.methods.get_filter_fields_tenant);
  assertExists(model.methods.get_filter_values_tenant);
  assertExists(model.methods.get_group_fields_tenant);
  assertExists(model.methods.get_group_values_tenant);
  assertExists(model.methods.create_asset_search_tenant);
  assertExists(model.methods.get_asset_search_results_tenant);
  assertExists(model.methods.get_asset_tenant);
  assertExists(model.methods.update_asset_tenant);
  assertExists(model.methods.list_asset_projects_tenant);
  assertExists(model.methods.list_asset_targets_tenant);
  assertExists(model.methods.get_tenant_memberships);
  assertExists(model.methods.update_tenant_membership);
  assertExists(model.methods.delete_tenant_membership);
  assertExists(model.methods.list_tenant_roles);
  assertExists(model.methods.create_tenant_role);
  assertExists(model.methods.get_tenant_role);
  assertExists(model.methods.update_tenant_role);
  assertExists(model.methods.delete_tenant_role);
});

Deno.test("tenants model: has expected resources", () => {
  assertExists(model.resources);
  assertExists(model.resources["tenants"]);
  assertExists(model.resources["tenant"]);
  assertExists(model.resources["get_broker_connection_integrations"]);
  assertExists(model.resources["broker_connection_integration"]);
  assertExists(model.resources["broker_deployments_for_tenant"]);
  assertExists(model.resources["connection_contexts"]);
  assertExists(model.resources["connection_context"]);
  assertExists(model.resources["broker_context"]);
  assertExists(model.resources["broker_context_integration"]);
  assertExists(model.resources["broker_deployments"]);
  assertExists(model.resources["broker_deployment"]);
  assertExists(model.resources["broker_connections"]);
  assertExists(model.resources["broker_connection"]);
  assertExists(model.resources["broker_orgs_for_bulk_migration"]);
  assertExists(model.resources["deployment_contexts"]);
  assertExists(model.resources["deployment_credentials"]);
  assertExists(model.resources["deployment_credential"]);
  assertExists(model.resources["assets_tenant"]);
  assertExists(model.resources["assets_bulk_tenant"]);
  assertExists(model.resources["get_filter_fields_tenant"]);
  assertExists(model.resources["get_filter_values_tenant"]);
  assertExists(model.resources["get_group_fields_tenant"]);
  assertExists(model.resources["get_group_values_tenant"]);
  assertExists(model.resources["asset_search_tenant"]);
  assertExists(model.resources["get_asset_search_results_tenant"]);
  assertExists(model.resources["asset_tenant"]);
  assertExists(model.resources["asset_projects_tenant"]);
  assertExists(model.resources["asset_targets_tenant"]);
  assertExists(model.resources["get_tenant_memberships"]);
  assertExists(model.resources["tenant_membership"]);
  assertExists(model.resources["tenant_roles"]);
  assertExists(model.resources["tenant_role"]);
});

// ---------------------------------------------------------------------------
// Mock Snyk API Server
// ---------------------------------------------------------------------------

interface CapturedRequest {
  method: string;
  path: string;
  search: string;
  headers: Record<string, string>;
  body: unknown;
}

function startMockSnykServer(
  responses: Record<string, { data: unknown; isCollection?: boolean }>,
): { url: string; server: Deno.HttpServer; requests: CapturedRequest[] } {
  const requests: CapturedRequest[] = [];
  const server = Deno.serve({ port: 0, onListen() {} }, async (req) => {
    const url = new URL(req.url);
    const path = url.pathname;

    // Capture the request (headers, method, path, query, parsed body).
    let reqBody: unknown = null;
    if (req.body) {
      const text = await req.text();
      if (text) {
        try {
          reqBody = JSON.parse(text);
        } catch {
          reqBody = text;
        }
      }
    }
    requests.push({
      method: req.method,
      path,
      search: url.search,
      headers: Object.fromEntries(req.headers),
      body: reqBody,
    });

    for (const [pattern, { data, isCollection }] of Object.entries(responses)) {
      if (path.includes(pattern)) {
        const body = isCollection
          ? {
            data: Array.isArray(data) ? data : [data],
            jsonapi: { version: "1.0" },
            links: { self: path, next: null },
          }
          : {
            data,
            jsonapi: { version: "1.0" },
            links: { self: path },
          };
        return Response.json(body, {
          headers: { "content-type": "application/vnd.api+json" },
        });
      }
    }

    return new Response(
      JSON.stringify({
        jsonapi: { version: "1.0" },
        errors: [{ status: "404", detail: "Not found" }],
      }),
      { status: 404, headers: { "content-type": "application/vnd.api+json" } },
    );
  });

  const addr = server.addr as Deno.NetAddr;
  return { url: `http://localhost:${addr.port}`, server, requests };
}

function installFetchMock(mockUrl: string): () => void {
  const originalFetch = globalThis.fetch;
  globalThis.fetch = (input, init) => {
    const reqUrl = typeof input === "string" ? input : input.toString();
    const newUrl = reqUrl.replace("https://api.snyk.io/rest", mockUrl);
    return originalFetch(newUrl, init);
  };
  return () => {
    globalThis.fetch = originalFetch;
  };
}

Deno.test({
  name: "tenants model: list_tenants fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockItem = {
      "id": "fixture-123",
      "type": "resource",
      "attributes": {
        "created_at": "2022-03-16T00:00:00Z",
        "name": "My Tenant",
        "slug": "my-tenant",
        "updated_at": "2022-03-16T00:00:00Z",
        "owner_id": "test-value",
      },
    };
    const { url, server, requests } = startMockSnykServer({
      "/tenants": { data: [mockItem], isCollection: true },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "version": "2024-10-15" },
        definition: {
          id: "test-id",
          name: "test-tenants",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).list_tenants.execute({}, context);
      assertEquals(result.dataHandles.length, 1);

      assertEquals(requests.length, 1);
      const req0 = requests[0];
      assertEquals(req0.method, "GET");
      assertStringIncludes(req0.path, "/tenants");
      assertStringIncludes(req0.search, "version=");
      assertEquals(req0.headers["authorization"], "token test-token");

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
      assertEquals(resources[0].specName, "tenants");
      const data = resources[0].data as {
        items: unknown[];
        truncated: boolean;
      };
      assertEquals(Array.isArray(data.items), true);
      assertEquals(data.items.length, 1);
      assertEquals(typeof data.truncated, "boolean");
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "tenants model: get_tenant fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "id": "fixture-123",
      "type": "resource",
      "attributes": {
        "created_at": "2022-03-16T00:00:00Z",
        "name": "My Tenant",
        "slug": "my-tenant",
        "updated_at": "2022-03-16T00:00:00Z",
        "owner_id": "test-value",
      },
    };
    const { url, server, requests } = startMockSnykServer({
      "/tenants/test-id-123": { data: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "version": "2024-10-15" },
        definition: {
          id: "test-id",
          name: "test-tenants",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).get_tenant.execute({ "tenant_id": "test-id-123" }, context);
      assertEquals(result.dataHandles.length, 1);

      assertEquals(requests.length, 1);
      const req0 = requests[0];
      assertEquals(req0.method, "GET");
      assertStringIncludes(req0.path, "/tenants/test-id-123");
      assertStringIncludes(req0.search, "version=");
      assertEquals(req0.headers["authorization"], "token test-token");

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
      assertEquals(resources[0].specName, "tenant");
      assertEquals(resources[0].name, "test-id-123");
      const data = resources[0].data as Record<string, unknown>;
      assertEquals(data.id, "fixture-123");
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "tenants model: update_tenant executes and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "id": "fixture-123",
      "type": "resource",
      "attributes": {
        "created_at": "2022-03-16T00:00:00Z",
        "name": "My Tenant",
        "slug": "my-tenant",
        "updated_at": "2022-03-16T00:00:00Z",
      },
    };
    const { url, server, requests } = startMockSnykServer({
      "/tenants/test-id-123": { data: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "version": "2024-10-15" },
        definition: {
          id: "test-id",
          name: "test-tenants",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).update_tenant.execute({
        "tenant_id": "test-id-123",
        "name": "test-resource",
      }, context);
      assertEquals(result.dataHandles.length, 1);

      assertEquals(requests.length, 1);
      const req0 = requests[0];
      assertEquals(req0.method, "PATCH");
      assertStringIncludes(req0.path, "/tenants/test-id-123");
      assertStringIncludes(req0.search, "version=");
      assertEquals(req0.headers["authorization"], "token test-token");
      assertEquals(req0.headers["content-type"], "application/vnd.api+json");
      assertExists(req0.body);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
      assertEquals(resources[0].specName, "tenant");
      assertEquals(resources[0].name, "test-id-123");
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name:
    "tenants model: create_broker_connection_integration creates and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = { "id": "new-123", "type": "resource", "attributes": {} };
    const { url, server, requests } = startMockSnykServer({
      "/tenants/test-id-123/brokers/connections/test-id-123/orgs/test-id-123/integration":
        { data: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "version": "2024-10-15" },
        definition: {
          id: "test-id",
          name: "test-tenants",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).create_broker_connection_integration.execute({
        "tenant_id": "test-id-123",
        "connection_id": "test-id-123",
        "org_id": "test-id-123",
        "name": "test-resource",
      }, context);
      assertEquals(result.dataHandles.length, 1);

      assertEquals(requests.length, 1);
      const req0 = requests[0];
      assertEquals(req0.method, "POST");
      assertStringIncludes(
        req0.path,
        "/tenants/test-id-123/brokers/connections/test-id-123/orgs/test-id-123/integration",
      );
      assertStringIncludes(req0.search, "version=");
      assertEquals(req0.headers["authorization"], "token test-token");
      assertEquals(req0.headers["content-type"], "application/vnd.api+json");
      assertExists(req0.body);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
      assertEquals(resources[0].specName, "broker_connection_integration");
      assertEquals(resources[0].name, "new-123");
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name:
    "tenants model: delete_broker_connection_integration executes successfully",
  sanitizeResources: false,
  fn: async () => {
    const { url, server, requests } = startMockSnykServer({
      "/tenants/test-id-123/brokers/connections/test-id-123/orgs/test-id-123/integrations/test-id-123":
        { data: { id: "test-id-123", type: "resource" } },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "version": "2024-10-15" },
        definition: {
          id: "test-id",
          name: "test-tenants",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).delete_broker_connection_integration.execute({
        "tenant_id": "test-id-123",
        "connection_id": "test-id-123",
        "org_id": "test-id-123",
        "integration_id": "test-id-123",
      }, context);
      assertEquals(result.dataHandles.length, 0);

      assertEquals(requests.length, 1);
      const req0 = requests[0];
      assertEquals(req0.method, "DELETE");
      assertStringIncludes(
        req0.path,
        "/tenants/test-id-123/brokers/connections/test-id-123/orgs/test-id-123/integrations/test-id-123",
      );
      assertStringIncludes(req0.search, "version=");
      assertEquals(req0.headers["authorization"], "token test-token");

      const resources = getWrittenResources();
      assertEquals(resources.length, 0);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "tenants model: list_tenants surfaces API errors",
  sanitizeResources: false,
  fn: async () => {
    // A server that returns 500 for every request, exercising the error path.
    const originalFetch = globalThis.fetch;
    const server = Deno.serve({ port: 0, onListen() {} }, () =>
      new Response(
        JSON.stringify({
          jsonapi: { version: "1.0" },
          errors: [{ status: "500", detail: "Internal Server Error" }],
        }),
        {
          status: 500,
          headers: { "content-type": "application/vnd.api+json" },
        },
      ));
    const addr = server.addr as Deno.NetAddr;
    const mockUrl = `http://localhost:${addr.port}`;
    globalThis.fetch = (input, init) => {
      const reqUrl = typeof input === "string" ? input : input.toString();
      return originalFetch(
        reqUrl.replace("https://api.snyk.io/rest", mockUrl),
        init,
      );
    };

    try {
      const { context } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "version": "2024-10-15" },
        definition: {
          id: "test-id",
          name: "test-tenants",
          version: 1,
          tags: {},
        },
      });

      let threw = false;
      try {
        await (model.methods as Record<
          string,
          {
            execute: (
              args: Record<string, unknown>,
              ctx: unknown,
            ) => Promise<{ dataHandles: unknown[] }>;
          }
        >).list_tenants.execute({}, context);
      } catch (_err) {
        threw = true;
      }
      assertEquals(threw, true);
    } finally {
      globalThis.fetch = originalFetch;
      await server.shutdown();
    }
  },
});
