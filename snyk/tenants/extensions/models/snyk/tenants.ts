/**
 * Snyk Tenants — tenant and organization lifecycle management
 *
 * Auto-generated by scripts/snyk-codegen — do not edit manually.
 *
 * @module
 */
// SPDX-License-Identifier: AGPL-3.0-or-later WITH Swamp-Extension-Exception

import { z } from "npm:zod@4.4.3";
import { snykApi, snykApiPaginated } from "./_lib/api.ts";

// =============================================================================
// Schemas
// =============================================================================

const GlobalArgsSchema = z.object({
  apiToken: z.string().meta({ sensitive: true }).describe("Snyk API token"),
  version: z.string().default("2024-10-15").describe("Snyk API version date"),
});

const TenantsItemSchema = z.object({
  id: z.string().describe("The Snyk ID of the tenant."),
  type: z.string().optional(),
  created_at: z.string().describe("The time the tenant was created."),
  name: z.string().describe("The display name of the tenant."),
  slug: z.string().describe(
    "The canonical (unique and URL-friendly) name of the tenant.",
  ),
  updated_at: z.string().describe("The time the tenant was last modified."),
  owner_id: z.string().optional().describe("Related owner ID"),
});

const ListTenantsSchema = z.object({
  items: z.array(TenantsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetTenantSchema = z.object({
  id: z.string().describe("The Snyk ID of the tenant."),
  type: z.string().optional(),
  created_at: z.string().describe("The time the tenant was created."),
  name: z.string().describe("The display name of the tenant."),
  slug: z.string().describe(
    "The canonical (unique and URL-friendly) name of the tenant.",
  ),
  updated_at: z.string().describe("The time the tenant was last modified."),
  owner_id: z.string().optional().describe("Related owner ID"),
});

const GetBrokerConnectionIntegrationsItemSchema = z.object({
  id: z.string(),
  type: z.string().optional(),
});

const GetBrokerConnectionIntegrationsSchema = z.object({
  items: z.array(GetBrokerConnectionIntegrationsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateBrokerConnectionIntegrationSchema = z.object({
  id: z.string(),
  type: z.string().optional(),
});

const BrokerDeploymentsForTenantItemSchema = z.object({
  id: z.string(),
  type: z.enum(["broker_deployment"]).optional(),
  broker_app_installed_in_org_id: z.string().describe(
    "Org Id in which the Broker App is installed",
  ),
  install_id: z.string().optional().describe("Associated Install ID"),
  metadata: z.object({}).describe(
    "Metadata information such user/org id or metrics",
  ),
});

const ListBrokerDeploymentsForTenantSchema = z.object({
  items: z.array(BrokerDeploymentsForTenantItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const ConnectionContextsItemSchema = z.object({
  id: z.string(),
  type: z.enum(["broker_context"]).optional(),
  context: z.record(z.string(), z.string()),
  broker_connections_id: z.string().optional().describe(
    "Related broker_connections ID",
  ),
  jsonapi_id: z.string().optional().describe("Related jsonapi ID"),
  links_id: z.string().optional().describe("Related links ID"),
  applied_integrations_id: z.string().optional().describe(
    "Related applied_integrations ID",
  ),
});

const ListConnectionContextsSchema = z.object({
  items: z.array(ConnectionContextsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetConnectionContextSchema = z.object({
  id: z.string(),
  type: z.enum(["broker_context"]).optional(),
  context: z.record(z.string(), z.string()),
  broker_connections_id: z.string().optional().describe(
    "Related broker_connections ID",
  ),
  jsonapi_id: z.string().optional().describe("Related jsonapi ID"),
  links_id: z.string().optional().describe("Related links ID"),
  applied_integrations_id: z.string().optional().describe(
    "Related applied_integrations ID",
  ),
});

const UpdateBrokerContextSchema = z.object({
  id: z.string(),
  type: z.enum(["broker_context"]).optional(),
  context: z.record(z.string(), z.string()),
  broker_connections_id: z.string().optional().describe(
    "Related broker_connections ID",
  ),
  jsonapi_id: z.string().optional().describe("Related jsonapi ID"),
  links_id: z.string().optional().describe("Related links ID"),
  applied_integrations_id: z.string().optional().describe(
    "Related applied_integrations ID",
  ),
});

const UpdateBrokerContextIntegrationSchema = z.object({
  id: z.string(),
  type: z.enum(["broker_context"]).optional(),
  integrations_relationships_id: z.string().optional().describe(
    "Related integrations_relationships ID",
  ),
  jsonapi_id: z.string().optional().describe("Related jsonapi ID"),
  links_id: z.string().optional().describe("Related links ID"),
});

const BrokerDeploymentsItemSchema = z.object({
  id: z.string(),
  type: z.enum(["broker_deployment"]).optional(),
  broker_app_installed_in_org_id: z.string().describe(
    "Org Id in which the Broker App is installed",
  ),
  install_id: z.string().optional().describe("Associated Install ID"),
  metadata: z.object({}).describe(
    "Metadata information such user/org id or metrics",
  ),
});

const ListBrokerDeploymentsSchema = z.object({
  items: z.array(BrokerDeploymentsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateBrokerDeploymentSchema = z.object({
  id: z.string(),
  type: z.enum(["broker_deployment"]).optional(),
  broker_app_installed_in_org_id: z.string().describe(
    "Org Id in which the Broker App is installed",
  ),
  install_id: z.string().optional().describe("Associated Install ID"),
  metadata: z.object({}).describe(
    "Metadata information such user/org id or metrics",
  ),
});

const BrokerConnectionsItemSchema = z.object({
  id: z.string(),
  type: z.enum(["broker_connection"]).optional(),
  deployment_id: z.string().describe("Associated Deployment ID"),
  identifier: z.string().optional().describe("Broker identifier"),
  name: z.string().describe("Associated name"),
  secrets: z.object({
    primary: z.unknown(),
    secondary: z.unknown(),
  }).optional(),
  configuration: z.object({
    required: z.record(z.string(), z.string()),
    type: z.string(),
    validations: z.array(z.object({})).optional(),
  }),
});

const ListBrokerConnectionsSchema = z.object({
  items: z.array(BrokerConnectionsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateBrokerConnectionSchema = z.object({
  id: z.string(),
  type: z.enum(["broker_connection"]).optional(),
  deployment_id: z.string().describe("Associated Deployment ID"),
  identifier: z.string().optional().describe("Broker identifier"),
  name: z.string().describe("Associated name"),
  secrets: z.object({
    primary: z.unknown(),
    secondary: z.unknown(),
  }).optional(),
  configuration: z.object({
    required: z.record(z.string(), z.string()),
    type: z.string(),
    validations: z.array(z.object({})).optional(),
  }),
});

const BrokerOrgsForBulkMigrationItemSchema = z.object({
  id: z.string().describe(
    "The organization ID associated with the bulk migration.",
  ),
  type: z.enum(["broker_organization"]).optional(),
});

const ListBrokerOrgsForBulkMigrationSchema = z.object({
  items: z.array(BrokerOrgsForBulkMigrationItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const DeploymentContextsItemSchema = z.object({
  id: z.string(),
  type: z.enum(["broker_context"]).optional(),
  context: z.record(z.string(), z.string()),
  broker_connections_id: z.string().optional().describe(
    "Related broker_connections ID",
  ),
  jsonapi_id: z.string().optional().describe("Related jsonapi ID"),
  links_id: z.string().optional().describe("Related links ID"),
  applied_integrations_id: z.string().optional().describe(
    "Related applied_integrations ID",
  ),
});

const ListDeploymentContextsSchema = z.object({
  items: z.array(DeploymentContextsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const DeploymentCredentialsItemSchema = z.object({
  id: z.string(),
  type: z.string().describe("Associated connection type"),
  comment: z.string().optional().describe("Optional comment information"),
  deployment_id: z.string().describe("Associated Deployment ID"),
  environment_variable_name: z.string(),
  broker_connections_id: z.string().optional().describe(
    "Related broker_connections ID",
  ),
  jsonapi_id: z.string().optional().describe("Related jsonapi ID"),
  links_id: z.string().optional().describe("Related links ID"),
});

const ListDeploymentCredentialsSchema = z.object({
  items: z.array(DeploymentCredentialsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateDeploymentCredentialSchema = z.object({
  id: z.string(),
  type: z.string().describe("Associated connection type"),
  comment: z.string().optional().describe("Optional comment information"),
  deployment_id: z.string().describe("Associated Deployment ID"),
  environment_variable_name: z.string(),
  broker_connections_id: z.string().optional().describe(
    "Related broker_connections ID",
  ),
  jsonapi_id: z.string().optional().describe("Related jsonapi ID"),
  links_id: z.string().optional().describe("Related links ID"),
});

const AssetsTenantItemSchema = z.object({
  id: z.string().describe("The unique identifier of the asset"),
  type: z.enum(["container_images", "sboms", "repositories"]).optional()
    .describe("The JSON:API resource type"),
  projects_id: z.string().optional().describe("Related projects ID"),
  targets_id: z.string().optional().describe("Related targets ID"),
});

const ListAssetsTenantSchema = z.object({
  items: z.array(AssetsTenantItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetFilterFieldsTenantItemSchema = z.object({
  id: z.string().describe("UUID identifier for the filter field"),
  type: z.enum(["string", "boolean", "datetime", "array", "object"]).describe(
    "Data type of the field (string, boolean, datetime, array, object)",
  ),
  asset_types: z.array(z.enum(["container_images", "sboms", "repositories"]))
    .describe("List of asset types this field applies to"),
  name: z.string().describe(
    'The field name used in RSQL filters (e.g., "class", "registry")',
  ),
  values_id: z.string().optional().describe("Related values ID"),
});

const GetFilterFieldsTenantSchema = z.object({
  items: z.array(GetFilterFieldsTenantItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetFilterValuesTenantItemSchema = z.object({
  id: z.string().describe("UUID identifier for the filter value"),
  type: z.enum(["filter_values"]).optional().describe(
    "The JSON:API resource type",
  ),
  key: z.string().optional().describe(
    "For object-type filters (e.g., tags), the key name. Only present for object-type filter values.",
  ),
  value: z.string().optional().describe(
    "The filter value. For simple filters, this is the value itself. For object-type filters (e.g., ta...",
  ),
});

const GetFilterValuesTenantSchema = z.object({
  items: z.array(GetFilterValuesTenantItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetGroupFieldsTenantItemSchema = z.object({
  id: z.string().describe("UUID identifier for the group field"),
  type: z.enum(["group_fields"]).optional().describe(
    "The JSON:API resource type",
  ),
  asset_types: z.array(z.string()).describe(
    "List of asset types this field applies to",
  ),
  name: z.string().describe(
    'The field name used for grouping (e.g., "class", "registry")',
  ),
  values_id: z.string().optional().describe("Related values ID"),
});

const GetGroupFieldsTenantSchema = z.object({
  items: z.array(GetGroupFieldsTenantItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetGroupValuesTenantItemSchema = z.object({
  id: z.string().describe("UUID identifier for the group value"),
  type: z.enum(["group_values"]).optional().describe(
    "The JSON:API resource type",
  ),
  value: z.string().describe("The grouped value"),
});

const GetGroupValuesTenantSchema = z.object({
  items: z.array(GetGroupValuesTenantItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetAssetSearchResultsTenantItemSchema = z.object({
  id: z.string().describe("The unique identifier of the asset"),
  type: z.enum(["container_images", "sboms", "repositories"]).optional()
    .describe("The JSON:API resource type"),
  projects_id: z.string().optional().describe("Related projects ID"),
  targets_id: z.string().optional().describe("Related targets ID"),
});

const GetAssetSearchResultsTenantSchema = z.object({
  items: z.array(GetAssetSearchResultsTenantItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetAssetTenantSchema = z.object({
  id: z.string().describe("The unique identifier of the asset"),
  type: z.enum(["container_images", "sboms", "repositories"]).optional()
    .describe("The JSON:API resource type"),
  projects_id: z.string().optional().describe("Related projects ID"),
  targets_id: z.string().optional().describe("Related targets ID"),
});

const AssetProjectsTenantItemSchema = z.object({
  id: z.string().describe("The unique identifier of the project"),
  type: z.enum(["projects"]).optional().describe("The JSON:API resource type"),
  base_image_remediation: z.object({
    base_image: z.unknown().optional(),
    base_image_name: z.string(),
    base_image_outdated: z.boolean().optional(),
    code: z.string(),
    distro_alert: z.string().optional(),
    proposed_base_images: z.unknown().optional(),
  }).optional().describe(
    "Base image upgrade recommendation data from container scanning",
  ),
  is_canonical: z.boolean().optional().describe(
    "Indicates whether this project is the canonical project for its target_file. Projects are grouped...",
  ),
  issues: z.object({
    critical: z.number().int(),
    high: z.number().int(),
    low: z.number().int(),
    medium: z.number().int(),
  }).optional().describe("Issue counts by severity"),
  monitor_created_at: z.string().optional().describe(
    "Timestamp when the monitor was created",
  ),
  monitor_id: z.string().optional().describe(
    "The ID of the monitor associated with this project",
  ),
  org_id: z.string().optional().describe(
    "The ID of the organization that owns this project",
  ),
  project_type: z.enum([
    "rubygems",
    "composer",
    "sbt",
    "maven",
    "gomodules",
    "poetry",
    "cocoapods",
    "pipenv",
    "pnpm",
    "pub",
    "yarn",
    "yarn-workspace",
    "paket",
    "pip",
    "unmanaged",
    "gradle",
    "nuget",
    "cpp",
    "npm",
    "conan",
    "govendor",
    "hex",
    "golangdep",
    "cargo",
    "swift",
    "golang",
    "secrets",
    "iac",
    "uv",
    "sast",
    "armconfig",
    "helmconfig",
    "terraformconfig",
    "cloudformationconfig",
    "k8sconfig",
    "cloudconfig",
    "dockerfile",
    "deb",
    "apk",
    "linux",
    "rpm",
  ]).optional().describe("Type of the project (e.g., npm, maven)"),
  risk_score: z.number().int().optional().describe(
    "The calculated risk score for this project",
  ),
  snapshot_created_at: z.string().optional().describe(
    "Timestamp when the snapshot was created",
  ),
  snapshot_id: z.string().optional().describe("The ID of the snapshot"),
  target_file: z.string().optional().describe("Path to the manifest file"),
  target_id: z.string().optional().describe(
    "The ID of the target this project belongs to",
  ),
  target_origin: z.string().optional().describe(
    "Origin of the target (e.g., github, gitlab)",
  ),
  target_ref: z.string().optional().describe(
    "The target reference (e.g., branch name)",
  ),
});

const ListAssetProjectsTenantSchema = z.object({
  items: z.array(AssetProjectsTenantItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const AssetTargetsTenantItemSchema = z.object({
  id: z.string().describe("The unique identifier of the target"),
  type: z.enum(["target"]).optional().describe("The JSON:API resource type"),
  imported_at: z.string().optional().describe(
    "Timestamp when the target was imported",
  ),
  integration_id: z.string().optional().describe(
    "The ID of the integration associated with this target",
  ),
  target_file: z.string().optional().describe("Path to the manifest file"),
  target_origin: z.string().optional().describe(
    "Origin of the target (e.g., github, gitlab)",
  ),
  target_ref: z.string().optional().describe(
    "The target reference (e.g., branch name)",
  ),
});

const ListAssetTargetsTenantSchema = z.object({
  items: z.array(AssetTargetsTenantItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetTenantMembershipsItemSchema = z.object({
  id: z.string().describe("Unique identifier for a tenant membership."),
  type: z.enum(["tenant_membership"]).optional().describe(
    "Resource type for tenant memberships.",
  ),
  created_at: z.string().describe(
    "The time at which the resource was created.",
  ),
  role_id: z.string().optional().describe("Related role ID"),
  tenant_id: z.string().optional().describe("Related tenant ID"),
  user_id: z.string().optional().describe("Related user ID"),
});

const GetTenantMembershipsSchema = z.object({
  items: z.array(GetTenantMembershipsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const TenantRolesItemSchema = z.object({
  id: z.string(),
  type: z.enum(["tenant_role"]).optional().describe(
    "The type of the resource for tenant role operations",
  ),
  custom: z.boolean(),
  description: z.string(),
  name: z.string(),
  normalized_name: z.string().optional(),
  permissions: z.array(z.string()),
  tenant_id: z.string().optional().describe("Related tenant ID"),
});

const ListTenantRolesSchema = z.object({
  items: z.array(TenantRolesItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateTenantRoleSchema = z.object({
  id: z.string(),
  type: z.enum(["tenant_role"]).optional().describe(
    "The type of the resource for tenant role operations",
  ),
  custom: z.boolean(),
  description: z.string(),
  name: z.string(),
  normalized_name: z.string().optional(),
  permissions: z.array(z.string()),
  tenant_id: z.string().optional().describe("Related tenant ID"),
});

// =============================================================================
// Model Definition
// =============================================================================

/** Snyk Tenants — tenant and organization lifecycle management */
export const model = {
  type: "@webframp/snyk/tenants",
  version: "2026.07.19.1",
  globalArguments: GlobalArgsSchema,

  upgrades: [],

  resources: {
    "tenants": {
      description: "Get a list of all accessible Tenants",
      schema: ListTenantsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "tenant": {
      description: "Get a single Tenant by ID",
      schema: GetTenantSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "get_broker_connection_integrations": {
      description: "Get Integrations using the current Broker connection",
      schema: GetBrokerConnectionIntegrationsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "broker_connection_integration": {
      description: "Creates Broker connection Integration Configuration",
      schema: CreateBrokerConnectionIntegrationSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "broker_deployments_for_tenant": {
      description: "List Broker deployments for tenant",
      schema: ListBrokerDeploymentsForTenantSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "connection_contexts": {
      description: "List Connection contexts",
      schema: ListConnectionContextsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "connection_context": {
      description: "List Connection context",
      schema: GetConnectionContextSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "broker_context": {
      description: "Updates Broker Context",
      schema: UpdateBrokerContextSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "broker_context_integration": {
      description:
        "Updates an integration to be associated with a Broker context",
      schema: UpdateBrokerContextIntegrationSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "broker_deployments": {
      description: "List Broker deployments",
      schema: ListBrokerDeploymentsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "broker_deployment": {
      description: "Creates Broker Deployment",
      schema: CreateBrokerDeploymentSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "broker_connections": {
      description: "List Broker connections",
      schema: ListBrokerConnectionsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "broker_connection": {
      description: "Creates Broker connection",
      schema: CreateBrokerConnectionSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "broker_orgs_for_bulk_migration": {
      description: "List organizations for bulk migration",
      schema: ListBrokerOrgsForBulkMigrationSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "deployment_contexts": {
      description: "List Deployment contexts",
      schema: ListDeploymentContextsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "deployment_credentials": {
      description: "List Deployment credentials",
      schema: ListDeploymentCredentialsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "deployment_credential": {
      description: "Create deployment credential",
      schema: CreateDeploymentCredentialSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "assets_tenant": {
      description: "List or search all assets (synchronous) (Early Access)",
      schema: ListAssetsTenantSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "assets_bulk_tenant": {
      description: "Bulk update asset attributes (Early Access)",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "get_filter_fields_tenant": {
      description: "Get available filter fields (Early Access)",
      schema: GetFilterFieldsTenantSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "get_filter_values_tenant": {
      description: "Get filter value suggestions (autocomplete) (Early Access)",
      schema: GetFilterValuesTenantSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "get_group_fields_tenant": {
      description: "Get available group fields (Early Access)",
      schema: GetGroupFieldsTenantSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "get_group_values_tenant": {
      description: "Get group value aggregation (Early Access)",
      schema: GetGroupValuesTenantSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "asset_search_tenant": {
      description: "Create an asset search (asynchronous) (Early Access)",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "get_asset_search_results_tenant": {
      description:
        "Retrieve asset search results (asynchronous) (Early Access)",
      schema: GetAssetSearchResultsTenantSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "asset_tenant": {
      description: "Get a single asset by ID (Early Access)",
      schema: GetAssetTenantSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "asset_projects_tenant": {
      description: "List projects for an asset (Early Access)",
      schema: ListAssetProjectsTenantSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "asset_targets_tenant": {
      description: "List targets for an asset (Early Access)",
      schema: ListAssetTargetsTenantSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "get_tenant_memberships": {
      description: "Get all memberships of the tenant (Early Access)",
      schema: GetTenantMembershipsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "tenant_membership": {
      description: "Update tenant membership (Early Access)",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "tenant_roles": {
      description: "List all available roles for a given tenant (Early Access)",
      schema: ListTenantRolesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "tenant_role": {
      description:
        "Create a custom tenant role for a given tenant (Early Access)",
      schema: CreateTenantRoleSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
  },

  methods: {
    list_tenants: {
      description: "Get a list of all accessible Tenants",
      arguments: z.object({
        name: z.string().optional().describe(
          "Only return tenants whose name contains this value.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await snykApiPaginated(
          apiToken,
          `/tenants`,
          version,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("tenants", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} tenants", { count: results.length });
        return { dataHandles: [handle] };
      },
    },
    get_tenant: {
      description: "Get a single Tenant by ID",
      arguments: z.object({
        tenant_id: z.string().describe("Unique identifier for tenant"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const result = await snykApi(
          apiToken,
          "GET",
          `/tenants/${args.tenant_id}`,
          version,
        );

        const handle = await context.writeResource(
          "tenant",
          String(args.tenant_id),
          result,
        );
        context.logger.info("Fetched tenant", {});
        return { dataHandles: [handle] };
      },
    },
    update_tenant: {
      description: "Update tenant",
      arguments: z.object({
        tenant_id: z.string().describe("Unique identifier for tenant"),
        data: z.object({
          attributes: z.unknown().optional(),
          id: z.string().optional(),
          type: z.unknown().optional(),
        }),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>(["tenant_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await snykApi(
          apiToken,
          "PATCH",
          `/tenants/${args.tenant_id}`,
          version,
          body,
        );

        const handle = await context.writeResource(
          "tenant",
          String(args.tenant_id),
          result,
        );
        context.logger.info("Updated tenant", {});
        return { dataHandles: [handle] };
      },
    },
    get_broker_connection_integrations: {
      description: "Get Integrations using the current Broker connection",
      arguments: z.object({
        tenant_id: z.string().describe("Tenant ID"),
        connection_id: z.string().describe("Connection ID"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>(["tenant_id", "connection_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await snykApiPaginated(
          apiToken,
          `/tenants/${args.tenant_id}/brokers/connections/${args.connection_id}/integrations`,
          version,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "get_broker_connection_integrations",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info(
          "Found {count} get_broker_connection_integrations",
          { count: results.length },
        );
        return { dataHandles: [handle] };
      },
    },
    create_broker_connection_integration: {
      description: "Creates Broker connection Integration Configuration",
      arguments: z.object({
        tenant_id: z.string().describe("Tenant ID"),
        connection_id: z.string().describe("Connection ID"),
        data: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>(["tenant_id", "connection_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await snykApi(
          apiToken,
          "POST",
          `/tenants/${args.tenant_id}/brokers/connections/${args.connection_id}/orgs/${args.org_id}/integration`,
          version,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "broker_connection_integration",
          id,
          result,
        );
        context.logger.info("Created broker_connection_integration {id}", {
          id,
        });
        return { dataHandles: [handle] };
      },
    },
    delete_broker_connection_integration: {
      description: "Deletes an Integration for a Broker connection",
      arguments: z.object({
        tenant_id: z.string().describe("Tenant ID"),
        connection_id: z.string().describe("Connection ID"),
        integration_id: z.string().describe("Integration ID"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        await snykApi(
          apiToken,
          "DELETE",
          `/tenants/${args.tenant_id}/brokers/connections/${args.connection_id}/orgs/${args.org_id}/integrations/${args.integration_id}`,
          version,
        );

        context.logger.info("Deleted resource {id}", {
          id: args.integration_id,
        });
        return { dataHandles: [] };
      },
    },
    list_broker_deployments_for_tenant: {
      description: "List Broker deployments for tenant",
      arguments: z.object({
        tenant_id: z.string().describe("Tenant ID"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>(["tenant_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await snykApiPaginated(
          apiToken,
          `/tenants/${args.tenant_id}/brokers/deployments`,
          version,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "broker_deployments_for_tenant",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} broker_deployments_for_tenant", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    list_connection_contexts: {
      description: "List Connection contexts",
      arguments: z.object({
        tenant_id: z.string().describe("Tenant ID"),
        install_id: z.string().describe("Install ID"),
        connection_id: z.string().describe("Connection ID"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>([
          "tenant_id",
          "install_id",
          "connection_id",
        ]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await snykApiPaginated(
          apiToken,
          `/tenants/${args.tenant_id}/brokers/installs/${args.install_id}/connections/${args.connection_id}/contexts`,
          version,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "connection_contexts",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} connection_contexts", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    get_connection_context: {
      description: "List Connection context",
      arguments: z.object({
        tenant_id: z.string().describe("Tenant ID"),
        install_id: z.string().describe("Install ID"),
        context_id: z.string().describe("Context ID"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const result = await snykApi(
          apiToken,
          "GET",
          `/tenants/${args.tenant_id}/brokers/installs/${args.install_id}/contexts/${args.context_id}`,
          version,
        );

        const handle = await context.writeResource(
          "connection_context",
          String(args.context_id),
          result,
        );
        context.logger.info("Fetched connection_context", {});
        return { dataHandles: [handle] };
      },
    },
    update_broker_context: {
      description: "Updates Broker Context",
      arguments: z.object({
        tenant_id: z.string().describe("Tenant ID"),
        install_id: z.string().describe("Install ID"),
        context_id: z.string().describe("Context ID"),
        data: z.object({
          attributes: z.unknown(),
          id: z.string(),
          type: z.enum(["broker_context"]),
        }),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>([
          "tenant_id",
          "install_id",
          "context_id",
        ]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await snykApi(
          apiToken,
          "PATCH",
          `/tenants/${args.tenant_id}/brokers/installs/${args.install_id}/contexts/${args.context_id}`,
          version,
          body,
        );

        const handle = await context.writeResource(
          "broker_context",
          String(args.context_id),
          result,
        );
        context.logger.info("Updated broker_context", {});
        return { dataHandles: [handle] };
      },
    },
    delete_broker_context: {
      description: "Deletes broker context",
      arguments: z.object({
        tenant_id: z.string().describe("Tenant ID"),
        install_id: z.string().describe("Install ID"),
        context_id: z.string().describe("Context ID"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        await snykApi(
          apiToken,
          "DELETE",
          `/tenants/${args.tenant_id}/brokers/installs/${args.install_id}/contexts/${args.context_id}`,
          version,
        );

        context.logger.info("Deleted resource {id}", { id: args.context_id });
        return { dataHandles: [] };
      },
    },
    update_broker_context_integration: {
      description:
        "Updates an integration to be associated with a Broker context",
      arguments: z.object({
        tenant_id: z.string().describe("Tenant ID"),
        install_id: z.string().describe("Install ID"),
        context_id: z.string().describe("Context ID"),
        data: z.object({
          attributes: z.object({
            org_id: z.string(),
          }),
          id: z.string(),
          type: z.enum(["broker_integration"]),
        }),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>([
          "tenant_id",
          "install_id",
          "context_id",
        ]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await snykApi(
          apiToken,
          "PATCH",
          `/tenants/${args.tenant_id}/brokers/installs/${args.install_id}/contexts/${args.context_id}/integration`,
          version,
          body,
        );

        const handle = await context.writeResource(
          "broker_context_integration",
          String(args.context_id),
          result,
        );
        context.logger.info("Updated broker_context_integration", {});
        return { dataHandles: [handle] };
      },
    },
    delete_broker_context_integration: {
      description: "Deletes the Broker context association with an Integration",
      arguments: z.object({
        tenant_id: z.string().describe("Tenant ID"),
        install_id: z.string().describe("Install ID"),
        context_id: z.string().describe("Context ID"),
        integration_id: z.string().describe("Integration ID"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        await snykApi(
          apiToken,
          "DELETE",
          `/tenants/${args.tenant_id}/brokers/installs/${args.install_id}/contexts/${args.context_id}/integrations/${args.integration_id}`,
          version,
        );

        context.logger.info("Deleted resource {id}", {
          id: args.integration_id,
        });
        return { dataHandles: [] };
      },
    },
    list_broker_deployments: {
      description: "List Broker deployments",
      arguments: z.object({
        tenant_id: z.string().describe("Tenant ID"),
        install_id: z.string().describe("Install ID"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>(["tenant_id", "install_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await snykApiPaginated(
          apiToken,
          `/tenants/${args.tenant_id}/brokers/installs/${args.install_id}/deployments`,
          version,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "broker_deployments",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} broker_deployments", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_broker_deployment: {
      description: "Creates Broker Deployment",
      arguments: z.object({
        tenant_id: z.string().describe("Tenant ID"),
        install_id: z.string().describe("Install ID"),
        data: z.object({
          attributes: z.unknown(),
          type: z.enum(["broker_deployment"]),
        }),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>(["tenant_id", "install_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await snykApi(
          apiToken,
          "POST",
          `/tenants/${args.tenant_id}/brokers/installs/${args.install_id}/deployments`,
          version,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "broker_deployment",
          id,
          result,
        );
        context.logger.info("Created broker_deployment {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    update_broker_deployment: {
      description: "Updates Broker deployment",
      arguments: z.object({
        tenant_id: z.string().describe("Tenant ID"),
        install_id: z.string().describe("Install ID"),
        deployment_id: z.string().describe("Deployment ID"),
        data: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>([
          "tenant_id",
          "install_id",
          "deployment_id",
        ]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await snykApi(
          apiToken,
          "PATCH",
          `/tenants/${args.tenant_id}/brokers/installs/${args.install_id}/deployments/${args.deployment_id}`,
          version,
          body,
        );

        const handle = await context.writeResource(
          "broker_deployment",
          String(args.deployment_id),
          result,
        );
        context.logger.info("Updated broker_deployment", {});
        return { dataHandles: [handle] };
      },
    },
    delete_broker_deployment: {
      description: "Deletes Broker deployment",
      arguments: z.object({
        tenant_id: z.string().describe("Tenant ID"),
        install_id: z.string().describe("Install ID"),
        deployment_id: z.string().describe("Deployment ID"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        await snykApi(
          apiToken,
          "DELETE",
          `/tenants/${args.tenant_id}/brokers/installs/${args.install_id}/deployments/${args.deployment_id}`,
          version,
        );

        context.logger.info("Deleted resource {id}", {
          id: args.deployment_id,
        });
        return { dataHandles: [] };
      },
    },
    list_broker_connections: {
      description: "List Broker connections",
      arguments: z.object({
        tenant_id: z.string().describe("Tenant ID"),
        install_id: z.string().describe("Install ID"),
        deployment_id: z.string().describe("Deployment ID"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>([
          "tenant_id",
          "install_id",
          "deployment_id",
        ]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await snykApiPaginated(
          apiToken,
          `/tenants/${args.tenant_id}/brokers/installs/${args.install_id}/deployments/${args.deployment_id}/connections`,
          version,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "broker_connections",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} broker_connections", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_broker_connection: {
      description: "Creates Broker connection",
      arguments: z.object({
        tenant_id: z.string().describe("Tenant ID"),
        install_id: z.string().describe("Install ID"),
        deployment_id: z.string().describe("Deployment ID"),
        data: z.object({
          attributes: z.unknown(),
          type: z.enum(["broker_connection"]),
        }),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>([
          "tenant_id",
          "install_id",
          "deployment_id",
        ]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await snykApi(
          apiToken,
          "POST",
          `/tenants/${args.tenant_id}/brokers/installs/${args.install_id}/deployments/${args.deployment_id}/connections`,
          version,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "broker_connection",
          id,
          result,
        );
        context.logger.info("Created broker_connection {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    delete_broker_connections: {
      description: "Deletes Broker connections",
      arguments: z.object({
        tenant_id: z.string().describe("Tenant ID"),
        install_id: z.string().describe("Install ID"),
        deployment_id: z.string().describe("Deployment ID"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        await snykApi(
          apiToken,
          "DELETE",
          `/tenants/${args.tenant_id}/brokers/installs/${args.install_id}/deployments/${args.deployment_id}/connections`,
          version,
        );

        context.logger.info("Deleted resource {id}", {
          id: args.deployment_id,
        });
        return { dataHandles: [] };
      },
    },
    get_broker_connection: {
      description: "Get Broker connection",
      arguments: z.object({
        tenant_id: z.string().describe("Tenant ID"),
        install_id: z.string().describe("Install ID"),
        deployment_id: z.string().describe("Deployment ID"),
        connection_id: z.string().describe("Connection ID"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const result = await snykApi(
          apiToken,
          "GET",
          `/tenants/${args.tenant_id}/brokers/installs/${args.install_id}/deployments/${args.deployment_id}/connections/${args.connection_id}`,
          version,
        );

        const handle = await context.writeResource(
          "broker_connection",
          String(args.connection_id),
          result,
        );
        context.logger.info("Fetched broker_connection", {});
        return { dataHandles: [handle] };
      },
    },
    update_broker_connection: {
      description: "Updates Broker connection",
      arguments: z.object({
        tenant_id: z.string().describe("Tenant ID"),
        install_id: z.string().describe("Install ID"),
        deployment_id: z.string().describe("Deployment ID"),
        connection_id: z.string().describe("Connection ID"),
        data: z.object({
          attributes: z.unknown(),
          id: z.string(),
          type: z.enum(["broker_connection"]),
        }),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>([
          "tenant_id",
          "install_id",
          "deployment_id",
          "connection_id",
        ]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await snykApi(
          apiToken,
          "PATCH",
          `/tenants/${args.tenant_id}/brokers/installs/${args.install_id}/deployments/${args.deployment_id}/connections/${args.connection_id}`,
          version,
          body,
        );

        const handle = await context.writeResource(
          "broker_connection",
          String(args.connection_id),
          result,
        );
        context.logger.info("Updated broker_connection", {});
        return { dataHandles: [handle] };
      },
    },
    delete_broker_connection: {
      description: "Deletes Broker connection",
      arguments: z.object({
        tenant_id: z.string().describe("Tenant ID"),
        install_id: z.string().describe("Install ID"),
        deployment_id: z.string().describe("Deployment ID"),
        connection_id: z.string().describe("Connection ID"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        await snykApi(
          apiToken,
          "DELETE",
          `/tenants/${args.tenant_id}/brokers/installs/${args.install_id}/deployments/${args.deployment_id}/connections/${args.connection_id}`,
          version,
        );

        context.logger.info("Deleted resource {id}", {
          id: args.connection_id,
        });
        return { dataHandles: [] };
      },
    },
    list_broker_orgs_for_bulk_migration: {
      description: "List organizations for bulk migration",
      arguments: z.object({
        connection_id: z.string().describe("Connection ID"),
        deployment_id: z.string().describe("Deployment ID"),
        install_id: z.string().describe("Install ID"),
        tenant_id: z.string().describe("Tenant ID"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>([
          "connection_id",
          "deployment_id",
          "install_id",
          "tenant_id",
        ]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await snykApiPaginated(
          apiToken,
          `/tenants/${args.tenant_id}/brokers/installs/${args.install_id}/deployments/${args.deployment_id}/connections/${args.connection_id}/bulk_migration`,
          version,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "broker_orgs_for_bulk_migration",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} broker_orgs_for_bulk_migration", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_broker_orgs_for_bulk_migration: {
      description: "Performs bulk migration integrations to universal broker",
      arguments: z.object({
        connection_id: z.string().describe("Connection ID"),
        deployment_id: z.string().describe("Deployment ID"),
        install_id: z.string().describe("Install ID"),
        tenant_id: z.string().describe("Tenant ID"),
        data: z.object({
          type: z.enum(["broker_migration"]),
        }),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>([
          "connection_id",
          "deployment_id",
          "install_id",
          "tenant_id",
        ]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await snykApi(
          apiToken,
          "POST",
          `/tenants/${args.tenant_id}/brokers/installs/${args.install_id}/deployments/${args.deployment_id}/connections/${args.connection_id}/bulk_migration`,
          version,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "broker_orgs_for_bulk_migration",
          id,
          result,
        );
        context.logger.info("Created broker_orgs_for_bulk_migration {id}", {
          id,
        });
        return { dataHandles: [handle] };
      },
    },
    list_deployment_contexts: {
      description: "List Deployment contexts",
      arguments: z.object({
        tenant_id: z.string().describe("Tenant ID"),
        install_id: z.string().describe("Install ID"),
        deployment_id: z.string().describe("Deployment ID"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>([
          "tenant_id",
          "install_id",
          "deployment_id",
        ]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await snykApiPaginated(
          apiToken,
          `/tenants/${args.tenant_id}/brokers/installs/${args.install_id}/deployments/${args.deployment_id}/contexts`,
          version,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "deployment_contexts",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} deployment_contexts", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_broker_context: {
      description: "Create broker Context",
      arguments: z.object({
        tenant_id: z.string().describe("Tenant ID"),
        install_id: z.string().describe("Install ID"),
        deployment_id: z.string().describe("Deployment ID"),
        data: z.object({
          attributes: z.unknown(),
          type: z.enum(["broker_context"]),
        }),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>([
          "tenant_id",
          "install_id",
          "deployment_id",
        ]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await snykApi(
          apiToken,
          "POST",
          `/tenants/${args.tenant_id}/brokers/installs/${args.install_id}/deployments/${args.deployment_id}/contexts`,
          version,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "broker_context",
          id,
          result,
        );
        context.logger.info("Created broker_context {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    list_deployment_credentials: {
      description: "List Deployment credentials",
      arguments: z.object({
        tenant_id: z.string().describe("Tenant ID"),
        install_id: z.string().describe("Install ID"),
        deployment_id: z.string().describe("Deployment ID"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>([
          "tenant_id",
          "install_id",
          "deployment_id",
        ]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await snykApiPaginated(
          apiToken,
          `/tenants/${args.tenant_id}/brokers/installs/${args.install_id}/deployments/${args.deployment_id}/credentials`,
          version,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "deployment_credentials",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} deployment_credentials", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_deployment_credential: {
      description: "Create deployment credential",
      arguments: z.object({
        tenant_id: z.string().describe("Tenant ID"),
        install_id: z.string().describe("Install ID"),
        deployment_id: z.string().describe("Deployment ID"),
        data: z.object({
          attributes: z.unknown(),
          type: z.enum(["deployment_credential"]),
        }),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>([
          "tenant_id",
          "install_id",
          "deployment_id",
        ]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await snykApi(
          apiToken,
          "POST",
          `/tenants/${args.tenant_id}/brokers/installs/${args.install_id}/deployments/${args.deployment_id}/credentials`,
          version,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "deployment_credential",
          id,
          result,
        );
        context.logger.info("Created deployment_credential {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_deployment_credential: {
      description: "Get Deployment credential",
      arguments: z.object({
        tenant_id: z.string().describe("Tenant ID"),
        install_id: z.string().describe("Install ID"),
        deployment_id: z.string().describe("Deployment ID"),
        credential_id: z.string().describe("Credential ID"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const result = await snykApi(
          apiToken,
          "GET",
          `/tenants/${args.tenant_id}/brokers/installs/${args.install_id}/deployments/${args.deployment_id}/credentials/${args.credential_id}`,
          version,
        );

        const handle = await context.writeResource(
          "deployment_credential",
          String(args.credential_id),
          result,
        );
        context.logger.info("Fetched deployment_credential", {});
        return { dataHandles: [handle] };
      },
    },
    update_deployment_credential: {
      description: "Updates Deployment credential",
      arguments: z.object({
        tenant_id: z.string().describe("Tenant ID"),
        install_id: z.string().describe("Install ID"),
        deployment_id: z.string().describe("Deployment ID"),
        credential_id: z.string().describe("Credential ID"),
        data: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>([
          "tenant_id",
          "install_id",
          "deployment_id",
          "credential_id",
        ]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await snykApi(
          apiToken,
          "PATCH",
          `/tenants/${args.tenant_id}/brokers/installs/${args.install_id}/deployments/${args.deployment_id}/credentials/${args.credential_id}`,
          version,
          body,
        );

        const handle = await context.writeResource(
          "deployment_credential",
          String(args.credential_id),
          result,
        );
        context.logger.info("Updated deployment_credential", {});
        return { dataHandles: [handle] };
      },
    },
    delete_deployment_credential: {
      description: "Deletes Deployment credential",
      arguments: z.object({
        tenant_id: z.string().describe("Tenant ID"),
        install_id: z.string().describe("Install ID"),
        deployment_id: z.string().describe("Deployment ID"),
        credential_id: z.string().describe("Credential ID"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        await snykApi(
          apiToken,
          "DELETE",
          `/tenants/${args.tenant_id}/brokers/installs/${args.install_id}/deployments/${args.deployment_id}/credentials/${args.credential_id}`,
          version,
        );

        context.logger.info("Deleted resource {id}", {
          id: args.credential_id,
        });
        return { dataHandles: [] };
      },
    },
    list_assets_tenant: {
      description: "List or search all assets (synchronous) (Early Access)",
      arguments: z.object({
        tenant_id: z.string().describe("The unique identifier of the tenant"),
        filter: z.string().optional().describe(
          "RSQL filter expression for filtering results. See schema for full documentation.",
        ),
        sort: z.string().optional().describe(
          "Comma-separated sort fields. Prefix with `-` for descending order. **Supporte...",
        ),
        fields: z.string().optional().describe(
          "Sparse fieldsets allow clients to request only specific fields for a given re...",
        ),
        meta_count: z.enum(["with", "only"]).optional().describe(
          "Provide summary count in the response meta object when requested. When `with`...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>(["tenant_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await snykApiPaginated(
          apiToken,
          `/tenants/${args.tenant_id}/inventory/assets`,
          version,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("assets_tenant", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} assets_tenant", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    update_assets_bulk_tenant: {
      description: "Bulk update asset attributes (Early Access)",
      arguments: z.object({
        tenant_id: z.string().describe("The unique identifier of the tenant"),
        data: z.array(z.unknown()),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>(["tenant_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await snykApi(
          apiToken,
          "PATCH",
          `/tenants/${args.tenant_id}/inventory/assets`,
          version,
          body,
        );

        const handle = await context.writeResource(
          "assets_bulk_tenant",
          String(args.tenant_id),
          result,
        );
        context.logger.info("Updated assets_bulk_tenant", {});
        return { dataHandles: [handle] };
      },
    },
    get_filter_fields_tenant: {
      description: "Get available filter fields (Early Access)",
      arguments: z.object({
        tenant_id: z.string().describe("The unique identifier of the tenant"),
        asset_types: z.string().optional().describe(
          "Comma-separated list of asset types to filter the available filter fields",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>(["tenant_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await snykApiPaginated(
          apiToken,
          `/tenants/${args.tenant_id}/inventory/assets/filters`,
          version,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "get_filter_fields_tenant",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} get_filter_fields_tenant", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    get_filter_values_tenant: {
      description: "Get filter value suggestions (autocomplete) (Early Access)",
      arguments: z.object({
        tenant_id: z.string().describe("The unique identifier of the tenant"),
        filter_id: z.string().describe(
          "The UUID of the filter field to get values for (from the filter fields list e...",
        ),
        q: z.string().optional().describe(
          "Full text search term to filter the list of values. If keys_only is true, thi...",
        ),
        keys_only: z.boolean().optional().describe(
          "Return only the keys of the object filter values",
        ),
        key: z.string().optional().describe(
          "Return only the value for a specific key of the object filter values",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>(["tenant_id", "filter_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await snykApiPaginated(
          apiToken,
          `/tenants/${args.tenant_id}/inventory/assets/filters/${args.filter_id}/values`,
          version,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "get_filter_values_tenant",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} get_filter_values_tenant", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    get_group_fields_tenant: {
      description: "Get available group fields (Early Access)",
      arguments: z.object({
        tenant_id: z.string().describe("The unique identifier of the tenant"),
        asset_types: z.string().optional().describe(
          "Comma-separated list of asset types to filter the available group fields",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>(["tenant_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await snykApiPaginated(
          apiToken,
          `/tenants/${args.tenant_id}/inventory/assets/groups`,
          version,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "get_group_fields_tenant",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} get_group_fields_tenant", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    get_group_values_tenant: {
      description: "Get group value aggregation (Early Access)",
      arguments: z.object({
        tenant_id: z.string().describe("The unique identifier of the tenant"),
        group_field_id: z.string().describe(
          "The UUID of the group field to get values for (from the group fields list end...",
        ),
        asset_types: z.string().optional().describe(
          "Comma-separated list of asset types to filter the aggregation",
        ),
        filter: z.string().optional().describe(
          "RSQL filter expression for filtering which assets are included in aggregation...",
        ),
        sort: z.string().optional().describe(
          "Comma-separated sort fields for group values. Prefix with `-` for descending ...",
        ),
        meta_fields: z.string().optional().describe(
          "Meta fields to include in the response. Multiple fields can be specified. Ava...",
        ),
        aggregate: z.string().optional().describe(
          "Per-field aggregate function override for meta fields. All fields default to ...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>(["tenant_id", "group_field_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await snykApiPaginated(
          apiToken,
          `/tenants/${args.tenant_id}/inventory/assets/groups/${args.group_field_id}/values`,
          version,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "get_group_values_tenant",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} get_group_values_tenant", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_asset_search_tenant: {
      description: "Create an asset search (asynchronous) (Early Access)",
      arguments: z.object({
        tenant_id: z.string().describe("The unique identifier of the tenant"),
        data: z.object({
          attributes: z.object({
            filter: z.unknown().optional(),
            limit: z.number().int().min(10).max(100).optional(),
            meta_count: z.enum(["with", "only"]).optional(),
            sort: z.string().optional(),
          }).optional(),
          type: z.enum(["searches"]).optional(),
        }).optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>(["tenant_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await snykApi(
          apiToken,
          "POST",
          `/tenants/${args.tenant_id}/inventory/assets/searches`,
          version,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "asset_search_tenant",
          id,
          result,
        );
        context.logger.info("Created asset_search_tenant {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_asset_search_results_tenant: {
      description:
        "Retrieve asset search results (asynchronous) (Early Access)",
      arguments: z.object({
        tenant_id: z.string().describe("The unique identifier of the tenant"),
        search_id: z.string().describe(
          "The unique identifier of the search operation",
        ),
        sort: z.string().optional().describe(
          "Comma-separated sort fields. Prefix with `-` for descending order. **Supporte...",
        ),
        fields: z.string().optional().describe(
          "Sparse fieldsets allow clients to request only specific fields for a given re...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>(["tenant_id", "search_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await snykApiPaginated(
          apiToken,
          `/tenants/${args.tenant_id}/inventory/assets/searches/${args.search_id}/results`,
          version,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "get_asset_search_results_tenant",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} get_asset_search_results_tenant", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    get_asset_tenant: {
      description: "Get a single asset by ID (Early Access)",
      arguments: z.object({
        tenant_id: z.string().describe("The unique identifier of the tenant"),
        asset_id: z.string().describe("The unique identifier of the asset"),
        fields: z.string().optional().describe(
          "Sparse fieldsets allow clients to request only specific fields for a given re...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const queryParts: string[] = [];
        const excludeKeys = new Set<string>(["tenant_id", "asset_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) {
            queryParts.push(
              `${encodeURIComponent(k)}=${encodeURIComponent(String(v))}`,
            );
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await snykApi(
          apiToken,
          "GET",
          `/tenants/${args.tenant_id}/inventory/assets/${args.asset_id}${qs}`,
          version,
        );

        const handle = await context.writeResource(
          "asset_tenant",
          String(args.asset_id),
          result,
        );
        context.logger.info("Fetched asset_tenant", {});
        return { dataHandles: [handle] };
      },
    },
    update_asset_tenant: {
      description: "Update asset attributes (Early Access)",
      arguments: z.object({
        tenant_id: z.string().describe("The unique identifier of the tenant"),
        asset_id: z.string().describe("The unique identifier of the asset"),
        data: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>(["tenant_id", "asset_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await snykApi(
          apiToken,
          "PATCH",
          `/tenants/${args.tenant_id}/inventory/assets/${args.asset_id}`,
          version,
          body,
        );

        const handle = await context.writeResource(
          "asset_tenant",
          String(args.asset_id),
          result,
        );
        context.logger.info("Updated asset_tenant", {});
        return { dataHandles: [handle] };
      },
    },
    list_asset_projects_tenant: {
      description: "List projects for an asset (Early Access)",
      arguments: z.object({
        tenant_id: z.string().describe("The unique identifier of the tenant"),
        asset_id: z.string().describe("The unique identifier of the asset"),
        canonical: z.enum(["with", "only", "none"]).optional().describe(
          "Filter projects by canonical status. - `with`: Returns all projects (canonica...",
        ),
        target_id: z.string().optional().describe(
          "Filter projects by target ID. When provided, returns only projects that belon...",
        ),
        sort: z.enum([
          "snapshot_created_at",
          "-snapshot_created_at",
          "issues",
          "-issues",
        ]).optional().describe(
          "Sort field with optional direction prefix. Prefix with `-` for descending ord...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>(["tenant_id", "asset_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await snykApiPaginated(
          apiToken,
          `/tenants/${args.tenant_id}/inventory/assets/${args.asset_id}/relationships/projects`,
          version,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "asset_projects_tenant",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} asset_projects_tenant", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    list_asset_targets_tenant: {
      description: "List targets for an asset (Early Access)",
      arguments: z.object({
        tenant_id: z.string().describe("The unique identifier of the tenant"),
        asset_id: z.string().describe("The unique identifier of the asset"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>(["tenant_id", "asset_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await snykApiPaginated(
          apiToken,
          `/tenants/${args.tenant_id}/inventory/assets/${args.asset_id}/relationships/targets`,
          version,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "asset_targets_tenant",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} asset_targets_tenant", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    get_tenant_memberships: {
      description: "Get all memberships of the tenant (Early Access)",
      arguments: z.object({
        tenant_id: z.string().describe("Unique identifier of the tenant."),
        sort_by: z.enum([
          "username",
          "user_display_name",
          "email",
          "login_method",
          "role_name",
        ]).optional().describe("Which column to sort by."),
        sort_order: z.enum(["ASC", "DESC"]).optional().describe(
          "Order in which results are returned.",
        ),
        email: z.string().optional().describe(
          "Filter the response by Users that match the provided email",
        ),
        user_id: z.string().optional().describe(
          "Filter the response by Users that match the provided user ID",
        ),
        name: z.string().optional().describe(
          "Filter the response by Users that match the provided name",
        ),
        username: z.string().optional().describe(
          "Filter the response by Users that match the provided username",
        ),
        connection_type: z.string().optional().describe(
          "Filter the response by Users that match the provided connection type",
        ),
        role_name: z.string().optional().describe(
          "Filter the response for results only with the specified role.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>(["tenant_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await snykApiPaginated(
          apiToken,
          `/tenants/${args.tenant_id}/memberships`,
          version,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "get_tenant_memberships",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} get_tenant_memberships", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    update_tenant_membership: {
      description: "Update tenant membership (Early Access)",
      arguments: z.object({
        tenant_id: z.string().describe("Unique identifier of the tenant."),
        membership_id: z.string().describe(
          "Unique identifier of the tenant membership.",
        ),
        data: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>(["tenant_id", "membership_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await snykApi(
          apiToken,
          "PATCH",
          `/tenants/${args.tenant_id}/memberships/${args.membership_id}`,
          version,
          body,
        );

        const handle = await context.writeResource(
          "tenant_membership",
          String(args.membership_id),
          result,
        );
        context.logger.info("Updated tenant_membership", {});
        return { dataHandles: [handle] };
      },
    },
    delete_tenant_membership: {
      description:
        "Delete an individual tenant membership for a single user. (Early Access)",
      arguments: z.object({
        membership_id: z.string().describe(
          "Unique identifier of the tenant membership.",
        ),
        tenant_id: z.string().describe("Unique identifier of the tenant."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        await snykApi(
          apiToken,
          "DELETE",
          `/tenants/${args.tenant_id}/memberships/${args.membership_id}`,
          version,
        );

        context.logger.info("Deleted resource {id}", { id: args.tenant_id });
        return { dataHandles: [] };
      },
    },
    list_tenant_roles: {
      description: "List all available roles for a given tenant (Early Access)",
      arguments: z.object({
        tenant_id: z.string().describe("Unique identifier of the tenant."),
        name: z.string().optional().describe("Role name filter."),
        custom: z.string().optional().describe(
          "Whether role is custom or not.",
        ),
        assignable_by_me: z.string().optional().describe(
          "When true, only return roles that the current user can assign to others in th...",
        ),
        expand_permissions: z.boolean().optional().describe(
          "option to show all permission types",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>(["tenant_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await snykApiPaginated(
          apiToken,
          `/tenants/${args.tenant_id}/roles`,
          version,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("tenant_roles", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} tenant_roles", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_tenant_role: {
      description:
        "Create a custom tenant role for a given tenant (Early Access)",
      arguments: z.object({
        tenant_id: z.string().describe("Unique identifier of the tenant."),
        data: z.object({
          attributes: z.unknown(),
          type: z.unknown(),
        }),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>(["tenant_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await snykApi(
          apiToken,
          "POST",
          `/tenants/${args.tenant_id}/roles`,
          version,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource("tenant_role", id, result);
        context.logger.info("Created tenant_role {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_tenant_role: {
      description:
        "Return a specific role by its id and its tenant id. (Early Access)",
      arguments: z.object({
        tenant_id: z.string().describe("Unique identifier of the tenant."),
        role_id: z.string().describe("Unique identifier of the role."),
        has_users_assigned: z.boolean().optional().describe(
          "returns current memberships of the role in the meta relationships section",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const queryParts: string[] = [];
        const excludeKeys = new Set<string>(["tenant_id", "role_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) {
            queryParts.push(
              `${encodeURIComponent(k)}=${encodeURIComponent(String(v))}`,
            );
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await snykApi(
          apiToken,
          "GET",
          `/tenants/${args.tenant_id}/roles/${args.role_id}${qs}`,
          version,
        );

        const handle = await context.writeResource(
          "tenant_role",
          String(args.role_id),
          result,
        );
        context.logger.info("Fetched tenant_role", {});
        return { dataHandles: [handle] };
      },
    },
    update_tenant_role: {
      description:
        "Update a specific tenant role by its id and its tenant id. (Early Access)",
      arguments: z.object({
        tenant_id: z.string().describe("Unique identifier of the tenant."),
        role_id: z.string().describe("Unique identifier of the role."),
        force: z.string().optional().describe(
          "flag to force the update of a role, required if users are assigned to the role",
        ),
        data: z.object({
          attributes: z.unknown(),
          id: z.unknown().optional(),
          type: z.unknown(),
        }),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>(["tenant_id", "role_id", "force"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await snykApi(
          apiToken,
          "PATCH",
          `/tenants/${args.tenant_id}/roles/${args.role_id}`,
          version,
          body,
        );

        const handle = await context.writeResource(
          "tenant_role",
          String(args.role_id),
          result,
        );
        context.logger.info("Updated tenant_role", {});
        return { dataHandles: [handle] };
      },
    },
    delete_tenant_role: {
      description:
        "Delete a specific tenant role by its id and its tenant id. (Early Access)",
      arguments: z.object({
        tenant_id: z.string().describe("Unique identifier of the tenant."),
        role_id: z.string().describe("Unique identifier of the role."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        await snykApi(
          apiToken,
          "DELETE",
          `/tenants/${args.tenant_id}/roles/${args.role_id}`,
          version,
        );

        context.logger.info("Deleted resource {id}", { id: args.role_id });
        return { dataHandles: [] };
      },
    },
  },
};
