// Snyk Container Images — container image scanning and vulnerability data — Tests
// Auto-generated by scripts/snyk-codegen — do not edit manually.
// SPDX-License-Identifier: Apache-2.0

import { assertEquals, assertExists } from "jsr:@std/assert@1";
import { createModelTestContext } from "@systeminit/swamp-testing";
import { model } from "./container_images.ts";

// ---------------------------------------------------------------------------
// Model Export Structure Tests
// ---------------------------------------------------------------------------

Deno.test("container-images model: has correct type", () => {
  assertEquals(model.type, "@webframp/snyk/container-images");
});

Deno.test("container-images model: has valid version format", () => {
  const versionPattern = /^\d{4}\.\d{2}\.\d{2}\.\d+$/;
  assertEquals(versionPattern.test(model.version), true);
});

Deno.test("container-images model: has globalArguments with apiToken", () => {
  assertExists(model.globalArguments);
  const shape = model.globalArguments.shape;
  assertExists(shape.apiToken);
  assertExists(shape.orgId);
  assertExists(shape.version);
});

Deno.test("container-images model: has expected methods", () => {
  assertExists(model.methods);
  assertExists(model.methods.list_container_image);
  assertExists(model.methods.get_container_image);
  assertExists(model.methods.list_image_target_refs);
});

Deno.test("container-images model: has expected resources", () => {
  assertExists(model.resources);
  assertExists(model.resources["container_image"]);
  assertExists(model.resources["image_target_refs"]);
});

// ---------------------------------------------------------------------------
// Mock Snyk API Server
// ---------------------------------------------------------------------------

function startMockSnykServer(
  responses: Record<string, { data: unknown; isCollection?: boolean }>,
): { url: string; server: Deno.HttpServer } {
  const server = Deno.serve({ port: 0, onListen() {} }, (req) => {
    const url = new URL(req.url);
    const path = url.pathname;

    for (const [pattern, { data, isCollection }] of Object.entries(responses)) {
      if (path.includes(pattern)) {
        const body = isCollection
          ? {
            data: Array.isArray(data) ? data : [data],
            jsonapi: { version: "1.0" },
            links: { self: path, next: null },
          }
          : {
            data,
            jsonapi: { version: "1.0" },
            links: { self: path },
          };
        return Response.json(body, {
          headers: { "content-type": "application/vnd.api+json" },
        });
      }
    }

    return new Response(
      JSON.stringify({
        jsonapi: { version: "1.0" },
        errors: [{ status: "404", detail: "Not found" }],
      }),
      { status: 404, headers: { "content-type": "application/vnd.api+json" } },
    );
  });

  const addr = server.addr as Deno.NetAddr;
  return { url: `http://localhost:${addr.port}`, server };
}

function installFetchMock(mockUrl: string): () => void {
  const originalFetch = globalThis.fetch;
  globalThis.fetch = (input, init) => {
    const reqUrl = typeof input === "string" ? input : input.toString();
    const newUrl = reqUrl.replace("https://api.snyk.io/rest", mockUrl);
    return originalFetch(newUrl, init);
  };
  return () => {
    globalThis.fetch = originalFetch;
  };
}

Deno.test({
  name:
    "container-images model: list_container_image fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockItem = {
      "id": "fixture-123",
      "type": "resource",
      "attributes": {
        "layers": [],
        "names": [],
        "platform": "linux/amd64",
        "image_target_refs_id": "test-value",
      },
    };
    const { url, server } = startMockSnykServer({
      "/container_images": { data: [mockItem], isCollection: true },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: {
          "apiToken": "test-token",
          "version": "2024-10-15",
          "orgId": "test-org-123",
        },
        definition: {
          id: "test-id",
          name: "test-container-images",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).list_container_image.execute({}, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name:
    "container-images model: get_container_image fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "id": "fixture-123",
      "type": "resource",
      "attributes": {
        "layers": [],
        "names": [],
        "platform": "linux/amd64",
        "image_target_refs_id": "test-value",
      },
    };
    const { url, server } = startMockSnykServer({
      "/container_images/test-id-123": { data: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: {
          "apiToken": "test-token",
          "version": "2024-10-15",
          "orgId": "test-org-123",
        },
        definition: {
          id: "test-id",
          name: "test-container-images",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).get_container_image.execute({ "image_id": "test-id-123" }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});
