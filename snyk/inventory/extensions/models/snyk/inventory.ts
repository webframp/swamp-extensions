/**
 * Snyk Inventory — asset discovery for packages, containers, repos, and cloud resources
 *
 * Auto-generated by scripts/snyk-codegen — do not edit manually.
 *
 * @module
 */
// SPDX-License-Identifier: AGPL-3.0-or-later WITH Swamp-Extension-Exception

import { z } from "npm:zod@4.4.3";
import { snykApi, snykApiPaginated } from "./_lib/api.ts";

// =============================================================================
// Schemas
// =============================================================================

const GlobalArgsSchema = z.object({
  apiToken: z.string().meta({ sensitive: true }).describe("Snyk API token"),
  orgId: z.string().describe("Snyk organization ID"),
  version: z.string().default("2024-10-15").describe("Snyk API version date"),
});

const AssetsGroupItemSchema = z.object({
  id: z.string().describe("The unique identifier of the asset"),
  type: z.enum(["container_images", "sboms", "repositories"]).optional()
    .describe("The JSON:API resource type"),
  projects_id: z.string().optional().describe("Related projects ID"),
  targets_id: z.string().optional().describe("Related targets ID"),
});

const ListAssetsGroupSchema = z.object({
  items: z.array(AssetsGroupItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetFilterFieldsGroupItemSchema = z.object({
  id: z.string().describe("UUID identifier for the filter field"),
  type: z.enum(["string", "boolean", "datetime", "array", "object"]).describe(
    "Data type of the field (string, boolean, datetime, array, object)",
  ),
  asset_types: z.array(z.enum(["container_images", "sboms", "repositories"]))
    .describe("List of asset types this field applies to"),
  name: z.string().describe(
    'The field name used in RSQL filters (e.g., "class", "registry")',
  ),
  values_id: z.string().optional().describe("Related values ID"),
});

const GetFilterFieldsGroupSchema = z.object({
  items: z.array(GetFilterFieldsGroupItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetFilterValuesGroupItemSchema = z.object({
  id: z.string().describe("UUID identifier for the filter value"),
  type: z.enum(["filter_values"]).optional().describe(
    "The JSON:API resource type",
  ),
  key: z.string().optional().describe(
    "For object-type filters (e.g., tags), the key name. Only present for object-type filter values.",
  ),
  value: z.string().optional().describe(
    "The filter value. For simple filters, this is the value itself. For object-type filters (e.g., ta...",
  ),
});

const GetFilterValuesGroupSchema = z.object({
  items: z.array(GetFilterValuesGroupItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetGroupFieldsGroupItemSchema = z.object({
  id: z.string().describe("UUID identifier for the group field"),
  type: z.enum(["group_fields"]).optional().describe(
    "The JSON:API resource type",
  ),
  asset_types: z.array(z.string()).describe(
    "List of asset types this field applies to",
  ),
  name: z.string().describe(
    'The field name used for grouping (e.g., "class", "registry")',
  ),
  values_id: z.string().optional().describe("Related values ID"),
});

const GetGroupFieldsGroupSchema = z.object({
  items: z.array(GetGroupFieldsGroupItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetGroupValuesGroupItemSchema = z.object({
  id: z.string().describe("UUID identifier for the group value"),
  type: z.enum(["group_values"]).optional().describe(
    "The JSON:API resource type",
  ),
  value: z.string().describe("The grouped value"),
});

const GetGroupValuesGroupSchema = z.object({
  items: z.array(GetGroupValuesGroupItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetAssetSearchResultsGroupItemSchema = z.object({
  id: z.string().describe("The unique identifier of the asset"),
  type: z.enum(["container_images", "sboms", "repositories"]).optional()
    .describe("The JSON:API resource type"),
  projects_id: z.string().optional().describe("Related projects ID"),
  targets_id: z.string().optional().describe("Related targets ID"),
});

const GetAssetSearchResultsGroupSchema = z.object({
  items: z.array(GetAssetSearchResultsGroupItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetAssetGroupSchema = z.object({
  id: z.string().describe("The unique identifier of the asset"),
  type: z.enum(["container_images", "sboms", "repositories"]).optional()
    .describe("The JSON:API resource type"),
  projects_id: z.string().optional().describe("Related projects ID"),
  targets_id: z.string().optional().describe("Related targets ID"),
});

const AssetProjectsGroupItemSchema = z.object({
  id: z.string().describe("The unique identifier of the project"),
  type: z.enum(["projects"]).optional().describe("The JSON:API resource type"),
  base_image_remediation: z.object({
    base_image: z.unknown().optional(),
    base_image_name: z.string(),
    base_image_outdated: z.boolean().optional(),
    code: z.string(),
    distro_alert: z.string().optional(),
    proposed_base_images: z.unknown().optional(),
  }).optional().describe(
    "Base image upgrade recommendation data from container scanning",
  ),
  is_canonical: z.boolean().optional().describe(
    "Indicates whether this project is the canonical project for its target_file. Projects are grouped...",
  ),
  issues: z.object({
    critical: z.number().int(),
    high: z.number().int(),
    low: z.number().int(),
    medium: z.number().int(),
  }).optional().describe("Issue counts by severity"),
  monitor_created_at: z.string().optional().describe(
    "Timestamp when the monitor was created",
  ),
  monitor_id: z.string().optional().describe(
    "The ID of the monitor associated with this project",
  ),
  org_id: z.string().optional().describe(
    "The ID of the organization that owns this project",
  ),
  project_type: z.enum([
    "rubygems",
    "composer",
    "sbt",
    "maven",
    "gomodules",
    "poetry",
    "cocoapods",
    "pipenv",
    "pnpm",
    "pub",
    "yarn",
    "yarn-workspace",
    "paket",
    "pip",
    "unmanaged",
    "gradle",
    "nuget",
    "cpp",
    "npm",
    "conan",
    "govendor",
    "hex",
    "golangdep",
    "cargo",
    "swift",
    "golang",
    "secrets",
    "iac",
    "uv",
    "sast",
    "armconfig",
    "helmconfig",
    "terraformconfig",
    "cloudformationconfig",
    "k8sconfig",
    "cloudconfig",
    "dockerfile",
    "deb",
    "apk",
    "linux",
    "rpm",
  ]).optional().describe("Type of the project (e.g., npm, maven)"),
  risk_score: z.number().int().optional().describe(
    "The calculated risk score for this project",
  ),
  snapshot_created_at: z.string().optional().describe(
    "Timestamp when the snapshot was created",
  ),
  snapshot_id: z.string().optional().describe("The ID of the snapshot"),
  target_file: z.string().optional().describe("Path to the manifest file"),
  target_id: z.string().optional().describe(
    "The ID of the target this project belongs to",
  ),
  target_origin: z.string().optional().describe(
    "Origin of the target (e.g., github, gitlab)",
  ),
  target_ref: z.string().optional().describe(
    "The target reference (e.g., branch name)",
  ),
});

const ListAssetProjectsGroupSchema = z.object({
  items: z.array(AssetProjectsGroupItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const AssetTargetsGroupItemSchema = z.object({
  id: z.string().describe("The unique identifier of the target"),
  type: z.enum(["target"]).optional().describe("The JSON:API resource type"),
  imported_at: z.string().optional().describe(
    "Timestamp when the target was imported",
  ),
  integration_id: z.string().optional().describe(
    "The ID of the integration associated with this target",
  ),
  target_file: z.string().optional().describe("Path to the manifest file"),
  target_origin: z.string().optional().describe(
    "Origin of the target (e.g., github, gitlab)",
  ),
  target_ref: z.string().optional().describe(
    "The target reference (e.g., branch name)",
  ),
});

const ListAssetTargetsGroupSchema = z.object({
  items: z.array(AssetTargetsGroupItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const AssetsOrgItemSchema = z.object({
  id: z.string().describe("The unique identifier of the asset"),
  type: z.enum(["container_images", "sboms", "repositories"]).optional()
    .describe("The JSON:API resource type"),
  projects_id: z.string().optional().describe("Related projects ID"),
  targets_id: z.string().optional().describe("Related targets ID"),
});

const ListAssetsOrgSchema = z.object({
  items: z.array(AssetsOrgItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetFilterFieldsOrgItemSchema = z.object({
  id: z.string().describe("UUID identifier for the filter field"),
  type: z.enum(["string", "boolean", "datetime", "array", "object"]).describe(
    "Data type of the field (string, boolean, datetime, array, object)",
  ),
  asset_types: z.array(z.enum(["container_images", "sboms", "repositories"]))
    .describe("List of asset types this field applies to"),
  name: z.string().describe(
    'The field name used in RSQL filters (e.g., "class", "registry")',
  ),
  values_id: z.string().optional().describe("Related values ID"),
});

const GetFilterFieldsOrgSchema = z.object({
  items: z.array(GetFilterFieldsOrgItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetFilterValuesOrgItemSchema = z.object({
  id: z.string().describe("UUID identifier for the filter value"),
  type: z.enum(["filter_values"]).optional().describe(
    "The JSON:API resource type",
  ),
  key: z.string().optional().describe(
    "For object-type filters (e.g., tags), the key name. Only present for object-type filter values.",
  ),
  value: z.string().optional().describe(
    "The filter value. For simple filters, this is the value itself. For object-type filters (e.g., ta...",
  ),
});

const GetFilterValuesOrgSchema = z.object({
  items: z.array(GetFilterValuesOrgItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetGroupFieldsOrgItemSchema = z.object({
  id: z.string().describe("UUID identifier for the group field"),
  type: z.enum(["group_fields"]).optional().describe(
    "The JSON:API resource type",
  ),
  asset_types: z.array(z.string()).describe(
    "List of asset types this field applies to",
  ),
  name: z.string().describe(
    'The field name used for grouping (e.g., "class", "registry")',
  ),
  values_id: z.string().optional().describe("Related values ID"),
});

const GetGroupFieldsOrgSchema = z.object({
  items: z.array(GetGroupFieldsOrgItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetGroupValuesOrgItemSchema = z.object({
  id: z.string().describe("UUID identifier for the group value"),
  type: z.enum(["group_values"]).optional().describe(
    "The JSON:API resource type",
  ),
  value: z.string().describe("The grouped value"),
});

const GetGroupValuesOrgSchema = z.object({
  items: z.array(GetGroupValuesOrgItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetAssetSearchResultsOrgItemSchema = z.object({
  id: z.string().describe("The unique identifier of the asset"),
  type: z.enum(["container_images", "sboms", "repositories"]).optional()
    .describe("The JSON:API resource type"),
  projects_id: z.string().optional().describe("Related projects ID"),
  targets_id: z.string().optional().describe("Related targets ID"),
});

const GetAssetSearchResultsOrgSchema = z.object({
  items: z.array(GetAssetSearchResultsOrgItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetAssetOrgSchema = z.object({
  id: z.string().describe("The unique identifier of the asset"),
  type: z.enum(["container_images", "sboms", "repositories"]).optional()
    .describe("The JSON:API resource type"),
  projects_id: z.string().optional().describe("Related projects ID"),
  targets_id: z.string().optional().describe("Related targets ID"),
});

const AssetProjectsOrgItemSchema = z.object({
  id: z.string().describe("The unique identifier of the project"),
  type: z.enum(["projects"]).optional().describe("The JSON:API resource type"),
  base_image_remediation: z.object({
    base_image: z.unknown().optional(),
    base_image_name: z.string(),
    base_image_outdated: z.boolean().optional(),
    code: z.string(),
    distro_alert: z.string().optional(),
    proposed_base_images: z.unknown().optional(),
  }).optional().describe(
    "Base image upgrade recommendation data from container scanning",
  ),
  is_canonical: z.boolean().optional().describe(
    "Indicates whether this project is the canonical project for its target_file. Projects are grouped...",
  ),
  issues: z.object({
    critical: z.number().int(),
    high: z.number().int(),
    low: z.number().int(),
    medium: z.number().int(),
  }).optional().describe("Issue counts by severity"),
  monitor_created_at: z.string().optional().describe(
    "Timestamp when the monitor was created",
  ),
  monitor_id: z.string().optional().describe(
    "The ID of the monitor associated with this project",
  ),
  org_id: z.string().optional().describe(
    "The ID of the organization that owns this project",
  ),
  project_type: z.enum([
    "rubygems",
    "composer",
    "sbt",
    "maven",
    "gomodules",
    "poetry",
    "cocoapods",
    "pipenv",
    "pnpm",
    "pub",
    "yarn",
    "yarn-workspace",
    "paket",
    "pip",
    "unmanaged",
    "gradle",
    "nuget",
    "cpp",
    "npm",
    "conan",
    "govendor",
    "hex",
    "golangdep",
    "cargo",
    "swift",
    "golang",
    "secrets",
    "iac",
    "uv",
    "sast",
    "armconfig",
    "helmconfig",
    "terraformconfig",
    "cloudformationconfig",
    "k8sconfig",
    "cloudconfig",
    "dockerfile",
    "deb",
    "apk",
    "linux",
    "rpm",
  ]).optional().describe("Type of the project (e.g., npm, maven)"),
  risk_score: z.number().int().optional().describe(
    "The calculated risk score for this project",
  ),
  snapshot_created_at: z.string().optional().describe(
    "Timestamp when the snapshot was created",
  ),
  snapshot_id: z.string().optional().describe("The ID of the snapshot"),
  target_file: z.string().optional().describe("Path to the manifest file"),
  target_id: z.string().optional().describe(
    "The ID of the target this project belongs to",
  ),
  target_origin: z.string().optional().describe(
    "Origin of the target (e.g., github, gitlab)",
  ),
  target_ref: z.string().optional().describe(
    "The target reference (e.g., branch name)",
  ),
});

const ListAssetProjectsOrgSchema = z.object({
  items: z.array(AssetProjectsOrgItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const AssetTargetsOrgItemSchema = z.object({
  id: z.string().describe("The unique identifier of the target"),
  type: z.enum(["target"]).optional().describe("The JSON:API resource type"),
  imported_at: z.string().optional().describe(
    "Timestamp when the target was imported",
  ),
  integration_id: z.string().optional().describe(
    "The ID of the integration associated with this target",
  ),
  target_file: z.string().optional().describe("Path to the manifest file"),
  target_origin: z.string().optional().describe(
    "Origin of the target (e.g., github, gitlab)",
  ),
  target_ref: z.string().optional().describe(
    "The target reference (e.g., branch name)",
  ),
});

const ListAssetTargetsOrgSchema = z.object({
  items: z.array(AssetTargetsOrgItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

// =============================================================================
// Model Definition
// =============================================================================

/** Snyk Inventory — asset discovery for packages, containers, repos, and cloud resources */
export const model = {
  type: "@webframp/snyk/inventory",
  version: "2026.07.20.1",
  globalArguments: GlobalArgsSchema,

  upgrades: [],

  resources: {
    "assets_group": {
      description:
        "List or search all assets (synchronous) - Group scope (Early Access)",
      schema: ListAssetsGroupSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "assets_bulk_group": {
      description: "Bulk update asset attributes - Group scope (Early Access)",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "get_filter_fields_group": {
      description: "Get available filter fields - Group scope (Early Access)",
      schema: GetFilterFieldsGroupSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "get_filter_values_group": {
      description:
        "Get filter value suggestions (autocomplete) - Group scope (Early Access)",
      schema: GetFilterValuesGroupSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "get_group_fields_group": {
      description: "Get available group fields - Group scope (Early Access)",
      schema: GetGroupFieldsGroupSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "get_group_values_group": {
      description: "Get group value aggregation - Group scope (Early Access)",
      schema: GetGroupValuesGroupSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "asset_search_group": {
      description:
        "Create an asset search (asynchronous) - Group scope (Early Access)",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "get_asset_search_results_group": {
      description:
        "Retrieve asset search results (asynchronous) - Group scope (Early Access)",
      schema: GetAssetSearchResultsGroupSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "asset_group": {
      description: "Get a single asset by ID - Group scope (Early Access)",
      schema: GetAssetGroupSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "asset_projects_group": {
      description: "List projects for an asset (group scope) (Early Access)",
      schema: ListAssetProjectsGroupSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "asset_targets_group": {
      description: "List targets for an asset (group scope) (Early Access)",
      schema: ListAssetTargetsGroupSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "assets_org": {
      description:
        "List or search all assets (synchronous) - Org scope (Early Access)",
      schema: ListAssetsOrgSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "assets_bulk_org": {
      description: "Bulk update asset attributes - Org scope (Early Access)",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "get_filter_fields_org": {
      description: "Get available filter fields - Org scope (Early Access)",
      schema: GetFilterFieldsOrgSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "get_filter_values_org": {
      description:
        "Get filter value suggestions (autocomplete) - Org scope (Early Access)",
      schema: GetFilterValuesOrgSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "get_group_fields_org": {
      description: "Get available group fields - Org scope (Early Access)",
      schema: GetGroupFieldsOrgSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "get_group_values_org": {
      description: "Get group value aggregation - Org scope (Early Access)",
      schema: GetGroupValuesOrgSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "asset_search_org": {
      description:
        "Create an asset search (asynchronous) - Org scope (Early Access)",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "get_asset_search_results_org": {
      description:
        "Retrieve asset search results (asynchronous) - Org scope (Early Access)",
      schema: GetAssetSearchResultsOrgSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "asset_org": {
      description: "Get a single asset by ID - Org scope (Early Access)",
      schema: GetAssetOrgSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "asset_projects_org": {
      description: "List projects for an asset (org scope) (Early Access)",
      schema: ListAssetProjectsOrgSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "asset_targets_org": {
      description: "List targets for an asset (org scope) (Early Access)",
      schema: ListAssetTargetsOrgSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
  },

  methods: {
    list_assets_group: {
      description:
        "List or search all assets (synchronous) - Group scope (Early Access)",
      arguments: z.object({
        group_id: z.string().describe("The unique identifier of the group"),
        filter: z.string().optional().describe(
          "RSQL filter expression for filtering results. See schema for full documentation.",
        ),
        sort: z.string().optional().describe(
          "Comma-separated sort fields. Prefix with `-` for descending order.",
        ),
        fields: z.string().optional().describe(
          "Sparse fieldsets allow clients to request only specific fields for a given re...",
        ),
        meta_count: z.enum(["with", "only"]).optional().describe(
          "Provide summary count in the response meta object when requested. When `with`...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>(["group_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await snykApiPaginated(
          apiToken,
          `/groups/${args.group_id}/inventory/assets`,
          version,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("assets_group", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} assets_group", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    update_assets_bulk_group: {
      description: "Bulk update asset attributes - Group scope (Early Access)",
      arguments: z.object({
        group_id: z.string().describe("The unique identifier of the group"),
        data: z.array(z.unknown()),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>(["group_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await snykApi(
          apiToken,
          "PATCH",
          `/groups/${args.group_id}/inventory/assets`,
          version,
          body,
        );

        const handle = await context.writeResource(
          "assets_bulk_group",
          String(args.group_id),
          result,
        );
        context.logger.info("Updated assets_bulk_group", {});
        return { dataHandles: [handle] };
      },
    },
    get_filter_fields_group: {
      description: "Get available filter fields - Group scope (Early Access)",
      arguments: z.object({
        group_id: z.string().describe("The unique identifier of the group"),
        asset_types: z.string().optional().describe(
          "Comma-separated list of asset types to filter the available filter fields",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>(["group_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await snykApiPaginated(
          apiToken,
          `/groups/${args.group_id}/inventory/assets/filters`,
          version,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "get_filter_fields_group",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} get_filter_fields_group", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    get_filter_values_group: {
      description:
        "Get filter value suggestions (autocomplete) - Group scope (Early Access)",
      arguments: z.object({
        group_id: z.string().describe("The unique identifier of the group"),
        filter_id: z.string().describe(
          "The UUID of the filter field to get values for (from the filter fields list e...",
        ),
        q: z.string().optional().describe(
          "Full text search term to filter the list of values. If keys_only is true, thi...",
        ),
        keys_only: z.boolean().optional().describe(
          "Return only the keys of the object filter values",
        ),
        key: z.string().optional().describe(
          "Return only the value for a specific key of the object filter values",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>(["group_id", "filter_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await snykApiPaginated(
          apiToken,
          `/groups/${args.group_id}/inventory/assets/filters/${args.filter_id}/values`,
          version,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "get_filter_values_group",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} get_filter_values_group", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    get_group_fields_group: {
      description: "Get available group fields - Group scope (Early Access)",
      arguments: z.object({
        group_id: z.string().describe("The unique identifier of the group"),
        asset_types: z.string().optional().describe(
          "Comma-separated list of asset types to filter group fields",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>(["group_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await snykApiPaginated(
          apiToken,
          `/groups/${args.group_id}/inventory/assets/groups`,
          version,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "get_group_fields_group",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} get_group_fields_group", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    get_group_values_group: {
      description: "Get group value aggregation - Group scope (Early Access)",
      arguments: z.object({
        group_id: z.string().describe("The unique identifier of the group"),
        group_field_id: z.string().describe(
          "The UUID of the group field to get values for (from the group fields list end...",
        ),
        asset_types: z.string().optional().describe(
          "Comma-separated list of asset types to filter the aggregation",
        ),
        filter: z.string().optional().describe(
          "RSQL filter expression for filtering which assets are included in aggregation...",
        ),
        sort: z.string().optional().describe(
          "Comma-separated sort fields for group values. Prefix with `-` for descending ...",
        ),
        meta_fields: z.string().optional().describe(
          "Meta fields to include in the response. Multiple fields can be specified. Ava...",
        ),
        aggregate: z.string().optional().describe(
          "Per-field aggregate function override for meta fields. All fields default to ...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>(["group_id", "group_field_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await snykApiPaginated(
          apiToken,
          `/groups/${args.group_id}/inventory/assets/groups/${args.group_field_id}/values`,
          version,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "get_group_values_group",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} get_group_values_group", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_asset_search_group: {
      description:
        "Create an asset search (asynchronous) - Group scope (Early Access)",
      arguments: z.object({
        group_id: z.string().describe("The unique identifier of the group"),
        data: z.object({
          attributes: z.object({
            filter: z.unknown().optional(),
            limit: z.number().int().min(10).max(100).optional(),
            meta_count: z.enum(["with", "only"]).optional(),
            sort: z.string().optional(),
          }).optional(),
          type: z.enum(["searches"]).optional(),
        }).optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>(["group_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await snykApi(
          apiToken,
          "POST",
          `/groups/${args.group_id}/inventory/assets/searches`,
          version,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "asset_search_group",
          id,
          result,
        );
        context.logger.info("Created asset_search_group {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_asset_search_results_group: {
      description:
        "Retrieve asset search results (asynchronous) - Group scope (Early Access)",
      arguments: z.object({
        group_id: z.string().describe("The unique identifier of the group"),
        search_id: z.string().describe(
          "The unique identifier of the search operation",
        ),
        sort: z.string().optional().describe(
          "Sort order for results (e.g., -created_at for descending)",
        ),
        fields: z.string().optional().describe(
          "Sparse fieldsets allow clients to request only specific fields for a given re...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>(["group_id", "search_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await snykApiPaginated(
          apiToken,
          `/groups/${args.group_id}/inventory/assets/searches/${args.search_id}/results`,
          version,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "get_asset_search_results_group",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} get_asset_search_results_group", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    get_asset_group: {
      description: "Get a single asset by ID - Group scope (Early Access)",
      arguments: z.object({
        group_id: z.string().describe("The unique identifier of the group"),
        asset_id: z.string().describe("The unique identifier of the asset"),
        fields: z.string().optional().describe(
          "Sparse fieldsets allow clients to request only specific fields for a given re...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const queryParts: string[] = [];
        const excludeKeys = new Set<string>(["group_id", "asset_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) {
            queryParts.push(
              `${encodeURIComponent(k)}=${encodeURIComponent(String(v))}`,
            );
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await snykApi(
          apiToken,
          "GET",
          `/groups/${args.group_id}/inventory/assets/${args.asset_id}${qs}`,
          version,
        );

        const handle = await context.writeResource(
          "asset_group",
          String(args.asset_id),
          result,
        );
        context.logger.info("Fetched asset_group", {});
        return { dataHandles: [handle] };
      },
    },
    update_asset_group: {
      description: "Update asset attributes - Group scope (Early Access)",
      arguments: z.object({
        group_id: z.string().describe("The unique identifier of the group"),
        asset_id: z.string().describe("The unique identifier of the asset"),
        data: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>(["group_id", "asset_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await snykApi(
          apiToken,
          "PATCH",
          `/groups/${args.group_id}/inventory/assets/${args.asset_id}`,
          version,
          body,
        );

        const handle = await context.writeResource(
          "asset_group",
          String(args.asset_id),
          result,
        );
        context.logger.info("Updated asset_group", {});
        return { dataHandles: [handle] };
      },
    },
    list_asset_projects_group: {
      description: "List projects for an asset (group scope) (Early Access)",
      arguments: z.object({
        group_id: z.string().describe("The unique identifier of the group"),
        asset_id: z.string().describe("The unique identifier of the asset"),
        canonical: z.enum(["with", "only", "none"]).optional().describe(
          "Filter projects by canonical status. - `with`: Returns all projects (canonica...",
        ),
        target_id: z.string().optional().describe(
          "Filter projects by target ID. When provided, returns only projects that belon...",
        ),
        sort: z.enum([
          "snapshot_created_at",
          "-snapshot_created_at",
          "issues",
          "-issues",
        ]).optional().describe(
          "Sort field with optional direction prefix. Prefix with `-` for descending ord...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>(["group_id", "asset_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await snykApiPaginated(
          apiToken,
          `/groups/${args.group_id}/inventory/assets/${args.asset_id}/relationships/projects`,
          version,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "asset_projects_group",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} asset_projects_group", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    list_asset_targets_group: {
      description: "List targets for an asset (group scope) (Early Access)",
      arguments: z.object({
        group_id: z.string().describe("The unique identifier of the group"),
        asset_id: z.string().describe("The unique identifier of the asset"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>(["group_id", "asset_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await snykApiPaginated(
          apiToken,
          `/groups/${args.group_id}/inventory/assets/${args.asset_id}/relationships/targets`,
          version,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "asset_targets_group",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} asset_targets_group", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    list_assets_org: {
      description:
        "List or search all assets (synchronous) - Org scope (Early Access)",
      arguments: z.object({
        filter: z.string().optional().describe(
          "RSQL filter expression for filtering results. See schema for full documentation.",
        ),
        sort: z.string().optional().describe(
          "Comma-separated sort fields. Prefix with `-` for descending order.",
        ),
        fields: z.string().optional().describe(
          "Sparse fieldsets allow clients to request only specific fields for a given re...",
        ),
        meta_count: z.enum(["with", "only"]).optional().describe(
          "Provide summary count in the response meta object when requested. When `with`...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, orgId, version } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await snykApiPaginated(
          apiToken,
          `/orgs/${orgId}/inventory/assets`,
          version,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("assets_org", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} assets_org", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    update_assets_bulk_org: {
      description: "Bulk update asset attributes - Org scope (Early Access)",
      arguments: z.object({
        data: z.array(z.unknown()),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, orgId, version } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await snykApi(
          apiToken,
          "PATCH",
          `/orgs/${orgId}/inventory/assets`,
          version,
          body,
        );

        const handle = await context.writeResource(
          "assets_bulk_org",
          "updated",
          result,
        );
        context.logger.info("Updated assets_bulk_org", {});
        return { dataHandles: [handle] };
      },
    },
    get_filter_fields_org: {
      description: "Get available filter fields - Org scope (Early Access)",
      arguments: z.object({
        asset_types: z.string().optional().describe(
          "Comma-separated list of asset types to filter the available filter fields",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, orgId, version } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await snykApiPaginated(
          apiToken,
          `/orgs/${orgId}/inventory/assets/filters`,
          version,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "get_filter_fields_org",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} get_filter_fields_org", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    get_filter_values_org: {
      description:
        "Get filter value suggestions (autocomplete) - Org scope (Early Access)",
      arguments: z.object({
        filter_id: z.string().describe(
          "The UUID of the filter field to get values for (from the filter fields list e...",
        ),
        q: z.string().optional().describe(
          "Full text search term to filter the list of values. If keys_only is true, thi...",
        ),
        keys_only: z.boolean().optional().describe(
          "Return only the keys of the object filter values",
        ),
        key: z.string().optional().describe(
          "Return only the value for a specific key of the object filter values",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, orgId, version } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>(["filter_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await snykApiPaginated(
          apiToken,
          `/orgs/${orgId}/inventory/assets/filters/${args.filter_id}/values`,
          version,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "get_filter_values_org",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} get_filter_values_org", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    get_group_fields_org: {
      description: "Get available group fields - Org scope (Early Access)",
      arguments: z.object({
        asset_types: z.string().optional().describe(
          "Comma-separated list of asset types to filter group fields",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, orgId, version } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await snykApiPaginated(
          apiToken,
          `/orgs/${orgId}/inventory/assets/groups`,
          version,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "get_group_fields_org",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} get_group_fields_org", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    get_group_values_org: {
      description: "Get group value aggregation - Org scope (Early Access)",
      arguments: z.object({
        group_field_id: z.string().describe(
          "The UUID of the group field to get values for (from the group fields list end...",
        ),
        asset_types: z.string().optional().describe(
          "Comma-separated list of asset types to filter the aggregation",
        ),
        filter: z.string().optional().describe(
          "RSQL filter expression for filtering which assets are included in aggregation...",
        ),
        sort: z.string().optional().describe(
          "Comma-separated sort fields for group values. Prefix with `-` for descending ...",
        ),
        meta_fields: z.string().optional().describe(
          "Meta fields to include in the response. Multiple fields can be specified. Ava...",
        ),
        aggregate: z.string().optional().describe(
          "Per-field aggregate function override for meta fields. All fields default to ...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, orgId, version } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>(["group_field_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await snykApiPaginated(
          apiToken,
          `/orgs/${orgId}/inventory/assets/groups/${args.group_field_id}/values`,
          version,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "get_group_values_org",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} get_group_values_org", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_asset_search_org: {
      description:
        "Create an asset search (asynchronous) - Org scope (Early Access)",
      arguments: z.object({
        data: z.object({
          attributes: z.object({
            filter: z.unknown().optional(),
            limit: z.number().int().min(10).max(100).optional(),
            meta_count: z.enum(["with", "only"]).optional(),
            sort: z.string().optional(),
          }).optional(),
          type: z.enum(["searches"]).optional(),
        }).optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, orgId, version } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await snykApi(
          apiToken,
          "POST",
          `/orgs/${orgId}/inventory/assets/searches`,
          version,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "asset_search_org",
          id,
          result,
        );
        context.logger.info("Created asset_search_org {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_asset_search_results_org: {
      description:
        "Retrieve asset search results (asynchronous) - Org scope (Early Access)",
      arguments: z.object({
        search_id: z.string().describe(
          "The unique identifier of the search operation",
        ),
        sort: z.string().optional().describe(
          "Sort order for results (e.g., -created_at for descending)",
        ),
        fields: z.string().optional().describe(
          "Sparse fieldsets allow clients to request only specific fields for a given re...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, orgId, version } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>(["search_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await snykApiPaginated(
          apiToken,
          `/orgs/${orgId}/inventory/assets/searches/${args.search_id}/results`,
          version,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "get_asset_search_results_org",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} get_asset_search_results_org", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    get_asset_org: {
      description: "Get a single asset by ID - Org scope (Early Access)",
      arguments: z.object({
        asset_id: z.string().describe("The unique identifier of the asset"),
        fields: z.string().optional().describe(
          "Sparse fieldsets allow clients to request only specific fields for a given re...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, orgId, version } = context.globalArgs;
        const queryParts: string[] = [];
        const excludeKeys = new Set<string>(["asset_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) {
            queryParts.push(
              `${encodeURIComponent(k)}=${encodeURIComponent(String(v))}`,
            );
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await snykApi(
          apiToken,
          "GET",
          `/orgs/${orgId}/inventory/assets/${args.asset_id}${qs}`,
          version,
        );

        const handle = await context.writeResource(
          "asset_org",
          String(args.asset_id),
          result,
        );
        context.logger.info("Fetched asset_org", {});
        return { dataHandles: [handle] };
      },
    },
    update_asset_org: {
      description: "Update asset attributes - Org scope (Early Access)",
      arguments: z.object({
        asset_id: z.string().describe("The unique identifier of the asset"),
        data: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, orgId, version } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>(["asset_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await snykApi(
          apiToken,
          "PATCH",
          `/orgs/${orgId}/inventory/assets/${args.asset_id}`,
          version,
          body,
        );

        const handle = await context.writeResource(
          "asset_org",
          String(args.asset_id),
          result,
        );
        context.logger.info("Updated asset_org", {});
        return { dataHandles: [handle] };
      },
    },
    list_asset_projects_org: {
      description: "List projects for an asset (org scope) (Early Access)",
      arguments: z.object({
        asset_id: z.string().describe("The unique identifier of the asset"),
        canonical: z.enum(["with", "only", "none"]).optional().describe(
          "Filter projects by canonical status. - `with`: Returns all projects (canonica...",
        ),
        target_id: z.string().optional().describe(
          "Filter projects by target ID. When provided, returns only projects that belon...",
        ),
        sort: z.enum([
          "snapshot_created_at",
          "-snapshot_created_at",
          "issues",
          "-issues",
        ]).optional().describe(
          "Sort field with optional direction prefix. Prefix with `-` for descending ord...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, orgId, version } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>(["asset_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await snykApiPaginated(
          apiToken,
          `/orgs/${orgId}/inventory/assets/${args.asset_id}/relationships/projects`,
          version,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "asset_projects_org",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} asset_projects_org", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    list_asset_targets_org: {
      description: "List targets for an asset (org scope) (Early Access)",
      arguments: z.object({
        asset_id: z.string().describe("The unique identifier of the asset"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, orgId, version } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>(["asset_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await snykApiPaginated(
          apiToken,
          `/orgs/${orgId}/inventory/assets/${args.asset_id}/relationships/targets`,
          version,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "asset_targets_org",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} asset_targets_org", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
  },
};
