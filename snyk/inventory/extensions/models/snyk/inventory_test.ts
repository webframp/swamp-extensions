// Snyk Inventory — asset discovery for packages, containers, repos, and cloud resources — Tests
// Auto-generated by scripts/snyk-codegen — do not edit manually.
// SPDX-License-Identifier: Apache-2.0

import {
  assertEquals,
  assertExists,
  assertStringIncludes,
} from "jsr:@std/assert@1.0.19";
import { createModelTestContext } from "@systeminit/swamp-testing";
import { model } from "./inventory.ts";

// ---------------------------------------------------------------------------
// Model Export Structure Tests
// ---------------------------------------------------------------------------

Deno.test("inventory model: has correct type", () => {
  assertEquals(model.type, "@webframp/snyk/inventory");
});

Deno.test("inventory model: has valid version format", () => {
  const versionPattern = /^\d{4}\.\d{2}\.\d{2}\.\d+$/;
  assertEquals(versionPattern.test(model.version), true);
});

Deno.test("inventory model: has globalArguments with apiToken", () => {
  assertExists(model.globalArguments);
  const shape = model.globalArguments.shape;
  assertExists(shape.apiToken);
  assertExists(shape.orgId);
  assertExists(shape.version);
});

Deno.test("inventory model: has expected methods", () => {
  assertExists(model.methods);
  assertExists(model.methods.list_assets_group);
  assertExists(model.methods.update_assets_bulk_group);
  assertExists(model.methods.get_filter_fields_group);
  assertExists(model.methods.get_filter_values_group);
  assertExists(model.methods.get_group_fields_group);
  assertExists(model.methods.get_group_values_group);
  assertExists(model.methods.create_asset_search_group);
  assertExists(model.methods.get_asset_search_results_group);
  assertExists(model.methods.get_asset_group);
  assertExists(model.methods.update_asset_group);
  assertExists(model.methods.list_asset_projects_group);
  assertExists(model.methods.list_asset_targets_group);
  assertExists(model.methods.list_assets_org);
  assertExists(model.methods.update_assets_bulk_org);
  assertExists(model.methods.get_filter_fields_org);
  assertExists(model.methods.get_filter_values_org);
  assertExists(model.methods.get_group_fields_org);
  assertExists(model.methods.get_group_values_org);
  assertExists(model.methods.create_asset_search_org);
  assertExists(model.methods.get_asset_search_results_org);
  assertExists(model.methods.get_asset_org);
  assertExists(model.methods.update_asset_org);
  assertExists(model.methods.list_asset_projects_org);
  assertExists(model.methods.list_asset_targets_org);
});

Deno.test("inventory model: has expected resources", () => {
  assertExists(model.resources);
  assertExists(model.resources["assets_group"]);
  assertExists(model.resources["assets_bulk_group"]);
  assertExists(model.resources["get_filter_fields_group"]);
  assertExists(model.resources["get_filter_values_group"]);
  assertExists(model.resources["get_group_fields_group"]);
  assertExists(model.resources["get_group_values_group"]);
  assertExists(model.resources["asset_search_group"]);
  assertExists(model.resources["get_asset_search_results_group"]);
  assertExists(model.resources["asset_group"]);
  assertExists(model.resources["asset_projects_group"]);
  assertExists(model.resources["asset_targets_group"]);
  assertExists(model.resources["assets_org"]);
  assertExists(model.resources["assets_bulk_org"]);
  assertExists(model.resources["get_filter_fields_org"]);
  assertExists(model.resources["get_filter_values_org"]);
  assertExists(model.resources["get_group_fields_org"]);
  assertExists(model.resources["get_group_values_org"]);
  assertExists(model.resources["asset_search_org"]);
  assertExists(model.resources["get_asset_search_results_org"]);
  assertExists(model.resources["asset_org"]);
  assertExists(model.resources["asset_projects_org"]);
  assertExists(model.resources["asset_targets_org"]);
});

// ---------------------------------------------------------------------------
// Mock Snyk API Server
// ---------------------------------------------------------------------------

interface CapturedRequest {
  method: string;
  path: string;
  search: string;
  headers: Record<string, string>;
  body: unknown;
}

function startMockSnykServer(
  responses: Record<string, { data: unknown; isCollection?: boolean }>,
): { url: string; server: Deno.HttpServer; requests: CapturedRequest[] } {
  const requests: CapturedRequest[] = [];
  const server = Deno.serve({ port: 0, onListen() {} }, async (req) => {
    const url = new URL(req.url);
    const path = url.pathname;

    // Capture the request (headers, method, path, query, parsed body).
    let reqBody: unknown = null;
    if (req.body) {
      const text = await req.text();
      if (text) {
        try {
          reqBody = JSON.parse(text);
        } catch {
          reqBody = text;
        }
      }
    }
    requests.push({
      method: req.method,
      path,
      search: url.search,
      headers: Object.fromEntries(req.headers),
      body: reqBody,
    });

    for (const [pattern, { data, isCollection }] of Object.entries(responses)) {
      if (path.includes(pattern)) {
        const body = isCollection
          ? {
            data: Array.isArray(data) ? data : [data],
            jsonapi: { version: "1.0" },
            links: { self: path, next: null },
          }
          : {
            data,
            jsonapi: { version: "1.0" },
            links: { self: path },
          };
        return Response.json(body, {
          headers: { "content-type": "application/vnd.api+json" },
        });
      }
    }

    return new Response(
      JSON.stringify({
        jsonapi: { version: "1.0" },
        errors: [{ status: "404", detail: "Not found" }],
      }),
      { status: 404, headers: { "content-type": "application/vnd.api+json" } },
    );
  });

  const addr = server.addr as Deno.NetAddr;
  return { url: `http://localhost:${addr.port}`, server, requests };
}

function installFetchMock(mockUrl: string): () => void {
  const originalFetch = globalThis.fetch;
  globalThis.fetch = (input, init) => {
    const reqUrl = typeof input === "string" ? input : input.toString();
    const newUrl = reqUrl.replace("https://api.snyk.io/rest", mockUrl);
    return originalFetch(newUrl, init);
  };
  return () => {
    globalThis.fetch = originalFetch;
  };
}

Deno.test({
  name: "inventory model: list_assets_group fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockItem = {
      "id": "fixture-123",
      "type": "resource",
      "attributes": { "projects_id": "test-value", "targets_id": "test-value" },
    };
    const { url, server, requests } = startMockSnykServer({
      "/groups/test-id-123/inventory/assets": {
        data: [mockItem],
        isCollection: true,
      },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: {
          "apiToken": "test-token",
          "version": "2024-10-15",
          "orgId": "test-org-123",
        },
        definition: {
          id: "test-id",
          name: "test-inventory",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).list_assets_group.execute({ "group_id": "test-id-123" }, context);
      assertEquals(result.dataHandles.length, 1);

      assertEquals(requests.length, 1);
      const req0 = requests[0];
      assertEquals(req0.method, "GET");
      assertStringIncludes(req0.path, "/groups/test-id-123/inventory/assets");
      assertStringIncludes(req0.search, "version=");
      assertEquals(req0.headers["authorization"], "token test-token");

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
      assertEquals(resources[0].specName, "assets_group");
      const data = resources[0].data as {
        items: unknown[];
        truncated: boolean;
      };
      assertEquals(Array.isArray(data.items), true);
      assertEquals(data.items.length, 1);
      assertEquals(typeof data.truncated, "boolean");
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name:
    "inventory model: update_assets_bulk_group executes and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = { "id": "fixture-123", "type": "resource" };
    const { url, server, requests } = startMockSnykServer({
      "/groups/test-id-123/inventory/assets": { data: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: {
          "apiToken": "test-token",
          "version": "2024-10-15",
          "orgId": "test-org-123",
        },
        definition: {
          id: "test-id",
          name: "test-inventory",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).update_assets_bulk_group.execute({
        "group_id": "test-id-123",
        "name": "test-resource",
      }, context);
      assertEquals(result.dataHandles.length, 1);

      assertEquals(requests.length, 1);
      const req0 = requests[0];
      assertEquals(req0.method, "PATCH");
      assertStringIncludes(req0.path, "/groups/test-id-123/inventory/assets");
      assertStringIncludes(req0.search, "version=");
      assertEquals(req0.headers["authorization"], "token test-token");
      assertEquals(req0.headers["content-type"], "application/vnd.api+json");
      assertExists(req0.body);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
      assertEquals(resources[0].specName, "assets_bulk_group");
      assertEquals(resources[0].name, "test-id-123");
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name:
    "inventory model: create_asset_search_group creates and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = { "id": "new-123", "type": "resource" };
    const { url, server, requests } = startMockSnykServer({
      "/groups/test-id-123/inventory/assets/searches": { data: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: {
          "apiToken": "test-token",
          "version": "2024-10-15",
          "orgId": "test-org-123",
        },
        definition: {
          id: "test-id",
          name: "test-inventory",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).create_asset_search_group.execute({
        "group_id": "test-id-123",
        "name": "test-resource",
      }, context);
      assertEquals(result.dataHandles.length, 1);

      assertEquals(requests.length, 1);
      const req0 = requests[0];
      assertEquals(req0.method, "POST");
      assertStringIncludes(
        req0.path,
        "/groups/test-id-123/inventory/assets/searches",
      );
      assertStringIncludes(req0.search, "version=");
      assertEquals(req0.headers["authorization"], "token test-token");
      assertEquals(req0.headers["content-type"], "application/vnd.api+json");
      assertExists(req0.body);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
      assertEquals(resources[0].specName, "asset_search_group");
      assertEquals(resources[0].name, "new-123");
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "inventory model: get_asset_group fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "id": "fixture-123",
      "type": "resource",
      "attributes": { "projects_id": "test-value", "targets_id": "test-value" },
    };
    const { url, server, requests } = startMockSnykServer({
      "/groups/test-id-123/inventory/assets/test-id-123": { data: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: {
          "apiToken": "test-token",
          "version": "2024-10-15",
          "orgId": "test-org-123",
        },
        definition: {
          id: "test-id",
          name: "test-inventory",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).get_asset_group.execute({
        "group_id": "test-id-123",
        "asset_id": "test-id-123",
      }, context);
      assertEquals(result.dataHandles.length, 1);

      assertEquals(requests.length, 1);
      const req0 = requests[0];
      assertEquals(req0.method, "GET");
      assertStringIncludes(
        req0.path,
        "/groups/test-id-123/inventory/assets/test-id-123",
      );
      assertStringIncludes(req0.search, "version=");
      assertEquals(req0.headers["authorization"], "token test-token");

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
      assertEquals(resources[0].specName, "asset_group");
      assertEquals(resources[0].name, "test-id-123");
      const data = resources[0].data as Record<string, unknown>;
      assertEquals(data.id, "fixture-123");
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "inventory model: list_assets_group surfaces API errors",
  sanitizeResources: false,
  fn: async () => {
    // A server that returns 500 for every request, exercising the error path.
    const originalFetch = globalThis.fetch;
    const server = Deno.serve({ port: 0, onListen() {} }, () =>
      new Response(
        JSON.stringify({
          jsonapi: { version: "1.0" },
          errors: [{ status: "500", detail: "Internal Server Error" }],
        }),
        {
          status: 500,
          headers: { "content-type": "application/vnd.api+json" },
        },
      ));
    const addr = server.addr as Deno.NetAddr;
    const mockUrl = `http://localhost:${addr.port}`;
    globalThis.fetch = (input, init) => {
      const reqUrl = typeof input === "string" ? input : input.toString();
      return originalFetch(
        reqUrl.replace("https://api.snyk.io/rest", mockUrl),
        init,
      );
    };

    try {
      const { context } = createModelTestContext({
        globalArgs: {
          "apiToken": "test-token",
          "version": "2024-10-15",
          "orgId": "test-org-123",
        },
        definition: {
          id: "test-id",
          name: "test-inventory",
          version: 1,
          tags: {},
        },
      });

      let threw = false;
      try {
        await (model.methods as Record<
          string,
          {
            execute: (
              args: Record<string, unknown>,
              ctx: unknown,
            ) => Promise<{ dataHandles: unknown[] }>;
          }
        >).list_assets_group.execute({ "group_id": "test-id-123" }, context);
      } catch (_err) {
        threw = true;
      }
      assertEquals(threw, true);
    } finally {
      globalThis.fetch = originalFetch;
      await server.shutdown();
    }
  },
});
