// Snyk Cloud — cloud environments, scans, and resource posture management — Tests
// Auto-generated by scripts/snyk-codegen — do not edit manually.
// SPDX-License-Identifier: Apache-2.0

import { assertEquals, assertExists } from "jsr:@std/assert@1";
import { createModelTestContext } from "@systeminit/swamp-testing";
import { model } from "./cloud.ts";

// ---------------------------------------------------------------------------
// Model Export Structure Tests
// ---------------------------------------------------------------------------

Deno.test("cloud model: has correct type", () => {
  assertEquals(model.type, "@webframp/snyk/cloud");
});

Deno.test("cloud model: has valid version format", () => {
  const versionPattern = /^\d{4}\.\d{2}\.\d{2}\.\d+$/;
  assertEquals(versionPattern.test(model.version), true);
});

Deno.test("cloud model: has globalArguments with apiToken", () => {
  assertExists(model.globalArguments);
  const shape = model.globalArguments.shape;
  assertExists(shape.apiToken);
  assertExists(shape.orgId);
  assertExists(shape.version);
});

Deno.test("cloud model: has expected methods", () => {
  assertExists(model.methods);
  assertExists(model.methods.list_environments);
  assertExists(model.methods.create_environment);
  assertExists(model.methods.update_environment);
  assertExists(model.methods.delete_environment);
  assertExists(model.methods.get_permissions);
  assertExists(model.methods.list_resources);
  assertExists(model.methods.list_scan);
  assertExists(model.methods.create_scan);
  assertExists(model.methods.get_scan);
});

Deno.test("cloud model: has expected resources", () => {
  assertExists(model.resources);
  assertExists(model.resources["environments"]);
  assertExists(model.resources["environment"]);
  assertExists(model.resources["permissions"]);
  assertExists(model.resources["resources"]);
  assertExists(model.resources["scan"]);
});

// ---------------------------------------------------------------------------
// Mock Snyk API Server
// ---------------------------------------------------------------------------

function startMockSnykServer(
  responses: Record<string, { data: unknown; isCollection?: boolean }>,
): { url: string; server: Deno.HttpServer } {
  const server = Deno.serve({ port: 0, onListen() {} }, (req) => {
    const url = new URL(req.url);
    const path = url.pathname;

    for (const [pattern, { data, isCollection }] of Object.entries(responses)) {
      if (path.includes(pattern)) {
        const body = isCollection
          ? {
            data: Array.isArray(data) ? data : [data],
            jsonapi: { version: "1.0" },
            links: { self: path, next: null },
          }
          : {
            data,
            jsonapi: { version: "1.0" },
            links: { self: path },
          };
        return Response.json(body, {
          headers: { "content-type": "application/vnd.api+json" },
        });
      }
    }

    return new Response(
      JSON.stringify({
        jsonapi: { version: "1.0" },
        errors: [{ status: "404", detail: "Not found" }],
      }),
      { status: 404, headers: { "content-type": "application/vnd.api+json" } },
    );
  });

  const addr = server.addr as Deno.NetAddr;
  return { url: `http://localhost:${addr.port}`, server };
}

function installFetchMock(mockUrl: string): () => void {
  const originalFetch = globalThis.fetch;
  globalThis.fetch = (input, init) => {
    const reqUrl = typeof input === "string" ? input : input.toString();
    const newUrl = reqUrl.replace("https://api.snyk.io/rest", mockUrl);
    return originalFetch(newUrl, init);
  };
  return () => {
    globalThis.fetch = originalFetch;
  };
}

Deno.test({
  name: "cloud model: list_environments fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockItem = {
      "id": "fixture-123",
      "type": "resource",
      "attributes": {
        "created_at": "2022-05-06T12:25:15-04:00",
        "deleted_at": "2024-01-01T00:00:00Z",
        "kind": "aws",
        "name": "Demo AWS Environment",
        "options": {},
        "properties": {},
        "revision": 1,
        "updated_at": "2022-05-07T12:25:15-04:00",
      },
    };
    const { url, server } = startMockSnykServer({
      "/cloud/environments": { data: [mockItem], isCollection: true },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: {
          "apiToken": "test-token",
          "version": "2024-10-15",
          "orgId": "test-org-123",
        },
        definition: { id: "test-id", name: "test-cloud", version: 1, tags: {} },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).list_environments.execute({}, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "cloud model: create_environment creates and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "id": "new-123",
      "type": "resource",
      "attributes": {
        "created_at": "2022-05-06T12:25:15-04:00",
        "deleted_at": "2024-01-01T00:00:00Z",
        "kind": "aws",
        "name": "Demo AWS Environment",
        "options": {},
        "properties": {},
        "revision": 1,
        "updated_at": "2022-05-07T12:25:15-04:00",
      },
    };
    const { url, server } = startMockSnykServer({
      "/cloud/environments": { data: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: {
          "apiToken": "test-token",
          "version": "2024-10-15",
          "orgId": "test-org-123",
        },
        definition: { id: "test-id", name: "test-cloud", version: 1, tags: {} },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).create_environment.execute({ "name": "test-resource" }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "cloud model: update_environment executes and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "id": "fixture-123",
      "type": "resource",
      "attributes": {
        "created_at": "2022-05-06T12:25:15-04:00",
        "deleted_at": "2024-01-01T00:00:00Z",
        "kind": "aws",
        "name": "Demo AWS Environment",
        "options": {},
        "properties": {},
        "revision": 1,
        "updated_at": "2022-05-07T12:25:15-04:00",
      },
    };
    const { url, server } = startMockSnykServer({
      "/cloud/environments/test-id-123": { data: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: {
          "apiToken": "test-token",
          "version": "2024-10-15",
          "orgId": "test-org-123",
        },
        definition: { id: "test-id", name: "test-cloud", version: 1, tags: {} },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).update_environment.execute({
        "environment_id": "test-id-123",
        "name": "test-resource",
      }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "cloud model: delete_environment executes successfully",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockSnykServer({
      "/cloud/environments/test-id-123": {
        data: { id: "test-id-123", type: "resource" },
      },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context } = createModelTestContext({
        globalArgs: {
          "apiToken": "test-token",
          "version": "2024-10-15",
          "orgId": "test-org-123",
        },
        definition: { id: "test-id", name: "test-cloud", version: 1, tags: {} },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).delete_environment.execute(
        { "environment_id": "test-id-123" },
        context,
      );
      assertEquals(result.dataHandles.length, 0);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "cloud model: get_scan fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "id": "fixture-123",
      "type": "resource",
      "attributes": {
        "created_at": "2022-05-06T12:25:15-04:00",
        "deleted_at": "2024-01-01T00:00:00Z",
        "environment_id": "052781a7-17f6-494d-0000-25c8b509abcd",
        "error": "could not start scan",
        "finished_at": "2022-05-06T12:25:15-04:00",
        "kind": "user_initiated",
        "options": {
          "role_arn": "arn:aws:iam::123456789012:role/SnykCloud1234",
        },
        "organization_id": "9a46d918-8764-458c-1234-0987abcd6543",
        "partial_errors": "test-value",
        "revision": 1,
        "status": "in_progress",
        "updated_at": "2022-05-06T12:25:15-04:00",
      },
    };
    const { url, server } = startMockSnykServer({
      "/cloud/scans/test-id-123": { data: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: {
          "apiToken": "test-token",
          "version": "2024-10-15",
          "orgId": "test-org-123",
        },
        definition: { id: "test-id", name: "test-cloud", version: 1, tags: {} },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).get_scan.execute({ "scan_id": "test-id-123" }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});
