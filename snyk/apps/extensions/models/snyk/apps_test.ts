// Snyk Apps — OAuth application management, bots, installations — Tests
// Auto-generated by scripts/snyk-codegen — do not edit manually.
// SPDX-License-Identifier: Apache-2.0

import { assertEquals, assertExists } from "jsr:@std/assert@1";
import { createModelTestContext } from "@systeminit/swamp-testing";
import { model } from "./apps.ts";

// ---------------------------------------------------------------------------
// Model Export Structure Tests
// ---------------------------------------------------------------------------

Deno.test("apps model: has correct type", () => {
  assertEquals(model.type, "@webframp/snyk/apps");
});

Deno.test("apps model: has valid version format", () => {
  const versionPattern = /^\d{4}\.\d{2}\.\d{2}\.\d+$/;
  assertEquals(versionPattern.test(model.version), true);
});

Deno.test("apps model: has globalArguments with apiToken", () => {
  assertExists(model.globalArguments);
  const shape = model.globalArguments.shape;
  assertExists(shape.apiToken);
  assertExists(shape.orgId);
  assertExists(shape.version);
});

Deno.test("apps model: has expected methods", () => {
  assertExists(model.methods);
  assertExists(model.methods.get_app_installs_for_group);
  assertExists(model.methods.create_group_app_install);
  assertExists(model.methods.delete_group_app_install_by_id);
  assertExists(model.methods.update_group_app_install_secret);
  assertExists(model.methods.get_org_apps);
  assertExists(model.methods.create_org_app);
  assertExists(model.methods.get_app_by_id);
  assertExists(model.methods.update_app_creation_by_id);
  assertExists(model.methods.delete_app_by_id);
  assertExists(model.methods.create_manage_app_creation_secret);
  assertExists(model.methods.get_app_installs_for_org);
  assertExists(model.methods.create_org_app_install);
  assertExists(model.methods.delete_app_org_install_by_id);
  assertExists(model.methods.update_org_app_install_secret);
});

Deno.test("apps model: has expected resources", () => {
  assertExists(model.resources);
  assertExists(model.resources["get_app_installs_for_group"]);
  assertExists(model.resources["group_app_install"]);
  assertExists(model.resources["group_app_install_secret"]);
  assertExists(model.resources["get_org_apps"]);
  assertExists(model.resources["org_app"]);
  assertExists(model.resources["app_by_id"]);
  assertExists(model.resources["app_creation_by_id"]);
  assertExists(model.resources["manage_app_creation_secret"]);
  assertExists(model.resources["get_app_installs_for_org"]);
  assertExists(model.resources["org_app_install"]);
  assertExists(model.resources["org_app_install_secret"]);
});

// ---------------------------------------------------------------------------
// Mock Snyk API Server
// ---------------------------------------------------------------------------

function startMockSnykServer(
  responses: Record<string, { data: unknown; isCollection?: boolean }>,
): { url: string; server: Deno.HttpServer } {
  const server = Deno.serve({ port: 0, onListen() {} }, (req) => {
    const url = new URL(req.url);
    const path = url.pathname;

    for (const [pattern, { data, isCollection }] of Object.entries(responses)) {
      if (path.includes(pattern)) {
        const body = isCollection
          ? {
            data: Array.isArray(data) ? data : [data],
            jsonapi: { version: "1.0" },
            links: { self: path, next: null },
          }
          : {
            data,
            jsonapi: { version: "1.0" },
            links: { self: path },
          };
        return Response.json(body, {
          headers: { "content-type": "application/vnd.api+json" },
        });
      }
    }

    return new Response(
      JSON.stringify({
        jsonapi: { version: "1.0" },
        errors: [{ status: "404", detail: "Not found" }],
      }),
      { status: 404, headers: { "content-type": "application/vnd.api+json" } },
    );
  });

  const addr = server.addr as Deno.NetAddr;
  return { url: `http://localhost:${addr.port}`, server };
}

function installFetchMock(mockUrl: string): () => void {
  const originalFetch = globalThis.fetch;
  globalThis.fetch = (input, init) => {
    const reqUrl = typeof input === "string" ? input : input.toString();
    const newUrl = reqUrl.replace("https://api.snyk.io/rest", mockUrl);
    return originalFetch(newUrl, init);
  };
  return () => {
    globalThis.fetch = originalFetch;
  };
}

Deno.test({
  name: "apps model: get_app_installs_for_group fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockItem = {
      "id": "fixture-123",
      "type": "resource",
      "attributes": {
        "client_id": "941b423a-e0a0-4a33-a7ca-dd9e9e6bd8cf",
        "installed_at": "2024-04-30T16:07:46.230044Z",
        "app_id": "test-value",
      },
    };
    const { url, server } = startMockSnykServer({
      "/groups/test-id-123/apps/installs": {
        data: [mockItem],
        isCollection: true,
      },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: {
          "apiToken": "test-token",
          "version": "2024-10-15",
          "orgId": "test-org-123",
        },
        definition: { id: "test-id", name: "test-apps", version: 1, tags: {} },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).get_app_installs_for_group.execute(
        { "group_id": "test-id-123" },
        context,
      );
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "apps model: create_group_app_install creates and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "id": "new-123",
      "type": "resource",
      "attributes": {
        "client_id": "test-value",
        "client_secret": "test-value",
        "app_id": "test-value",
      },
    };
    const { url, server } = startMockSnykServer({
      "/groups/test-id-123/apps/installs": { data: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: {
          "apiToken": "test-token",
          "version": "2024-10-15",
          "orgId": "test-org-123",
        },
        definition: { id: "test-id", name: "test-apps", version: 1, tags: {} },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).create_group_app_install.execute({
        "group_id": "test-id-123",
        "name": "test-resource",
      }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "apps model: delete_group_app_install_by_id executes successfully",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockSnykServer({
      "/groups/test-id-123/apps/installs/test-id-123": {
        data: { id: "test-id-123", type: "resource" },
      },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context } = createModelTestContext({
        globalArgs: {
          "apiToken": "test-token",
          "version": "2024-10-15",
          "orgId": "test-org-123",
        },
        definition: { id: "test-id", name: "test-apps", version: 1, tags: {} },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).delete_group_app_install_by_id.execute({
        "group_id": "test-id-123",
        "install_id": "test-id-123",
      }, context);
      assertEquals(result.dataHandles.length, 0);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "apps model: get_app_by_id fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "id": "fixture-123",
      "type": "resource",
      "attributes": {
        "access_token_ttl_seconds": 3600,
        "client_id": "941b423a-e0a0-4a33-a7ca-dd9e9e6bd8cf",
        "context": "tenant",
        "grant_type": "authorization_code",
        "is_confidential": true,
        "is_public": false,
        "name": "My App",
        "org_public_id": "test-value",
        "redirect_uris": [
          "https://example.com/callback",
          "https://example.com/auth/snyk/callback",
        ],
        "scopes": [],
      },
    };
    const { url, server } = startMockSnykServer({
      "/apps/creations/test-id-123": { data: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: {
          "apiToken": "test-token",
          "version": "2024-10-15",
          "orgId": "test-org-123",
        },
        definition: { id: "test-id", name: "test-apps", version: 1, tags: {} },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).get_app_by_id.execute({ "app_id": "test-id-123" }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "apps model: update_app_creation_by_id executes and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "id": "fixture-123",
      "type": "resource",
      "attributes": {
        "access_token_ttl_seconds": 3600,
        "client_id": "941b423a-e0a0-4a33-a7ca-dd9e9e6bd8cf",
        "context": "tenant",
        "grant_type": "authorization_code",
        "is_confidential": true,
        "is_public": false,
        "name": "My App",
        "org_public_id": "test-value",
        "redirect_uris": [
          "https://example.com/callback",
          "https://example.com/auth/snyk/callback",
        ],
        "scopes": [],
      },
    };
    const { url, server } = startMockSnykServer({
      "/apps/creations/test-id-123": { data: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: {
          "apiToken": "test-token",
          "version": "2024-10-15",
          "orgId": "test-org-123",
        },
        definition: { id: "test-id", name: "test-apps", version: 1, tags: {} },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).update_app_creation_by_id.execute({
        "app_id": "test-id-123",
        "name": "test-resource",
      }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});
