/**
 * Snyk Apps — OAuth application management, bots, installations
 *
 * Auto-generated by scripts/snyk-codegen — do not edit manually.
 *
 * @module
 */
// SPDX-License-Identifier: AGPL-3.0-or-later WITH Swamp-Extension-Exception

import { z } from "npm:zod@4.4.3";
import { snykApi, snykApiPaginated } from "./_lib/api.ts";

// =============================================================================
// Schemas
// =============================================================================

const GlobalArgsSchema = z.object({
  apiToken: z.string().meta({ sensitive: true }).describe("Snyk API token"),
  orgId: z.string().describe("Snyk organization ID"),
  version: z.string().default("2024-10-15").describe("Snyk API version date"),
});

const GetAppInstallsForGroupItemSchema = z.object({
  id: z.string(),
  type: z.string().optional(),
  client_id: z.string().optional().describe(
    "The OAuth2 client id for the app installation. Only provided for installations of non-interactive...",
  ),
  installed_at: z.string().optional().describe(
    "Timestamp at which this app was first installed at.",
  ),
  app_id: z.string().optional().describe("Related app ID"),
});

const GetAppInstallsForGroupSchema = z.object({
  items: z.array(GetAppInstallsForGroupItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateGroupAppInstallSchema = z.object({
  id: z.string(),
  type: z.string().optional(),
  client_id: z.string(),
  client_secret: z.string(),
  app_id: z.string().optional().describe("Related app ID"),
});

const UpdateGroupAppInstallSecretSchema = z.object({
  id: z.string(),
  type: z.string().optional(),
  client_id: z.string().describe(
    "The OAuth2 client id for the app installation. Only provided for installations of non-interactive...",
  ),
  client_secret: z.string().min(1).describe(
    "The OAuth2 client secret for the app. This is the only time this secret will be returned, store i...",
  ),
  installed_at: z.string().optional().describe(
    "Timestamp at which this app was first installed at.",
  ),
  app_id: z.string().optional().describe("Related app ID"),
});

const GetOrgAppsItemSchema = z.object({
  id: z.string(),
  type: z.string().optional(),
  access_token_ttl_seconds: z.number().min(3600).max(86400).describe(
    "The access token time to live for your app, in seconds. It only affects the newly generated acces...",
  ),
  client_id: z.string().optional().describe(
    "The oauth2 client id for the app.",
  ),
  context: z.enum(["tenant", "user"]).describe(
    "Allow installing the app to at org/group level or user level. Defaults to tenant.",
  ),
  grant_type: z.enum(["authorization_code", "client_credentials"]).describe(
    "An authorization grant is a credential representing the resource owner's authorization (to access...",
  ),
  is_confidential: z.boolean().describe(
    "A boolean to indicate if an app is confidential or not as per the OAuth2 RFC. Confidential apps c...",
  ),
  is_public: z.boolean().describe(
    "A boolean to indicate if an app is publicly available or not.",
  ),
  name: z.string().min(1).describe(
    "New name of the app to display to users during authorization flow.",
  ),
  org_public_id: z.string().optional(),
  redirect_uris: z.array(z.string()).optional().describe(
    "List of allowed redirect URIs (OAuth callbacks) where Snyk will redirect users after authorizatio...",
  ),
  scopes: z.array(z.string().min(1)).describe(
    "The scopes this app is allowed to request during authorization.",
  ),
});

const GetOrgAppsSchema = z.object({
  items: z.array(GetOrgAppsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateOrgAppSchema = z.object({
  id: z.string(),
  type: z.string().optional(),
  access_token_ttl_seconds: z.number().min(3600).max(86400).describe(
    "The access token time to live for your app, in seconds. It only affects the newly generated acces...",
  ),
  client_id: z.string().describe("The oauth2 client id for the app."),
  client_secret: z.string().min(1).describe(
    "The oauth2 client secret for the app. This is the only time this secret will be returned, store i...",
  ),
  context: z.enum(["tenant", "user"]).describe(
    "Allow installing the app to at org/group level or user level. Defaults to tenant.",
  ),
  grant_type: z.enum(["authorization_code", "client_credentials"]).describe(
    "An authorization grant is a credential representing the resource owner's authorization (to access...",
  ),
  is_confidential: z.boolean().describe(
    "A boolean to indicate if an app is confidential or not as per the OAuth2 RFC. Confidential apps c...",
  ),
  is_public: z.boolean().describe(
    "A boolean to indicate if an app is publicly available or not.",
  ),
  name: z.string().min(1).describe(
    "New name of the app to display to users during authorization flow.",
  ),
  org_public_id: z.string().optional(),
  redirect_uris: z.array(z.string()).describe(
    "List of allowed redirect URIs (OAuth callbacks) where Snyk will redirect users after authorizatio...",
  ),
  scopes: z.array(z.string().min(1)).describe(
    "The scopes this app is allowed to request during authorization.",
  ),
});

const GetAppByIdSchema = z.object({
  id: z.string(),
  type: z.string().optional(),
  access_token_ttl_seconds: z.number().min(3600).max(86400).describe(
    "The access token time to live for your app, in seconds. It only affects the newly generated acces...",
  ),
  client_id: z.string().optional().describe(
    "The oauth2 client id for the app.",
  ),
  context: z.enum(["tenant", "user"]).describe(
    "Allow installing the app to at org/group level or user level. Defaults to tenant.",
  ),
  grant_type: z.enum(["authorization_code", "client_credentials"]).describe(
    "An authorization grant is a credential representing the resource owner's authorization (to access...",
  ),
  is_confidential: z.boolean().describe(
    "A boolean to indicate if an app is confidential or not as per the OAuth2 RFC. Confidential apps c...",
  ),
  is_public: z.boolean().describe(
    "A boolean to indicate if an app is publicly available or not.",
  ),
  name: z.string().min(1).describe(
    "New name of the app to display to users during authorization flow.",
  ),
  org_public_id: z.string().optional(),
  redirect_uris: z.array(z.string()).optional().describe(
    "List of allowed redirect URIs (OAuth callbacks) where Snyk will redirect users after authorizatio...",
  ),
  scopes: z.array(z.string().min(1)).describe(
    "The scopes this app is allowed to request during authorization.",
  ),
});

const UpdateAppCreationByIdSchema = z.object({
  id: z.string(),
  type: z.string().optional(),
  access_token_ttl_seconds: z.number().min(3600).max(86400).describe(
    "The access token time to live for your app, in seconds. It only affects the newly generated acces...",
  ),
  client_id: z.string().optional().describe(
    "The oauth2 client id for the app.",
  ),
  context: z.enum(["tenant", "user"]).describe(
    "Allow installing the app to at org/group level or user level. Defaults to tenant.",
  ),
  grant_type: z.enum(["authorization_code", "client_credentials"]).describe(
    "An authorization grant is a credential representing the resource owner's authorization (to access...",
  ),
  is_confidential: z.boolean().describe(
    "A boolean to indicate if an app is confidential or not as per the OAuth2 RFC. Confidential apps c...",
  ),
  is_public: z.boolean().describe(
    "A boolean to indicate if an app is publicly available or not.",
  ),
  name: z.string().min(1).describe(
    "New name of the app to display to users during authorization flow.",
  ),
  org_public_id: z.string().optional(),
  redirect_uris: z.array(z.string()).optional().describe(
    "List of allowed redirect URIs (OAuth callbacks) where Snyk will redirect users after authorizatio...",
  ),
  scopes: z.array(z.string().min(1)).describe(
    "The scopes this app is allowed to request during authorization.",
  ),
});

const CreateManageAppCreationSecretSchema = z.object({
  id: z.string(),
  type: z.string().optional(),
  access_token_ttl_seconds: z.number().min(3600).max(86400).describe(
    "The access token time to live for your app, in seconds. It only affects the newly generated acces...",
  ),
  client_id: z.string().describe("The oauth2 client id for the app."),
  client_secret: z.string().min(1).describe(
    "The oauth2 client secret for the app. This is the only time this secret will be returned, store i...",
  ),
  context: z.enum(["tenant", "user"]).describe(
    "Allow installing the app to at org/group level or user level. Defaults to tenant.",
  ),
  grant_type: z.enum(["authorization_code", "client_credentials"]).describe(
    "An authorization grant is a credential representing the resource owner's authorization (to access...",
  ),
  is_confidential: z.boolean().describe(
    "A boolean to indicate if an app is confidential or not as per the OAuth2 RFC. Confidential apps c...",
  ),
  is_public: z.boolean().describe(
    "A boolean to indicate if an app is publicly available or not.",
  ),
  name: z.string().min(1).describe(
    "New name of the app to display to users during authorization flow.",
  ),
  org_public_id: z.string().optional(),
  redirect_uris: z.array(z.string()).describe(
    "List of allowed redirect URIs (OAuth callbacks) where Snyk will redirect users after authorizatio...",
  ),
  scopes: z.array(z.string().min(1)).describe(
    "The scopes this app is allowed to request during authorization.",
  ),
});

const GetAppInstallsForOrgItemSchema = z.object({
  id: z.string(),
  type: z.string().optional(),
  client_id: z.string().optional().describe(
    "The OAuth2 client id for the app installation. Only provided for installations of non-interactive...",
  ),
  installed_at: z.string().optional().describe(
    "Timestamp at which this app was first installed at.",
  ),
  app_id: z.string().optional().describe("Related app ID"),
});

const GetAppInstallsForOrgSchema = z.object({
  items: z.array(GetAppInstallsForOrgItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateOrgAppInstallSchema = z.object({
  id: z.string(),
  type: z.string().optional(),
  client_id: z.string(),
  client_secret: z.string(),
  app_id: z.string().optional().describe("Related app ID"),
});

const UpdateOrgAppInstallSecretSchema = z.object({
  id: z.string(),
  type: z.string().optional(),
  client_id: z.string().describe(
    "The OAuth2 client id for the app installation. Only provided for installations of non-interactive...",
  ),
  client_secret: z.string().min(1).describe(
    "The OAuth2 client secret for the app. This is the only time this secret will be returned, store i...",
  ),
  installed_at: z.string().optional().describe(
    "Timestamp at which this app was first installed at.",
  ),
  app_id: z.string().optional().describe("Related app ID"),
});

// =============================================================================
// Model Definition
// =============================================================================

/** Snyk Apps — OAuth application management, bots, installations */
export const model = {
  type: "@webframp/snyk/apps",
  version: "2026.07.20.1",
  globalArguments: GlobalArgsSchema,

  upgrades: [],

  resources: {
    "get_app_installs_for_group": {
      description: "Get a list of Snyk Apps installed for a Group",
      schema: GetAppInstallsForGroupSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "group_app_install": {
      description: "Install a Snyk App for a Group",
      schema: CreateGroupAppInstallSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "group_app_install_secret": {
      description:
        "Manage client secret for non-interactive Snyk App installations",
      schema: UpdateGroupAppInstallSecretSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "get_org_apps": {
      description: "Get a list of Snyk Apps created by an Organization",
      schema: GetOrgAppsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "org_app": {
      description: "Create a new Snyk App for an organization",
      schema: CreateOrgAppSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "app_by_id": {
      description: "Get a Snyk App by app ID",
      schema: GetAppByIdSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "app_creation_by_id": {
      description:
        "Update app creation attributes such as name, redirect URIs, and access token time to live using the App ID",
      schema: UpdateAppCreationByIdSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "manage_app_creation_secret": {
      description: "Manage client secret for a Snyk App",
      schema: CreateManageAppCreationSecretSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "get_app_installs_for_org": {
      description: "Get a list of Snyk Apps installed for an Organization",
      schema: GetAppInstallsForOrgSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "org_app_install": {
      description: "Install a Snyk App for an Organization",
      schema: CreateOrgAppInstallSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "org_app_install_secret": {
      description:
        "Manage client secret for non-interactive Snyk App installations",
      schema: UpdateOrgAppInstallSecretSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
  },

  methods: {
    get_app_installs_for_group: {
      description: "Get a list of Snyk Apps installed for a Group",
      arguments: z.object({
        group_id: z.string().describe("Group ID"),
        expand: z.string().optional().describe("Expand relationships."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>(["group_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await snykApiPaginated(
          apiToken,
          `/groups/${args.group_id}/apps/installs`,
          version,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "get_app_installs_for_group",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} get_app_installs_for_group", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_group_app_install: {
      description: "Install a Snyk App for a Group",
      arguments: z.object({
        group_id: z.string().describe("Group ID"),
        data: z.object({
          type: z.enum(["app_install"]).optional(),
        }),
        relationships: z.object({
          app: z.object({
            data: z.object({
              id: z.unknown(),
              type: z.enum(["app"]),
            }),
          }),
        }),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>(["group_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await snykApi(
          apiToken,
          "POST",
          `/groups/${args.group_id}/apps/installs`,
          version,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "group_app_install",
          id,
          result,
        );
        context.logger.info("Created group_app_install {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    delete_group_app_install_by_id: {
      description: "Revoke app authorization for a Snyk group with install ID",
      arguments: z.object({
        group_id: z.string().describe("Group ID"),
        install_id: z.string().describe("Install ID"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        await snykApi(
          apiToken,
          "DELETE",
          `/groups/${args.group_id}/apps/installs/${args.install_id}`,
          version,
        );

        context.logger.info("Deleted resource {id}", { id: args.install_id });
        return { dataHandles: [] };
      },
    },
    update_group_app_install_secret: {
      description:
        "Manage client secret for non-interactive Snyk App installations",
      arguments: z.object({
        group_id: z.string().describe("Group ID"),
        install_id: z.string().describe("Install ID"),
        data: z.object({
          attributes: z.object({
            mode: z.enum(["replace", "create", "delete"]),
            secret: z.string().optional(),
          }),
          type: z.enum(["app"]),
        }),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>(["group_id", "install_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await snykApi(
          apiToken,
          "POST",
          `/groups/${args.group_id}/apps/installs/${args.install_id}/secrets`,
          version,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "update_group_app_install_secret",
          id,
          result,
        );
        context.logger.info("Created update_group_app_install_secret {id}", {
          id,
        });
        return { dataHandles: [handle] };
      },
    },
    get_org_apps: {
      description: "Get a list of Snyk Apps created by an Organization",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, orgId, version } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await snykApiPaginated(
          apiToken,
          `/orgs/${orgId}/apps/creations`,
          version,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("get_org_apps", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} get_org_apps", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_org_app: {
      description: "Create a new Snyk App for an organization",
      arguments: z.object({
        data: z.object({
          attributes: z.object({
            access_token_ttl_seconds: z.unknown().optional(),
            context: z.unknown(),
            name: z.unknown(),
            redirect_uris: z.unknown(),
            scopes: z.unknown(),
          }),
          type: z.enum(["app"]),
        }),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, orgId, version } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await snykApi(
          apiToken,
          "POST",
          `/orgs/${orgId}/apps/creations`,
          version,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource("org_app", id, result);
        context.logger.info("Created org_app {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_app_by_id: {
      description: "Get a Snyk App by app ID",
      arguments: z.object({
        app_id: z.string().describe("App ID"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, orgId, version } = context.globalArgs;
        const result = await snykApi(
          apiToken,
          "GET",
          `/orgs/${orgId}/apps/creations/${args.app_id}`,
          version,
        );

        const handle = await context.writeResource(
          "app_by_id",
          String(args.app_id),
          result,
        );
        context.logger.info("Fetched app_by_id", {});
        return { dataHandles: [handle] };
      },
    },
    update_app_creation_by_id: {
      description:
        "Update app creation attributes such as name, redirect URIs, and access token time to live using the App ID",
      arguments: z.object({
        app_id: z.string().describe("App ID"),
        data: z.object({
          attributes: z.object({
            access_token_ttl_seconds: z.unknown().optional(),
            name: z.unknown().optional(),
            redirect_uris: z.unknown().optional(),
          }).optional(),
          type: z.enum(["app"]).optional(),
        }),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, orgId, version } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>(["app_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await snykApi(
          apiToken,
          "PATCH",
          `/orgs/${orgId}/apps/creations/${args.app_id}`,
          version,
          body,
        );

        const handle = await context.writeResource(
          "app_creation_by_id",
          String(args.app_id),
          result,
        );
        context.logger.info("Updated app_creation_by_id", {});
        return { dataHandles: [handle] };
      },
    },
    delete_app_by_id: {
      description: "Delete a Snyk App by app ID",
      arguments: z.object({
        app_id: z.string().describe("App ID"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, orgId, version } = context.globalArgs;
        await snykApi(
          apiToken,
          "DELETE",
          `/orgs/${orgId}/apps/creations/${args.app_id}`,
          version,
        );

        context.logger.info("Deleted resource {id}", { id: args.app_id });
        return { dataHandles: [] };
      },
    },
    create_manage_app_creation_secret: {
      description: "Manage client secret for a Snyk App",
      arguments: z.object({
        app_id: z.string().describe("App ID"),
        data: z.object({
          attributes: z.object({
            mode: z.enum(["replace", "create", "delete"]),
            secret: z.string().optional(),
          }),
          type: z.enum(["app"]),
        }),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, orgId, version } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>(["app_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await snykApi(
          apiToken,
          "POST",
          `/orgs/${orgId}/apps/creations/${args.app_id}/secrets`,
          version,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "manage_app_creation_secret",
          id,
          result,
        );
        context.logger.info("Created manage_app_creation_secret {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_app_installs_for_org: {
      description: "Get a list of Snyk Apps installed for an Organization",
      arguments: z.object({
        expand: z.string().optional().describe("Expand relationships."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, orgId, version } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await snykApiPaginated(
          apiToken,
          `/orgs/${orgId}/apps/installs`,
          version,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "get_app_installs_for_org",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} get_app_installs_for_org", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_org_app_install: {
      description: "Install a Snyk App for an Organization",
      arguments: z.object({
        data: z.object({
          type: z.enum(["app_install"]).optional(),
        }),
        relationships: z.object({
          app: z.object({
            data: z.object({
              id: z.unknown(),
              type: z.enum(["app"]),
            }),
          }),
        }),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, orgId, version } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await snykApi(
          apiToken,
          "POST",
          `/orgs/${orgId}/apps/installs`,
          version,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "org_app_install",
          id,
          result,
        );
        context.logger.info("Created org_app_install {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    delete_app_org_install_by_id: {
      description:
        "Revoke app authorization for a Snyk organization with install ID",
      arguments: z.object({
        install_id: z.string().describe("Install ID"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, orgId, version } = context.globalArgs;
        await snykApi(
          apiToken,
          "DELETE",
          `/orgs/${orgId}/apps/installs/${args.install_id}`,
          version,
        );

        context.logger.info("Deleted resource {id}", { id: args.install_id });
        return { dataHandles: [] };
      },
    },
    update_org_app_install_secret: {
      description:
        "Manage client secret for non-interactive Snyk App installations",
      arguments: z.object({
        install_id: z.string().describe("Install ID"),
        data: z.object({
          attributes: z.object({
            mode: z.enum(["replace", "create", "delete"]),
            secret: z.string().optional(),
          }),
          type: z.enum(["app"]),
        }),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, orgId, version } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>(["install_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await snykApi(
          apiToken,
          "POST",
          `/orgs/${orgId}/apps/installs/${args.install_id}/secrets`,
          version,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "update_org_app_install_secret",
          id,
          result,
        );
        context.logger.info("Created update_org_app_install_secret {id}", {
          id,
        });
        return { dataHandles: [handle] };
      },
    },
  },
};
