/**
 * Snyk Settings — organization and group setting management
 *
 * Auto-generated by scripts/snyk-codegen — do not edit manually.
 *
 * @module
 */
// SPDX-License-Identifier: AGPL-3.0-or-later WITH Swamp-Extension-Exception

import { z } from "npm:zod@4.4.3";
import { snykApi } from "./_lib/api.ts";

const EXTENSION_NAME = "@webframp/snyk/settings";

// =============================================================================
// Schemas
// =============================================================================

const GlobalArgsSchema = z.object({
  apiToken: z.string().meta({ sensitive: true }).describe("Snyk API token"),
  orgId: z.string().describe("Snyk organization ID"),
  version: z.string().default("2024-10-15").describe("Snyk API version date"),
});

const GetIacSettingsForGroupSchema = z.object({
  id: z.string().describe("ID"),
  type: z.string().optional().describe("Content type"),
  custom_rules: z.object({
    is_enabled: z.unknown().optional(),
    oci_registry_tag: z.unknown().optional(),
    oci_registry_url: z.unknown().optional(),
  }).optional().describe(
    "The Infrastructure as Code custom rules settings for a group.",
  ),
  updated: z.string().optional().describe(
    "The last time the settings were updated.",
  ),
  fetchedAt: z.string().optional().describe(
    "ISO 8601 timestamp when data was fetched",
  ),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const GetOpensourceBrokerSettingForGroupSchema = z.object({
  fetchedAt: z.string().optional().describe(
    "ISO 8601 timestamp when data was fetched",
  ),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const EnableOpensourceBrokerForGroupSchema = z.object({
  fetchedAt: z.string().optional().describe(
    "ISO 8601 timestamp when data was fetched",
  ),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const GetPullRequestTemplateSchema = z.object({
  id: z.string(),
  type: z.string().optional(),
  commit_message: z.string().min(1).optional().describe(
    "The commit message that will be used when the pull request is created",
  ),
  description: z.string().min(1).optional().describe(
    "The description of the pull request",
  ),
  title: z.string().min(1).optional().describe(
    "Specify a title for the pull request",
  ),
  fetchedAt: z.string().optional().describe(
    "ISO 8601 timestamp when data was fetched",
  ),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const CreateOrUpdatePullRequestTemplateSchema = z.object({
  id: z.string(),
  type: z.string().optional(),
  commit_message: z.string().min(1).optional().describe(
    "The commit message that will be used when the pull request is created",
  ),
  description: z.string().min(1).optional().describe(
    "The description of the pull request",
  ),
  title: z.string().min(1).optional().describe(
    "Specify a title for the pull request",
  ),
  fetchedAt: z.string().optional().describe(
    "ISO 8601 timestamp when data was fetched",
  ),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const GetIacSettingsForOrgSchema = z.object({
  id: z.string().describe("ID"),
  type: z.string().optional().describe("Content type"),
  custom_rules: z.object({
    inherit_from_parent: z.unknown().optional(),
    is_enabled: z.unknown().optional(),
    oci_registry_tag: z.unknown().optional(),
    oci_registry_url: z.unknown().optional(),
    parents: z.object({
      group: z.object({
        custom_rules: z.object({
          is_enabled: z.unknown().optional(),
          oci_registry_tag: z.unknown().optional(),
          oci_registry_url: z.unknown().optional(),
        }).optional(),
        updated: z.unknown().optional(),
      }).optional(),
    }).optional(),
    updated: z.unknown().optional(),
  }).optional().describe(
    "The Infrastructure as Code custom rules settings for an org.",
  ),
  fetchedAt: z.string().optional().describe(
    "ISO 8601 timestamp when data was fetched",
  ),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const GetOrgLanguagesSettingsSchema = z.object({
  id: z.string().describe("The organization's public ID."),
  type: z.enum(["languages_settings"]).optional(),
  languages: z.object({
    dotnet: z.unknown(),
    golang: z.unknown(),
    java: z.unknown(),
    javascript: z.unknown(),
    php: z.unknown(),
    python: z.unknown(),
  }),
  fetchedAt: z.string().optional().describe(
    "ISO 8601 timestamp when data was fetched",
  ),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const GetOpenSourceSettingsForOrgSchema = z.object({
  id: z.string().describe("ID"),
  type: z.string().optional().describe("Content type"),
  reachability: z.object({
    enabled: z.boolean().optional(),
  }).optional().describe("The reachability setting for Org."),
  fetchedAt: z.string().optional().describe(
    "ISO 8601 timestamp when data was fetched",
  ),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const GetOpensourceBrokerSettingSchema = z.object({
  fetchedAt: z.string().optional().describe(
    "ISO 8601 timestamp when data was fetched",
  ),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const EnableOpensourceBrokerForOrgSchema = z.object({
  fetchedAt: z.string().optional().describe(
    "ISO 8601 timestamp when data was fetched",
  ),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const GetOpensourceBrokerEcosystemSettingsForOrgSchema = z.object({
  fetchedAt: z.string().optional().describe(
    "ISO 8601 timestamp when data was fetched",
  ),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const GetOpensourcePrivateRegistryEcosystemSettingsForOrgSchema = z.object({
  fetchedAt: z.string().optional().describe(
    "ISO 8601 timestamp when data was fetched",
  ),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const GetSastSettingsSchema = z.object({
  id: z.string(),
  type: z.string().optional(),
  autofix_enabled: z.boolean().optional(),
  sast_enabled: z.boolean(),
  fetchedAt: z.string().optional().describe(
    "ISO 8601 timestamp when data was fetched",
  ),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const UpdateOrgSastSettingsSchema = z.object({
  id: z.string(),
  type: z.string().optional(),
  autofix_enabled: z.boolean().optional(),
  sast_enabled: z.boolean(),
  fetchedAt: z.string().optional().describe(
    "ISO 8601 timestamp when data was fetched",
  ),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const GetSecretsSettingsSchema = z.object({
  id: z.string(),
  type: z.string().optional(),
  secrets_enabled: z.boolean(),
  fetchedAt: z.string().optional().describe(
    "ISO 8601 timestamp when data was fetched",
  ),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const UpdateOrgSecretsSettingsSchema = z.object({
  id: z.string(),
  type: z.string().optional(),
  secrets_enabled: z.boolean(),
  fetchedAt: z.string().optional().describe(
    "ISO 8601 timestamp when data was fetched",
  ),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

// =============================================================================
// Model Definition
// =============================================================================

/** Snyk Settings — organization and group setting management */
export const model = {
  type: "@webframp/snyk/settings",
  version: "2026.08.28.1",
  globalArguments: GlobalArgsSchema,

  upgrades: [
    {
      toVersion: "2026.08.24.1",
      description:
        "Added optional durationMs, collectedBy, and fetchedAt output metadata fields",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.08.25.1",
      description: "Label metadata update, no schema changes",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.08.26.1",
      description:
        "No schema changes — restored inline npm:zod specifier for registry scoring; retained strict mode",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.08.26.2",
      description:
        "No schema changes — restored inline npm:zod specifier for registry scoring; retained strict mode",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.08.28.1",
      description:
        "No schema changes — normalized license to Apache-2.0 and corrected copyright holder to Sean Escriva",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
  ],

  resources: {
    "iac_settings_for_group": {
      description: "Get the Infrastructure as Code Settings for a group",
      schema: GetIacSettingsForGroupSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "opensource_broker_setting_for_group": {
      description: "Get opensource broker setting for group",
      schema: GetOpensourceBrokerSettingForGroupSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "enable_opensource_broker_for_group": {
      description: "Enable opensource broker for group",
      schema: EnableOpensourceBrokerForGroupSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "pull_request_template": {
      description: "Get pull request template for group",
      schema: GetPullRequestTemplateSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "or_update_pull_request_template": {
      description: "Create or update pull request template for group",
      schema: CreateOrUpdatePullRequestTemplateSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "iac_settings_for_org": {
      description: "Get the Infrastructure as Code Settings for an org.",
      schema: GetIacSettingsForOrgSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "org_languages_settings": {
      description: "Get language settings for an organization (Early Access)",
      schema: GetOrgLanguagesSettingsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "open_source_settings_for_org": {
      description: "Get the Open Source Settings for an Org. (Early Access)",
      schema: GetOpenSourceSettingsForOrgSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "opensource_broker_setting": {
      description: "Get opensource broker setting for organization",
      schema: GetOpensourceBrokerSettingSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "enable_opensource_broker_for_org": {
      description: "Enable opensource broker for organization",
      schema: EnableOpensourceBrokerForOrgSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "opensource_broker_ecosystem_settings_for_org": {
      description:
        "Get opensource broker settings of ecosystem for organization",
      schema: GetOpensourceBrokerEcosystemSettingsForOrgSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "opensource_private_registry_ecosystem_settings_for_org": {
      description:
        "Get opensource private registry settings of ecosystem for organization",
      schema: GetOpensourcePrivateRegistryEcosystemSettingsForOrgSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "sast_settings": {
      description: "Retrieves the SAST settings for an org",
      schema: GetSastSettingsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "org_sast_settings": {
      description: "Enable/Disable the Snyk Code settings for an org",
      schema: UpdateOrgSastSettingsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "secrets_settings": {
      description: "Retrieves the Secrets settings for an org (Early Access)",
      schema: GetSecretsSettingsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "org_secrets_settings": {
      description: "Update the Secrets settings for an org (Early Access)",
      schema: UpdateOrgSecretsSettingsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
  },

  methods: {
    get_iac_settings_for_group: {
      description: "Get the Infrastructure as Code Settings for a group",
      arguments: z.object({
        group_id: z.string().min(1, "group_id must not be empty").describe(
          "The id of the group whose Infrastructure as Code settings are requested",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, version } = context.globalArgs;
        const result = await snykApi(
          apiToken,
          "GET",
          `/groups/${args.group_id}/settings/iac`,
          version,
        );

        const handle = await context.writeResource(
          "iac_settings_for_group",
          String(args.group_id),
          {
            ...result,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info("Fetched iac_settings_for_group", {});
        return { dataHandles: [handle] };
      },
    },
    update_iac_settings_for_group: {
      description: "Update the Infrastructure as Code Settings for a group",
      arguments: z.object({
        group_id: z.string().min(1, "group_id must not be empty").describe(
          "The id of the group whose Infrastructure as Code settings are getting updated",
        ),
        data: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, version } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>(["group_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await snykApi(
          apiToken,
          "PATCH",
          `/groups/${args.group_id}/settings/iac`,
          version,
          body,
        );

        const handle = await context.writeResource(
          "iac_settings_for_group",
          String(args.group_id),
          {
            ...result,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info("Updated iac_settings_for_group", {});
        return { dataHandles: [handle] };
      },
    },
    get_opensource_broker_setting_for_group: {
      description: "Get opensource broker setting for group",
      arguments: z.object({
        group_id: z.string().min(1, "group_id must not be empty").describe(
          "Group ID",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, version } = context.globalArgs;
        const result = await snykApi(
          apiToken,
          "GET",
          `/groups/${args.group_id}/settings/opensource/broker`,
          version,
        );

        const handle = await context.writeResource(
          "opensource_broker_setting_for_group",
          String(args.group_id),
          {
            ...result,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info("Fetched opensource_broker_setting_for_group", {});
        return { dataHandles: [handle] };
      },
    },
    enable_opensource_broker_for_group: {
      description: "Enable opensource broker for group",
      arguments: z.object({
        group_id: z.string().min(1, "group_id must not be empty").describe(
          "Group ID",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, version } = context.globalArgs;

        const result = await snykApi(
          apiToken,
          "POST",
          `/groups/${args.group_id}/settings/opensource/broker`,
          version,
        );

        const handle = await context.writeResource(
          "enable_opensource_broker_for_group",
          "latest",
          {
            ...result ?? {},
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info("Executed enable_opensource_broker_for_group", {});
        return { dataHandles: [handle] };
      },
    },
    delete_opensource_broker_setting_for_group: {
      description: "Delete opensource broker setting for group",
      arguments: z.object({
        group_id: z.string().min(1, "group_id must not be empty").describe(
          "Group ID",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        await snykApi(
          apiToken,
          "DELETE",
          `/groups/${args.group_id}/settings/opensource/broker`,
          version,
        );

        context.logger.info("Deleted resource {id}", { id: args.group_id });
        return { dataHandles: [] };
      },
    },
    get_pull_request_template: {
      description: "Get pull request template for group",
      arguments: z.object({
        group_id: z.string().min(1, "group_id must not be empty").describe(
          "Snyk Group ID",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, version } = context.globalArgs;
        const result = await snykApi(
          apiToken,
          "GET",
          `/groups/${args.group_id}/settings/pull_request_template`,
          version,
        );

        const handle = await context.writeResource(
          "pull_request_template",
          String(args.group_id),
          {
            ...result,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info("Fetched pull_request_template", {});
        return { dataHandles: [handle] };
      },
    },
    create_or_update_pull_request_template: {
      description: "Create or update pull request template for group",
      arguments: z.object({
        group_id: z.string().min(1, "group_id must not be empty").describe(
          "Snyk Group ID",
        ),
        data: z.object({
          attributes: z.unknown(),
          type: z.unknown(),
        }),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, version } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>(["group_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await snykApi(
          apiToken,
          "POST",
          `/groups/${args.group_id}/settings/pull_request_template`,
          version,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "or_update_pull_request_template",
          id,
          {
            ...result,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info("Created or_update_pull_request_template {id}", {
          id,
        });
        return { dataHandles: [handle] };
      },
    },
    delete_pull_request_template: {
      description: "Delete pull request template for group",
      arguments: z.object({
        group_id: z.string().min(1, "group_id must not be empty").describe(
          "Snyk Group ID",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, version } = context.globalArgs;
        await snykApi(
          apiToken,
          "DELETE",
          `/groups/${args.group_id}/settings/pull_request_template`,
          version,
        );

        context.logger.info("Deleted resource {id}", { id: args.group_id });
        return { dataHandles: [] };
      },
    },
    get_iac_settings_for_org: {
      description: "Get the Infrastructure as Code Settings for an org.",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, orgId, version } = context.globalArgs;
        const result = await snykApi(
          apiToken,
          "GET",
          `/orgs/${orgId}/settings/iac`,
          version,
        );

        const handle = await context.writeResource(
          "iac_settings_for_org",
          "latest",
          {
            ...result,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info("Fetched iac_settings_for_org", {});
        return { dataHandles: [handle] };
      },
    },
    update_iac_settings_for_org: {
      description: "Update the Infrastructure as Code Settings for an org",
      arguments: z.object({
        data: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, orgId, version } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await snykApi(
          apiToken,
          "PATCH",
          `/orgs/${orgId}/settings/iac`,
          version,
          body,
        );

        const handle = await context.writeResource(
          "iac_settings_for_org",
          "updated",
          {
            ...result,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info("Updated iac_settings_for_org", {});
        return { dataHandles: [handle] };
      },
    },
    get_org_languages_settings: {
      description: "Get language settings for an organization (Early Access)",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, orgId, version } = context.globalArgs;
        const result = await snykApi(
          apiToken,
          "GET",
          `/orgs/${orgId}/settings/open_source/languages`,
          version,
        );

        const handle = await context.writeResource(
          "org_languages_settings",
          "latest",
          {
            ...result,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info("Fetched org_languages_settings", {});
        return { dataHandles: [handle] };
      },
    },
    update_org_languages_settings: {
      description:
        "Update language settings for an organization (Early Access)",
      arguments: z.object({
        language: z.string().min(1, "language must not be empty").describe(
          "The language for which settings are being updated",
        ),
        data: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, orgId, version } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>(["language"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await snykApi(
          apiToken,
          "PATCH",
          `/orgs/${orgId}/settings/open_source/languages/${args.language}`,
          version,
          body,
        );

        const handle = await context.writeResource(
          "org_languages_settings",
          String(args.language),
          {
            ...result,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info("Updated org_languages_settings", {});
        return { dataHandles: [handle] };
      },
    },
    get_open_source_settings_for_org: {
      description: "Get the Open Source Settings for an Org. (Early Access)",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, orgId, version } = context.globalArgs;
        const result = await snykApi(
          apiToken,
          "GET",
          `/orgs/${orgId}/settings/opensource`,
          version,
        );

        const handle = await context.writeResource(
          "open_source_settings_for_org",
          "latest",
          {
            ...result,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info("Fetched open_source_settings_for_org", {});
        return { dataHandles: [handle] };
      },
    },
    get_opensource_broker_setting: {
      description: "Get opensource broker setting for organization",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, orgId, version } = context.globalArgs;
        const result = await snykApi(
          apiToken,
          "GET",
          `/orgs/${orgId}/settings/opensource/broker`,
          version,
        );

        const handle = await context.writeResource(
          "opensource_broker_setting",
          "latest",
          {
            ...result,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info("Fetched opensource_broker_setting", {});
        return { dataHandles: [handle] };
      },
    },
    enable_opensource_broker_for_org: {
      description: "Enable opensource broker for organization",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, orgId, version } = context.globalArgs;

        const result = await snykApi(
          apiToken,
          "POST",
          `/orgs/${orgId}/settings/opensource/broker`,
          version,
        );

        const handle = await context.writeResource(
          "enable_opensource_broker_for_org",
          "latest",
          {
            ...result ?? {},
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info("Executed enable_opensource_broker_for_org", {});
        return { dataHandles: [handle] };
      },
    },
    delete_opensource_broker_setting_for_org: {
      description: "Delete opensource broker setting for organization",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, orgId, version } = context.globalArgs;
        await snykApi(
          apiToken,
          "DELETE",
          `/orgs/${orgId}/settings/opensource/broker`,
          version,
        );

        context.logger.info("Deleted resource {id}", { id: "unknown" });
        return { dataHandles: [] };
      },
    },
    get_opensource_broker_ecosystem_settings_for_org: {
      description:
        "Get opensource broker settings of ecosystem for organization",
      arguments: z.object({
        ecosystem: z.string().min(1, "ecosystem must not be empty").describe(
          "The ecosystem identifier",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, orgId, version } = context.globalArgs;
        const result = await snykApi(
          apiToken,
          "GET",
          `/orgs/${orgId}/settings/opensource/${args.ecosystem}/broker`,
          version,
        );

        const handle = await context.writeResource(
          "opensource_broker_ecosystem_settings_for_org",
          String(args.ecosystem),
          {
            ...result,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info(
          "Fetched opensource_broker_ecosystem_settings_for_org",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    update_opensource_broker_ecosystem_settings_for_org: {
      description:
        "Update opensource broker settings of ecosystem for organization",
      arguments: z.object({
        ecosystem: z.string().min(1, "ecosystem must not be empty").describe(
          "The ecosystem identifier",
        ),
        data: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, orgId, version } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>(["ecosystem"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await snykApi(
          apiToken,
          "PATCH",
          `/orgs/${orgId}/settings/opensource/${args.ecosystem}/broker`,
          version,
          body,
        );

        const handle = await context.writeResource(
          "opensource_broker_ecosystem_settings_for_org",
          String(args.ecosystem),
          {
            ...result,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info(
          "Updated opensource_broker_ecosystem_settings_for_org",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    get_opensource_private_registry_ecosystem_settings_for_org: {
      description:
        "Get opensource private registry settings of ecosystem for organization",
      arguments: z.object({
        ecosystem: z.string().min(1, "ecosystem must not be empty").describe(
          "The ecosystem identifier",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, orgId, version } = context.globalArgs;
        const result = await snykApi(
          apiToken,
          "GET",
          `/orgs/${orgId}/settings/opensource/${args.ecosystem}/private-registries`,
          version,
        );

        const handle = await context.writeResource(
          "opensource_private_registry_ecosystem_settings_for_org",
          String(args.ecosystem),
          {
            ...result,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info(
          "Fetched opensource_private_registry_ecosystem_settings_for_org",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    update_opensource_private_registry_ecosystem_settings_for_org: {
      description:
        "Update opensource private registry settings of ecosystem for organization",
      arguments: z.object({
        ecosystem: z.string().min(1, "ecosystem must not be empty").describe(
          "The ecosystem identifier",
        ),
        data: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, orgId, version } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>(["ecosystem"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await snykApi(
          apiToken,
          "PATCH",
          `/orgs/${orgId}/settings/opensource/${args.ecosystem}/private-registries`,
          version,
          body,
        );

        const handle = await context.writeResource(
          "opensource_private_registry_ecosystem_settings_for_org",
          String(args.ecosystem),
          {
            ...result,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info(
          "Updated opensource_private_registry_ecosystem_settings_for_org",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    get_sast_settings: {
      description: "Retrieves the SAST settings for an org",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, orgId, version } = context.globalArgs;
        const result = await snykApi(
          apiToken,
          "GET",
          `/orgs/${orgId}/settings/sast`,
          version,
        );

        const handle = await context.writeResource(
          "sast_settings",
          "latest",
          {
            ...result,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info("Fetched sast_settings", {});
        return { dataHandles: [handle] };
      },
    },
    update_org_sast_settings: {
      description: "Enable/Disable the Snyk Code settings for an org",
      arguments: z.object({
        data: z.object({
          attributes: z.object({
            sast_enabled: z.boolean(),
          }),
          id: z.string(),
          type: z.string(),
        }),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, orgId, version } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await snykApi(
          apiToken,
          "PATCH",
          `/orgs/${orgId}/settings/sast`,
          version,
          body,
        );

        const handle = await context.writeResource(
          "org_sast_settings",
          "updated",
          {
            ...result,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info("Updated org_sast_settings", {});
        return { dataHandles: [handle] };
      },
    },
    get_secrets_settings: {
      description: "Retrieves the Secrets settings for an org (Early Access)",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, orgId, version } = context.globalArgs;
        const result = await snykApi(
          apiToken,
          "GET",
          `/orgs/${orgId}/settings/secrets`,
          version,
        );

        const handle = await context.writeResource(
          "secrets_settings",
          "latest",
          {
            ...result,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info("Fetched secrets_settings", {});
        return { dataHandles: [handle] };
      },
    },
    update_org_secrets_settings: {
      description: "Update the Secrets settings for an org (Early Access)",
      arguments: z.object({
        data: z.object({
          attributes: z.object({
            secrets_enabled: z.boolean(),
          }),
          id: z.string(),
          type: z.string(),
        }),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, orgId, version } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await snykApi(
          apiToken,
          "PATCH",
          `/orgs/${orgId}/settings/secrets`,
          version,
          body,
        );

        const handle = await context.writeResource(
          "org_secrets_settings",
          "updated",
          {
            ...result,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info("Updated org_secrets_settings", {});
        return { dataHandles: [handle] };
      },
    },
  },
};
