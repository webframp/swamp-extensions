// Snyk Service Accounts — automated access management for CI/CD — Tests
// Auto-generated by scripts/snyk-codegen — do not edit manually.
// SPDX-License-Identifier: Apache-2.0

import { assertEquals, assertExists } from "jsr:@std/assert@1";
import { createModelTestContext } from "@systeminit/swamp-testing";
import { model } from "./service_accounts.ts";

// ---------------------------------------------------------------------------
// Model Export Structure Tests
// ---------------------------------------------------------------------------

Deno.test("service-accounts model: has correct type", () => {
  assertEquals(model.type, "@webframp/snyk/service-accounts");
});

Deno.test("service-accounts model: has valid version format", () => {
  const versionPattern = /^\d{4}\.\d{2}\.\d{2}\.\d+$/;
  assertEquals(versionPattern.test(model.version), true);
});

Deno.test("service-accounts model: has globalArguments with apiToken", () => {
  assertExists(model.globalArguments);
  const shape = model.globalArguments.shape;
  assertExists(shape.apiToken);
  assertExists(shape.orgId);
  assertExists(shape.version);
});

Deno.test("service-accounts model: has expected methods", () => {
  assertExists(model.methods);
  assertExists(model.methods.get_many_group_service_account);
  assertExists(model.methods.create_group_service_account);
  assertExists(model.methods.get_one_group_service_account);
  assertExists(model.methods.update_group_service_account);
  assertExists(model.methods.delete_one_group_service_account);
  assertExists(model.methods.update_service_account_secret);
  assertExists(model.methods.get_many_org_service_accounts);
  assertExists(model.methods.create_org_service_account);
  assertExists(model.methods.get_one_org_service_account);
  assertExists(model.methods.update_org_service_account);
  assertExists(model.methods.delete_service_account);
  assertExists(model.methods.update_org_service_account_secret);
});

Deno.test("service-accounts model: has expected resources", () => {
  assertExists(model.resources);
  assertExists(model.resources["get_many_group_service_account"]);
  assertExists(model.resources["group_service_account"]);
  assertExists(model.resources["one_group_service_account"]);
  assertExists(model.resources["service_account_secret"]);
  assertExists(model.resources["get_many_org_service_accounts"]);
  assertExists(model.resources["org_service_account"]);
  assertExists(model.resources["one_org_service_account"]);
  assertExists(model.resources["org_service_account_secret"]);
});

// ---------------------------------------------------------------------------
// Mock Snyk API Server
// ---------------------------------------------------------------------------

function startMockSnykServer(
  responses: Record<string, { data: unknown; isCollection?: boolean }>,
): { url: string; server: Deno.HttpServer } {
  const server = Deno.serve({ port: 0, onListen() {} }, (req) => {
    const url = new URL(req.url);
    const path = url.pathname;

    for (const [pattern, { data, isCollection }] of Object.entries(responses)) {
      if (path.includes(pattern)) {
        const body = isCollection
          ? {
            data: Array.isArray(data) ? data : [data],
            jsonapi: { version: "1.0" },
            links: { self: path, next: null },
          }
          : {
            data,
            jsonapi: { version: "1.0" },
            links: { self: path },
          };
        return Response.json(body, {
          headers: { "content-type": "application/vnd.api+json" },
        });
      }
    }

    return new Response(
      JSON.stringify({
        jsonapi: { version: "1.0" },
        errors: [{ status: "404", detail: "Not found" }],
      }),
      { status: 404, headers: { "content-type": "application/vnd.api+json" } },
    );
  });

  const addr = server.addr as Deno.NetAddr;
  return { url: `http://localhost:${addr.port}`, server };
}

function installFetchMock(mockUrl: string): () => void {
  const originalFetch = globalThis.fetch;
  globalThis.fetch = (input, init) => {
    const reqUrl = typeof input === "string" ? input : input.toString();
    const newUrl = reqUrl.replace("https://api.snyk.io/rest", mockUrl);
    return originalFetch(newUrl, init);
  };
  return () => {
    globalThis.fetch = originalFetch;
  };
}

Deno.test({
  name:
    "service-accounts model: get_many_group_service_account fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockItem = {
      "id": "fixture-123",
      "type": "resource",
      "attributes": {
        "access_token": "test-value",
        "access_token_expires_at": "2025-08-16T00:00:00Z",
        "access_token_ttl_seconds": 1,
        "api_key": "test-value",
        "auth_type": "api_key",
        "client_id": "test-value",
        "client_secret": "test-value",
        "created_at": "2025-08-16T00:00:00Z",
        "jwks_url": "test-value",
        "level": "Group",
        "name": "test-value",
        "role_id": "test-value",
      },
    };
    const { url, server } = startMockSnykServer({
      "/groups/test-id-123/service_accounts": {
        data: [mockItem],
        isCollection: true,
      },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: {
          "apiToken": "test-token",
          "version": "2024-10-15",
          "orgId": "test-org-123",
        },
        definition: {
          id: "test-id",
          name: "test-service-accounts",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).get_many_group_service_account.execute({}, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name:
    "service-accounts model: create_group_service_account creates and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "id": "new-123",
      "type": "resource",
      "attributes": {
        "access_token": "test-value",
        "access_token_expires_at": "2025-08-16T00:00:00Z",
        "access_token_ttl_seconds": 1,
        "api_key": "test-value",
        "auth_type": "api_key",
        "client_id": "test-value",
        "client_secret": "test-value",
        "created_at": "2025-08-16T00:00:00Z",
        "jwks_url": "test-value",
        "level": "Group",
        "name": "test-value",
        "role_id": "test-value",
      },
    };
    const { url, server } = startMockSnykServer({
      "/groups/test-id-123/service_accounts": { data: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: {
          "apiToken": "test-token",
          "version": "2024-10-15",
          "orgId": "test-org-123",
        },
        definition: {
          id: "test-id",
          name: "test-service-accounts",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).create_group_service_account.execute(
        { "name": "test-resource" },
        context,
      );
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name:
    "service-accounts model: get_one_group_service_account fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "id": "fixture-123",
      "type": "resource",
      "attributes": {
        "access_token": "test-value",
        "access_token_expires_at": "2025-08-16T00:00:00Z",
        "access_token_ttl_seconds": 1,
        "api_key": "test-value",
        "auth_type": "api_key",
        "client_id": "test-value",
        "client_secret": "test-value",
        "created_at": "2025-08-16T00:00:00Z",
        "jwks_url": "test-value",
        "level": "Group",
        "name": "test-value",
        "role_id": "test-value",
      },
    };
    const { url, server } = startMockSnykServer({
      "/groups/test-id-123/service_accounts/test-id-123": { data: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: {
          "apiToken": "test-token",
          "version": "2024-10-15",
          "orgId": "test-org-123",
        },
        definition: {
          id: "test-id",
          name: "test-service-accounts",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).get_one_group_service_account.execute({
        "serviceaccount_id": "test-id-123",
      }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name:
    "service-accounts model: update_group_service_account executes and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "id": "fixture-123",
      "type": "resource",
      "attributes": {
        "access_token": "test-value",
        "access_token_expires_at": "2025-08-16T00:00:00Z",
        "access_token_ttl_seconds": 1,
        "api_key": "test-value",
        "auth_type": "api_key",
        "client_id": "test-value",
        "client_secret": "test-value",
        "created_at": "2025-08-16T00:00:00Z",
        "jwks_url": "test-value",
        "level": "Group",
        "name": "test-value",
        "role_id": "test-value",
      },
    };
    const { url, server } = startMockSnykServer({
      "/groups/test-id-123/service_accounts/test-id-123": { data: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: {
          "apiToken": "test-token",
          "version": "2024-10-15",
          "orgId": "test-org-123",
        },
        definition: {
          id: "test-id",
          name: "test-service-accounts",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).update_group_service_account.execute({
        "serviceaccount_id": "test-id-123",
        "name": "test-resource",
      }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name:
    "service-accounts model: delete_one_group_service_account executes successfully",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockSnykServer({
      "/groups/test-id-123/service_accounts/test-id-123": {
        data: { id: "test-id-123", type: "resource" },
      },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context } = createModelTestContext({
        globalArgs: {
          "apiToken": "test-token",
          "version": "2024-10-15",
          "orgId": "test-org-123",
        },
        definition: {
          id: "test-id",
          name: "test-service-accounts",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).delete_one_group_service_account.execute({
        "serviceaccount_id": "test-id-123",
      }, context);
      assertEquals(result.dataHandles.length, 0);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});
