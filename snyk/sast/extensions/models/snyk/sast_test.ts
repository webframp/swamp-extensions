// Snyk SAST — static application security testing results and management — Tests
// Auto-generated by scripts/snyk-codegen — do not edit manually.
// SPDX-License-Identifier: Apache-2.0

import { assertEquals, assertExists } from "jsr:@std/assert@1";
import { createModelTestContext } from "@systeminit/swamp-testing";
import { model } from "./sast.ts";

// ---------------------------------------------------------------------------
// Model Export Structure Tests
// ---------------------------------------------------------------------------

Deno.test("sast model: has correct type", () => {
  assertEquals(model.type, "@webframp/snyk/sast");
});

Deno.test("sast model: has valid version format", () => {
  const versionPattern = /^\d{4}\.\d{2}\.\d{2}\.\d+$/;
  assertEquals(versionPattern.test(model.version), true);
});

Deno.test("sast model: has globalArguments with apiToken", () => {
  assertExists(model.globalArguments);
  const shape = model.globalArguments.shape;
  assertExists(shape.apiToken);
  assertExists(shape.groupId);
  assertExists(shape.version);
});

Deno.test("sast model: has expected methods", () => {
  assertExists(model.methods);
  assertExists(model.methods.list_sast_rule_extensions_by_group);
  assertExists(model.methods.create_sast_rule_extension);
  assertExists(model.methods.create_sast_impact_test);
  assertExists(model.methods.get_sast_impact_test_status);
  assertExists(model.methods.get_sast_impact_test_result);
  assertExists(model.methods.get_sast_rule_extension);
  assertExists(model.methods.update_sast_rule_extension);
  assertExists(model.methods.delete_sast_rule_extension);
});

Deno.test("sast model: has expected resources", () => {
  assertExists(model.resources);
  assertExists(model.resources["sast_rule_extensions_by_group"]);
  assertExists(model.resources["sast_rule_extension"]);
  assertExists(model.resources["sast_impact_test"]);
  assertExists(model.resources["sast_impact_test_status"]);
  assertExists(model.resources["sast_impact_test_result"]);
});

// ---------------------------------------------------------------------------
// Mock Snyk API Server
// ---------------------------------------------------------------------------

function startMockSnykServer(
  responses: Record<string, { data: unknown; isCollection?: boolean }>,
): { url: string; server: Deno.HttpServer } {
  const server = Deno.serve({ port: 0, onListen() {} }, (req) => {
    const url = new URL(req.url);
    const path = url.pathname;

    for (const [pattern, { data, isCollection }] of Object.entries(responses)) {
      if (path.includes(pattern)) {
        const body = isCollection
          ? {
            data: Array.isArray(data) ? data : [data],
            jsonapi: { version: "1.0" },
            links: { self: path, next: null },
          }
          : {
            data,
            jsonapi: { version: "1.0" },
            links: { self: path },
          };
        return Response.json(body, {
          headers: { "content-type": "application/vnd.api+json" },
        });
      }
    }

    return new Response(
      JSON.stringify({
        jsonapi: { version: "1.0" },
        errors: [{ status: "404", detail: "Not found" }],
      }),
      { status: 404, headers: { "content-type": "application/vnd.api+json" } },
    );
  });

  const addr = server.addr as Deno.NetAddr;
  return { url: `http://localhost:${addr.port}`, server };
}

function installFetchMock(mockUrl: string): () => void {
  const originalFetch = globalThis.fetch;
  globalThis.fetch = (input, init) => {
    const reqUrl = typeof input === "string" ? input : input.toString();
    const newUrl = reqUrl.replace("https://api.snyk.io/rest", mockUrl);
    return originalFetch(newUrl, init);
  };
  return () => {
    globalThis.fetch = originalFetch;
  };
}

Deno.test({
  name:
    "sast model: list_sast_rule_extensions_by_group fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockItem = {
      "id": "fixture-123",
      "type": "resource",
      "attributes": {
        "configuration": { "rule_keys": null, "type": null },
        "created_at": "2024-01-01T00:00:00Z",
        "created_by": "test-value",
        "description": "test-value",
        "kind": "sast_sanitizer",
        "published_at": "2024-01-01T00:00:00Z",
        "published_by": "test-value",
        "signature": { "fully_qualified_name": null },
        "status": "published",
        "updated_at": "2024-01-01T00:00:00Z",
        "updated_by": "test-value",
      },
    };
    const { url, server } = startMockSnykServer({
      "/sast/rule_extensions": { data: [mockItem], isCollection: true },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: {
          "apiToken": "test-token",
          "version": "2024-10-15",
          "groupId": "test-org-123",
        },
        definition: { id: "test-id", name: "test-sast", version: 1, tags: {} },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).list_sast_rule_extensions_by_group.execute({}, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "sast model: create_sast_rule_extension creates and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "id": "new-123",
      "type": "resource",
      "attributes": {
        "configuration": { "rule_keys": null, "type": null },
        "created_at": "2024-01-01T00:00:00Z",
        "created_by": "test-value",
        "description": "test-value",
        "kind": "sast_sanitizer",
        "published_at": "2024-01-01T00:00:00Z",
        "published_by": "test-value",
        "signature": { "fully_qualified_name": null },
        "status": "published",
        "updated_at": "2024-01-01T00:00:00Z",
        "updated_by": "test-value",
      },
    };
    const { url, server } = startMockSnykServer({
      "/sast/rule_extensions": { data: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: {
          "apiToken": "test-token",
          "version": "2024-10-15",
          "groupId": "test-org-123",
        },
        definition: { id: "test-id", name: "test-sast", version: 1, tags: {} },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).create_sast_rule_extension.execute(
        { "name": "test-resource" },
        context,
      );
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "sast model: get_sast_impact_test_status fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "id": "fixture-123",
      "type": "resource",
      "attributes": {
        "code": "SNYK-0009",
        "detail": "The request was missing these required fields: ...",
        "links": { "about": "https://example.com/about_this_error" },
        "meta": { "key": "value" },
        "source": { "parameter": "param1", "pointer": "/data/attributes" },
        "status": "STARTED",
        "title": "Bad request",
      },
    };
    const { url, server } = startMockSnykServer({
      "/sast/rule_extensions/tests/test-id-123": { data: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: {
          "apiToken": "test-token",
          "version": "2024-10-15",
          "groupId": "test-org-123",
        },
        definition: { id: "test-id", name: "test-sast", version: 1, tags: {} },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).get_sast_impact_test_status.execute(
        { "test_id": "test-id-123" },
        context,
      );
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "sast model: update_sast_rule_extension executes and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "id": "fixture-123",
      "type": "resource",
      "attributes": {
        "configuration": { "rule_keys": null, "type": null },
        "created_at": "2024-01-01T00:00:00Z",
        "created_by": "test-value",
        "description": "test-value",
        "kind": "sast_sanitizer",
        "published_at": "2024-01-01T00:00:00Z",
        "published_by": "test-value",
        "signature": { "fully_qualified_name": null },
        "status": "published",
        "updated_at": "2024-01-01T00:00:00Z",
        "updated_by": "test-value",
      },
    };
    const { url, server } = startMockSnykServer({
      "/sast/rule_extensions/test-id-123": { data: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: {
          "apiToken": "test-token",
          "version": "2024-10-15",
          "groupId": "test-org-123",
        },
        definition: { id: "test-id", name: "test-sast", version: 1, tags: {} },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).update_sast_rule_extension.execute({
        "rule_extension_id": "test-id-123",
        "name": "test-resource",
      }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "sast model: delete_sast_rule_extension executes successfully",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockSnykServer({
      "/sast/rule_extensions/test-id-123": {
        data: { id: "test-id-123", type: "resource" },
      },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context } = createModelTestContext({
        globalArgs: {
          "apiToken": "test-token",
          "version": "2024-10-15",
          "groupId": "test-org-123",
        },
        definition: { id: "test-id", name: "test-sast", version: 1, tags: {} },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).delete_sast_rule_extension.execute({
        "rule_extension_id": "test-id-123",
      }, context);
      assertEquals(result.dataHandles.length, 0);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});
