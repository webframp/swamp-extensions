/**
 * Cloudflare Gateway — DNS/HTTP policies, locations, proxy endpoints
 *
 * Auto-generated by scripts/cloudflare-codegen — do not edit manually.
 *
 * @module
 */
// SPDX-License-Identifier: AGPL-3.0-or-later WITH Swamp-Extension-Exception

import { z } from "npm:zod@4.4.3";
import { cfApi, cfApiPaginated } from "./_lib/api.ts";

const EXTENSION_NAME = "@webframp/cloudflare/gateway";

// =============================================================================
// Schemas
// =============================================================================

const GlobalArgsSchema = z.object({
  apiToken: z.string().meta({ sensitive: true }).describe(
    "Cloudflare API token",
  ),
  accountId: z.string().describe("Cloudflare account ID"),
});

const GetZeroTrustAccountInformationSchema = z.object({
  gateway_tag: z.unknown().optional(),
  id: z.unknown().optional(),
  provider_name: z.unknown().optional(),
  fetchedAt: z.string().optional().describe(
    "ISO 8601 timestamp when data was fetched",
  ),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const CreateZeroTrustAccountSchema = z.object({
  gateway_tag: z.unknown().optional(),
  id: z.unknown().optional(),
  provider_name: z.unknown().optional(),
  fetchedAt: z.string().optional().describe(
    "ISO 8601 timestamp when data was fetched",
  ),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const ApplicationAndApplicationTypeMappingsItemSchema = z.union([
  z.object({
    application_type_id: z.unknown().optional(),
    created_at: z.unknown().optional(),
    id: z.unknown().optional(),
    name: z.unknown().optional(),
  }),
  z.object({
    created_at: z.unknown().optional(),
    description: z.string().optional(),
    id: z.unknown().optional(),
    name: z.unknown().optional(),
  }),
]);

const ListApplicationAndApplicationTypeMappingsSchema = z.object({
  items: z.array(ApplicationAndApplicationTypeMappingsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const ListSchema = z.object({
  approved_apps: z.unknown().optional(),
  created_at: z.unknown().optional(),
  in_review_apps: z.unknown().optional(),
  unapproved_apps: z.unknown().optional(),
  updated_at: z.unknown().optional(),
  fetchedAt: z.string().optional().describe(
    "ISO 8601 timestamp when data was fetched",
  ),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const UpdateSchema = z.object({
  approved_apps: z.unknown().optional(),
  created_at: z.unknown().optional(),
  in_review_apps: z.unknown().optional(),
  unapproved_apps: z.unknown().optional(),
  updated_at: z.unknown().optional(),
  fetchedAt: z.string().optional().describe(
    "ISO 8601 timestamp when data was fetched",
  ),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const GetAuditSshSettingsSchema = z.object({
  created_at: z.unknown().optional(),
  public_key: z.unknown().optional(),
  seed_id: z.unknown().optional(),
  updated_at: z.unknown().optional(),
  fetchedAt: z.string().optional().describe(
    "ISO 8601 timestamp when data was fetched",
  ),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const ZeroTrustRotateSshAccountSeedSchema = z.object({
  created_at: z.unknown().optional(),
  public_key: z.unknown().optional(),
  seed_id: z.unknown().optional(),
  updated_at: z.unknown().optional(),
  fetchedAt: z.string().optional().describe(
    "ISO 8601 timestamp when data was fetched",
  ),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const CategoriesItemSchema = z.object({
  beta: z.unknown().optional(),
  class: z.unknown().optional(),
  description: z.unknown().optional(),
  id: z.unknown().optional(),
  name: z.unknown().optional(),
  subcategories: z.array(z.unknown()).optional().describe(
    "Provide all subcategories for this category.",
  ),
});

const ListCategoriesSchema = z.object({
  items: z.array(CategoriesItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const ZeroTrustCertificatesItemSchema = z.object({
  binding_status: z.unknown().optional(),
  certificate: z.string().optional().describe(
    "Provide the CA certificate (read-only).",
  ),
  created_at: z.unknown().optional(),
  expires_on: z.unknown().optional(),
  fingerprint: z.string().optional().describe(
    "Provide the SHA256 fingerprint of the certificate (read-only).",
  ),
  id: z.unknown().optional(),
  in_use: z.boolean().optional().describe(
    "Indicate whether Gateway TLS interception uses this certificate (read-only). You cannot set this ...",
  ),
  issuer_org: z.string().optional().describe(
    "Indicate the organization that issued the certificate (read-only).",
  ),
  issuer_raw: z.string().optional().describe(
    "Provide the entire issuer field of the certificate (read-only).",
  ),
  type: z.unknown().optional(),
  updated_at: z.unknown().optional(),
  uploaded_on: z.unknown().optional(),
});

const ListZeroTrustCertificatesSchema = z.object({
  items: z.array(ZeroTrustCertificatesItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const CreateZeroTrustCertificateSchema = z.object({
  binding_status: z.unknown().optional(),
  certificate: z.string().optional().describe(
    "Provide the CA certificate (read-only).",
  ),
  created_at: z.unknown().optional(),
  expires_on: z.unknown().optional(),
  fingerprint: z.string().optional().describe(
    "Provide the SHA256 fingerprint of the certificate (read-only).",
  ),
  id: z.unknown().optional(),
  in_use: z.boolean().optional().describe(
    "Indicate whether Gateway TLS interception uses this certificate (read-only). You cannot set this ...",
  ),
  issuer_org: z.string().optional().describe(
    "Indicate the organization that issued the certificate (read-only).",
  ),
  issuer_raw: z.string().optional().describe(
    "Provide the entire issuer field of the certificate (read-only).",
  ),
  type: z.unknown().optional(),
  updated_at: z.unknown().optional(),
  uploaded_on: z.unknown().optional(),
  fetchedAt: z.string().optional().describe(
    "ISO 8601 timestamp when data was fetched",
  ),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const GetZeroTrustCertificatesZeroTrustCertificateDetailsSchema = z.object({
  binding_status: z.unknown().optional(),
  certificate: z.string().optional().describe(
    "Provide the CA certificate (read-only).",
  ),
  created_at: z.unknown().optional(),
  expires_on: z.unknown().optional(),
  fingerprint: z.string().optional().describe(
    "Provide the SHA256 fingerprint of the certificate (read-only).",
  ),
  id: z.unknown().optional(),
  in_use: z.boolean().optional().describe(
    "Indicate whether Gateway TLS interception uses this certificate (read-only). You cannot set this ...",
  ),
  issuer_org: z.string().optional().describe(
    "Indicate the organization that issued the certificate (read-only).",
  ),
  issuer_raw: z.string().optional().describe(
    "Provide the entire issuer field of the certificate (read-only).",
  ),
  type: z.unknown().optional(),
  updated_at: z.unknown().optional(),
  uploaded_on: z.unknown().optional(),
  fetchedAt: z.string().optional().describe(
    "ISO 8601 timestamp when data was fetched",
  ),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const ZeroTrustCertificatesDeactivateZeroTrustCertificateSchema = z.object({
  binding_status: z.unknown().optional(),
  certificate: z.string().optional().describe(
    "Provide the CA certificate (read-only).",
  ),
  created_at: z.unknown().optional(),
  expires_on: z.unknown().optional(),
  fingerprint: z.string().optional().describe(
    "Provide the SHA256 fingerprint of the certificate (read-only).",
  ),
  id: z.unknown().optional(),
  in_use: z.boolean().optional().describe(
    "Indicate whether Gateway TLS interception uses this certificate (read-only). You cannot set this ...",
  ),
  issuer_org: z.string().optional().describe(
    "Indicate the organization that issued the certificate (read-only).",
  ),
  issuer_raw: z.string().optional().describe(
    "Provide the entire issuer field of the certificate (read-only).",
  ),
  type: z.unknown().optional(),
  updated_at: z.unknown().optional(),
  uploaded_on: z.unknown().optional(),
  fetchedAt: z.string().optional().describe(
    "ISO 8601 timestamp when data was fetched",
  ),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const GetZeroTrustAccountConfigurationSchema = z.object({
  settings: z.object({
    activity_log: z.unknown().optional(),
    antivirus: z.unknown().optional(),
    block_page: z.unknown().optional(),
    body_scanning: z.unknown().optional(),
    browser_isolation: z.unknown().optional(),
    certificate: z.unknown().optional(),
    custom_certificate: z.unknown().optional(),
    extended_email_matching: z.unknown().optional(),
    fips: z.unknown().optional(),
    host_selector: z.unknown().optional(),
    inspection: z.unknown().optional(),
    max_ttl_secs: z.unknown().optional(),
    protocol_detection: z.unknown().optional(),
    sandbox: z.unknown().optional(),
    tls_decrypt: z.unknown().optional(),
  }).optional().describe("Specify account settings."),
  created_at: z.unknown().optional(),
  updated_at: z.unknown().optional(),
  fetchedAt: z.string().optional().describe(
    "ISO 8601 timestamp when data was fetched",
  ),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const PatchZeroTrustAccountConfigurationSchema = z.object({
  settings: z.object({
    activity_log: z.unknown().optional(),
    antivirus: z.unknown().optional(),
    block_page: z.unknown().optional(),
    body_scanning: z.unknown().optional(),
    browser_isolation: z.unknown().optional(),
    certificate: z.unknown().optional(),
    custom_certificate: z.unknown().optional(),
    extended_email_matching: z.unknown().optional(),
    fips: z.unknown().optional(),
    host_selector: z.unknown().optional(),
    inspection: z.unknown().optional(),
    max_ttl_secs: z.unknown().optional(),
    protocol_detection: z.unknown().optional(),
    sandbox: z.unknown().optional(),
    tls_decrypt: z.unknown().optional(),
  }).optional().describe("Specify account settings."),
  created_at: z.unknown().optional(),
  updated_at: z.unknown().optional(),
  fetchedAt: z.string().optional().describe(
    "ISO 8601 timestamp when data was fetched",
  ),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const DnsDestinationIpsItemSchema = z.object({
  backup_ip: z.string(),
  id: z.string(),
  pair_type: z.enum(["shared", "dedicated", "byoip"]).describe(
    "Specify whether the pair shared across multiple accounts (shared) or available exclusively to thi...",
  ),
  primary_ip: z.string(),
});

const ListDnsDestinationIpsSchema = z.object({
  items: z.array(DnsDestinationIpsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const GetEgressCidrPairsItemSchema = z.object({
  geolocation: z.object({
    city: z.string().optional(),
    country: z.string().optional(),
  }).describe("Specify the geographic location of this CIDR pair."),
  ipv4: z.string().describe(
    "Specify the IPv4 address of this egress CIDR pair.",
  ),
  ipv4_colo_name: z.string().describe(
    "Specify the colocation from which this IPv4 address egresses.",
  ),
  ipv6_cidr: z.string().describe(
    "Specify the IPv6 network address of this egress CIDR pair.",
  ),
});

const GetEgressCidrPairsSchema = z.object({
  items: z.array(GetEgressCidrPairsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const ZeroTrustListsItemSchema = z.object({
  count: z.unknown().optional(),
  created_at: z.unknown().optional(),
  description: z.unknown().optional(),
  id: z.unknown().optional(),
  items: z.unknown().optional(),
  name: z.unknown().optional(),
  type: z.unknown().optional(),
  updated_at: z.unknown().optional(),
});

const ListZeroTrustListsSchema = z.object({
  items: z.array(ZeroTrustListsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const ListDetailsSchema = z.object({
  count: z.unknown().optional(),
  created_at: z.unknown().optional(),
  description: z.unknown().optional(),
  id: z.unknown().optional(),
  items: z.unknown().optional(),
  name: z.unknown().optional(),
  type: z.unknown().optional(),
  updated_at: z.unknown().optional(),
  fetchedAt: z.string().optional().describe(
    "ISO 8601 timestamp when data was fetched",
  ),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const ItemsItemSchema = z.object({
  created_at: z.unknown().optional(),
  description: z.unknown().optional(),
  value: z.unknown().optional(),
});

const ListItemsSchema = z.object({
  items: z.array(ItemsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const ZeroTrustGatewayLocationsItemSchema = z.object({
  client_default: z.unknown().optional(),
  created_at: z.unknown().optional(),
  dns_destination_ips_id: z.unknown().optional(),
  dns_destination_ipv6_block_id: z.unknown().optional(),
  doh_subdomain: z.unknown().optional(),
  ecs_support: z.unknown().optional(),
  endpoints: z.unknown().optional(),
  id: z.unknown().optional(),
  ip: z.unknown().optional(),
  ipv4_destination: z.string().optional().describe(
    "Show the primary destination IPv4 address from the pair identified dns_destination_ips_id. This f...",
  ),
  ipv4_destination_backup: z.string().optional().describe(
    "Show the backup destination IPv4 address from the pair identified dns_destination_ips_id. This fi...",
  ),
  max_ttl: z.unknown().optional(),
  name: z.unknown().optional(),
  networks: z.unknown().optional(),
  updated_at: z.unknown().optional(),
});

const ListZeroTrustGatewayLocationsSchema = z.object({
  items: z.array(ZeroTrustGatewayLocationsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const CreateZeroTrustGatewayLocationSchema = z.object({
  client_default: z.unknown().optional(),
  created_at: z.unknown().optional(),
  dns_destination_ips_id: z.unknown().optional(),
  dns_destination_ipv6_block_id: z.unknown().optional(),
  doh_subdomain: z.unknown().optional(),
  ecs_support: z.unknown().optional(),
  endpoints: z.unknown().optional(),
  id: z.unknown().optional(),
  ip: z.unknown().optional(),
  ipv4_destination: z.string().optional().describe(
    "Show the primary destination IPv4 address from the pair identified dns_destination_ips_id. This f...",
  ),
  ipv4_destination_backup: z.string().optional().describe(
    "Show the backup destination IPv4 address from the pair identified dns_destination_ips_id. This fi...",
  ),
  max_ttl: z.unknown().optional(),
  name: z.unknown().optional(),
  networks: z.unknown().optional(),
  updated_at: z.unknown().optional(),
  fetchedAt: z.string().optional().describe(
    "ISO 8601 timestamp when data was fetched",
  ),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const GetZeroTrustGatewayLocationsZeroTrustGatewayLocationDetailsSchema = z
  .object({
    client_default: z.unknown().optional(),
    created_at: z.unknown().optional(),
    dns_destination_ips_id: z.unknown().optional(),
    dns_destination_ipv6_block_id: z.unknown().optional(),
    doh_subdomain: z.unknown().optional(),
    ecs_support: z.unknown().optional(),
    endpoints: z.unknown().optional(),
    id: z.unknown().optional(),
    ip: z.unknown().optional(),
    ipv4_destination: z.string().optional().describe(
      "Show the primary destination IPv4 address from the pair identified dns_destination_ips_id. This f...",
    ),
    ipv4_destination_backup: z.string().optional().describe(
      "Show the backup destination IPv4 address from the pair identified dns_destination_ips_id. This fi...",
    ),
    max_ttl: z.unknown().optional(),
    name: z.unknown().optional(),
    networks: z.unknown().optional(),
    updated_at: z.unknown().optional(),
  });

const GetLoggingSettingsForTheZeroTrustAccountSchema = z.object({
  redact_pii: z.boolean().optional().describe(
    "Indicate whether to redact personally identifiable information from activity logging (PII fields ...",
  ),
  settings_by_rule_type: z.object({
    dns: z.unknown().optional(),
    http: z.unknown().optional(),
    l4: z.unknown().optional(),
  }).optional().describe("Configure logging settings for each rule type."),
  fetchedAt: z.string().optional().describe(
    "ISO 8601 timestamp when data was fetched",
  ),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const ZeroTrustGatewayOperationsItemSchema = z.object({
  created_at: z.unknown().optional(),
  data: z.unknown().optional(),
  id: z.unknown().optional(),
  operation_type: z.enum(["create_list"]).optional().describe(
    "The type of operation.",
  ),
  processing_error: z.string().nullable().optional().describe(
    "A human-readable error message if the operation failed. Only present when the operation status is...",
  ),
  result: z.string().nullable().optional().describe(
    "The result of the operation. Only present when the operation has completed successfully.",
  ),
  status: z.enum(["pending", "active", "failed", "complete"]).optional()
    .describe("The status of the operation."),
  updated_at: z.unknown().optional(),
});

const ListZeroTrustGatewayOperationsSchema = z.object({
  items: z.array(ZeroTrustGatewayOperationsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const GetZeroTrustGatewayOperationsZeroTrustGatewayOperationDetailsSchema = z
  .object({
    created_at: z.unknown().optional(),
    data: z.unknown().optional(),
    id: z.unknown().optional(),
    operation_type: z.enum(["create_list"]).optional().describe(
      "The type of operation.",
    ),
    processing_error: z.string().nullable().optional().describe(
      "A human-readable error message if the operation failed. Only present when the operation status is...",
    ),
    result: z.string().nullable().optional().describe(
      "The result of the operation. Only present when the operation has completed successfully.",
    ),
    status: z.enum(["pending", "active", "failed", "complete"]).optional()
      .describe("The status of the operation."),
    updated_at: z.unknown().optional(),
  });

const CreatePacfileSchema = z.object({
  contents: z.unknown().optional(),
  created_at: z.unknown().optional(),
  description: z.unknown().optional(),
  id: z.unknown().optional(),
  name: z.unknown().optional(),
  slug: z.unknown().optional(),
  updated_at: z.unknown().optional(),
  url: z.unknown().optional(),
  fetchedAt: z.string().optional().describe(
    "ISO 8601 timestamp when data was fetched",
  ),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const GetZeroTrustGatewayPacfilesDetailsSchema = z.object({
  contents: z.unknown().optional(),
  created_at: z.unknown().optional(),
  description: z.unknown().optional(),
  id: z.unknown().optional(),
  name: z.unknown().optional(),
  slug: z.unknown().optional(),
  updated_at: z.unknown().optional(),
  url: z.unknown().optional(),
  fetchedAt: z.string().optional().describe(
    "ISO 8601 timestamp when data was fetched",
  ),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const ProxyEndpointsItemSchema = z.union([
  z.object({
    created_at: z.unknown().optional(),
    id: z.unknown().optional(),
    ips: z.unknown(),
    kind: z.enum(["ip"]).optional(),
    name: z.unknown(),
    subdomain: z.unknown().optional(),
    updated_at: z.unknown().optional(),
  }),
  z.object({
    created_at: z.unknown().optional(),
    id: z.unknown().optional(),
    kind: z.enum(["identity"]),
    name: z.unknown(),
    subdomain: z.unknown().optional(),
    updated_at: z.unknown().optional(),
  }),
]);

const ListProxyEndpointsSchema = z.object({
  items: z.array(ProxyEndpointsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const CreateProxyEndpointSchema = z.union([
  z.object({
    created_at: z.unknown().optional(),
    id: z.unknown().optional(),
    ips: z.unknown(),
    kind: z.enum(["ip"]).optional(),
    name: z.unknown(),
    subdomain: z.unknown().optional(),
    updated_at: z.unknown().optional(),
  }),
  z.object({
    created_at: z.unknown().optional(),
    id: z.unknown().optional(),
    kind: z.enum(["identity"]),
    name: z.unknown(),
    subdomain: z.unknown().optional(),
    updated_at: z.unknown().optional(),
  }),
]);

const GetZeroTrustGatewayProxyEndpointsProxyEndpointDetailsSchema = z.union([
  z.object({
    created_at: z.unknown().optional(),
    id: z.unknown().optional(),
    ips: z.unknown(),
    kind: z.enum(["ip"]).optional(),
    name: z.unknown(),
    subdomain: z.unknown().optional(),
    updated_at: z.unknown().optional(),
  }),
  z.object({
    created_at: z.unknown().optional(),
    id: z.unknown().optional(),
    kind: z.enum(["identity"]),
    name: z.unknown(),
    subdomain: z.unknown().optional(),
    updated_at: z.unknown().optional(),
  }),
]);

const ZeroTrustGatewayRulesItemSchema = z.object({
  action: z.unknown(),
  created_at: z.unknown().optional(),
  deleted_at: z.unknown().optional(),
  description: z.unknown().optional(),
  device_posture: z.unknown().optional(),
  enabled: z.unknown(),
  expiration: z.unknown().optional(),
  filters: z.unknown(),
  id: z.unknown().optional(),
  identity: z.unknown().optional(),
  name: z.unknown(),
  precedence: z.unknown(),
  read_only: z.unknown().optional(),
  rule_settings: z.unknown().optional(),
  schedule: z.unknown().optional(),
  sharable: z.unknown().optional(),
  source_account: z.unknown().optional(),
  traffic: z.unknown(),
  updated_at: z.unknown().optional(),
  version: z.unknown().optional(),
  warning_status: z.unknown().optional(),
});

const ListZeroTrustGatewayRulesSchema = z.object({
  items: z.array(ZeroTrustGatewayRulesItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const CreateZeroTrustGatewayRuleSchema = z.object({
  action: z.unknown(),
  created_at: z.unknown().optional(),
  deleted_at: z.unknown().optional(),
  description: z.unknown().optional(),
  device_posture: z.unknown().optional(),
  enabled: z.unknown(),
  expiration: z.unknown().optional(),
  filters: z.unknown(),
  id: z.unknown().optional(),
  identity: z.unknown().optional(),
  name: z.unknown(),
  precedence: z.unknown(),
  read_only: z.unknown().optional(),
  rule_settings: z.unknown().optional(),
  schedule: z.unknown().optional(),
  sharable: z.unknown().optional(),
  source_account: z.unknown().optional(),
  traffic: z.unknown(),
  updated_at: z.unknown().optional(),
  version: z.unknown().optional(),
  warning_status: z.unknown().optional(),
  fetchedAt: z.string().optional().describe(
    "ISO 8601 timestamp when data was fetched",
  ),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const PatchMultipleZeroTrustGatewayRulesSchema = z.object({
  action: z.unknown(),
  created_at: z.unknown().optional(),
  deleted_at: z.unknown().optional(),
  description: z.unknown().optional(),
  device_posture: z.unknown().optional(),
  enabled: z.unknown(),
  expiration: z.unknown().optional(),
  filters: z.unknown(),
  id: z.unknown().optional(),
  identity: z.unknown().optional(),
  name: z.unknown(),
  precedence: z.unknown(),
  read_only: z.unknown().optional(),
  rule_settings: z.unknown().optional(),
  schedule: z.unknown().optional(),
  sharable: z.unknown().optional(),
  source_account: z.unknown().optional(),
  traffic: z.unknown(),
  updated_at: z.unknown().optional(),
  version: z.unknown().optional(),
  warning_status: z.unknown().optional(),
  fetchedAt: z.string().optional().describe(
    "ISO 8601 timestamp when data was fetched",
  ),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const ZeroTrustGatewayRulesTenantItemSchema = z.object({
  action: z.unknown(),
  created_at: z.unknown().optional(),
  deleted_at: z.unknown().optional(),
  description: z.unknown().optional(),
  device_posture: z.unknown().optional(),
  enabled: z.unknown(),
  expiration: z.unknown().optional(),
  filters: z.unknown(),
  id: z.unknown().optional(),
  identity: z.unknown().optional(),
  name: z.unknown(),
  precedence: z.unknown(),
  read_only: z.unknown().optional(),
  rule_settings: z.unknown().optional(),
  schedule: z.unknown().optional(),
  sharable: z.unknown().optional(),
  source_account: z.unknown().optional(),
  traffic: z.unknown(),
  updated_at: z.unknown().optional(),
  version: z.unknown().optional(),
  warning_status: z.unknown().optional(),
});

const ListZeroTrustGatewayRulesTenantSchema = z.object({
  items: z.array(ZeroTrustGatewayRulesTenantItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const GetZeroTrustGatewayRulesZeroTrustGatewayRuleDetailsSchema = z.object({
  action: z.unknown(),
  created_at: z.unknown().optional(),
  deleted_at: z.unknown().optional(),
  description: z.unknown().optional(),
  device_posture: z.unknown().optional(),
  enabled: z.unknown(),
  expiration: z.unknown().optional(),
  filters: z.unknown(),
  id: z.unknown().optional(),
  identity: z.unknown().optional(),
  name: z.unknown(),
  precedence: z.unknown(),
  read_only: z.unknown().optional(),
  rule_settings: z.unknown().optional(),
  schedule: z.unknown().optional(),
  sharable: z.unknown().optional(),
  source_account: z.unknown().optional(),
  traffic: z.unknown(),
  updated_at: z.unknown().optional(),
  version: z.unknown().optional(),
  warning_status: z.unknown().optional(),
  fetchedAt: z.string().optional().describe(
    "ISO 8601 timestamp when data was fetched",
  ),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const PatchZeroTrustGatewayRuleSchema = z.object({
  action: z.unknown(),
  created_at: z.unknown().optional(),
  deleted_at: z.unknown().optional(),
  description: z.unknown().optional(),
  device_posture: z.unknown().optional(),
  enabled: z.unknown(),
  expiration: z.unknown().optional(),
  filters: z.unknown(),
  id: z.unknown().optional(),
  identity: z.unknown().optional(),
  name: z.unknown(),
  precedence: z.unknown(),
  read_only: z.unknown().optional(),
  rule_settings: z.unknown().optional(),
  schedule: z.unknown().optional(),
  sharable: z.unknown().optional(),
  source_account: z.unknown().optional(),
  traffic: z.unknown(),
  updated_at: z.unknown().optional(),
  version: z.unknown().optional(),
  warning_status: z.unknown().optional(),
  fetchedAt: z.string().optional().describe(
    "ISO 8601 timestamp when data was fetched",
  ),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const ZeroTrustGatewayRulesResetExpirationZeroTrustGatewayRuleSchema = z.object(
  {
    action: z.unknown(),
    created_at: z.unknown().optional(),
    deleted_at: z.unknown().optional(),
    description: z.unknown().optional(),
    device_posture: z.unknown().optional(),
    enabled: z.unknown(),
    expiration: z.unknown().optional(),
    filters: z.unknown(),
    id: z.unknown().optional(),
    identity: z.unknown().optional(),
    name: z.unknown(),
    precedence: z.unknown(),
    read_only: z.unknown().optional(),
    rule_settings: z.unknown().optional(),
    schedule: z.unknown().optional(),
    sharable: z.unknown().optional(),
    source_account: z.unknown().optional(),
    traffic: z.unknown(),
    updated_at: z.unknown().optional(),
    version: z.unknown().optional(),
    warning_status: z.unknown().optional(),
  },
);

// =============================================================================
// Model Definition
// =============================================================================

/** Cloudflare Gateway — DNS/HTTP policies, locations, proxy endpoints */
export const model = {
  type: "@webframp/cloudflare/gateway",
  version: "2026.08.25.1",
  globalArguments: GlobalArgsSchema,

  upgrades: [
    {
      toVersion: "2026.08.24.1",
      description:
        "Added optional durationMs, collectedBy, and fetchedAt output metadata fields",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
  ],

  resources: {
    "zero_trust_account_information": {
      description: "Get Zero Trust account information",
      schema: GetZeroTrustAccountInformationSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "zero_trust_account": {
      description: "Create Zero Trust account",
      schema: CreateZeroTrustAccountSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "application_and_application_type_mappings": {
      description: "List application and application type mappings",
      schema: ListApplicationAndApplicationTypeMappingsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "list": {
      description: "List applications review statuses",
      schema: ListSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "update": {
      description: "Update applications review statuses",
      schema: UpdateSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "audit_ssh_settings": {
      description: "Get Zero Trust SSH settings",
      schema: GetAuditSshSettingsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "zero_trust_rotate_ssh_account_seed": {
      description: "Rotate Zero Trust SSH account seed",
      schema: ZeroTrustRotateSshAccountSeedSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "categories": {
      description: "List categories",
      schema: ListCategoriesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "zero_trust_certificates": {
      description: "List Zero Trust certificates",
      schema: ListZeroTrustCertificatesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "zero_trust_certificate": {
      description: "Create Zero Trust certificate",
      schema: CreateZeroTrustCertificateSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "zero_trust_certificates_zero_trust_certificate_details": {
      description: "Get Zero Trust certificate details",
      schema: GetZeroTrustCertificatesZeroTrustCertificateDetailsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "zero_trust_certificates_activate_zero_trust_certificate": {
      description: "Activate a Zero Trust certificate",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "zero_trust_certificates_deactivate_zero_trust_certificate": {
      description: "Deactivate a Zero Trust certificate",
      schema: ZeroTrustCertificatesDeactivateZeroTrustCertificateSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "zero_trust_account_configuration": {
      description: "Get Zero Trust account configuration",
      schema: GetZeroTrustAccountConfigurationSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "patch_zero_trust_account_configuration": {
      description: "Patch Zero Trust account configuration",
      schema: PatchZeroTrustAccountConfigurationSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "dns_destination_ips": {
      description: "List Zero Trust Gateway DNS destination IPv4 address pairs",
      schema: ListDnsDestinationIpsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "get_egress_cidr_pairs": {
      description: "Get gateway egress CIDRs pairs assigned to this account",
      schema: GetEgressCidrPairsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "zero_trust_lists": {
      description: "List Zero Trust lists",
      schema: ListZeroTrustListsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "list_details": {
      description: "Get Zero Trust list details",
      schema: ListDetailsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "items": {
      description: "Get Zero Trust list items",
      schema: ListItemsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "zero_trust_gateway_locations": {
      description: "List Zero Trust Gateway locations",
      schema: ListZeroTrustGatewayLocationsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "zero_trust_gateway_location": {
      description: "Create a Zero Trust Gateway location",
      schema: CreateZeroTrustGatewayLocationSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "zero_trust_gateway_locations_zero_trust_gateway_location_details": {
      description: "Get Zero Trust Gateway location details",
      schema: GetZeroTrustGatewayLocationsZeroTrustGatewayLocationDetailsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "logging_settings_for_the_zero_trust_account": {
      description: "Get logging settings for the Zero Trust account",
      schema: GetLoggingSettingsForTheZeroTrustAccountSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "zero_trust_gateway_operations": {
      description: "List Zero Trust Gateway operations",
      schema: ListZeroTrustGatewayOperationsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "zero_trust_gateway_operations_zero_trust_gateway_operation_details": {
      description: "Zero Trust Gateway operation details",
      schema:
        GetZeroTrustGatewayOperationsZeroTrustGatewayOperationDetailsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "pacfile": {
      description: "Create a PAC file",
      schema: CreatePacfileSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "zero_trust_gateway_pacfiles_details": {
      description: "Get a PAC file",
      schema: GetZeroTrustGatewayPacfilesDetailsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "proxy_endpoints": {
      description: "List proxy endpoints",
      schema: ListProxyEndpointsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "proxy_endpoint": {
      description: "Create a proxy endpoint",
      schema: CreateProxyEndpointSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "zero_trust_gateway_proxy_endpoints_proxy_endpoint_details": {
      description: "Get a proxy endpoint",
      schema: GetZeroTrustGatewayProxyEndpointsProxyEndpointDetailsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "zero_trust_gateway_rules": {
      description: "List Zero Trust Gateway rules",
      schema: ListZeroTrustGatewayRulesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "zero_trust_gateway_rule": {
      description: "Create a Zero Trust Gateway rule",
      schema: CreateZeroTrustGatewayRuleSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "patch_multiple_zero_trust_gateway_rules": {
      description: "Patch multiple Zero Trust Gateway rules",
      schema: PatchMultipleZeroTrustGatewayRulesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "zero_trust_gateway_rules_tenant": {
      description:
        "List Zero Trust Gateway rules inherited from the parent account",
      schema: ListZeroTrustGatewayRulesTenantSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "zero_trust_gateway_rules_zero_trust_gateway_rule_details": {
      description: "Get Zero Trust Gateway rule details.",
      schema: GetZeroTrustGatewayRulesZeroTrustGatewayRuleDetailsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "patch_zero_trust_gateway_rule": {
      description: "Patch a Zero Trust Gateway rule",
      schema: PatchZeroTrustGatewayRuleSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "zero_trust_gateway_rules_reset_expiration_zero_trust_gateway_rule": {
      description: "Reset the expiration of a Zero Trust Gateway Rule",
      schema: ZeroTrustGatewayRulesResetExpirationZeroTrustGatewayRuleSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
  },

  methods: {
    get_zero_trust_account_information: {
      description: "Get Zero Trust account information",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, accountId } = context.globalArgs;
        let result: Record<string, unknown>;
        try {
          result = await cfApi<Record<string, unknown>>(
            apiToken,
            "GET",
            `/accounts/${accountId}/gateway`,
          );
        } catch (error) {
          throw new Error(
            `Failed to get Zero Trust account information (accountId=${accountId}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        const handle = await context.writeResource(
          "zero_trust_account_information",
          "latest",
          {
            ...result,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info("Fetched zero_trust_account_information", {});
        return { dataHandles: [handle] };
      },
    },
    create_zero_trust_account: {
      description: "Create Zero Trust account",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, accountId } = context.globalArgs;

        let result: Record<string, unknown>;
        try {
          result = await cfApi<Record<string, unknown>>(
            apiToken,
            "POST",
            `/accounts/${accountId}/gateway`,
          );
        } catch (error) {
          throw new Error(
            `Failed to create Zero Trust account (accountId=${accountId}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        const handle = await context.writeResource(
          "create_zero_trust_account",
          "latest",
          {
            ...result ?? {},
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info("Executed create_zero_trust_account", {});
        return { dataHandles: [handle] };
      },
    },
    list_application_and_application_type_mappings: {
      description: "List application and application type mappings",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        let results: Record<string, unknown>[];
        let truncated: boolean;
        try {
          ({ results, truncated } = await cfApiPaginated<
            Record<string, unknown>
          >(
            apiToken,
            `/accounts/${accountId}/gateway/app_types`,
            params,
          ));
        } catch (error) {
          throw new Error(
            `Failed to list application and application type mappings (accountId=${accountId}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "application_and_application_type_mappings",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );

        context.logger.info(
          "Found {count} application_and_application_type_mappings",
          { count: results.length },
        );
        return { dataHandles: [handle] };
      },
    },
    list: {
      description: "List applications review statuses",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, accountId } = context.globalArgs;
        let result: Record<string, unknown>;
        try {
          result = await cfApi<Record<string, unknown>>(
            apiToken,
            "GET",
            `/accounts/${accountId}/gateway/apps/review_status`,
          );
        } catch (error) {
          throw new Error(
            `Failed to list applications review statuses (accountId=${accountId}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        const handle = await context.writeResource("list", "latest", {
          ...result,
          fetchedAt: new Date().toISOString(),
          durationMs: Date.now() - startMs,
          collectedBy: EXTENSION_NAME,
        });
        context.logger.info("Fetched list", {});
        return { dataHandles: [handle] };
      },
    },
    update: {
      description: "Update applications review statuses",
      arguments: z.object({
        approved_apps: z.unknown(),
        in_review_apps: z.unknown(),
        unapproved_apps: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        let result: Record<string, unknown>;
        try {
          result = await cfApi<Record<string, unknown>>(
            apiToken,
            "PUT",
            `/accounts/${accountId}/gateway/apps/review_status`,
            body,
          );
        } catch (error) {
          throw new Error(
            `Failed to update applications review statuses (accountId=${accountId}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        const handle = await context.writeResource("update", "updated", {
          ...result,
          fetchedAt: new Date().toISOString(),
          durationMs: Date.now() - startMs,
          collectedBy: EXTENSION_NAME,
        });
        context.logger.info("Updated update", {});
        return { dataHandles: [handle] };
      },
    },
    get_audit_ssh_settings: {
      description: "Get Zero Trust SSH settings",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, accountId } = context.globalArgs;
        let result: Record<string, unknown>;
        try {
          result = await cfApi<Record<string, unknown>>(
            apiToken,
            "GET",
            `/accounts/${accountId}/gateway/audit_ssh_settings`,
          );
        } catch (error) {
          throw new Error(
            `Failed to get Zero Trust SSH settings (accountId=${accountId}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        const handle = await context.writeResource(
          "audit_ssh_settings",
          "latest",
          {
            ...result,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info("Fetched audit_ssh_settings", {});
        return { dataHandles: [handle] };
      },
    },
    update_audit_ssh_settings: {
      description: "Update Zero Trust SSH settings",
      arguments: z.object({
        public_key: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        let result: Record<string, unknown>;
        try {
          result = await cfApi<Record<string, unknown>>(
            apiToken,
            "PUT",
            `/accounts/${accountId}/gateway/audit_ssh_settings`,
            body,
          );
        } catch (error) {
          throw new Error(
            `Failed to update Zero Trust SSH settings (accountId=${accountId}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        const handle = await context.writeResource(
          "audit_ssh_settings",
          "updated",
          {
            ...result,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info("Updated audit_ssh_settings", {});
        return { dataHandles: [handle] };
      },
    },
    zero_trust_rotate_ssh_account_seed: {
      description: "Rotate Zero Trust SSH account seed",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, accountId } = context.globalArgs;

        let result: Record<string, unknown>;
        try {
          result = await cfApi<Record<string, unknown>>(
            apiToken,
            "POST",
            `/accounts/${accountId}/gateway/audit_ssh_settings/rotate_seed`,
          );
        } catch (error) {
          throw new Error(
            `Failed to rotate Zero Trust SSH account seed (accountId=${accountId}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        const handle = await context.writeResource(
          "zero_trust_rotate_ssh_account_seed",
          "latest",
          {
            ...result ?? {},
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info("Executed zero_trust_rotate_ssh_account_seed", {});
        return { dataHandles: [handle] };
      },
    },
    list_categories: {
      description: "List categories",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        let results: Record<string, unknown>[];
        let truncated: boolean;
        try {
          ({ results, truncated } = await cfApiPaginated<
            Record<string, unknown>
          >(
            apiToken,
            `/accounts/${accountId}/gateway/categories`,
            params,
          ));
        } catch (error) {
          throw new Error(
            `Failed to list categories (accountId=${accountId}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("categories", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
          durationMs: Date.now() - startMs,
          collectedBy: EXTENSION_NAME,
        });

        context.logger.info("Found {count} categories", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    list_zero_trust_certificates: {
      description: "List Zero Trust certificates",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        let results: Record<string, unknown>[];
        let truncated: boolean;
        try {
          ({ results, truncated } = await cfApiPaginated<
            Record<string, unknown>
          >(
            apiToken,
            `/accounts/${accountId}/gateway/certificates`,
            params,
          ));
        } catch (error) {
          throw new Error(
            `Failed to list Zero Trust certificates (accountId=${accountId}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "zero_trust_certificates",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );

        context.logger.info("Found {count} zero_trust_certificates", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_zero_trust_certificate: {
      description: "Create Zero Trust certificate",
      arguments: z.object({
        validity_period_days: z.number().int().optional().describe(
          "Sets the certificate validity period in days (range: 1-10,950 days / ~30 year...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        let result: Record<string, unknown>;
        try {
          result = await cfApi<Record<string, unknown>>(
            apiToken,
            "POST",
            `/accounts/${accountId}/gateway/certificates`,
            body,
          );
        } catch (error) {
          throw new Error(
            `Failed to create Zero Trust certificate (accountId=${accountId}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "zero_trust_certificate",
          id,
          {
            ...result,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info("Created zero_trust_certificate {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_zero_trust_certificates_zero_trust_certificate_details: {
      description: "Get Zero Trust certificate details",
      arguments: z.object({
        certificate_id: z.string().min(1, "certificate_id must not be empty"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, accountId } = context.globalArgs;
        let result: Record<string, unknown>;
        try {
          result = await cfApi<Record<string, unknown>>(
            apiToken,
            "GET",
            `/accounts/${accountId}/gateway/certificates/${args.certificate_id}`,
          );
        } catch (error) {
          throw new Error(
            `Failed to get Zero Trust certificate details (accountId=${accountId} certificate_id=${args.certificate_id}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        const handle = await context.writeResource(
          "zero_trust_certificates_zero_trust_certificate_details",
          String(args.certificate_id),
          {
            ...result,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info(
          "Fetched zero_trust_certificates_zero_trust_certificate_details",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    delete_zero_trust_certificate: {
      description: "Delete Zero Trust certificate",
      arguments: z.object({
        certificate_id: z.string().min(1, "certificate_id must not be empty"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        try {
          await cfApi(
            apiToken,
            "DELETE",
            `/accounts/${accountId}/gateway/certificates/${args.certificate_id}`,
          );
        } catch (error) {
          throw new Error(
            `Failed to delete Zero Trust certificate (accountId=${accountId} certificate_id=${args.certificate_id}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        context.logger.info("Deleted resource {id}", {
          id: args.certificate_id,
        });
        return { dataHandles: [] };
      },
    },
    zero_trust_certificates_activate_zero_trust_certificate: {
      description: "Activate a Zero Trust certificate",
      arguments: z.object({
        certificate_id: z.string().min(1, "certificate_id must not be empty"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, accountId } = context.globalArgs;

        let result: Record<string, unknown>;
        try {
          result = await cfApi<Record<string, unknown>>(
            apiToken,
            "POST",
            `/accounts/${accountId}/gateway/certificates/${args.certificate_id}/activate`,
          );
        } catch (error) {
          throw new Error(
            `Failed to activate a Zero Trust certificate (accountId=${accountId} certificate_id=${args.certificate_id}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        const handle = await context.writeResource(
          "zero_trust_certificates_activate_zero_trust_certificate",
          "latest",
          {
            ...result ?? {},
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info(
          "Executed zero_trust_certificates_activate_zero_trust_certificate",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    zero_trust_certificates_deactivate_zero_trust_certificate: {
      description: "Deactivate a Zero Trust certificate",
      arguments: z.object({
        certificate_id: z.string().min(1, "certificate_id must not be empty"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, accountId } = context.globalArgs;

        let result: Record<string, unknown>;
        try {
          result = await cfApi<Record<string, unknown>>(
            apiToken,
            "POST",
            `/accounts/${accountId}/gateway/certificates/${args.certificate_id}/deactivate`,
          );
        } catch (error) {
          throw new Error(
            `Failed to deactivate a Zero Trust certificate (accountId=${accountId} certificate_id=${args.certificate_id}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        const handle = await context.writeResource(
          "zero_trust_certificates_deactivate_zero_trust_certificate",
          "latest",
          {
            ...result ?? {},
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info(
          "Executed zero_trust_certificates_deactivate_zero_trust_certificate",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    get_zero_trust_account_configuration: {
      description: "Get Zero Trust account configuration",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, accountId } = context.globalArgs;
        let result: Record<string, unknown>;
        try {
          result = await cfApi<Record<string, unknown>>(
            apiToken,
            "GET",
            `/accounts/${accountId}/gateway/configuration`,
          );
        } catch (error) {
          throw new Error(
            `Failed to get Zero Trust account configuration (accountId=${accountId}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        const handle = await context.writeResource(
          "zero_trust_account_configuration",
          "latest",
          {
            ...result,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info("Fetched zero_trust_account_configuration", {});
        return { dataHandles: [handle] };
      },
    },
    update_zero_trust_account_configuration: {
      description: "Update Zero Trust account configuration",
      arguments: z.object({
        settings: z.object({
          activity_log: z.unknown().optional(),
          antivirus: z.unknown().optional(),
          block_page: z.unknown().optional(),
          body_scanning: z.unknown().optional(),
          browser_isolation: z.unknown().optional(),
          certificate: z.unknown().optional(),
          custom_certificate: z.unknown().optional(),
          extended_email_matching: z.unknown().optional(),
          fips: z.unknown().optional(),
          host_selector: z.unknown().optional(),
          inspection: z.unknown().optional(),
          max_ttl_secs: z.unknown().optional(),
          protocol_detection: z.unknown().optional(),
          sandbox: z.unknown().optional(),
          tls_decrypt: z.unknown().optional(),
        }).optional().describe("Specify account settings."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        let result: Record<string, unknown>;
        try {
          result = await cfApi<Record<string, unknown>>(
            apiToken,
            "PUT",
            `/accounts/${accountId}/gateway/configuration`,
            body,
          );
        } catch (error) {
          throw new Error(
            `Failed to update Zero Trust account configuration (accountId=${accountId}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        const handle = await context.writeResource(
          "zero_trust_account_configuration",
          "updated",
          {
            ...result,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info("Updated zero_trust_account_configuration", {});
        return { dataHandles: [handle] };
      },
    },
    patch_zero_trust_account_configuration: {
      description: "Patch Zero Trust account configuration",
      arguments: z.object({
        settings: z.object({
          activity_log: z.unknown().optional(),
          antivirus: z.unknown().optional(),
          block_page: z.unknown().optional(),
          body_scanning: z.unknown().optional(),
          browser_isolation: z.unknown().optional(),
          certificate: z.unknown().optional(),
          custom_certificate: z.unknown().optional(),
          extended_email_matching: z.unknown().optional(),
          fips: z.unknown().optional(),
          host_selector: z.unknown().optional(),
          inspection: z.unknown().optional(),
          max_ttl_secs: z.unknown().optional(),
          protocol_detection: z.unknown().optional(),
          sandbox: z.unknown().optional(),
          tls_decrypt: z.unknown().optional(),
        }).optional().describe("Specify account settings."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        let result: Record<string, unknown>;
        try {
          result = await cfApi<Record<string, unknown>>(
            apiToken,
            "PATCH",
            `/accounts/${accountId}/gateway/configuration`,
            body,
          );
        } catch (error) {
          throw new Error(
            `Failed to patch Zero Trust account configuration (accountId=${accountId}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        const handle = await context.writeResource(
          "patch_zero_trust_account_configuration",
          "updated",
          {
            ...result,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info(
          "Updated patch_zero_trust_account_configuration",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    list_dns_destination_ips: {
      description: "List Zero Trust Gateway DNS destination IPv4 address pairs",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        let results: Record<string, unknown>[];
        let truncated: boolean;
        try {
          ({ results, truncated } = await cfApiPaginated<
            Record<string, unknown>
          >(
            apiToken,
            `/accounts/${accountId}/gateway/dns_destination_ips`,
            params,
          ));
        } catch (error) {
          throw new Error(
            `Failed to list Zero Trust Gateway DNS destination IPv4 address pairs (accountId=${accountId}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "dns_destination_ips",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );

        context.logger.info("Found {count} dns_destination_ips", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    get_egress_cidr_pairs: {
      description: "Get gateway egress CIDRs pairs assigned to this account",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        let results: Record<string, unknown>[];
        let truncated: boolean;
        try {
          ({ results, truncated } = await cfApiPaginated<
            Record<string, unknown>
          >(
            apiToken,
            `/accounts/${accountId}/gateway/egress_cidr_pairs`,
            params,
          ));
        } catch (error) {
          throw new Error(
            `Failed to get gateway egress CIDRs pairs assigned to this account (accountId=${accountId}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "get_egress_cidr_pairs",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );

        context.logger.info("Found {count} get_egress_cidr_pairs", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    list_zero_trust_lists: {
      description: "List Zero Trust lists",
      arguments: z.object({
        type: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        let results: Record<string, unknown>[];
        let truncated: boolean;
        try {
          ({ results, truncated } = await cfApiPaginated<
            Record<string, unknown>
          >(
            apiToken,
            `/accounts/${accountId}/gateway/lists`,
            params,
          ));
        } catch (error) {
          throw new Error(
            `Failed to list Zero Trust lists (accountId=${accountId}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("zero_trust_lists", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
          durationMs: Date.now() - startMs,
          collectedBy: EXTENSION_NAME,
        });

        context.logger.info("Found {count} zero_trust_lists", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    list_details: {
      description: "Get Zero Trust list details",
      arguments: z.object({
        list_id: z.string().min(1, "list_id must not be empty"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, accountId } = context.globalArgs;
        let result: Record<string, unknown>;
        try {
          result = await cfApi<Record<string, unknown>>(
            apiToken,
            "GET",
            `/accounts/${accountId}/gateway/lists/${args.list_id}`,
          );
        } catch (error) {
          throw new Error(
            `Failed to get Zero Trust list details (accountId=${accountId} list_id=${args.list_id}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        const handle = await context.writeResource(
          "list_details",
          String(args.list_id),
          {
            ...result,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info("Fetched list_details", {});
        return { dataHandles: [handle] };
      },
    },
    list_items: {
      description: "Get Zero Trust list items",
      arguments: z.object({
        list_id: z.string().min(1, "list_id must not be empty"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["list_id", "page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        let results: Record<string, unknown>[];
        let truncated: boolean;
        try {
          ({ results, truncated } = await cfApiPaginated<
            Record<string, unknown>
          >(
            apiToken,
            `/accounts/${accountId}/gateway/lists/${args.list_id}/items`,
            params,
          ));
        } catch (error) {
          throw new Error(
            `Failed to get Zero Trust list items (accountId=${accountId} list_id=${args.list_id}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("items", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
          durationMs: Date.now() - startMs,
          collectedBy: EXTENSION_NAME,
        });

        context.logger.info("Found {count} items", { count: results.length });
        return { dataHandles: [handle] };
      },
    },
    list_zero_trust_gateway_locations: {
      description: "List Zero Trust Gateway locations",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        let results: Record<string, unknown>[];
        let truncated: boolean;
        try {
          ({ results, truncated } = await cfApiPaginated<
            Record<string, unknown>
          >(
            apiToken,
            `/accounts/${accountId}/gateway/locations`,
            params,
          ));
        } catch (error) {
          throw new Error(
            `Failed to list Zero Trust Gateway locations (accountId=${accountId}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "zero_trust_gateway_locations",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );

        context.logger.info("Found {count} zero_trust_gateway_locations", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_zero_trust_gateway_location: {
      description: "Create a Zero Trust Gateway location",
      arguments: z.object({
        client_default: z.unknown().optional(),
        dns_destination_ips_id: z.unknown().optional(),
        ecs_support: z.unknown().optional(),
        endpoints: z.unknown().optional(),
        max_ttl: z.unknown().optional(),
        name: z.unknown(),
        networks: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        let result: Record<string, unknown>;
        try {
          result = await cfApi<Record<string, unknown>>(
            apiToken,
            "POST",
            `/accounts/${accountId}/gateway/locations`,
            body,
          );
        } catch (error) {
          throw new Error(
            `Failed to create a Zero Trust Gateway location (accountId=${accountId}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "zero_trust_gateway_location",
          id,
          {
            ...result,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info("Created zero_trust_gateway_location {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_zero_trust_gateway_locations_zero_trust_gateway_location_details: {
      description: "Get Zero Trust Gateway location details",
      arguments: z.object({
        location_id: z.string().min(1, "location_id must not be empty"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, accountId } = context.globalArgs;
        let result: Record<string, unknown>;
        try {
          result = await cfApi<Record<string, unknown>>(
            apiToken,
            "GET",
            `/accounts/${accountId}/gateway/locations/${args.location_id}`,
          );
        } catch (error) {
          throw new Error(
            `Failed to get Zero Trust Gateway location details (accountId=${accountId} location_id=${args.location_id}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        const handle = await context.writeResource(
          "zero_trust_gateway_locations_zero_trust_gateway_location_details",
          String(args.location_id),
          {
            ...result,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info(
          "Fetched zero_trust_gateway_locations_zero_trust_gateway_location_details",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    update_zero_trust_gateway_location: {
      description: "Update a Zero Trust Gateway location",
      arguments: z.object({
        location_id: z.string().min(1, "location_id must not be empty"),
        client_default: z.unknown().optional(),
        dns_destination_ips_id: z.unknown().optional(),
        ecs_support: z.unknown().optional(),
        endpoints: z.unknown().optional(),
        max_ttl: z.unknown().optional(),
        name: z.unknown(),
        networks: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["location_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        let result: Record<string, unknown>;
        try {
          result = await cfApi<Record<string, unknown>>(
            apiToken,
            "PUT",
            `/accounts/${accountId}/gateway/locations/${args.location_id}`,
            body,
          );
        } catch (error) {
          throw new Error(
            `Failed to update a Zero Trust Gateway location (accountId=${accountId} location_id=${args.location_id}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        const handle = await context.writeResource(
          "zero_trust_gateway_location",
          String(args.location_id),
          {
            ...result,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info("Updated zero_trust_gateway_location", {});
        return { dataHandles: [handle] };
      },
    },
    delete_zero_trust_gateway_location: {
      description: "Delete a Zero Trust Gateway location",
      arguments: z.object({
        location_id: z.string().min(1, "location_id must not be empty"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        try {
          await cfApi(
            apiToken,
            "DELETE",
            `/accounts/${accountId}/gateway/locations/${args.location_id}`,
          );
        } catch (error) {
          throw new Error(
            `Failed to delete a Zero Trust Gateway location (accountId=${accountId} location_id=${args.location_id}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        context.logger.info("Deleted resource {id}", { id: args.location_id });
        return { dataHandles: [] };
      },
    },
    get_logging_settings_for_the_zero_trust_account: {
      description: "Get logging settings for the Zero Trust account",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, accountId } = context.globalArgs;
        let result: Record<string, unknown>;
        try {
          result = await cfApi<Record<string, unknown>>(
            apiToken,
            "GET",
            `/accounts/${accountId}/gateway/logging`,
          );
        } catch (error) {
          throw new Error(
            `Failed to get logging settings for the Zero Trust account (accountId=${accountId}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        const handle = await context.writeResource(
          "logging_settings_for_the_zero_trust_account",
          "latest",
          {
            ...result,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info(
          "Fetched logging_settings_for_the_zero_trust_account",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    update_logging_settings_for_the_zero_trust_account: {
      description: "Update Zero Trust account logging settings",
      arguments: z.object({
        redact_pii: z.boolean().optional().describe(
          "Indicate whether to redact personally identifiable information from activity ...",
        ),
        settings_by_rule_type: z.object({
          dns: z.unknown().optional(),
          http: z.unknown().optional(),
          l4: z.unknown().optional(),
        }).optional().describe(
          "Configure logging settings for each rule type.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        let result: Record<string, unknown>;
        try {
          result = await cfApi<Record<string, unknown>>(
            apiToken,
            "PUT",
            `/accounts/${accountId}/gateway/logging`,
            body,
          );
        } catch (error) {
          throw new Error(
            `Failed to update Zero Trust account logging settings (accountId=${accountId}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        const handle = await context.writeResource(
          "logging_settings_for_the_zero_trust_account",
          "updated",
          {
            ...result,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info(
          "Updated logging_settings_for_the_zero_trust_account",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    list_zero_trust_gateway_operations: {
      description: "List Zero Trust Gateway operations",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        let results: Record<string, unknown>[];
        let truncated: boolean;
        try {
          ({ results, truncated } = await cfApiPaginated<
            Record<string, unknown>
          >(
            apiToken,
            `/accounts/${accountId}/gateway/operations`,
            params,
          ));
        } catch (error) {
          throw new Error(
            `Failed to list Zero Trust Gateway operations (accountId=${accountId}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "zero_trust_gateway_operations",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );

        context.logger.info("Found {count} zero_trust_gateway_operations", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    get_zero_trust_gateway_operations_zero_trust_gateway_operation_details: {
      description: "Zero Trust Gateway operation details",
      arguments: z.object({
        operation_id: z.string().min(1, "operation_id must not be empty"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, accountId } = context.globalArgs;
        let result: Record<string, unknown>;
        try {
          result = await cfApi<Record<string, unknown>>(
            apiToken,
            "GET",
            `/accounts/${accountId}/gateway/operations/${args.operation_id}`,
          );
        } catch (error) {
          throw new Error(
            `Failed to zero Trust Gateway operation details (accountId=${accountId} operation_id=${args.operation_id}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        const handle = await context.writeResource(
          "zero_trust_gateway_operations_zero_trust_gateway_operation_details",
          String(args.operation_id),
          {
            ...result,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info(
          "Fetched zero_trust_gateway_operations_zero_trust_gateway_operation_details",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    create_pacfile: {
      description: "Create a PAC file",
      arguments: z.object({
        contents: z.unknown(),
        description: z.unknown().optional(),
        name: z.unknown(),
        slug: z.string().optional().describe(
          "URL-friendly version of the PAC file name. If not provided, it will be auto-g...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        let result: Record<string, unknown>;
        try {
          result = await cfApi<Record<string, unknown>>(
            apiToken,
            "POST",
            `/accounts/${accountId}/gateway/pacfiles`,
            body,
          );
        } catch (error) {
          throw new Error(
            `Failed to create a PAC file (accountId=${accountId}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource("pacfile", id, {
          ...result,
          fetchedAt: new Date().toISOString(),
          durationMs: Date.now() - startMs,
          collectedBy: EXTENSION_NAME,
        });
        context.logger.info("Created pacfile {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_zero_trust_gateway_pacfiles_details: {
      description: "Get a PAC file",
      arguments: z.object({
        pacfile_id: z.string().min(1, "pacfile_id must not be empty"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, accountId } = context.globalArgs;
        let result: Record<string, unknown>;
        try {
          result = await cfApi<Record<string, unknown>>(
            apiToken,
            "GET",
            `/accounts/${accountId}/gateway/pacfiles/${args.pacfile_id}`,
          );
        } catch (error) {
          throw new Error(
            `Failed to get a PAC file (accountId=${accountId} pacfile_id=${args.pacfile_id}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        const handle = await context.writeResource(
          "zero_trust_gateway_pacfiles_details",
          String(args.pacfile_id),
          {
            ...result,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info("Fetched zero_trust_gateway_pacfiles_details", {});
        return { dataHandles: [handle] };
      },
    },
    delete: {
      description: "Delete a PAC file",
      arguments: z.object({
        pacfile_id: z.string().min(1, "pacfile_id must not be empty"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        try {
          await cfApi(
            apiToken,
            "DELETE",
            `/accounts/${accountId}/gateway/pacfiles/${args.pacfile_id}`,
          );
        } catch (error) {
          throw new Error(
            `Failed to delete a PAC file (accountId=${accountId} pacfile_id=${args.pacfile_id}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        context.logger.info("Deleted resource {id}", { id: args.pacfile_id });
        return { dataHandles: [] };
      },
    },
    list_proxy_endpoints: {
      description: "List proxy endpoints",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        let results: Record<string, unknown>[];
        let truncated: boolean;
        try {
          ({ results, truncated } = await cfApiPaginated<
            Record<string, unknown>
          >(
            apiToken,
            `/accounts/${accountId}/gateway/proxy_endpoints`,
            params,
          ));
        } catch (error) {
          throw new Error(
            `Failed to list proxy endpoints (accountId=${accountId}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("proxy_endpoints", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
          durationMs: Date.now() - startMs,
          collectedBy: EXTENSION_NAME,
        });

        context.logger.info("Found {count} proxy_endpoints", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_proxy_endpoint: {
      description: "Create a proxy endpoint",
      arguments: z.object({
        body: z.union([
          z.object({
            kind: z.enum(["ip"]).optional(),
            name: z.unknown(),
          }),
          z.object({
            kind: z.enum(["identity"]),
            name: z.unknown(),
          }),
        ]),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, accountId } = context.globalArgs;

        const body = args.body;

        let result: Record<string, unknown>;
        try {
          result = await cfApi<Record<string, unknown>>(
            apiToken,
            "POST",
            `/accounts/${accountId}/gateway/proxy_endpoints`,
            body,
          );
        } catch (error) {
          throw new Error(
            `Failed to create a proxy endpoint (accountId=${accountId}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "proxy_endpoint",
          id,
          {
            ...result,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info("Created proxy_endpoint {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_zero_trust_gateway_proxy_endpoints_proxy_endpoint_details: {
      description: "Get a proxy endpoint",
      arguments: z.object({
        proxy_endpoint_id: z.string().min(
          1,
          "proxy_endpoint_id must not be empty",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, accountId } = context.globalArgs;
        let result: Record<string, unknown>;
        try {
          result = await cfApi<Record<string, unknown>>(
            apiToken,
            "GET",
            `/accounts/${accountId}/gateway/proxy_endpoints/${args.proxy_endpoint_id}`,
          );
        } catch (error) {
          throw new Error(
            `Failed to get a proxy endpoint (accountId=${accountId} proxy_endpoint_id=${args.proxy_endpoint_id}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        const handle = await context.writeResource(
          "zero_trust_gateway_proxy_endpoints_proxy_endpoint_details",
          String(args.proxy_endpoint_id),
          {
            ...result,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info(
          "Fetched zero_trust_gateway_proxy_endpoints_proxy_endpoint_details",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    update_proxy_endpoint: {
      description: "Update a proxy endpoint",
      arguments: z.object({
        proxy_endpoint_id: z.string().min(
          1,
          "proxy_endpoint_id must not be empty",
        ),
        ips: z.unknown().optional(),
        name: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["proxy_endpoint_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        let result: Record<string, unknown>;
        try {
          result = await cfApi<Record<string, unknown>>(
            apiToken,
            "PATCH",
            `/accounts/${accountId}/gateway/proxy_endpoints/${args.proxy_endpoint_id}`,
            body,
          );
        } catch (error) {
          throw new Error(
            `Failed to update a proxy endpoint (accountId=${accountId} proxy_endpoint_id=${args.proxy_endpoint_id}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        const handle = await context.writeResource(
          "proxy_endpoint",
          String(args.proxy_endpoint_id),
          {
            ...result,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info("Updated proxy_endpoint", {});
        return { dataHandles: [handle] };
      },
    },
    delete_proxy_endpoint: {
      description: "Delete a proxy endpoint",
      arguments: z.object({
        proxy_endpoint_id: z.string().min(
          1,
          "proxy_endpoint_id must not be empty",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        try {
          await cfApi(
            apiToken,
            "DELETE",
            `/accounts/${accountId}/gateway/proxy_endpoints/${args.proxy_endpoint_id}`,
          );
        } catch (error) {
          throw new Error(
            `Failed to delete a proxy endpoint (accountId=${accountId} proxy_endpoint_id=${args.proxy_endpoint_id}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        context.logger.info("Deleted resource {id}", {
          id: args.proxy_endpoint_id,
        });
        return { dataHandles: [] };
      },
    },
    list_zero_trust_gateway_rules: {
      description: "List Zero Trust Gateway rules",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        let results: Record<string, unknown>[];
        let truncated: boolean;
        try {
          ({ results, truncated } = await cfApiPaginated<
            Record<string, unknown>
          >(
            apiToken,
            `/accounts/${accountId}/gateway/rules`,
            params,
          ));
        } catch (error) {
          throw new Error(
            `Failed to list Zero Trust Gateway rules (accountId=${accountId}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "zero_trust_gateway_rules",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );

        context.logger.info("Found {count} zero_trust_gateway_rules", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_zero_trust_gateway_rule: {
      description: "Create a Zero Trust Gateway rule",
      arguments: z.object({
        action: z.unknown(),
        description: z.unknown().optional(),
        device_posture: z.unknown().optional(),
        enabled: z.unknown().optional(),
        expiration: z.unknown().optional(),
        filters: z.unknown().optional(),
        identity: z.unknown().optional(),
        name: z.unknown(),
        precedence: z.unknown().optional(),
        rule_settings: z.unknown().optional(),
        schedule: z.unknown().optional(),
        traffic: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        let result: Record<string, unknown>;
        try {
          result = await cfApi<Record<string, unknown>>(
            apiToken,
            "POST",
            `/accounts/${accountId}/gateway/rules`,
            body,
          );
        } catch (error) {
          throw new Error(
            `Failed to create a Zero Trust Gateway rule (accountId=${accountId}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "zero_trust_gateway_rule",
          id,
          {
            ...result,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info("Created zero_trust_gateway_rule {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    patch_multiple_zero_trust_gateway_rules: {
      description: "Patch multiple Zero Trust Gateway rules",
      arguments: z.object({
        items: z.array(z.object({
          description: z.unknown().optional(),
          enabled: z.unknown().optional(),
          id: z.unknown(),
          name: z.unknown().optional(),
          precedence: z.unknown().optional(),
        })),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, accountId } = context.globalArgs;

        const body = args.items;

        let result: Record<string, unknown>;
        try {
          result = await cfApi<Record<string, unknown>>(
            apiToken,
            "PATCH",
            `/accounts/${accountId}/gateway/rules`,
            body,
          );
        } catch (error) {
          throw new Error(
            `Failed to patch multiple Zero Trust Gateway rules (accountId=${accountId}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        const handle = await context.writeResource(
          "patch_multiple_zero_trust_gateway_rules",
          "updated",
          {
            ...result,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info(
          "Updated patch_multiple_zero_trust_gateway_rules",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    list_zero_trust_gateway_rules_tenant: {
      description:
        "List Zero Trust Gateway rules inherited from the parent account",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        let results: Record<string, unknown>[];
        let truncated: boolean;
        try {
          ({ results, truncated } = await cfApiPaginated<
            Record<string, unknown>
          >(
            apiToken,
            `/accounts/${accountId}/gateway/rules/tenant`,
            params,
          ));
        } catch (error) {
          throw new Error(
            `Failed to list Zero Trust Gateway rules inherited from the parent account (accountId=${accountId}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "zero_trust_gateway_rules_tenant",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );

        context.logger.info("Found {count} zero_trust_gateway_rules_tenant", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    get_zero_trust_gateway_rules_zero_trust_gateway_rule_details: {
      description: "Get Zero Trust Gateway rule details.",
      arguments: z.object({
        rule_id: z.string().min(1, "rule_id must not be empty"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, accountId } = context.globalArgs;
        let result: Record<string, unknown>;
        try {
          result = await cfApi<Record<string, unknown>>(
            apiToken,
            "GET",
            `/accounts/${accountId}/gateway/rules/${args.rule_id}`,
          );
        } catch (error) {
          throw new Error(
            `Failed to get Zero Trust Gateway rule details. (accountId=${accountId} rule_id=${args.rule_id}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        const handle = await context.writeResource(
          "zero_trust_gateway_rules_zero_trust_gateway_rule_details",
          String(args.rule_id),
          {
            ...result,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info(
          "Fetched zero_trust_gateway_rules_zero_trust_gateway_rule_details",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    update_zero_trust_gateway_rule: {
      description: "Update a Zero Trust Gateway rule",
      arguments: z.object({
        rule_id: z.string().min(1, "rule_id must not be empty"),
        action: z.unknown(),
        description: z.unknown().optional(),
        device_posture: z.unknown().optional(),
        enabled: z.unknown().optional(),
        expiration: z.unknown().optional(),
        filters: z.unknown().optional(),
        identity: z.unknown().optional(),
        name: z.unknown(),
        precedence: z.unknown().optional(),
        rule_settings: z.unknown().optional(),
        schedule: z.unknown().optional(),
        traffic: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["rule_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        let result: Record<string, unknown>;
        try {
          result = await cfApi<Record<string, unknown>>(
            apiToken,
            "PUT",
            `/accounts/${accountId}/gateway/rules/${args.rule_id}`,
            body,
          );
        } catch (error) {
          throw new Error(
            `Failed to update a Zero Trust Gateway rule (accountId=${accountId} rule_id=${args.rule_id}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        const handle = await context.writeResource(
          "zero_trust_gateway_rule",
          String(args.rule_id),
          {
            ...result,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info("Updated zero_trust_gateway_rule", {});
        return { dataHandles: [handle] };
      },
    },
    patch_zero_trust_gateway_rule: {
      description: "Patch a Zero Trust Gateway rule",
      arguments: z.object({
        rule_id: z.string().min(1, "rule_id must not be empty"),
        description: z.unknown().optional(),
        enabled: z.unknown().optional(),
        name: z.unknown().optional(),
        precedence: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["rule_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        let result: Record<string, unknown>;
        try {
          result = await cfApi<Record<string, unknown>>(
            apiToken,
            "PATCH",
            `/accounts/${accountId}/gateway/rules/${args.rule_id}`,
            body,
          );
        } catch (error) {
          throw new Error(
            `Failed to patch a Zero Trust Gateway rule (accountId=${accountId} rule_id=${args.rule_id}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        const handle = await context.writeResource(
          "patch_zero_trust_gateway_rule",
          String(args.rule_id),
          {
            ...result,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info("Updated patch_zero_trust_gateway_rule", {});
        return { dataHandles: [handle] };
      },
    },
    delete_zero_trust_gateway_rule: {
      description: "Delete a Zero Trust Gateway rule",
      arguments: z.object({
        rule_id: z.string().min(1, "rule_id must not be empty"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        try {
          await cfApi(
            apiToken,
            "DELETE",
            `/accounts/${accountId}/gateway/rules/${args.rule_id}`,
          );
        } catch (error) {
          throw new Error(
            `Failed to delete a Zero Trust Gateway rule (accountId=${accountId} rule_id=${args.rule_id}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        context.logger.info("Deleted resource {id}", { id: args.rule_id });
        return { dataHandles: [] };
      },
    },
    zero_trust_gateway_rules_reset_expiration_zero_trust_gateway_rule: {
      description: "Reset the expiration of a Zero Trust Gateway Rule",
      arguments: z.object({
        rule_id: z.string().min(1, "rule_id must not be empty"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const startMs = Date.now();
        const { apiToken, accountId } = context.globalArgs;

        let result: Record<string, unknown>;
        try {
          result = await cfApi<Record<string, unknown>>(
            apiToken,
            "POST",
            `/accounts/${accountId}/gateway/rules/${args.rule_id}/reset_expiration`,
          );
        } catch (error) {
          throw new Error(
            `Failed to reset the expiration of a Zero Trust Gateway Rule (accountId=${accountId} rule_id=${args.rule_id}): ${
              error instanceof Error ? error.message : String(error)
            }`,
            { cause: error },
          );
        }

        const handle = await context.writeResource(
          "zero_trust_gateway_rules_reset_expiration_zero_trust_gateway_rule",
          "latest",
          {
            ...result ?? {},
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );
        context.logger.info(
          "Executed zero_trust_gateway_rules_reset_expiration_zero_trust_gateway_rule",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
  },
};
