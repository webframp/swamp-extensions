// Cloudflare Images — upload, transform, deliver, and manage image pipelines — Tests
// Auto-generated by scripts/cloudflare-codegen — do not edit manually.
// SPDX-License-Identifier: Apache-2.0

import { assertEquals, assertExists } from "jsr:@std/assert@1.0.19";
import { createModelTestContext } from "@systeminit/swamp-testing";
import { model } from "./images.ts";

// ---------------------------------------------------------------------------
// Model Export Structure Tests
// ---------------------------------------------------------------------------

Deno.test("images model: has correct type", () => {
  assertEquals(model.type, "@webframp/cloudflare/images");
});

Deno.test("images model: has valid version format", () => {
  const versionPattern = /^\d{4}\.\d{2}\.\d{2}\.\d+$/;
  assertEquals(versionPattern.test(model.version), true);
});

Deno.test("images model: has globalArguments with apiToken", () => {
  assertExists(model.globalArguments);
  const shape = model.globalArguments.shape;
  assertExists(shape.apiToken);
  assertExists(shape.accountId);
});

Deno.test("images model: has expected methods", () => {
  assertExists(model.methods);
  assertExists(model.methods.cloudflare_images_upload_an_image_via_url);
  assertExists(model.methods.list_signing_keys);
  assertExists(model.methods.update_cloudflare_images_keys_add_signing_key);
  assertExists(model.methods.delete_signing_key);
  assertExists(model.methods.get_cloudflare_images_images_usage_statistics);
  assertExists(model.methods.list_variants);
  assertExists(model.methods.create_a_variant);
  assertExists(model.methods.get_cloudflare_images_variants_variant_details);
  assertExists(model.methods.update_a_variant);
  assertExists(model.methods.delete_a_variant);
  assertExists(
    model.methods.get_cloudflare_images_variants_variant_details_flat,
  );
  assertExists(model.methods.get_cloudflare_images_image_details);
  assertExists(model.methods.update_image);
  assertExists(model.methods.delete_image);
  assertExists(model.methods.list_images_v2);
  assertExists(model.methods.create_authenticated_direct_upload_url_v_2);
  assertExists(model.methods.list_migrations);
  assertExists(model.methods.create_migration);
  assertExists(model.methods.get_migration);
  assertExists(model.methods.delete_migration);
  assertExists(model.methods.get_migration_progress);
  assertExists(
    model.methods.update_cloudflare_images_sourcingkit_abort_migration,
  );
  assertExists(
    model.methods.update_cloudflare_images_sourcingkit_start_migration,
  );
  assertExists(model.methods.list_migration_logs);
  assertExists(model.methods.list_sources);
  assertExists(model.methods.create_source);
  assertExists(
    model.methods
      .create_cloudflare_images_sourcingkit_precheck_source_connectivity,
  );
  assertExists(model.methods.get_source);
  assertExists(model.methods.update_source);
  assertExists(model.methods.delete_source);
  assertExists(model.methods.get_source_connectivity);
});

Deno.test("images model: has expected resources", () => {
  assertExists(model.resources);
  assertExists(model.resources["cloudflare_images_upload_an_image_via_url"]);
  assertExists(model.resources["list_signing_keys"]);
  assertExists(model.resources["cloudflare_images_keys_add_signing_key"]);
  assertExists(model.resources["cloudflare_images_images_usage_statistics"]);
  assertExists(model.resources["list_variants"]);
  assertExists(model.resources["a_variant"]);
  assertExists(model.resources["cloudflare_images_variants_variant_details"]);
  assertExists(
    model.resources["cloudflare_images_variants_variant_details_flat"],
  );
  assertExists(model.resources["cloudflare_images_image_details"]);
  assertExists(model.resources["image"]);
  assertExists(model.resources["list_images_v2"]);
  assertExists(model.resources["authenticated_direct_upload_url_v_2"]);
  assertExists(model.resources["list_migrations"]);
  assertExists(model.resources["migration"]);
  assertExists(model.resources["migration_progress"]);
  assertExists(
    model.resources["cloudflare_images_sourcingkit_abort_migration"],
  );
  assertExists(
    model.resources["cloudflare_images_sourcingkit_start_migration"],
  );
  assertExists(model.resources["list_migration_logs"]);
  assertExists(model.resources["list_sources"]);
  assertExists(model.resources["source"]);
  assertExists(
    model
      .resources["cloudflare_images_sourcingkit_precheck_source_connectivity"],
  );
  assertExists(model.resources["source_connectivity"]);
});

// ---------------------------------------------------------------------------
// Mock Cloudflare API Server
// ---------------------------------------------------------------------------

function startMockCfServer(
  responses: Record<string, { result: unknown; isCollection?: boolean }>,
): { url: string; server: Deno.HttpServer } {
  const server = Deno.serve({ port: 0, onListen() {} }, (req) => {
    const url = new URL(req.url);
    const path = url.pathname;

    for (
      const [pattern, { result, isCollection }] of Object.entries(responses)
    ) {
      if (path.includes(pattern)) {
        return Response.json({
          success: true,
          errors: [],
          messages: [],
          result,
          result_info: isCollection
            ? {
              page: 1,
              per_page: 50,
              total_count: Array.isArray(result) ? result.length : 1,
              total_pages: 1,
            }
            : undefined,
        });
      }
    }

    return Response.json({
      success: false,
      errors: [{ code: 404, message: "Not found" }],
      messages: [],
      result: null,
    });
  });

  const addr = server.addr as Deno.NetAddr;
  return { url: `http://localhost:${addr.port}`, server };
}

function installFetchMock(mockUrl: string): () => void {
  const originalFetch = globalThis.fetch;
  globalThis.fetch = (input, init) => {
    const reqUrl = typeof input === "string" ? input : input.toString();
    const newUrl = reqUrl.replace(
      "https://api.cloudflare.com/client/v4",
      mockUrl,
    );
    return originalFetch(newUrl, init);
  };
  return () => {
    globalThis.fetch = originalFetch;
  };
}

Deno.test({
  name:
    "images model: cloudflare_images_upload_an_image_via_url executes and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "creator": null,
      "filename": null,
      "id": null,
      "meta": null,
      "requireSignedURLs": null,
      "uploaded": null,
      "variants": null,
    };
    const { url, server } = startMockCfServer({
      "/images/v1": { result: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: {
          id: "test-id",
          name: "test-images",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).cloudflare_images_upload_an_image_via_url.execute({
        "creator": null,
        "filename": null,
        "id": null,
        "meta": null,
        "requireSignedURLs": null,
        "uploaded": null,
        "variants": null,
      }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "images model: list_signing_keys fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = { "keys": [null] };
    const { url, server } = startMockCfServer({
      "/images/v1/keys": { result: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: {
          id: "test-id",
          name: "test-images",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).list_signing_keys.execute({}, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name:
    "images model: update_cloudflare_images_keys_add_signing_key executes and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = { "keys": [null] };
    const { url, server } = startMockCfServer({
      "/images/v1/keys/test-id-123": { result: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: {
          id: "test-id",
          name: "test-images",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).update_cloudflare_images_keys_add_signing_key.execute({
        "signing_key_name": "test-id-123",
        "keys": [null],
      }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "images model: delete_signing_key executes successfully",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockCfServer({
      "/images/v1/keys/test-id-123": { result: { id: "test-id-123" } },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: {
          id: "test-id",
          name: "test-images",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).delete_signing_key.execute(
        { "signing_key_name": "test-id-123" },
        context,
      );
      assertEquals(result.dataHandles.length, 0);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "images model: create_a_variant creates and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = { "id": "new-123", "variant": null };
    const { url, server } = startMockCfServer({
      "/images/v1/variants": { result: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: {
          id: "test-id",
          name: "test-images",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).create_a_variant.execute({ "variant": null }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});
