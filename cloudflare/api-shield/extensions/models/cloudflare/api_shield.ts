/**
 * Cloudflare API Shield — schema validation, endpoint discovery, sequence rules
 *
 * Auto-generated by scripts/cloudflare-codegen — do not edit manually.
 *
 * @module
 */
// SPDX-License-Identifier: Apache-2.0

import { z } from "npm:zod@4.4.3";
import { cfApi, cfApiPaginated, sanitizeInstanceName } from "./_lib/api.ts";

const EXTENSION_NAME = "@webframp/cloudflare/api-shield";

// =============================================================================
// Schemas
// =============================================================================

const GlobalArgsSchema = z.object({
  apiToken: z.string().meta({ sensitive: true }).describe(
    "Cloudflare API token; overrides the CLOUDFLARE_API_TOKEN environment variable. Wire with a vault.get(...) expression to source it from a vault.",
  ).optional(),
  zoneId: z.string().describe("Cloudflare zone ID"),
});

const GetApiShieldApiDiscoveryRetrieveDiscoveredOperationsOnAZoneAsOpenapiSchema =
  z.object({
    schemas: z.array(z.unknown()),
    timestamp: z.unknown(),
  }).passthrough();

const ApiShieldApiDiscoveryRetrieveDiscoveredOperationsOnAZoneItemSchema = z
  .object({
    features: z.unknown().optional(),
    id: z.unknown(),
    last_updated: z.unknown(),
    origin: z.array(z.unknown()).describe(
      "API discovery engine(s) that discovered this operation",
    ),
    state: z.unknown(),
    endpoint: z.unknown(),
    host: z.unknown(),
    method: z.unknown(),
  }).passthrough();

const ListApiShieldApiDiscoveryRetrieveDiscoveredOperationsOnAZoneSchema = z
  .object({
    items: z.array(
      ApiShieldApiDiscoveryRetrieveDiscoveredOperationsOnAZoneItemSchema,
    ),
    truncated: z.boolean(),
    fetchedAt: z.string(),
    durationMs: z.number().optional().describe(
      "Method execution duration in milliseconds",
    ),
    collectedBy: z.string().optional().describe(
      "Extension that collected this data",
    ),
  });

const PatchDiscoveredOperationsSchema = z.record(z.string(), z.unknown());

const ApiShieldApiDiscoveryRetrieveDiscoveredOperationByIdItemSchema = z.object(
  {
    features: z.unknown().optional(),
    id: z.unknown(),
    last_updated: z.unknown(),
    origin: z.array(z.unknown()).describe(
      "API discovery engine(s) that discovered this operation",
    ),
    state: z.unknown(),
    endpoint: z.unknown(),
    host: z.unknown(),
    method: z.unknown(),
  },
).passthrough();

const ListApiShieldApiDiscoveryRetrieveDiscoveredOperationByIdSchema = z.object(
  {
    items: z.array(
      ApiShieldApiDiscoveryRetrieveDiscoveredOperationByIdItemSchema,
    ),
    truncated: z.boolean(),
    fetchedAt: z.string(),
    durationMs: z.number().optional().describe(
      "Method execution duration in milliseconds",
    ),
    collectedBy: z.string().optional().describe(
      "Extension that collected this data",
    ),
  },
);

const PatchDiscoveredOperationSchema = z.object({
  state: z.unknown().optional(),
}).passthrough();

const CreateApiShieldExpressionTemplatesFallthroughSchema = z.object({
  expression: z.string().describe("WAF Expression for fallthrough"),
  title: z.string().describe("Title for the expression"),
}).passthrough();

const GetLabelsItemSchema = z.object({
  created_at: z.unknown(),
  description: z.unknown(),
  last_updated: z.unknown(),
  metadata: z.unknown(),
  name: z.unknown(),
  source: z.unknown(),
  mapped_resources: z.unknown().optional(),
}).passthrough();

const GetLabelsSchema = z.object({
  items: z.array(GetLabelsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const GetManagedLabelSchema = z.object({
  created_at: z.unknown(),
  description: z.unknown(),
  last_updated: z.unknown(),
  metadata: z.unknown(),
  name: z.unknown(),
  source: z.unknown(),
  mapped_resources: z.unknown().optional(),
}).passthrough();

const UpdateApiShieldLabelsReplaceOperationsAttachedToManagedLabelSchema = z
  .object({
    created_at: z.unknown(),
    description: z.unknown(),
    last_updated: z.unknown(),
    metadata: z.unknown(),
    name: z.unknown(),
    source: z.unknown(),
    mapped_resources: z.unknown().optional(),
  }).passthrough();

const CreateUserLabelsSchema = z.object({
  created_at: z.unknown(),
  description: z.unknown(),
  last_updated: z.unknown(),
  metadata: z.unknown(),
  name: z.unknown(),
  source: z.unknown(),
}).passthrough();

const GetUserLabelSchema = z.object({
  created_at: z.unknown(),
  description: z.unknown(),
  last_updated: z.unknown(),
  metadata: z.unknown(),
  name: z.unknown(),
  source: z.unknown(),
  mapped_resources: z.unknown().optional(),
}).passthrough();

const PutUserLabelSchema = z.object({
  created_at: z.unknown(),
  description: z.unknown(),
  last_updated: z.unknown(),
  metadata: z.unknown(),
  name: z.unknown(),
  source: z.unknown(),
}).passthrough();

const PatchUserLabelSchema = z.object({
  created_at: z.unknown(),
  description: z.unknown(),
  last_updated: z.unknown(),
  metadata: z.unknown(),
  name: z.unknown(),
  source: z.unknown(),
}).passthrough();

const UpdateApiShieldLabelsReplaceOperationsAttachedToUserLabelSchema = z
  .object({
    created_at: z.unknown(),
    description: z.unknown(),
    last_updated: z.unknown(),
    metadata: z.unknown(),
    name: z.unknown(),
    source: z.unknown(),
    mapped_resources: z.unknown().optional(),
  }).passthrough();

const ApiShieldEndpointManagementRetrieveInformationAboutAllOperationsOnAZoneItemSchema =
  z.object({
    endpoint: z.unknown(),
    host: z.unknown(),
    method: z.unknown(),
    last_updated: z.unknown(),
    operation_id: z.unknown(),
    features: z.unknown().optional(),
  }).passthrough();

const ListApiShieldEndpointManagementRetrieveInformationAboutAllOperationsOnAZoneSchema =
  z.object({
    items: z.array(
      ApiShieldEndpointManagementRetrieveInformationAboutAllOperationsOnAZoneItemSchema,
    ),
    truncated: z.boolean(),
    fetchedAt: z.string(),
    durationMs: z.number().optional().describe(
      "Method execution duration in milliseconds",
    ),
    collectedBy: z.string().optional().describe(
      "Extension that collected this data",
    ),
  });

const ApiShieldEndpointManagementAddOperationsToAZoneSchema = z.object({
  endpoint: z.unknown(),
  host: z.unknown(),
  method: z.unknown(),
  last_updated: z.unknown(),
  operation_id: z.unknown(),
  features: z.unknown().optional(),
}).passthrough();

const CreateApiShieldEndpointManagementAddOperationToAZoneSchema = z.object({
  endpoint: z.unknown(),
  host: z.unknown(),
  method: z.unknown(),
  last_updated: z.unknown(),
  operation_id: z.unknown(),
  features: z.unknown().optional(),
  schemas: z.unknown().optional(),
}).passthrough();

const CreateApiShieldOperationsBulkPostLabelsToOperationsSchema = z.object({
  endpoint: z.unknown(),
  host: z.unknown(),
  method: z.unknown(),
  last_updated: z.unknown(),
  operation_id: z.unknown(),
  labels: z.array(z.unknown()).optional(),
}).passthrough();

const PutLabelsToOperationsSchema = z.object({
  endpoint: z.unknown(),
  host: z.unknown(),
  method: z.unknown(),
  last_updated: z.unknown(),
  operation_id: z.unknown(),
  labels: z.array(z.unknown()).optional(),
}).passthrough();

const GetApiShieldEndpointManagementRetrieveInformationAboutAnOperationSchema =
  z.object({
    endpoint: z.unknown(),
    host: z.unknown(),
    method: z.unknown(),
    last_updated: z.unknown(),
    operation_id: z.unknown(),
    features: z.unknown().optional(),
    schemas: z.unknown().optional(),
  }).passthrough();

const CreateApiShieldOperationsPostLabelsToOperationSchema = z.object({
  endpoint: z.unknown(),
  host: z.unknown(),
  method: z.unknown(),
  last_updated: z.unknown(),
  operation_id: z.unknown(),
  labels: z.array(z.unknown()).optional(),
}).passthrough();

const PutLabelsToOperationSchema = z.object({
  endpoint: z.unknown(),
  host: z.unknown(),
  method: z.unknown(),
  last_updated: z.unknown(),
  operation_id: z.unknown(),
  labels: z.array(z.unknown()).optional(),
}).passthrough();

const GetApiShieldEndpointManagementRetrieveOperationsAndFeaturesAsOpenApiSchemasSchema =
  z.object({
    schemas: z.array(z.unknown()).optional(),
    timestamp: z.string().optional(),
  }).passthrough();

// =============================================================================
// Model Definition
// =============================================================================

/** Cloudflare API Shield — schema validation, endpoint discovery, sequence rules */
export const model = {
  type: "@webframp/cloudflare/api-shield",
  version: "2026.08.28.2",
  globalArguments: GlobalArgsSchema,

  upgrades: [
    {
      toVersion: "2026.08.24.1",
      description:
        "Added optional durationMs, collectedBy, and fetchedAt output metadata fields",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.08.25.1",
      description: "Label metadata update, no schema changes",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.08.26.1",
      description:
        "No schema changes — restored inline npm:zod specifier for registry scoring; retained strict mode",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.08.26.2",
      description:
        "No schema changes — restored inline npm:zod specifier for registry scoring; retained strict mode",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.08.28.1",
      description:
        "No schema changes — normalized license to Apache-2.0 and corrected copyright holder to Sean Escriva",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.08.28.2",
      description: "Regenerated from updated API spec; no migration required",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
  ],

  resources: {
    "api_shield_settings_retrieve_information_about_specific_configuration_properties":
      {
        description:
          "Retrieve information about specific configuration properties",
        schema: z.object({}),
        lifetime: "infinite" as const,
        garbageCollection: 20,
      },
    "api_shield_settings_set_configuration_properties": {
      description: "Update configuration properties",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "api_shield_api_discovery_retrieve_discovered_operations_on_a_zone_as_openapi":
      {
        description:
          "Retrieve discovered operations on a zone rendered as OpenAPI schemas",
        schema:
          GetApiShieldApiDiscoveryRetrieveDiscoveredOperationsOnAZoneAsOpenapiSchema,
        lifetime: "infinite" as const,
        garbageCollection: 20,
      },
    "api_shield_api_discovery_retrieve_discovered_operations_on_a_zone": {
      description: "Retrieve discovered operations on a zone",
      schema:
        ListApiShieldApiDiscoveryRetrieveDiscoveredOperationsOnAZoneSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "patch_discovered_operations": {
      description: "Patch discovered operations",
      schema: PatchDiscoveredOperationsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "api_shield_api_discovery_retrieve_discovered_operation_by_id": {
      description: "Retrieve a discovered operation",
      schema: ListApiShieldApiDiscoveryRetrieveDiscoveredOperationByIdSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "patch_discovered_operation": {
      description: "Patch discovered operation",
      schema: PatchDiscoveredOperationSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "api_shield_expression_templates_fallthrough": {
      description:
        "Generate fallthrough WAF expression template from a set of API hosts",
      schema: CreateApiShieldExpressionTemplatesFallthroughSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "get_labels": {
      description: "Retrieve all labels",
      schema: GetLabelsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "managed_label": {
      description: "Retrieve managed label",
      schema: GetManagedLabelSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "api_shield_labels_replace_operations_attached_to_managed_label": {
      description: "Replace operation(s) attached to a managed label",
      schema:
        UpdateApiShieldLabelsReplaceOperationsAttachedToManagedLabelSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "user_labels": {
      description: "Create user labels",
      schema: CreateUserLabelsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "user_label": {
      description: "Retrieve user label",
      schema: GetUserLabelSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "put_user_label": {
      description: "Update user label",
      schema: PutUserLabelSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "patch_user_label": {
      description: "Patch user label",
      schema: PatchUserLabelSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "api_shield_labels_replace_operations_attached_to_user_label": {
      description: "Replace operation(s) attached to a user label",
      schema: UpdateApiShieldLabelsReplaceOperationsAttachedToUserLabelSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "api_shield_endpoint_management_retrieve_information_about_all_operations_on_a_zone":
      {
        description: "Retrieve information about all operations on a zone",
        schema:
          ListApiShieldEndpointManagementRetrieveInformationAboutAllOperationsOnAZoneSchema,
        lifetime: "infinite" as const,
        garbageCollection: 10,
      },
    "api_shield_endpoint_management_add_operations_to_a_zone": {
      description: "Add operations to a zone",
      schema: ApiShieldEndpointManagementAddOperationsToAZoneSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "api_shield_endpoint_management_add_operation_to_a_zone": {
      description: "Add one operation to a zone",
      schema: CreateApiShieldEndpointManagementAddOperationToAZoneSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "api_shield_operations_bulk_post_labels_to_operations": {
      description:
        "Bulk attach label(s) on operation(s) in endpoint management",
      schema: CreateApiShieldOperationsBulkPostLabelsToOperationsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "put_labels_to_operations": {
      description:
        "Bulk replace label(s) on operation(s) in endpoint management",
      schema: PutLabelsToOperationsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "api_shield_endpoint_management_retrieve_information_about_an_operation": {
      description: "Retrieve information about an operation",
      schema:
        GetApiShieldEndpointManagementRetrieveInformationAboutAnOperationSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "api_shield_operations_post_labels_to_operation": {
      description: "Attach label(s) on an operation in endpoint management",
      schema: CreateApiShieldOperationsPostLabelsToOperationSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "put_labels_to_operation": {
      description: "Replace label(s) on an operation in endpoint management",
      schema: PutLabelsToOperationSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "api_shield_endpoint_management_retrieve_operations_and_features_as_open_api_schemas":
      {
        description: "Retrieve operations and features as OpenAPI schemas",
        schema:
          GetApiShieldEndpointManagementRetrieveOperationsAndFeaturesAsOpenApiSchemasSchema,
        lifetime: "infinite" as const,
        garbageCollection: 20,
      },
  },

  methods: {
    get_api_shield_settings_retrieve_information_about_specific_configuration_properties:
      {
        description:
          "Retrieve information about specific configuration properties",
        arguments: z.object({
          normalize: z.boolean().optional().describe(
            "Ensures that the configuration is written or retrieved in normalized fashion",
          ),
        }),
        execute: async (
          _args: Record<string, unknown>,
          context: {
            globalArgs: Record<string, string>;
            writeResource: (
              spec: string,
              instance: string,
              data: unknown,
            ) => Promise<{ name: string }>;
            logger: {
              info: (msg: string, props: Record<string, unknown>) => void;
            };
          },
        ) => {
          const { apiToken, zoneId } = context.globalArgs;
          const result = await cfApi<Record<string, unknown>>(
            apiToken,
            "GET",
            `/zones/${zoneId}/api_gateway/configuration`,
          );

          const handle = await context.writeResource(
            "api_shield_settings_retrieve_information_about_specific_configuration_properties",
            "latest",
            result,
          );
          context.logger.info(
            "Fetched api_shield_settings_retrieve_information_about_specific_configuration_properties",
            {},
          );
          return { dataHandles: [handle] };
        },
      },
    update_api_shield_settings_set_configuration_properties: {
      description: "Update configuration properties",
      arguments: z.object({
        normalize: z.boolean().optional().describe(
          "Ensures that the configuration is written or retrieved in normalized fashion",
        ),
        auth_id_characteristics: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["normalize"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }
        const queryParts: string[] = [];
        const queryKeys = new Set(["normalize"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/zones/${zoneId}/api_gateway/configuration${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "api_shield_settings_set_configuration_properties",
          "updated",
          result,
        );
        context.logger.info(
          "Updated api_shield_settings_set_configuration_properties",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    get_api_shield_api_discovery_retrieve_discovered_operations_on_a_zone_as_openapi:
      {
        description:
          "Retrieve discovered operations on a zone rendered as OpenAPI schemas",
        arguments: z.object({}),
        execute: async (
          _args: Record<string, unknown>,
          context: {
            globalArgs: Record<string, string>;
            writeResource: (
              spec: string,
              instance: string,
              data: unknown,
            ) => Promise<{ name: string }>;
            logger: {
              info: (msg: string, props: Record<string, unknown>) => void;
            };
          },
        ) => {
          const { apiToken, zoneId } = context.globalArgs;
          const result = await cfApi<Record<string, unknown>>(
            apiToken,
            "GET",
            `/zones/${zoneId}/api_gateway/discovery`,
          );

          const handle = await context.writeResource(
            "api_shield_api_discovery_retrieve_discovered_operations_on_a_zone_as_openapi",
            "latest",
            result,
          );
          context.logger.info(
            "Fetched api_shield_api_discovery_retrieve_discovered_operations_on_a_zone_as_openapi",
            {},
          );
          return { dataHandles: [handle] };
        },
      },
    list_api_shield_api_discovery_retrieve_discovered_operations_on_a_zone: {
      description: "Retrieve discovered operations on a zone",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;
        const startMs = Date.now();
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/zones/${zoneId}/api_gateway/discovery/operations`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "api_shield_api_discovery_retrieve_discovered_operations_on_a_zone",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );

        context.logger.info(
          "Found {count} api_shield_api_discovery_retrieve_discovered_operations_on_a_zone",
          { count: results.length },
        );
        return { dataHandles: [handle] };
      },
    },
    patch_discovered_operations: {
      description: "Patch discovered operations",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/zones/${zoneId}/api_gateway/discovery/operations`,
          body,
        );

        const handle = await context.writeResource(
          "patch_discovered_operations",
          "updated",
          result,
        );
        context.logger.info("Updated patch_discovered_operations", {});
        return { dataHandles: [handle] };
      },
    },
    list_api_shield_api_discovery_retrieve_discovered_operation_by_id: {
      description: "Retrieve a discovered operation",
      arguments: z.object({
        discovery_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;
        const startMs = Date.now();
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["discovery_id", "page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/zones/${zoneId}/api_gateway/discovery/operations/${args.discovery_id}`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "api_shield_api_discovery_retrieve_discovered_operation_by_id",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );

        context.logger.info(
          "Found {count} api_shield_api_discovery_retrieve_discovered_operation_by_id",
          { count: results.length },
        );
        return { dataHandles: [handle] };
      },
    },
    patch_discovered_operation: {
      description: "Patch discovered operation",
      arguments: z.object({
        discovery_id: z.string(),
        state: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["discovery_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/zones/${zoneId}/api_gateway/discovery/operations/${args.discovery_id}`,
          body,
        );

        const handle = await context.writeResource(
          "patch_discovered_operation",
          sanitizeInstanceName(String(args.discovery_id)),
          result,
        );
        context.logger.info("Updated patch_discovered_operation", {});
        return { dataHandles: [handle] };
      },
    },
    create_api_shield_expression_templates_fallthrough: {
      description:
        "Generate fallthrough WAF expression template from a set of API hosts",
      arguments: z.object({
        hosts: z.array(z.string()).describe(
          "List of hosts to be targeted in the expression",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/zones/${zoneId}/api_gateway/expression-template/fallthrough`,
          body,
        );

        const id = sanitizeInstanceName(
          (result as { id?: string }).id ?? "created",
        );
        const handle = await context.writeResource(
          "api_shield_expression_templates_fallthrough",
          id,
          result,
        );
        context.logger.info(
          "Created api_shield_expression_templates_fallthrough {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    get_labels: {
      description: "Retrieve all labels",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;
        const startMs = Date.now();
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/zones/${zoneId}/api_gateway/labels`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("get_labels", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
          durationMs: Date.now() - startMs,
          collectedBy: EXTENSION_NAME,
        });

        context.logger.info("Found {count} get_labels", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    get_managed_label: {
      description: "Retrieve managed label",
      arguments: z.object({
        name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/zones/${zoneId}/api_gateway/labels/managed/${args.name}`,
        );

        const handle = await context.writeResource(
          "managed_label",
          sanitizeInstanceName(String(args.name)),
          result,
        );
        context.logger.info("Fetched managed_label", {});
        return { dataHandles: [handle] };
      },
    },
    update_api_shield_labels_replace_operations_attached_to_managed_label: {
      description: "Replace operation(s) attached to a managed label",
      arguments: z.object({
        name: z.string(),
        selector: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["name"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/zones/${zoneId}/api_gateway/labels/managed/${args.name}/resources/operation`,
          body,
        );

        const handle = await context.writeResource(
          "api_shield_labels_replace_operations_attached_to_managed_label",
          sanitizeInstanceName(String(args.name)),
          result,
        );
        context.logger.info(
          "Updated api_shield_labels_replace_operations_attached_to_managed_label",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    create_user_labels: {
      description: "Create user labels",
      arguments: z.object({
        items: z.array(z.unknown()),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;

        const body = args.items;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/zones/${zoneId}/api_gateway/labels/user`,
          body,
        );

        const handle = await context.writeResource(
          "create_user_labels",
          "latest",
          result ?? {},
        );
        context.logger.info("Executed create_user_labels", {});
        return { dataHandles: [handle] };
      },
    },
    delete_user_labels: {
      description: "Delete user labels",
      arguments: z.object({
        items: z.array(z.unknown()),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;

        const body = args.items;

        await cfApi(
          apiToken,
          "DELETE",
          `/zones/${zoneId}/api_gateway/labels/user`,
          body,
        );

        context.logger.info("Deleted resource {id}", { id: "unknown" });
        return { dataHandles: [] };
      },
    },
    get_user_label: {
      description: "Retrieve user label",
      arguments: z.object({
        name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/zones/${zoneId}/api_gateway/labels/user/${args.name}`,
        );

        const handle = await context.writeResource(
          "user_label",
          sanitizeInstanceName(String(args.name)),
          result,
        );
        context.logger.info("Fetched user_label", {});
        return { dataHandles: [handle] };
      },
    },
    put_user_label: {
      description: "Update user label",
      arguments: z.object({
        name: z.string(),
        description: z.unknown().optional(),
        metadata: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["name"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/zones/${zoneId}/api_gateway/labels/user/${args.name}`,
          body,
        );

        const handle = await context.writeResource(
          "put_user_label",
          sanitizeInstanceName(String(args.name)),
          result,
        );
        context.logger.info("Updated put_user_label", {});
        return { dataHandles: [handle] };
      },
    },
    patch_user_label: {
      description: "Patch user label",
      arguments: z.object({
        name: z.string(),
        description: z.unknown().optional(),
        metadata: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["name"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/zones/${zoneId}/api_gateway/labels/user/${args.name}`,
          body,
        );

        const handle = await context.writeResource(
          "patch_user_label",
          sanitizeInstanceName(String(args.name)),
          result,
        );
        context.logger.info("Updated patch_user_label", {});
        return { dataHandles: [handle] };
      },
    },
    delete_user_label: {
      description: "Delete user label",
      arguments: z.object({
        name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;

        await cfApi(
          apiToken,
          "DELETE",
          `/zones/${zoneId}/api_gateway/labels/user/${args.name}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.name });
        return { dataHandles: [] };
      },
    },
    update_api_shield_labels_replace_operations_attached_to_user_label: {
      description: "Replace operation(s) attached to a user label",
      arguments: z.object({
        name: z.string(),
        selector: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["name"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/zones/${zoneId}/api_gateway/labels/user/${args.name}/resources/operation`,
          body,
        );

        const handle = await context.writeResource(
          "api_shield_labels_replace_operations_attached_to_user_label",
          sanitizeInstanceName(String(args.name)),
          result,
        );
        context.logger.info(
          "Updated api_shield_labels_replace_operations_attached_to_user_label",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    list_api_shield_endpoint_management_retrieve_information_about_all_operations_on_a_zone:
      {
        description: "Retrieve information about all operations on a zone",
        arguments: z.object({
          order: z.enum(["method", "host", "endpoint", "thresholds.$key"])
            .optional(),
        }),
        execute: async (
          args: Record<string, unknown>,
          context: {
            globalArgs: Record<string, string>;
            writeResource: (
              spec: string,
              instance: string,
              data: unknown,
            ) => Promise<{ name: string }>;
            logger: {
              info: (msg: string, props: Record<string, unknown>) => void;
            };
          },
        ) => {
          const { apiToken, zoneId } = context.globalArgs;
          const startMs = Date.now();
          const params: Record<string, string> = {};
          const excludeKeys = new Set(["page", "per_page"]);
          for (const [k, v] of Object.entries(args)) {
            if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
          }

          const { results, truncated } = await cfApiPaginated<
            Record<string, unknown>
          >(
            apiToken,
            `/zones/${zoneId}/api_gateway/operations`,
            params,
          );

          if (truncated) {
            context.logger.info(
              "WARNING: results truncated at {count} (pagination cap)",
              { count: results.length },
            );
          }

          const handle = await context.writeResource(
            "api_shield_endpoint_management_retrieve_information_about_all_operations_on_a_zone",
            "main",
            {
              items: results,
              truncated,
              fetchedAt: new Date().toISOString(),
              durationMs: Date.now() - startMs,
              collectedBy: EXTENSION_NAME,
            },
          );

          context.logger.info(
            "Found {count} api_shield_endpoint_management_retrieve_information_about_all_operations_on_a_zone",
            { count: results.length },
          );
          return { dataHandles: [handle] };
        },
      },
    api_shield_endpoint_management_add_operations_to_a_zone: {
      description: "Add operations to a zone",
      arguments: z.object({
        items: z.array(z.unknown()),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;

        const body = args.items;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/zones/${zoneId}/api_gateway/operations`,
          body,
        );

        const handle = await context.writeResource(
          "api_shield_endpoint_management_add_operations_to_a_zone",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed api_shield_endpoint_management_add_operations_to_a_zone",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    delete_multiple_operations: {
      description: "Delete multiple operations",
      arguments: z.object({
        items: z.array(z.unknown()),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;

        const body = args.items;

        await cfApi(
          apiToken,
          "DELETE",
          `/zones/${zoneId}/api_gateway/operations`,
          body,
        );

        context.logger.info("Deleted resource {id}", { id: "unknown" });
        return { dataHandles: [] };
      },
    },
    create_api_shield_endpoint_management_add_operation_to_a_zone: {
      description: "Add one operation to a zone",
      arguments: z.object({
        endpoint: z.unknown(),
        host: z.unknown(),
        method: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/zones/${zoneId}/api_gateway/operations/item`,
          body,
        );

        const id = sanitizeInstanceName(
          (result as { id?: string }).id ?? "created",
        );
        const handle = await context.writeResource(
          "api_shield_endpoint_management_add_operation_to_a_zone",
          id,
          result,
        );
        context.logger.info(
          "Created api_shield_endpoint_management_add_operation_to_a_zone {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    create_api_shield_operations_bulk_post_labels_to_operations: {
      description:
        "Bulk attach label(s) on operation(s) in endpoint management",
      arguments: z.object({
        managed: z.object({
          labels: z.array(z.unknown()).optional(),
        }).optional(),
        selector: z.unknown(),
        user: z.object({
          labels: z.array(z.unknown()).optional(),
        }).optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/zones/${zoneId}/api_gateway/operations/labels`,
          body,
        );

        const id = sanitizeInstanceName(
          (result as { id?: string }).id ?? "created",
        );
        const handle = await context.writeResource(
          "api_shield_operations_bulk_post_labels_to_operations",
          id,
          result,
        );
        context.logger.info(
          "Created api_shield_operations_bulk_post_labels_to_operations {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    put_labels_to_operations: {
      description:
        "Bulk replace label(s) on operation(s) in endpoint management",
      arguments: z.object({
        managed: z.object({
          labels: z.array(z.unknown()),
        }).describe("Managed labels to replace for all affected operations"),
        selector: z.unknown(),
        user: z.object({
          labels: z.array(z.unknown()),
        }).describe("User labels to replace for all affected operations"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/zones/${zoneId}/api_gateway/operations/labels`,
          body,
        );

        const handle = await context.writeResource(
          "put_labels_to_operations",
          "updated",
          result,
        );
        context.logger.info("Updated put_labels_to_operations", {});
        return { dataHandles: [handle] };
      },
    },
    delete_labels_to_operations: {
      description:
        "Bulk remove label(s) on operation(s) in endpoint management",
      arguments: z.object({
        managed: z.object({
          labels: z.array(z.unknown()).optional(),
        }).optional(),
        selector: z.unknown(),
        user: z.object({
          labels: z.array(z.unknown()).optional(),
        }).optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;

        await cfApi(
          apiToken,
          "DELETE",
          `/zones/${zoneId}/api_gateway/operations/labels`,
          args,
        );

        context.logger.info("Deleted resource {id}", { id: "unknown" });
        return { dataHandles: [] };
      },
    },
    get_api_shield_endpoint_management_retrieve_information_about_an_operation:
      {
        description: "Retrieve information about an operation",
        arguments: z.object({
          operation_id: z.string(),
        }),
        execute: async (
          args: Record<string, unknown>,
          context: {
            globalArgs: Record<string, string>;
            writeResource: (
              spec: string,
              instance: string,
              data: unknown,
            ) => Promise<{ name: string }>;
            logger: {
              info: (msg: string, props: Record<string, unknown>) => void;
            };
          },
        ) => {
          const { apiToken, zoneId } = context.globalArgs;
          const result = await cfApi<Record<string, unknown>>(
            apiToken,
            "GET",
            `/zones/${zoneId}/api_gateway/operations/${args.operation_id}`,
          );

          const handle = await context.writeResource(
            "api_shield_endpoint_management_retrieve_information_about_an_operation",
            sanitizeInstanceName(String(args.operation_id)),
            result,
          );
          context.logger.info(
            "Fetched api_shield_endpoint_management_retrieve_information_about_an_operation",
            {},
          );
          return { dataHandles: [handle] };
        },
      },
    delete_an_operation: {
      description: "Delete an operation",
      arguments: z.object({
        operation_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;

        await cfApi(
          apiToken,
          "DELETE",
          `/zones/${zoneId}/api_gateway/operations/${args.operation_id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.operation_id });
        return { dataHandles: [] };
      },
    },
    create_api_shield_operations_post_labels_to_operation: {
      description: "Attach label(s) on an operation in endpoint management",
      arguments: z.object({
        operation_id: z.string(),
        managed: z.array(z.unknown()).optional().describe(
          "List of managed label names.",
        ),
        user: z.array(z.unknown()).optional().describe(
          "List of user label names.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["operation_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/zones/${zoneId}/api_gateway/operations/${args.operation_id}/labels`,
          body,
        );

        const id = sanitizeInstanceName(
          (result as { id?: string }).id ?? "created",
        );
        const handle = await context.writeResource(
          "api_shield_operations_post_labels_to_operation",
          id,
          result,
        );
        context.logger.info(
          "Created api_shield_operations_post_labels_to_operation {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    put_labels_to_operation: {
      description: "Replace label(s) on an operation in endpoint management",
      arguments: z.object({
        operation_id: z.string(),
        managed: z.array(z.unknown()).optional().describe(
          "List of managed label names. Omitting this property or passing an empty array...",
        ),
        user: z.array(z.unknown()).optional().describe(
          "List of user label names. Omitting this property or passing an empty array wi...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["operation_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/zones/${zoneId}/api_gateway/operations/${args.operation_id}/labels`,
          body,
        );

        const handle = await context.writeResource(
          "put_labels_to_operation",
          sanitizeInstanceName(String(args.operation_id)),
          result,
        );
        context.logger.info("Updated put_labels_to_operation", {});
        return { dataHandles: [handle] };
      },
    },
    delete_labels_from_operation: {
      description: "Remove label(s) on an operation in endpoint management",
      arguments: z.object({
        operation_id: z.string(),
        managed: z.array(z.unknown()).optional().describe(
          "List of managed label names.",
        ),
        user: z.array(z.unknown()).optional().describe(
          "List of user label names.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["operation_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        await cfApi(
          apiToken,
          "DELETE",
          `/zones/${zoneId}/api_gateway/operations/${args.operation_id}/labels`,
          body,
        );

        context.logger.info("Deleted resource {id}", { id: args.operation_id });
        return { dataHandles: [] };
      },
    },
    get_api_shield_endpoint_management_retrieve_operations_and_features_as_open_api_schemas:
      {
        description: "Retrieve operations and features as OpenAPI schemas",
        arguments: z.object({
          host: z.string().optional(),
        }),
        execute: async (
          _args: Record<string, unknown>,
          context: {
            globalArgs: Record<string, string>;
            writeResource: (
              spec: string,
              instance: string,
              data: unknown,
            ) => Promise<{ name: string }>;
            logger: {
              info: (msg: string, props: Record<string, unknown>) => void;
            };
          },
        ) => {
          const { apiToken, zoneId } = context.globalArgs;
          const result = await cfApi<Record<string, unknown>>(
            apiToken,
            "GET",
            `/zones/${zoneId}/api_gateway/schemas`,
          );

          const handle = await context.writeResource(
            "api_shield_endpoint_management_retrieve_operations_and_features_as_open_api_schemas",
            "latest",
            result,
          );
          context.logger.info(
            "Fetched api_shield_endpoint_management_retrieve_operations_and_features_as_open_api_schemas",
            {},
          );
          return { dataHandles: [handle] };
        },
      },
  },
};
