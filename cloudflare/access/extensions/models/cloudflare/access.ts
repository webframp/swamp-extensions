/**
 * Cloudflare Access (Zero Trust) — applications, policies, identity providers, certificates
 *
 * Auto-generated by scripts/cloudflare-codegen — do not edit manually.
 *
 * @module
 */
// SPDX-License-Identifier: AGPL-3.0-or-later WITH Swamp-Extension-Exception

import { z } from "npm:zod@4.4.3";
import { cfApi, cfApiPaginated } from "./_lib/api.ts";

// =============================================================================
// Schemas
// =============================================================================

const GlobalArgsSchema = z.object({
  apiToken: z.string().meta({ sensitive: true }).describe(
    "Cloudflare API token",
  ),
  accountId: z.string().describe("Cloudflare account ID"),
});

const PortalsItemSchema = z.object({
  allow_code_mode: z.boolean().optional().describe(
    "Allow remote code execution in Dynamic Workers (beta)",
  ),
  created_at: z.string().optional(),
  created_by: z.string().optional(),
  description: z.string().max(512).optional(),
  hostname: z.string(),
  id: z.string().min(1).max(32).describe("portal id"),
  modified_at: z.string().optional(),
  modified_by: z.string().optional(),
  name: z.string().max(350),
  secure_web_gateway: z.boolean().optional().describe(
    "Route outbound MCP traffic through Zero Trust Secure Web Gateway",
  ),
  servers: z.array(z.object({
    auth_type: z.enum(["oauth", "bearer", "unauthenticated"]),
    created_at: z.string().optional(),
    created_by: z.string().optional(),
    default_disabled: z.boolean().optional(),
    description: z.string().max(512).nullable().optional(),
    error: z.string().optional(),
    error_details: z.object({
      cause: z.string().optional(),
      is_upstream: z.boolean().optional(),
      mcp_code: z.number().optional(),
      retryable: z.boolean().optional(),
      status_code: z.number().optional(),
    }).optional(),
    hostname: z.string(),
    id: z.string().min(1).max(32),
    is_shared_oauth_callback_enabled: z.boolean().optional(),
    last_successful_sync: z.string().optional(),
    last_synced: z.string().optional(),
    modified_at: z.string().optional(),
    modified_by: z.string().optional(),
    name: z.string().max(350),
    on_behalf: z.boolean().optional(),
    prompts: z.array(z.record(z.string(), z.unknown())),
    secure_web_gateway: z.boolean().optional(),
    server_id: z.string().min(1).max(32),
    status: z.enum(["waiting", "ready", "stale", "error"]).optional(),
    tools: z.array(z.record(z.string(), z.unknown())),
    updated_prompts: z.array(z.object({
      enabled: z.boolean().optional(),
      name: z.string(),
      portal_alias: z.string().optional(),
      portal_description: z.string().optional(),
      server_alias: z.string().optional(),
      server_description: z.string().optional(),
    })).optional(),
    updated_tools: z.array(z.object({
      enabled: z.boolean().optional(),
      name: z.string(),
      portal_alias: z.string().optional(),
      portal_description: z.string().optional(),
      server_alias: z.string().optional(),
      server_description: z.string().optional(),
    })).optional(),
  })),
});

const ListPortalsSchema = z.object({
  items: z.array(PortalsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetMcpPortalsApiFetchGatewaysSchema = z.object({
  allow_code_mode: z.boolean().optional().describe(
    "Allow remote code execution in Dynamic Workers (beta)",
  ),
  created_at: z.string().optional(),
  created_by: z.string().optional(),
  description: z.string().max(512).optional(),
  hostname: z.string(),
  id: z.string().min(1).max(32).describe("portal id"),
  modified_at: z.string().optional(),
  modified_by: z.string().optional(),
  name: z.string().max(350),
  secure_web_gateway: z.boolean().optional().describe(
    "Route outbound MCP traffic through Zero Trust Secure Web Gateway",
  ),
  servers: z.array(z.object({
    auth_type: z.enum(["oauth", "bearer", "unauthenticated"]),
    created_at: z.string().optional(),
    created_by: z.string().optional(),
    default_disabled: z.boolean().optional(),
    description: z.string().max(512).nullable().optional(),
    error: z.string().optional(),
    error_details: z.object({
      cause: z.string().optional(),
      is_upstream: z.boolean().optional(),
      mcp_code: z.number().optional(),
      retryable: z.boolean().optional(),
      status_code: z.number().optional(),
    }).optional(),
    hostname: z.string(),
    id: z.string().min(1).max(32),
    is_shared_oauth_callback_enabled: z.boolean().optional(),
    last_successful_sync: z.string().optional(),
    last_synced: z.string().optional(),
    modified_at: z.string().optional(),
    modified_by: z.string().optional(),
    name: z.string().max(350),
    on_behalf: z.boolean().optional(),
    prompts: z.array(z.record(z.string(), z.unknown())),
    secure_web_gateway: z.boolean().optional(),
    server_id: z.string().min(1).max(32),
    status: z.enum(["waiting", "ready", "stale", "error"]).optional(),
    tools: z.array(z.record(z.string(), z.unknown())),
    updated_prompts: z.array(z.object({
      enabled: z.boolean().optional(),
      name: z.string(),
      portal_alias: z.string().optional(),
      portal_description: z.string().optional(),
      server_alias: z.string().optional(),
      server_description: z.string().optional(),
    })).optional(),
    updated_tools: z.array(z.object({
      enabled: z.boolean().optional(),
      name: z.string(),
      portal_alias: z.string().optional(),
      portal_description: z.string().optional(),
      server_alias: z.string().optional(),
      server_description: z.string().optional(),
    })).optional(),
  })),
});

const ServersItemSchema = z.object({
  auth_type: z.enum(["oauth", "bearer", "unauthenticated"]),
  created_at: z.string().optional(),
  created_by: z.string().optional(),
  description: z.string().max(512).nullable().optional(),
  error: z.string().optional(),
  error_details: z.object({
    cause: z.string().optional(),
    is_upstream: z.boolean().optional(),
    mcp_code: z.number().optional(),
    retryable: z.boolean().optional(),
    status_code: z.number().optional(),
  }).optional(),
  hostname: z.string(),
  id: z.string().min(1).max(32).describe("server id"),
  is_shared_oauth_callback_enabled: z.boolean().optional().describe(
    "When true, the gateway worker uses the shared Cloudflare-owned OAuth callback endpoint as the red...",
  ),
  last_successful_sync: z.string().optional(),
  last_synced: z.string().optional(),
  modified_at: z.string().optional(),
  modified_by: z.string().optional(),
  name: z.string().max(350),
  prompts: z.array(z.record(z.string(), z.unknown())),
  secure_web_gateway: z.boolean().optional().describe(
    "Route outbound traffic to this MCP server through Zero Trust Secure Web Gateway",
  ),
  status: z.enum(["waiting", "ready", "stale", "error"]).optional().describe(
    "Current sync state of the server",
  ),
  tools: z.array(z.record(z.string(), z.unknown())),
  updated_prompts: z.array(z.object({
    alias: z.string().max(40).optional(),
    description: z.string().optional(),
    enabled: z.boolean().optional(),
    name: z.string(),
  })).optional(),
  updated_tools: z.array(z.object({
    alias: z.string().max(40).optional(),
    description: z.string().optional(),
    enabled: z.boolean().optional(),
    name: z.string(),
  })).optional(),
});

const ListServersSchema = z.object({
  items: z.array(ServersItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetMcpPortalsApiFetchServersSchema = z.object({
  auth_type: z.enum(["oauth", "bearer", "unauthenticated"]),
  created_at: z.string().optional(),
  created_by: z.string().optional(),
  description: z.string().max(512).nullable().optional(),
  error: z.string().optional(),
  error_details: z.object({
    cause: z.string().optional(),
    is_upstream: z.boolean().optional(),
    mcp_code: z.number().optional(),
    retryable: z.boolean().optional(),
    status_code: z.number().optional(),
  }).optional(),
  hostname: z.string(),
  id: z.string().min(1).max(32).describe("server id"),
  is_shared_oauth_callback_enabled: z.boolean().optional().describe(
    "When true, the gateway worker uses the shared Cloudflare-owned OAuth callback endpoint as the red...",
  ),
  last_successful_sync: z.string().optional(),
  last_synced: z.string().optional(),
  modified_at: z.string().optional(),
  modified_by: z.string().optional(),
  name: z.string().max(350),
  prompts: z.array(z.record(z.string(), z.unknown())),
  secure_web_gateway: z.boolean().optional().describe(
    "Route outbound traffic to this MCP server through Zero Trust Secure Web Gateway",
  ),
  status: z.enum(["waiting", "ready", "stale", "error"]).optional().describe(
    "Current sync state of the server",
  ),
  tools: z.array(z.record(z.string(), z.unknown())),
  updated_prompts: z.array(z.object({
    alias: z.string().max(40).optional(),
    description: z.string().optional(),
    enabled: z.boolean().optional(),
    name: z.string(),
  })).optional(),
  updated_tools: z.array(z.object({
    alias: z.string().max(40).optional(),
    description: z.string().optional(),
    enabled: z.boolean().optional(),
    name: z.string(),
  })).optional(),
});

const McpPortalsApiSyncServerSchema = z.object({
  error: z.string().optional(),
  error_details: z.object({
    cause: z.string().optional(),
    is_upstream: z.boolean().optional(),
    mcp_code: z.number().optional(),
    retryable: z.boolean().optional(),
    status_code: z.number().optional(),
  }).optional(),
  status: z.enum(["waiting", "ready", "stale", "error"]).optional(),
});

const AccessApplicationsItemSchema = z.union([
  z.object({}),
  z.object({}),
  z.object({}),
  z.object({}),
  z.object({}),
  z.object({}),
  z.object({}),
  z.object({}),
  z.object({}),
  z.object({}),
  z.object({}),
  z.object({}),
  z.object({}),
]);

const ListAccessApplicationsSchema = z.object({
  items: z.array(AccessApplicationsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const AccessApplicationsAddAnApplicationSchema = z.union([
  z.object({}),
  z.object({}),
  z.object({}),
  z.object({}),
  z.object({}),
  z.object({}),
  z.object({}),
  z.object({}),
  z.object({}),
  z.object({}),
  z.object({}),
  z.object({}),
  z.object({}),
]);

const ShortLivedCertificateCAsItemSchema = z.object({
  aud: z.unknown().optional(),
  id: z.unknown().optional(),
  public_key: z.unknown().optional(),
});

const ListShortLivedCertificateCAsSchema = z.object({
  items: z.array(ShortLivedCertificateCAsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetAnAccessApplicationSchema = z.union([
  z.object({}),
  z.object({}),
  z.object({}),
  z.object({}),
  z.object({}),
  z.object({}),
  z.object({}),
  z.object({}),
  z.object({}),
  z.object({}),
  z.object({}),
  z.object({}),
  z.object({}),
]);

const GetAShortLivedCertificateCaSchema = z.object({
  aud: z.unknown().optional(),
  id: z.unknown().optional(),
  public_key: z.unknown().optional(),
});

const AccessAppPoliciesItemSchema = z.object({
  created_at: z.unknown().optional(),
  decision: z.unknown().optional(),
  exclude: z.unknown().optional(),
  id: z.unknown().optional(),
  include: z.unknown().optional(),
  name: z.unknown().optional(),
  require: z.unknown().optional(),
  updated_at: z.unknown().optional(),
  precedence: z.unknown().optional(),
});

const ListAccessAppPoliciesSchema = z.object({
  items: z.array(AccessAppPoliciesItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateAnAccessPolicySchema = z.object({
  created_at: z.unknown().optional(),
  decision: z.unknown().optional(),
  exclude: z.unknown().optional(),
  id: z.unknown().optional(),
  include: z.unknown().optional(),
  name: z.unknown().optional(),
  require: z.unknown().optional(),
  updated_at: z.unknown().optional(),
  precedence: z.unknown().optional(),
});

const UpdateAccessPoliciesConvertReusableSchema = z.object({
  created_at: z.unknown().optional(),
  decision: z.unknown().optional(),
  exclude: z.unknown().optional(),
  id: z.unknown().optional(),
  include: z.unknown().optional(),
  name: z.unknown().optional(),
  require: z.unknown().optional(),
  updated_at: z.unknown().optional(),
  precedence: z.unknown().optional(),
});

const GetAccessApplicationsTestAccessPoliciesSchema = z.object({
  app_state: z.object({
    app_uid: z.unknown().optional(),
    aud: z.string().optional(),
    hostname: z.string().optional(),
    name: z.string().optional(),
    policies: z.array(z.object({})).optional(),
    status: z.string().optional(),
  }).optional(),
  user_identity: z.object({
    account_id: z.string().optional(),
    device_sessions: z.object({}).optional(),
    email: z.string().optional(),
    geo: z.object({
      country: z.string().optional(),
    }).optional(),
    iat: z.number().int().optional(),
    id: z.string().optional(),
    is_gateway: z.boolean().optional(),
    is_warp: z.boolean().optional(),
    name: z.string().optional(),
    user_uuid: z.unknown().optional(),
    version: z.number().int().optional(),
  }).optional(),
});

const ListItemSchema = z.object({
  aaguid: z.unknown(),
  name: z.unknown(),
});

const ListSchema = z.object({
  items: z.array(ListItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const MtlsCertificatesItemSchema = z.object({
  associated_hostnames: z.unknown().optional(),
  created_at: z.unknown().optional(),
  expires_on: z.unknown().optional(),
  fingerprint: z.unknown().optional(),
  id: z.string().optional().describe(
    "The ID of the application that will use this certificate.",
  ),
  name: z.unknown().optional(),
  updated_at: z.unknown().optional(),
});

const ListMtlsCertificatesSchema = z.object({
  items: z.array(MtlsCertificatesItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateAccessMtlsAuthenticationAddAnMtlsCertificateSchema = z.object({
  associated_hostnames: z.unknown().optional(),
  created_at: z.unknown().optional(),
  expires_on: z.unknown().optional(),
  fingerprint: z.unknown().optional(),
  id: z.string().optional().describe(
    "The ID of the application that will use this certificate.",
  ),
  name: z.unknown().optional(),
  updated_at: z.unknown().optional(),
});

const MtlsCertificatesHostnameSettingsItemSchema = z.object({
  china_network: z.boolean().describe(
    "Request client certificates for this hostname in China. Can only be set to true if this zone is c...",
  ),
  client_certificate_forwarding: z.boolean().describe(
    "Client Certificate Forwarding is a feature that takes the client cert provided by the eyeball to ...",
  ),
  hostname: z.string().describe("The hostname that these settings apply to."),
});

const ListMtlsCertificatesHostnameSettingsSchema = z.object({
  items: z.array(MtlsCertificatesHostnameSettingsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetAnMtlsCertificateSchema = z.object({
  associated_hostnames: z.unknown().optional(),
  created_at: z.unknown().optional(),
  expires_on: z.unknown().optional(),
  fingerprint: z.unknown().optional(),
  id: z.string().optional().describe(
    "The ID of the application that will use this certificate.",
  ),
  name: z.unknown().optional(),
  updated_at: z.unknown().optional(),
});

const CustomPagesItemSchema = z.object({
  app_count: z.unknown().optional(),
  created_at: z.unknown().optional(),
  name: z.unknown(),
  type: z.unknown(),
  uid: z.unknown().optional(),
  updated_at: z.unknown().optional(),
});

const ListCustomPagesSchema = z.object({
  items: z.array(CustomPagesItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateACustomPageSchema = z.object({
  app_count: z.unknown().optional(),
  created_at: z.unknown().optional(),
  name: z.unknown(),
  type: z.unknown(),
  uid: z.unknown().optional(),
  updated_at: z.unknown().optional(),
});

const SshCaItemSchema = z.object({
  id: z.string().optional().describe("The key ID of this certificate."),
  public_key: z.string().optional().describe(
    "The public key of this certificate.",
  ),
});

const ListSshCaSchema = z.object({
  items: z.array(SshCaItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const AccessGatewayCaAddAnSshCaSchema = z.object({
  id: z.string().optional().describe("The key ID of this certificate."),
  public_key: z.string().optional().describe(
    "The public key of this certificate.",
  ),
});

const AccessGroupsItemSchema = z.object({
  created_at: z.unknown().optional(),
  exclude: z.unknown().optional(),
  id: z.unknown().optional(),
  include: z.unknown().optional(),
  is_default: z.unknown().optional(),
  name: z.unknown().optional(),
  require: z.unknown().optional(),
  updated_at: z.unknown().optional(),
});

const ListAccessGroupsSchema = z.object({
  items: z.array(AccessGroupsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateAnAccessGroupSchema = z.object({
  created_at: z.unknown().optional(),
  exclude: z.unknown().optional(),
  id: z.unknown().optional(),
  include: z.unknown().optional(),
  is_default: z.unknown().optional(),
  name: z.unknown().optional(),
  require: z.unknown().optional(),
  updated_at: z.unknown().optional(),
});

const AccessIdentityProvidersItemSchema = z.union([
  z.unknown(),
  z.unknown(),
  z.unknown(),
  z.unknown(),
  z.unknown(),
  z.unknown(),
  z.unknown(),
  z.unknown(),
  z.unknown(),
  z.unknown(),
  z.unknown(),
  z.unknown(),
  z.unknown(),
  z.unknown(),
  z.unknown(),
]);

const ListAccessIdentityProvidersSchema = z.object({
  items: z.array(AccessIdentityProvidersItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const AccessIdentityProvidersAddAnAccessIdentityProviderSchema = z.union([
  z.unknown(),
  z.unknown(),
  z.unknown(),
  z.unknown(),
  z.unknown(),
  z.unknown(),
  z.unknown(),
  z.unknown(),
  z.unknown(),
  z.unknown(),
  z.unknown(),
  z.unknown(),
  z.unknown(),
  z.unknown(),
  z.unknown(),
]);

const GetAnAccessIdentityProviderSchema = z.union([
  z.unknown(),
  z.unknown(),
  z.unknown(),
  z.unknown(),
  z.unknown(),
  z.unknown(),
  z.unknown(),
  z.unknown(),
  z.unknown(),
  z.unknown(),
  z.unknown(),
  z.unknown(),
  z.unknown(),
  z.unknown(),
  z.unknown(),
]);

const CreateSamlCertificateForIdentityProviderSchema = z.object({
  created_at: z.string().describe(
    "Timestamp when the certificate set was created",
  ),
  current_certificate: z.unknown().optional().describe(
    "The currently active certificate used for encrypting SAML assertions",
  ),
  previous_certificate: z.object({}).nullable().optional().describe(
    "The previous certificate, maintained during rotation to ensure continuity. Null if no rotation ha...",
  ),
  uid: z.string().describe("Unique identifier for the certificate set"),
  updated_at: z.string().describe(
    "Timestamp when the certificate set was last updated (e.g., during rotation)",
  ),
});

const ScimGroupResourcesItemSchema = z.object({
  displayName: z.string().optional().describe(
    "The display name of the SCIM Group resource.",
  ),
  externalId: z.unknown().optional(),
  id: z.unknown().optional(),
  meta: z.unknown().optional(),
  schemas: z.array(z.string()).optional().describe(
    "The list of URIs which indicate the attributes contained within a SCIM resource.",
  ),
});

const ListScimGroupResourcesSchema = z.object({
  items: z.array(ScimGroupResourcesItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const ScimUserResourcesItemSchema = z.object({
  active: z.boolean().optional().describe(
    "Determines the status of the SCIM User resource.",
  ),
  displayName: z.string().optional().describe(
    "The name of the SCIM User resource.",
  ),
  emails: z.array(z.object({
    primary: z.boolean().optional(),
    type: z.string().optional(),
    value: z.string().optional(),
  })).optional(),
  externalId: z.string().optional().describe(
    "The IdP-generated Id of the SCIM resource.",
  ),
  id: z.unknown().optional(),
  meta: z.unknown().optional(),
  schemas: z.array(z.string()).optional().describe(
    "The list of URIs which indicate the attributes contained within a SCIM resource.",
  ),
});

const ListScimUserResourcesSchema = z.object({
  items: z.array(ScimUserResourcesItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateSchema = z.object({
  created_at: z.unknown(),
  id: z.unknown().describe("UID of the IdP federation grant."),
  idp_id: z.string().describe("UID of the identity provider being federated."),
});

const GetSchema = z.object({
  created_at: z.unknown(),
  id: z.unknown().describe("UID of the IdP federation grant."),
  idp_id: z.string().describe("UID of the identity provider being federated."),
});

const GetTheAccessKeyConfigurationSchema = z.object({
  days_until_next_rotation: z.unknown().optional(),
  key_rotation_interval_days: z.unknown().optional(),
  last_key_rotation_at: z.unknown().optional(),
});

const AccessKeyConfigurationRotateAccessKeysSchema = z.object({
  days_until_next_rotation: z.unknown().optional(),
  key_rotation_interval_days: z.unknown().optional(),
  last_key_rotation_at: z.unknown().optional(),
});

const GetAccessAuthenticationLogsItemSchema = z.object({
  action: z.unknown().optional(),
  allowed: z.unknown().optional(),
  app_domain: z.unknown().optional(),
  app_uid: z.unknown().optional(),
  connection: z.unknown().optional(),
  created_at: z.unknown().optional(),
  ip_address: z.unknown().optional(),
  ray_id: z.unknown().optional(),
  user_email: z.unknown().optional(),
});

const GetAccessAuthenticationLogsSchema = z.object({
  items: z.array(GetAccessAuthenticationLogsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const AccessScimUpdateLogsItemSchema = z.object({
  cf_resource_id: z.string().optional().describe(
    "The unique Cloudflare-generated Id of the SCIM resource.",
  ),
  error_description: z.string().optional().describe(
    "The error message which is generated when the status of the SCIM request is 'FAILURE'.",
  ),
  idp_id: z.string().optional().describe(
    "The unique Id of the IdP that has SCIM enabled.",
  ),
  idp_resource_id: z.string().optional().describe(
    "The IdP-generated Id of the SCIM resource.",
  ),
  logged_at: z.unknown().optional(),
  request_body: z.string().optional().describe(
    "The JSON-encoded string body of the SCIM request.",
  ),
  request_method: z.string().optional().describe(
    "The request method of the SCIM request.",
  ),
  resource_group_name: z.string().optional().describe(
    "The display name of the SCIM Group resource if it exists.",
  ),
  resource_type: z.string().optional().describe(
    "The resource type of the SCIM request.",
  ),
  resource_user_email: z.string().optional().describe(
    "The email address of the SCIM User resource if it exists.",
  ),
  status: z.string().optional().describe("The status of the SCIM request."),
});

const ListAccessScimUpdateLogsSchema = z.object({
  items: z.array(AccessScimUpdateLogsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetYourZeroTrustOrganizationSchema = z.object({
  allow_authenticate_via_warp: z.unknown().optional(),
  auth_domain: z.unknown().optional(),
  auto_redirect_to_identity: z.unknown().optional(),
  created_at: z.unknown().optional(),
  custom_pages: z.unknown().optional(),
  deny_unmatched_requests: z.unknown().optional(),
  deny_unmatched_requests_exempted_zone_names: z.unknown().optional(),
  is_ui_read_only: z.unknown().optional(),
  login_design: z.unknown().optional(),
  mfa_config: z.unknown().optional(),
  mfa_piv_key_requirements: z.unknown().optional(),
  mfa_required_for_all_apps: z.unknown().optional(),
  name: z.unknown().optional(),
  session_duration: z.unknown().optional(),
  ui_read_only_toggle_reason: z.unknown().optional(),
  updated_at: z.unknown().optional(),
  user_seat_expiration_inactive_time: z.unknown().optional(),
  warp_auth_session_duration: z.unknown().optional(),
});

const GetYourZeroTrustOrganizationDohSettingsSchema = z.object({
  doh_jwt_duration: z.string().optional().describe(
    "The duration the DoH JWT is valid for. Must be in the format `300ms` or `2h45m`. Valid time units...",
  ),
});

const CreateZeroTrustOrganizationRevokeAllAccessTokensForAUserSchema = z.union([
  z.literal(true),
  z.literal(false),
]);

const AccessReusablePoliciesItemSchema = z.object({
  created_at: z.unknown().optional(),
  decision: z.unknown().optional(),
  exclude: z.unknown().optional(),
  id: z.unknown().optional(),
  include: z.unknown().optional(),
  name: z.unknown().optional(),
  require: z.unknown().optional(),
  updated_at: z.unknown().optional(),
  app_count: z.unknown().optional(),
  reusable: z.union([z.literal(true)]).optional(),
});

const ListAccessReusablePoliciesSchema = z.object({
  items: z.array(AccessReusablePoliciesItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateAnAccessReusablePolicySchema = z.object({
  created_at: z.unknown().optional(),
  decision: z.unknown().optional(),
  exclude: z.unknown().optional(),
  id: z.unknown().optional(),
  include: z.unknown().optional(),
  name: z.unknown().optional(),
  require: z.unknown().optional(),
  updated_at: z.unknown().optional(),
  app_count: z.unknown().optional(),
  reusable: z.union([z.literal(true)]).optional(),
});

const CreateAccessPolicyTestsSchema = z.object({
  id: z.unknown().optional(),
  status: z.unknown().optional(),
});

const GetAnUpdateSchema = z.object({
  id: z.unknown().optional(),
  percent_approved: z.unknown().optional(),
  percent_blocked: z.unknown().optional(),
  percent_errored: z.unknown().optional(),
  percent_users_processed: z.unknown().optional(),
  status: z.unknown().optional(),
  total_users: z.unknown().optional(),
  users_approved: z.unknown().optional(),
  users_blocked: z.unknown().optional(),
  users_errored: z.unknown().optional(),
});

const GetAUserPageItemSchema = z.object({
  email: z.unknown().optional(),
  id: z.unknown().optional(),
  name: z.unknown().optional(),
  status: z.unknown().optional(),
});

const GetAUserPageSchema = z.object({
  items: z.array(GetAUserPageItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CertificateSetsItemSchema = z.object({
  created_at: z.string().describe("When the certificate set was created"),
  current_certificate: z.unknown().optional().describe(
    "The current active certificate",
  ),
  previous_certificate: z.object({}).nullable().optional().describe(
    "The previous certificate (maintained during rotation period). May be null when no rotation has oc...",
  ),
  uid: z.string().describe("Unique identifier for the certificate set"),
  updated_at: z.string().describe("When the certificate set was last updated"),
});

const ListCertificateSetsSchema = z.object({
  items: z.array(CertificateSetsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetCertificateSetSchema = z.object({
  created_at: z.string().describe("When the certificate set was created"),
  current_certificate: z.unknown().optional().describe(
    "The current active certificate",
  ),
  previous_certificate: z.object({}).nullable().optional().describe(
    "The previous certificate (maintained during rotation period). May be null when no rotation has oc...",
  ),
  uid: z.string().describe("Unique identifier for the certificate set"),
  updated_at: z.string().describe("When the certificate set was last updated"),
});

const AccessSamlCertificatesRotateCertificateSchema = z.object({
  created_at: z.string().describe("When the certificate set was created"),
  current_certificate: z.unknown().optional().describe(
    "The current active certificate",
  ),
  previous_certificate: z.object({}).nullable().optional().describe(
    "The previous certificate (maintained during rotation period). May be null when no rotation has oc...",
  ),
  uid: z.string().describe("Unique identifier for the certificate set"),
  updated_at: z.string().describe("When the certificate set was last updated"),
});

const UpdateAUserSeatSchema = z.object({
  access_seat: z.unknown().optional(),
  created_at: z.unknown().optional(),
  gateway_seat: z.unknown().optional(),
  seat_uid: z.unknown().optional(),
  updated_at: z.unknown().optional(),
});

const ServiceTokensItemSchema = z.object({
  client_id: z.unknown().optional(),
  created_at: z.unknown().optional(),
  duration: z.unknown().optional(),
  expires_at: z.unknown().optional(),
  id: z.unknown().optional(),
  last_seen_at: z.unknown().optional(),
  name: z.unknown().optional(),
  updated_at: z.unknown().optional(),
});

const ListServiceTokensSchema = z.object({
  items: z.array(ServiceTokensItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateAServiceTokenSchema = z.object({
  client_id: z.unknown().optional(),
  client_secret: z.unknown().optional(),
  created_at: z.unknown().optional(),
  duration: z.unknown().optional(),
  id: z.string().optional().describe("The ID of the service token."),
  name: z.unknown().optional(),
  updated_at: z.unknown().optional(),
});

const AccessServiceTokensRefreshAServiceTokenSchema = z.object({
  client_id: z.unknown().optional(),
  created_at: z.unknown().optional(),
  duration: z.unknown().optional(),
  expires_at: z.unknown().optional(),
  id: z.unknown().optional(),
  last_seen_at: z.unknown().optional(),
  name: z.unknown().optional(),
  updated_at: z.unknown().optional(),
});

const CreateAccessServiceTokensRotateAServiceTokenSchema = z.object({
  client_id: z.unknown().optional(),
  client_secret: z.unknown().optional(),
  created_at: z.unknown().optional(),
  duration: z.unknown().optional(),
  id: z.string().optional().describe("The ID of the service token."),
  name: z.unknown().optional(),
  updated_at: z.unknown().optional(),
});

const TagsItemSchema = z.object({
  app_count: z.number().int().optional().describe(
    "The number of applications that have this tag",
  ),
  created_at: z.unknown().optional(),
  name: z.unknown(),
  updated_at: z.unknown().optional(),
});

const ListTagsSchema = z.object({
  items: z.array(TagsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateTagSchema = z.object({
  app_count: z.number().int().optional().describe(
    "The number of applications that have this tag",
  ),
  created_at: z.unknown().optional(),
  name: z.unknown(),
  updated_at: z.unknown().optional(),
});

const GetATagSchema = z.object({
  app_count: z.number().int().optional().describe(
    "The number of applications that have this tag",
  ),
  created_at: z.unknown().optional(),
  name: z.unknown(),
  updated_at: z.unknown().optional(),
});

const GetUsersItemSchema = z.object({
  access_seat: z.unknown().optional(),
  active_device_count: z.unknown().optional(),
  created_at: z.unknown().optional(),
  email: z.unknown().optional(),
  gateway_seat: z.unknown().optional(),
  id: z.unknown().optional(),
  last_successful_login: z.unknown().optional(),
  name: z.unknown().optional(),
  seat_uid: z.unknown().optional(),
  uid: z.unknown().optional(),
  updated_at: z.unknown().optional(),
});

const GetUsersSchema = z.object({
  items: z.array(GetUsersItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateUserSchema = z.object({
  access_seat: z.unknown().optional(),
  active_device_count: z.unknown().optional(),
  created_at: z.unknown().optional(),
  email: z.unknown().optional(),
  gateway_seat: z.unknown().optional(),
  id: z.unknown().optional(),
  last_successful_login: z.unknown().optional(),
  name: z.unknown().optional(),
  seat_uid: z.unknown().optional(),
  uid: z.unknown().optional(),
  updated_at: z.unknown().optional(),
});

const GetActiveSessionsItemSchema = z.object({
  expiration: z.number().int().optional(),
  metadata: z.object({
    apps: z.record(
      z.string(),
      z.object({
        hostname: z.string().optional(),
        name: z.string().optional(),
        type: z.string().optional(),
        uid: z.string().optional(),
      }),
    ).optional(),
    expires: z.number().int().optional(),
    iat: z.number().int().optional(),
    nonce: z.string().optional(),
    ttl: z.number().int().optional(),
  }).optional(),
  name: z.string().optional(),
});

const GetActiveSessionsSchema = z.object({
  items: z.array(GetActiveSessionsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetActiveSessionSchema = z.object({
  account_id: z.string().optional(),
  auth_status: z.string().optional(),
  common_name: z.string().optional(),
  devicePosture: z.record(z.string(), z.unknown()).optional(),
  device_id: z.string().optional(),
  device_sessions: z.unknown().optional(),
  email: z.string().optional(),
  geo: z.unknown().optional(),
  iat: z.number().optional(),
  idp: z.object({
    id: z.string().optional(),
    type: z.string().optional(),
  }).optional(),
  ip: z.string().optional(),
  is_gateway: z.boolean().optional(),
  is_warp: z.boolean().optional(),
  mtls_auth: z.object({
    auth_status: z.string().optional(),
    cert_issuer_dn: z.string().optional(),
    cert_issuer_ski: z.string().optional(),
    cert_presented: z.boolean().optional(),
    cert_serial: z.string().optional(),
  }).optional(),
  service_token_id: z.string().optional(),
  service_token_status: z.boolean().optional(),
  user_uuid: z.string().optional(),
  version: z.number().optional(),
  isActive: z.boolean().optional(),
});

const GetFailedLoginsItemSchema = z.object({
  expiration: z.number().int().optional(),
  metadata: z.object({}).optional(),
});

const GetFailedLoginsSchema = z.object({
  items: z.array(GetFailedLoginsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetLastSeenIdentitySchema = z.object({
  account_id: z.string().optional(),
  auth_status: z.string().optional(),
  common_name: z.string().optional(),
  devicePosture: z.record(z.string(), z.unknown()).optional(),
  device_id: z.string().optional(),
  device_sessions: z.unknown().optional(),
  email: z.string().optional(),
  geo: z.unknown().optional(),
  iat: z.number().optional(),
  idp: z.object({
    id: z.string().optional(),
    type: z.string().optional(),
  }).optional(),
  ip: z.string().optional(),
  is_gateway: z.boolean().optional(),
  is_warp: z.boolean().optional(),
  mtls_auth: z.object({
    auth_status: z.string().optional(),
    cert_issuer_dn: z.string().optional(),
    cert_issuer_ski: z.string().optional(),
    cert_presented: z.boolean().optional(),
    cert_serial: z.string().optional(),
  }).optional(),
  service_token_id: z.string().optional(),
  service_token_status: z.boolean().optional(),
  user_uuid: z.string().optional(),
  version: z.number().optional(),
});

// =============================================================================
// Model Definition
// =============================================================================

/** Cloudflare Access (Zero Trust) — applications, policies, identity providers, certificates */
export const model = {
  type: "@webframp/cloudflare/access",
  version: "2026.07.19.1",
  globalArguments: GlobalArgsSchema,

  upgrades: [],

  resources: {
    "portals": {
      description: "List MCP Portals",
      schema: ListPortalsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "mcp_portals_api_fetch_gateways": {
      description: "Read details of an MCP Portal",
      schema: GetMcpPortalsApiFetchGatewaysSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "servers": {
      description: "List MCP Servers",
      schema: ListServersSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "mcp_portals_api_fetch_servers": {
      description: "Read the details of a MCP Server",
      schema: GetMcpPortalsApiFetchServersSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "mcp_portals_api_sync_server": {
      description: "Sync MCP Server Capabilities",
      schema: McpPortalsApiSyncServerSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "access_applications": {
      description: "List Access applications",
      schema: ListAccessApplicationsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "access_applications_add_an_application": {
      description: "Add an Access application",
      schema: AccessApplicationsAddAnApplicationSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "short_lived_certificate_c_as": {
      description: "List short-lived certificate CAs",
      schema: ListShortLivedCertificateCAsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "an_access_application": {
      description: "Get an Access application",
      schema: GetAnAccessApplicationSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "a_short_lived_certificate_ca": {
      description: "Get a short-lived certificate CA",
      schema: GetAShortLivedCertificateCaSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "access_app_policies": {
      description: "List Access application policies",
      schema: ListAccessAppPoliciesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "an_access_policy": {
      description: "Create an Access application policy",
      schema: CreateAnAccessPolicySchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "access_policies_convert_reusable": {
      description: "Convert an Access application policy to a reusable policy",
      schema: UpdateAccessPoliciesConvertReusableSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "access_applications_revoke_service_tokens": {
      description: "Revoke application tokens",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "access_application_settings": {
      description: "Update Access application settings",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "access_applications_test_access_policies": {
      description: "Test Access policies",
      schema: GetAccessApplicationsTestAccessPoliciesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "list": {
      description: "List authenticator device AAGUIDs",
      schema: ListSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "mtls_certificates": {
      description: "List mTLS certificates",
      schema: ListMtlsCertificatesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "access_mtls_authentication_add_an_mtls_certificate": {
      description: "Add an mTLS certificate",
      schema: CreateAccessMtlsAuthenticationAddAnMtlsCertificateSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "mtls_certificates_hostname_settings": {
      description: "List all mTLS hostname settings",
      schema: ListMtlsCertificatesHostnameSettingsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "an_mtls_certificate_settings": {
      description: "Update an mTLS certificate's hostname settings",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "an_mtls_certificate": {
      description: "Get an mTLS certificate",
      schema: GetAnMtlsCertificateSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "custom_pages": {
      description: "List custom pages",
      schema: ListCustomPagesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "a_custom_page": {
      description: "Create a custom page",
      schema: CreateACustomPageSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "ssh_ca": {
      description: "List SSH Certificate Authorities (CA)",
      schema: ListSshCaSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "access_gateway_ca_add_an_ssh_ca": {
      description: "Add a new SSH Certificate Authority (CA)",
      schema: AccessGatewayCaAddAnSshCaSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "access_groups": {
      description: "List Access groups",
      schema: ListAccessGroupsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "an_access_group": {
      description: "Create an Access group",
      schema: CreateAnAccessGroupSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "access_identity_providers": {
      description: "List Access identity providers",
      schema: ListAccessIdentityProvidersSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "access_identity_providers_add_an_access_identity_provider": {
      description: "Add an Access identity provider",
      schema: AccessIdentityProvidersAddAnAccessIdentityProviderSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "an_access_identity_provider": {
      description: "Get an Access identity provider",
      schema: GetAnAccessIdentityProviderSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "saml_certificate_for_identity_provider": {
      description: "Create SAML encryption certificate for Identity Provider",
      schema: CreateSamlCertificateForIdentityProviderSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "scim_group_resources": {
      description: "List SCIM Group resources",
      schema: ListScimGroupResourcesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "scim_user_resources": {
      description: "List SCIM User resources",
      schema: ListScimUserResourcesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "create": {
      description: "Create an IdP federation grant",
      schema: CreateSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "get": {
      description: "Get an IdP federation grant",
      schema: GetSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "the_access_key_configuration": {
      description: "Get the Access key configuration",
      schema: GetTheAccessKeyConfigurationSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "access_key_configuration_rotate_access_keys": {
      description: "Rotate Access keys",
      schema: AccessKeyConfigurationRotateAccessKeysSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "get_access_authentication_logs": {
      description: "Get Access authentication logs",
      schema: GetAccessAuthenticationLogsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "access_scim_update_logs": {
      description: "List Access SCIM update logs",
      schema: ListAccessScimUpdateLogsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "your_zero_trust_organization": {
      description: "Get your Zero Trust organization",
      schema: GetYourZeroTrustOrganizationSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "your_zero_trust_organization_doh_settings": {
      description: "Get your Zero Trust organization DoH settings",
      schema: GetYourZeroTrustOrganizationDohSettingsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "zero_trust_organization_revoke_all_access_tokens_for_a_user": {
      description: "Revoke all Access tokens for a user",
      schema: CreateZeroTrustOrganizationRevokeAllAccessTokensForAUserSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "access_reusable_policies": {
      description: "List Access reusable policies",
      schema: ListAccessReusablePoliciesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "an_access_reusable_policy": {
      description: "Create an Access reusable policy",
      schema: CreateAnAccessReusablePolicySchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "access_policy_tests": {
      description: "Start Access policy test",
      schema: CreateAccessPolicyTestsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "an_update": {
      description: "Get the current status of a given Access policy test",
      schema: GetAnUpdateSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "get_a_user_page": {
      description: "Get an Access policy test users page",
      schema: GetAUserPageSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "certificate_sets": {
      description: "List SAML certificate sets",
      schema: ListCertificateSetsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "certificate_set": {
      description: "Get SAML certificate set",
      schema: GetCertificateSetSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "pem": {
      description: "Download current certificate in PEM format",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "access_saml_certificates_rotate_certificate": {
      description: "Rotate SAML certificate",
      schema: AccessSamlCertificatesRotateCertificateSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "a_user_seat": {
      description: "Update a user seat",
      schema: UpdateAUserSeatSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "service_tokens": {
      description: "List service tokens",
      schema: ListServiceTokensSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "a_service_token": {
      description: "Create a service token",
      schema: CreateAServiceTokenSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "access_service_tokens_refresh_a_service_token": {
      description: "Refresh a service token",
      schema: AccessServiceTokensRefreshAServiceTokenSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "access_service_tokens_rotate_a_service_token": {
      description: "Rotate a service token",
      schema: CreateAccessServiceTokensRotateAServiceTokenSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "tags": {
      description: "List tags",
      schema: ListTagsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "tag": {
      description: "Create a tag",
      schema: CreateTagSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "a_tag": {
      description: "Get a tag",
      schema: GetATagSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "get_users": {
      description: "Get users",
      schema: GetUsersSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "user": {
      description: "Create a user",
      schema: CreateUserSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "get_active_sessions": {
      description: "Get active sessions",
      schema: GetActiveSessionsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "active_session": {
      description: "Get single active session",
      schema: GetActiveSessionSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "get_failed_logins": {
      description: "Get failed logins",
      schema: GetFailedLoginsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "last_seen_identity": {
      description: "Get last seen identity",
      schema: GetLastSeenIdentitySchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
  },

  methods: {
    list_portals: {
      description: "List MCP Portals",
      arguments: z.object({
        page: z.number().optional(),
        per_page: z.number().optional(),
        search: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/access/ai-controls/mcp/portals`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("portals", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} portals", { count: results.length });
        return { dataHandles: [handle] };
      },
    },
    create_portals: {
      description: "Create a new MCP Portal",
      arguments: z.object({
        allow_code_mode: z.boolean().optional().describe(
          "Allow remote code execution in Dynamic Workers (beta)",
        ),
        description: z.string().max(512).optional(),
        hostname: z.string(),
        name: z.string().max(350),
        secure_web_gateway: z.boolean().optional().describe(
          "Route outbound MCP traffic through Zero Trust Secure Web Gateway",
        ),
        servers: z.array(z.object({
          default_disabled: z.boolean().optional(),
          on_behalf: z.boolean().optional(),
          server_id: z.string().min(1).max(32),
          updated_prompts: z.array(z.object({
            alias: z.string().max(40).optional(),
            description: z.string().optional(),
            enabled: z.boolean().optional(),
            name: z.string(),
          })).optional(),
          updated_tools: z.array(z.object({
            alias: z.string().max(40).optional(),
            description: z.string().optional(),
            enabled: z.boolean().optional(),
            name: z.string(),
          })).optional(),
        })).optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/access/ai-controls/mcp/portals`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource("portals", id, result);
        context.logger.info("Created portals {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_mcp_portals_api_fetch_gateways: {
      description: "Read details of an MCP Portal",
      arguments: z.object({
        id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/access/ai-controls/mcp/portals/${args.id}`,
        );

        const handle = await context.writeResource(
          "mcp_portals_api_fetch_gateways",
          String(args.id),
          result,
        );
        context.logger.info("Fetched mcp_portals_api_fetch_gateways", {});
        return { dataHandles: [handle] };
      },
    },
    update_portals: {
      description: "Update a MCP Portal",
      arguments: z.object({
        id: z.string(),
        allow_code_mode: z.boolean().optional().describe(
          "Allow remote code execution in Dynamic Workers (beta)",
        ),
        description: z.string().max(512).optional(),
        hostname: z.string().optional(),
        name: z.string().max(350).optional(),
        secure_web_gateway: z.boolean().optional().describe(
          "Route outbound MCP traffic through Zero Trust Secure Web Gateway",
        ),
        servers: z.array(z.object({
          default_disabled: z.boolean().optional(),
          on_behalf: z.boolean().optional(),
          server_id: z.string().min(1).max(32),
          updated_prompts: z.array(z.object({
            alias: z.string().max(40).optional(),
            description: z.string().optional(),
            enabled: z.boolean().optional(),
            name: z.string(),
          })).optional(),
          updated_tools: z.array(z.object({
            alias: z.string().max(40).optional(),
            description: z.string().optional(),
            enabled: z.boolean().optional(),
            name: z.string(),
          })).optional(),
        })).optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/access/ai-controls/mcp/portals/${args.id}`,
          args,
        );

        const handle = await context.writeResource(
          "portals",
          String(args.id),
          result,
        );
        context.logger.info("Updated portals", {});
        return { dataHandles: [handle] };
      },
    },
    delete_portals: {
      description: "Delete a MCP Portal",
      arguments: z.object({
        id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/access/ai-controls/mcp/portals/${args.id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.id });
        return { dataHandles: [] };
      },
    },
    list_servers: {
      description: "List MCP Servers",
      arguments: z.object({
        page: z.number().optional(),
        per_page: z.number().optional(),
        search: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/access/ai-controls/mcp/servers`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("servers", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} servers", { count: results.length });
        return { dataHandles: [handle] };
      },
    },
    create_servers: {
      description: "Create a new MCP Server",
      arguments: z.object({
        auth_credentials: z.string().optional(),
        auth_type: z.enum(["oauth", "bearer", "unauthenticated"]),
        description: z.string().max(512).nullable().optional(),
        hostname: z.string(),
        is_shared_oauth_callback_enabled: z.boolean().optional().describe(
          "When true, the gateway worker uses the shared Cloudflare-owned OAuth callback...",
        ),
        name: z.string().max(350),
        secure_web_gateway: z.boolean().optional().describe(
          "Route outbound traffic to this MCP server through Zero Trust Secure Web Gateway",
        ),
        updated_prompts: z.array(z.object({
          alias: z.string().max(40).optional(),
          description: z.string().optional(),
          enabled: z.boolean().optional(),
          name: z.string(),
        })).optional(),
        updated_tools: z.array(z.object({
          alias: z.string().max(40).optional(),
          description: z.string().optional(),
          enabled: z.boolean().optional(),
          name: z.string(),
        })).optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/access/ai-controls/mcp/servers`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource("servers", id, result);
        context.logger.info("Created servers {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_mcp_portals_api_fetch_servers: {
      description: "Read the details of a MCP Server",
      arguments: z.object({
        id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/access/ai-controls/mcp/servers/${args.id}`,
        );

        const handle = await context.writeResource(
          "mcp_portals_api_fetch_servers",
          String(args.id),
          result,
        );
        context.logger.info("Fetched mcp_portals_api_fetch_servers", {});
        return { dataHandles: [handle] };
      },
    },
    update_servers: {
      description: "Update a MCP Server",
      arguments: z.object({
        id: z.string(),
        auth_credentials: z.string().optional(),
        description: z.string().max(512).nullable().optional(),
        is_shared_oauth_callback_enabled: z.boolean().optional().describe(
          "When true, the gateway worker uses the shared Cloudflare-owned OAuth callback...",
        ),
        name: z.string().max(350).optional(),
        secure_web_gateway: z.boolean().optional().describe(
          "Route outbound traffic to this MCP server through Zero Trust Secure Web Gateway",
        ),
        updated_prompts: z.array(z.object({
          alias: z.string().max(40).optional(),
          description: z.string().optional(),
          enabled: z.boolean().optional(),
          name: z.string(),
        })).optional(),
        updated_tools: z.array(z.object({
          alias: z.string().max(40).optional(),
          description: z.string().optional(),
          enabled: z.boolean().optional(),
          name: z.string(),
        })).optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/access/ai-controls/mcp/servers/${args.id}`,
          args,
        );

        const handle = await context.writeResource(
          "servers",
          String(args.id),
          result,
        );
        context.logger.info("Updated servers", {});
        return { dataHandles: [handle] };
      },
    },
    delete_servers: {
      description: "Delete a MCP Server",
      arguments: z.object({
        id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/access/ai-controls/mcp/servers/${args.id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.id });
        return { dataHandles: [] };
      },
    },
    mcp_portals_api_sync_server: {
      description: "Sync MCP Server Capabilities",
      arguments: z.object({
        id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/access/ai-controls/mcp/servers/${args.id}/sync`,
        );

        const handle = await context.writeResource(
          "mcp_portals_api_sync_server",
          "latest",
          result,
        );
        context.logger.info("Executed mcp_portals_api_sync_server", {});
        return { dataHandles: [handle] };
      },
    },
    list_access_applications: {
      description: "List Access applications",
      arguments: z.object({
        name: z.string().optional(),
        domain: z.string().optional(),
        aud: z.string().optional(),
        target_attributes: z.string().optional(),
        exact: z.boolean().optional(),
        search: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/access/apps`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "access_applications",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} access_applications", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    access_applications_add_an_application: {
      description: "Add an Access application",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/access/apps`,
          args,
        );

        const handle = await context.writeResource(
          "access_applications_add_an_application",
          "latest",
          result,
        );
        context.logger.info(
          "Executed access_applications_add_an_application",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    list_short_lived_certificate_c_as: {
      description: "List short-lived certificate CAs",
      arguments: z.object({
        per_page: z.number().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/access/apps/ca`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "short_lived_certificate_c_as",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} short_lived_certificate_c_as", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    get_an_access_application: {
      description: "Get an Access application",
      arguments: z.object({
        app_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/access/apps/${args.app_id}`,
        );

        const handle = await context.writeResource(
          "an_access_application",
          String(args.app_id),
          result,
        );
        context.logger.info("Fetched an_access_application", {});
        return { dataHandles: [handle] };
      },
    },
    update_an_access_application: {
      description: "Update an Access application",
      arguments: z.object({
        app_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/access/apps/${args.app_id}`,
          args,
        );

        const handle = await context.writeResource(
          "an_access_application",
          String(args.app_id),
          result,
        );
        context.logger.info("Updated an_access_application", {});
        return { dataHandles: [handle] };
      },
    },
    delete_an_access_application: {
      description: "Delete an Access application",
      arguments: z.object({
        app_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/access/apps/${args.app_id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.app_id });
        return { dataHandles: [] };
      },
    },
    get_a_short_lived_certificate_ca: {
      description: "Get a short-lived certificate CA",
      arguments: z.object({
        app_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/access/apps/${args.app_id}/ca`,
        );

        const handle = await context.writeResource(
          "a_short_lived_certificate_ca",
          String(args.app_id),
          result,
        );
        context.logger.info("Fetched a_short_lived_certificate_ca", {});
        return { dataHandles: [handle] };
      },
    },
    create_a_short_lived_certificate_ca: {
      description: "Create a short-lived certificate CA",
      arguments: z.object({
        app_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/access/apps/${args.app_id}/ca`,
        );

        const handle = await context.writeResource(
          "create_a_short_lived_certificate_ca",
          "latest",
          result,
        );
        context.logger.info("Executed create_a_short_lived_certificate_ca", {});
        return { dataHandles: [handle] };
      },
    },
    delete_a_short_lived_certificate_ca: {
      description: "Delete a short-lived certificate CA",
      arguments: z.object({
        app_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/access/apps/${args.app_id}/ca`,
        );

        context.logger.info("Deleted resource {id}", { id: args.app_id });
        return { dataHandles: [] };
      },
    },
    list_access_app_policies: {
      description: "List Access application policies",
      arguments: z.object({
        app_id: z.string().describe("The application ID."),
        per_page: z.number().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const pathParams = new Set(["app_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !pathParams.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/access/apps/${args.app_id}/policies`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "access_app_policies",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} access_app_policies", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_an_access_policy: {
      description: "Create an Access application policy",
      arguments: z.object({
        app_id: z.string().describe("The application ID."),
        precedence: z.unknown().optional(),
        decision: z.unknown(),
        exclude: z.unknown().optional(),
        include: z.unknown(),
        name: z.unknown(),
        require: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/access/apps/${args.app_id}/policies`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "an_access_policy",
          id,
          result,
        );
        context.logger.info("Created an_access_policy {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_an_access_policy: {
      description: "Get an Access application policy",
      arguments: z.object({
        app_id: z.string().describe("The application ID."),
        policy_id: z.string().describe("The policy ID."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/access/apps/${args.app_id}/policies/${args.policy_id}`,
        );

        const handle = await context.writeResource(
          "an_access_policy",
          String(args.policy_id),
          result,
        );
        context.logger.info("Fetched an_access_policy", {});
        return { dataHandles: [handle] };
      },
    },
    update_an_access_policy: {
      description: "Update an Access application policy",
      arguments: z.object({
        app_id: z.string().describe("The application ID."),
        policy_id: z.string().describe("The policy ID."),
        precedence: z.unknown().optional(),
        decision: z.unknown(),
        exclude: z.unknown().optional(),
        include: z.unknown(),
        name: z.unknown(),
        require: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/access/apps/${args.app_id}/policies/${args.policy_id}`,
          args,
        );

        const handle = await context.writeResource(
          "an_access_policy",
          String(args.policy_id),
          result,
        );
        context.logger.info("Updated an_access_policy", {});
        return { dataHandles: [handle] };
      },
    },
    delete_an_access_policy: {
      description: "Delete an Access application policy",
      arguments: z.object({
        app_id: z.string().describe("The application ID."),
        policy_id: z.string().describe("The policy ID."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/access/apps/${args.app_id}/policies/${args.policy_id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.policy_id });
        return { dataHandles: [] };
      },
    },
    update_access_policies_convert_reusable: {
      description: "Convert an Access application policy to a reusable policy",
      arguments: z.object({
        app_id: z.string().describe("The application ID."),
        policy_id: z.string().describe("The policy ID."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/access/apps/${args.app_id}/policies/${args.policy_id}/make_reusable`,
          args,
        );

        const handle = await context.writeResource(
          "access_policies_convert_reusable",
          String(args.policy_id),
          result,
        );
        context.logger.info("Updated access_policies_convert_reusable", {});
        return { dataHandles: [handle] };
      },
    },
    access_applications_revoke_service_tokens: {
      description: "Revoke application tokens",
      arguments: z.object({
        app_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/access/apps/${args.app_id}/revoke_tokens`,
        );

        const handle = await context.writeResource(
          "access_applications_revoke_service_tokens",
          "latest",
          result,
        );
        context.logger.info(
          "Executed access_applications_revoke_service_tokens",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    update_access_application_settings: {
      description: "Update Access application settings",
      arguments: z.object({
        app_id: z.string(),
        allow_iframe: z.unknown().optional(),
        skip_interstitial: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/access/apps/${args.app_id}/settings`,
          args,
        );

        const handle = await context.writeResource(
          "access_application_settings",
          String(args.app_id),
          result,
        );
        context.logger.info("Updated access_application_settings", {});
        return { dataHandles: [handle] };
      },
    },
    get_access_applications_test_access_policies: {
      description: "Test Access policies",
      arguments: z.object({
        app_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/access/apps/${args.app_id}/user_policy_checks`,
        );

        const handle = await context.writeResource(
          "access_applications_test_access_policies",
          String(args.app_id),
          result,
        );
        context.logger.info(
          "Fetched access_applications_test_access_policies",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    list: {
      description: "List authenticator device AAGUIDs",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/access/authenticator_device_aaguids`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("list", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} list", { count: results.length });
        return { dataHandles: [handle] };
      },
    },
    list_mtls_certificates: {
      description: "List mTLS certificates",
      arguments: z.object({
        per_page: z.number().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/access/certificates`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "mtls_certificates",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} mtls_certificates", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_access_mtls_authentication_add_an_mtls_certificate: {
      description: "Add an mTLS certificate",
      arguments: z.object({
        associated_hostnames: z.unknown().optional(),
        certificate: z.string().describe("The certificate content."),
        name: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/access/certificates`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "access_mtls_authentication_add_an_mtls_certificate",
          id,
          result,
        );
        context.logger.info(
          "Created access_mtls_authentication_add_an_mtls_certificate {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    list_mtls_certificates_hostname_settings: {
      description: "List all mTLS hostname settings",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/access/certificates/settings`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "mtls_certificates_hostname_settings",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info(
          "Found {count} mtls_certificates_hostname_settings",
          { count: results.length },
        );
        return { dataHandles: [handle] };
      },
    },
    update_an_mtls_certificate_settings: {
      description: "Update an mTLS certificate's hostname settings",
      arguments: z.object({
        settings: z.array(z.unknown()),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/access/certificates/settings`,
          args,
        );

        const handle = await context.writeResource(
          "an_mtls_certificate_settings",
          "updated",
          result,
        );
        context.logger.info("Updated an_mtls_certificate_settings", {});
        return { dataHandles: [handle] };
      },
    },
    get_an_mtls_certificate: {
      description: "Get an mTLS certificate",
      arguments: z.object({
        certificate_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/access/certificates/${args.certificate_id}`,
        );

        const handle = await context.writeResource(
          "an_mtls_certificate",
          String(args.certificate_id),
          result,
        );
        context.logger.info("Fetched an_mtls_certificate", {});
        return { dataHandles: [handle] };
      },
    },
    update_an_mtls_certificate: {
      description: "Update an mTLS certificate",
      arguments: z.object({
        certificate_id: z.string(),
        associated_hostnames: z.unknown(),
        name: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/access/certificates/${args.certificate_id}`,
          args,
        );

        const handle = await context.writeResource(
          "an_mtls_certificate",
          String(args.certificate_id),
          result,
        );
        context.logger.info("Updated an_mtls_certificate", {});
        return { dataHandles: [handle] };
      },
    },
    delete_an_mtls_certificate: {
      description: "Delete an mTLS certificate",
      arguments: z.object({
        certificate_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/access/certificates/${args.certificate_id}`,
        );

        context.logger.info("Deleted resource {id}", {
          id: args.certificate_id,
        });
        return { dataHandles: [] };
      },
    },
    list_custom_pages: {
      description: "List custom pages",
      arguments: z.object({
        per_page: z.number().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/access/custom_pages`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("custom_pages", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} custom_pages", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_a_custom_page: {
      description: "Create a custom page",
      arguments: z.object({
        app_count: z.unknown().optional(),
        created_at: z.unknown().optional(),
        custom_html: z.string().describe("Custom page HTML."),
        name: z.unknown(),
        type: z.unknown(),
        uid: z.unknown().optional(),
        updated_at: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/access/custom_pages`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource("a_custom_page", id, result);
        context.logger.info("Created a_custom_page {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_a_custom_page: {
      description: "Get a custom page",
      arguments: z.object({
        custom_page_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/access/custom_pages/${args.custom_page_id}`,
        );

        const handle = await context.writeResource(
          "a_custom_page",
          String(args.custom_page_id),
          result,
        );
        context.logger.info("Fetched a_custom_page", {});
        return { dataHandles: [handle] };
      },
    },
    update_a_custom_page: {
      description: "Update a custom page",
      arguments: z.object({
        custom_page_id: z.string(),
        app_count: z.unknown().optional(),
        created_at: z.unknown().optional(),
        custom_html: z.string().describe("Custom page HTML."),
        name: z.unknown(),
        type: z.unknown(),
        uid: z.unknown().optional(),
        updated_at: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/access/custom_pages/${args.custom_page_id}`,
          args,
        );

        const handle = await context.writeResource(
          "a_custom_page",
          String(args.custom_page_id),
          result,
        );
        context.logger.info("Updated a_custom_page", {});
        return { dataHandles: [handle] };
      },
    },
    delete_a_custom_page: {
      description: "Delete a custom page",
      arguments: z.object({
        custom_page_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/access/custom_pages/${args.custom_page_id}`,
        );

        context.logger.info("Deleted resource {id}", {
          id: args.custom_page_id,
        });
        return { dataHandles: [] };
      },
    },
    list_ssh_ca: {
      description: "List SSH Certificate Authorities (CA)",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/access/gateway_ca`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("ssh_ca", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} ssh_ca", { count: results.length });
        return { dataHandles: [handle] };
      },
    },
    access_gateway_ca_add_an_ssh_ca: {
      description: "Add a new SSH Certificate Authority (CA)",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/access/gateway_ca`,
        );

        const handle = await context.writeResource(
          "access_gateway_ca_add_an_ssh_ca",
          "latest",
          result,
        );
        context.logger.info("Executed access_gateway_ca_add_an_ssh_ca", {});
        return { dataHandles: [handle] };
      },
    },
    delete_an_ssh_ca: {
      description: "Delete an SSH Certificate Authority (CA)",
      arguments: z.object({
        certificate_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/access/gateway_ca/${args.certificate_id}`,
        );

        context.logger.info("Deleted resource {id}", {
          id: args.certificate_id,
        });
        return { dataHandles: [] };
      },
    },
    list_access_groups: {
      description: "List Access groups",
      arguments: z.object({
        name: z.string().optional(),
        search: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/access/groups`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("access_groups", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} access_groups", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_an_access_group: {
      description: "Create an Access group",
      arguments: z.object({
        exclude: z.unknown().optional(),
        include: z.unknown(),
        is_default: z.unknown().optional(),
        name: z.unknown(),
        require: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/access/groups`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "an_access_group",
          id,
          result,
        );
        context.logger.info("Created an_access_group {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_an_access_group: {
      description: "Get an Access group",
      arguments: z.object({
        group_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/access/groups/${args.group_id}`,
        );

        const handle = await context.writeResource(
          "an_access_group",
          String(args.group_id),
          result,
        );
        context.logger.info("Fetched an_access_group", {});
        return { dataHandles: [handle] };
      },
    },
    update_an_access_group: {
      description: "Update an Access group",
      arguments: z.object({
        group_id: z.string(),
        exclude: z.unknown().optional(),
        include: z.unknown(),
        is_default: z.unknown().optional(),
        name: z.unknown(),
        require: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/access/groups/${args.group_id}`,
          args,
        );

        const handle = await context.writeResource(
          "an_access_group",
          String(args.group_id),
          result,
        );
        context.logger.info("Updated an_access_group", {});
        return { dataHandles: [handle] };
      },
    },
    delete_an_access_group: {
      description: "Delete an Access group",
      arguments: z.object({
        group_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/access/groups/${args.group_id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.group_id });
        return { dataHandles: [] };
      },
    },
    list_access_identity_providers: {
      description: "List Access identity providers",
      arguments: z.object({
        scim_enabled: z.string().optional(),
        per_page: z.number().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/access/identity_providers`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "access_identity_providers",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} access_identity_providers", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    access_identity_providers_add_an_access_identity_provider: {
      description: "Add an Access identity provider",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/access/identity_providers`,
          args,
        );

        const handle = await context.writeResource(
          "access_identity_providers_add_an_access_identity_provider",
          "latest",
          result,
        );
        context.logger.info(
          "Executed access_identity_providers_add_an_access_identity_provider",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    get_an_access_identity_provider: {
      description: "Get an Access identity provider",
      arguments: z.object({
        identity_provider_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/access/identity_providers/${args.identity_provider_id}`,
        );

        const handle = await context.writeResource(
          "an_access_identity_provider",
          String(args.identity_provider_id),
          result,
        );
        context.logger.info("Fetched an_access_identity_provider", {});
        return { dataHandles: [handle] };
      },
    },
    update_an_access_identity_provider: {
      description: "Update an Access identity provider",
      arguments: z.object({
        identity_provider_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/access/identity_providers/${args.identity_provider_id}`,
          args,
        );

        const handle = await context.writeResource(
          "an_access_identity_provider",
          String(args.identity_provider_id),
          result,
        );
        context.logger.info("Updated an_access_identity_provider", {});
        return { dataHandles: [handle] };
      },
    },
    delete_an_access_identity_provider: {
      description: "Delete an Access identity provider",
      arguments: z.object({
        identity_provider_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/access/identity_providers/${args.identity_provider_id}`,
        );

        context.logger.info("Deleted resource {id}", {
          id: args.identity_provider_id,
        });
        return { dataHandles: [] };
      },
    },
    create_saml_certificate_for_identity_provider: {
      description: "Create SAML encryption certificate for Identity Provider",
      arguments: z.object({
        identity_provider_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/access/identity_providers/${args.identity_provider_id}/saml_certificate`,
        );

        const handle = await context.writeResource(
          "create_saml_certificate_for_identity_provider",
          "latest",
          result,
        );
        context.logger.info(
          "Executed create_saml_certificate_for_identity_provider",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    list_scim_group_resources: {
      description: "List SCIM Group resources",
      arguments: z.object({
        identity_provider_id: z.string(),
        cf_resource_id: z.string().optional(),
        idp_resource_id: z.string().optional(),
        name: z.string().optional(),
        per_page: z.number().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const pathParams = new Set(["identity_provider_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !pathParams.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/access/identity_providers/${args.identity_provider_id}/scim/groups`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "scim_group_resources",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} scim_group_resources", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    list_scim_user_resources: {
      description: "List SCIM User resources",
      arguments: z.object({
        identity_provider_id: z.string(),
        cf_resource_id: z.string().optional(),
        idp_resource_id: z.string().optional(),
        username: z.string().optional(),
        email: z.string().optional(),
        name: z.string().optional(),
        per_page: z.number().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const pathParams = new Set(["identity_provider_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !pathParams.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/access/identity_providers/${args.identity_provider_id}/scim/users`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "scim_user_resources",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} scim_user_resources", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create: {
      description: "Create an IdP federation grant",
      arguments: z.object({
        idp_id: z.string().describe(
          "UID of the identity provider to federate. Must be an existing identity provid...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/access/idp_federation_grants`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource("create", id, result);
        context.logger.info("Created create {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get: {
      description: "Get an IdP federation grant",
      arguments: z.object({
        grant_id: z.string().describe("UID of the IdP federation grant."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/access/idp_federation_grants/${args.grant_id}`,
        );

        const handle = await context.writeResource(
          "get",
          String(args.grant_id),
          result,
        );
        context.logger.info("Fetched get", {});
        return { dataHandles: [handle] };
      },
    },
    delete: {
      description: "Delete an IdP federation grant",
      arguments: z.object({
        grant_id: z.string().describe("UID of the IdP federation grant."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/access/idp_federation_grants/${args.grant_id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.grant_id });
        return { dataHandles: [] };
      },
    },
    get_the_access_key_configuration: {
      description: "Get the Access key configuration",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/access/keys`,
        );

        const handle = await context.writeResource(
          "the_access_key_configuration",
          "latest",
          result,
        );
        context.logger.info("Fetched the_access_key_configuration", {});
        return { dataHandles: [handle] };
      },
    },
    update_the_access_key_configuration: {
      description: "Update the Access key configuration",
      arguments: z.object({
        key_rotation_interval_days: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/access/keys`,
          args,
        );

        const handle = await context.writeResource(
          "the_access_key_configuration",
          "updated",
          result,
        );
        context.logger.info("Updated the_access_key_configuration", {});
        return { dataHandles: [handle] };
      },
    },
    access_key_configuration_rotate_access_keys: {
      description: "Rotate Access keys",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/access/keys/rotate`,
        );

        const handle = await context.writeResource(
          "access_key_configuration_rotate_access_keys",
          "latest",
          result,
        );
        context.logger.info(
          "Executed access_key_configuration_rotate_access_keys",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    get_access_authentication_logs: {
      description: "Get Access authentication logs",
      arguments: z.object({
        limit: z.number().optional().describe(
          "The maximum number of log entries to retrieve.",
        ),
        direction: z.enum(["desc", "asc"]).optional().describe(
          "The chronological sorting order for the logs.",
        ),
        since: z.string().optional().describe(
          "The earliest event timestamp to query.",
        ),
        until: z.string().optional().describe(
          "The latest event timestamp to query.",
        ),
        per_page: z.number().optional(),
        email: z.string().optional().describe(
          "Filter by user email. Match mode is controlled by `emailOp` (preferred) or the legacy `email_exact` flag. - Default (no `emailOp`, `email_exact=false` or unset): substring match — `email=@example.com` returns all events with that domain. - Exact match: set `emailOp=eq` (preferred) or `email_exact=true` — e.g. `email=user@example.com&email_exact=true` returns only that user. - Explicit substring match: set `emailOp=contains` (without `email_exact=true`). When both are set, `email_exact=true` takes precedence and the match is exact. - Exclusion: set `emailOp=neq`. With `email_exact=true` this is an exact-value exclusion; without it, a fuzzy substring exclusion. ",
        ),
        email_exact: z.boolean().optional().describe(
          "When true, `email` is matched exactly instead of substring matching.",
        ),
        user_id: z.string().optional().describe(
          "Deprecated. Accepted for backward compatibility but no longer applied as a filter. Use `email` instead. ",
        ),
        allowedOp: z.enum(["eq", "neq"]).optional().describe(
          "Operator for the `allowed` filter.",
        ),
        country_codeOp: z.enum(["eq", "neq"]).optional().describe(
          "Operator for the `country_code` filter.",
        ),
        app_typeOp: z.enum(["eq", "neq"]).optional().describe(
          "Operator for the `app_type` filter.",
        ),
        app_uidOp: z.enum(["eq", "neq"]).optional().describe(
          "Operator for the `app_uid` filter.",
        ),
        ray_idOp: z.enum(["eq", "neq"]).optional().describe(
          "Operator for the `ray_id` filter.",
        ),
        emailOp: z.enum(["eq", "neq", "contains"]).optional().describe(
          "Operator for the `email` filter. `contains` performs a substring (case-sensitive) match. When `email_exact=true` is also set, `email_exact` takes precedence and `contains` is ignored. ",
        ),
        idpOp: z.enum(["eq", "neq"]).optional().describe(
          "Operator for the `idp` filter.",
        ),
        non_identityOp: z.enum(["eq", "neq"]).optional().describe(
          "Operator for the `non_identity` filter.",
        ),
        user_idOp: z.enum(["eq", "neq"]).optional().describe(
          "Deprecated. Accepted for backward compatibility but no longer applied as a filter (the `user_id` parameter is itself deprecated). ",
        ),
        fields: z.string().optional().describe(
          "Comma-separated list of fields to include in the response. When omitted, all fields are returned. ",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/access/logs/access_requests`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "get_access_authentication_logs",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} get_access_authentication_logs", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    list_access_scim_update_logs: {
      description: "List Access SCIM update logs",
      arguments: z.object({
        limit: z.string().optional(),
        direction: z.string().optional(),
        since: z.string().optional(),
        until: z.string().optional(),
        idp_id: z.string().optional(),
        status: z.string().optional(),
        resource_type: z.string().optional(),
        request_method: z.string().optional(),
        resource_user_email: z.string().optional(),
        resource_group_name: z.string().optional(),
        cf_resource_id: z.string().optional(),
        idp_resource_id: z.string().optional(),
        per_page: z.number().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/access/logs/scim/updates`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "access_scim_update_logs",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} access_scim_update_logs", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    get_your_zero_trust_organization: {
      description: "Get your Zero Trust organization",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/access/organizations`,
        );

        const handle = await context.writeResource(
          "your_zero_trust_organization",
          "latest",
          result,
        );
        context.logger.info("Fetched your_zero_trust_organization", {});
        return { dataHandles: [handle] };
      },
    },
    create_your_zero_trust_organization: {
      description: "Create your Zero Trust organization",
      arguments: z.object({
        allow_authenticate_via_warp: z.unknown().optional(),
        auth_domain: z.unknown(),
        auto_redirect_to_identity: z.unknown().optional(),
        deny_unmatched_requests: z.unknown().optional(),
        deny_unmatched_requests_exempted_zone_names: z.unknown().optional(),
        is_ui_read_only: z.unknown().optional(),
        login_design: z.unknown().optional(),
        mfa_config: z.unknown().optional(),
        mfa_piv_key_requirements: z.unknown().optional(),
        mfa_required_for_all_apps: z.unknown().optional(),
        name: z.unknown(),
        session_duration: z.unknown().optional(),
        ui_read_only_toggle_reason: z.unknown().optional(),
        user_seat_expiration_inactive_time: z.unknown().optional(),
        warp_auth_session_duration: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/access/organizations`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "your_zero_trust_organization",
          id,
          result,
        );
        context.logger.info("Created your_zero_trust_organization {id}", {
          id,
        });
        return { dataHandles: [handle] };
      },
    },
    update_your_zero_trust_organization: {
      description: "Update your Zero Trust organization",
      arguments: z.object({
        allow_authenticate_via_warp: z.unknown().optional(),
        auth_domain: z.unknown().optional(),
        auto_redirect_to_identity: z.unknown().optional(),
        custom_pages: z.unknown().optional(),
        deny_unmatched_requests: z.unknown().optional(),
        deny_unmatched_requests_exempted_zone_names: z.unknown().optional(),
        is_ui_read_only: z.unknown().optional(),
        login_design: z.unknown().optional(),
        mfa_config: z.unknown().optional(),
        mfa_piv_key_requirements: z.unknown().optional(),
        mfa_required_for_all_apps: z.unknown().optional(),
        name: z.unknown().optional(),
        session_duration: z.unknown().optional(),
        ui_read_only_toggle_reason: z.unknown().optional(),
        user_seat_expiration_inactive_time: z.unknown().optional(),
        warp_auth_session_duration: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/access/organizations`,
          args,
        );

        const handle = await context.writeResource(
          "your_zero_trust_organization",
          "updated",
          result,
        );
        context.logger.info("Updated your_zero_trust_organization", {});
        return { dataHandles: [handle] };
      },
    },
    get_your_zero_trust_organization_doh_settings: {
      description: "Get your Zero Trust organization DoH settings",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/access/organizations/doh`,
        );

        const handle = await context.writeResource(
          "your_zero_trust_organization_doh_settings",
          "latest",
          result,
        );
        context.logger.info(
          "Fetched your_zero_trust_organization_doh_settings",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    update_your_zero_trust_organization_doh_settings: {
      description: "Update your Zero Trust organization DoH settings",
      arguments: z.object({
        doh_jwt_duration: z.unknown().optional(),
        service_token_id: z.string().optional().describe(
          "The uuid of the service token you want to use for DoH authentication",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/access/organizations/doh`,
          args,
        );

        const handle = await context.writeResource(
          "your_zero_trust_organization_doh_settings",
          "updated",
          result,
        );
        context.logger.info(
          "Updated your_zero_trust_organization_doh_settings",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    create_zero_trust_organization_revoke_all_access_tokens_for_a_user: {
      description: "Revoke all Access tokens for a user",
      arguments: z.object({
        devices: z.boolean().optional().describe(
          "When set to `true`, all devices associated with the user will be revoked.",
        ),
        email: z.string().describe("The email of the user to revoke."),
        user_uid: z.string().optional().describe(
          "The uuid of the user to revoke.",
        ),
        warp_session_reauth: z.boolean().optional().describe(
          "When set to `true`, the user will be required to re-authenticate to WARP for ...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/access/organizations/revoke_user`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "zero_trust_organization_revoke_all_access_tokens_for_a_user",
          id,
          result,
        );
        context.logger.info(
          "Created zero_trust_organization_revoke_all_access_tokens_for_a_user {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    list_access_reusable_policies: {
      description: "List Access reusable policies",
      arguments: z.object({
        per_page: z.number().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/access/policies`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "access_reusable_policies",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} access_reusable_policies", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_an_access_reusable_policy: {
      description: "Create an Access reusable policy",
      arguments: z.object({
        decision: z.unknown(),
        exclude: z.unknown().optional(),
        include: z.unknown(),
        name: z.unknown(),
        require: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/access/policies`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "an_access_reusable_policy",
          id,
          result,
        );
        context.logger.info("Created an_access_reusable_policy {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_an_access_reusable_policy: {
      description: "Get an Access reusable policy",
      arguments: z.object({
        policy_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/access/policies/${args.policy_id}`,
        );

        const handle = await context.writeResource(
          "an_access_reusable_policy",
          String(args.policy_id),
          result,
        );
        context.logger.info("Fetched an_access_reusable_policy", {});
        return { dataHandles: [handle] };
      },
    },
    update_an_access_reusable_policy: {
      description: "Update an Access reusable policy",
      arguments: z.object({
        policy_id: z.string(),
        decision: z.unknown(),
        exclude: z.unknown().optional(),
        include: z.unknown(),
        name: z.unknown(),
        require: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/access/policies/${args.policy_id}`,
          args,
        );

        const handle = await context.writeResource(
          "an_access_reusable_policy",
          String(args.policy_id),
          result,
        );
        context.logger.info("Updated an_access_reusable_policy", {});
        return { dataHandles: [handle] };
      },
    },
    delete_an_access_reusable_policy: {
      description: "Delete an Access reusable policy",
      arguments: z.object({
        policy_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/access/policies/${args.policy_id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.policy_id });
        return { dataHandles: [] };
      },
    },
    create_access_policy_tests: {
      description: "Start Access policy test",
      arguments: z.object({
        policies: z.array(z.union([z.unknown(), z.string()])).optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/access/policy-tests`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "access_policy_tests",
          id,
          result,
        );
        context.logger.info("Created access_policy_tests {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_an_update: {
      description: "Get the current status of a given Access policy test",
      arguments: z.object({
        policy_test_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/access/policy-tests/${args.policy_test_id}`,
        );

        const handle = await context.writeResource(
          "an_update",
          String(args.policy_test_id),
          result,
        );
        context.logger.info("Fetched an_update", {});
        return { dataHandles: [handle] };
      },
    },
    get_a_user_page: {
      description: "Get an Access policy test users page",
      arguments: z.object({
        policy_test_id: z.string(),
        per_page: z.number().optional(),
        status: z.enum(["success", "fail", "error"]).optional().describe(
          "Filter users by their policy evaluation status.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const pathParams = new Set(["policy_test_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !pathParams.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/access/policy-tests/${args.policy_test_id}/users`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("get_a_user_page", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} get_a_user_page", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    list_certificate_sets: {
      description: "List SAML certificate sets",
      arguments: z.object({
        page: z.number().optional().describe(
          "Page number of paginated results.",
        ),
        per_page: z.number().optional().describe(
          "Maximum number of results per page.",
        ),
        id: z.string().optional().describe(
          "Filter by SAML certificate set UID. Accepts a comma-separated list of UIDs.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/access/saml_certificates`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("certificate_sets", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} certificate_sets", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    get_certificate_set: {
      description: "Get SAML certificate set",
      arguments: z.object({
        saml_cert_set_id: z.string().describe(
          "UID of the SAML certificate set.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/access/saml_certificates/${args.saml_cert_set_id}`,
        );

        const handle = await context.writeResource(
          "certificate_set",
          String(args.saml_cert_set_id),
          result,
        );
        context.logger.info("Fetched certificate_set", {});
        return { dataHandles: [handle] };
      },
    },
    get_pem: {
      description: "Download current certificate in PEM format",
      arguments: z.object({
        saml_cert_set_id: z.string().describe(
          "UID of the SAML certificate set.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/access/saml_certificates/${args.saml_cert_set_id}/pem`,
        );

        const handle = await context.writeResource(
          "pem",
          String(args.saml_cert_set_id),
          result,
        );
        context.logger.info("Fetched pem", {});
        return { dataHandles: [handle] };
      },
    },
    access_saml_certificates_rotate_certificate: {
      description: "Rotate SAML certificate",
      arguments: z.object({
        saml_cert_set_id: z.string().describe(
          "UID of the SAML certificate set to rotate.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/access/saml_certificates/${args.saml_cert_set_id}/rotate`,
        );

        const handle = await context.writeResource(
          "access_saml_certificates_rotate_certificate",
          "latest",
          result,
        );
        context.logger.info(
          "Executed access_saml_certificates_rotate_certificate",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    update_a_user_seat: {
      description: "Update a user seat",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/accounts/${accountId}/access/seats`,
          args,
        );

        const handle = await context.writeResource(
          "a_user_seat",
          "updated",
          result,
        );
        context.logger.info("Updated a_user_seat", {});
        return { dataHandles: [handle] };
      },
    },
    list_service_tokens: {
      description: "List service tokens",
      arguments: z.object({
        name: z.string().optional(),
        search: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/access/service_tokens`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("service_tokens", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} service_tokens", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_a_service_token: {
      description: "Create a service token",
      arguments: z.object({
        client_secret_version: z.unknown().optional(),
        duration: z.unknown().optional(),
        name: z.unknown(),
        previous_client_secret_expires_at: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/access/service_tokens`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "a_service_token",
          id,
          result,
        );
        context.logger.info("Created a_service_token {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_a_service_token: {
      description: "Get a service token",
      arguments: z.object({
        service_token_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/access/service_tokens/${args.service_token_id}`,
        );

        const handle = await context.writeResource(
          "a_service_token",
          String(args.service_token_id),
          result,
        );
        context.logger.info("Fetched a_service_token", {});
        return { dataHandles: [handle] };
      },
    },
    update_a_service_token: {
      description: "Update a service token",
      arguments: z.object({
        service_token_id: z.string(),
        client_secret_version: z.unknown().optional(),
        duration: z.unknown().optional(),
        name: z.unknown().optional(),
        previous_client_secret_expires_at: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/access/service_tokens/${args.service_token_id}`,
          args,
        );

        const handle = await context.writeResource(
          "a_service_token",
          String(args.service_token_id),
          result,
        );
        context.logger.info("Updated a_service_token", {});
        return { dataHandles: [handle] };
      },
    },
    delete_a_service_token: {
      description: "Delete a service token",
      arguments: z.object({
        service_token_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/access/service_tokens/${args.service_token_id}`,
        );

        context.logger.info("Deleted resource {id}", {
          id: args.service_token_id,
        });
        return { dataHandles: [] };
      },
    },
    access_service_tokens_refresh_a_service_token: {
      description: "Refresh a service token",
      arguments: z.object({
        service_token_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/access/service_tokens/${args.service_token_id}/refresh`,
        );

        const handle = await context.writeResource(
          "access_service_tokens_refresh_a_service_token",
          "latest",
          result,
        );
        context.logger.info(
          "Executed access_service_tokens_refresh_a_service_token",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    create_access_service_tokens_rotate_a_service_token: {
      description: "Rotate a service token",
      arguments: z.object({
        service_token_id: z.string(),
        previous_client_secret_expires_at: z.string().optional().describe(
          "The expiration of the previous `client_secret`. If not provided, it defaults ...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/access/service_tokens/${args.service_token_id}/rotate`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "access_service_tokens_rotate_a_service_token",
          id,
          result,
        );
        context.logger.info(
          "Created access_service_tokens_rotate_a_service_token {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    list_tags: {
      description: "List tags",
      arguments: z.object({
        per_page: z.number().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/access/tags`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("tags", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} tags", { count: results.length });
        return { dataHandles: [handle] };
      },
    },
    create_tag: {
      description: "Create a tag",
      arguments: z.object({
        name: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/access/tags`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource("tag", id, result);
        context.logger.info("Created tag {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_a_tag: {
      description: "Get a tag",
      arguments: z.object({
        tag_name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/access/tags/${args.tag_name}`,
        );

        const handle = await context.writeResource(
          "a_tag",
          String(args.tag_name),
          result,
        );
        context.logger.info("Fetched a_tag", {});
        return { dataHandles: [handle] };
      },
    },
    update_a_tag: {
      description: "Update a tag",
      arguments: z.object({
        tag_name: z.string(),
        created_at: z.unknown().optional(),
        name: z.unknown(),
        updated_at: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/access/tags/${args.tag_name}`,
          args,
        );

        const handle = await context.writeResource(
          "a_tag",
          String(args.tag_name),
          result,
        );
        context.logger.info("Updated a_tag", {});
        return { dataHandles: [handle] };
      },
    },
    delete_a_tag: {
      description: "Delete a tag",
      arguments: z.object({
        tag_name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/access/tags/${args.tag_name}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.tag_name });
        return { dataHandles: [] };
      },
    },
    get_users: {
      description: "Get users",
      arguments: z.object({
        name: z.string().optional(),
        email: z.string().optional(),
        search: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/access/users`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("get_users", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} get_users", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_user: {
      description: "Create a user",
      arguments: z.object({
        email: z.unknown(),
        name: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/access/users`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource("user", id, result);
        context.logger.info("Created user {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_user: {
      description: "Get a user",
      arguments: z.object({
        user_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/access/users/${args.user_id}`,
        );

        const handle = await context.writeResource(
          "user",
          String(args.user_id),
          result,
        );
        context.logger.info("Fetched user", {});
        return { dataHandles: [handle] };
      },
    },
    update_user: {
      description: "Update a user",
      arguments: z.object({
        user_id: z.string(),
        email: z.unknown(),
        name: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/access/users/${args.user_id}`,
          args,
        );

        const handle = await context.writeResource(
          "user",
          String(args.user_id),
          result,
        );
        context.logger.info("Updated user", {});
        return { dataHandles: [handle] };
      },
    },
    delete_user: {
      description: "Delete a user",
      arguments: z.object({
        user_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/access/users/${args.user_id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.user_id });
        return { dataHandles: [] };
      },
    },
    get_active_sessions: {
      description: "Get active sessions",
      arguments: z.object({
        user_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const pathParams = new Set(["user_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !pathParams.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/access/users/${args.user_id}/active_sessions`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "get_active_sessions",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} get_active_sessions", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    get_active_session: {
      description: "Get single active session",
      arguments: z.object({
        user_id: z.string(),
        nonce: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/access/users/${args.user_id}/active_sessions/${args.nonce}`,
        );

        const handle = await context.writeResource(
          "active_session",
          String(args.nonce),
          result,
        );
        context.logger.info("Fetched active_session", {});
        return { dataHandles: [handle] };
      },
    },
    get_failed_logins: {
      description: "Get failed logins",
      arguments: z.object({
        user_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const pathParams = new Set(["user_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !pathParams.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/access/users/${args.user_id}/failed_logins`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "get_failed_logins",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} get_failed_logins", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    get_last_seen_identity: {
      description: "Get last seen identity",
      arguments: z.object({
        user_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/access/users/${args.user_id}/last_seen_identity`,
        );

        const handle = await context.writeResource(
          "last_seen_identity",
          String(args.user_id),
          result,
        );
        context.logger.info("Fetched last_seen_identity", {});
        return { dataHandles: [handle] };
      },
    },
    delete_mfa_authenticator: {
      description: "Delete a user's MFA device",
      arguments: z.object({
        user_id: z.string(),
        authenticator_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/access/users/${args.user_id}/mfa_authenticators/${args.authenticator_id}`,
        );

        context.logger.info("Deleted resource {id}", {
          id: args.authenticator_id,
        });
        return { dataHandles: [] };
      },
    },
  },
};
