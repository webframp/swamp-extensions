/**
 * Cloudflare Pages — projects, deployments, domains, build configs
 *
 * Auto-generated by scripts/cloudflare-codegen — do not edit manually.
 *
 * @module
 */
// SPDX-License-Identifier: AGPL-3.0-or-later WITH Swamp-Extension-Exception

import { z } from "npm:zod@4.4.3";
import { cfApi, cfApiPaginated } from "./_lib/api.ts";

// =============================================================================
// Schemas
// =============================================================================

const GlobalArgsSchema = z.object({
  apiToken: z.string().meta({ sensitive: true }).describe(
    "Cloudflare API token",
  ),
  accountId: z.string().describe("Cloudflare account ID"),
});

const GetProjectsItemSchema = z.object({
  build_config: z.unknown().optional(),
  canonical_deployment: z.unknown(),
  created_on: z.string().describe("When the project was created."),
  deployment_configs: z.object({
    preview: z.unknown(),
    production: z.unknown(),
  }).describe("Configs for deployments in a project."),
  domains: z.array(z.string()).optional().describe(
    "A list of associated custom domains for the project.",
  ),
  framework: z.string().describe("Framework the project is using."),
  framework_version: z.string().describe(
    "Version of the framework the project is using.",
  ),
  id: z.string().describe("ID of the project."),
  latest_deployment: z.unknown(),
  name: z.unknown(),
  preview_script_name: z.string().describe("Name of the preview script."),
  production_branch: z.string().describe(
    "Production branch of the project. Used to identify production deployments.",
  ),
  production_script_name: z.string().describe("Name of the production script."),
  source: z.unknown().optional(),
  subdomain: z.string().optional().describe(
    "The Cloudflare subdomain associated with the project.",
  ),
  uses_functions: z.boolean().nullable().describe(
    "Whether the project uses functions.",
  ),
});

const GetProjectsSchema = z.object({
  items: z.array(GetProjectsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateProjectSchema = z.object({
  build_config: z.unknown().optional(),
  canonical_deployment: z.unknown(),
  created_on: z.string().describe("When the project was created."),
  deployment_configs: z.object({
    preview: z.unknown(),
    production: z.unknown(),
  }).describe("Configs for deployments in a project."),
  domains: z.array(z.string()).optional().describe(
    "A list of associated custom domains for the project.",
  ),
  framework: z.string().describe("Framework the project is using."),
  framework_version: z.string().describe(
    "Version of the framework the project is using.",
  ),
  id: z.string().describe("ID of the project."),
  latest_deployment: z.unknown(),
  name: z.unknown(),
  preview_script_name: z.string().describe("Name of the preview script."),
  production_branch: z.string().describe(
    "Production branch of the project. Used to identify production deployments.",
  ),
  production_script_name: z.string().describe("Name of the production script."),
  source: z.unknown().optional(),
  subdomain: z.string().optional().describe(
    "The Cloudflare subdomain associated with the project.",
  ),
  uses_functions: z.boolean().nullable().describe(
    "Whether the project uses functions.",
  ),
});

const GetDeploymentsItemSchema = z.object({
  aliases: z.array(z.string()).nullable().describe(
    "A list of alias URLs pointing to this deployment.",
  ),
  build_config: z.unknown(),
  created_on: z.string().describe("When the deployment was created."),
  deployment_trigger: z.object({
    metadata: z.object({
      branch: z.string(),
      commit_dirty: z.boolean(),
      commit_hash: z.string(),
      commit_message: z.string(),
    }),
    type: z.enum(["github:push", "ad_hoc", "deploy_hook"]),
  }).describe("Info about what caused the deployment."),
  env_vars: z.unknown(),
  environment: z.enum(["preview", "production"]).describe("Type of deploy."),
  id: z.string().describe("Id of the deployment."),
  is_skipped: z.boolean().describe("If the deployment has been skipped."),
  latest_stage: z.unknown(),
  modified_on: z.string().describe("When the deployment was last modified."),
  project_id: z.string().describe("Id of the project."),
  project_name: z.unknown(),
  short_id: z.string().describe("Short Id (8 character) of the deployment."),
  skip_reason: z.enum([
    "commit_message",
    "preview_deployments_disabled",
    "production_deployments_disabled",
    "path_config",
    "branch_config",
    "pages_to_workers_conversion",
  ]).nullable().optional().describe("Why the deployment was skipped."),
  source: z.unknown(),
  stages: z.array(z.unknown()).describe("List of past stages."),
  url: z.string().describe("The live URL to view this deployment."),
  uses_functions: z.boolean().nullable().optional().describe(
    "Whether the deployment uses functions.",
  ),
});

const GetDeploymentsSchema = z.object({
  items: z.array(GetDeploymentsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateDeploymentSchema = z.object({
  aliases: z.array(z.string()).nullable().describe(
    "A list of alias URLs pointing to this deployment.",
  ),
  build_config: z.unknown(),
  created_on: z.string().describe("When the deployment was created."),
  deployment_trigger: z.object({
    metadata: z.object({
      branch: z.string(),
      commit_dirty: z.boolean(),
      commit_hash: z.string(),
      commit_message: z.string(),
    }),
    type: z.enum(["github:push", "ad_hoc", "deploy_hook"]),
  }).describe("Info about what caused the deployment."),
  env_vars: z.unknown(),
  environment: z.enum(["preview", "production"]).describe("Type of deploy."),
  id: z.string().describe("Id of the deployment."),
  is_skipped: z.boolean().describe("If the deployment has been skipped."),
  latest_stage: z.unknown(),
  modified_on: z.string().describe("When the deployment was last modified."),
  project_id: z.string().describe("Id of the project."),
  project_name: z.unknown(),
  short_id: z.string().describe("Short Id (8 character) of the deployment."),
  skip_reason: z.enum([
    "commit_message",
    "preview_deployments_disabled",
    "production_deployments_disabled",
    "path_config",
    "branch_config",
    "pages_to_workers_conversion",
  ]).nullable().optional().describe("Why the deployment was skipped."),
  source: z.unknown(),
  stages: z.array(z.unknown()).describe("List of past stages."),
  url: z.string().describe("The live URL to view this deployment."),
  uses_functions: z.boolean().nullable().optional().describe(
    "Whether the deployment uses functions.",
  ),
});

const GetDeploymentInfoSchema = z.object({
  aliases: z.array(z.string()).nullable().describe(
    "A list of alias URLs pointing to this deployment.",
  ),
  build_config: z.unknown(),
  created_on: z.string().describe("When the deployment was created."),
  deployment_trigger: z.object({
    metadata: z.object({
      branch: z.string(),
      commit_dirty: z.boolean(),
      commit_hash: z.string(),
      commit_message: z.string(),
    }),
    type: z.enum(["github:push", "ad_hoc", "deploy_hook"]),
  }).describe("Info about what caused the deployment."),
  env_vars: z.unknown(),
  environment: z.enum(["preview", "production"]).describe("Type of deploy."),
  id: z.string().describe("Id of the deployment."),
  is_skipped: z.boolean().describe("If the deployment has been skipped."),
  latest_stage: z.unknown(),
  modified_on: z.string().describe("When the deployment was last modified."),
  project_id: z.string().describe("Id of the project."),
  project_name: z.unknown(),
  short_id: z.string().describe("Short Id (8 character) of the deployment."),
  skip_reason: z.enum([
    "commit_message",
    "preview_deployments_disabled",
    "production_deployments_disabled",
    "path_config",
    "branch_config",
    "pages_to_workers_conversion",
  ]).nullable().optional().describe("Why the deployment was skipped."),
  source: z.unknown(),
  stages: z.array(z.unknown()).describe("List of past stages."),
  url: z.string().describe("The live URL to view this deployment."),
  uses_functions: z.boolean().nullable().optional().describe(
    "Whether the deployment uses functions.",
  ),
});

const GetDeploymentLogsSchema = z.object({
  data: z.array(z.object({
    line: z.string(),
    ts: z.string(),
  })),
  includes_container_logs: z.boolean(),
  total: z.number().int(),
});

const PagesDeploymentRetryDeploymentSchema = z.object({
  aliases: z.array(z.string()).nullable().describe(
    "A list of alias URLs pointing to this deployment.",
  ),
  build_config: z.unknown(),
  created_on: z.string().describe("When the deployment was created."),
  deployment_trigger: z.object({
    metadata: z.object({
      branch: z.string(),
      commit_dirty: z.boolean(),
      commit_hash: z.string(),
      commit_message: z.string(),
    }),
    type: z.enum(["github:push", "ad_hoc", "deploy_hook"]),
  }).describe("Info about what caused the deployment."),
  env_vars: z.unknown(),
  environment: z.enum(["preview", "production"]).describe("Type of deploy."),
  id: z.string().describe("Id of the deployment."),
  is_skipped: z.boolean().describe("If the deployment has been skipped."),
  latest_stage: z.unknown(),
  modified_on: z.string().describe("When the deployment was last modified."),
  project_id: z.string().describe("Id of the project."),
  project_name: z.unknown(),
  short_id: z.string().describe("Short Id (8 character) of the deployment."),
  skip_reason: z.enum([
    "commit_message",
    "preview_deployments_disabled",
    "production_deployments_disabled",
    "path_config",
    "branch_config",
    "pages_to_workers_conversion",
  ]).nullable().optional().describe("Why the deployment was skipped."),
  source: z.unknown(),
  stages: z.array(z.unknown()).describe("List of past stages."),
  url: z.string().describe("The live URL to view this deployment."),
  uses_functions: z.boolean().nullable().optional().describe(
    "Whether the deployment uses functions.",
  ),
});

const PagesDeploymentRollbackDeploymentSchema = z.object({
  aliases: z.array(z.string()).nullable().describe(
    "A list of alias URLs pointing to this deployment.",
  ),
  build_config: z.unknown(),
  created_on: z.string().describe("When the deployment was created."),
  deployment_trigger: z.object({
    metadata: z.object({
      branch: z.string(),
      commit_dirty: z.boolean(),
      commit_hash: z.string(),
      commit_message: z.string(),
    }),
    type: z.enum(["github:push", "ad_hoc", "deploy_hook"]),
  }).describe("Info about what caused the deployment."),
  env_vars: z.unknown(),
  environment: z.enum(["preview", "production"]).describe("Type of deploy."),
  id: z.string().describe("Id of the deployment."),
  is_skipped: z.boolean().describe("If the deployment has been skipped."),
  latest_stage: z.unknown(),
  modified_on: z.string().describe("When the deployment was last modified."),
  project_id: z.string().describe("Id of the project."),
  project_name: z.unknown(),
  short_id: z.string().describe("Short Id (8 character) of the deployment."),
  skip_reason: z.enum([
    "commit_message",
    "preview_deployments_disabled",
    "production_deployments_disabled",
    "path_config",
    "branch_config",
    "pages_to_workers_conversion",
  ]).nullable().optional().describe("Why the deployment was skipped."),
  source: z.unknown(),
  stages: z.array(z.unknown()).describe("List of past stages."),
  url: z.string().describe("The live URL to view this deployment."),
  uses_functions: z.boolean().nullable().optional().describe(
    "Whether the deployment uses functions.",
  ),
});

const CreateTailSchema = z.object({
  id: z.string().describe("Identifier of the tail session."),
  url: z.string().optional().describe(
    "Optional WebSocket URL to connect to for receiving tail events, when returned by the tail service.",
  ),
});

const GetDomainsItemSchema = z.object({
  certificate_authority: z.enum(["google", "lets_encrypt"]),
  created_on: z.string(),
  domain_id: z.string(),
  id: z.string(),
  name: z.unknown(),
  status: z.enum([
    "initializing",
    "pending",
    "active",
    "deactivated",
    "blocked",
    "error",
  ]),
  validation_data: z.object({
    error_message: z.string().optional(),
    method: z.enum(["http", "txt"]),
    status: z.enum([
      "initializing",
      "pending",
      "active",
      "deactivated",
      "error",
    ]),
    txt_name: z.string().optional(),
    txt_value: z.string().optional(),
  }),
  verification_data: z.object({
    error_message: z.string().optional(),
    status: z.enum(["pending", "active", "deactivated", "blocked", "error"]),
  }),
  zone_tag: z.string(),
});

const GetDomainsSchema = z.object({
  items: z.array(GetDomainsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreatePagesDomainsAddDomainSchema = z.object({
  certificate_authority: z.enum(["google", "lets_encrypt"]),
  created_on: z.string(),
  domain_id: z.string(),
  id: z.string(),
  name: z.unknown(),
  status: z.enum([
    "initializing",
    "pending",
    "active",
    "deactivated",
    "blocked",
    "error",
  ]),
  validation_data: z.object({
    error_message: z.string().optional(),
    method: z.enum(["http", "txt"]),
    status: z.enum([
      "initializing",
      "pending",
      "active",
      "deactivated",
      "error",
    ]),
    txt_name: z.string().optional(),
    txt_value: z.string().optional(),
  }),
  verification_data: z.object({
    error_message: z.string().optional(),
    status: z.enum(["pending", "active", "deactivated", "blocked", "error"]),
  }),
  zone_tag: z.string(),
});

const GetDomainSchema = z.object({
  certificate_authority: z.enum(["google", "lets_encrypt"]),
  created_on: z.string(),
  domain_id: z.string(),
  id: z.string(),
  name: z.unknown(),
  status: z.enum([
    "initializing",
    "pending",
    "active",
    "deactivated",
    "blocked",
    "error",
  ]),
  validation_data: z.object({
    error_message: z.string().optional(),
    method: z.enum(["http", "txt"]),
    status: z.enum([
      "initializing",
      "pending",
      "active",
      "deactivated",
      "error",
    ]),
    txt_name: z.string().optional(),
    txt_value: z.string().optional(),
  }),
  verification_data: z.object({
    error_message: z.string().optional(),
    status: z.enum(["pending", "active", "deactivated", "blocked", "error"]),
  }),
  zone_tag: z.string(),
});

const PatchDomainSchema = z.object({
  certificate_authority: z.enum(["google", "lets_encrypt"]),
  created_on: z.string(),
  domain_id: z.string(),
  id: z.string(),
  name: z.unknown(),
  status: z.enum([
    "initializing",
    "pending",
    "active",
    "deactivated",
    "blocked",
    "error",
  ]),
  validation_data: z.object({
    error_message: z.string().optional(),
    method: z.enum(["http", "txt"]),
    status: z.enum([
      "initializing",
      "pending",
      "active",
      "deactivated",
      "error",
    ]),
    txt_name: z.string().optional(),
    txt_value: z.string().optional(),
  }),
  verification_data: z.object({
    error_message: z.string().optional(),
    status: z.enum(["pending", "active", "deactivated", "blocked", "error"]),
  }),
  zone_tag: z.string(),
});

const PagesPurgeBuildCacheSchema = z.object({}).nullable();

const CreatePagesProjectConnectProjectSourceSchema = z.object({
  build_config: z.unknown().optional(),
  canonical_deployment: z.unknown(),
  created_on: z.string().describe("When the project was created."),
  deployment_configs: z.object({
    preview: z.unknown(),
    production: z.unknown(),
  }).describe("Configs for deployments in a project."),
  domains: z.array(z.string()).optional().describe(
    "A list of associated custom domains for the project.",
  ),
  framework: z.string().describe("Framework the project is using."),
  framework_version: z.string().describe(
    "Version of the framework the project is using.",
  ),
  id: z.string().describe("ID of the project."),
  latest_deployment: z.unknown(),
  name: z.unknown(),
  preview_script_name: z.string().describe("Name of the preview script."),
  production_branch: z.string().describe(
    "Production branch of the project. Used to identify production deployments.",
  ),
  production_script_name: z.string().describe("Name of the production script."),
  source: z.unknown().optional(),
  subdomain: z.string().optional().describe(
    "The Cloudflare subdomain associated with the project.",
  ),
  uses_functions: z.boolean().nullable().describe(
    "Whether the project uses functions.",
  ),
});

const GetUploadTokenSchema = z.object({
  jwt: z.string().describe(
    "Short-lived JWT used to authenticate Pages Direct Upload asset operations.",
  ),
});

// =============================================================================
// Model Definition
// =============================================================================

/** Cloudflare Pages — projects, deployments, domains, build configs */
export const model = {
  type: "@webframp/cloudflare/pages",
  version: "2026.07.19.1",
  globalArguments: GlobalArgsSchema,

  upgrades: [],

  resources: {
    "get_projects": {
      description: "Get projects",
      schema: GetProjectsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "project": {
      description: "Create project",
      schema: CreateProjectSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "get_deployments": {
      description: "Get deployments",
      schema: GetDeploymentsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "deployment": {
      description: "Create deployment",
      schema: CreateDeploymentSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "deployment_info": {
      description: "Get deployment info",
      schema: GetDeploymentInfoSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "deployment_logs": {
      description: "Get deployment logs",
      schema: GetDeploymentLogsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "pages_deployment_retry_deployment": {
      description: "Retry deployment",
      schema: PagesDeploymentRetryDeploymentSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "pages_deployment_rollback_deployment": {
      description: "Rollback deployment",
      schema: PagesDeploymentRollbackDeploymentSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "tail": {
      description: "Create deployment tail",
      schema: CreateTailSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "get_domains": {
      description: "Get domains",
      schema: GetDomainsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "pages_domains_add_domain": {
      description: "Add domain",
      schema: CreatePagesDomainsAddDomainSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "domain": {
      description: "Get domain",
      schema: GetDomainSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "patch_domain": {
      description: "Patch domain",
      schema: PatchDomainSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "pages_purge_build_cache": {
      description: "Purge build cache",
      schema: PagesPurgeBuildCacheSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "pages_project_connect_project_source": {
      description: "Connect project source",
      schema: CreatePagesProjectConnectProjectSourceSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "upload_token": {
      description: "Get upload token",
      schema: GetUploadTokenSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
  },

  methods: {
    get_projects: {
      description: "Get projects",
      arguments: z.object({
        page: z.number().optional(),
        per_page: z.number().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/pages/projects`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("get_projects", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} get_projects", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_project: {
      description: "Create project",
      arguments: z.object({
        build_config: z.object({
          build_caching: z.boolean().optional(),
          build_command: z.string().optional(),
          destination_dir: z.string().optional(),
          root_dir: z.string().optional(),
          web_analytics_tag: z.string().nullable().optional(),
          web_analytics_token: z.string().nullable().optional(),
        }).optional().describe("Configs for the project build process."),
        deployment_configs: z.object({
          preview: z.unknown().optional(),
          production: z.unknown().optional(),
        }).optional().describe("Configs for deployments in a project."),
        name: z.string().describe("Name of the project."),
        production_branch: z.string().describe(
          "Production branch of the project. Used to identify production deployments.",
        ),
        source: z.object({
          config: z.object({
            deployments_enabled: z.boolean().optional(),
            owner: z.string().optional(),
            owner_id: z.string().optional(),
            path_excludes: z.array(z.string()).optional(),
            path_includes: z.array(z.string()).optional(),
            pr_comments_enabled: z.boolean().optional(),
            preview_branch_excludes: z.array(z.string()).optional(),
            preview_branch_includes: z.array(z.string()).optional(),
            preview_deployment_setting: z.enum(["all", "none", "custom"])
              .optional(),
            production_branch: z.string().optional(),
            production_deployments_enabled: z.boolean().optional(),
            repo_id: z.string().optional(),
            repo_name: z.string().optional(),
          }),
          type: z.enum(["github", "gitlab"]),
        }).optional().describe("Configs for the project source control."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/pages/projects`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource("project", id, result);
        context.logger.info("Created project {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_project: {
      description: "Get project",
      arguments: z.object({
        project_name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/pages/projects/${args.project_name}`,
        );

        const handle = await context.writeResource(
          "project",
          String(args.project_name),
          result,
        );
        context.logger.info("Fetched project", {});
        return { dataHandles: [handle] };
      },
    },
    update_project: {
      description: "Update project",
      arguments: z.object({
        project_name: z.string(),
        build_config: z.object({
          build_caching: z.boolean().optional(),
          build_command: z.string().optional(),
          destination_dir: z.string().optional(),
          root_dir: z.string().optional(),
          web_analytics_tag: z.string().nullable().optional(),
          web_analytics_token: z.string().nullable().optional(),
        }).optional().describe("Configs for the project build process."),
        deployment_configs: z.object({
          preview: z.unknown().optional(),
          production: z.unknown().optional(),
        }).optional().describe("Configs for deployments in a project."),
        name: z.string().optional().describe("Name of the project."),
        production_branch: z.string().optional().describe(
          "Production branch of the project. Used to identify production deployments.",
        ),
        source: z.object({
          config: z.object({
            deployments_enabled: z.boolean().optional(),
            owner: z.string().optional(),
            owner_id: z.string().optional(),
            path_excludes: z.array(z.string()).optional(),
            path_includes: z.array(z.string()).optional(),
            pr_comments_enabled: z.boolean().optional(),
            preview_branch_excludes: z.array(z.string()).optional(),
            preview_branch_includes: z.array(z.string()).optional(),
            preview_deployment_setting: z.enum(["all", "none", "custom"])
              .optional(),
            production_branch: z.string().optional(),
            production_deployments_enabled: z.boolean().optional(),
            repo_id: z.string().optional(),
            repo_name: z.string().optional(),
          }),
          type: z.enum(["github", "gitlab"]),
        }).optional().describe("Configs for the project source control."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/accounts/${accountId}/pages/projects/${args.project_name}`,
          args,
        );

        const handle = await context.writeResource(
          "project",
          String(args.project_name),
          result,
        );
        context.logger.info("Updated project", {});
        return { dataHandles: [handle] };
      },
    },
    delete_project: {
      description: "Delete project",
      arguments: z.object({
        project_name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/pages/projects/${args.project_name}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.project_name });
        return { dataHandles: [] };
      },
    },
    get_deployments: {
      description: "Get deployments",
      arguments: z.object({
        project_name: z.string(),
        env: z.enum(["production", "preview"]).optional(),
        page: z.number().optional(),
        per_page: z.number().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const pathParams = new Set(["project_name"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !pathParams.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/pages/projects/${args.project_name}/deployments`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("get_deployments", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} get_deployments", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_deployment: {
      description: "Create deployment",
      arguments: z.object({
        project_name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/pages/projects/${args.project_name}/deployments`,
        );

        const handle = await context.writeResource(
          "create_deployment",
          "latest",
          result,
        );
        context.logger.info("Executed create_deployment", {});
        return { dataHandles: [handle] };
      },
    },
    get_deployment_info: {
      description: "Get deployment info",
      arguments: z.object({
        deployment_id: z.string(),
        project_name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/pages/projects/${args.project_name}/deployments/${args.deployment_id}`,
        );

        const handle = await context.writeResource(
          "deployment_info",
          String(args.project_name),
          result,
        );
        context.logger.info("Fetched deployment_info", {});
        return { dataHandles: [handle] };
      },
    },
    delete_deployment: {
      description: "Delete deployment",
      arguments: z.object({
        deployment_id: z.string(),
        project_name: z.string(),
        force: z.boolean().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/pages/projects/${args.project_name}/deployments/${args.deployment_id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.project_name });
        return { dataHandles: [] };
      },
    },
    get_deployment_logs: {
      description: "Get deployment logs",
      arguments: z.object({
        deployment_id: z.string(),
        project_name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/pages/projects/${args.project_name}/deployments/${args.deployment_id}/history/logs`,
        );

        const handle = await context.writeResource(
          "deployment_logs",
          String(args.project_name),
          result,
        );
        context.logger.info("Fetched deployment_logs", {});
        return { dataHandles: [handle] };
      },
    },
    pages_deployment_retry_deployment: {
      description: "Retry deployment",
      arguments: z.object({
        deployment_id: z.string(),
        project_name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/pages/projects/${args.project_name}/deployments/${args.deployment_id}/retry`,
        );

        const handle = await context.writeResource(
          "pages_deployment_retry_deployment",
          "latest",
          result,
        );
        context.logger.info("Executed pages_deployment_retry_deployment", {});
        return { dataHandles: [handle] };
      },
    },
    pages_deployment_rollback_deployment: {
      description: "Rollback deployment",
      arguments: z.object({
        deployment_id: z.string(),
        project_name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/pages/projects/${args.project_name}/deployments/${args.deployment_id}/rollback`,
        );

        const handle = await context.writeResource(
          "pages_deployment_rollback_deployment",
          "latest",
          result,
        );
        context.logger.info(
          "Executed pages_deployment_rollback_deployment",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    create_tail: {
      description: "Create deployment tail",
      arguments: z.object({
        deployment_id: z.string(),
        project_name: z.string(),
        filters: z.array(z.record(z.string(), z.unknown())).optional().describe(
          "Filters to apply to the tail session.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/pages/projects/${args.project_name}/deployments/${args.deployment_id}/tails`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource("tail", id, result);
        context.logger.info("Created tail {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    delete_tail: {
      description: "Delete deployment tail",
      arguments: z.object({
        tail_id: z.string(),
        deployment_id: z.string(),
        project_name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/pages/projects/${args.project_name}/deployments/${args.deployment_id}/tails/${args.tail_id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.project_name });
        return { dataHandles: [] };
      },
    },
    get_domains: {
      description: "Get domains",
      arguments: z.object({
        project_name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const pathParams = new Set(["project_name"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !pathParams.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/pages/projects/${args.project_name}/domains`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("get_domains", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} get_domains", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_pages_domains_add_domain: {
      description: "Add domain",
      arguments: z.object({
        project_name: z.string(),
        name: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/pages/projects/${args.project_name}/domains`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "pages_domains_add_domain",
          id,
          result,
        );
        context.logger.info("Created pages_domains_add_domain {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_domain: {
      description: "Get domain",
      arguments: z.object({
        domain_name: z.string(),
        project_name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/pages/projects/${args.project_name}/domains/${args.domain_name}`,
        );

        const handle = await context.writeResource(
          "domain",
          String(args.project_name),
          result,
        );
        context.logger.info("Fetched domain", {});
        return { dataHandles: [handle] };
      },
    },
    patch_domain: {
      description: "Patch domain",
      arguments: z.object({
        domain_name: z.string(),
        project_name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/accounts/${accountId}/pages/projects/${args.project_name}/domains/${args.domain_name}`,
          args,
        );

        const handle = await context.writeResource(
          "patch_domain",
          String(args.project_name),
          result,
        );
        context.logger.info("Updated patch_domain", {});
        return { dataHandles: [handle] };
      },
    },
    delete_domain: {
      description: "Delete domain",
      arguments: z.object({
        domain_name: z.string(),
        project_name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/pages/projects/${args.project_name}/domains/${args.domain_name}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.project_name });
        return { dataHandles: [] };
      },
    },
    pages_purge_build_cache: {
      description: "Purge build cache",
      arguments: z.object({
        project_name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/pages/projects/${args.project_name}/purge_build_cache`,
        );

        const handle = await context.writeResource(
          "pages_purge_build_cache",
          "latest",
          result,
        );
        context.logger.info("Executed pages_purge_build_cache", {});
        return { dataHandles: [handle] };
      },
    },
    create_pages_project_connect_project_source: {
      description: "Connect project source",
      arguments: z.object({
        project_name: z.string(),
        config: z.object({
          deployments_enabled: z.boolean(),
          owner: z.string(),
          owner_id: z.string(),
          path_excludes: z.array(z.string()),
          path_includes: z.array(z.string()),
          pr_comments_enabled: z.boolean(),
          preview_branch_excludes: z.array(z.string()),
          preview_branch_includes: z.array(z.string()),
          preview_deployment_setting: z.enum(["all", "none", "custom"]),
          production_branch: z.string(),
          production_deployments_enabled: z.boolean(),
          repo_id: z.string(),
          repo_name: z.string(),
        }),
        type: z.enum(["github", "gitlab"]).describe(
          "The source control management provider.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/pages/projects/${args.project_name}/source`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "pages_project_connect_project_source",
          id,
          result,
        );
        context.logger.info(
          "Created pages_project_connect_project_source {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    delete_pages_project_disconnect_project_source: {
      description: "Disconnect project source",
      arguments: z.object({
        project_name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/pages/projects/${args.project_name}/source`,
        );

        context.logger.info("Deleted resource {id}", { id: args.project_name });
        return { dataHandles: [] };
      },
    },
    get_upload_token: {
      description: "Get upload token",
      arguments: z.object({
        project_name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/pages/projects/${args.project_name}/upload-token`,
        );

        const handle = await context.writeResource(
          "upload_token",
          String(args.project_name),
          result,
        );
        context.logger.info("Fetched upload_token", {});
        return { dataHandles: [handle] };
      },
    },
  },
};
