// Cloudflare Queues — queue management, consumers, message operations — Tests
// Auto-generated by scripts/cloudflare-codegen — do not edit manually.
// SPDX-License-Identifier: Apache-2.0

import { assertEquals, assertExists } from "jsr:@std/assert@1.0.19";
import { createModelTestContext } from "@systeminit/swamp-testing";
import { model } from "./queues.ts";

// ---------------------------------------------------------------------------
// Model Export Structure Tests
// ---------------------------------------------------------------------------

Deno.test("queues model: has correct type", () => {
  assertEquals(model.type, "@webframp/cloudflare/queues");
});

Deno.test("queues model: has valid version format", () => {
  const versionPattern = /^\d{4}\.\d{2}\.\d{2}\.\d+$/;
  assertEquals(versionPattern.test(model.version), true);
});

Deno.test("queues model: has globalArguments with apiToken", () => {
  assertExists(model.globalArguments);
  const shape = model.globalArguments.shape;
  assertExists(shape.apiToken);
  assertExists(shape.accountId);
});

Deno.test("queues model: has expected methods", () => {
  assertExists(model.methods);
  assertExists(model.methods.list);
  assertExists(model.methods.create);
  assertExists(model.methods.get);
  assertExists(model.methods.update);
  assertExists(model.methods.update_partial);
  assertExists(model.methods.delete);
  assertExists(model.methods.list_consumers);
  assertExists(model.methods.create_consumer);
  assertExists(model.methods.get_consumer);
  assertExists(model.methods.update_consumer);
  assertExists(model.methods.delete_consumer);
  assertExists(model.methods.create_queues_push_message);
  assertExists(model.methods.create_queues_ack_messages);
  assertExists(model.methods.create_queues_push_messages);
  assertExists(model.methods.create_queues_preview_messages);
  assertExists(model.methods.create_queues_ack_preview_messages);
  assertExists(model.methods.create_queues_pull_messages);
  assertExists(model.methods.get_metrics);
  assertExists(model.methods.create_queues_purge);
});

Deno.test("queues model: has expected resources", () => {
  assertExists(model.resources);
  assertExists(model.resources["list"]);
  assertExists(model.resources["create"]);
  assertExists(model.resources["get"]);
  assertExists(model.resources["update"]);
  assertExists(model.resources["partial"]);
  assertExists(model.resources["consumers"]);
  assertExists(model.resources["consumer"]);
  assertExists(model.resources["queues_push_message"]);
  assertExists(model.resources["queues_ack_messages"]);
  assertExists(model.resources["queues_push_messages"]);
  assertExists(model.resources["queues_preview_messages"]);
  assertExists(model.resources["queues_ack_preview_messages"]);
  assertExists(model.resources["queues_pull_messages"]);
  assertExists(model.resources["metrics"]);
  assertExists(model.resources["queues_purge"]);
});

// ---------------------------------------------------------------------------
// Mock Cloudflare API Server
// ---------------------------------------------------------------------------

function startMockCfServer(
  responses: Record<string, { result: unknown; isCollection?: boolean }>,
): { url: string; server: Deno.HttpServer } {
  const server = Deno.serve({ port: 0, onListen() {} }, (req) => {
    const url = new URL(req.url);
    const path = url.pathname;

    for (
      const [pattern, { result, isCollection }] of Object.entries(responses)
    ) {
      if (path.includes(pattern)) {
        return Response.json({
          success: true,
          errors: [],
          messages: [],
          result,
          result_info: isCollection
            ? {
              page: 1,
              per_page: 50,
              total_count: Array.isArray(result) ? result.length : 1,
              total_pages: 1,
            }
            : undefined,
        });
      }
    }

    return Response.json({
      success: false,
      errors: [{ code: 404, message: "Not found" }],
      messages: [],
      result: null,
    });
  });

  const addr = server.addr as Deno.NetAddr;
  return { url: `http://localhost:${addr.port}`, server };
}

function installFetchMock(mockUrl: string): () => void {
  const originalFetch = globalThis.fetch;
  globalThis.fetch = (input, init) => {
    const reqUrl = typeof input === "string" ? input : input.toString();
    const newUrl = reqUrl.replace(
      "https://api.cloudflare.com/client/v4",
      mockUrl,
    );
    return originalFetch(newUrl, init);
  };
  return () => {
    globalThis.fetch = originalFetch;
  };
}

Deno.test({
  name: "queues model: list fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = [{
      "consumers": [null],
      "consumers_total_count": 1,
      "created_on": "test-value",
      "modified_on": "test-value",
      "producers": [null],
      "producers_total_count": 1,
      "queue_id": "test-value",
      "queue_name": null,
      "settings": null,
    }];
    const { url, server } = startMockCfServer({
      "/queues": { result: mockData, isCollection: true },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: {
          id: "test-id",
          name: "test-queues",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).list.execute({}, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "queues model: create creates and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "id": "new-123",
      "consumers": [null],
      "consumers_total_count": 1,
      "created_on": "test-value",
      "modified_on": "test-value",
      "producers": [null],
      "producers_total_count": 1,
      "queue_id": "test-value",
      "queue_name": null,
      "settings": null,
    };
    const { url, server } = startMockCfServer({
      "/queues": { result: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: {
          id: "test-id",
          name: "test-queues",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).create.execute({
        "consumers": [null],
        "consumers_total_count": 1,
        "created_on": "test-value",
        "modified_on": "test-value",
        "producers": [null],
        "producers_total_count": 1,
        "queue_id": "test-value",
        "queue_name": null,
        "settings": null,
      }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "queues model: get fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "consumers": [null],
      "consumers_total_count": 1,
      "created_on": "test-value",
      "modified_on": "test-value",
      "producers": [null],
      "producers_total_count": 1,
      "queue_id": "test-value",
      "queue_name": null,
      "settings": null,
    };
    const { url, server } = startMockCfServer({
      "/queues/test-id-123": { result: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: {
          id: "test-id",
          name: "test-queues",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).get.execute({ "queue_id": "test-id-123" }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "queues model: update executes and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "consumers": [null],
      "consumers_total_count": 1,
      "created_on": "test-value",
      "modified_on": "test-value",
      "producers": [null],
      "producers_total_count": 1,
      "queue_id": "test-value",
      "queue_name": null,
      "settings": null,
    };
    const { url, server } = startMockCfServer({
      "/queues/test-id-123": { result: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: {
          id: "test-id",
          name: "test-queues",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).update.execute({
        "queue_id": "test-value",
        "consumers": [null],
        "consumers_total_count": 1,
        "created_on": "test-value",
        "modified_on": "test-value",
        "producers": [null],
        "producers_total_count": 1,
        "queue_name": null,
        "settings": null,
      }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "queues model: delete executes successfully",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockCfServer({
      "/queues/test-id-123": { result: { id: "test-id-123" } },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: {
          id: "test-id",
          name: "test-queues",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).delete.execute({ "queue_id": "test-id-123" }, context);
      assertEquals(result.dataHandles.length, 0);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "queues model: create_consumer executes and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {};
    const { url, server } = startMockCfServer({
      "/queues/test-id-123/consumers": { result: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: {
          id: "test-id",
          name: "test-queues",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).create_consumer.execute({ "queue_id": "test-id-123" }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});
