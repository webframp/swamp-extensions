/**
 * Cloudflare Waiting Room — traffic queuing, rules, events, analytics
 *
 * Auto-generated by scripts/cloudflare-codegen — do not edit manually.
 *
 * @module
 */
// SPDX-License-Identifier: AGPL-3.0-or-later WITH Swamp-Extension-Exception

import { z } from "npm:zod@4.4.3";
import { cfApi, cfApiPaginated } from "./_lib/api.ts";

// =============================================================================
// Schemas
// =============================================================================

const GlobalArgsSchema = z.object({
  apiToken: z.string().meta({ sensitive: true }).describe(
    "Cloudflare API token",
  ),
  zoneId: z.string().describe("Cloudflare zone ID"),
});

const WaitingRoomsItemSchema = z.object({
  additional_routes: z.unknown().optional(),
  cookie_attributes: z.unknown().optional(),
  cookie_suffix: z.unknown().optional(),
  created_on: z.unknown().optional(),
  custom_page_html: z.unknown().optional(),
  default_template_language: z.unknown().optional(),
  description: z.unknown().optional(),
  disable_session_renewal: z.unknown().optional(),
  enabled_origin_commands: z.unknown().optional(),
  host: z.unknown().optional(),
  id: z.unknown().optional(),
  json_response_enabled: z.unknown().optional(),
  modified_on: z.unknown().optional(),
  name: z.unknown().optional(),
  new_users_per_minute: z.unknown().optional(),
  next_event_prequeue_start_time: z.unknown().optional(),
  next_event_start_time: z.unknown().optional(),
  path: z.unknown().optional(),
  queue_all: z.unknown().optional(),
  queueing_method: z.unknown().optional(),
  queueing_status_code: z.unknown().optional(),
  session_duration: z.unknown().optional(),
  suspended: z.unknown().optional(),
  total_active_users: z.unknown().optional(),
  turnstile_action: z.unknown().optional(),
  turnstile_mode: z.unknown().optional(),
});

const ListWaitingRoomsSchema = z.object({
  items: z.array(WaitingRoomsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateWaitingRoomSchema = z.object({
  additional_routes: z.unknown().optional(),
  cookie_attributes: z.unknown().optional(),
  cookie_suffix: z.unknown().optional(),
  created_on: z.unknown().optional(),
  custom_page_html: z.unknown().optional(),
  default_template_language: z.unknown().optional(),
  description: z.unknown().optional(),
  disable_session_renewal: z.unknown().optional(),
  enabled_origin_commands: z.unknown().optional(),
  host: z.unknown().optional(),
  id: z.unknown().optional(),
  json_response_enabled: z.unknown().optional(),
  modified_on: z.unknown().optional(),
  name: z.unknown().optional(),
  new_users_per_minute: z.unknown().optional(),
  next_event_prequeue_start_time: z.unknown().optional(),
  next_event_start_time: z.unknown().optional(),
  path: z.unknown().optional(),
  queue_all: z.unknown().optional(),
  queueing_method: z.unknown().optional(),
  queueing_status_code: z.unknown().optional(),
  session_duration: z.unknown().optional(),
  suspended: z.unknown().optional(),
  total_active_users: z.unknown().optional(),
  turnstile_action: z.unknown().optional(),
  turnstile_mode: z.unknown().optional(),
});

const CreateACustomWaitingRoomPagePreviewSchema = z.object({
  preview_url: z.unknown().optional(),
});

const GetZoneSettingsSchema = z.object({
  search_engine_crawler_bypass: z.unknown(),
});

const PatchZoneSettingsSchema = z.object({
  search_engine_crawler_bypass: z.unknown(),
});

const GetWaitingRoomWaitingRoomDetailsSchema = z.object({
  additional_routes: z.unknown().optional(),
  cookie_attributes: z.unknown().optional(),
  cookie_suffix: z.unknown().optional(),
  created_on: z.unknown().optional(),
  custom_page_html: z.unknown().optional(),
  default_template_language: z.unknown().optional(),
  description: z.unknown().optional(),
  disable_session_renewal: z.unknown().optional(),
  enabled_origin_commands: z.unknown().optional(),
  host: z.unknown().optional(),
  id: z.unknown().optional(),
  json_response_enabled: z.unknown().optional(),
  modified_on: z.unknown().optional(),
  name: z.unknown().optional(),
  new_users_per_minute: z.unknown().optional(),
  next_event_prequeue_start_time: z.unknown().optional(),
  next_event_start_time: z.unknown().optional(),
  path: z.unknown().optional(),
  queue_all: z.unknown().optional(),
  queueing_method: z.unknown().optional(),
  queueing_status_code: z.unknown().optional(),
  session_duration: z.unknown().optional(),
  suspended: z.unknown().optional(),
  total_active_users: z.unknown().optional(),
  turnstile_action: z.unknown().optional(),
  turnstile_mode: z.unknown().optional(),
});

const PatchWaitingRoomSchema = z.object({
  additional_routes: z.unknown().optional(),
  cookie_attributes: z.unknown().optional(),
  cookie_suffix: z.unknown().optional(),
  created_on: z.unknown().optional(),
  custom_page_html: z.unknown().optional(),
  default_template_language: z.unknown().optional(),
  description: z.unknown().optional(),
  disable_session_renewal: z.unknown().optional(),
  enabled_origin_commands: z.unknown().optional(),
  host: z.unknown().optional(),
  id: z.unknown().optional(),
  json_response_enabled: z.unknown().optional(),
  modified_on: z.unknown().optional(),
  name: z.unknown().optional(),
  new_users_per_minute: z.unknown().optional(),
  next_event_prequeue_start_time: z.unknown().optional(),
  next_event_start_time: z.unknown().optional(),
  path: z.unknown().optional(),
  queue_all: z.unknown().optional(),
  queueing_method: z.unknown().optional(),
  queueing_status_code: z.unknown().optional(),
  session_duration: z.unknown().optional(),
  suspended: z.unknown().optional(),
  total_active_users: z.unknown().optional(),
  turnstile_action: z.unknown().optional(),
  turnstile_mode: z.unknown().optional(),
});

const EventsItemSchema = z.object({
  created_on: z.unknown().optional(),
  custom_page_html: z.unknown().optional(),
  description: z.unknown().optional(),
  disable_session_renewal: z.unknown().optional(),
  event_end_time: z.unknown().optional(),
  event_start_time: z.unknown().optional(),
  id: z.unknown().optional(),
  modified_on: z.unknown().optional(),
  name: z.unknown().optional(),
  new_users_per_minute: z.unknown().optional(),
  prequeue_start_time: z.unknown().optional(),
  queueing_method: z.unknown().optional(),
  session_duration: z.unknown().optional(),
  shuffle_at_event_start: z.unknown().optional(),
  suspended: z.unknown().optional(),
  total_active_users: z.unknown().optional(),
  turnstile_action: z.unknown().optional(),
  turnstile_mode: z.unknown().optional(),
});

const ListEventsSchema = z.object({
  items: z.array(EventsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateEventSchema = z.object({
  created_on: z.unknown().optional(),
  custom_page_html: z.unknown().optional(),
  description: z.unknown().optional(),
  disable_session_renewal: z.unknown().optional(),
  event_end_time: z.unknown().optional(),
  event_start_time: z.unknown().optional(),
  id: z.unknown().optional(),
  modified_on: z.unknown().optional(),
  name: z.unknown().optional(),
  new_users_per_minute: z.unknown().optional(),
  prequeue_start_time: z.unknown().optional(),
  queueing_method: z.unknown().optional(),
  session_duration: z.unknown().optional(),
  shuffle_at_event_start: z.unknown().optional(),
  suspended: z.unknown().optional(),
  total_active_users: z.unknown().optional(),
  turnstile_action: z.unknown().optional(),
  turnstile_mode: z.unknown().optional(),
});

const GetWaitingRoomEventDetailsSchema = z.object({
  created_on: z.unknown().optional(),
  custom_page_html: z.unknown().optional(),
  description: z.unknown().optional(),
  disable_session_renewal: z.unknown().optional(),
  event_end_time: z.unknown().optional(),
  event_start_time: z.unknown().optional(),
  id: z.unknown().optional(),
  modified_on: z.unknown().optional(),
  name: z.unknown().optional(),
  new_users_per_minute: z.unknown().optional(),
  prequeue_start_time: z.unknown().optional(),
  queueing_method: z.unknown().optional(),
  session_duration: z.unknown().optional(),
  shuffle_at_event_start: z.unknown().optional(),
  suspended: z.unknown().optional(),
  total_active_users: z.unknown().optional(),
  turnstile_action: z.unknown().optional(),
  turnstile_mode: z.unknown().optional(),
});

const PatchEventSchema = z.object({
  created_on: z.unknown().optional(),
  custom_page_html: z.unknown().optional(),
  description: z.unknown().optional(),
  disable_session_renewal: z.unknown().optional(),
  event_end_time: z.unknown().optional(),
  event_start_time: z.unknown().optional(),
  id: z.unknown().optional(),
  modified_on: z.unknown().optional(),
  name: z.unknown().optional(),
  new_users_per_minute: z.unknown().optional(),
  prequeue_start_time: z.unknown().optional(),
  queueing_method: z.unknown().optional(),
  session_duration: z.unknown().optional(),
  shuffle_at_event_start: z.unknown().optional(),
  suspended: z.unknown().optional(),
  total_active_users: z.unknown().optional(),
  turnstile_action: z.unknown().optional(),
  turnstile_mode: z.unknown().optional(),
});

const GetWaitingRoomPreviewActiveEventDetailsSchema = z.object({
  created_on: z.unknown().optional(),
  custom_page_html: z.unknown().optional(),
  description: z.unknown().optional(),
  disable_session_renewal: z.unknown().optional(),
  event_end_time: z.unknown().optional(),
  event_start_time: z.unknown().optional(),
  id: z.unknown().optional(),
  modified_on: z.unknown().optional(),
  name: z.unknown().optional(),
  new_users_per_minute: z.unknown().optional(),
  prequeue_start_time: z.unknown().optional(),
  queueing_method: z.unknown().optional(),
  session_duration: z.unknown().optional(),
  shuffle_at_event_start: z.unknown().optional(),
  suspended: z.unknown().optional(),
  total_active_users: z.unknown().optional(),
});

const WaitingRoomRulesItemSchema = z.object({
  action: z.unknown().optional(),
  description: z.unknown().optional(),
  enabled: z.unknown().optional(),
  expression: z.unknown().optional(),
  id: z.unknown().optional(),
  last_updated: z.unknown().optional(),
  version: z.unknown().optional(),
});

const ListWaitingRoomRulesSchema = z.object({
  items: z.array(WaitingRoomRulesItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateWaitingRoomRuleSchema = z.object({
  action: z.unknown().optional(),
  description: z.unknown().optional(),
  enabled: z.unknown().optional(),
  expression: z.unknown().optional(),
  id: z.unknown().optional(),
  last_updated: z.unknown().optional(),
  version: z.unknown().optional(),
});

const UpdateWaitingRoomReplaceWaitingRoomRulesSchema = z.object({
  action: z.unknown().optional(),
  description: z.unknown().optional(),
  enabled: z.unknown().optional(),
  expression: z.unknown().optional(),
  id: z.unknown().optional(),
  last_updated: z.unknown().optional(),
  version: z.unknown().optional(),
});

const PatchWaitingRoomRuleSchema = z.object({
  action: z.unknown().optional(),
  description: z.unknown().optional(),
  enabled: z.unknown().optional(),
  expression: z.unknown().optional(),
  id: z.unknown().optional(),
  last_updated: z.unknown().optional(),
  version: z.unknown().optional(),
});

const GetWaitingRoomStatusSchema = z.object({
  estimated_queued_users: z.unknown().optional(),
  estimated_total_active_users: z.unknown().optional(),
  event_id: z.unknown().optional(),
  max_estimated_time_minutes: z.unknown().optional(),
  status: z.unknown().optional(),
});

// =============================================================================
// Model Definition
// =============================================================================

/** Cloudflare Waiting Room — traffic queuing, rules, events, analytics */
export const model = {
  type: "@webframp/cloudflare/waiting-room",
  version: "2026.07.19.1",
  globalArguments: GlobalArgsSchema,

  upgrades: [],

  resources: {
    "waiting_rooms": {
      description: "List waiting rooms for zone",
      schema: ListWaitingRoomsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "waiting_room": {
      description: "Create waiting room",
      schema: CreateWaitingRoomSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "a_custom_waiting_room_page_preview": {
      description: "Create a custom waiting room page preview",
      schema: CreateACustomWaitingRoomPagePreviewSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "zone_settings": {
      description: "Get zone-level Waiting Room settings",
      schema: GetZoneSettingsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "patch_zone_settings": {
      description: "Patch zone-level Waiting Room settings",
      schema: PatchZoneSettingsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "waiting_room_waiting_room_details": {
      description: "Waiting room details",
      schema: GetWaitingRoomWaitingRoomDetailsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "patch_waiting_room": {
      description: "Patch waiting room",
      schema: PatchWaitingRoomSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "events": {
      description: "List events",
      schema: ListEventsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "event": {
      description: "Create event",
      schema: CreateEventSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "waiting_room_event_details": {
      description: "Event details",
      schema: GetWaitingRoomEventDetailsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "patch_event": {
      description: "Patch event",
      schema: PatchEventSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "waiting_room_preview_active_event_details": {
      description: "Preview active event details",
      schema: GetWaitingRoomPreviewActiveEventDetailsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "waiting_room_rules": {
      description: "List Waiting Room Rules",
      schema: ListWaitingRoomRulesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "waiting_room_rule": {
      description: "Create Waiting Room Rule",
      schema: CreateWaitingRoomRuleSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "waiting_room_replace_waiting_room_rules": {
      description: "Replace Waiting Room Rules",
      schema: UpdateWaitingRoomReplaceWaitingRoomRulesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "patch_waiting_room_rule": {
      description: "Patch Waiting Room Rule",
      schema: PatchWaitingRoomRuleSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "waiting_room_status": {
      description: "Get waiting room status",
      schema: GetWaitingRoomStatusSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
  },

  methods: {
    list_waiting_rooms: {
      description: "List waiting rooms for zone",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/zones/${zoneId}/waiting_rooms`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("waiting_rooms", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} waiting_rooms", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_waiting_room: {
      description: "Create waiting room",
      arguments: z.object({
        additional_routes: z.unknown().optional(),
        cookie_attributes: z.unknown().optional(),
        cookie_suffix: z.unknown().optional(),
        custom_page_html: z.unknown().optional(),
        default_template_language: z.unknown().optional(),
        description: z.unknown().optional(),
        disable_session_renewal: z.unknown().optional(),
        enabled_origin_commands: z.unknown().optional(),
        host: z.unknown(),
        json_response_enabled: z.unknown().optional(),
        name: z.unknown(),
        new_users_per_minute: z.unknown(),
        path: z.unknown().optional(),
        queue_all: z.unknown().optional(),
        queueing_method: z.unknown().optional(),
        queueing_status_code: z.unknown().optional(),
        session_duration: z.unknown().optional(),
        suspended: z.unknown().optional(),
        total_active_users: z.unknown(),
        turnstile_action: z.unknown().optional(),
        turnstile_mode: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/zones/${zoneId}/waiting_rooms`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource("waiting_room", id, result);
        context.logger.info("Created waiting_room {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    create_a_custom_waiting_room_page_preview: {
      description: "Create a custom waiting room page preview",
      arguments: z.object({
        custom_html: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/zones/${zoneId}/waiting_rooms/preview`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "a_custom_waiting_room_page_preview",
          id,
          result,
        );
        context.logger.info("Created a_custom_waiting_room_page_preview {id}", {
          id,
        });
        return { dataHandles: [handle] };
      },
    },
    get_zone_settings: {
      description: "Get zone-level Waiting Room settings",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/zones/${zoneId}/waiting_rooms/settings`,
        );

        const handle = await context.writeResource(
          "zone_settings",
          "latest",
          result,
        );
        context.logger.info("Fetched zone_settings", {});
        return { dataHandles: [handle] };
      },
    },
    update_zone_settings: {
      description: "Update zone-level Waiting Room settings",
      arguments: z.object({
        search_engine_crawler_bypass: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/zones/${zoneId}/waiting_rooms/settings`,
          body,
        );

        const handle = await context.writeResource(
          "zone_settings",
          "updated",
          result,
        );
        context.logger.info("Updated zone_settings", {});
        return { dataHandles: [handle] };
      },
    },
    patch_zone_settings: {
      description: "Patch zone-level Waiting Room settings",
      arguments: z.object({
        search_engine_crawler_bypass: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/zones/${zoneId}/waiting_rooms/settings`,
          body,
        );

        const handle = await context.writeResource(
          "patch_zone_settings",
          "updated",
          result,
        );
        context.logger.info("Updated patch_zone_settings", {});
        return { dataHandles: [handle] };
      },
    },
    get_waiting_room_waiting_room_details: {
      description: "Waiting room details",
      arguments: z.object({
        waiting_room_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/zones/${zoneId}/waiting_rooms/${args.waiting_room_id}`,
        );

        const handle = await context.writeResource(
          "waiting_room_waiting_room_details",
          String(args.waiting_room_id),
          result,
        );
        context.logger.info("Fetched waiting_room_waiting_room_details", {});
        return { dataHandles: [handle] };
      },
    },
    update_waiting_room: {
      description: "Update waiting room",
      arguments: z.object({
        waiting_room_id: z.string(),
        additional_routes: z.unknown().optional(),
        cookie_attributes: z.unknown().optional(),
        cookie_suffix: z.unknown().optional(),
        custom_page_html: z.unknown().optional(),
        default_template_language: z.unknown().optional(),
        description: z.unknown().optional(),
        disable_session_renewal: z.unknown().optional(),
        enabled_origin_commands: z.unknown().optional(),
        host: z.unknown(),
        json_response_enabled: z.unknown().optional(),
        name: z.unknown(),
        new_users_per_minute: z.unknown(),
        path: z.unknown().optional(),
        queue_all: z.unknown().optional(),
        queueing_method: z.unknown().optional(),
        queueing_status_code: z.unknown().optional(),
        session_duration: z.unknown().optional(),
        suspended: z.unknown().optional(),
        total_active_users: z.unknown(),
        turnstile_action: z.unknown().optional(),
        turnstile_mode: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const pathKeys = new Set(["waiting_room_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!pathKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/zones/${zoneId}/waiting_rooms/${args.waiting_room_id}`,
          body,
        );

        const handle = await context.writeResource(
          "waiting_room",
          String(args.waiting_room_id),
          result,
        );
        context.logger.info("Updated waiting_room", {});
        return { dataHandles: [handle] };
      },
    },
    patch_waiting_room: {
      description: "Patch waiting room",
      arguments: z.object({
        waiting_room_id: z.string(),
        additional_routes: z.unknown().optional(),
        cookie_attributes: z.unknown().optional(),
        cookie_suffix: z.unknown().optional(),
        custom_page_html: z.unknown().optional(),
        default_template_language: z.unknown().optional(),
        description: z.unknown().optional(),
        disable_session_renewal: z.unknown().optional(),
        enabled_origin_commands: z.unknown().optional(),
        host: z.unknown(),
        json_response_enabled: z.unknown().optional(),
        name: z.unknown(),
        new_users_per_minute: z.unknown(),
        path: z.unknown().optional(),
        queue_all: z.unknown().optional(),
        queueing_method: z.unknown().optional(),
        queueing_status_code: z.unknown().optional(),
        session_duration: z.unknown().optional(),
        suspended: z.unknown().optional(),
        total_active_users: z.unknown(),
        turnstile_action: z.unknown().optional(),
        turnstile_mode: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const pathKeys = new Set(["waiting_room_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!pathKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/zones/${zoneId}/waiting_rooms/${args.waiting_room_id}`,
          body,
        );

        const handle = await context.writeResource(
          "patch_waiting_room",
          String(args.waiting_room_id),
          result,
        );
        context.logger.info("Updated patch_waiting_room", {});
        return { dataHandles: [handle] };
      },
    },
    delete_waiting_room: {
      description: "Delete waiting room",
      arguments: z.object({
        waiting_room_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/zones/${zoneId}/waiting_rooms/${args.waiting_room_id}`,
        );

        context.logger.info("Deleted resource {id}", {
          id: args.waiting_room_id,
        });
        return { dataHandles: [] };
      },
    },
    list_events: {
      description: "List events",
      arguments: z.object({
        waiting_room_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["waiting_room_id", "page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/zones/${zoneId}/waiting_rooms/${args.waiting_room_id}/events`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("events", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} events", { count: results.length });
        return { dataHandles: [handle] };
      },
    },
    create_event: {
      description: "Create event",
      arguments: z.object({
        waiting_room_id: z.string(),
        custom_page_html: z.unknown().optional(),
        description: z.unknown().optional(),
        disable_session_renewal: z.unknown().optional(),
        event_end_time: z.unknown(),
        event_start_time: z.unknown(),
        name: z.unknown(),
        new_users_per_minute: z.unknown().optional(),
        prequeue_start_time: z.unknown().optional(),
        queueing_method: z.unknown().optional(),
        session_duration: z.unknown().optional(),
        shuffle_at_event_start: z.unknown().optional(),
        suspended: z.unknown().optional(),
        total_active_users: z.unknown().optional(),
        turnstile_action: z.unknown().optional(),
        turnstile_mode: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const pathKeys = new Set(["waiting_room_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!pathKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/zones/${zoneId}/waiting_rooms/${args.waiting_room_id}/events`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource("event", id, result);
        context.logger.info("Created event {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_waiting_room_event_details: {
      description: "Event details",
      arguments: z.object({
        event_id: z.string(),
        waiting_room_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/zones/${zoneId}/waiting_rooms/${args.waiting_room_id}/events/${args.event_id}`,
        );

        const handle = await context.writeResource(
          "waiting_room_event_details",
          String(args.waiting_room_id),
          result,
        );
        context.logger.info("Fetched waiting_room_event_details", {});
        return { dataHandles: [handle] };
      },
    },
    update_event: {
      description: "Update event",
      arguments: z.object({
        event_id: z.string(),
        waiting_room_id: z.string(),
        custom_page_html: z.unknown().optional(),
        description: z.unknown().optional(),
        disable_session_renewal: z.unknown().optional(),
        event_end_time: z.unknown(),
        event_start_time: z.unknown(),
        name: z.unknown(),
        new_users_per_minute: z.unknown().optional(),
        prequeue_start_time: z.unknown().optional(),
        queueing_method: z.unknown().optional(),
        session_duration: z.unknown().optional(),
        shuffle_at_event_start: z.unknown().optional(),
        suspended: z.unknown().optional(),
        total_active_users: z.unknown().optional(),
        turnstile_action: z.unknown().optional(),
        turnstile_mode: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const pathKeys = new Set(["event_id", "waiting_room_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!pathKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/zones/${zoneId}/waiting_rooms/${args.waiting_room_id}/events/${args.event_id}`,
          body,
        );

        const handle = await context.writeResource(
          "event",
          String(args.waiting_room_id),
          result,
        );
        context.logger.info("Updated event", {});
        return { dataHandles: [handle] };
      },
    },
    patch_event: {
      description: "Patch event",
      arguments: z.object({
        event_id: z.string(),
        waiting_room_id: z.string(),
        custom_page_html: z.unknown().optional(),
        description: z.unknown().optional(),
        disable_session_renewal: z.unknown().optional(),
        event_end_time: z.unknown(),
        event_start_time: z.unknown(),
        name: z.unknown(),
        new_users_per_minute: z.unknown().optional(),
        prequeue_start_time: z.unknown().optional(),
        queueing_method: z.unknown().optional(),
        session_duration: z.unknown().optional(),
        shuffle_at_event_start: z.unknown().optional(),
        suspended: z.unknown().optional(),
        total_active_users: z.unknown().optional(),
        turnstile_action: z.unknown().optional(),
        turnstile_mode: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const pathKeys = new Set(["event_id", "waiting_room_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!pathKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/zones/${zoneId}/waiting_rooms/${args.waiting_room_id}/events/${args.event_id}`,
          body,
        );

        const handle = await context.writeResource(
          "patch_event",
          String(args.waiting_room_id),
          result,
        );
        context.logger.info("Updated patch_event", {});
        return { dataHandles: [handle] };
      },
    },
    delete_event: {
      description: "Delete event",
      arguments: z.object({
        event_id: z.string(),
        waiting_room_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/zones/${zoneId}/waiting_rooms/${args.waiting_room_id}/events/${args.event_id}`,
        );

        context.logger.info("Deleted resource {id}", {
          id: args.waiting_room_id,
        });
        return { dataHandles: [] };
      },
    },
    get_waiting_room_preview_active_event_details: {
      description: "Preview active event details",
      arguments: z.object({
        event_id: z.string(),
        waiting_room_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/zones/${zoneId}/waiting_rooms/${args.waiting_room_id}/events/${args.event_id}/details`,
        );

        const handle = await context.writeResource(
          "waiting_room_preview_active_event_details",
          String(args.waiting_room_id),
          result,
        );
        context.logger.info(
          "Fetched waiting_room_preview_active_event_details",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    list_waiting_room_rules: {
      description: "List Waiting Room Rules",
      arguments: z.object({
        waiting_room_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["waiting_room_id", "page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/zones/${zoneId}/waiting_rooms/${args.waiting_room_id}/rules`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "waiting_room_rules",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} waiting_room_rules", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_waiting_room_rule: {
      description: "Create Waiting Room Rule",
      arguments: z.object({
        waiting_room_id: z.string(),
        action: z.unknown(),
        description: z.unknown().optional(),
        enabled: z.unknown().optional(),
        expression: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const pathKeys = new Set(["waiting_room_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!pathKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/zones/${zoneId}/waiting_rooms/${args.waiting_room_id}/rules`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "waiting_room_rule",
          id,
          result,
        );
        context.logger.info("Created waiting_room_rule {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    update_waiting_room_replace_waiting_room_rules: {
      description: "Replace Waiting Room Rules",
      arguments: z.object({
        waiting_room_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const pathKeys = new Set(["waiting_room_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!pathKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/zones/${zoneId}/waiting_rooms/${args.waiting_room_id}/rules`,
          body,
        );

        const handle = await context.writeResource(
          "waiting_room_replace_waiting_room_rules",
          String(args.waiting_room_id),
          result,
        );
        context.logger.info(
          "Updated waiting_room_replace_waiting_room_rules",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    patch_waiting_room_rule: {
      description: "Patch Waiting Room Rule",
      arguments: z.object({
        rule_id: z.string(),
        waiting_room_id: z.string(),
        action: z.unknown(),
        description: z.unknown().optional(),
        enabled: z.unknown().optional(),
        expression: z.unknown(),
        position: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const pathKeys = new Set(["rule_id", "waiting_room_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!pathKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/zones/${zoneId}/waiting_rooms/${args.waiting_room_id}/rules/${args.rule_id}`,
          body,
        );

        const handle = await context.writeResource(
          "patch_waiting_room_rule",
          String(args.waiting_room_id),
          result,
        );
        context.logger.info("Updated patch_waiting_room_rule", {});
        return { dataHandles: [handle] };
      },
    },
    delete_waiting_room_rule: {
      description: "Delete Waiting Room Rule",
      arguments: z.object({
        rule_id: z.string(),
        waiting_room_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/zones/${zoneId}/waiting_rooms/${args.waiting_room_id}/rules/${args.rule_id}`,
        );

        context.logger.info("Deleted resource {id}", {
          id: args.waiting_room_id,
        });
        return { dataHandles: [] };
      },
    },
    get_waiting_room_status: {
      description: "Get waiting room status",
      arguments: z.object({
        waiting_room_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/zones/${zoneId}/waiting_rooms/${args.waiting_room_id}/status`,
        );

        const handle = await context.writeResource(
          "waiting_room_status",
          String(args.waiting_room_id),
          result,
        );
        context.logger.info("Fetched waiting_room_status", {});
        return { dataHandles: [handle] };
      },
    },
  },
};
