/**
 * Cloudflare Magic Transit — GRE tunnels, static routes, health checks, IPsec
 *
 * Auto-generated by scripts/cloudflare-codegen — do not edit manually.
 *
 * @module
 */
// SPDX-License-Identifier: AGPL-3.0-or-later WITH Swamp-Extension-Exception

import { z } from "npm:zod@4.4.3";
import { cfApi, cfApiPaginated } from "./_lib/api.ts";

// =============================================================================
// Schemas
// =============================================================================

const GlobalArgsSchema = z.object({
  apiToken: z.string().meta({ sensitive: true }).describe(
    "Cloudflare API token",
  ),
  accountId: z.string().describe("Cloudflare account ID"),
});

const ListdnsprotectionrulesforaccountItemSchema = z.object({
  burst_sensitivity: z.string().describe(
    "The burst sensitivity. Must be one of 'low', 'medium', 'high'.",
  ),
  created_on: z.string().describe(
    "The creation timestamp of the DNS Protection rule.",
  ),
  id: z.string().describe("The unique ID of the DNS Protection rule."),
  mode: z.string().describe(
    "The mode for DNS Protection. Must be one of 'enabled', 'disabled', 'monitoring'.",
  ),
  modified_on: z.string().describe(
    "The last modification timestamp of the DNS Protection rule.",
  ),
  name: z.string().describe(
    "The name of the DNS Protection rule. Value is relative to the 'scope' setting. For 'global' scope...",
  ),
  profile_sensitivity: z.string().describe(
    "The profile sensitivity. Recommended setting is 'low'. Must be one of 'low', 'medium', 'high', or...",
  ),
  rate_sensitivity: z.string().describe(
    "The rate sensitivity. Must be one of 'low', 'medium', 'high'.",
  ),
  scope: z.string().describe(
    "The scope for the DNS Protection rule. Must be one of 'global', 'region', or 'datacenter'.",
  ),
});

const ListdnsprotectionrulesforaccountSchema = z.object({
  items: z.array(ListdnsprotectionrulesforaccountItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreatednsprotectionruleSchema = z.object({
  burst_sensitivity: z.string().describe(
    "The burst sensitivity. Must be one of 'low', 'medium', 'high'.",
  ),
  created_on: z.string().describe(
    "The creation timestamp of the DNS Protection rule.",
  ),
  id: z.string().describe("The unique ID of the DNS Protection rule."),
  mode: z.string().describe(
    "The mode for DNS Protection. Must be one of 'enabled', 'disabled', 'monitoring'.",
  ),
  modified_on: z.string().describe(
    "The last modification timestamp of the DNS Protection rule.",
  ),
  name: z.string().describe(
    "The name of the DNS Protection rule. Value is relative to the 'scope' setting. For 'global' scope...",
  ),
  profile_sensitivity: z.string().describe(
    "The profile sensitivity. Recommended setting is 'low'. Must be one of 'low', 'medium', 'high', or...",
  ),
  rate_sensitivity: z.string().describe(
    "The rate sensitivity. Must be one of 'low', 'medium', 'high'.",
  ),
  scope: z.string().describe(
    "The scope for the DNS Protection rule. Must be one of 'global', 'region', or 'datacenter'.",
  ),
});

const GetdnsprotectionruleSchema = z.object({
  burst_sensitivity: z.string().describe(
    "The burst sensitivity. Must be one of 'low', 'medium', 'high'.",
  ),
  created_on: z.string().describe(
    "The creation timestamp of the DNS Protection rule.",
  ),
  id: z.string().describe("The unique ID of the DNS Protection rule."),
  mode: z.string().describe(
    "The mode for DNS Protection. Must be one of 'enabled', 'disabled', 'monitoring'.",
  ),
  modified_on: z.string().describe(
    "The last modification timestamp of the DNS Protection rule.",
  ),
  name: z.string().describe(
    "The name of the DNS Protection rule. Value is relative to the 'scope' setting. For 'global' scope...",
  ),
  profile_sensitivity: z.string().describe(
    "The profile sensitivity. Recommended setting is 'low'. Must be one of 'low', 'medium', 'high', or...",
  ),
  rate_sensitivity: z.string().describe(
    "The rate sensitivity. Must be one of 'low', 'medium', 'high'.",
  ),
  scope: z.string().describe(
    "The scope for the DNS Protection rule. Must be one of 'global', 'region', or 'datacenter'.",
  ),
});

const UpdatednsprotectionruleSchema = z.object({
  burst_sensitivity: z.string().describe(
    "The burst sensitivity. Must be one of 'low', 'medium', 'high'.",
  ),
  created_on: z.string().describe(
    "The creation timestamp of the DNS Protection rule.",
  ),
  id: z.string().describe("The unique ID of the DNS Protection rule."),
  mode: z.string().describe(
    "The mode for DNS Protection. Must be one of 'enabled', 'disabled', 'monitoring'.",
  ),
  modified_on: z.string().describe(
    "The last modification timestamp of the DNS Protection rule.",
  ),
  name: z.string().describe(
    "The name of the DNS Protection rule. Value is relative to the 'scope' setting. For 'global' scope...",
  ),
  profile_sensitivity: z.string().describe(
    "The profile sensitivity. Recommended setting is 'low'. Must be one of 'low', 'medium', 'high', or...",
  ),
  rate_sensitivity: z.string().describe(
    "The rate sensitivity. Must be one of 'low', 'medium', 'high'.",
  ),
  scope: z.string().describe(
    "The scope for the DNS Protection rule. Must be one of 'global', 'region', or 'datacenter'.",
  ),
});

const ListallowlistprefixesforaccountItemSchema = z.object({
  comment: z.string().describe(
    "An optional comment describing the allowlist prefix.",
  ),
  created_on: z.string().describe(
    "The creation timestamp of the allowlist prefix.",
  ),
  enabled: z.boolean().describe(
    "Whether to enable the allowlist prefix into effect. Defaults to false.",
  ),
  id: z.string().describe("The unique ID of the allowlist prefix."),
  modified_on: z.string().describe(
    "The last modification timestamp of the allowlist prefix.",
  ),
  prefix: z.string().describe("The allowlist prefix in CIDR format."),
});

const ListallowlistprefixesforaccountSchema = z.object({
  items: z.array(ListallowlistprefixesforaccountItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateallowlistedprefixSchema = z.object({
  comment: z.string().describe(
    "An optional comment describing the allowlist prefix.",
  ),
  created_on: z.string().describe(
    "The creation timestamp of the allowlist prefix.",
  ),
  enabled: z.boolean().describe(
    "Whether to enable the allowlist prefix into effect. Defaults to false.",
  ),
  id: z.string().describe("The unique ID of the allowlist prefix."),
  modified_on: z.string().describe(
    "The last modification timestamp of the allowlist prefix.",
  ),
  prefix: z.string().describe("The allowlist prefix in CIDR format."),
});

const GetallowlistprefixSchema = z.object({
  comment: z.string().describe(
    "An optional comment describing the allowlist prefix.",
  ),
  created_on: z.string().describe(
    "The creation timestamp of the allowlist prefix.",
  ),
  enabled: z.boolean().describe(
    "Whether to enable the allowlist prefix into effect. Defaults to false.",
  ),
  id: z.string().describe("The unique ID of the allowlist prefix."),
  modified_on: z.string().describe(
    "The last modification timestamp of the allowlist prefix.",
  ),
  prefix: z.string().describe("The allowlist prefix in CIDR format."),
});

const UpdateallowlistprefixSchema = z.object({
  comment: z.string().describe(
    "An optional comment describing the allowlist prefix.",
  ),
  created_on: z.string().describe(
    "The creation timestamp of the allowlist prefix.",
  ),
  enabled: z.boolean().describe(
    "Whether to enable the allowlist prefix into effect. Defaults to false.",
  ),
  id: z.string().describe("The unique ID of the allowlist prefix."),
  modified_on: z.string().describe(
    "The last modification timestamp of the allowlist prefix.",
  ),
  prefix: z.string().describe("The allowlist prefix in CIDR format."),
});

const ListprefixesforaccountItemSchema = z.object({
  comment: z.string().describe("A comment describing the prefix."),
  created_on: z.string().describe("The creation timestamp of the prefix."),
  excluded: z.boolean().describe(
    "Whether to exclude the prefix from protection.",
  ),
  id: z.string().describe("The unique ID of the prefix."),
  modified_on: z.string().describe(
    "The last modification timestamp of the prefix.",
  ),
  prefix: z.string().describe("The prefix in CIDR format."),
});

const ListprefixesforaccountSchema = z.object({
  items: z.array(ListprefixesforaccountItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateprefixSchema = z.object({
  comment: z.string().describe("A comment describing the prefix."),
  created_on: z.string().describe("The creation timestamp of the prefix."),
  excluded: z.boolean().describe(
    "Whether to exclude the prefix from protection.",
  ),
  id: z.string().describe("The unique ID of the prefix."),
  modified_on: z.string().describe(
    "The last modification timestamp of the prefix.",
  ),
  prefix: z.string().describe("The prefix in CIDR format."),
});

const BulkcreateprefixesSchema = z.object({
  comment: z.string().describe("A comment describing the prefix."),
  created_on: z.string().describe("The creation timestamp of the prefix."),
  excluded: z.boolean().describe(
    "Whether to exclude the prefix from protection.",
  ),
  id: z.string().describe("The unique ID of the prefix."),
  modified_on: z.string().describe(
    "The last modification timestamp of the prefix.",
  ),
  prefix: z.string().describe("The prefix in CIDR format."),
});

const GetprefixSchema = z.object({
  comment: z.string().describe("A comment describing the prefix."),
  created_on: z.string().describe("The creation timestamp of the prefix."),
  excluded: z.boolean().describe(
    "Whether to exclude the prefix from protection.",
  ),
  id: z.string().describe("The unique ID of the prefix."),
  modified_on: z.string().describe(
    "The last modification timestamp of the prefix.",
  ),
  prefix: z.string().describe("The prefix in CIDR format."),
});

const UpdateprefixSchema = z.object({
  comment: z.string().describe("A comment describing the prefix."),
  created_on: z.string().describe("The creation timestamp of the prefix."),
  excluded: z.boolean().describe(
    "Whether to exclude the prefix from protection.",
  ),
  id: z.string().describe("The unique ID of the prefix."),
  modified_on: z.string().describe(
    "The last modification timestamp of the prefix.",
  ),
  prefix: z.string().describe("The prefix in CIDR format."),
});

const ListsynprotectionfiltersforaccountItemSchema = z.object({
  created_on: z.string().describe(
    "The creation timestamp of the expression filter.",
  ),
  expression: z.string().describe("The filter expression."),
  id: z.string().describe("The unique ID of the expression filter."),
  mode: z.string().describe(
    "The filter's mode. Must be one of 'enabled', 'disabled', 'monitoring'.",
  ),
  modified_on: z.string().describe(
    "The last modification timestamp of the expression filter.",
  ),
});

const ListsynprotectionfiltersforaccountSchema = z.object({
  items: z.array(ListsynprotectionfiltersforaccountItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreatesynprotectionfilterSchema = z.object({
  created_on: z.string().describe(
    "The creation timestamp of the expression filter.",
  ),
  expression: z.string().describe("The filter expression."),
  id: z.string().describe("The unique ID of the expression filter."),
  mode: z.string().describe(
    "The filter's mode. Must be one of 'enabled', 'disabled', 'monitoring'.",
  ),
  modified_on: z.string().describe(
    "The last modification timestamp of the expression filter.",
  ),
});

const GetsynprotectionfilterSchema = z.object({
  created_on: z.string().describe(
    "The creation timestamp of the expression filter.",
  ),
  expression: z.string().describe("The filter expression."),
  id: z.string().describe("The unique ID of the expression filter."),
  mode: z.string().describe(
    "The filter's mode. Must be one of 'enabled', 'disabled', 'monitoring'.",
  ),
  modified_on: z.string().describe(
    "The last modification timestamp of the expression filter.",
  ),
});

const UpdatesynprotectionfilterSchema = z.object({
  created_on: z.string().describe(
    "The creation timestamp of the expression filter.",
  ),
  expression: z.string().describe("The filter expression."),
  id: z.string().describe("The unique ID of the expression filter."),
  mode: z.string().describe(
    "The filter's mode. Must be one of 'enabled', 'disabled', 'monitoring'.",
  ),
  modified_on: z.string().describe(
    "The last modification timestamp of the expression filter.",
  ),
});

const ListsynprotectionrulesforaccountItemSchema = z.object({
  burst_sensitivity: z.string().describe(
    "The burst sensitivity. Must be one of 'low', 'medium', 'high'.",
  ),
  created_on: z.string().describe(
    "The creation timestamp of the SYN Protection rule.",
  ),
  id: z.string().describe("The unique ID of the SYN Protection rule."),
  mitigation_type: z.string().describe(
    "The type of mitigation for SYN Protection. Must be one of 'challenge' or 'retransmit'.",
  ),
  mode: z.string().describe(
    "The mode for SYN Protection. Must be one of 'enabled', 'disabled', 'monitoring'.",
  ),
  modified_on: z.string().describe(
    "The last modification timestamp of the SYN Protection rule.",
  ),
  name: z.string().describe(
    "The name of the SYN Protection rule. Value is relative to the 'scope' setting. For 'global' scope...",
  ),
  rate_sensitivity: z.string().describe(
    "The rate sensitivity. Must be one of 'low', 'medium', 'high'.",
  ),
  scope: z.string().describe(
    "The scope for the SYN Protection rule. Must be one of 'global', 'region', or 'datacenter'.",
  ),
});

const ListsynprotectionrulesforaccountSchema = z.object({
  items: z.array(ListsynprotectionrulesforaccountItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreatesynprotectionruleSchema = z.object({
  burst_sensitivity: z.string().describe(
    "The burst sensitivity. Must be one of 'low', 'medium', 'high'.",
  ),
  created_on: z.string().describe(
    "The creation timestamp of the SYN Protection rule.",
  ),
  id: z.string().describe("The unique ID of the SYN Protection rule."),
  mitigation_type: z.string().describe(
    "The type of mitigation for SYN Protection. Must be one of 'challenge' or 'retransmit'.",
  ),
  mode: z.string().describe(
    "The mode for SYN Protection. Must be one of 'enabled', 'disabled', 'monitoring'.",
  ),
  modified_on: z.string().describe(
    "The last modification timestamp of the SYN Protection rule.",
  ),
  name: z.string().describe(
    "The name of the SYN Protection rule. Value is relative to the 'scope' setting. For 'global' scope...",
  ),
  rate_sensitivity: z.string().describe(
    "The rate sensitivity. Must be one of 'low', 'medium', 'high'.",
  ),
  scope: z.string().describe(
    "The scope for the SYN Protection rule. Must be one of 'global', 'region', or 'datacenter'.",
  ),
});

const GetsynprotectionruleSchema = z.object({
  burst_sensitivity: z.string().describe(
    "The burst sensitivity. Must be one of 'low', 'medium', 'high'.",
  ),
  created_on: z.string().describe(
    "The creation timestamp of the SYN Protection rule.",
  ),
  id: z.string().describe("The unique ID of the SYN Protection rule."),
  mitigation_type: z.string().describe(
    "The type of mitigation for SYN Protection. Must be one of 'challenge' or 'retransmit'.",
  ),
  mode: z.string().describe(
    "The mode for SYN Protection. Must be one of 'enabled', 'disabled', 'monitoring'.",
  ),
  modified_on: z.string().describe(
    "The last modification timestamp of the SYN Protection rule.",
  ),
  name: z.string().describe(
    "The name of the SYN Protection rule. Value is relative to the 'scope' setting. For 'global' scope...",
  ),
  rate_sensitivity: z.string().describe(
    "The rate sensitivity. Must be one of 'low', 'medium', 'high'.",
  ),
  scope: z.string().describe(
    "The scope for the SYN Protection rule. Must be one of 'global', 'region', or 'datacenter'.",
  ),
});

const UpdatesynprotectionruleSchema = z.object({
  burst_sensitivity: z.string().describe(
    "The burst sensitivity. Must be one of 'low', 'medium', 'high'.",
  ),
  created_on: z.string().describe(
    "The creation timestamp of the SYN Protection rule.",
  ),
  id: z.string().describe("The unique ID of the SYN Protection rule."),
  mitigation_type: z.string().describe(
    "The type of mitigation for SYN Protection. Must be one of 'challenge' or 'retransmit'.",
  ),
  mode: z.string().describe(
    "The mode for SYN Protection. Must be one of 'enabled', 'disabled', 'monitoring'.",
  ),
  modified_on: z.string().describe(
    "The last modification timestamp of the SYN Protection rule.",
  ),
  name: z.string().describe(
    "The name of the SYN Protection rule. Value is relative to the 'scope' setting. For 'global' scope...",
  ),
  rate_sensitivity: z.string().describe(
    "The rate sensitivity. Must be one of 'low', 'medium', 'high'.",
  ),
  scope: z.string().describe(
    "The scope for the SYN Protection rule. Must be one of 'global', 'region', or 'datacenter'.",
  ),
});

const ListtcpflowprotectionfiltersforaccountItemSchema = z.object({
  created_on: z.string().describe(
    "The creation timestamp of the expression filter.",
  ),
  expression: z.string().describe("The filter expression."),
  id: z.string().describe("The unique ID of the expression filter."),
  mode: z.string().describe(
    "The filter's mode. Must be one of 'enabled', 'disabled', 'monitoring'.",
  ),
  modified_on: z.string().describe(
    "The last modification timestamp of the expression filter.",
  ),
});

const ListtcpflowprotectionfiltersforaccountSchema = z.object({
  items: z.array(ListtcpflowprotectionfiltersforaccountItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreatetcpflowprotectionfilterSchema = z.object({
  created_on: z.string().describe(
    "The creation timestamp of the expression filter.",
  ),
  expression: z.string().describe("The filter expression."),
  id: z.string().describe("The unique ID of the expression filter."),
  mode: z.string().describe(
    "The filter's mode. Must be one of 'enabled', 'disabled', 'monitoring'.",
  ),
  modified_on: z.string().describe(
    "The last modification timestamp of the expression filter.",
  ),
});

const GettcpflowprotectionfilterSchema = z.object({
  created_on: z.string().describe(
    "The creation timestamp of the expression filter.",
  ),
  expression: z.string().describe("The filter expression."),
  id: z.string().describe("The unique ID of the expression filter."),
  mode: z.string().describe(
    "The filter's mode. Must be one of 'enabled', 'disabled', 'monitoring'.",
  ),
  modified_on: z.string().describe(
    "The last modification timestamp of the expression filter.",
  ),
});

const UpdatetcpflowprotectionfilterSchema = z.object({
  created_on: z.string().describe(
    "The creation timestamp of the expression filter.",
  ),
  expression: z.string().describe("The filter expression."),
  id: z.string().describe("The unique ID of the expression filter."),
  mode: z.string().describe(
    "The filter's mode. Must be one of 'enabled', 'disabled', 'monitoring'.",
  ),
  modified_on: z.string().describe(
    "The last modification timestamp of the expression filter.",
  ),
});

const ListtcpflowprotectionrulesforaccountItemSchema = z.object({
  burst_sensitivity: z.string().describe(
    "The burst sensitivity. Must be one of 'low', 'medium', 'high'.",
  ),
  created_on: z.string().describe(
    "The creation timestamp of the TCP Flow Protection rule.",
  ),
  id: z.string().describe("The unique ID of the TCP Flow Protection rule."),
  mode: z.string().describe(
    "The mode for TCP Flow Protection. Must be one of 'enabled', 'disabled', 'monitoring'.",
  ),
  modified_on: z.string().describe(
    "The last modification timestamp of the TCP Flow Protection rule.",
  ),
  name: z.string().describe(
    "The name of the TCP Flow Protection rule. Value is relative to the 'scope' setting. For 'global' ...",
  ),
  rate_sensitivity: z.string().describe(
    "The rate sensitivity. Must be one of 'low', 'medium', 'high'.",
  ),
  scope: z.string().describe(
    "The scope for the TCP Flow Protection rule. Must be one of 'global', 'region', or 'datacenter'.",
  ),
});

const ListtcpflowprotectionrulesforaccountSchema = z.object({
  items: z.array(ListtcpflowprotectionrulesforaccountItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreatetcpflowprotectionruleSchema = z.object({
  burst_sensitivity: z.string().describe(
    "The burst sensitivity. Must be one of 'low', 'medium', 'high'.",
  ),
  created_on: z.string().describe(
    "The creation timestamp of the TCP Flow Protection rule.",
  ),
  id: z.string().describe("The unique ID of the TCP Flow Protection rule."),
  mode: z.string().describe(
    "The mode for TCP Flow Protection. Must be one of 'enabled', 'disabled', 'monitoring'.",
  ),
  modified_on: z.string().describe(
    "The last modification timestamp of the TCP Flow Protection rule.",
  ),
  name: z.string().describe(
    "The name of the TCP Flow Protection rule. Value is relative to the 'scope' setting. For 'global' ...",
  ),
  rate_sensitivity: z.string().describe(
    "The rate sensitivity. Must be one of 'low', 'medium', 'high'.",
  ),
  scope: z.string().describe(
    "The scope for the TCP Flow Protection rule. Must be one of 'global', 'region', or 'datacenter'.",
  ),
});

const GettcpflowprotectionruleSchema = z.object({
  burst_sensitivity: z.string().describe(
    "The burst sensitivity. Must be one of 'low', 'medium', 'high'.",
  ),
  created_on: z.string().describe(
    "The creation timestamp of the TCP Flow Protection rule.",
  ),
  id: z.string().describe("The unique ID of the TCP Flow Protection rule."),
  mode: z.string().describe(
    "The mode for TCP Flow Protection. Must be one of 'enabled', 'disabled', 'monitoring'.",
  ),
  modified_on: z.string().describe(
    "The last modification timestamp of the TCP Flow Protection rule.",
  ),
  name: z.string().describe(
    "The name of the TCP Flow Protection rule. Value is relative to the 'scope' setting. For 'global' ...",
  ),
  rate_sensitivity: z.string().describe(
    "The rate sensitivity. Must be one of 'low', 'medium', 'high'.",
  ),
  scope: z.string().describe(
    "The scope for the TCP Flow Protection rule. Must be one of 'global', 'region', or 'datacenter'.",
  ),
});

const UpdatetcpflowprotectionruleSchema = z.object({
  burst_sensitivity: z.string().describe(
    "The burst sensitivity. Must be one of 'low', 'medium', 'high'.",
  ),
  created_on: z.string().describe(
    "The creation timestamp of the TCP Flow Protection rule.",
  ),
  id: z.string().describe("The unique ID of the TCP Flow Protection rule."),
  mode: z.string().describe(
    "The mode for TCP Flow Protection. Must be one of 'enabled', 'disabled', 'monitoring'.",
  ),
  modified_on: z.string().describe(
    "The last modification timestamp of the TCP Flow Protection rule.",
  ),
  name: z.string().describe(
    "The name of the TCP Flow Protection rule. Value is relative to the 'scope' setting. For 'global' ...",
  ),
  rate_sensitivity: z.string().describe(
    "The rate sensitivity. Must be one of 'low', 'medium', 'high'.",
  ),
  scope: z.string().describe(
    "The scope for the TCP Flow Protection rule. Must be one of 'global', 'region', or 'datacenter'.",
  ),
});

const GetprotectionstatusSchema = z.object({
  enabled: z.boolean(),
});

const UpdateprotectionstatusSchema = z.object({
  enabled: z.boolean(),
});

const AppsItemSchema = z.union([z.unknown(), z.unknown()]);

const ListAppsSchema = z.object({
  items: z.array(AppsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateMagicAccountAppsAddAppSchema = z.object({
  account_app_id: z.unknown(),
  hostnames: z.unknown().optional(),
  ip_subnets: z.unknown().optional(),
  name: z.unknown().optional(),
  source_subnets: z.unknown().optional(),
  type: z.unknown().optional(),
});

const UpdateAppSchema = z.object({
  account_app_id: z.unknown(),
  hostnames: z.unknown().optional(),
  ip_subnets: z.unknown().optional(),
  name: z.unknown().optional(),
  source_subnets: z.unknown().optional(),
  type: z.unknown().optional(),
});

const PatchAppSchema = z.object({
  account_app_id: z.unknown(),
  hostnames: z.unknown().optional(),
  ip_subnets: z.unknown().optional(),
  name: z.unknown().optional(),
  source_subnets: z.unknown().optional(),
  type: z.unknown().optional(),
});

const FilterProfilesItemSchema = z.object({
  created_on: z.string().optional(),
  description: z.string().max(1024).describe(
    "Description of the filter profile",
  ),
  id: z.unknown(),
  match_action: z.unknown(),
  modified_on: z.string().optional(),
  name: z.string().min(1).max(255).describe(
    "Friendly name for the filter profile",
  ),
  targets: z.array(z.string()).describe(
    "List of CIDR prefixes. Each entry may carry an optional suffix that specifies which prefix length...",
  ),
});

const ListFilterProfilesSchema = z.object({
  items: z.array(FilterProfilesItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateFilterProfileSchema = z.object({
  created_on: z.string().optional(),
  description: z.string().max(1024).describe(
    "Description of the filter profile",
  ),
  id: z.unknown(),
  match_action: z.unknown(),
  modified_on: z.string().optional(),
  name: z.string().min(1).max(255).describe(
    "Friendly name for the filter profile",
  ),
  targets: z.array(z.string()).describe(
    "List of CIDR prefixes. Each entry may carry an optional suffix that specifies which prefix length...",
  ),
});

const GetSettingsSchema = z.object({
  cloudflare_asn: z.number().int().min(1).describe(
    "Route advertisements from Cloudflare to ramps in this account will use this ASN.",
  ),
  modified_on: z.string().optional(),
  redistribute: z.unknown(),
});

const Cf1SitesItemSchema = z.object({
  created_on: z.string().optional(),
  description: z.string().optional().describe(
    "A human-provided description of the CF1 Site.",
  ),
  id: z.unknown().optional(),
  location: z.unknown().optional(),
  modified_on: z.string().optional(),
  name: z.string().describe(
    "A human-provided name describing the CF1 Site that should be unique within the account.",
  ),
});

const ListCf1SitesSchema = z.object({
  items: z.array(Cf1SitesItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetCf1SiteSchema = z.object({
  created_on: z.string().optional(),
  description: z.string().optional().describe(
    "A human-provided description of the CF1 Site.",
  ),
  id: z.unknown().optional(),
  location: z.unknown().optional(),
  modified_on: z.string().optional(),
  name: z.string().describe(
    "A human-provided name describing the CF1 Site that should be unique within the account.",
  ),
});

const Cf1SiteRampsItemSchema = z.union([
  z.unknown(),
  z.unknown(),
  z.unknown(),
  z.unknown(),
  z.unknown(),
]);

const ListCf1SiteRampsSchema = z.object({
  items: z.array(Cf1SiteRampsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetCf1SiteRampSchema = z.union([
  z.unknown(),
  z.unknown(),
  z.unknown(),
  z.unknown(),
  z.unknown(),
]);

const ListInterconnectsSchema = z.object({
  interconnects: z.array(z.unknown()).optional(),
});

const UpdateMultipleInterconnectsSchema = z.object({
  modified: z.boolean().optional(),
  modified_interconnects: z.array(z.unknown()).optional(),
});

const ListInterconnectDetailsSchema = z.object({
  interconnect: z.unknown().optional(),
});

const UpdateInterconnectSchema = z.object({
  modified: z.boolean().optional(),
  modified_interconnect: z.unknown().optional(),
});

const ListItemSchema = z.object({
  description: z.string(),
  destination_id: z.unknown(),
  destination_type: z.unknown(),
  errors: z.record(z.string(), z.unknown()).optional(),
  id: z.unknown(),
  includes_discoveries_until: z.string().optional(),
  last_attempted_update_at: z.string().optional(),
  last_successful_update_at: z.string().optional(),
  last_user_update_at: z.string(),
  name: z.string(),
  policy: z.string(),
  update_mode: z.unknown(),
});

const ListSchema = z.object({
  items: z.array(ListItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateSchema = z.object({
  description: z.string(),
  destination_id: z.unknown(),
  destination_type: z.unknown(),
  errors: z.record(z.string(), z.unknown()).optional(),
  id: z.unknown(),
  includes_discoveries_until: z.string().optional(),
  last_attempted_update_at: z.string().optional(),
  last_successful_update_at: z.string().optional(),
  last_user_update_at: z.string(),
  name: z.string(),
  policy: z.string(),
  update_mode: z.unknown(),
});

const GetCatalogSyncsReadSchema = z.object({
  description: z.string(),
  destination_id: z.unknown(),
  destination_type: z.unknown(),
  errors: z.record(z.string(), z.unknown()).optional(),
  id: z.unknown(),
  includes_discoveries_until: z.string().optional(),
  last_attempted_update_at: z.string().optional(),
  last_successful_update_at: z.string().optional(),
  last_user_update_at: z.string(),
  name: z.string(),
  policy: z.string(),
  update_mode: z.unknown(),
});

const UpdateSchema = z.object({
  description: z.string(),
  destination_id: z.unknown(),
  destination_type: z.unknown(),
  errors: z.record(z.string(), z.unknown()).optional(),
  id: z.unknown(),
  includes_discoveries_until: z.string().optional(),
  last_attempted_update_at: z.string().optional(),
  last_successful_update_at: z.string().optional(),
  last_user_update_at: z.string(),
  name: z.string(),
  policy: z.string(),
  update_mode: z.unknown(),
});

const PatchSchema = z.object({
  description: z.string(),
  destination_id: z.unknown(),
  destination_type: z.unknown(),
  errors: z.record(z.string(), z.unknown()).optional(),
  id: z.unknown(),
  includes_discoveries_until: z.string().optional(),
  last_attempted_update_at: z.string().optional(),
  last_successful_update_at: z.string().optional(),
  last_user_update_at: z.string(),
  name: z.string(),
  policy: z.string(),
  update_mode: z.unknown(),
});

const CatalogSyncsRefreshSchema = z.string();

const GetOnrampsMwanAddrSpaceReadSchema = z.object({
  prefixes: z.array(z.unknown()),
});

const GetOnrampsReadSchema = z.object({
  attached_hubs: z.array(z.unknown()).optional(),
  attached_vpcs: z.array(z.unknown()).optional(),
  cloud_asn: z.number().int().optional(),
  cloud_type: z.unknown(),
  description: z.string().optional(),
  dynamic_routing: z.boolean(),
  hub: z.unknown().optional(),
  id: z.unknown(),
  install_routes_in_cloud: z.boolean(),
  install_routes_in_magic_wan: z.boolean(),
  last_applied_at: z.string().optional(),
  last_exported_at: z.string().optional(),
  last_planned_at: z.string().optional(),
  manage_hub_to_hub_attachments: z.boolean().optional(),
  manage_vpc_to_hub_attachments: z.boolean().optional(),
  name: z.string(),
  planned_monthly_cost_estimate: z.unknown().optional(),
  planned_resources: z.array(z.unknown()).optional(),
  planned_resources_unavailable: z.boolean().optional(),
  post_apply_monthly_cost_estimate: z.unknown().optional(),
  post_apply_resources: z.record(z.string(), z.unknown()).optional(),
  post_apply_resources_unavailable: z.boolean().optional(),
  region: z.string().optional(),
  status: z.unknown().optional(),
  type: z.unknown(),
  updated_at: z.string(),
  vpc: z.unknown().optional(),
  vpcs_by_id: z.record(z.string(), z.unknown()).optional(),
  vpcs_by_id_unavailable: z.array(z.unknown()).optional().describe(
    "The list of vpc IDs for which resource details failed to generate.",
  ),
});

const GetProvidersReadSchema = z.object({
  aws_arn: z.string().optional(),
  azure_subscription_id: z.string().optional(),
  azure_tenant_id: z.string().optional(),
  cloud_type: z.unknown(),
  description: z.string().optional(),
  friendly_name: z.string(),
  gcp_project_id: z.string().optional(),
  gcp_service_account_email: z.string().optional(),
  id: z.unknown(),
  last_updated: z.string(),
  lifecycle_state: z.unknown(),
  state: z.unknown(),
  state_v2: z.unknown(),
  status: z.unknown().optional(),
});

const GetProvidersInitialSetupSchema = z.union([
  z.unknown(),
  z.unknown(),
  z.unknown(),
]);

const CreateResourcesCatalogPolicyPreviewSchema = z.string();

const GetResourcesCatalogReadSchema = z.object({
  account_id: z.unknown(),
  cloud_type: z.unknown(),
  config: z.record(z.string(), z.unknown()),
  deployment_provider: z.unknown(),
  id: z.unknown(),
  managed: z.boolean(),
  managed_by: z.array(z.unknown()).optional(),
  monthly_cost_estimate: z.unknown(),
  name: z.string(),
  native_id: z.string(),
  observations: z.record(z.string(), z.unknown()),
  provider_ids: z.array(z.unknown()),
  provider_names_by_id: z.record(z.string(), z.string()),
  region: z.string(),
  resource_group: z.string(),
  resource_type: z.unknown(),
  sections: z.array(z.unknown()),
  state: z.record(z.string(), z.unknown()),
  tags: z.record(z.string(), z.string()),
  updated_at: z.string(),
  url: z.string(),
});

const GetSchema = z.object({
  activated: z.boolean(),
  device: z.unknown().optional(),
  id: z.unknown(),
  interrupt_window_days_of_week: z.array(z.unknown()).describe(
    "Allowed days of the week for upgrades. Default is all days.",
  ),
  interrupt_window_duration_hours: z.number().min(1).max(24),
  interrupt_window_embargo_dates: z.array(z.unknown()).describe(
    "List of dates (YYYY-MM-DD) when upgrades are blocked.",
  ),
  interrupt_window_hour_of_day: z.number(),
  last_heartbeat: z.string().optional(),
  last_seen_version: z.string().optional(),
  last_updated: z.string(),
  license_key: z.string().optional(),
  notes: z.string(),
  primary: z.boolean(),
  site_id: z.string().optional(),
  timezone: z.string(),
});

const UpdateMconnConnectorsEditSchema = z.object({
  activated: z.boolean(),
  device: z.unknown().optional(),
  id: z.unknown(),
  interrupt_window_days_of_week: z.array(z.unknown()).describe(
    "Allowed days of the week for upgrades. Default is all days.",
  ),
  interrupt_window_duration_hours: z.number().min(1).max(24),
  interrupt_window_embargo_dates: z.array(z.unknown()).describe(
    "List of dates (YYYY-MM-DD) when upgrades are blocked.",
  ),
  interrupt_window_hour_of_day: z.number(),
  last_heartbeat: z.string().optional(),
  last_seen_version: z.string().optional(),
  last_updated: z.string(),
  license_key: z.string().optional(),
  notes: z.string(),
  primary: z.boolean(),
  site_id: z.string().optional(),
  timezone: z.string(),
});

const ListGreTunnelsSchema = z.object({
  gre_tunnels: z.array(z.unknown()).optional(),
});

const CreateGreTunnelsSchema = z.object({
  automatic_return_routing: z.unknown().optional(),
  bgp: z.unknown().optional(),
  bgp_status: z.unknown().optional(),
  cloudflare_gre_endpoint: z.unknown(),
  created_on: z.unknown().optional(),
  customer_gre_endpoint: z.unknown(),
  description: z.unknown().optional(),
  health_check: z.unknown().optional(),
  id: z.unknown(),
  interface_address: z.unknown(),
  interface_address6: z.unknown().optional(),
  modified_on: z.unknown().optional(),
  mtu: z.unknown().optional(),
  name: z.unknown(),
  ttl: z.unknown().optional(),
});

const UpdateMultipleGreTunnelsSchema = z.object({
  modified: z.boolean().optional(),
  modified_gre_tunnels: z.array(z.unknown()).optional(),
});

const ListGreTunnelDetailsSchema = z.object({
  gre_tunnel: z.unknown().optional(),
});

const UpdateGreTunnelSchema = z.object({
  modified: z.boolean().optional(),
  modified_gre_tunnel: z.unknown().optional(),
});

const ListIpsecTunnelsSchema = z.object({
  ipsec_tunnels: z.array(z.unknown()).optional(),
});

const CreateIpsecTunnelsSchema = z.object({
  allow_null_cipher: z.unknown().optional(),
  automatic_return_routing: z.unknown().optional(),
  bgp: z.unknown().optional(),
  bgp_status: z.unknown().optional(),
  cloudflare_endpoint: z.unknown(),
  created_on: z.unknown().optional(),
  custom_remote_identities: z.unknown().optional(),
  customer_endpoint: z.unknown().optional(),
  description: z.unknown().optional(),
  health_check: z.unknown().optional(),
  id: z.unknown(),
  interface_address: z.unknown(),
  interface_address6: z.unknown().optional(),
  modified_on: z.unknown().optional(),
  name: z.unknown(),
  psk_metadata: z.unknown().optional(),
  replay_protection: z.unknown().optional(),
});

const UpdateMultipleIpsecTunnelsSchema = z.object({
  modified: z.boolean().optional(),
  modified_ipsec_tunnels: z.array(z.unknown()).optional(),
});

const CreateMagicIpsecTunnelsSetPreSharedKeysForIpsecTunnelsSchema = z.object({
  successfully_applied_psks: z.record(z.string(), z.unknown()).optional()
    .describe("Map of tunnel IDs to successfully applied PSK details."),
  unapplied_psks: z.record(z.string(), z.string()).optional().describe(
    "Map of tunnel IDs to failure reasons for PSKs that could not be applied.",
  ),
});

const ListIpsecTunnelDetailsSchema = z.object({
  ipsec_tunnel: z.unknown().optional(),
});

const UpdateIpsecTunnelSchema = z.object({
  modified: z.boolean().optional(),
  modified_ipsec_tunnel: z.unknown().optional(),
});

const MagicIpsecTunnelsGeneratePreSharedKeyPskForIpsecTunnelsSchema = z.object({
  ipsec_tunnel_id: z.unknown().optional(),
  psk: z.unknown().optional(),
  psk_metadata: z.unknown().optional(),
});

const ListRedundancyGroupsSchema = z.object({
  redundancy_groups: z.array(z.unknown()).optional(),
});

const CreateRedundancyGroupSchema = z.object({
  created_on: z.string().optional(),
  description: z.string().optional().describe("Optional description"),
  id: z.string().optional().describe("Redundancy group identifier"),
  modified_on: z.string().optional(),
  name: z.string().optional().describe("Human-readable name"),
});

const ListRoutesSchema = z.object({
  routes: z.array(z.unknown()).optional(),
});

const CreateRoutesSchema = z.object({
  created_on: z.unknown().optional(),
  description: z.unknown().optional(),
  id: z.unknown(),
  modified_on: z.unknown().optional(),
  nexthop: z.unknown(),
  prefix: z.unknown(),
  priority: z.unknown(),
  scope: z.unknown().optional(),
  weight: z.unknown().optional(),
});

const UpdateManyRoutesSchema = z.object({
  modified: z.boolean().optional(),
  modified_routes: z.array(z.unknown()).optional(),
});

const GetMagicStaticRoutesRouteDetailsSchema = z.object({
  route: z.unknown().optional(),
});

const UpdateRouteSchema = z.object({
  modified: z.boolean().optional(),
  modified_route: z.unknown().optional(),
});

const SitesItemSchema = z.object({
  connector_id: z.unknown().optional(),
  description: z.string().optional(),
  ha_mode: z.boolean().optional().describe(
    "Site high availability mode. If set to true, the site can have two connectors and runs in high av...",
  ),
  id: z.unknown().optional(),
  location: z.unknown().optional(),
  name: z.unknown().optional(),
  secondary_connector_id: z.unknown().optional(),
});

const ListSitesSchema = z.object({
  items: z.array(SitesItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateSiteSchema = z.object({
  connector_id: z.unknown().optional(),
  description: z.string().optional(),
  ha_mode: z.boolean().optional().describe(
    "Site high availability mode. If set to true, the site can have two connectors and runs in high av...",
  ),
  id: z.unknown().optional(),
  location: z.unknown().optional(),
  name: z.unknown().optional(),
  secondary_connector_id: z.unknown().optional(),
});

const GetMagicSitesSiteDetailsSchema = z.object({
  connector_id: z.unknown().optional(),
  description: z.string().optional(),
  ha_mode: z.boolean().optional().describe(
    "Site high availability mode. If set to true, the site can have two connectors and runs in high av...",
  ),
  id: z.unknown().optional(),
  location: z.unknown().optional(),
  name: z.unknown().optional(),
  secondary_connector_id: z.unknown().optional(),
});

const PatchSiteSchema = z.object({
  connector_id: z.unknown().optional(),
  description: z.string().optional(),
  ha_mode: z.boolean().optional().describe(
    "Site high availability mode. If set to true, the site can have two connectors and runs in high av...",
  ),
  id: z.unknown().optional(),
  location: z.unknown().optional(),
  name: z.unknown().optional(),
  secondary_connector_id: z.unknown().optional(),
});

const AclsItemSchema = z.object({
  description: z.string().optional().describe("Description for the ACL."),
  forward_locally: z.unknown().optional(),
  id: z.unknown().optional(),
  lan_1: z.unknown().optional(),
  lan_2: z.unknown().optional(),
  name: z.string().optional().describe("The name of the ACL."),
  protocols: z.array(z.enum(["tcp", "udp", "icmp"])).optional(),
  unidirectional: z.unknown().optional(),
});

const ListAclsSchema = z.object({
  items: z.array(AclsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateAclSchema = z.object({
  description: z.string().optional().describe("Description for the ACL."),
  forward_locally: z.unknown().optional(),
  id: z.unknown().optional(),
  lan_1: z.unknown().optional(),
  lan_2: z.unknown().optional(),
  name: z.string().optional().describe("The name of the ACL."),
  protocols: z.array(z.enum(["tcp", "udp", "icmp"])).optional(),
  unidirectional: z.unknown().optional(),
});

const GetMagicSiteAclsAclDetailsSchema = z.object({
  description: z.string().optional().describe("Description for the ACL."),
  forward_locally: z.unknown().optional(),
  id: z.unknown().optional(),
  lan_1: z.unknown().optional(),
  lan_2: z.unknown().optional(),
  name: z.string().optional().describe("The name of the ACL."),
  protocols: z.array(z.enum(["tcp", "udp", "icmp"])).optional(),
  unidirectional: z.unknown().optional(),
});

const PatchAclSchema = z.object({
  description: z.string().optional().describe("Description for the ACL."),
  forward_locally: z.unknown().optional(),
  id: z.unknown().optional(),
  lan_1: z.unknown().optional(),
  lan_2: z.unknown().optional(),
  name: z.string().optional().describe("The name of the ACL."),
  protocols: z.array(z.enum(["tcp", "udp", "icmp"])).optional(),
  unidirectional: z.unknown().optional(),
});

const AppConfigsItemSchema = z.object({
  breakout: z.unknown().optional(),
  id: z.unknown(),
  preferred_wans: z.unknown().optional(),
  priority: z.unknown().optional(),
  site_id: z.unknown(),
});

const ListAppConfigsSchema = z.object({
  items: z.array(AppConfigsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateMagicSiteAppConfigsAddAppConfigSchema = z.object({
  breakout: z.unknown().optional(),
  id: z.unknown(),
  preferred_wans: z.unknown().optional(),
  priority: z.unknown().optional(),
  site_id: z.unknown(),
});

const UpdateAppConfigSchema = z.object({
  breakout: z.unknown().optional(),
  id: z.unknown(),
  preferred_wans: z.unknown().optional(),
  priority: z.unknown().optional(),
  site_id: z.unknown(),
});

const PatchAppConfigSchema = z.object({
  breakout: z.unknown().optional(),
  id: z.unknown(),
  preferred_wans: z.unknown().optional(),
  priority: z.unknown().optional(),
  site_id: z.unknown(),
});

const LansItemSchema = z.object({
  bond_id: z.unknown().optional(),
  ha_link: z.boolean().optional().describe(
    "mark true to use this LAN for HA probing. only works for site with HA turned on. only one LAN can...",
  ),
  id: z.unknown().optional(),
  is_breakout: z.boolean().optional().describe(
    "mark true to use this LAN for source-based breakout traffic",
  ),
  is_prioritized: z.boolean().optional().describe(
    "mark true to use this LAN for source-based prioritized traffic",
  ),
  name: z.string().optional(),
  nat: z.unknown().optional(),
  physport: z.unknown().optional(),
  routed_subnets: z.array(z.unknown()).optional(),
  site_id: z.unknown().optional(),
  static_addressing: z.unknown().optional(),
  vlan_tag: z.unknown().optional(),
});

const ListLansSchema = z.object({
  items: z.array(LansItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateLanSchema = z.object({
  bond_id: z.unknown().optional(),
  ha_link: z.boolean().optional().describe(
    "mark true to use this LAN for HA probing. only works for site with HA turned on. only one LAN can...",
  ),
  id: z.unknown().optional(),
  is_breakout: z.boolean().optional().describe(
    "mark true to use this LAN for source-based breakout traffic",
  ),
  is_prioritized: z.boolean().optional().describe(
    "mark true to use this LAN for source-based prioritized traffic",
  ),
  name: z.string().optional(),
  nat: z.unknown().optional(),
  physport: z.unknown().optional(),
  routed_subnets: z.array(z.unknown()).optional(),
  site_id: z.unknown().optional(),
  static_addressing: z.unknown().optional(),
  vlan_tag: z.unknown().optional(),
});

const GetMagicSiteLansLanDetailsSchema = z.object({
  bond_id: z.unknown().optional(),
  ha_link: z.boolean().optional().describe(
    "mark true to use this LAN for HA probing. only works for site with HA turned on. only one LAN can...",
  ),
  id: z.unknown().optional(),
  is_breakout: z.boolean().optional().describe(
    "mark true to use this LAN for source-based breakout traffic",
  ),
  is_prioritized: z.boolean().optional().describe(
    "mark true to use this LAN for source-based prioritized traffic",
  ),
  name: z.string().optional(),
  nat: z.unknown().optional(),
  physport: z.unknown().optional(),
  routed_subnets: z.array(z.unknown()).optional(),
  site_id: z.unknown().optional(),
  static_addressing: z.unknown().optional(),
  vlan_tag: z.unknown().optional(),
});

const PatchLanSchema = z.object({
  bond_id: z.unknown().optional(),
  ha_link: z.boolean().optional().describe(
    "mark true to use this LAN for HA probing. only works for site with HA turned on. only one LAN can...",
  ),
  id: z.unknown().optional(),
  is_breakout: z.boolean().optional().describe(
    "mark true to use this LAN for source-based breakout traffic",
  ),
  is_prioritized: z.boolean().optional().describe(
    "mark true to use this LAN for source-based prioritized traffic",
  ),
  name: z.string().optional(),
  nat: z.unknown().optional(),
  physport: z.unknown().optional(),
  routed_subnets: z.array(z.unknown()).optional(),
  site_id: z.unknown().optional(),
  static_addressing: z.unknown().optional(),
  vlan_tag: z.unknown().optional(),
});

const GetMagicSiteNetflowConfigDetailsSchema = z.object({
  active_timeout: z.number().int().min(1).max(5400).optional().describe(
    "Timeout in seconds for active flows (defaults to 30).",
  ),
  collector_ip: z.string().describe("IPv4 address of the NetFlow collector."),
  collector_port: z.number().int().min(1).max(65535).optional().describe(
    "UDP port of the NetFlow collector (defaults to 2055).",
  ),
  inactive_timeout: z.number().int().min(1).max(5400).optional().describe(
    "Timeout in seconds for inactive flows (defaults to 15).",
  ),
  sampling_rate: z.number().int().min(1).max(10000).optional().describe(
    "Sampling rate for NetFlow records (1 = every packet, 1000 = 1 in 1000 packets). Defaults to 1.",
  ),
});

const CreateNetflowConfigSchema = z.object({
  active_timeout: z.number().int().min(1).max(5400).optional().describe(
    "Timeout in seconds for active flows (defaults to 30).",
  ),
  collector_ip: z.string().describe("IPv4 address of the NetFlow collector."),
  collector_port: z.number().int().min(1).max(65535).optional().describe(
    "UDP port of the NetFlow collector (defaults to 2055).",
  ),
  inactive_timeout: z.number().int().min(1).max(5400).optional().describe(
    "Timeout in seconds for inactive flows (defaults to 15).",
  ),
  sampling_rate: z.number().int().min(1).max(10000).optional().describe(
    "Sampling rate for NetFlow records (1 = every packet, 1000 = 1 in 1000 packets). Defaults to 1.",
  ),
});

const PatchNetflowConfigSchema = z.object({
  active_timeout: z.number().int().min(1).max(5400).optional().describe(
    "Timeout in seconds for active flows (defaults to 30).",
  ),
  collector_ip: z.string().describe("IPv4 address of the NetFlow collector."),
  collector_port: z.number().int().min(1).max(65535).optional().describe(
    "UDP port of the NetFlow collector (defaults to 2055).",
  ),
  inactive_timeout: z.number().int().min(1).max(5400).optional().describe(
    "Timeout in seconds for inactive flows (defaults to 15).",
  ),
  sampling_rate: z.number().int().min(1).max(10000).optional().describe(
    "Sampling rate for NetFlow records (1 = every packet, 1000 = 1 in 1000 packets). Defaults to 1.",
  ),
});

const WansItemSchema = z.object({
  health_check_rate: z.enum(["low", "mid", "high"]).optional().describe(
    "Magic WAN health check rate for tunnels created on this link. The default value is `mid`.",
  ),
  id: z.unknown().optional(),
  name: z.string().optional(),
  physport: z.unknown().optional(),
  priority: z.number().int().optional().describe(
    "Priority of WAN for traffic loadbalancing.",
  ),
  site_id: z.unknown().optional(),
  static_addressing: z.unknown().optional(),
  vlan_tag: z.unknown().optional(),
});

const ListWansSchema = z.object({
  items: z.array(WansItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateWanSchema = z.object({
  health_check_rate: z.enum(["low", "mid", "high"]).optional().describe(
    "Magic WAN health check rate for tunnels created on this link. The default value is `mid`.",
  ),
  id: z.unknown().optional(),
  name: z.string().optional(),
  physport: z.unknown().optional(),
  priority: z.number().int().optional().describe(
    "Priority of WAN for traffic loadbalancing.",
  ),
  site_id: z.unknown().optional(),
  static_addressing: z.unknown().optional(),
  vlan_tag: z.unknown().optional(),
});

const GetMagicSiteWansWanDetailsSchema = z.object({
  health_check_rate: z.enum(["low", "mid", "high"]).optional().describe(
    "Magic WAN health check rate for tunnels created on this link. The default value is `mid`.",
  ),
  id: z.unknown().optional(),
  name: z.string().optional(),
  physport: z.unknown().optional(),
  priority: z.number().int().optional().describe(
    "Priority of WAN for traffic loadbalancing.",
  ),
  site_id: z.unknown().optional(),
  static_addressing: z.unknown().optional(),
  vlan_tag: z.unknown().optional(),
});

const PatchWanSchema = z.object({
  health_check_rate: z.enum(["low", "mid", "high"]).optional().describe(
    "Magic WAN health check rate for tunnels created on this link. The default value is `mid`.",
  ),
  id: z.unknown().optional(),
  name: z.string().optional(),
  physport: z.unknown().optional(),
  priority: z.number().int().optional().describe(
    "Priority of WAN for traffic loadbalancing.",
  ),
  site_id: z.unknown().optional(),
  static_addressing: z.unknown().optional(),
  vlan_tag: z.unknown().optional(),
});

// =============================================================================
// Model Definition
// =============================================================================

/** Cloudflare Magic Transit — GRE tunnels, static routes, health checks, IPsec */
export const model = {
  type: "@webframp/cloudflare/magic-transit",
  version: "2026.07.19.1",
  globalArguments: GlobalArgsSchema,

  upgrades: [],

  resources: {
    "listdnsprotectionrulesforaccount": {
      description: "List all DNS Protection rules.",
      schema: ListdnsprotectionrulesforaccountSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "creatednsprotectionrule": {
      description: "Create DNS Protection rule.",
      schema: CreatednsprotectionruleSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "getdnsprotectionrule": {
      description: "Get DNS Protection rule.",
      schema: GetdnsprotectionruleSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "updatednsprotectionrule": {
      description: "Update DNS Protection rule.",
      schema: UpdatednsprotectionruleSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "listallowlistprefixesforaccount": {
      description: "List all allowlist prefixes.",
      schema: ListallowlistprefixesforaccountSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "createallowlistedprefix": {
      description: "Create allowlist prefix.",
      schema: CreateallowlistedprefixSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "getallowlistprefix": {
      description: "Get allowlist prefix.",
      schema: GetallowlistprefixSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "updateallowlistprefix": {
      description: "Update allowlist prefix.",
      schema: UpdateallowlistprefixSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "listprefixesforaccount": {
      description: "List all prefixes.",
      schema: ListprefixesforaccountSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "createprefix": {
      description: "Create prefix.",
      schema: CreateprefixSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "bulkcreateprefixes": {
      description: "Create multiple prefixes.",
      schema: BulkcreateprefixesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "getprefix": {
      description: "Get prefix.",
      schema: GetprefixSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "updateprefix": {
      description: "Update prefix.",
      schema: UpdateprefixSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "listsynprotectionfiltersforaccount": {
      description: "List all SYN Protection filters.",
      schema: ListsynprotectionfiltersforaccountSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "createsynprotectionfilter": {
      description: "Create a SYN Protection filter.",
      schema: CreatesynprotectionfilterSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "getsynprotectionfilter": {
      description: "Get SYN Protection filter.",
      schema: GetsynprotectionfilterSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "updatesynprotectionfilter": {
      description: "Update SYN Protection filter.",
      schema: UpdatesynprotectionfilterSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "listsynprotectionrulesforaccount": {
      description: "List all SYN Protection rules.",
      schema: ListsynprotectionrulesforaccountSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "createsynprotectionrule": {
      description: "Create SYN Protection rule.",
      schema: CreatesynprotectionruleSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "getsynprotectionrule": {
      description: "Get SYN Protection rule.",
      schema: GetsynprotectionruleSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "updatesynprotectionrule": {
      description: "Update SYN Protection rule.",
      schema: UpdatesynprotectionruleSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "listtcpflowprotectionfiltersforaccount": {
      description: "List all TCP Flow Protection filters.",
      schema: ListtcpflowprotectionfiltersforaccountSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "createtcpflowprotectionfilter": {
      description: "Create a TCP Flow Protection filter.",
      schema: CreatetcpflowprotectionfilterSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "gettcpflowprotectionfilter": {
      description: "Get TCP Flow Protection filter.",
      schema: GettcpflowprotectionfilterSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "updatetcpflowprotectionfilter": {
      description: "Update TCP Flow Protection filter.",
      schema: UpdatetcpflowprotectionfilterSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "listtcpflowprotectionrulesforaccount": {
      description: "List all TCP Flow Protection rules.",
      schema: ListtcpflowprotectionrulesforaccountSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "createtcpflowprotectionrule": {
      description: "Create TCP Flow Protection rule.",
      schema: CreatetcpflowprotectionruleSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "gettcpflowprotectionrule": {
      description: "Get TCP Flow Protection rule.",
      schema: GettcpflowprotectionruleSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "updatetcpflowprotectionrule": {
      description: "Update TCP Flow Protection rule.",
      schema: UpdatetcpflowprotectionruleSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "getprotectionstatus": {
      description: "Get protection status.",
      schema: GetprotectionstatusSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "updateprotectionstatus": {
      description: "Update protection status.",
      schema: UpdateprotectionstatusSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "apps": {
      description: "List Apps",
      schema: ListAppsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "magic_account_apps_add_app": {
      description: "Create a new App",
      schema: CreateMagicAccountAppsAddAppSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "app": {
      description: "Update an App",
      schema: UpdateAppSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "patch_app": {
      description: "Update an App",
      schema: PatchAppSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "filter_profiles": {
      description: "List BGP Filter Profiles",
      schema: ListFilterProfilesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "filter_profile": {
      description: "Create BGP Filter Profile",
      schema: CreateFilterProfileSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "settings": {
      description: "Get BGP Settings",
      schema: GetSettingsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "cf1_sites": {
      description: "List CF1 Sites",
      schema: ListCf1SitesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "cf1_site": {
      description: "Get CF1 Site",
      schema: GetCf1SiteSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "cf1_site_ramps": {
      description: "List CF1 Site Ramps",
      schema: ListCf1SiteRampsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "cf1_site_ramp": {
      description: "Get CF1 Site Ramp",
      schema: GetCf1SiteRampSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "list_interconnects": {
      description: "List interconnects",
      schema: ListInterconnectsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "multiple_interconnects": {
      description: "Update multiple interconnects",
      schema: UpdateMultipleInterconnectsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "list_interconnect_details": {
      description: "List interconnect Details",
      schema: ListInterconnectDetailsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "interconnect": {
      description: "Update interconnect",
      schema: UpdateInterconnectSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "list": {
      description: "List Catalog Syncs",
      schema: ListSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "create": {
      description: "Create Catalog Sync",
      schema: CreateSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "catalog_syncs_read": {
      description: "Read Catalog Sync",
      schema: GetCatalogSyncsReadSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "update": {
      description: "Update Catalog Sync",
      schema: UpdateSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "patch": {
      description: "Patch Catalog Sync",
      schema: PatchSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "catalog_syncs_refresh": {
      description: "Run Catalog Sync",
      schema: CatalogSyncsRefreshSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "onramps_mwan_addr_space_read": {
      description: "Read Magic WAN Address Space",
      schema: GetOnrampsMwanAddrSpaceReadSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "onramps_read": {
      description: "Read On-ramp",
      schema: GetOnrampsReadSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "onramps_apply": {
      description: "Apply On-ramp",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "onramps_export": {
      description: "Export as Terraform",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "onramps_plan": {
      description: "Plan On-ramp",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "providers_discover_all": {
      description: "Run Discovery for All Integrations",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "providers_read": {
      description: "Read Cloud Integration",
      schema: GetProvidersReadSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "providers_discover": {
      description: "Run Discovery",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "providers_initial_setup": {
      description: "Get Cloud Integration Setup Config",
      schema: GetProvidersInitialSetupSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "resources_catalog_export": {
      description: "Export Resources",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "resources_catalog_policy_preview": {
      description: "Preview Rego Query",
      schema: CreateResourcesCatalogPolicyPreviewSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "resources_catalog_read": {
      description: "Read Resource",
      schema: GetResourcesCatalogReadSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "get": {
      description: "Get Connector",
      schema: GetSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "mconn_connectors_edit": {
      description: "Edit Connector",
      schema: UpdateMconnConnectorsEditSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "list_gre_tunnels": {
      description: "List GRE tunnels",
      schema: ListGreTunnelsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "gre_tunnels": {
      description: "Create a GRE tunnel",
      schema: CreateGreTunnelsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "multiple_gre_tunnels": {
      description: "Update multiple GRE tunnels",
      schema: UpdateMultipleGreTunnelsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "list_gre_tunnel_details": {
      description: "List GRE Tunnel Details",
      schema: ListGreTunnelDetailsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "gre_tunnel": {
      description: "Update GRE Tunnel",
      schema: UpdateGreTunnelSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "list_ipsec_tunnels": {
      description: "List IPsec tunnels",
      schema: ListIpsecTunnelsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "ipsec_tunnels": {
      description: "Create an IPsec tunnel",
      schema: CreateIpsecTunnelsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "multiple_ipsec_tunnels": {
      description: "Update multiple IPsec tunnels",
      schema: UpdateMultipleIpsecTunnelsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "magic_ipsec_tunnels_set_pre_shared_keys_for_ipsec_tunnels": {
      description: "Set Pre-Shared Keys (PSK) for IPsec tunnels",
      schema: CreateMagicIpsecTunnelsSetPreSharedKeysForIpsecTunnelsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "list_ipsec_tunnel_details": {
      description: "List IPsec tunnel details",
      schema: ListIpsecTunnelDetailsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "ipsec_tunnel": {
      description: "Update IPsec Tunnel",
      schema: UpdateIpsecTunnelSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "magic_ipsec_tunnels_generate_pre_shared_key_psk_for_ipsec_tunnels": {
      description: "Generate Pre-Shared Key (PSK) for IPsec tunnels",
      schema: MagicIpsecTunnelsGeneratePreSharedKeyPskForIpsecTunnelsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "list_redundancy_groups": {
      description: "List Redundancy Groups",
      schema: ListRedundancyGroupsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "redundancy_group": {
      description: "Create a Redundancy Group",
      schema: CreateRedundancyGroupSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "list_routes": {
      description: "List Routes",
      schema: ListRoutesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "routes": {
      description: "Create a Route",
      schema: CreateRoutesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "many_routes": {
      description: "Update Many Routes",
      schema: UpdateManyRoutesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "magic_static_routes_route_details": {
      description: "Route Details",
      schema: GetMagicStaticRoutesRouteDetailsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "route": {
      description: "Update Route",
      schema: UpdateRouteSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "sites": {
      description: "List Sites",
      schema: ListSitesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "site": {
      description: "Create a new Site",
      schema: CreateSiteSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "magic_sites_site_details": {
      description: "Site Details",
      schema: GetMagicSitesSiteDetailsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "patch_site": {
      description: "Patch Site",
      schema: PatchSiteSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "acls": {
      description: "List Site ACLs",
      schema: ListAclsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "acl": {
      description: "Create a new Site ACL",
      schema: CreateAclSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "magic_site_acls_acl_details": {
      description: "Site ACL Details",
      schema: GetMagicSiteAclsAclDetailsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "patch_acl": {
      description: "Patch Site ACL",
      schema: PatchAclSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "app_configs": {
      description: "List App Configs",
      schema: ListAppConfigsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "magic_site_app_configs_add_app_config": {
      description: "Create a new App Config",
      schema: CreateMagicSiteAppConfigsAddAppConfigSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "app_config": {
      description: "Update an App Config",
      schema: UpdateAppConfigSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "patch_app_config": {
      description: "Update an App Config",
      schema: PatchAppConfigSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "lans": {
      description: "List Site LANs",
      schema: ListLansSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "lan": {
      description: "Create a new Site LAN",
      schema: CreateLanSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "magic_site_lans_lan_details": {
      description: "Site LAN Details",
      schema: GetMagicSiteLansLanDetailsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "patch_lan": {
      description: "Patch Site LAN",
      schema: PatchLanSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "magic_site_netflow_config_details": {
      description: "NetFlow Configuration Details",
      schema: GetMagicSiteNetflowConfigDetailsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "netflow_config": {
      description: "Create NetFlow Configuration",
      schema: CreateNetflowConfigSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "patch_netflow_config": {
      description: "Update NetFlow Configuration",
      schema: PatchNetflowConfigSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "wans": {
      description: "List Site WANs",
      schema: ListWansSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "wan": {
      description: "Create a new Site WAN",
      schema: CreateWanSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "magic_site_wans_wan_details": {
      description: "Site WAN Details",
      schema: GetMagicSiteWansWanDetailsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "patch_wan": {
      description: "Patch Site WAN",
      schema: PatchWanSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
  },

  methods: {
    listdnsprotectionrulesforaccount: {
      description: "List all DNS Protection rules.",
      arguments: z.object({
        page: z.number().optional().describe(
          "The page number for pagination. Defaults to 1.",
        ),
        per_page: z.number().optional().describe(
          "The number of items per page. Must be between 10 and 1000. Defaults to 25.",
        ),
        order: z.string().optional().describe(
          "The field to order by. Defaults to 'prefix'.",
        ),
        direction: z.string().optional().describe(
          "The direction of ordering (ASC or DESC). Defaults to 'ASC'.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/magic/advanced_dns_protection/configs/dns_protection/rules`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "listdnsprotectionrulesforaccount",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} listdnsprotectionrulesforaccount", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    creatednsprotectionrule: {
      description: "Create DNS Protection rule.",
      arguments: z.object({
        burst_sensitivity: z.string().describe(
          "The burst sensitivity. Must be one of 'low', 'medium', 'high'.",
        ),
        mode: z.string().describe(
          "The mode for DNS Protection. Must be one of 'enabled', 'disabled', 'monitoring'.",
        ),
        name: z.string().describe(
          "The name of the DNS Protection rule. Value is relative to the 'scope' setting...",
        ),
        profile_sensitivity: z.string().describe(
          "The profile sensitivity. Recommended setting is 'low'. Must be one of 'low', ...",
        ),
        rate_sensitivity: z.string().describe(
          "The rate sensitivity. Must be one of 'low', 'medium', 'high'.",
        ),
        scope: z.string().describe(
          "The scope for the DNS Protection rule. Must be one of 'global', 'region', or ...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/magic/advanced_dns_protection/configs/dns_protection/rules`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "creatednsprotectionrule",
          id,
          result,
        );
        context.logger.info("Created creatednsprotectionrule {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    deletednsprotectionrulesforaccount: {
      description: "Delete all DNS Protection rules.",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/magic/advanced_dns_protection/configs/dns_protection/rules`,
        );

        context.logger.info("Deleted resource {id}", { id: "unknown" });
        return { dataHandles: [] };
      },
    },
    getdnsprotectionrule: {
      description: "Get DNS Protection rule.",
      arguments: z.object({
        rule_id: z.string().describe("The UUID of the DNS Protection rule."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/magic/advanced_dns_protection/configs/dns_protection/rules/${args.rule_id}`,
        );

        const handle = await context.writeResource(
          "getdnsprotectionrule",
          String(args.rule_id),
          result,
        );
        context.logger.info("Fetched getdnsprotectionrule", {});
        return { dataHandles: [handle] };
      },
    },
    updatednsprotectionrule: {
      description: "Update DNS Protection rule.",
      arguments: z.object({
        rule_id: z.string().describe(
          "The UUID of the DNS Protection rule to update.",
        ),
        burst_sensitivity: z.string().optional().describe(
          "The new burst sensitivity. Optional. Must be one of 'low', 'medium', 'high'.",
        ),
        mode: z.string().optional().describe(
          "The new mode for DNS Protection. Optional. Must be one of 'enabled', 'disable...",
        ),
        profile_sensitivity: z.string().optional().describe(
          "The new profile sensitivity. Optional. Recommended setting is 'low'. Must be ...",
        ),
        rate_sensitivity: z.string().optional().describe(
          "The new rate sensitivity. Optional. Must be one of 'low', 'medium', 'high'.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["rule_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/accounts/${accountId}/magic/advanced_dns_protection/configs/dns_protection/rules/${args.rule_id}`,
          body,
        );

        const handle = await context.writeResource(
          "updatednsprotectionrule",
          String(args.rule_id),
          result,
        );
        context.logger.info("Updated updatednsprotectionrule", {});
        return { dataHandles: [handle] };
      },
    },
    deletednsprotectionrule: {
      description: "Delete DNS Protection rule.",
      arguments: z.object({
        rule_id: z.string().describe(
          "The UUID of the DNS Protection rule to delete.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/magic/advanced_dns_protection/configs/dns_protection/rules/${args.rule_id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.rule_id });
        return { dataHandles: [] };
      },
    },
    listallowlistprefixesforaccount: {
      description: "List all allowlist prefixes.",
      arguments: z.object({
        page: z.number().optional().describe(
          "The page number for pagination. Defaults to 1.",
        ),
        per_page: z.number().optional().describe(
          "The number of items per page. Must be between 10 and 1000. Defaults to 25.",
        ),
        order: z.string().optional().describe(
          "The field to order by. Defaults to 'prefix'.",
        ),
        direction: z.string().optional().describe(
          "The direction of ordering (ASC or DESC). Defaults to 'ASC'.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/magic/advanced_tcp_protection/configs/allowlist`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "listallowlistprefixesforaccount",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} listallowlistprefixesforaccount", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    createallowlistedprefix: {
      description: "Create allowlist prefix.",
      arguments: z.object({
        comment: z.string().describe(
          "An comment describing the allowlist prefix.",
        ),
        enabled: z.boolean().describe(
          "Whether to enable the allowlist prefix into effect.",
        ),
        prefix: z.string().describe(
          "The allowlist prefix to add in CIDR format.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/magic/advanced_tcp_protection/configs/allowlist`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "createallowlistedprefix",
          id,
          result,
        );
        context.logger.info("Created createallowlistedprefix {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    deleteallowlistprefixesforaccount: {
      description: "Delete all allowlist prefixes.",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/magic/advanced_tcp_protection/configs/allowlist`,
        );

        context.logger.info("Deleted resource {id}", { id: "unknown" });
        return { dataHandles: [] };
      },
    },
    getallowlistprefix: {
      description: "Get allowlist prefix.",
      arguments: z.object({
        prefix_id: z.string().describe("The UUID of the allowlist prefix."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/magic/advanced_tcp_protection/configs/allowlist/${args.prefix_id}`,
        );

        const handle = await context.writeResource(
          "getallowlistprefix",
          String(args.prefix_id),
          result,
        );
        context.logger.info("Fetched getallowlistprefix", {});
        return { dataHandles: [handle] };
      },
    },
    updateallowlistprefix: {
      description: "Update allowlist prefix.",
      arguments: z.object({
        prefix_id: z.string().describe(
          "The UUID of the allowlist prefix to update.",
        ),
        comment: z.string().optional().describe(
          "A comment describing the allowlist prefix. Optional.",
        ),
        enabled: z.boolean().optional().describe(
          "Whether to enable the allowlist prefix into effect. Optional.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["prefix_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/accounts/${accountId}/magic/advanced_tcp_protection/configs/allowlist/${args.prefix_id}`,
          body,
        );

        const handle = await context.writeResource(
          "updateallowlistprefix",
          String(args.prefix_id),
          result,
        );
        context.logger.info("Updated updateallowlistprefix", {});
        return { dataHandles: [handle] };
      },
    },
    deleteallowlistprefix: {
      description: "Delete allowlist prefix.",
      arguments: z.object({
        prefix_id: z.string().describe(
          "The UUID of the allowlist prefix to delete.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/magic/advanced_tcp_protection/configs/allowlist/${args.prefix_id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.prefix_id });
        return { dataHandles: [] };
      },
    },
    listprefixesforaccount: {
      description: "List all prefixes.",
      arguments: z.object({
        page: z.number().optional().describe(
          "The page number for pagination. Defaults to 1.",
        ),
        per_page: z.number().optional().describe(
          "The number of items per page. Must be between 10 and 1000. Defaults to 25.",
        ),
        order: z.string().optional().describe(
          "The field to order by. Defaults to 'prefix'.",
        ),
        direction: z.string().optional().describe(
          "The direction of ordering (ASC or DESC). Defaults to 'ASC'.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/magic/advanced_tcp_protection/configs/prefixes`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "listprefixesforaccount",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} listprefixesforaccount", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    createprefix: {
      description: "Create prefix.",
      arguments: z.object({
        comment: z.string().describe("A comment describing the prefix."),
        excluded: z.boolean().describe(
          "Whether to exclude the prefix from protection.",
        ),
        prefix: z.string().describe("The prefix to add in CIDR format."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/magic/advanced_tcp_protection/configs/prefixes`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource("createprefix", id, result);
        context.logger.info("Created createprefix {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    deleteprefixesforaccount: {
      description: "Delete all prefixes.",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/magic/advanced_tcp_protection/configs/prefixes`,
        );

        context.logger.info("Deleted resource {id}", { id: "unknown" });
        return { dataHandles: [] };
      },
    },
    bulkcreateprefixes: {
      description: "Create multiple prefixes.",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/magic/advanced_tcp_protection/configs/prefixes/bulk`,
          args,
        );

        const handle = await context.writeResource(
          "bulkcreateprefixes",
          "latest",
          result ?? {},
        );
        context.logger.info("Executed bulkcreateprefixes", {});
        return { dataHandles: [handle] };
      },
    },
    getprefix: {
      description: "Get prefix.",
      arguments: z.object({
        prefix_id: z.string().describe("The UUID of the prefix."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/magic/advanced_tcp_protection/configs/prefixes/${args.prefix_id}`,
        );

        const handle = await context.writeResource(
          "getprefix",
          String(args.prefix_id),
          result,
        );
        context.logger.info("Fetched getprefix", {});
        return { dataHandles: [handle] };
      },
    },
    updateprefix: {
      description: "Update prefix.",
      arguments: z.object({
        prefix_id: z.string().describe("The UUID of the prefix to update."),
        comment: z.string().optional().describe(
          "A new comment for the prefix. Optional.",
        ),
        excluded: z.boolean().optional().describe(
          "Whether to exclude the prefix from protection. Optional.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["prefix_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/accounts/${accountId}/magic/advanced_tcp_protection/configs/prefixes/${args.prefix_id}`,
          body,
        );

        const handle = await context.writeResource(
          "updateprefix",
          String(args.prefix_id),
          result,
        );
        context.logger.info("Updated updateprefix", {});
        return { dataHandles: [handle] };
      },
    },
    deleteprefix: {
      description: "Delete prefix.",
      arguments: z.object({
        prefix_id: z.string().describe("The UUID of the prefix to delete."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/magic/advanced_tcp_protection/configs/prefixes/${args.prefix_id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.prefix_id });
        return { dataHandles: [] };
      },
    },
    listsynprotectionfiltersforaccount: {
      description: "List all SYN Protection filters.",
      arguments: z.object({
        mode: z.string().optional().describe(
          "The mode of the filters to get. Optional. Valid values: 'enabled', 'disabled', 'monitoring'.",
        ),
        page: z.number().optional().describe(
          "The page number for pagination. Defaults to 1.",
        ),
        per_page: z.number().optional().describe(
          "The number of items per page. Must be between 10 and 1000. Defaults to 25.",
        ),
        order: z.string().optional().describe(
          "The field to order by. Defaults to 'prefix'.",
        ),
        direction: z.string().optional().describe(
          "The direction of ordering (ASC or DESC). Defaults to 'ASC'.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/magic/advanced_tcp_protection/configs/syn_protection/filters`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "listsynprotectionfiltersforaccount",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info(
          "Found {count} listsynprotectionfiltersforaccount",
          { count: results.length },
        );
        return { dataHandles: [handle] };
      },
    },
    createsynprotectionfilter: {
      description: "Create a SYN Protection filter.",
      arguments: z.object({
        expression: z.string().describe("The filter expression."),
        mode: z.string().describe(
          "The filter's mode. Must be one of 'enabled', 'disabled', 'monitoring'.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/magic/advanced_tcp_protection/configs/syn_protection/filters`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "createsynprotectionfilter",
          id,
          result,
        );
        context.logger.info("Created createsynprotectionfilter {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    deletesynprotectionfiltersforaccount: {
      description: "Delete all SYN Protection filters.",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/magic/advanced_tcp_protection/configs/syn_protection/filters`,
        );

        context.logger.info("Deleted resource {id}", { id: "unknown" });
        return { dataHandles: [] };
      },
    },
    getsynprotectionfilter: {
      description: "Get SYN Protection filter.",
      arguments: z.object({
        filter_id: z.string().describe("The UUID of the filter to retrieve."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/magic/advanced_tcp_protection/configs/syn_protection/filters/${args.filter_id}`,
        );

        const handle = await context.writeResource(
          "getsynprotectionfilter",
          String(args.filter_id),
          result,
        );
        context.logger.info("Fetched getsynprotectionfilter", {});
        return { dataHandles: [handle] };
      },
    },
    updatesynprotectionfilter: {
      description: "Update SYN Protection filter.",
      arguments: z.object({
        filter_id: z.string().describe("The UUID of the filter to update."),
        expression: z.string().optional().describe(
          "The new filter expression. Optional.",
        ),
        mode: z.string().optional().describe(
          "The new mode for the filter. Optional. Must be one of 'enabled', 'disabled', ...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["filter_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/accounts/${accountId}/magic/advanced_tcp_protection/configs/syn_protection/filters/${args.filter_id}`,
          body,
        );

        const handle = await context.writeResource(
          "updatesynprotectionfilter",
          String(args.filter_id),
          result,
        );
        context.logger.info("Updated updatesynprotectionfilter", {});
        return { dataHandles: [handle] };
      },
    },
    deletesynprotectionfilter: {
      description: "Delete SYN Protection filter.",
      arguments: z.object({
        filter_id: z.string().describe("The UUID of the filter to delete."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/magic/advanced_tcp_protection/configs/syn_protection/filters/${args.filter_id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.filter_id });
        return { dataHandles: [] };
      },
    },
    listsynprotectionrulesforaccount: {
      description: "List all SYN Protection rules.",
      arguments: z.object({
        page: z.number().optional().describe(
          "The page number for pagination. Defaults to 1.",
        ),
        per_page: z.number().optional().describe(
          "The number of items per page. Must be between 10 and 1000. Defaults to 25.",
        ),
        order: z.string().optional().describe(
          "The field to order by. Defaults to 'prefix'.",
        ),
        direction: z.string().optional().describe(
          "The direction of ordering (ASC or DESC). Defaults to 'ASC'.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/magic/advanced_tcp_protection/configs/syn_protection/rules`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "listsynprotectionrulesforaccount",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} listsynprotectionrulesforaccount", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    createsynprotectionrule: {
      description: "Create SYN Protection rule.",
      arguments: z.object({
        burst_sensitivity: z.string().describe(
          "The burst sensitivity. Must be one of 'low', 'medium', 'high'.",
        ),
        mitigation_type: z.string().optional().describe(
          "The type of mitigation. Must be one of 'challenge' or 'retransmit'. Optional....",
        ),
        mode: z.string().describe(
          "The mode for SYN Protection. Must be one of 'enabled', 'disabled', 'monitoring'.",
        ),
        name: z.string().describe(
          "The name of the SYN Protection rule. Value is relative to the 'scope' setting...",
        ),
        rate_sensitivity: z.string().describe(
          "The rate sensitivity. Must be one of 'low', 'medium', 'high'.",
        ),
        scope: z.string().describe(
          "The scope for the SYN Protection rule. Must be one of 'global', 'region', or ...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/magic/advanced_tcp_protection/configs/syn_protection/rules`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "createsynprotectionrule",
          id,
          result,
        );
        context.logger.info("Created createsynprotectionrule {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    deletesynprotectionrulesforaccount: {
      description: "Delete all SYN Protection rules.",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/magic/advanced_tcp_protection/configs/syn_protection/rules`,
        );

        context.logger.info("Deleted resource {id}", { id: "unknown" });
        return { dataHandles: [] };
      },
    },
    getsynprotectionrule: {
      description: "Get SYN Protection rule.",
      arguments: z.object({
        rule_id: z.string().describe("The UUID of the SYN Protection rule."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/magic/advanced_tcp_protection/configs/syn_protection/rules/${args.rule_id}`,
        );

        const handle = await context.writeResource(
          "getsynprotectionrule",
          String(args.rule_id),
          result,
        );
        context.logger.info("Fetched getsynprotectionrule", {});
        return { dataHandles: [handle] };
      },
    },
    updatesynprotectionrule: {
      description: "Update SYN Protection rule.",
      arguments: z.object({
        rule_id: z.string().describe(
          "The UUID of the SYN Protection rule to update.",
        ),
        burst_sensitivity: z.string().optional().describe(
          "The new burst sensitivity. Optional. Must be one of 'low', 'medium', 'high'.",
        ),
        mitigation_type: z.string().optional().describe(
          "The new mitigation type. Optional. Must be one of 'challenge' or 'retransmit'.",
        ),
        mode: z.string().optional().describe(
          "The new mode for SYN Protection. Optional. Must be one of 'enabled', 'disable...",
        ),
        rate_sensitivity: z.string().optional().describe(
          "The new rate sensitivity. Optional. Must be one of 'low', 'medium', 'high'.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["rule_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/accounts/${accountId}/magic/advanced_tcp_protection/configs/syn_protection/rules/${args.rule_id}`,
          body,
        );

        const handle = await context.writeResource(
          "updatesynprotectionrule",
          String(args.rule_id),
          result,
        );
        context.logger.info("Updated updatesynprotectionrule", {});
        return { dataHandles: [handle] };
      },
    },
    deletesynprotectionrule: {
      description: "Delete SYN Protection rule.",
      arguments: z.object({
        rule_id: z.string().describe(
          "The UUID of the SYN Protection rule to delete.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/magic/advanced_tcp_protection/configs/syn_protection/rules/${args.rule_id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.rule_id });
        return { dataHandles: [] };
      },
    },
    listtcpflowprotectionfiltersforaccount: {
      description: "List all TCP Flow Protection filters.",
      arguments: z.object({
        mode: z.string().optional().describe(
          "The mode of the filters to get. Optional. Valid values: 'enabled', 'disabled', 'monitoring'.",
        ),
        page: z.number().optional().describe(
          "The page number for pagination. Defaults to 1.",
        ),
        per_page: z.number().optional().describe(
          "The number of items per page. Must be between 10 and 1000. Defaults to 25.",
        ),
        order: z.string().optional().describe(
          "The field to order by. Defaults to 'prefix'.",
        ),
        direction: z.string().optional().describe(
          "The direction of ordering (ASC or DESC). Defaults to 'ASC'.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/magic/advanced_tcp_protection/configs/tcp_flow_protection/filters`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "listtcpflowprotectionfiltersforaccount",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info(
          "Found {count} listtcpflowprotectionfiltersforaccount",
          { count: results.length },
        );
        return { dataHandles: [handle] };
      },
    },
    createtcpflowprotectionfilter: {
      description: "Create a TCP Flow Protection filter.",
      arguments: z.object({
        expression: z.string().describe("The filter expression."),
        mode: z.string().describe(
          "The filter's mode. Must be one of 'enabled', 'disabled', 'monitoring'.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/magic/advanced_tcp_protection/configs/tcp_flow_protection/filters`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "createtcpflowprotectionfilter",
          id,
          result,
        );
        context.logger.info("Created createtcpflowprotectionfilter {id}", {
          id,
        });
        return { dataHandles: [handle] };
      },
    },
    deletetcpflowprotectionfiltersforaccount: {
      description: "Delete all TCP Flow Protection filters.",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/magic/advanced_tcp_protection/configs/tcp_flow_protection/filters`,
        );

        context.logger.info("Deleted resource {id}", { id: "unknown" });
        return { dataHandles: [] };
      },
    },
    gettcpflowprotectionfilter: {
      description: "Get TCP Flow Protection filter.",
      arguments: z.object({
        filter_id: z.string().describe("The UUID of the filter to retrieve."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/magic/advanced_tcp_protection/configs/tcp_flow_protection/filters/${args.filter_id}`,
        );

        const handle = await context.writeResource(
          "gettcpflowprotectionfilter",
          String(args.filter_id),
          result,
        );
        context.logger.info("Fetched gettcpflowprotectionfilter", {});
        return { dataHandles: [handle] };
      },
    },
    updatetcpflowprotectionfilter: {
      description: "Update TCP Flow Protection filter.",
      arguments: z.object({
        filter_id: z.string().describe("The UUID of the filter to update."),
        expression: z.string().optional().describe(
          "The new filter expression. Optional.",
        ),
        mode: z.string().optional().describe(
          "The new mode for the filter. Optional. Must be one of 'enabled', 'disabled', ...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["filter_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/accounts/${accountId}/magic/advanced_tcp_protection/configs/tcp_flow_protection/filters/${args.filter_id}`,
          body,
        );

        const handle = await context.writeResource(
          "updatetcpflowprotectionfilter",
          String(args.filter_id),
          result,
        );
        context.logger.info("Updated updatetcpflowprotectionfilter", {});
        return { dataHandles: [handle] };
      },
    },
    deletetcpflowprotectionfilter: {
      description: "Delete TCP Flow Protection filter.",
      arguments: z.object({
        filter_id: z.string().describe("The UUID of the filter to delete."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/magic/advanced_tcp_protection/configs/tcp_flow_protection/filters/${args.filter_id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.filter_id });
        return { dataHandles: [] };
      },
    },
    listtcpflowprotectionrulesforaccount: {
      description: "List all TCP Flow Protection rules.",
      arguments: z.object({
        page: z.number().optional().describe(
          "The page number for pagination. Defaults to 1.",
        ),
        per_page: z.number().optional().describe(
          "The number of items per page. Must be between 10 and 1000. Defaults to 25.",
        ),
        order: z.string().optional().describe(
          "The field to order by. Defaults to 'prefix'.",
        ),
        direction: z.string().optional().describe(
          "The direction of ordering (ASC or DESC). Defaults to 'ASC'.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/magic/advanced_tcp_protection/configs/tcp_flow_protection/rules`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "listtcpflowprotectionrulesforaccount",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info(
          "Found {count} listtcpflowprotectionrulesforaccount",
          { count: results.length },
        );
        return { dataHandles: [handle] };
      },
    },
    createtcpflowprotectionrule: {
      description: "Create TCP Flow Protection rule.",
      arguments: z.object({
        burst_sensitivity: z.string().describe(
          "The burst sensitivity. Must be one of 'low', 'medium', 'high'.",
        ),
        mode: z.string().describe(
          "The mode for the TCP Flow Protection. Must be one of 'enabled', 'disabled', '...",
        ),
        name: z.string().describe(
          "The name of the TCP Flow Protection rule. Value is relative to the 'scope' se...",
        ),
        rate_sensitivity: z.string().describe(
          "The rate sensitivity. Must be one of 'low', 'medium', 'high'.",
        ),
        scope: z.string().describe(
          "The scope for the TCP Flow Protection rule.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/magic/advanced_tcp_protection/configs/tcp_flow_protection/rules`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "createtcpflowprotectionrule",
          id,
          result,
        );
        context.logger.info("Created createtcpflowprotectionrule {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    deletetcpflowprotectionrulesforaccount: {
      description: "Delete all TCP Flow Protection rules.",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/magic/advanced_tcp_protection/configs/tcp_flow_protection/rules`,
        );

        context.logger.info("Deleted resource {id}", { id: "unknown" });
        return { dataHandles: [] };
      },
    },
    gettcpflowprotectionrule: {
      description: "Get TCP Flow Protection rule.",
      arguments: z.object({
        rule_id: z.string().describe(
          "The UUID of the TCP Flow Protection rule.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/magic/advanced_tcp_protection/configs/tcp_flow_protection/rules/${args.rule_id}`,
        );

        const handle = await context.writeResource(
          "gettcpflowprotectionrule",
          String(args.rule_id),
          result,
        );
        context.logger.info("Fetched gettcpflowprotectionrule", {});
        return { dataHandles: [handle] };
      },
    },
    updatetcpflowprotectionrule: {
      description: "Update TCP Flow Protection rule.",
      arguments: z.object({
        rule_id: z.string().describe(
          "The UUID of the TCP Flow Protection rule to update.",
        ),
        burst_sensitivity: z.string().optional().describe(
          "The new burst sensitivity. Optional. Must be one of 'low', 'medium', 'high'.",
        ),
        mode: z.string().optional().describe(
          "The new mode for TCP Flow Protection. Optional. Must be one of 'enabled', 'di...",
        ),
        rate_sensitivity: z.string().optional().describe(
          "The new rate sensitivity. Optional. Must be one of 'low', 'medium', 'high'.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["rule_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/accounts/${accountId}/magic/advanced_tcp_protection/configs/tcp_flow_protection/rules/${args.rule_id}`,
          body,
        );

        const handle = await context.writeResource(
          "updatetcpflowprotectionrule",
          String(args.rule_id),
          result,
        );
        context.logger.info("Updated updatetcpflowprotectionrule", {});
        return { dataHandles: [handle] };
      },
    },
    deletetcpflowprotectionrule: {
      description: "Delete TCP Flow Protection rule.",
      arguments: z.object({
        rule_id: z.string().describe(
          "The UUID of the TCP Flow Protection rule to delete.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/magic/advanced_tcp_protection/configs/tcp_flow_protection/rules/${args.rule_id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.rule_id });
        return { dataHandles: [] };
      },
    },
    getprotectionstatus: {
      description: "Get protection status.",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/magic/advanced_tcp_protection/configs/tcp_protection_status`,
        );

        const handle = await context.writeResource(
          "getprotectionstatus",
          "latest",
          result,
        );
        context.logger.info("Fetched getprotectionstatus", {});
        return { dataHandles: [handle] };
      },
    },
    updateprotectionstatus: {
      description: "Update protection status.",
      arguments: z.object({
        enabled: z.boolean().describe("Enables or disables protection."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/accounts/${accountId}/magic/advanced_tcp_protection/configs/tcp_protection_status`,
          body,
        );

        const handle = await context.writeResource(
          "updateprotectionstatus",
          "updated",
          result,
        );
        context.logger.info("Updated updateprotectionstatus", {});
        return { dataHandles: [handle] };
      },
    },
    list_apps: {
      description: "List Apps",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/magic/apps`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("apps", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} apps", { count: results.length });
        return { dataHandles: [handle] };
      },
    },
    create_magic_account_apps_add_app: {
      description: "Create a new App",
      arguments: z.object({
        hostnames: z.unknown().optional(),
        ip_subnets: z.unknown().optional(),
        name: z.unknown(),
        source_subnets: z.unknown().optional(),
        type: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/magic/apps`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "magic_account_apps_add_app",
          id,
          result,
        );
        context.logger.info("Created magic_account_apps_add_app {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    update_app: {
      description: "Update an App",
      arguments: z.object({
        account_app_id: z.string(),
        hostnames: z.unknown().optional(),
        ip_subnets: z.unknown().optional(),
        name: z.unknown().optional(),
        source_subnets: z.unknown().optional(),
        type: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["account_app_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/magic/apps/${args.account_app_id}`,
          body,
        );

        const handle = await context.writeResource(
          "app",
          String(args.account_app_id),
          result,
        );
        context.logger.info("Updated app", {});
        return { dataHandles: [handle] };
      },
    },
    patch_app: {
      description: "Update an App",
      arguments: z.object({
        account_app_id: z.string(),
        hostnames: z.unknown().optional(),
        ip_subnets: z.unknown().optional(),
        name: z.unknown().optional(),
        source_subnets: z.unknown().optional(),
        type: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["account_app_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/accounts/${accountId}/magic/apps/${args.account_app_id}`,
          body,
        );

        const handle = await context.writeResource(
          "patch_app",
          String(args.account_app_id),
          result,
        );
        context.logger.info("Updated patch_app", {});
        return { dataHandles: [handle] };
      },
    },
    delete_app: {
      description: "Delete Account App",
      arguments: z.object({
        account_app_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/magic/apps/${args.account_app_id}`,
        );

        context.logger.info("Deleted resource {id}", {
          id: args.account_app_id,
        });
        return { dataHandles: [] };
      },
    },
    list_filter_profiles: {
      description: "List BGP Filter Profiles",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/magic/bgp/filter_profiles`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("filter_profiles", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} filter_profiles", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_filter_profile: {
      description: "Create BGP Filter Profile",
      arguments: z.object({
        description: z.string().max(1024).optional().describe(
          "Description of the filter profile",
        ),
        match_action: z.unknown(),
        name: z.string().min(1).max(255).describe(
          "Friendly name for the filter profile",
        ),
        targets: z.array(z.string()).describe(
          "List of CIDR prefixes. Each entry may carry an optional suffix that specifies...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/magic/bgp/filter_profiles`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "filter_profile",
          id,
          result,
        );
        context.logger.info("Created filter_profile {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_filter_profile: {
      description: "Get BGP Filter Profile",
      arguments: z.object({
        profile_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/magic/bgp/filter_profiles/${args.profile_id}`,
        );

        const handle = await context.writeResource(
          "filter_profile",
          String(args.profile_id),
          result,
        );
        context.logger.info("Fetched filter_profile", {});
        return { dataHandles: [handle] };
      },
    },
    update_filter_profile: {
      description: "Update BGP Filter Profile",
      arguments: z.object({
        profile_id: z.string(),
        description: z.string().max(1024).optional().describe(
          "Description of the filter profile",
        ),
        match_action: z.unknown().optional(),
        name: z.string().min(1).max(255).optional().describe(
          "Friendly name for the filter profile",
        ),
        targets: z.array(z.string()).optional().describe(
          "List of CIDR prefixes. Each entry may carry an optional suffix that specifies...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["profile_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/magic/bgp/filter_profiles/${args.profile_id}`,
          body,
        );

        const handle = await context.writeResource(
          "filter_profile",
          String(args.profile_id),
          result,
        );
        context.logger.info("Updated filter_profile", {});
        return { dataHandles: [handle] };
      },
    },
    delete_filter_profile: {
      description: "Delete BGP Filter Profile",
      arguments: z.object({
        profile_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/magic/bgp/filter_profiles/${args.profile_id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.profile_id });
        return { dataHandles: [] };
      },
    },
    get_settings: {
      description: "Get BGP Settings",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/magic/bgp/settings`,
        );

        const handle = await context.writeResource(
          "settings",
          "latest",
          result,
        );
        context.logger.info("Fetched settings", {});
        return { dataHandles: [handle] };
      },
    },
    update_settings: {
      description: "Update BGP Settings",
      arguments: z.object({
        cloudflare_asn: z.number().int().min(1).optional().describe(
          "Route advertisements from Cloudflare to ramps in this account will use this ASN.",
        ),
        redistribute: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/magic/bgp/settings`,
          body,
        );

        const handle = await context.writeResource(
          "settings",
          "updated",
          result,
        );
        context.logger.info("Updated settings", {});
        return { dataHandles: [handle] };
      },
    },
    list_cf1_sites: {
      description: "List CF1 Sites",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/magic/cf1_sites`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("cf1_sites", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} cf1_sites", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_cf1_sites: {
      description: "Create CF1 Sites",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/magic/cf1_sites`,
          args,
        );

        const handle = await context.writeResource(
          "create_cf1_sites",
          "latest",
          result ?? {},
        );
        context.logger.info("Executed create_cf1_sites", {});
        return { dataHandles: [handle] };
      },
    },
    get_cf1_site: {
      description: "Get CF1 Site",
      arguments: z.object({
        cf1_site_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/magic/cf1_sites/${args.cf1_site_id}`,
        );

        const handle = await context.writeResource(
          "cf1_site",
          String(args.cf1_site_id),
          result,
        );
        context.logger.info("Fetched cf1_site", {});
        return { dataHandles: [handle] };
      },
    },
    update_cf1_site: {
      description: "Update CF1 Site",
      arguments: z.object({
        cf1_site_id: z.string(),
        description: z.string().optional().describe(
          "A human-provided description of the CF1 Site.",
        ),
        location: z.unknown().optional(),
        name: z.string().optional().describe(
          "A human-provided name describing the CF1 Site that should be unique within th...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["cf1_site_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/accounts/${accountId}/magic/cf1_sites/${args.cf1_site_id}`,
          body,
        );

        const handle = await context.writeResource(
          "cf1_site",
          String(args.cf1_site_id),
          result,
        );
        context.logger.info("Updated cf1_site", {});
        return { dataHandles: [handle] };
      },
    },
    delete_cf1_site: {
      description: "Delete CF1 Site",
      arguments: z.object({
        cf1_site_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/magic/cf1_sites/${args.cf1_site_id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.cf1_site_id });
        return { dataHandles: [] };
      },
    },
    list_cf1_site_ramps: {
      description: "List CF1 Site Ramps",
      arguments: z.object({
        cf1_site_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["cf1_site_id", "page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/magic/cf1_sites/${args.cf1_site_id}/ramps`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("cf1_site_ramps", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} cf1_site_ramps", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_cf1_site_ramps: {
      description: "Create CF1 Site Ramps",
      arguments: z.object({
        cf1_site_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["cf1_site_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/magic/cf1_sites/${args.cf1_site_id}/ramps`,
          body,
        );

        const handle = await context.writeResource(
          "create_cf1_site_ramps",
          "latest",
          result ?? {},
        );
        context.logger.info("Executed create_cf1_site_ramps", {});
        return { dataHandles: [handle] };
      },
    },
    get_cf1_site_ramp: {
      description: "Get CF1 Site Ramp",
      arguments: z.object({
        cf1_site_id: z.string(),
        ramp_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/magic/cf1_sites/${args.cf1_site_id}/ramps/${args.ramp_id}`,
        );

        const handle = await context.writeResource(
          "cf1_site_ramp",
          String(args.ramp_id),
          result,
        );
        context.logger.info("Fetched cf1_site_ramp", {});
        return { dataHandles: [handle] };
      },
    },
    delete_cf1_site_ramp: {
      description: "Delete CF1 Site Ramp",
      arguments: z.object({
        cf1_site_id: z.string(),
        ramp_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/magic/cf1_sites/${args.cf1_site_id}/ramps/${args.ramp_id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.ramp_id });
        return { dataHandles: [] };
      },
    },
    list_interconnects: {
      description: "List interconnects",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/magic/cf_interconnects`,
        );

        const handle = await context.writeResource(
          "list_interconnects",
          "latest",
          result,
        );
        context.logger.info("Fetched list_interconnects", {});
        return { dataHandles: [handle] };
      },
    },
    update_multiple_interconnects: {
      description: "Update multiple interconnects",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/magic/cf_interconnects`,
          body,
        );

        const handle = await context.writeResource(
          "multiple_interconnects",
          "updated",
          result,
        );
        context.logger.info("Updated multiple_interconnects", {});
        return { dataHandles: [handle] };
      },
    },
    list_interconnect_details: {
      description: "List interconnect Details",
      arguments: z.object({
        cf_interconnect_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/magic/cf_interconnects/${args.cf_interconnect_id}`,
        );

        const handle = await context.writeResource(
          "list_interconnect_details",
          String(args.cf_interconnect_id),
          result,
        );
        context.logger.info("Fetched list_interconnect_details", {});
        return { dataHandles: [handle] };
      },
    },
    update_interconnect: {
      description: "Update interconnect",
      arguments: z.object({
        cf_interconnect_id: z.string(),
        automatic_return_routing: z.unknown().optional(),
        description: z.unknown().optional(),
        gre: z.unknown().optional(),
        health_check: z.unknown().optional(),
        interface_address: z.unknown().optional(),
        interface_address6: z.unknown().optional(),
        mtu: z.unknown().optional(),
        name: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["cf_interconnect_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/magic/cf_interconnects/${args.cf_interconnect_id}`,
          body,
        );

        const handle = await context.writeResource(
          "interconnect",
          String(args.cf_interconnect_id),
          result,
        );
        context.logger.info("Updated interconnect", {});
        return { dataHandles: [handle] };
      },
    },
    list: {
      description: "List Catalog Syncs",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/magic/cloud/catalog-syncs`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("list", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} list", { count: results.length });
        return { dataHandles: [handle] };
      },
    },
    create: {
      description: "Create Catalog Sync",
      arguments: z.object({
        description: z.string().optional(),
        destination_type: z.unknown(),
        name: z.string(),
        policy: z.string().optional(),
        update_mode: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/magic/cloud/catalog-syncs`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource("create", id, result);
        context.logger.info("Created create {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_catalog_syncs_read: {
      description: "Read Catalog Sync",
      arguments: z.object({
        sync_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/magic/cloud/catalog-syncs/${args.sync_id}`,
        );

        const handle = await context.writeResource(
          "catalog_syncs_read",
          String(args.sync_id),
          result,
        );
        context.logger.info("Fetched catalog_syncs_read", {});
        return { dataHandles: [handle] };
      },
    },
    update: {
      description: "Update Catalog Sync",
      arguments: z.object({
        sync_id: z.string(),
        description: z.string().optional(),
        name: z.string().optional(),
        policy: z.string().optional(),
        update_mode: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["sync_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/magic/cloud/catalog-syncs/${args.sync_id}`,
          body,
        );

        const handle = await context.writeResource(
          "update",
          String(args.sync_id),
          result,
        );
        context.logger.info("Updated update", {});
        return { dataHandles: [handle] };
      },
    },
    patch: {
      description: "Patch Catalog Sync",
      arguments: z.object({
        sync_id: z.string(),
        description: z.string().optional(),
        name: z.string().optional(),
        policy: z.string().optional(),
        update_mode: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["sync_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/accounts/${accountId}/magic/cloud/catalog-syncs/${args.sync_id}`,
          body,
        );

        const handle = await context.writeResource(
          "patch",
          String(args.sync_id),
          result,
        );
        context.logger.info("Updated patch", {});
        return { dataHandles: [handle] };
      },
    },
    delete: {
      description: "Delete Catalog Sync",
      arguments: z.object({
        sync_id: z.string(),
        delete_destination: z.boolean().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/magic/cloud/catalog-syncs/${args.sync_id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.sync_id });
        return { dataHandles: [] };
      },
    },
    catalog_syncs_refresh: {
      description: "Run Catalog Sync",
      arguments: z.object({
        sync_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/magic/cloud/catalog-syncs/${args.sync_id}/refresh`,
        );

        const handle = await context.writeResource(
          "catalog_syncs_refresh",
          "latest",
          result ?? {},
        );
        context.logger.info("Executed catalog_syncs_refresh", {});
        return { dataHandles: [handle] };
      },
    },
    get_onramps_mwan_addr_space_read: {
      description: "Read Magic WAN Address Space",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/magic/cloud/onramps/magic_wan_address_space`,
        );

        const handle = await context.writeResource(
          "onramps_mwan_addr_space_read",
          "latest",
          result,
        );
        context.logger.info("Fetched onramps_mwan_addr_space_read", {});
        return { dataHandles: [handle] };
      },
    },
    get_onramps_read: {
      description: "Read On-ramp",
      arguments: z.object({
        onramp_id: z.string(),
        status: z.boolean().optional(),
        vpcs: z.boolean().optional(),
        post_apply_resources: z.boolean().optional(),
        planned_resources: z.boolean().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/magic/cloud/onramps/${args.onramp_id}`,
        );

        const handle = await context.writeResource(
          "onramps_read",
          String(args.onramp_id),
          result,
        );
        context.logger.info("Fetched onramps_read", {});
        return { dataHandles: [handle] };
      },
    },
    onramps_apply: {
      description: "Apply On-ramp",
      arguments: z.object({
        onramp_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/magic/cloud/onramps/${args.onramp_id}/apply`,
        );

        const handle = await context.writeResource(
          "onramps_apply",
          "latest",
          result ?? {},
        );
        context.logger.info("Executed onramps_apply", {});
        return { dataHandles: [handle] };
      },
    },
    onramps_export: {
      description: "Export as Terraform",
      arguments: z.object({
        onramp_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/magic/cloud/onramps/${args.onramp_id}/export`,
        );

        const handle = await context.writeResource(
          "onramps_export",
          "latest",
          result ?? {},
        );
        context.logger.info("Executed onramps_export", {});
        return { dataHandles: [handle] };
      },
    },
    onramps_plan: {
      description: "Plan On-ramp",
      arguments: z.object({
        onramp_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/magic/cloud/onramps/${args.onramp_id}/plan`,
        );

        const handle = await context.writeResource(
          "onramps_plan",
          "latest",
          result ?? {},
        );
        context.logger.info("Executed onramps_plan", {});
        return { dataHandles: [handle] };
      },
    },
    providers_discover_all: {
      description: "Run Discovery for All Integrations",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/magic/cloud/providers/discover`,
        );

        const handle = await context.writeResource(
          "providers_discover_all",
          "latest",
          result ?? {},
        );
        context.logger.info("Executed providers_discover_all", {});
        return { dataHandles: [handle] };
      },
    },
    get_providers_read: {
      description: "Read Cloud Integration",
      arguments: z.object({
        provider_id: z.string(),
        status: z.boolean().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/magic/cloud/providers/${args.provider_id}`,
        );

        const handle = await context.writeResource(
          "providers_read",
          String(args.provider_id),
          result,
        );
        context.logger.info("Fetched providers_read", {});
        return { dataHandles: [handle] };
      },
    },
    providers_discover: {
      description: "Run Discovery",
      arguments: z.object({
        provider_id: z.string(),
        v2: z.boolean().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const queryParts: string[] = [];
        const queryKeys = new Set(["v2"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/magic/cloud/providers/${args.provider_id}/discover${qs}`,
        );

        const handle = await context.writeResource(
          "providers_discover",
          "latest",
          result ?? {},
        );
        context.logger.info("Executed providers_discover", {});
        return { dataHandles: [handle] };
      },
    },
    get_providers_initial_setup: {
      description: "Get Cloud Integration Setup Config",
      arguments: z.object({
        provider_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/magic/cloud/providers/${args.provider_id}/initial_setup`,
        );

        const handle = await context.writeResource(
          "providers_initial_setup",
          String(args.provider_id),
          result,
        );
        context.logger.info("Fetched providers_initial_setup", {});
        return { dataHandles: [handle] };
      },
    },
    get_resources_catalog_export: {
      description: "Export Resources",
      arguments: z.object({
        provider_id: z.string().optional(),
        resource_type: z.string().optional(),
        resource_id: z.string().optional(),
        region: z.string().optional(),
        resource_group: z.string().optional(),
        search: z.string().optional(),
        order_by: z.string().optional().describe(
          'One of ["id", "resource_type", "region"].',
        ),
        desc: z.boolean().optional(),
        v2: z.boolean().optional(),
      }),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/magic/cloud/resources/export`,
        );

        const handle = await context.writeResource(
          "resources_catalog_export",
          "latest",
          result,
        );
        context.logger.info("Fetched resources_catalog_export", {});
        return { dataHandles: [handle] };
      },
    },
    create_resources_catalog_policy_preview: {
      description: "Preview Rego Query",
      arguments: z.object({
        policy: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/magic/cloud/resources/policy-preview`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "resources_catalog_policy_preview",
          id,
          result,
        );
        context.logger.info("Created resources_catalog_policy_preview {id}", {
          id,
        });
        return { dataHandles: [handle] };
      },
    },
    get_resources_catalog_read: {
      description: "Read Resource",
      arguments: z.object({
        resource_id: z.string(),
        v2: z.boolean().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/magic/cloud/resources/${args.resource_id}`,
        );

        const handle = await context.writeResource(
          "resources_catalog_read",
          String(args.resource_id),
          result,
        );
        context.logger.info("Fetched resources_catalog_read", {});
        return { dataHandles: [handle] };
      },
    },
    get: {
      description: "Get Connector",
      arguments: z.object({
        connector_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/magic/connectors/${args.connector_id}`,
        );

        const handle = await context.writeResource(
          "get",
          String(args.connector_id),
          result,
        );
        context.logger.info("Fetched get", {});
        return { dataHandles: [handle] };
      },
    },
    update_mconn_connectors_edit: {
      description: "Edit Connector",
      arguments: z.object({
        connector_id: z.string(),
        activated: z.boolean().optional(),
        interrupt_window_days_of_week: z.array(z.unknown()).optional().describe(
          "Allowed days of the week for upgrades. Default is all days.",
        ),
        interrupt_window_duration_hours: z.number().min(1).max(24).optional(),
        interrupt_window_embargo_dates: z.array(z.unknown()).optional()
          .describe("List of dates (YYYY-MM-DD) when upgrades are blocked."),
        interrupt_window_hour_of_day: z.number().optional(),
        notes: z.string().optional(),
        primary: z.boolean().optional(),
        site_id: z.string().optional(),
        timezone: z.string().optional(),
        provision_license: z.boolean().optional().describe(
          "When true, regenerate license key for the connector.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["connector_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/accounts/${accountId}/magic/connectors/${args.connector_id}`,
          body,
        );

        const handle = await context.writeResource(
          "mconn_connectors_edit",
          String(args.connector_id),
          result,
        );
        context.logger.info("Updated mconn_connectors_edit", {});
        return { dataHandles: [handle] };
      },
    },
    list_gre_tunnels: {
      description: "List GRE tunnels",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/magic/gre_tunnels`,
        );

        const handle = await context.writeResource(
          "list_gre_tunnels",
          "latest",
          result,
        );
        context.logger.info("Fetched list_gre_tunnels", {});
        return { dataHandles: [handle] };
      },
    },
    create_gre_tunnels: {
      description: "Create a GRE tunnel",
      arguments: z.object({
        automatic_return_routing: z.unknown().optional(),
        bgp: z.unknown().optional(),
        cloudflare_gre_endpoint: z.unknown(),
        customer_gre_endpoint: z.unknown(),
        description: z.unknown().optional(),
        health_check: z.unknown().optional(),
        interface_address: z.unknown(),
        interface_address6: z.unknown().optional(),
        mtu: z.unknown().optional(),
        name: z.unknown(),
        ttl: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/magic/gre_tunnels`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource("gre_tunnels", id, result);
        context.logger.info("Created gre_tunnels {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    update_multiple_gre_tunnels: {
      description: "Update multiple GRE tunnels",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/magic/gre_tunnels`,
          body,
        );

        const handle = await context.writeResource(
          "multiple_gre_tunnels",
          "updated",
          result,
        );
        context.logger.info("Updated multiple_gre_tunnels", {});
        return { dataHandles: [handle] };
      },
    },
    list_gre_tunnel_details: {
      description: "List GRE Tunnel Details",
      arguments: z.object({
        gre_tunnel_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/magic/gre_tunnels/${args.gre_tunnel_id}`,
        );

        const handle = await context.writeResource(
          "list_gre_tunnel_details",
          String(args.gre_tunnel_id),
          result,
        );
        context.logger.info("Fetched list_gre_tunnel_details", {});
        return { dataHandles: [handle] };
      },
    },
    update_gre_tunnel: {
      description: "Update GRE Tunnel",
      arguments: z.object({
        gre_tunnel_id: z.string(),
        automatic_return_routing: z.unknown().optional(),
        cloudflare_gre_endpoint: z.unknown(),
        customer_gre_endpoint: z.unknown(),
        description: z.unknown().optional(),
        health_check: z.unknown().optional(),
        interface_address: z.unknown(),
        interface_address6: z.unknown().optional(),
        mtu: z.unknown().optional(),
        name: z.unknown(),
        ttl: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["gre_tunnel_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/magic/gre_tunnels/${args.gre_tunnel_id}`,
          body,
        );

        const handle = await context.writeResource(
          "gre_tunnel",
          String(args.gre_tunnel_id),
          result,
        );
        context.logger.info("Updated gre_tunnel", {});
        return { dataHandles: [handle] };
      },
    },
    delete_gre_tunnel: {
      description: "Delete GRE Tunnel",
      arguments: z.object({
        gre_tunnel_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/magic/gre_tunnels/${args.gre_tunnel_id}`,
        );

        context.logger.info("Deleted resource {id}", {
          id: args.gre_tunnel_id,
        });
        return { dataHandles: [] };
      },
    },
    list_ipsec_tunnels: {
      description: "List IPsec tunnels",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/magic/ipsec_tunnels`,
        );

        const handle = await context.writeResource(
          "list_ipsec_tunnels",
          "latest",
          result,
        );
        context.logger.info("Fetched list_ipsec_tunnels", {});
        return { dataHandles: [handle] };
      },
    },
    create_ipsec_tunnels: {
      description: "Create an IPsec tunnel",
      arguments: z.object({
        automatic_return_routing: z.unknown().optional(),
        bgp: z.unknown().optional(),
        cloudflare_endpoint: z.unknown(),
        custom_remote_identities: z.unknown().optional(),
        customer_endpoint: z.unknown().optional(),
        description: z.unknown().optional(),
        health_check: z.unknown().optional(),
        interface_address: z.unknown(),
        interface_address6: z.unknown().optional(),
        name: z.unknown(),
        psk: z.unknown().optional(),
        replay_protection: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/magic/ipsec_tunnels`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource("ipsec_tunnels", id, result);
        context.logger.info("Created ipsec_tunnels {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    update_multiple_ipsec_tunnels: {
      description: "Update multiple IPsec tunnels",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/magic/ipsec_tunnels`,
          body,
        );

        const handle = await context.writeResource(
          "multiple_ipsec_tunnels",
          "updated",
          result,
        );
        context.logger.info("Updated multiple_ipsec_tunnels", {});
        return { dataHandles: [handle] };
      },
    },
    create_magic_ipsec_tunnels_set_pre_shared_keys_for_ipsec_tunnels: {
      description: "Set Pre-Shared Keys (PSK) for IPsec tunnels",
      arguments: z.object({
        validate_only: z.boolean().optional().describe(
          "If `true`, only run validation without persisting changes.",
        ),
        psks: z.array(z.unknown()).describe("List of tunnel ID and PSK pairs."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["validate_only"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }
        const queryParts: string[] = [];
        const queryKeys = new Set(["validate_only"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/magic/ipsec_tunnels/psk${qs}`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "magic_ipsec_tunnels_set_pre_shared_keys_for_ipsec_tunnels",
          id,
          result,
        );
        context.logger.info(
          "Created magic_ipsec_tunnels_set_pre_shared_keys_for_ipsec_tunnels {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    list_ipsec_tunnel_details: {
      description: "List IPsec tunnel details",
      arguments: z.object({
        ipsec_tunnel_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/magic/ipsec_tunnels/${args.ipsec_tunnel_id}`,
        );

        const handle = await context.writeResource(
          "list_ipsec_tunnel_details",
          String(args.ipsec_tunnel_id),
          result,
        );
        context.logger.info("Fetched list_ipsec_tunnel_details", {});
        return { dataHandles: [handle] };
      },
    },
    update_ipsec_tunnel: {
      description: "Update IPsec Tunnel",
      arguments: z.object({
        ipsec_tunnel_id: z.string(),
        automatic_return_routing: z.unknown().optional(),
        bgp: z.unknown().optional(),
        cloudflare_endpoint: z.unknown(),
        custom_remote_identities: z.unknown().optional(),
        customer_endpoint: z.unknown().optional(),
        description: z.unknown().optional(),
        health_check: z.unknown().optional(),
        interface_address: z.unknown(),
        interface_address6: z.unknown().optional(),
        name: z.unknown(),
        psk: z.unknown().optional(),
        replay_protection: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["ipsec_tunnel_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/magic/ipsec_tunnels/${args.ipsec_tunnel_id}`,
          body,
        );

        const handle = await context.writeResource(
          "ipsec_tunnel",
          String(args.ipsec_tunnel_id),
          result,
        );
        context.logger.info("Updated ipsec_tunnel", {});
        return { dataHandles: [handle] };
      },
    },
    delete_ipsec_tunnel: {
      description: "Delete IPsec Tunnel",
      arguments: z.object({
        ipsec_tunnel_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/magic/ipsec_tunnels/${args.ipsec_tunnel_id}`,
        );

        context.logger.info("Deleted resource {id}", {
          id: args.ipsec_tunnel_id,
        });
        return { dataHandles: [] };
      },
    },
    magic_ipsec_tunnels_generate_pre_shared_key_psk_for_ipsec_tunnels: {
      description: "Generate Pre-Shared Key (PSK) for IPsec tunnels",
      arguments: z.object({
        ipsec_tunnel_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/magic/ipsec_tunnels/${args.ipsec_tunnel_id}/psk_generate`,
        );

        const handle = await context.writeResource(
          "magic_ipsec_tunnels_generate_pre_shared_key_psk_for_ipsec_tunnels",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed magic_ipsec_tunnels_generate_pre_shared_key_psk_for_ipsec_tunnels",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    list_redundancy_groups: {
      description: "List Redundancy Groups",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/magic/redundancy_groups`,
        );

        const handle = await context.writeResource(
          "list_redundancy_groups",
          "latest",
          result,
        );
        context.logger.info("Fetched list_redundancy_groups", {});
        return { dataHandles: [handle] };
      },
    },
    create_redundancy_group: {
      description: "Create a Redundancy Group",
      arguments: z.object({
        description: z.string().max(1024).optional().describe(
          "Optional description",
        ),
        members: z.array(z.unknown()).optional().describe(
          "Tunnels to add to the group",
        ),
        name: z.string().min(1).max(255).describe(
          "Human-readable name for the redundancy group",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/magic/redundancy_groups`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "redundancy_group",
          id,
          result,
        );
        context.logger.info("Created redundancy_group {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_redundancy_group: {
      description: "Get Redundancy Group Details",
      arguments: z.object({
        redundancy_group_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/magic/redundancy_groups/${args.redundancy_group_id}`,
        );

        const handle = await context.writeResource(
          "redundancy_group",
          String(args.redundancy_group_id),
          result,
        );
        context.logger.info("Fetched redundancy_group", {});
        return { dataHandles: [handle] };
      },
    },
    update_redundancy_group: {
      description: "Update a Redundancy Group",
      arguments: z.object({
        redundancy_group_id: z.string(),
        description: z.string().max(1024).optional().describe(
          "Optional description",
        ),
        members: z.array(z.unknown()).optional().describe(
          "Tunnels to add to the group",
        ),
        name: z.string().min(1).max(255).describe(
          "Human-readable name for the redundancy group",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["redundancy_group_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/magic/redundancy_groups/${args.redundancy_group_id}`,
          body,
        );

        const handle = await context.writeResource(
          "redundancy_group",
          String(args.redundancy_group_id),
          result,
        );
        context.logger.info("Updated redundancy_group", {});
        return { dataHandles: [handle] };
      },
    },
    delete_redundancy_group: {
      description: "Delete a Redundancy Group",
      arguments: z.object({
        redundancy_group_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/magic/redundancy_groups/${args.redundancy_group_id}`,
        );

        context.logger.info("Deleted resource {id}", {
          id: args.redundancy_group_id,
        });
        return { dataHandles: [] };
      },
    },
    list_routes: {
      description: "List Routes",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/magic/routes`,
        );

        const handle = await context.writeResource(
          "list_routes",
          "latest",
          result,
        );
        context.logger.info("Fetched list_routes", {});
        return { dataHandles: [handle] };
      },
    },
    create_routes: {
      description: "Create a Route",
      arguments: z.object({
        description: z.unknown().optional(),
        nexthop: z.unknown(),
        prefix: z.unknown(),
        priority: z.unknown(),
        scope: z.unknown().optional(),
        weight: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/magic/routes`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource("routes", id, result);
        context.logger.info("Created routes {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    update_many_routes: {
      description: "Update Many Routes",
      arguments: z.object({
        routes: z.array(z.unknown()),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/magic/routes`,
          body,
        );

        const handle = await context.writeResource(
          "many_routes",
          "updated",
          result,
        );
        context.logger.info("Updated many_routes", {});
        return { dataHandles: [handle] };
      },
    },
    delete_many_routes: {
      description: "Delete Many Routes",
      arguments: z.object({
        routes: z.array(z.unknown()),
      }),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/magic/routes`,
        );

        context.logger.info("Deleted resource {id}", { id: "unknown" });
        return { dataHandles: [] };
      },
    },
    get_magic_static_routes_route_details: {
      description: "Route Details",
      arguments: z.object({
        route_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/magic/routes/${args.route_id}`,
        );

        const handle = await context.writeResource(
          "magic_static_routes_route_details",
          String(args.route_id),
          result,
        );
        context.logger.info("Fetched magic_static_routes_route_details", {});
        return { dataHandles: [handle] };
      },
    },
    update_route: {
      description: "Update Route",
      arguments: z.object({
        route_id: z.string(),
        description: z.unknown().optional(),
        nexthop: z.unknown(),
        prefix: z.unknown(),
        priority: z.unknown(),
        scope: z.unknown().optional(),
        weight: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["route_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/magic/routes/${args.route_id}`,
          body,
        );

        const handle = await context.writeResource(
          "route",
          String(args.route_id),
          result,
        );
        context.logger.info("Updated route", {});
        return { dataHandles: [handle] };
      },
    },
    delete_route: {
      description: "Delete Route",
      arguments: z.object({
        route_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/magic/routes/${args.route_id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.route_id });
        return { dataHandles: [] };
      },
    },
    list_sites: {
      description: "List Sites",
      arguments: z.object({
        connectorid: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/magic/sites`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("sites", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} sites", { count: results.length });
        return { dataHandles: [handle] };
      },
    },
    create_site: {
      description: "Create a new Site",
      arguments: z.object({
        connector_id: z.unknown().optional(),
        description: z.string().optional(),
        ha_mode: z.boolean().optional().describe(
          "Site high availability mode. If set to true, the site can have two connectors...",
        ),
        location: z.unknown().optional(),
        name: z.unknown(),
        secondary_connector_id: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/magic/sites`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource("site", id, result);
        context.logger.info("Created site {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_magic_sites_site_details: {
      description: "Site Details",
      arguments: z.object({
        site_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/magic/sites/${args.site_id}`,
        );

        const handle = await context.writeResource(
          "magic_sites_site_details",
          String(args.site_id),
          result,
        );
        context.logger.info("Fetched magic_sites_site_details", {});
        return { dataHandles: [handle] };
      },
    },
    update_site: {
      description: "Update Site",
      arguments: z.object({
        site_id: z.string(),
        connector_id: z.unknown().optional(),
        description: z.string().optional(),
        location: z.unknown().optional(),
        name: z.unknown().optional(),
        secondary_connector_id: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["site_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/magic/sites/${args.site_id}`,
          body,
        );

        const handle = await context.writeResource(
          "site",
          String(args.site_id),
          result,
        );
        context.logger.info("Updated site", {});
        return { dataHandles: [handle] };
      },
    },
    patch_site: {
      description: "Patch Site",
      arguments: z.object({
        site_id: z.string(),
        connector_id: z.unknown().optional(),
        description: z.string().optional(),
        location: z.unknown().optional(),
        name: z.unknown().optional(),
        secondary_connector_id: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["site_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/accounts/${accountId}/magic/sites/${args.site_id}`,
          body,
        );

        const handle = await context.writeResource(
          "patch_site",
          String(args.site_id),
          result,
        );
        context.logger.info("Updated patch_site", {});
        return { dataHandles: [handle] };
      },
    },
    delete_site: {
      description: "Delete Site",
      arguments: z.object({
        site_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/magic/sites/${args.site_id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.site_id });
        return { dataHandles: [] };
      },
    },
    list_acls: {
      description: "List Site ACLs",
      arguments: z.object({
        site_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["site_id", "page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/magic/sites/${args.site_id}/acls`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("acls", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} acls", { count: results.length });
        return { dataHandles: [handle] };
      },
    },
    create_acl: {
      description: "Create a new Site ACL",
      arguments: z.object({
        site_id: z.string(),
        description: z.string().optional().describe("Description for the ACL."),
        forward_locally: z.unknown().optional(),
        lan_1: z.unknown(),
        lan_2: z.unknown(),
        name: z.string().describe("The name of the ACL."),
        protocols: z.array(z.enum(["tcp", "udp", "icmp"])).optional(),
        unidirectional: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["site_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/magic/sites/${args.site_id}/acls`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource("acl", id, result);
        context.logger.info("Created acl {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_magic_site_acls_acl_details: {
      description: "Site ACL Details",
      arguments: z.object({
        site_id: z.string(),
        acl_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/magic/sites/${args.site_id}/acls/${args.acl_id}`,
        );

        const handle = await context.writeResource(
          "magic_site_acls_acl_details",
          String(args.acl_id),
          result,
        );
        context.logger.info("Fetched magic_site_acls_acl_details", {});
        return { dataHandles: [handle] };
      },
    },
    update_acl: {
      description: "Update Site ACL",
      arguments: z.object({
        site_id: z.string(),
        acl_id: z.string(),
        description: z.string().optional().describe("Description for the ACL."),
        forward_locally: z.unknown().optional(),
        lan_1: z.unknown().optional(),
        lan_2: z.unknown().optional(),
        name: z.string().optional().describe("The name of the ACL."),
        protocols: z.array(z.enum(["tcp", "udp", "icmp"])).optional(),
        unidirectional: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["site_id", "acl_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/magic/sites/${args.site_id}/acls/${args.acl_id}`,
          body,
        );

        const handle = await context.writeResource(
          "acl",
          String(args.acl_id),
          result,
        );
        context.logger.info("Updated acl", {});
        return { dataHandles: [handle] };
      },
    },
    patch_acl: {
      description: "Patch Site ACL",
      arguments: z.object({
        site_id: z.string(),
        acl_id: z.string(),
        description: z.string().optional().describe("Description for the ACL."),
        forward_locally: z.unknown().optional(),
        lan_1: z.unknown().optional(),
        lan_2: z.unknown().optional(),
        name: z.string().optional().describe("The name of the ACL."),
        protocols: z.array(z.enum(["tcp", "udp", "icmp"])).optional(),
        unidirectional: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["site_id", "acl_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/accounts/${accountId}/magic/sites/${args.site_id}/acls/${args.acl_id}`,
          body,
        );

        const handle = await context.writeResource(
          "patch_acl",
          String(args.acl_id),
          result,
        );
        context.logger.info("Updated patch_acl", {});
        return { dataHandles: [handle] };
      },
    },
    delete_acl: {
      description: "Delete Site ACL",
      arguments: z.object({
        site_id: z.string(),
        acl_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/magic/sites/${args.site_id}/acls/${args.acl_id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.acl_id });
        return { dataHandles: [] };
      },
    },
    list_app_configs: {
      description: "List App Configs",
      arguments: z.object({
        site_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["site_id", "page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/magic/sites/${args.site_id}/app_configs`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("app_configs", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} app_configs", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_magic_site_app_configs_add_app_config: {
      description: "Create a new App Config",
      arguments: z.object({
        site_id: z.string(),
        breakout: z.unknown().optional(),
        preferred_wans: z.unknown().optional(),
        priority: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["site_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/magic/sites/${args.site_id}/app_configs`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "magic_site_app_configs_add_app_config",
          id,
          result,
        );
        context.logger.info(
          "Created magic_site_app_configs_add_app_config {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    update_app_config: {
      description: "Update an App Config",
      arguments: z.object({
        site_id: z.string(),
        app_config_id: z.string(),
        account_app_id: z.unknown().optional(),
        breakout: z.unknown().optional(),
        managed_app_id: z.unknown().optional(),
        preferred_wans: z.unknown().optional(),
        priority: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["site_id", "app_config_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/magic/sites/${args.site_id}/app_configs/${args.app_config_id}`,
          body,
        );

        const handle = await context.writeResource(
          "app_config",
          String(args.app_config_id),
          result,
        );
        context.logger.info("Updated app_config", {});
        return { dataHandles: [handle] };
      },
    },
    patch_app_config: {
      description: "Update an App Config",
      arguments: z.object({
        site_id: z.string(),
        app_config_id: z.string(),
        account_app_id: z.unknown().optional(),
        breakout: z.unknown().optional(),
        managed_app_id: z.unknown().optional(),
        preferred_wans: z.unknown().optional(),
        priority: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["site_id", "app_config_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/accounts/${accountId}/magic/sites/${args.site_id}/app_configs/${args.app_config_id}`,
          body,
        );

        const handle = await context.writeResource(
          "patch_app_config",
          String(args.app_config_id),
          result,
        );
        context.logger.info("Updated patch_app_config", {});
        return { dataHandles: [handle] };
      },
    },
    delete_app_config: {
      description: "Delete App Config",
      arguments: z.object({
        site_id: z.string(),
        app_config_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/magic/sites/${args.site_id}/app_configs/${args.app_config_id}`,
        );

        context.logger.info("Deleted resource {id}", {
          id: args.app_config_id,
        });
        return { dataHandles: [] };
      },
    },
    list_lans: {
      description: "List Site LANs",
      arguments: z.object({
        site_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["site_id", "page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/magic/sites/${args.site_id}/lans`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("lans", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} lans", { count: results.length });
        return { dataHandles: [handle] };
      },
    },
    create_lan: {
      description: "Create a new Site LAN",
      arguments: z.object({
        site_id: z.string(),
        bond_id: z.unknown().optional(),
        ha_link: z.boolean().optional().describe(
          "mark true to use this LAN for HA probing. only works for site with HA turned ...",
        ),
        is_breakout: z.boolean().optional().describe(
          "mark true to use this LAN for source-based breakout traffic",
        ),
        is_prioritized: z.boolean().optional().describe(
          "mark true to use this LAN for source-based prioritized traffic",
        ),
        name: z.string().optional(),
        nat: z.unknown().optional(),
        physport: z.unknown().optional(),
        routed_subnets: z.array(z.unknown()).optional(),
        static_addressing: z.unknown().optional(),
        vlan_tag: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["site_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/magic/sites/${args.site_id}/lans`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource("lan", id, result);
        context.logger.info("Created lan {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_magic_site_lans_lan_details: {
      description: "Site LAN Details",
      arguments: z.object({
        site_id: z.string(),
        lan_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/magic/sites/${args.site_id}/lans/${args.lan_id}`,
        );

        const handle = await context.writeResource(
          "magic_site_lans_lan_details",
          String(args.lan_id),
          result,
        );
        context.logger.info("Fetched magic_site_lans_lan_details", {});
        return { dataHandles: [handle] };
      },
    },
    update_lan: {
      description: "Update Site LAN",
      arguments: z.object({
        site_id: z.string(),
        lan_id: z.string(),
        bond_id: z.unknown().optional(),
        is_breakout: z.boolean().optional().describe(
          "mark true to use this LAN for source-based breakout traffic",
        ),
        is_prioritized: z.boolean().optional().describe(
          "mark true to use this LAN for source-based prioritized traffic",
        ),
        name: z.string().optional(),
        nat: z.unknown().optional(),
        physport: z.unknown().optional(),
        routed_subnets: z.array(z.unknown()).optional(),
        static_addressing: z.unknown().optional(),
        vlan_tag: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["site_id", "lan_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/magic/sites/${args.site_id}/lans/${args.lan_id}`,
          body,
        );

        const handle = await context.writeResource(
          "lan",
          String(args.lan_id),
          result,
        );
        context.logger.info("Updated lan", {});
        return { dataHandles: [handle] };
      },
    },
    patch_lan: {
      description: "Patch Site LAN",
      arguments: z.object({
        site_id: z.string(),
        lan_id: z.string(),
        bond_id: z.unknown().optional(),
        is_breakout: z.boolean().optional().describe(
          "mark true to use this LAN for source-based breakout traffic",
        ),
        is_prioritized: z.boolean().optional().describe(
          "mark true to use this LAN for source-based prioritized traffic",
        ),
        name: z.string().optional(),
        nat: z.unknown().optional(),
        physport: z.unknown().optional(),
        routed_subnets: z.array(z.unknown()).optional(),
        static_addressing: z.unknown().optional(),
        vlan_tag: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["site_id", "lan_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/accounts/${accountId}/magic/sites/${args.site_id}/lans/${args.lan_id}`,
          body,
        );

        const handle = await context.writeResource(
          "patch_lan",
          String(args.lan_id),
          result,
        );
        context.logger.info("Updated patch_lan", {});
        return { dataHandles: [handle] };
      },
    },
    delete_lan: {
      description: "Delete Site LAN",
      arguments: z.object({
        site_id: z.string(),
        lan_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/magic/sites/${args.site_id}/lans/${args.lan_id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.lan_id });
        return { dataHandles: [] };
      },
    },
    get_magic_site_netflow_config_details: {
      description: "NetFlow Configuration Details",
      arguments: z.object({
        site_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/magic/sites/${args.site_id}/netflow_config`,
        );

        const handle = await context.writeResource(
          "magic_site_netflow_config_details",
          String(args.site_id),
          result,
        );
        context.logger.info("Fetched magic_site_netflow_config_details", {});
        return { dataHandles: [handle] };
      },
    },
    create_netflow_config: {
      description: "Create NetFlow Configuration",
      arguments: z.object({
        site_id: z.string(),
        active_timeout: z.number().int().min(1).max(5400).optional().describe(
          "Timeout in seconds for active flows.",
        ),
        collector_ip: z.string().optional().describe(
          "IPv4 address of the NetFlow collector.",
        ),
        collector_port: z.number().int().min(1).max(65535).optional().describe(
          "UDP port of the NetFlow collector.",
        ),
        inactive_timeout: z.number().int().min(1).max(5400).optional().describe(
          "Timeout in seconds for inactive flows.",
        ),
        sampling_rate: z.number().int().min(1).max(10000).optional().describe(
          "Sampling rate for NetFlow records (1 = every packet).",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["site_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/magic/sites/${args.site_id}/netflow_config`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "netflow_config",
          id,
          result,
        );
        context.logger.info("Created netflow_config {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    update_netflow_config: {
      description: "Update NetFlow Configuration",
      arguments: z.object({
        site_id: z.string(),
        active_timeout: z.number().int().min(1).max(5400).optional().describe(
          "Timeout in seconds for active flows.",
        ),
        collector_ip: z.string().optional().describe(
          "IPv4 address of the NetFlow collector.",
        ),
        collector_port: z.number().int().min(1).max(65535).optional().describe(
          "UDP port of the NetFlow collector.",
        ),
        inactive_timeout: z.number().int().min(1).max(5400).optional().describe(
          "Timeout in seconds for inactive flows.",
        ),
        sampling_rate: z.number().int().min(1).max(10000).optional().describe(
          "Sampling rate for NetFlow records (1 = every packet).",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["site_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/magic/sites/${args.site_id}/netflow_config`,
          body,
        );

        const handle = await context.writeResource(
          "netflow_config",
          String(args.site_id),
          result,
        );
        context.logger.info("Updated netflow_config", {});
        return { dataHandles: [handle] };
      },
    },
    patch_netflow_config: {
      description: "Update NetFlow Configuration",
      arguments: z.object({
        site_id: z.string(),
        active_timeout: z.number().int().min(1).max(5400).optional().describe(
          "Timeout in seconds for active flows.",
        ),
        collector_ip: z.string().optional().describe(
          "IPv4 address of the NetFlow collector.",
        ),
        collector_port: z.number().int().min(1).max(65535).optional().describe(
          "UDP port of the NetFlow collector.",
        ),
        inactive_timeout: z.number().int().min(1).max(5400).optional().describe(
          "Timeout in seconds for inactive flows.",
        ),
        sampling_rate: z.number().int().min(1).max(10000).optional().describe(
          "Sampling rate for NetFlow records (1 = every packet).",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["site_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/accounts/${accountId}/magic/sites/${args.site_id}/netflow_config`,
          body,
        );

        const handle = await context.writeResource(
          "patch_netflow_config",
          String(args.site_id),
          result,
        );
        context.logger.info("Updated patch_netflow_config", {});
        return { dataHandles: [handle] };
      },
    },
    delete_netflow_config: {
      description: "Delete NetFlow Configuration",
      arguments: z.object({
        site_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/magic/sites/${args.site_id}/netflow_config`,
        );

        context.logger.info("Deleted resource {id}", { id: args.site_id });
        return { dataHandles: [] };
      },
    },
    list_wans: {
      description: "List Site WANs",
      arguments: z.object({
        site_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["site_id", "page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/magic/sites/${args.site_id}/wans`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("wans", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} wans", { count: results.length });
        return { dataHandles: [handle] };
      },
    },
    create_wan: {
      description: "Create a new Site WAN",
      arguments: z.object({
        site_id: z.string(),
        name: z.string().optional(),
        physport: z.unknown(),
        priority: z.number().int().optional(),
        static_addressing: z.unknown().optional(),
        vlan_tag: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["site_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/magic/sites/${args.site_id}/wans`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource("wan", id, result);
        context.logger.info("Created wan {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_magic_site_wans_wan_details: {
      description: "Site WAN Details",
      arguments: z.object({
        site_id: z.string(),
        wan_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/magic/sites/${args.site_id}/wans/${args.wan_id}`,
        );

        const handle = await context.writeResource(
          "magic_site_wans_wan_details",
          String(args.wan_id),
          result,
        );
        context.logger.info("Fetched magic_site_wans_wan_details", {});
        return { dataHandles: [handle] };
      },
    },
    update_wan: {
      description: "Update Site WAN",
      arguments: z.object({
        site_id: z.string(),
        wan_id: z.string(),
        name: z.string().optional(),
        physport: z.unknown().optional(),
        priority: z.number().int().optional(),
        static_addressing: z.unknown().optional(),
        vlan_tag: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["site_id", "wan_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/magic/sites/${args.site_id}/wans/${args.wan_id}`,
          body,
        );

        const handle = await context.writeResource(
          "wan",
          String(args.wan_id),
          result,
        );
        context.logger.info("Updated wan", {});
        return { dataHandles: [handle] };
      },
    },
    patch_wan: {
      description: "Patch Site WAN",
      arguments: z.object({
        site_id: z.string(),
        wan_id: z.string(),
        name: z.string().optional(),
        physport: z.unknown().optional(),
        priority: z.number().int().optional(),
        static_addressing: z.unknown().optional(),
        vlan_tag: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["site_id", "wan_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/accounts/${accountId}/magic/sites/${args.site_id}/wans/${args.wan_id}`,
          body,
        );

        const handle = await context.writeResource(
          "patch_wan",
          String(args.wan_id),
          result,
        );
        context.logger.info("Updated patch_wan", {});
        return { dataHandles: [handle] };
      },
    },
    delete_wan: {
      description: "Delete Site WAN",
      arguments: z.object({
        site_id: z.string(),
        wan_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/magic/sites/${args.site_id}/wans/${args.wan_id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.wan_id });
        return { dataHandles: [] };
      },
    },
  },
};
