/**
 * Cloudflare Stream — video upload, encoding, delivery, live streaming
 *
 * Auto-generated by scripts/cloudflare-codegen — do not edit manually.
 *
 * @module
 */
// SPDX-License-Identifier: AGPL-3.0-or-later WITH Swamp-Extension-Exception

import { z } from "npm:zod@4.4.3";
import { cfApi, cfApiPaginated } from "./_lib/api.ts";

// =============================================================================
// Schemas
// =============================================================================

const GlobalArgsSchema = z.object({
  apiToken: z.string().meta({ sensitive: true }).describe(
    "Cloudflare API token",
  ),
  accountId: z.string().describe("Cloudflare account ID"),
});

const VideosItemSchema = z.object({
  allowedOrigins: z.unknown().optional(),
  clippedFrom: z.string().optional().describe(
    "The unique identifier of the source video this video was clipped from.",
  ),
  created: z.unknown().optional(),
  creator: z.unknown().optional(),
  duration: z.unknown().optional(),
  input: z.unknown().optional(),
  liveInput: z.unknown().optional(),
  maxDurationSeconds: z.unknown().optional(),
  maxSizeBytes: z.number().int().optional().describe(
    "The maximum size in bytes for the video upload.",
  ),
  meta: z.unknown().optional(),
  modified: z.unknown().optional(),
  playback: z.unknown().optional(),
  preview: z.unknown().optional(),
  publicDetails: z.object({
    channel_link: z.string().nullable().optional(),
    logo: z.string().nullable().optional(),
    media_id: z.number().int().optional(),
    share_link: z.string().nullable().optional(),
    title: z.string().nullable().optional(),
  }).optional().describe(
    "Public details for the video including title, share link, channel link, and logo.",
  ),
  readyToStream: z.unknown().optional(),
  readyToStreamAt: z.unknown().optional(),
  requireSignedURLs: z.unknown().optional(),
  scheduledDeletion: z.unknown().optional(),
  size: z.unknown().optional(),
  status: z.unknown().optional(),
  thumbnail: z.unknown().optional(),
  thumbnailTimestampPct: z.unknown().optional(),
  uid: z.unknown().optional(),
  uploadExpiry: z.unknown().optional(),
  uploaded: z.unknown().optional(),
  watermark: z.unknown().optional(),
});

const ListVideosSchema = z.object({
  items: z.array(VideosItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateStreamVideoClippingClipVideosGivenAStartAndEndTimeSchema = z.object(
  {
    allowedOrigins: z.unknown().optional(),
    clippedFrom: z.string().optional().describe(
      "The unique identifier of the source video this video was clipped from.",
    ),
    created: z.unknown().optional(),
    creator: z.unknown().optional(),
    duration: z.unknown().optional(),
    input: z.unknown().optional(),
    liveInput: z.unknown().optional(),
    maxDurationSeconds: z.unknown().optional(),
    maxSizeBytes: z.number().int().optional().describe(
      "The maximum size in bytes for the video upload.",
    ),
    meta: z.unknown().optional(),
    modified: z.unknown().optional(),
    playback: z.unknown().optional(),
    preview: z.unknown().optional(),
    publicDetails: z.object({
      channel_link: z.string().nullable().optional(),
      logo: z.string().nullable().optional(),
      media_id: z.number().int().optional(),
      share_link: z.string().nullable().optional(),
      title: z.string().nullable().optional(),
    }).optional().describe(
      "Public details for the video including title, share link, channel link, and logo.",
    ),
    readyToStream: z.unknown().optional(),
    readyToStreamAt: z.unknown().optional(),
    requireSignedURLs: z.unknown().optional(),
    scheduledDeletion: z.unknown().optional(),
    size: z.unknown().optional(),
    status: z.unknown().optional(),
    thumbnail: z.unknown().optional(),
    thumbnailTimestampPct: z.unknown().optional(),
    uid: z.unknown().optional(),
    uploadExpiry: z.unknown().optional(),
    uploaded: z.unknown().optional(),
    watermark: z.unknown().optional(),
  },
);

const CreateStreamVideosUploadVideosFromAUrlSchema = z.object({
  allowedOrigins: z.unknown().optional(),
  clippedFrom: z.string().optional().describe(
    "The unique identifier of the source video this video was clipped from.",
  ),
  created: z.unknown().optional(),
  creator: z.unknown().optional(),
  duration: z.unknown().optional(),
  input: z.unknown().optional(),
  liveInput: z.unknown().optional(),
  maxDurationSeconds: z.unknown().optional(),
  maxSizeBytes: z.number().int().optional().describe(
    "The maximum size in bytes for the video upload.",
  ),
  meta: z.unknown().optional(),
  modified: z.unknown().optional(),
  playback: z.unknown().optional(),
  preview: z.unknown().optional(),
  publicDetails: z.object({
    channel_link: z.string().nullable().optional(),
    logo: z.string().nullable().optional(),
    media_id: z.number().int().optional(),
    share_link: z.string().nullable().optional(),
    title: z.string().nullable().optional(),
  }).optional().describe(
    "Public details for the video including title, share link, channel link, and logo.",
  ),
  readyToStream: z.unknown().optional(),
  readyToStreamAt: z.unknown().optional(),
  requireSignedURLs: z.unknown().optional(),
  scheduledDeletion: z.unknown().optional(),
  size: z.unknown().optional(),
  status: z.unknown().optional(),
  thumbnail: z.unknown().optional(),
  thumbnailTimestampPct: z.unknown().optional(),
  uid: z.unknown().optional(),
  uploadExpiry: z.unknown().optional(),
  uploaded: z.unknown().optional(),
  watermark: z.unknown().optional(),
});

const CreateStreamVideosUploadVideosViaDirectUploadUrLsSchema = z.object({
  scheduledDeletion: z.unknown().optional(),
  uid: z.unknown().optional(),
  uploadURL: z.string().optional().describe(
    "The URL an unauthenticated upload can use for a single `HTTP POST multipart/form-data` request.",
  ),
  watermark: z.unknown().optional(),
});

const SigningKeysItemSchema = z.object({
  created: z.unknown().optional(),
  id: z.unknown().optional(),
  key_id: z.string().optional().describe(
    "The unique identifier for the signing key.",
  ),
});

const ListSigningKeysSchema = z.object({
  items: z.array(SigningKeysItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const ListLiveInputsSchema = z.object({
  liveInputs: z.array(z.unknown()).optional(),
  range: z.number().int().optional().describe(
    "The total number of remaining live inputs based on cursor position.",
  ),
  total: z.number().int().optional().describe(
    "The total number of live inputs that match the provided filters.",
  ),
});

const CreateALiveInputSchema = z.object({
  created: z.unknown().optional(),
  deleteRecordingAfterDays: z.unknown().optional(),
  enabled: z.unknown().optional(),
  keysRotatedAt: z.unknown().optional(),
  meta: z.unknown().optional(),
  modified: z.unknown().optional(),
  preferLowLatency: z.unknown().optional(),
  recording: z.unknown().optional(),
  rtmps: z.unknown().optional(),
  rtmpsPlayback: z.unknown().optional(),
  srt: z.unknown().optional(),
  srtPlayback: z.unknown().optional(),
  status: z.unknown().optional(),
  uid: z.unknown().optional(),
  webRTC: z.unknown().optional(),
  webRTCPlayback: z.unknown().optional(),
});

const GetStreamLiveInputsRetrieveALiveInputSchema = z.object({
  created: z.unknown().optional(),
  deleteRecordingAfterDays: z.unknown().optional(),
  enabled: z.unknown().optional(),
  keysRotatedAt: z.unknown().optional(),
  meta: z.unknown().optional(),
  modified: z.unknown().optional(),
  preferLowLatency: z.unknown().optional(),
  recording: z.unknown().optional(),
  rtmps: z.unknown().optional(),
  rtmpsPlayback: z.unknown().optional(),
  srt: z.unknown().optional(),
  srtPlayback: z.unknown().optional(),
  status: z.unknown().optional(),
  uid: z.unknown().optional(),
  webRTC: z.unknown().optional(),
  webRTCPlayback: z.unknown().optional(),
});

const StreamLiveInputsDisableALiveInputSchema = z.object({
  created: z.unknown().optional(),
  deleteRecordingAfterDays: z.unknown().optional(),
  enabled: z.unknown().optional(),
  keysRotatedAt: z.unknown().optional(),
  meta: z.unknown().optional(),
  modified: z.unknown().optional(),
  preferLowLatency: z.unknown().optional(),
  recording: z.unknown().optional(),
  rtmps: z.unknown().optional(),
  rtmpsPlayback: z.unknown().optional(),
  srt: z.unknown().optional(),
  srtPlayback: z.unknown().optional(),
  status: z.unknown().optional(),
  uid: z.unknown().optional(),
  webRTC: z.unknown().optional(),
  webRTCPlayback: z.unknown().optional(),
});

const StreamLiveInputsEnableALiveInputSchema = z.object({
  created: z.unknown().optional(),
  deleteRecordingAfterDays: z.unknown().optional(),
  enabled: z.unknown().optional(),
  keysRotatedAt: z.unknown().optional(),
  meta: z.unknown().optional(),
  modified: z.unknown().optional(),
  preferLowLatency: z.unknown().optional(),
  recording: z.unknown().optional(),
  rtmps: z.unknown().optional(),
  rtmpsPlayback: z.unknown().optional(),
  srt: z.unknown().optional(),
  srtPlayback: z.unknown().optional(),
  status: z.unknown().optional(),
  uid: z.unknown().optional(),
  webRTC: z.unknown().optional(),
  webRTCPlayback: z.unknown().optional(),
});

const AllOutputsAssociatedWithASpecifiedLiveInputItemSchema = z.object({
  enabled: z.unknown().optional(),
  streamKey: z.unknown().optional(),
  uid: z.unknown().optional(),
  url: z.unknown().optional(),
});

const ListAllOutputsAssociatedWithASpecifiedLiveInputSchema = z.object({
  items: z.array(AllOutputsAssociatedWithASpecifiedLiveInputItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateANewOutputConnectedToALiveInputSchema = z.object({
  enabled: z.unknown().optional(),
  streamKey: z.unknown().optional(),
  uid: z.unknown().optional(),
  url: z.unknown().optional(),
});

const UpdateAnOutputSchema = z.object({
  enabled: z.unknown().optional(),
  streamKey: z.unknown().optional(),
  uid: z.unknown().optional(),
  url: z.unknown().optional(),
});

const StreamLiveInputsRotateKeysForALiveInputSchema = z.object({
  created: z.unknown().optional(),
  deleteRecordingAfterDays: z.unknown().optional(),
  enabled: z.unknown().optional(),
  keysRotatedAt: z.unknown().optional(),
  meta: z.unknown().optional(),
  modified: z.unknown().optional(),
  preferLowLatency: z.unknown().optional(),
  recording: z.unknown().optional(),
  rtmps: z.unknown().optional(),
  rtmpsPlayback: z.unknown().optional(),
  srt: z.unknown().optional(),
  srtPlayback: z.unknown().optional(),
  status: z.unknown().optional(),
  uid: z.unknown().optional(),
  webRTC: z.unknown().optional(),
  webRTCPlayback: z.unknown().optional(),
});

const GetStreamVideosStorageUsageSchema = z.object({
  creator: z.unknown().optional(),
  totalStorageMinutes: z.number().optional().describe(
    "The total minutes of video content stored in the account. May contain decimal values.",
  ),
  totalStorageMinutesLimit: z.number().int().optional().describe(
    "The storage capacity alloted for the account.",
  ),
  videoCount: z.number().int().optional().describe(
    "The total count of videos associated with the account.",
  ),
});

const GetAccountStreamUsageItemSchema = z.object({
  streamMinutesViewed: z.number().int().optional().describe(
    "Number of Stream billable minutes viewed in this time period.",
  ),
  ts: z.number().int().optional().describe(
    "Unix timestamp (epoch seconds) for the start of this time period.",
  ),
});

const GetAccountStreamUsageSchema = z.object({
  items: z.array(GetAccountStreamUsageItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const WatermarkProfilesItemSchema = z.object({
  created: z.unknown().optional(),
  downloadedFrom: z.unknown().optional(),
  height: z.unknown().optional(),
  name: z.unknown().optional(),
  opacity: z.unknown().optional(),
  padding: z.unknown().optional(),
  position: z.unknown().optional(),
  scale: z.unknown().optional(),
  size: z.unknown().optional(),
  uid: z.unknown().optional(),
  width: z.unknown().optional(),
});

const ListWatermarkProfilesSchema = z.object({
  items: z.array(WatermarkProfilesItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateWatermarkProfilesViaBasicUploadSchema = z.object({
  created: z.unknown().optional(),
  downloadedFrom: z.unknown().optional(),
  height: z.unknown().optional(),
  name: z.unknown().optional(),
  opacity: z.unknown().optional(),
  padding: z.unknown().optional(),
  position: z.unknown().optional(),
  scale: z.unknown().optional(),
  size: z.unknown().optional(),
  uid: z.unknown().optional(),
  width: z.unknown().optional(),
});

const GetStreamWatermarkProfileWatermarkProfileDetailsSchema = z.object({
  created: z.unknown().optional(),
  downloadedFrom: z.unknown().optional(),
  height: z.unknown().optional(),
  name: z.unknown().optional(),
  opacity: z.unknown().optional(),
  padding: z.unknown().optional(),
  position: z.unknown().optional(),
  scale: z.unknown().optional(),
  size: z.unknown().optional(),
  uid: z.unknown().optional(),
  width: z.unknown().optional(),
});

const GetStreamWebhookViewWebhooksSchema = z.object({
  modified: z.string().optional().describe(
    "The date and time the webhook was last modified.",
  ),
  notificationUrl: z.string().optional().describe(
    "The URL where webhooks will be sent.",
  ),
  notification_url: z.string().optional().describe(
    "The URL where webhooks will be sent.",
  ),
  secret: z.string().optional().describe(
    "The secret used to verify webhook signatures.",
  ),
});

const CreateWebhooksSchema = z.object({
  modified: z.string().optional().describe(
    "The date and time the webhook was last modified.",
  ),
  notificationUrl: z.string().optional().describe(
    "The URL where webhooks will be sent.",
  ),
  notification_url: z.string().optional().describe(
    "The URL where webhooks will be sent.",
  ),
  secret: z.string().optional().describe(
    "The secret used to verify webhook signatures.",
  ),
});

const GetStreamVideosRetrieveVideoDetailsSchema = z.object({
  allowedOrigins: z.unknown().optional(),
  clippedFrom: z.string().optional().describe(
    "The unique identifier of the source video this video was clipped from.",
  ),
  created: z.unknown().optional(),
  creator: z.unknown().optional(),
  duration: z.unknown().optional(),
  input: z.unknown().optional(),
  liveInput: z.unknown().optional(),
  maxDurationSeconds: z.unknown().optional(),
  maxSizeBytes: z.number().int().optional().describe(
    "The maximum size in bytes for the video upload.",
  ),
  meta: z.unknown().optional(),
  modified: z.unknown().optional(),
  playback: z.unknown().optional(),
  preview: z.unknown().optional(),
  publicDetails: z.object({
    channel_link: z.string().nullable().optional(),
    logo: z.string().nullable().optional(),
    media_id: z.number().int().optional(),
    share_link: z.string().nullable().optional(),
    title: z.string().nullable().optional(),
  }).optional().describe(
    "Public details for the video including title, share link, channel link, and logo.",
  ),
  readyToStream: z.unknown().optional(),
  readyToStreamAt: z.unknown().optional(),
  requireSignedURLs: z.unknown().optional(),
  scheduledDeletion: z.unknown().optional(),
  size: z.unknown().optional(),
  status: z.unknown().optional(),
  thumbnail: z.unknown().optional(),
  thumbnailTimestampPct: z.unknown().optional(),
  uid: z.unknown().optional(),
  uploadExpiry: z.unknown().optional(),
  uploaded: z.unknown().optional(),
  watermark: z.unknown().optional(),
});

const UpdateVideoDetailsSchema = z.object({
  allowedOrigins: z.unknown().optional(),
  clippedFrom: z.string().optional().describe(
    "The unique identifier of the source video this video was clipped from.",
  ),
  created: z.unknown().optional(),
  creator: z.unknown().optional(),
  duration: z.unknown().optional(),
  input: z.unknown().optional(),
  liveInput: z.unknown().optional(),
  maxDurationSeconds: z.unknown().optional(),
  maxSizeBytes: z.number().int().optional().describe(
    "The maximum size in bytes for the video upload.",
  ),
  meta: z.unknown().optional(),
  modified: z.unknown().optional(),
  playback: z.unknown().optional(),
  preview: z.unknown().optional(),
  publicDetails: z.object({
    channel_link: z.string().nullable().optional(),
    logo: z.string().nullable().optional(),
    media_id: z.number().int().optional(),
    share_link: z.string().nullable().optional(),
    title: z.string().nullable().optional(),
  }).optional().describe(
    "Public details for the video including title, share link, channel link, and logo.",
  ),
  readyToStream: z.unknown().optional(),
  readyToStreamAt: z.unknown().optional(),
  requireSignedURLs: z.unknown().optional(),
  scheduledDeletion: z.unknown().optional(),
  size: z.unknown().optional(),
  status: z.unknown().optional(),
  thumbnail: z.unknown().optional(),
  thumbnailTimestampPct: z.unknown().optional(),
  uid: z.unknown().optional(),
  uploadExpiry: z.unknown().optional(),
  uploaded: z.unknown().optional(),
  watermark: z.unknown().optional(),
});

const ListAudioTracksSchema = z.object({
  audio: z.array(z.unknown()).optional().describe(
    "Array of audio tracks for the video.",
  ),
});

const CreateAddAudioTrackSchema = z.object({
  default: z.unknown().optional(),
  label: z.unknown().optional(),
  status: z.unknown().optional(),
  uid: z.unknown().optional(),
});

const UpdateEditAudioTracksSchema = z.object({
  default: z.unknown().optional(),
  label: z.unknown().optional(),
  status: z.unknown().optional(),
  uid: z.unknown().optional(),
});

const CaptionsOrSubtitlesItemSchema = z.object({
  generated: z.unknown().optional(),
  label: z.unknown().optional(),
  language: z.unknown().optional(),
  status: z.unknown().optional(),
});

const ListCaptionsOrSubtitlesSchema = z.object({
  items: z.array(CaptionsOrSubtitlesItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetCaptionOrSubtitleForLanguageSchema = z.object({
  generated: z.unknown().optional(),
  label: z.unknown().optional(),
  language: z.unknown().optional(),
  status: z.unknown().optional(),
});

const UpdateStreamSubtitlesCaptionsUploadCaptionsOrSubtitlesSchema = z.object({
  generated: z.unknown().optional(),
  label: z.unknown().optional(),
  language: z.unknown().optional(),
  status: z.unknown().optional(),
});

const StreamSubtitlesCaptionsGenerateCaptionOrSubtitleForLanguageSchema = z
  .object({
    generated: z.unknown().optional(),
    label: z.unknown().optional(),
    language: z.unknown().optional(),
    status: z.unknown().optional(),
  });

const ListDownloadsSchema = z.object({
  audio: z.unknown().optional().describe(
    "The audio-only download. Only present if this download type has been created.",
  ),
  default: z.unknown().optional().describe(
    "The default video download. Only present if this download type has been created.",
  ),
});

const CreateDownloadsSchema = z.object({
  audio: z.unknown().optional().describe(
    "The audio-only download. Only present if this download type has been created.",
  ),
  default: z.unknown().optional().describe(
    "The default video download. Only present if this download type has been created.",
  ),
});

const CreateTypeSpecificDownloadsSchema = z.object({
  audio: z.unknown().optional().describe(
    "The audio-only download. Only present if this download type has been created.",
  ),
  default: z.unknown().optional().describe(
    "The default video download. Only present if this download type has been created.",
  ),
});

const CreateSignedUrlTokensForVideosSchema = z.object({
  token: z.string().optional().describe(
    "The signed token used with the signed URLs feature.",
  ),
});

// =============================================================================
// Model Definition
// =============================================================================

/** Cloudflare Stream — video upload, encoding, delivery, live streaming */
export const model = {
  type: "@webframp/cloudflare/stream",
  version: "2026.07.19.1",
  globalArguments: GlobalArgsSchema,

  upgrades: [],

  resources: {
    "videos": {
      description: "List videos",
      schema: ListVideosSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "stream_videos_initiate_video_uploads_using_tus": {
      description: "Initiate video uploads using TUS",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "stream_video_clipping_clip_videos_given_a_start_and_end_time": {
      description: "Clip videos given a start and end time",
      schema: CreateStreamVideoClippingClipVideosGivenAStartAndEndTimeSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "stream_videos_upload_videos_from_a_url": {
      description: "Upload videos from a URL",
      schema: CreateStreamVideosUploadVideosFromAUrlSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "stream_videos_upload_videos_via_direct_upload_ur_ls": {
      description: "Upload videos via direct upload URLs",
      schema: CreateStreamVideosUploadVideosViaDirectUploadUrLsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "signing_keys": {
      description: "List signing keys",
      schema: ListSigningKeysSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "list_live_inputs": {
      description: "List live inputs",
      schema: ListLiveInputsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "a_live_input": {
      description: "Create a live input",
      schema: CreateALiveInputSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "stream_live_inputs_retrieve_a_live_input": {
      description: "Retrieve a live input",
      schema: GetStreamLiveInputsRetrieveALiveInputSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "stream_live_inputs_disable_a_live_input": {
      description: "Disable a live input",
      schema: StreamLiveInputsDisableALiveInputSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "stream_live_inputs_enable_a_live_input": {
      description: "Enable a live input",
      schema: StreamLiveInputsEnableALiveInputSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "all_outputs_associated_with_a_specified_live_input": {
      description: "List all outputs associated with a specified live input",
      schema: ListAllOutputsAssociatedWithASpecifiedLiveInputSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "a_new_output_connected_to_a_live_input": {
      description: "Create a new output, connected to a live input",
      schema: CreateANewOutputConnectedToALiveInputSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "an_output": {
      description: "Update an output",
      schema: UpdateAnOutputSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "stream_live_inputs_rotate_keys_for_a_live_input": {
      description: "Rotate keys for a live input",
      schema: StreamLiveInputsRotateKeysForALiveInputSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "stream_videos_storage_usage": {
      description: "Storage use",
      schema: GetStreamVideosStorageUsageSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "get_account_stream_usage": {
      description: "Get account Stream usage",
      schema: GetAccountStreamUsageSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "watermark_profiles": {
      description: "List watermark profiles",
      schema: ListWatermarkProfilesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "watermark_profiles_via_basic_upload": {
      description: "Create watermark profiles via basic upload",
      schema: CreateWatermarkProfilesViaBasicUploadSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "stream_watermark_profile_watermark_profile_details": {
      description: "Watermark profile details",
      schema: GetStreamWatermarkProfileWatermarkProfileDetailsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "stream_webhook_view_webhooks": {
      description: "View webhooks",
      schema: GetStreamWebhookViewWebhooksSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "webhooks": {
      description: "Create webhooks",
      schema: CreateWebhooksSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "stream_videos_retrieve_video_details": {
      description: "Retrieve video details",
      schema: GetStreamVideosRetrieveVideoDetailsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "video_details": {
      description: "Edit video details",
      schema: UpdateVideoDetailsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "list_audio_tracks": {
      description: "List additional audio tracks on a video",
      schema: ListAudioTracksSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "add_audio_track": {
      description: "Add audio tracks to a video",
      schema: CreateAddAudioTrackSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "edit_audio_tracks": {
      description: "Edit additional audio tracks on a video",
      schema: UpdateEditAudioTracksSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "captions_or_subtitles": {
      description: "List captions or subtitles",
      schema: ListCaptionsOrSubtitlesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "caption_or_subtitle_for_language": {
      description: "List captions or subtitles for a provided language",
      schema: GetCaptionOrSubtitleForLanguageSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "stream_subtitles_captions_upload_captions_or_subtitles": {
      description: "Upload captions or subtitles",
      schema: UpdateStreamSubtitlesCaptionsUploadCaptionsOrSubtitlesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "stream_subtitles_captions_generate_caption_or_subtitle_for_language": {
      description:
        "Generate captions or subtitles for a provided language via AI",
      schema: StreamSubtitlesCaptionsGenerateCaptionOrSubtitleForLanguageSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "vtt_caption_or_subtitle": {
      description: "Return WebVTT captions for a provided language",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "list_downloads": {
      description: "List downloads",
      schema: ListDownloadsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "downloads": {
      description: "Create downloads",
      schema: CreateDownloadsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "type_specific_downloads": {
      description: "Create download",
      schema: CreateTypeSpecificDownloadsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "stream_videos_retreieve_embed_code_html": {
      description: "Retrieve embed Code HTML",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "signed_url_tokens_for_videos": {
      description: "Create signed URL tokens for videos",
      schema: CreateSignedUrlTokensForVideosSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
  },

  methods: {
    list_videos: {
      description: "List videos",
      arguments: z.object({
        status: z.string().optional(),
        creator: z.string().optional(),
        type: z.string().optional(),
        asc: z.string().optional(),
        video_name: z.string().optional(),
        search: z.string().optional(),
        start: z.string().optional(),
        end: z.string().optional(),
        include_counts: z.string().optional(),
        id: z.string().optional().describe(
          "Filter by video ID(s). Can be a single ID or a comma-separated list of IDs.",
        ),
        name: z.string().optional().describe(
          "Filter by video name/UID(s). Can be a single name or a comma-separated list.",
        ),
        live_input_id: z.string().optional().describe(
          "Filter by live input ID to find videos associated with a specific live stream.",
        ),
        before: z.string().optional().describe(
          "Alias for 'end'. Returns videos created before this date/time (RFC 3339 format).",
        ),
        after: z.string().optional().describe(
          "Alias for 'start'. Returns videos created after this date/time (RFC 3339 format).",
        ),
        limit: z.number().optional().describe(
          "Maximum number of videos to return (default 1000, max 1000).",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/stream`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("videos", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} videos", { count: results.length });
        return { dataHandles: [handle] };
      },
    },
    stream_videos_initiate_video_uploads_using_tus: {
      description: "Initiate video uploads using TUS",
      arguments: z.object({
        direct_user: z.string().optional(),
      }),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/stream`,
        );

        const handle = await context.writeResource(
          "stream_videos_initiate_video_uploads_using_tus",
          "latest",
          result,
        );
        context.logger.info(
          "Executed stream_videos_initiate_video_uploads_using_tus",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    create_stream_video_clipping_clip_videos_given_a_start_and_end_time: {
      description: "Clip videos given a start and end time",
      arguments: z.object({
        allowedOrigins: z.unknown().optional(),
        clippedFromVideoUID: z.unknown(),
        creator: z.unknown().optional(),
        endTimeSeconds: z.unknown(),
        input: z.string().optional().describe(
          "A video's URL. Preferred over 'url'.",
        ),
        meta: z.unknown().optional(),
        name: z.string().optional().describe("A name for the video."),
        requireSignedURLs: z.unknown().optional(),
        scheduledDeletion: z.unknown().optional(),
        startTimeSeconds: z.unknown(),
        thumbnailTimestampPct: z.unknown().optional(),
        url: z.string().optional().describe(
          "A video's URL (legacy field, use 'input' instead).",
        ),
        watermark: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/stream/clip`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "stream_video_clipping_clip_videos_given_a_start_and_end_time",
          id,
          result,
        );
        context.logger.info(
          "Created stream_video_clipping_clip_videos_given_a_start_and_end_time {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    create_stream_videos_upload_videos_from_a_url: {
      description: "Upload videos from a URL",
      arguments: z.object({
        allowedOrigins: z.unknown().optional(),
        creator: z.unknown().optional(),
        input: z.string().optional().describe(
          "A video's URL. The server must be publicly routable and support `HTTP HEAD` r...",
        ),
        meta: z.unknown().optional(),
        name: z.string().optional().describe(
          "A video's name. Used for legacy compatibility.",
        ),
        requireSignedURLs: z.unknown().optional(),
        scheduledDeletion: z.unknown().optional(),
        thumbnailTimestampPct: z.unknown().optional(),
        url: z.string().optional().describe(
          "A video's URL. The server must be publicly routable and support `HTTP HEAD` r...",
        ),
        watermark: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/stream/copy`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "stream_videos_upload_videos_from_a_url",
          id,
          result,
        );
        context.logger.info(
          "Created stream_videos_upload_videos_from_a_url {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    create_stream_videos_upload_videos_via_direct_upload_ur_ls: {
      description: "Upload videos via direct upload URLs",
      arguments: z.object({
        allowedOrigins: z.unknown().optional(),
        creator: z.unknown().optional(),
        expiry: z.string().optional().describe(
          "The date and time after upload when videos will not be accepted.",
        ),
        maxDurationSeconds: z.unknown(),
        meta: z.unknown().optional(),
        requireSignedURLs: z.unknown().optional(),
        scheduledDeletion: z.unknown().optional(),
        thumbnailTimestampPct: z.unknown().optional(),
        watermark: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/stream/direct_upload`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "stream_videos_upload_videos_via_direct_upload_ur_ls",
          id,
          result,
        );
        context.logger.info(
          "Created stream_videos_upload_videos_via_direct_upload_ur_ls {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    list_signing_keys: {
      description: "List signing keys",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/stream/keys`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("signing_keys", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} signing_keys", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_signing_keys: {
      description: "Create signing keys",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/stream/keys`,
        );

        const handle = await context.writeResource(
          "create_signing_keys",
          "latest",
          result,
        );
        context.logger.info("Executed create_signing_keys", {});
        return { dataHandles: [handle] };
      },
    },
    delete_signing_keys: {
      description: "Delete signing keys",
      arguments: z.object({
        identifier: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/stream/keys/${args.identifier}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.identifier });
        return { dataHandles: [] };
      },
    },
    list_live_inputs: {
      description: "List live inputs",
      arguments: z.object({
        include_counts: z.string().optional(),
      }),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/stream/live_inputs`,
        );

        const handle = await context.writeResource(
          "list_live_inputs",
          "latest",
          result,
        );
        context.logger.info("Fetched list_live_inputs", {});
        return { dataHandles: [handle] };
      },
    },
    create_a_live_input: {
      description: "Create a live input",
      arguments: z.object({
        defaultCreator: z.unknown().optional(),
        deleteRecordingAfterDays: z.unknown().optional(),
        enabled: z.unknown().optional(),
        meta: z.unknown().optional(),
        preferLowLatency: z.unknown().optional(),
        recording: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/stream/live_inputs`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource("a_live_input", id, result);
        context.logger.info("Created a_live_input {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_stream_live_inputs_retrieve_a_live_input: {
      description: "Retrieve a live input",
      arguments: z.object({
        live_input_identifier: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/stream/live_inputs/${args.live_input_identifier}`,
        );

        const handle = await context.writeResource(
          "stream_live_inputs_retrieve_a_live_input",
          String(args.live_input_identifier),
          result,
        );
        context.logger.info(
          "Fetched stream_live_inputs_retrieve_a_live_input",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    update_a_live_input: {
      description: "Update a live input",
      arguments: z.object({
        live_input_identifier: z.string(),
        defaultCreator: z.unknown().optional(),
        deleteRecordingAfterDays: z.unknown().optional(),
        enabled: z.unknown().optional(),
        meta: z.unknown().optional(),
        preferLowLatency: z.unknown().optional(),
        recording: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/stream/live_inputs/${args.live_input_identifier}`,
          args,
        );

        const handle = await context.writeResource(
          "a_live_input",
          String(args.live_input_identifier),
          result,
        );
        context.logger.info("Updated a_live_input", {});
        return { dataHandles: [handle] };
      },
    },
    delete_a_live_input: {
      description: "Delete a live input",
      arguments: z.object({
        live_input_identifier: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/stream/live_inputs/${args.live_input_identifier}`,
        );

        context.logger.info("Deleted resource {id}", {
          id: args.live_input_identifier,
        });
        return { dataHandles: [] };
      },
    },
    stream_live_inputs_disable_a_live_input: {
      description: "Disable a live input",
      arguments: z.object({
        live_input_identifier: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/stream/live_inputs/${args.live_input_identifier}/disable`,
        );

        const handle = await context.writeResource(
          "stream_live_inputs_disable_a_live_input",
          "latest",
          result,
        );
        context.logger.info(
          "Executed stream_live_inputs_disable_a_live_input",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    stream_live_inputs_enable_a_live_input: {
      description: "Enable a live input",
      arguments: z.object({
        live_input_identifier: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/stream/live_inputs/${args.live_input_identifier}/enable`,
        );

        const handle = await context.writeResource(
          "stream_live_inputs_enable_a_live_input",
          "latest",
          result,
        );
        context.logger.info(
          "Executed stream_live_inputs_enable_a_live_input",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    list_all_outputs_associated_with_a_specified_live_input: {
      description: "List all outputs associated with a specified live input",
      arguments: z.object({
        live_input_identifier: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const pathParams = new Set(["live_input_identifier"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !pathParams.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/stream/live_inputs/${args.live_input_identifier}/outputs`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "all_outputs_associated_with_a_specified_live_input",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info(
          "Found {count} all_outputs_associated_with_a_specified_live_input",
          { count: results.length },
        );
        return { dataHandles: [handle] };
      },
    },
    create_a_new_output_connected_to_a_live_input: {
      description: "Create a new output, connected to a live input",
      arguments: z.object({
        live_input_identifier: z.string(),
        enabled: z.unknown().optional(),
        streamKey: z.unknown(),
        url: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/stream/live_inputs/${args.live_input_identifier}/outputs`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "a_new_output_connected_to_a_live_input",
          id,
          result,
        );
        context.logger.info(
          "Created a_new_output_connected_to_a_live_input {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    update_an_output: {
      description: "Update an output",
      arguments: z.object({
        output_identifier: z.string(),
        live_input_identifier: z.string(),
        enabled: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/stream/live_inputs/${args.live_input_identifier}/outputs/${args.output_identifier}`,
          args,
        );

        const handle = await context.writeResource(
          "an_output",
          String(args.live_input_identifier),
          result,
        );
        context.logger.info("Updated an_output", {});
        return { dataHandles: [handle] };
      },
    },
    delete_an_output: {
      description: "Delete an output",
      arguments: z.object({
        output_identifier: z.string(),
        live_input_identifier: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/stream/live_inputs/${args.live_input_identifier}/outputs/${args.output_identifier}`,
        );

        context.logger.info("Deleted resource {id}", {
          id: args.live_input_identifier,
        });
        return { dataHandles: [] };
      },
    },
    stream_live_inputs_rotate_keys_for_a_live_input: {
      description: "Rotate keys for a live input",
      arguments: z.object({
        live_input_identifier: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/stream/live_inputs/${args.live_input_identifier}/rotate_keys`,
        );

        const handle = await context.writeResource(
          "stream_live_inputs_rotate_keys_for_a_live_input",
          "latest",
          result,
        );
        context.logger.info(
          "Executed stream_live_inputs_rotate_keys_for_a_live_input",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    get_stream_videos_storage_usage: {
      description: "Storage use",
      arguments: z.object({
        creator: z.string().optional(),
      }),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/stream/storage-usage`,
        );

        const handle = await context.writeResource(
          "stream_videos_storage_usage",
          "latest",
          result,
        );
        context.logger.info("Fetched stream_videos_storage_usage", {});
        return { dataHandles: [handle] };
      },
    },
    get_account_stream_usage: {
      description: "Get account Stream usage",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/stream/usage`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "get_account_stream_usage",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} get_account_stream_usage", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    list_watermark_profiles: {
      description: "List watermark profiles",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/stream/watermarks`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "watermark_profiles",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} watermark_profiles", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_watermark_profiles_via_basic_upload: {
      description: "Create watermark profiles via basic upload",
      arguments: z.object({
        name: z.unknown().optional(),
        opacity: z.unknown().optional(),
        padding: z.unknown().optional(),
        position: z.unknown().optional(),
        scale: z.unknown().optional(),
        url: z.string().optional().describe(
          "URL of the watermark image to copy.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/stream/watermarks`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "watermark_profiles_via_basic_upload",
          id,
          result,
        );
        context.logger.info(
          "Created watermark_profiles_via_basic_upload {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    get_stream_watermark_profile_watermark_profile_details: {
      description: "Watermark profile details",
      arguments: z.object({
        identifier: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/stream/watermarks/${args.identifier}`,
        );

        const handle = await context.writeResource(
          "stream_watermark_profile_watermark_profile_details",
          String(args.identifier),
          result,
        );
        context.logger.info(
          "Fetched stream_watermark_profile_watermark_profile_details",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    delete_watermark_profiles: {
      description: "Delete watermark profiles",
      arguments: z.object({
        identifier: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/stream/watermarks/${args.identifier}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.identifier });
        return { dataHandles: [] };
      },
    },
    get_stream_webhook_view_webhooks: {
      description: "View webhooks",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/stream/webhook`,
        );

        const handle = await context.writeResource(
          "stream_webhook_view_webhooks",
          "latest",
          result,
        );
        context.logger.info("Fetched stream_webhook_view_webhooks", {});
        return { dataHandles: [handle] };
      },
    },
    create_webhooks: {
      description: "Create webhooks",
      arguments: z.object({
        notificationUrl: z.unknown().optional(),
        notification_url: z.string().optional().describe(
          "The URL where webhooks will be sent.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/stream/webhook`,
          args,
        );

        const handle = await context.writeResource(
          "create_webhooks",
          "updated",
          result,
        );
        context.logger.info("Updated create_webhooks", {});
        return { dataHandles: [handle] };
      },
    },
    delete_webhooks: {
      description: "Delete webhooks",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/stream/webhook`,
        );

        context.logger.info("Deleted resource {id}", { id: "unknown" });
        return { dataHandles: [] };
      },
    },
    get_stream_videos_retrieve_video_details: {
      description: "Retrieve video details",
      arguments: z.object({
        identifier: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/stream/${args.identifier}`,
        );

        const handle = await context.writeResource(
          "stream_videos_retrieve_video_details",
          String(args.identifier),
          result,
        );
        context.logger.info("Fetched stream_videos_retrieve_video_details", {});
        return { dataHandles: [handle] };
      },
    },
    update_video_details: {
      description: "Edit video details",
      arguments: z.object({
        identifier: z.string(),
        allowedOrigins: z.unknown().optional(),
        creator: z.unknown().optional(),
        maxDurationSeconds: z.unknown().optional(),
        meta: z.unknown().optional(),
        publicDetails: z.object({
          channel_link: z.string().nullable().optional(),
          logo: z.string().nullable().optional(),
          share_link: z.string().nullable().optional(),
          title: z.string().nullable().optional(),
        }).optional().describe(
          "Public details for the video including title, share link, channel link, and l...",
        ),
        requireSignedURLs: z.unknown().optional(),
        scheduledDeletion: z.unknown().optional(),
        thumbnailTimestampPct: z.unknown().optional(),
        uid: z.string().optional().describe(
          "The unique identifier for the video. Can be used to verify the video being up...",
        ),
        uploadExpiry: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/stream/${args.identifier}`,
          args,
        );

        const handle = await context.writeResource(
          "update_video_details",
          "latest",
          result,
        );
        context.logger.info("Executed update_video_details", {});
        return { dataHandles: [handle] };
      },
    },
    delete_video: {
      description: "Delete video",
      arguments: z.object({
        identifier: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/stream/${args.identifier}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.identifier });
        return { dataHandles: [] };
      },
    },
    list_audio_tracks: {
      description: "List additional audio tracks on a video",
      arguments: z.object({
        identifier: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/stream/${args.identifier}/audio`,
        );

        const handle = await context.writeResource(
          "list_audio_tracks",
          String(args.identifier),
          result,
        );
        context.logger.info("Fetched list_audio_tracks", {});
        return { dataHandles: [handle] };
      },
    },
    create_add_audio_track: {
      description: "Add audio tracks to a video",
      arguments: z.object({
        identifier: z.string(),
        label: z.unknown(),
        url: z.string().optional().describe(
          "An audio track URL. The server must be publicly routable and support `HTTP HE...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/stream/${args.identifier}/audio/copy`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "add_audio_track",
          id,
          result,
        );
        context.logger.info("Created add_audio_track {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    update_edit_audio_tracks: {
      description: "Edit additional audio tracks on a video",
      arguments: z.object({
        identifier: z.string(),
        audio_identifier: z.string(),
        default: z.unknown().optional(),
        label: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/accounts/${accountId}/stream/${args.identifier}/audio/${args.audio_identifier}`,
          args,
        );

        const handle = await context.writeResource(
          "edit_audio_tracks",
          String(args.audio_identifier),
          result,
        );
        context.logger.info("Updated edit_audio_tracks", {});
        return { dataHandles: [handle] };
      },
    },
    delete_audio_tracks: {
      description: "Delete additional audio tracks on a video",
      arguments: z.object({
        identifier: z.string(),
        audio_identifier: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/stream/${args.identifier}/audio/${args.audio_identifier}`,
        );

        context.logger.info("Deleted resource {id}", {
          id: args.audio_identifier,
        });
        return { dataHandles: [] };
      },
    },
    list_captions_or_subtitles: {
      description: "List captions or subtitles",
      arguments: z.object({
        identifier: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const pathParams = new Set(["identifier"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !pathParams.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/stream/${args.identifier}/captions`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "captions_or_subtitles",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} captions_or_subtitles", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    get_caption_or_subtitle_for_language: {
      description: "List captions or subtitles for a provided language",
      arguments: z.object({
        language: z.string(),
        identifier: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/stream/${args.identifier}/captions/${args.language}`,
        );

        const handle = await context.writeResource(
          "caption_or_subtitle_for_language",
          String(args.identifier),
          result,
        );
        context.logger.info("Fetched caption_or_subtitle_for_language", {});
        return { dataHandles: [handle] };
      },
    },
    update_stream_subtitles_captions_upload_captions_or_subtitles: {
      description: "Upload captions or subtitles",
      arguments: z.object({
        language: z.string(),
        identifier: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/stream/${args.identifier}/captions/${args.language}`,
          args,
        );

        const handle = await context.writeResource(
          "stream_subtitles_captions_upload_captions_or_subtitles",
          String(args.identifier),
          result,
        );
        context.logger.info(
          "Updated stream_subtitles_captions_upload_captions_or_subtitles",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    delete_captions_or_subtitles: {
      description: "Delete captions or subtitles",
      arguments: z.object({
        language: z.string(),
        identifier: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/stream/${args.identifier}/captions/${args.language}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.identifier });
        return { dataHandles: [] };
      },
    },
    stream_subtitles_captions_generate_caption_or_subtitle_for_language: {
      description:
        "Generate captions or subtitles for a provided language via AI",
      arguments: z.object({
        language: z.string(),
        identifier: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/stream/${args.identifier}/captions/${args.language}/generate`,
        );

        const handle = await context.writeResource(
          "stream_subtitles_captions_generate_caption_or_subtitle_for_language",
          "latest",
          result,
        );
        context.logger.info(
          "Executed stream_subtitles_captions_generate_caption_or_subtitle_for_language",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    get_vtt_caption_or_subtitle: {
      description: "Return WebVTT captions for a provided language",
      arguments: z.object({
        language: z.string(),
        identifier: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/stream/${args.identifier}/captions/${args.language}/vtt`,
        );

        const handle = await context.writeResource(
          "vtt_caption_or_subtitle",
          String(args.identifier),
          result,
        );
        context.logger.info("Fetched vtt_caption_or_subtitle", {});
        return { dataHandles: [handle] };
      },
    },
    list_downloads: {
      description: "List downloads",
      arguments: z.object({
        identifier: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/stream/${args.identifier}/downloads`,
        );

        const handle = await context.writeResource(
          "list_downloads",
          String(args.identifier),
          result,
        );
        context.logger.info("Fetched list_downloads", {});
        return { dataHandles: [handle] };
      },
    },
    create_downloads: {
      description: "Create downloads",
      arguments: z.object({
        identifier: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/stream/${args.identifier}/downloads`,
        );

        const handle = await context.writeResource(
          "create_downloads",
          "latest",
          result,
        );
        context.logger.info("Executed create_downloads", {});
        return { dataHandles: [handle] };
      },
    },
    delete_downloads: {
      description: "Delete downloads",
      arguments: z.object({
        identifier: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/stream/${args.identifier}/downloads`,
        );

        context.logger.info("Deleted resource {id}", { id: args.identifier });
        return { dataHandles: [] };
      },
    },
    create_type_specific_downloads: {
      description: "Create download",
      arguments: z.object({
        identifier: z.string(),
        download_type: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/stream/${args.identifier}/downloads/${args.download_type}`,
        );

        const handle = await context.writeResource(
          "create_type_specific_downloads",
          "latest",
          result,
        );
        context.logger.info("Executed create_type_specific_downloads", {});
        return { dataHandles: [handle] };
      },
    },
    delete_type_specific_downloads: {
      description: "Delete download",
      arguments: z.object({
        identifier: z.string(),
        download_type: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/stream/${args.identifier}/downloads/${args.download_type}`,
        );

        context.logger.info("Deleted resource {id}", {
          id: args.download_type,
        });
        return { dataHandles: [] };
      },
    },
    get_stream_videos_retreieve_embed_code_html: {
      description: "Retrieve embed Code HTML",
      arguments: z.object({
        identifier: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/stream/${args.identifier}/embed`,
        );

        const handle = await context.writeResource(
          "stream_videos_retreieve_embed_code_html",
          String(args.identifier),
          result,
        );
        context.logger.info(
          "Fetched stream_videos_retreieve_embed_code_html",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    create_signed_url_tokens_for_videos: {
      description: "Create signed URL tokens for videos",
      arguments: z.object({
        identifier: z.string(),
        accessRules: z.array(z.unknown()).optional().describe(
          "The optional list of access rule constraints on the token. Access can be bloc...",
        ),
        downloadable: z.boolean().optional().describe(
          "The optional boolean value that enables using signed tokens to access MP4 dow...",
        ),
        exp: z.number().int().optional().describe(
          "The optional unix epoch timestamp that specficies the time after a token is n...",
        ),
        flags: z.object({
          original: z.boolean().optional(),
        }).optional().describe("Optional flags for the signed token."),
        nbf: z.number().int().optional().describe(
          "The optional unix epoch timestamp that specifies the time before a the token ...",
        ),
        pem: z.string().optional().describe(
          "The optional base64 encoded private key in PEM format associated with a Strea...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/stream/${args.identifier}/token`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "signed_url_tokens_for_videos",
          id,
          result,
        );
        context.logger.info("Created signed_url_tokens_for_videos {id}", {
          id,
        });
        return { dataHandles: [handle] };
      },
    },
  },
};
