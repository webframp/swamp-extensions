// Cloudflare Load Balancing — pools, monitors, load balancers, steering policies — Tests
// Auto-generated by scripts/cloudflare-codegen — do not edit manually.
// SPDX-License-Identifier: Apache-2.0

import { assertEquals, assertExists } from "jsr:@std/assert@1.0.19";
import { createModelTestContext } from "@systeminit/swamp-testing";
import { model } from "./load_balancing.ts";

// ---------------------------------------------------------------------------
// Model Export Structure Tests
// ---------------------------------------------------------------------------

Deno.test("load-balancing model: has correct type", () => {
  assertEquals(model.type, "@webframp/cloudflare/load-balancing");
});

Deno.test("load-balancing model: has valid version format", () => {
  const versionPattern = /^\d{4}\.\d{2}\.\d{2}\.\d+$/;
  assertEquals(versionPattern.test(model.version), true);
});

Deno.test("load-balancing model: has globalArguments with apiToken", () => {
  assertExists(model.globalArguments);
  const shape = model.globalArguments.shape;
  assertExists(shape.apiToken);
  assertExists(shape.accountId);
});

Deno.test("load-balancing model: has expected methods", () => {
  assertExists(model.methods);
  assertExists(model.methods.list_account_load_balancers);
  assertExists(model.methods.create_account_load_balancer);
  assertExists(model.methods.list_monitor_groups);
  assertExists(model.methods.create_monitor_group);
  assertExists(
    model.methods
      .get_account_load_balancer_monitor_groups_monitor_group_details,
  );
  assertExists(model.methods.update_monitor_group);
  assertExists(model.methods.patch_monitor_group);
  assertExists(model.methods.delete_monitor_group);
  assertExists(model.methods.list_monitor_group_references);
  assertExists(model.methods.list_monitors);
  assertExists(model.methods.create_monitor);
  assertExists(
    model.methods.get_account_load_balancer_monitors_monitor_details,
  );
  assertExists(model.methods.update_monitor);
  assertExists(model.methods.patch_monitor);
  assertExists(model.methods.delete_monitor);
  assertExists(
    model.methods.create_account_load_balancer_monitors_preview_monitor,
  );
  assertExists(model.methods.list_monitor_references);
  assertExists(model.methods.list_pools);
  assertExists(model.methods.create_pool);
  assertExists(model.methods.patch_pools);
  assertExists(model.methods.get_account_load_balancer_pools_pool_details);
  assertExists(model.methods.update_pool);
  assertExists(model.methods.patch_pool);
  assertExists(model.methods.delete_pool);
  assertExists(
    model.methods.get_account_load_balancer_pools_pool_health_details,
  );
  assertExists(model.methods.create_account_load_balancer_pools_preview_pool);
  assertExists(model.methods.list_pool_references);
  assertExists(model.methods.get_account_load_balancer_monitors_preview_result);
  assertExists(model.methods.list_regions);
  assertExists(model.methods.get_region);
  assertExists(model.methods.get_account_load_balancer_search_search_resources);
  assertExists(model.methods.list_load_balancer_usage);
  assertExists(
    model.methods.get_account_load_balancers_account_load_balancer_details,
  );
  assertExists(model.methods.update_account_load_balancer);
  assertExists(model.methods.patch_account_load_balancer);
  assertExists(model.methods.delete_account_load_balancer);
  assertExists(model.methods.list_load_balancers);
  assertExists(model.methods.create_load_balancer);
  assertExists(model.methods.get_load_balancers_load_balancer_details);
  assertExists(model.methods.update_load_balancer);
  assertExists(model.methods.patch_load_balancer);
  assertExists(model.methods.delete_load_balancer);
});

Deno.test("load-balancing model: has expected resources", () => {
  assertExists(model.resources);
  assertExists(model.resources["account_load_balancers"]);
  assertExists(model.resources["account_load_balancer"]);
  assertExists(model.resources["monitor_groups"]);
  assertExists(model.resources["monitor_group"]);
  assertExists(
    model
      .resources["account_load_balancer_monitor_groups_monitor_group_details"],
  );
  assertExists(model.resources["patch_monitor_group"]);
  assertExists(model.resources["monitor_group_references"]);
  assertExists(model.resources["monitors"]);
  assertExists(model.resources["monitor"]);
  assertExists(
    model.resources["account_load_balancer_monitors_monitor_details"],
  );
  assertExists(model.resources["patch_monitor"]);
  assertExists(
    model.resources["account_load_balancer_monitors_preview_monitor"],
  );
  assertExists(model.resources["monitor_references"]);
  assertExists(model.resources["pools"]);
  assertExists(model.resources["pool"]);
  assertExists(model.resources["patch_pools"]);
  assertExists(model.resources["account_load_balancer_pools_pool_details"]);
  assertExists(model.resources["patch_pool"]);
  assertExists(
    model.resources["account_load_balancer_pools_pool_health_details"],
  );
  assertExists(model.resources["account_load_balancer_pools_preview_pool"]);
  assertExists(model.resources["pool_references"]);
  assertExists(
    model.resources["account_load_balancer_monitors_preview_result"],
  );
  assertExists(model.resources["list_regions"]);
  assertExists(model.resources["region"]);
  assertExists(
    model.resources["account_load_balancer_search_search_resources"],
  );
  assertExists(model.resources["list_load_balancer_usage"]);
  assertExists(
    model.resources["account_load_balancers_account_load_balancer_details"],
  );
  assertExists(model.resources["patch_account_load_balancer"]);
  assertExists(model.resources["load_balancers"]);
  assertExists(model.resources["load_balancer"]);
  assertExists(model.resources["load_balancers_load_balancer_details"]);
  assertExists(model.resources["patch_load_balancer"]);
});

// ---------------------------------------------------------------------------
// Mock Cloudflare API Server
// ---------------------------------------------------------------------------

function startMockCfServer(
  responses: Record<string, { result: unknown; isCollection?: boolean }>,
): { url: string; server: Deno.HttpServer } {
  const server = Deno.serve({ port: 0, onListen() {} }, (req) => {
    const url = new URL(req.url);
    const path = url.pathname;

    for (
      const [pattern, { result, isCollection }] of Object.entries(responses)
    ) {
      if (path.includes(pattern)) {
        return Response.json({
          success: true,
          errors: [],
          messages: [],
          result,
          result_info: isCollection
            ? {
              page: 1,
              per_page: 50,
              total_count: Array.isArray(result) ? result.length : 1,
              total_pages: 1,
            }
            : undefined,
        });
      }
    }

    return Response.json({
      success: false,
      errors: [{ code: 404, message: "Not found" }],
      messages: [],
      result: null,
    });
  });

  const addr = server.addr as Deno.NetAddr;
  return { url: `http://localhost:${addr.port}`, server };
}

function installFetchMock(mockUrl: string): () => void {
  const originalFetch = globalThis.fetch;
  globalThis.fetch = (input, init) => {
    const reqUrl = typeof input === "string" ? input : input.toString();
    const newUrl = reqUrl.replace(
      "https://api.cloudflare.com/client/v4",
      mockUrl,
    );
    return originalFetch(newUrl, init);
  };
  return () => {
    globalThis.fetch = originalFetch;
  };
}

Deno.test({
  name:
    "load-balancing model: list_account_load_balancers fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = [{
      "adaptive_routing": null,
      "country_pools": null,
      "created_on": null,
      "default_pools": null,
      "description": null,
      "enabled": null,
      "fallback_pool": null,
      "id": null,
      "location_strategy": null,
      "modified_on": null,
      "name": null,
      "networks": null,
      "pool_sets": null,
      "pop_pools": null,
      "proxied": null,
      "random_steering": null,
      "region_pools": null,
      "rules": null,
      "session_affinity": null,
      "session_affinity_attributes": null,
      "session_affinity_ttl": null,
      "steering_policy": null,
      "ttl": null,
      "zone_name": null,
    }];
    const { url, server } = startMockCfServer({
      "/load_balancers": { result: mockData, isCollection: true },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: {
          id: "test-id",
          name: "test-load-balancing",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).list_account_load_balancers.execute({}, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name:
    "load-balancing model: create_account_load_balancer creates and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "id": null,
      "adaptive_routing": null,
      "country_pools": null,
      "created_on": null,
      "default_pools": null,
      "description": null,
      "enabled": null,
      "fallback_pool": null,
      "location_strategy": null,
      "modified_on": null,
      "name": null,
      "networks": null,
      "pool_sets": null,
      "pop_pools": null,
      "proxied": null,
      "random_steering": null,
      "region_pools": null,
      "rules": null,
      "session_affinity": null,
      "session_affinity_attributes": null,
      "session_affinity_ttl": null,
      "steering_policy": null,
      "ttl": null,
      "zone_name": null,
    };
    const { url, server } = startMockCfServer({
      "/load_balancers": { result: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: {
          id: "test-id",
          name: "test-load-balancing",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).create_account_load_balancer.execute({
        "adaptive_routing": null,
        "country_pools": null,
        "created_on": null,
        "default_pools": null,
        "description": null,
        "enabled": null,
        "fallback_pool": null,
        "id": null,
        "location_strategy": null,
        "modified_on": null,
        "name": null,
        "networks": null,
        "pool_sets": null,
        "pop_pools": null,
        "proxied": null,
        "random_steering": null,
        "region_pools": null,
        "rules": null,
        "session_affinity": null,
        "session_affinity_attributes": null,
        "session_affinity_ttl": null,
        "steering_policy": null,
        "ttl": null,
        "zone_name": null,
      }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name:
    "load-balancing model: get_account_load_balancer_monitor_groups_monitor_group_details fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "created_on": "2014-01-01T05:20:00.12345Z",
      "description": "Primary datacenter monitors",
      "id": null,
      "members": [null],
      "modified_on": "2014-01-01T05:20:00.12345Z",
    };
    const { url, server } = startMockCfServer({
      "/load_balancers/monitor_groups/test-id-123": { result: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: {
          id: "test-id",
          name: "test-load-balancing",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).get_account_load_balancer_monitor_groups_monitor_group_details
        .execute({ "monitor_group_id": "test-id-123" }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name:
    "load-balancing model: update_monitor_group executes and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "created_on": "2014-01-01T05:20:00.12345Z",
      "description": "Primary datacenter monitors",
      "id": null,
      "members": [null],
      "modified_on": "2014-01-01T05:20:00.12345Z",
    };
    const { url, server } = startMockCfServer({
      "/load_balancers/monitor_groups/test-id-123": { result: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: {
          id: "test-id",
          name: "test-load-balancing",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).update_monitor_group.execute({
        "monitor_group_id": "test-id-123",
        "created_on": "2014-01-01T05:20:00.12345Z",
        "description": "Primary datacenter monitors",
        "id": null,
        "members": [null],
        "modified_on": "2014-01-01T05:20:00.12345Z",
      }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "load-balancing model: delete_monitor_group executes successfully",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockCfServer({
      "/load_balancers/monitor_groups/test-id-123": {
        result: { id: "test-id-123" },
      },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: {
          id: "test-id",
          name: "test-load-balancing",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).delete_monitor_group.execute(
        { "monitor_group_id": "test-id-123" },
        context,
      );
      assertEquals(result.dataHandles.length, 0);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});
