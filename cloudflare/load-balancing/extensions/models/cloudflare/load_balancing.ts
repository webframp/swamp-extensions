/**
 * Cloudflare Load Balancing — pools, monitors, load balancers, steering policies
 *
 * Auto-generated by scripts/cloudflare-codegen — do not edit manually.
 *
 * @module
 */
// SPDX-License-Identifier: AGPL-3.0-or-later WITH Swamp-Extension-Exception

import { z } from "npm:zod@4.4.3";
import { cfApi, cfApiPaginated } from "./_lib/api.ts";

// =============================================================================
// Schemas
// =============================================================================

const GlobalArgsSchema = z.object({
  apiToken: z.string().meta({ sensitive: true }).describe(
    "Cloudflare API token",
  ),
  accountId: z.string().describe("Cloudflare account ID"),
});

const AccountLoadBalancersItemSchema = z.object({
  adaptive_routing: z.unknown().optional(),
  country_pools: z.unknown().optional(),
  created_on: z.unknown().optional(),
  default_pools: z.unknown().optional(),
  description: z.unknown().optional(),
  enabled: z.unknown().optional(),
  fallback_pool: z.unknown().optional(),
  id: z.unknown().optional(),
  location_strategy: z.unknown().optional(),
  modified_on: z.unknown().optional(),
  name: z.unknown().optional(),
  networks: z.unknown().optional(),
  pool_sets: z.unknown().optional(),
  pop_pools: z.unknown().optional(),
  proxied: z.unknown().optional(),
  random_steering: z.unknown().optional(),
  region_pools: z.unknown().optional(),
  rules: z.unknown().optional(),
  session_affinity: z.unknown().optional(),
  session_affinity_attributes: z.unknown().optional(),
  session_affinity_ttl: z.unknown().optional(),
  steering_policy: z.unknown().optional(),
  ttl: z.unknown().optional(),
  zone_name: z.unknown().optional(),
});

const ListAccountLoadBalancersSchema = z.object({
  items: z.array(AccountLoadBalancersItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateAccountLoadBalancerSchema = z.object({
  adaptive_routing: z.unknown().optional(),
  country_pools: z.unknown().optional(),
  created_on: z.unknown().optional(),
  default_pools: z.unknown().optional(),
  description: z.unknown().optional(),
  enabled: z.unknown().optional(),
  fallback_pool: z.unknown().optional(),
  id: z.unknown().optional(),
  location_strategy: z.unknown().optional(),
  modified_on: z.unknown().optional(),
  name: z.unknown().optional(),
  networks: z.unknown().optional(),
  pool_sets: z.unknown().optional(),
  pop_pools: z.unknown().optional(),
  proxied: z.unknown().optional(),
  random_steering: z.unknown().optional(),
  region_pools: z.unknown().optional(),
  rules: z.unknown().optional(),
  session_affinity: z.unknown().optional(),
  session_affinity_attributes: z.unknown().optional(),
  session_affinity_ttl: z.unknown().optional(),
  steering_policy: z.unknown().optional(),
  ttl: z.unknown().optional(),
  zone_name: z.unknown().optional(),
});

const MonitorGroupsItemSchema = z.object({
  created_on: z.string().optional().describe(
    "The timestamp of when the monitor group was created",
  ),
  description: z.string().describe("A short description of the monitor group"),
  id: z.unknown(),
  members: z.array(z.unknown()).describe("List of monitors in this group"),
  modified_on: z.string().optional().describe(
    "The timestamp of when the monitor group was last updated",
  ),
});

const ListMonitorGroupsSchema = z.object({
  items: z.array(MonitorGroupsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateMonitorGroupSchema = z.object({
  created_on: z.string().optional().describe(
    "The timestamp of when the monitor group was created",
  ),
  description: z.string().describe("A short description of the monitor group"),
  id: z.unknown(),
  members: z.array(z.unknown()).describe("List of monitors in this group"),
  modified_on: z.string().optional().describe(
    "The timestamp of when the monitor group was last updated",
  ),
});

const GetAccountLoadBalancerMonitorGroupsMonitorGroupDetailsSchema = z.object({
  created_on: z.string().optional().describe(
    "The timestamp of when the monitor group was created",
  ),
  description: z.string().describe("A short description of the monitor group"),
  id: z.unknown(),
  members: z.array(z.unknown()).describe("List of monitors in this group"),
  modified_on: z.string().optional().describe(
    "The timestamp of when the monitor group was last updated",
  ),
});

const PatchMonitorGroupSchema = z.object({
  created_on: z.string().optional().describe(
    "The timestamp of when the monitor group was created",
  ),
  description: z.string().describe("A short description of the monitor group"),
  id: z.unknown(),
  members: z.array(z.unknown()).describe("List of monitors in this group"),
  modified_on: z.string().optional().describe(
    "The timestamp of when the monitor group was last updated",
  ),
});

const MonitorGroupReferencesItemSchema = z.object({
  reference_type: z.enum(["*", "referral", "referrer"]).optional(),
  resource_id: z.string().optional(),
  resource_name: z.string().optional(),
  resource_type: z.string().optional(),
});

const ListMonitorGroupReferencesSchema = z.object({
  items: z.array(MonitorGroupReferencesItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const MonitorsItemSchema = z.object({
  allow_insecure: z.unknown().optional(),
  consecutive_down: z.unknown().optional(),
  consecutive_up: z.unknown().optional(),
  description: z.unknown().optional(),
  expected_body: z.unknown().optional(),
  expected_codes: z.unknown().optional(),
  follow_redirects: z.unknown().optional(),
  header: z.unknown().optional(),
  interval: z.unknown().optional(),
  method: z.unknown().optional(),
  path: z.unknown().optional(),
  port: z.unknown().optional(),
  probe_zone: z.unknown().optional(),
  retries: z.unknown().optional(),
  timeout: z.unknown().optional(),
  type: z.unknown().optional(),
  created_on: z.unknown().optional(),
  id: z.unknown().optional(),
  modified_on: z.unknown().optional(),
});

const ListMonitorsSchema = z.object({
  items: z.array(MonitorsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateMonitorSchema = z.object({
  allow_insecure: z.unknown().optional(),
  consecutive_down: z.unknown().optional(),
  consecutive_up: z.unknown().optional(),
  description: z.unknown().optional(),
  expected_body: z.unknown().optional(),
  expected_codes: z.unknown().optional(),
  follow_redirects: z.unknown().optional(),
  header: z.unknown().optional(),
  interval: z.unknown().optional(),
  method: z.unknown().optional(),
  path: z.unknown().optional(),
  port: z.unknown().optional(),
  probe_zone: z.unknown().optional(),
  retries: z.unknown().optional(),
  timeout: z.unknown().optional(),
  type: z.unknown().optional(),
  created_on: z.unknown().optional(),
  id: z.unknown().optional(),
  modified_on: z.unknown().optional(),
});

const GetAccountLoadBalancerMonitorsMonitorDetailsSchema = z.object({
  allow_insecure: z.unknown().optional(),
  consecutive_down: z.unknown().optional(),
  consecutive_up: z.unknown().optional(),
  description: z.unknown().optional(),
  expected_body: z.unknown().optional(),
  expected_codes: z.unknown().optional(),
  follow_redirects: z.unknown().optional(),
  header: z.unknown().optional(),
  interval: z.unknown().optional(),
  method: z.unknown().optional(),
  path: z.unknown().optional(),
  port: z.unknown().optional(),
  probe_zone: z.unknown().optional(),
  retries: z.unknown().optional(),
  timeout: z.unknown().optional(),
  type: z.unknown().optional(),
  created_on: z.unknown().optional(),
  id: z.unknown().optional(),
  modified_on: z.unknown().optional(),
});

const PatchMonitorSchema = z.object({
  allow_insecure: z.unknown().optional(),
  consecutive_down: z.unknown().optional(),
  consecutive_up: z.unknown().optional(),
  description: z.unknown().optional(),
  expected_body: z.unknown().optional(),
  expected_codes: z.unknown().optional(),
  follow_redirects: z.unknown().optional(),
  header: z.unknown().optional(),
  interval: z.unknown().optional(),
  method: z.unknown().optional(),
  path: z.unknown().optional(),
  port: z.unknown().optional(),
  probe_zone: z.unknown().optional(),
  retries: z.unknown().optional(),
  timeout: z.unknown().optional(),
  type: z.unknown().optional(),
  created_on: z.unknown().optional(),
  id: z.unknown().optional(),
  modified_on: z.unknown().optional(),
});

const CreateAccountLoadBalancerMonitorsPreviewMonitorSchema = z.object({
  pools: z.record(z.string(), z.string()).optional().describe(
    "Monitored pool IDs mapped to their respective names.",
  ),
  preview_id: z.unknown().optional(),
});

const MonitorReferencesItemSchema = z.object({
  reference_type: z.enum(["*", "referral", "referrer"]).optional(),
  resource_id: z.string().optional(),
  resource_name: z.string().optional(),
  resource_type: z.string().optional(),
});

const ListMonitorReferencesSchema = z.object({
  items: z.array(MonitorReferencesItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const PoolsItemSchema = z.object({
  check_regions: z.unknown().optional(),
  created_on: z.unknown().optional(),
  description: z.unknown().optional(),
  disabled_at: z.unknown().optional(),
  enabled: z.unknown().optional(),
  id: z.unknown().optional(),
  latitude: z.unknown().optional(),
  load_shedding: z.unknown().optional(),
  longitude: z.unknown().optional(),
  minimum_origins: z.unknown().optional(),
  modified_on: z.unknown().optional(),
  monitor: z.unknown().optional(),
  monitor_group: z.unknown().optional(),
  name: z.unknown().optional(),
  networks: z.unknown().optional(),
  notification_email: z.unknown().optional(),
  notification_filter: z.unknown().optional(),
  origin_steering: z.unknown().optional(),
  origins: z.unknown().optional(),
});

const ListPoolsSchema = z.object({
  items: z.array(PoolsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreatePoolSchema = z.object({
  check_regions: z.unknown().optional(),
  created_on: z.unknown().optional(),
  description: z.unknown().optional(),
  disabled_at: z.unknown().optional(),
  enabled: z.unknown().optional(),
  id: z.unknown().optional(),
  latitude: z.unknown().optional(),
  load_shedding: z.unknown().optional(),
  longitude: z.unknown().optional(),
  minimum_origins: z.unknown().optional(),
  modified_on: z.unknown().optional(),
  monitor: z.unknown().optional(),
  monitor_group: z.unknown().optional(),
  name: z.unknown().optional(),
  networks: z.unknown().optional(),
  notification_email: z.unknown().optional(),
  notification_filter: z.unknown().optional(),
  origin_steering: z.unknown().optional(),
  origins: z.unknown().optional(),
});

const PatchPoolsSchema = z.object({
  check_regions: z.unknown().optional(),
  created_on: z.unknown().optional(),
  description: z.unknown().optional(),
  disabled_at: z.unknown().optional(),
  enabled: z.unknown().optional(),
  id: z.unknown().optional(),
  latitude: z.unknown().optional(),
  load_shedding: z.unknown().optional(),
  longitude: z.unknown().optional(),
  minimum_origins: z.unknown().optional(),
  modified_on: z.unknown().optional(),
  monitor: z.unknown().optional(),
  monitor_group: z.unknown().optional(),
  name: z.unknown().optional(),
  networks: z.unknown().optional(),
  notification_email: z.unknown().optional(),
  notification_filter: z.unknown().optional(),
  origin_steering: z.unknown().optional(),
  origins: z.unknown().optional(),
});

const GetAccountLoadBalancerPoolsPoolDetailsSchema = z.object({
  check_regions: z.unknown().optional(),
  created_on: z.unknown().optional(),
  description: z.unknown().optional(),
  disabled_at: z.unknown().optional(),
  enabled: z.unknown().optional(),
  id: z.unknown().optional(),
  latitude: z.unknown().optional(),
  load_shedding: z.unknown().optional(),
  longitude: z.unknown().optional(),
  minimum_origins: z.unknown().optional(),
  modified_on: z.unknown().optional(),
  monitor: z.unknown().optional(),
  monitor_group: z.unknown().optional(),
  name: z.unknown().optional(),
  networks: z.unknown().optional(),
  notification_email: z.unknown().optional(),
  notification_filter: z.unknown().optional(),
  origin_steering: z.unknown().optional(),
  origins: z.unknown().optional(),
});

const PatchPoolSchema = z.object({
  check_regions: z.unknown().optional(),
  created_on: z.unknown().optional(),
  description: z.unknown().optional(),
  disabled_at: z.unknown().optional(),
  enabled: z.unknown().optional(),
  id: z.unknown().optional(),
  latitude: z.unknown().optional(),
  load_shedding: z.unknown().optional(),
  longitude: z.unknown().optional(),
  minimum_origins: z.unknown().optional(),
  modified_on: z.unknown().optional(),
  monitor: z.unknown().optional(),
  monitor_group: z.unknown().optional(),
  name: z.unknown().optional(),
  networks: z.unknown().optional(),
  notification_email: z.unknown().optional(),
  notification_filter: z.unknown().optional(),
  origin_steering: z.unknown().optional(),
  origins: z.unknown().optional(),
});

const GetAccountLoadBalancerPoolsPoolHealthDetailsSchema = z.object({
  pool_id: z.string().optional().describe("Pool ID."),
  pop_health: z.object({
    healthy: z.boolean().optional(),
    origins: z.array(z.unknown()).optional(),
  }).optional().describe("List of regions and associated health status."),
});

const CreateAccountLoadBalancerPoolsPreviewPoolSchema = z.object({
  pools: z.record(z.string(), z.string()).optional().describe(
    "Monitored pool IDs mapped to their respective names.",
  ),
  preview_id: z.unknown().optional(),
});

const PoolReferencesItemSchema = z.object({
  reference_type: z.enum(["*", "referral", "referrer"]).optional(),
  resource_id: z.string().optional(),
  resource_name: z.string().optional(),
  resource_type: z.string().optional(),
});

const ListPoolReferencesSchema = z.object({
  items: z.array(PoolReferencesItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetAccountLoadBalancerMonitorsPreviewResultSchema = z.record(
  z.string(),
  z.object({
    healthy: z.boolean().optional(),
    origins: z.array(z.record(z.string(), z.unknown())).optional(),
  }),
);

const ListRegionsSchema = z.object({});

const GetRegionSchema = z.object({});

const GetAccountLoadBalancerSearchSearchResourcesSchema = z.object({
  resources: z.array(z.unknown()).optional().describe(
    "A list of resources matching the search query.",
  ),
});

const ListLoadBalancerUsageSchema = z.object({
  load_balancers: z.number().int().describe(
    "The number of configured load balancers.",
  ),
  max_origins_per_pool: z.number().int().describe(
    "The largest number of origins in any single pool.",
  ),
  minimum_monitor_interval: z.number().int().describe(
    "The smallest monitor interval (in seconds) configured across all monitors.",
  ),
  monitors: z.number().int().describe("The number of configured monitors."),
  origins: z.number().int().describe(
    "The number of configured origins across all pools.",
  ),
  pools: z.number().int().describe("The number of configured pools."),
});

const GetAccountLoadBalancersAccountLoadBalancerDetailsSchema = z.object({
  adaptive_routing: z.unknown().optional(),
  country_pools: z.unknown().optional(),
  created_on: z.unknown().optional(),
  default_pools: z.unknown().optional(),
  description: z.unknown().optional(),
  enabled: z.unknown().optional(),
  fallback_pool: z.unknown().optional(),
  id: z.unknown().optional(),
  location_strategy: z.unknown().optional(),
  modified_on: z.unknown().optional(),
  name: z.unknown().optional(),
  networks: z.unknown().optional(),
  pool_sets: z.unknown().optional(),
  pop_pools: z.unknown().optional(),
  proxied: z.unknown().optional(),
  random_steering: z.unknown().optional(),
  region_pools: z.unknown().optional(),
  rules: z.unknown().optional(),
  session_affinity: z.unknown().optional(),
  session_affinity_attributes: z.unknown().optional(),
  session_affinity_ttl: z.unknown().optional(),
  steering_policy: z.unknown().optional(),
  ttl: z.unknown().optional(),
  zone_name: z.unknown().optional(),
});

const PatchAccountLoadBalancerSchema = z.object({
  adaptive_routing: z.unknown().optional(),
  country_pools: z.unknown().optional(),
  created_on: z.unknown().optional(),
  default_pools: z.unknown().optional(),
  description: z.unknown().optional(),
  enabled: z.unknown().optional(),
  fallback_pool: z.unknown().optional(),
  id: z.unknown().optional(),
  location_strategy: z.unknown().optional(),
  modified_on: z.unknown().optional(),
  name: z.unknown().optional(),
  networks: z.unknown().optional(),
  pool_sets: z.unknown().optional(),
  pop_pools: z.unknown().optional(),
  proxied: z.unknown().optional(),
  random_steering: z.unknown().optional(),
  region_pools: z.unknown().optional(),
  rules: z.unknown().optional(),
  session_affinity: z.unknown().optional(),
  session_affinity_attributes: z.unknown().optional(),
  session_affinity_ttl: z.unknown().optional(),
  steering_policy: z.unknown().optional(),
  ttl: z.unknown().optional(),
  zone_name: z.unknown().optional(),
});

const LoadBalancersItemSchema = z.object({
  adaptive_routing: z.unknown().optional(),
  country_pools: z.unknown().optional(),
  created_on: z.unknown().optional(),
  default_pools: z.unknown().optional(),
  description: z.unknown().optional(),
  enabled: z.unknown().optional(),
  fallback_pool: z.unknown().optional(),
  id: z.unknown().optional(),
  location_strategy: z.unknown().optional(),
  modified_on: z.unknown().optional(),
  name: z.unknown().optional(),
  networks: z.unknown().optional(),
  pool_sets: z.unknown().optional(),
  pop_pools: z.unknown().optional(),
  proxied: z.unknown().optional(),
  random_steering: z.unknown().optional(),
  region_pools: z.unknown().optional(),
  rules: z.unknown().optional(),
  session_affinity: z.unknown().optional(),
  session_affinity_attributes: z.unknown().optional(),
  session_affinity_ttl: z.unknown().optional(),
  steering_policy: z.unknown().optional(),
  ttl: z.unknown().optional(),
  zone_name: z.unknown().optional(),
});

const ListLoadBalancersSchema = z.object({
  items: z.array(LoadBalancersItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateLoadBalancerSchema = z.object({
  adaptive_routing: z.unknown().optional(),
  country_pools: z.unknown().optional(),
  created_on: z.unknown().optional(),
  default_pools: z.unknown().optional(),
  description: z.unknown().optional(),
  enabled: z.unknown().optional(),
  fallback_pool: z.unknown().optional(),
  id: z.unknown().optional(),
  location_strategy: z.unknown().optional(),
  modified_on: z.unknown().optional(),
  name: z.unknown().optional(),
  networks: z.unknown().optional(),
  pool_sets: z.unknown().optional(),
  pop_pools: z.unknown().optional(),
  proxied: z.unknown().optional(),
  random_steering: z.unknown().optional(),
  region_pools: z.unknown().optional(),
  rules: z.unknown().optional(),
  session_affinity: z.unknown().optional(),
  session_affinity_attributes: z.unknown().optional(),
  session_affinity_ttl: z.unknown().optional(),
  steering_policy: z.unknown().optional(),
  ttl: z.unknown().optional(),
  zone_name: z.unknown().optional(),
});

const GetLoadBalancersLoadBalancerDetailsSchema = z.object({
  adaptive_routing: z.unknown().optional(),
  country_pools: z.unknown().optional(),
  created_on: z.unknown().optional(),
  default_pools: z.unknown().optional(),
  description: z.unknown().optional(),
  enabled: z.unknown().optional(),
  fallback_pool: z.unknown().optional(),
  id: z.unknown().optional(),
  location_strategy: z.unknown().optional(),
  modified_on: z.unknown().optional(),
  name: z.unknown().optional(),
  networks: z.unknown().optional(),
  pool_sets: z.unknown().optional(),
  pop_pools: z.unknown().optional(),
  proxied: z.unknown().optional(),
  random_steering: z.unknown().optional(),
  region_pools: z.unknown().optional(),
  rules: z.unknown().optional(),
  session_affinity: z.unknown().optional(),
  session_affinity_attributes: z.unknown().optional(),
  session_affinity_ttl: z.unknown().optional(),
  steering_policy: z.unknown().optional(),
  ttl: z.unknown().optional(),
  zone_name: z.unknown().optional(),
});

const PatchLoadBalancerSchema = z.object({
  adaptive_routing: z.unknown().optional(),
  country_pools: z.unknown().optional(),
  created_on: z.unknown().optional(),
  default_pools: z.unknown().optional(),
  description: z.unknown().optional(),
  enabled: z.unknown().optional(),
  fallback_pool: z.unknown().optional(),
  id: z.unknown().optional(),
  location_strategy: z.unknown().optional(),
  modified_on: z.unknown().optional(),
  name: z.unknown().optional(),
  networks: z.unknown().optional(),
  pool_sets: z.unknown().optional(),
  pop_pools: z.unknown().optional(),
  proxied: z.unknown().optional(),
  random_steering: z.unknown().optional(),
  region_pools: z.unknown().optional(),
  rules: z.unknown().optional(),
  session_affinity: z.unknown().optional(),
  session_affinity_attributes: z.unknown().optional(),
  session_affinity_ttl: z.unknown().optional(),
  steering_policy: z.unknown().optional(),
  ttl: z.unknown().optional(),
  zone_name: z.unknown().optional(),
});

// =============================================================================
// Model Definition
// =============================================================================

/** Cloudflare Load Balancing — pools, monitors, load balancers, steering policies */
export const model = {
  type: "@webframp/cloudflare/load-balancing",
  version: "2026.07.19.1",
  globalArguments: GlobalArgsSchema,

  upgrades: [],

  resources: {
    "account_load_balancers": {
      description: "List Account Load Balancers",
      schema: ListAccountLoadBalancersSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "account_load_balancer": {
      description: "Create Account Load Balancer",
      schema: CreateAccountLoadBalancerSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "monitor_groups": {
      description: "List Monitor Groups",
      schema: ListMonitorGroupsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "monitor_group": {
      description: "Create Monitor Group",
      schema: CreateMonitorGroupSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "account_load_balancer_monitor_groups_monitor_group_details": {
      description: "Monitor Group Details",
      schema: GetAccountLoadBalancerMonitorGroupsMonitorGroupDetailsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "patch_monitor_group": {
      description: "Patch Monitor Group",
      schema: PatchMonitorGroupSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "monitor_group_references": {
      description: "List Monitor Group References",
      schema: ListMonitorGroupReferencesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "monitors": {
      description: "List Monitors",
      schema: ListMonitorsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "monitor": {
      description: "Create Monitor",
      schema: CreateMonitorSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "account_load_balancer_monitors_monitor_details": {
      description: "Monitor Details",
      schema: GetAccountLoadBalancerMonitorsMonitorDetailsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "patch_monitor": {
      description: "Patch Monitor",
      schema: PatchMonitorSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "account_load_balancer_monitors_preview_monitor": {
      description: "Preview Monitor",
      schema: CreateAccountLoadBalancerMonitorsPreviewMonitorSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "monitor_references": {
      description: "List Monitor References",
      schema: ListMonitorReferencesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "pools": {
      description: "List Pools",
      schema: ListPoolsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "pool": {
      description: "Create Pool",
      schema: CreatePoolSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "patch_pools": {
      description: "Patch Pools",
      schema: PatchPoolsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "account_load_balancer_pools_pool_details": {
      description: "Pool Details",
      schema: GetAccountLoadBalancerPoolsPoolDetailsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "patch_pool": {
      description: "Patch Pool",
      schema: PatchPoolSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "account_load_balancer_pools_pool_health_details": {
      description: "Pool Health Details",
      schema: GetAccountLoadBalancerPoolsPoolHealthDetailsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "account_load_balancer_pools_preview_pool": {
      description: "Preview Pool",
      schema: CreateAccountLoadBalancerPoolsPreviewPoolSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "pool_references": {
      description: "List Pool References",
      schema: ListPoolReferencesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "account_load_balancer_monitors_preview_result": {
      description: "Preview Result",
      schema: GetAccountLoadBalancerMonitorsPreviewResultSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "list_regions": {
      description: "List Regions",
      schema: ListRegionsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "region": {
      description: "Get Region",
      schema: GetRegionSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "account_load_balancer_search_search_resources": {
      description: "Search Resources",
      schema: GetAccountLoadBalancerSearchSearchResourcesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "list_load_balancer_usage": {
      description: "List Load Balancer Usage",
      schema: ListLoadBalancerUsageSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "account_load_balancers_account_load_balancer_details": {
      description: "Account Load Balancer Details",
      schema: GetAccountLoadBalancersAccountLoadBalancerDetailsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "patch_account_load_balancer": {
      description: "Patch Account Load Balancer",
      schema: PatchAccountLoadBalancerSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "load_balancers": {
      description: "List Load Balancers",
      schema: ListLoadBalancersSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "load_balancer": {
      description: "Create Load Balancer",
      schema: CreateLoadBalancerSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "load_balancers_load_balancer_details": {
      description: "Load Balancer Details",
      schema: GetLoadBalancersLoadBalancerDetailsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "patch_load_balancer": {
      description: "Patch Load Balancer",
      schema: PatchLoadBalancerSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
  },

  methods: {
    list_account_load_balancers: {
      description: "List Account Load Balancers",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/load_balancers`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "account_load_balancers",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} account_load_balancers", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_account_load_balancer: {
      description: "Create Account Load Balancer",
      arguments: z.object({
        adaptive_routing: z.unknown().optional(),
        country_pools: z.unknown().optional(),
        default_pools: z.unknown(),
        description: z.unknown().optional(),
        enabled: z.unknown().optional(),
        fallback_pool: z.unknown(),
        location_strategy: z.unknown().optional(),
        name: z.unknown(),
        networks: z.unknown().optional(),
        pop_pools: z.unknown().optional(),
        proxied: z.unknown().optional(),
        random_steering: z.unknown().optional(),
        region_pools: z.unknown().optional(),
        rules: z.unknown().optional(),
        session_affinity: z.unknown().optional(),
        session_affinity_attributes: z.unknown().optional(),
        session_affinity_ttl: z.unknown().optional(),
        steering_policy: z.unknown().optional(),
        ttl: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/load_balancers`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "account_load_balancer",
          id,
          result,
        );
        context.logger.info("Created account_load_balancer {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    list_monitor_groups: {
      description: "List Monitor Groups",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/load_balancers/monitor_groups`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("monitor_groups", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} monitor_groups", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_monitor_group: {
      description: "Create Monitor Group",
      arguments: z.object({
        created_on: z.string().optional().describe(
          "The timestamp of when the monitor group was created",
        ),
        description: z.string().describe(
          "A short description of the monitor group",
        ),
        members: z.array(z.unknown()).describe(
          "List of monitors in this group",
        ),
        modified_on: z.string().optional().describe(
          "The timestamp of when the monitor group was last updated",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/load_balancers/monitor_groups`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource("monitor_group", id, result);
        context.logger.info("Created monitor_group {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_account_load_balancer_monitor_groups_monitor_group_details: {
      description: "Monitor Group Details",
      arguments: z.object({
        monitor_group_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/load_balancers/monitor_groups/${args.monitor_group_id}`,
        );

        const handle = await context.writeResource(
          "account_load_balancer_monitor_groups_monitor_group_details",
          String(args.monitor_group_id),
          result,
        );
        context.logger.info(
          "Fetched account_load_balancer_monitor_groups_monitor_group_details",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    update_monitor_group: {
      description: "Update Monitor Group",
      arguments: z.object({
        monitor_group_id: z.string(),
        created_on: z.string().optional().describe(
          "The timestamp of when the monitor group was created",
        ),
        description: z.string().describe(
          "A short description of the monitor group",
        ),
        members: z.array(z.unknown()).describe(
          "List of monitors in this group",
        ),
        modified_on: z.string().optional().describe(
          "The timestamp of when the monitor group was last updated",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/load_balancers/monitor_groups/${args.monitor_group_id}`,
          args,
        );

        const handle = await context.writeResource(
          "monitor_group",
          String(args.monitor_group_id),
          result,
        );
        context.logger.info("Updated monitor_group", {});
        return { dataHandles: [handle] };
      },
    },
    patch_monitor_group: {
      description: "Patch Monitor Group",
      arguments: z.object({
        monitor_group_id: z.string(),
        created_on: z.string().optional().describe(
          "The timestamp of when the monitor group was created",
        ),
        description: z.string().describe(
          "A short description of the monitor group",
        ),
        members: z.array(z.unknown()).describe(
          "List of monitors in this group",
        ),
        modified_on: z.string().optional().describe(
          "The timestamp of when the monitor group was last updated",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/accounts/${accountId}/load_balancers/monitor_groups/${args.monitor_group_id}`,
          args,
        );

        const handle = await context.writeResource(
          "patch_monitor_group",
          String(args.monitor_group_id),
          result,
        );
        context.logger.info("Updated patch_monitor_group", {});
        return { dataHandles: [handle] };
      },
    },
    delete_monitor_group: {
      description: "Delete Monitor Group",
      arguments: z.object({
        monitor_group_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/load_balancers/monitor_groups/${args.monitor_group_id}`,
        );

        context.logger.info("Deleted resource {id}", {
          id: args.monitor_group_id,
        });
        return { dataHandles: [] };
      },
    },
    list_monitor_group_references: {
      description: "List Monitor Group References",
      arguments: z.object({
        monitor_group_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const pathParams = new Set(["monitor_group_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !pathParams.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/load_balancers/monitor_groups/${args.monitor_group_id}/references`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "monitor_group_references",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} monitor_group_references", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    list_monitors: {
      description: "List Monitors",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/load_balancers/monitors`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("monitors", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} monitors", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_monitor: {
      description: "Create Monitor",
      arguments: z.object({
        allow_insecure: z.unknown().optional(),
        consecutive_down: z.unknown().optional(),
        consecutive_up: z.unknown().optional(),
        description: z.unknown().optional(),
        expected_body: z.unknown().optional(),
        expected_codes: z.unknown().optional(),
        follow_redirects: z.unknown().optional(),
        header: z.unknown().optional(),
        interval: z.unknown().optional(),
        method: z.unknown().optional(),
        path: z.unknown().optional(),
        port: z.unknown().optional(),
        probe_zone: z.unknown().optional(),
        retries: z.unknown().optional(),
        timeout: z.unknown().optional(),
        type: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/load_balancers/monitors`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource("monitor", id, result);
        context.logger.info("Created monitor {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_account_load_balancer_monitors_monitor_details: {
      description: "Monitor Details",
      arguments: z.object({
        monitor_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/load_balancers/monitors/${args.monitor_id}`,
        );

        const handle = await context.writeResource(
          "account_load_balancer_monitors_monitor_details",
          String(args.monitor_id),
          result,
        );
        context.logger.info(
          "Fetched account_load_balancer_monitors_monitor_details",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    update_monitor: {
      description: "Update Monitor",
      arguments: z.object({
        monitor_id: z.string(),
        allow_insecure: z.unknown().optional(),
        consecutive_down: z.unknown().optional(),
        consecutive_up: z.unknown().optional(),
        description: z.unknown().optional(),
        expected_body: z.unknown().optional(),
        expected_codes: z.unknown().optional(),
        follow_redirects: z.unknown().optional(),
        header: z.unknown().optional(),
        interval: z.unknown().optional(),
        method: z.unknown().optional(),
        path: z.unknown().optional(),
        port: z.unknown().optional(),
        probe_zone: z.unknown().optional(),
        retries: z.unknown().optional(),
        timeout: z.unknown().optional(),
        type: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/load_balancers/monitors/${args.monitor_id}`,
          args,
        );

        const handle = await context.writeResource(
          "monitor",
          String(args.monitor_id),
          result,
        );
        context.logger.info("Updated monitor", {});
        return { dataHandles: [handle] };
      },
    },
    patch_monitor: {
      description: "Patch Monitor",
      arguments: z.object({
        monitor_id: z.string(),
        allow_insecure: z.unknown().optional(),
        consecutive_down: z.unknown().optional(),
        consecutive_up: z.unknown().optional(),
        description: z.unknown().optional(),
        expected_body: z.unknown().optional(),
        expected_codes: z.unknown().optional(),
        follow_redirects: z.unknown().optional(),
        header: z.unknown().optional(),
        interval: z.unknown().optional(),
        method: z.unknown().optional(),
        path: z.unknown().optional(),
        port: z.unknown().optional(),
        probe_zone: z.unknown().optional(),
        retries: z.unknown().optional(),
        timeout: z.unknown().optional(),
        type: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/accounts/${accountId}/load_balancers/monitors/${args.monitor_id}`,
          args,
        );

        const handle = await context.writeResource(
          "patch_monitor",
          String(args.monitor_id),
          result,
        );
        context.logger.info("Updated patch_monitor", {});
        return { dataHandles: [handle] };
      },
    },
    delete_monitor: {
      description: "Delete Monitor",
      arguments: z.object({
        monitor_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/load_balancers/monitors/${args.monitor_id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.monitor_id });
        return { dataHandles: [] };
      },
    },
    create_account_load_balancer_monitors_preview_monitor: {
      description: "Preview Monitor",
      arguments: z.object({
        monitor_id: z.string(),
        allow_insecure: z.unknown().optional(),
        consecutive_down: z.unknown().optional(),
        consecutive_up: z.unknown().optional(),
        description: z.unknown().optional(),
        expected_body: z.unknown().optional(),
        expected_codes: z.unknown().optional(),
        follow_redirects: z.unknown().optional(),
        header: z.unknown().optional(),
        interval: z.unknown().optional(),
        method: z.unknown().optional(),
        path: z.unknown().optional(),
        port: z.unknown().optional(),
        probe_zone: z.unknown().optional(),
        retries: z.unknown().optional(),
        timeout: z.unknown().optional(),
        type: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/load_balancers/monitors/${args.monitor_id}/preview`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "account_load_balancer_monitors_preview_monitor",
          id,
          result,
        );
        context.logger.info(
          "Created account_load_balancer_monitors_preview_monitor {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    list_monitor_references: {
      description: "List Monitor References",
      arguments: z.object({
        monitor_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const pathParams = new Set(["monitor_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !pathParams.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/load_balancers/monitors/${args.monitor_id}/references`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "monitor_references",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} monitor_references", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    list_pools: {
      description: "List Pools",
      arguments: z.object({
        monitor: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/load_balancers/pools`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("pools", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} pools", { count: results.length });
        return { dataHandles: [handle] };
      },
    },
    create_pool: {
      description: "Create Pool",
      arguments: z.object({
        description: z.unknown().optional(),
        enabled: z.unknown().optional(),
        latitude: z.unknown().optional(),
        load_shedding: z.unknown().optional(),
        longitude: z.unknown().optional(),
        minimum_origins: z.unknown().optional(),
        monitor: z.unknown().optional(),
        monitor_group: z.unknown().optional(),
        name: z.unknown(),
        notification_email: z.unknown().optional(),
        notification_filter: z.unknown().optional(),
        origin_steering: z.unknown().optional(),
        origins: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/load_balancers/pools`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource("pool", id, result);
        context.logger.info("Created pool {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    patch_pools: {
      description: "Patch Pools",
      arguments: z.object({
        notification_email: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/accounts/${accountId}/load_balancers/pools`,
          args,
        );

        const handle = await context.writeResource(
          "patch_pools",
          "updated",
          result,
        );
        context.logger.info("Updated patch_pools", {});
        return { dataHandles: [handle] };
      },
    },
    get_account_load_balancer_pools_pool_details: {
      description: "Pool Details",
      arguments: z.object({
        pool_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/load_balancers/pools/${args.pool_id}`,
        );

        const handle = await context.writeResource(
          "account_load_balancer_pools_pool_details",
          String(args.pool_id),
          result,
        );
        context.logger.info(
          "Fetched account_load_balancer_pools_pool_details",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    update_pool: {
      description: "Update Pool",
      arguments: z.object({
        pool_id: z.string(),
        check_regions: z.unknown().optional(),
        description: z.unknown().optional(),
        disabled_at: z.unknown().optional(),
        enabled: z.unknown().optional(),
        latitude: z.unknown().optional(),
        load_shedding: z.unknown().optional(),
        longitude: z.unknown().optional(),
        minimum_origins: z.unknown().optional(),
        monitor: z.unknown().optional(),
        monitor_group: z.unknown().optional(),
        name: z.unknown(),
        notification_email: z.unknown().optional(),
        notification_filter: z.unknown().optional(),
        origin_steering: z.unknown().optional(),
        origins: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/load_balancers/pools/${args.pool_id}`,
          args,
        );

        const handle = await context.writeResource(
          "pool",
          String(args.pool_id),
          result,
        );
        context.logger.info("Updated pool", {});
        return { dataHandles: [handle] };
      },
    },
    patch_pool: {
      description: "Patch Pool",
      arguments: z.object({
        pool_id: z.string(),
        check_regions: z.unknown().optional(),
        description: z.unknown().optional(),
        disabled_at: z.unknown().optional(),
        enabled: z.unknown().optional(),
        latitude: z.unknown().optional(),
        load_shedding: z.unknown().optional(),
        longitude: z.unknown().optional(),
        minimum_origins: z.unknown().optional(),
        monitor: z.unknown().optional(),
        monitor_group: z.unknown().optional(),
        name: z.unknown().optional(),
        notification_email: z.unknown().optional(),
        notification_filter: z.unknown().optional(),
        origin_steering: z.unknown().optional(),
        origins: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/accounts/${accountId}/load_balancers/pools/${args.pool_id}`,
          args,
        );

        const handle = await context.writeResource(
          "patch_pool",
          String(args.pool_id),
          result,
        );
        context.logger.info("Updated patch_pool", {});
        return { dataHandles: [handle] };
      },
    },
    delete_pool: {
      description: "Delete Pool",
      arguments: z.object({
        pool_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/load_balancers/pools/${args.pool_id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.pool_id });
        return { dataHandles: [] };
      },
    },
    get_account_load_balancer_pools_pool_health_details: {
      description: "Pool Health Details",
      arguments: z.object({
        pool_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/load_balancers/pools/${args.pool_id}/health`,
        );

        const handle = await context.writeResource(
          "account_load_balancer_pools_pool_health_details",
          String(args.pool_id),
          result,
        );
        context.logger.info(
          "Fetched account_load_balancer_pools_pool_health_details",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    create_account_load_balancer_pools_preview_pool: {
      description: "Preview Pool",
      arguments: z.object({
        pool_id: z.string(),
        allow_insecure: z.unknown().optional(),
        consecutive_down: z.unknown().optional(),
        consecutive_up: z.unknown().optional(),
        description: z.unknown().optional(),
        expected_body: z.unknown().optional(),
        expected_codes: z.unknown().optional(),
        follow_redirects: z.unknown().optional(),
        header: z.unknown().optional(),
        interval: z.unknown().optional(),
        method: z.unknown().optional(),
        path: z.unknown().optional(),
        port: z.unknown().optional(),
        probe_zone: z.unknown().optional(),
        retries: z.unknown().optional(),
        timeout: z.unknown().optional(),
        type: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/load_balancers/pools/${args.pool_id}/preview`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "account_load_balancer_pools_preview_pool",
          id,
          result,
        );
        context.logger.info(
          "Created account_load_balancer_pools_preview_pool {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    list_pool_references: {
      description: "List Pool References",
      arguments: z.object({
        pool_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const pathParams = new Set(["pool_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !pathParams.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/load_balancers/pools/${args.pool_id}/references`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("pool_references", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} pool_references", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    get_account_load_balancer_monitors_preview_result: {
      description: "Preview Result",
      arguments: z.object({
        preview_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/load_balancers/preview/${args.preview_id}`,
        );

        const handle = await context.writeResource(
          "account_load_balancer_monitors_preview_result",
          String(args.preview_id),
          result,
        );
        context.logger.info(
          "Fetched account_load_balancer_monitors_preview_result",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    list_regions: {
      description: "List Regions",
      arguments: z.object({
        subdivision_code: z.string().optional(),
        subdivision_code_a2: z.string().optional(),
        country_code_a2: z.string().optional(),
      }),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/load_balancers/regions`,
        );

        const handle = await context.writeResource(
          "list_regions",
          "latest",
          result,
        );
        context.logger.info("Fetched list_regions", {});
        return { dataHandles: [handle] };
      },
    },
    get_region: {
      description: "Get Region",
      arguments: z.object({
        region_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/load_balancers/regions/${args.region_id}`,
        );

        const handle = await context.writeResource(
          "region",
          String(args.region_id),
          result,
        );
        context.logger.info("Fetched region", {});
        return { dataHandles: [handle] };
      },
    },
    get_account_load_balancer_search_search_resources: {
      description: "Search Resources",
      arguments: z.object({
        query: z.string().optional(),
        references: z.enum(["", "*", "referral", "referrer"]).optional(),
        page: z.number().optional(),
        per_page: z.number().optional(),
      }),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/load_balancers/search`,
        );

        const handle = await context.writeResource(
          "account_load_balancer_search_search_resources",
          "latest",
          result,
        );
        context.logger.info(
          "Fetched account_load_balancer_search_search_resources",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    list_load_balancer_usage: {
      description: "List Load Balancer Usage",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/load_balancers/usage`,
        );

        const handle = await context.writeResource(
          "list_load_balancer_usage",
          "latest",
          result,
        );
        context.logger.info("Fetched list_load_balancer_usage", {});
        return { dataHandles: [handle] };
      },
    },
    get_account_load_balancers_account_load_balancer_details: {
      description: "Account Load Balancer Details",
      arguments: z.object({
        load_balancer_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/load_balancers/${args.load_balancer_id}`,
        );

        const handle = await context.writeResource(
          "account_load_balancers_account_load_balancer_details",
          String(args.load_balancer_id),
          result,
        );
        context.logger.info(
          "Fetched account_load_balancers_account_load_balancer_details",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    update_account_load_balancer: {
      description: "Update Account Load Balancer",
      arguments: z.object({
        load_balancer_id: z.string(),
        adaptive_routing: z.unknown().optional(),
        country_pools: z.unknown().optional(),
        default_pools: z.unknown(),
        description: z.unknown().optional(),
        enabled: z.unknown().optional(),
        fallback_pool: z.unknown(),
        location_strategy: z.unknown().optional(),
        name: z.unknown(),
        networks: z.unknown().optional(),
        pop_pools: z.unknown().optional(),
        proxied: z.unknown().optional(),
        random_steering: z.unknown().optional(),
        region_pools: z.unknown().optional(),
        rules: z.unknown().optional(),
        session_affinity: z.unknown().optional(),
        session_affinity_attributes: z.unknown().optional(),
        session_affinity_ttl: z.unknown().optional(),
        steering_policy: z.unknown().optional(),
        ttl: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/load_balancers/${args.load_balancer_id}`,
          args,
        );

        const handle = await context.writeResource(
          "account_load_balancer",
          String(args.load_balancer_id),
          result,
        );
        context.logger.info("Updated account_load_balancer", {});
        return { dataHandles: [handle] };
      },
    },
    patch_account_load_balancer: {
      description: "Patch Account Load Balancer",
      arguments: z.object({
        load_balancer_id: z.string(),
        adaptive_routing: z.unknown().optional(),
        country_pools: z.unknown().optional(),
        default_pools: z.unknown().optional(),
        description: z.unknown().optional(),
        enabled: z.unknown().optional(),
        fallback_pool: z.unknown().optional(),
        location_strategy: z.unknown().optional(),
        name: z.unknown().optional(),
        networks: z.unknown().optional(),
        pop_pools: z.unknown().optional(),
        proxied: z.unknown().optional(),
        random_steering: z.unknown().optional(),
        region_pools: z.unknown().optional(),
        rules: z.unknown().optional(),
        session_affinity: z.unknown().optional(),
        session_affinity_attributes: z.unknown().optional(),
        session_affinity_ttl: z.unknown().optional(),
        steering_policy: z.unknown().optional(),
        ttl: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/accounts/${accountId}/load_balancers/${args.load_balancer_id}`,
          args,
        );

        const handle = await context.writeResource(
          "patch_account_load_balancer",
          String(args.load_balancer_id),
          result,
        );
        context.logger.info("Updated patch_account_load_balancer", {});
        return { dataHandles: [handle] };
      },
    },
    delete_account_load_balancer: {
      description: "Delete Account Load Balancer",
      arguments: z.object({
        load_balancer_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/load_balancers/${args.load_balancer_id}`,
        );

        context.logger.info("Deleted resource {id}", {
          id: args.load_balancer_id,
        });
        return { dataHandles: [] };
      },
    },
    list_load_balancers: {
      description: "List Load Balancers",
      arguments: z.object({
        zone_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        const params: Record<string, string> = {};
        const pathParams = new Set(["zone_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !pathParams.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/zones/${args.zone_id}/load_balancers`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("load_balancers", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} load_balancers", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_load_balancer: {
      description: "Create Load Balancer",
      arguments: z.object({
        zone_id: z.string(),
        adaptive_routing: z.unknown().optional(),
        country_pools: z.unknown().optional(),
        default_pools: z.unknown(),
        description: z.unknown().optional(),
        fallback_pool: z.unknown(),
        location_strategy: z.unknown().optional(),
        name: z.unknown(),
        networks: z.unknown().optional(),
        pop_pools: z.unknown().optional(),
        proxied: z.unknown().optional(),
        random_steering: z.unknown().optional(),
        region_pools: z.unknown().optional(),
        rules: z.unknown().optional(),
        session_affinity: z.unknown().optional(),
        session_affinity_attributes: z.unknown().optional(),
        session_affinity_ttl: z.unknown().optional(),
        steering_policy: z.unknown().optional(),
        ttl: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/zones/${args.zone_id}/load_balancers`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource("load_balancer", id, result);
        context.logger.info("Created load_balancer {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_load_balancers_load_balancer_details: {
      description: "Load Balancer Details",
      arguments: z.object({
        zone_id: z.string(),
        load_balancer_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/zones/${args.zone_id}/load_balancers/${args.load_balancer_id}`,
        );

        const handle = await context.writeResource(
          "load_balancers_load_balancer_details",
          String(args.load_balancer_id),
          result,
        );
        context.logger.info("Fetched load_balancers_load_balancer_details", {});
        return { dataHandles: [handle] };
      },
    },
    update_load_balancer: {
      description: "Update Load Balancer",
      arguments: z.object({
        zone_id: z.string(),
        load_balancer_id: z.string(),
        adaptive_routing: z.unknown().optional(),
        country_pools: z.unknown().optional(),
        default_pools: z.unknown(),
        description: z.unknown().optional(),
        enabled: z.unknown().optional(),
        fallback_pool: z.unknown(),
        location_strategy: z.unknown().optional(),
        name: z.unknown(),
        networks: z.unknown().optional(),
        pop_pools: z.unknown().optional(),
        proxied: z.unknown().optional(),
        random_steering: z.unknown().optional(),
        region_pools: z.unknown().optional(),
        rules: z.unknown().optional(),
        session_affinity: z.unknown().optional(),
        session_affinity_attributes: z.unknown().optional(),
        session_affinity_ttl: z.unknown().optional(),
        steering_policy: z.unknown().optional(),
        ttl: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/zones/${args.zone_id}/load_balancers/${args.load_balancer_id}`,
          args,
        );

        const handle = await context.writeResource(
          "load_balancer",
          String(args.load_balancer_id),
          result,
        );
        context.logger.info("Updated load_balancer", {});
        return { dataHandles: [handle] };
      },
    },
    patch_load_balancer: {
      description: "Patch Load Balancer",
      arguments: z.object({
        zone_id: z.string(),
        load_balancer_id: z.string(),
        adaptive_routing: z.unknown().optional(),
        country_pools: z.unknown().optional(),
        default_pools: z.unknown().optional(),
        description: z.unknown().optional(),
        enabled: z.unknown().optional(),
        fallback_pool: z.unknown().optional(),
        location_strategy: z.unknown().optional(),
        name: z.unknown().optional(),
        pop_pools: z.unknown().optional(),
        proxied: z.unknown().optional(),
        random_steering: z.unknown().optional(),
        region_pools: z.unknown().optional(),
        rules: z.unknown().optional(),
        session_affinity: z.unknown().optional(),
        session_affinity_attributes: z.unknown().optional(),
        session_affinity_ttl: z.unknown().optional(),
        steering_policy: z.unknown().optional(),
        ttl: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/zones/${args.zone_id}/load_balancers/${args.load_balancer_id}`,
          args,
        );

        const handle = await context.writeResource(
          "patch_load_balancer",
          String(args.load_balancer_id),
          result,
        );
        context.logger.info("Updated patch_load_balancer", {});
        return { dataHandles: [handle] };
      },
    },
    delete_load_balancer: {
      description: "Delete Load Balancer",
      arguments: z.object({
        zone_id: z.string(),
        load_balancer_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/zones/${args.zone_id}/load_balancers/${args.load_balancer_id}`,
        );

        context.logger.info("Deleted resource {id}", {
          id: args.load_balancer_id,
        });
        return { dataHandles: [] };
      },
    },
  },
};
