// Cloudflare Turnstile — CAPTCHA-free challenges, site widgets — Tests
// Auto-generated by scripts/cloudflare-codegen — do not edit manually.
// SPDX-License-Identifier: Apache-2.0

import { assertEquals, assertExists } from "jsr:@std/assert@1";
import { createModelTestContext } from "@systeminit/swamp-testing";
import { model } from "./turnstile.ts";

// ---------------------------------------------------------------------------
// Model Export Structure Tests
// ---------------------------------------------------------------------------

Deno.test("turnstile model: has correct type", () => {
  assertEquals(model.type, "@webframp/cloudflare/turnstile");
});

Deno.test("turnstile model: has valid version format", () => {
  const versionPattern = /^\d{4}\.\d{2}\.\d{2}\.\d+$/;
  assertEquals(versionPattern.test(model.version), true);
});

Deno.test("turnstile model: has globalArguments with apiToken", () => {
  assertExists(model.globalArguments);
  const shape = model.globalArguments.shape;
  assertExists(shape.apiToken);
  assertExists(shape.accountId);
});

Deno.test("turnstile model: has expected methods", () => {
  assertExists(model.methods);
  assertExists(model.methods.list);
  assertExists(model.methods.get_create);
  assertExists(model.methods.get_get);
  assertExists(model.methods.get_update);
  assertExists(model.methods.get_delete);
  assertExists(model.methods.get_rotate_secret);
});

Deno.test("turnstile model: has expected resources", () => {
  assertExists(model.resources);
  assertExists(model.resources["list"]);
  assertExists(model.resources["create"]);
  assertExists(model.resources["get"]);
  assertExists(model.resources["update"]);
  assertExists(model.resources["rotate_secret"]);
});

// ---------------------------------------------------------------------------
// Mock Cloudflare API Server
// ---------------------------------------------------------------------------

function startMockCfServer(
  responses: Record<string, { result: unknown; isCollection?: boolean }>,
): { url: string; server: Deno.HttpServer } {
  const server = Deno.serve({ port: 0, onListen() {} }, (req) => {
    const url = new URL(req.url);
    const path = url.pathname;

    for (
      const [pattern, { result, isCollection }] of Object.entries(responses)
    ) {
      if (path.includes(pattern)) {
        return Response.json({
          success: true,
          errors: [],
          messages: [],
          result,
          result_info: isCollection
            ? {
              page: 1,
              per_page: 50,
              total_count: Array.isArray(result) ? result.length : 1,
              total_pages: 1,
            }
            : undefined,
        });
      }
    }

    return Response.json({
      success: false,
      errors: [{ code: 404, message: "Not found" }],
      messages: [],
      result: null,
    });
  });

  const addr = server.addr as Deno.NetAddr;
  return { url: `http://localhost:${addr.port}`, server };
}

function installFetchMock(mockUrl: string): () => void {
  const originalFetch = globalThis.fetch;
  globalThis.fetch = (input, init) => {
    const reqUrl = typeof input === "string" ? input : input.toString();
    const newUrl = reqUrl.replace(
      "https://api.cloudflare.com/client/v4",
      mockUrl,
    );
    return originalFetch(newUrl, init);
  };
  return () => {
    globalThis.fetch = originalFetch;
  };
}

Deno.test({
  name: "turnstile model: list fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = [{
      "bot_fight_mode": null,
      "clearance_level": null,
      "created_on": null,
      "deployed_via": null,
      "domains": null,
      "ephemeral_id": null,
      "last_modified_via": null,
      "mode": null,
      "modified_on": null,
      "name": null,
      "offlabel": null,
      "region": null,
      "sitekey": null,
    }];
    const { url, server } = startMockCfServer({
      "/challenges/widgets": { result: mockData, isCollection: true },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: {
          id: "test-id",
          name: "test-turnstile",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).list.execute({}, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "turnstile model: get_create creates and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "id": "new-123",
      "bot_fight_mode": null,
      "clearance_level": null,
      "created_on": null,
      "deployed_via": null,
      "domains": null,
      "ephemeral_id": null,
      "last_modified_via": null,
      "mode": null,
      "modified_on": null,
      "name": null,
      "offlabel": null,
      "region": null,
      "secret": null,
      "sitekey": null,
    };
    const { url, server } = startMockCfServer({
      "/challenges/widgets": { result: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: {
          id: "test-id",
          name: "test-turnstile",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).get_create.execute({
        "bot_fight_mode": null,
        "clearance_level": null,
        "created_on": null,
        "deployed_via": null,
        "domains": null,
        "ephemeral_id": null,
        "last_modified_via": null,
        "mode": null,
        "modified_on": null,
        "name": null,
        "offlabel": null,
        "region": null,
        "secret": null,
        "sitekey": null,
      }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "turnstile model: get_get fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "bot_fight_mode": null,
      "clearance_level": null,
      "created_on": null,
      "deployed_via": null,
      "domains": null,
      "ephemeral_id": null,
      "last_modified_via": null,
      "mode": null,
      "modified_on": null,
      "name": null,
      "offlabel": null,
      "region": null,
      "secret": null,
      "sitekey": null,
    };
    const { url, server } = startMockCfServer({
      "/challenges/widgets/test-id-123": { result: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: {
          id: "test-id",
          name: "test-turnstile",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).get_get.execute({ "sitekey": "test-id-123" }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "turnstile model: get_update executes and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "bot_fight_mode": null,
      "clearance_level": null,
      "created_on": null,
      "deployed_via": null,
      "domains": null,
      "ephemeral_id": null,
      "last_modified_via": null,
      "mode": null,
      "modified_on": null,
      "name": null,
      "offlabel": null,
      "region": null,
      "secret": null,
      "sitekey": null,
    };
    const { url, server } = startMockCfServer({
      "/challenges/widgets/test-id-123": { result: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: {
          id: "test-id",
          name: "test-turnstile",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).get_update.execute({
        "sitekey": null,
        "bot_fight_mode": null,
        "clearance_level": null,
        "created_on": null,
        "deployed_via": null,
        "domains": null,
        "ephemeral_id": null,
        "last_modified_via": null,
        "mode": null,
        "modified_on": null,
        "name": null,
        "offlabel": null,
        "region": null,
        "secret": null,
      }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "turnstile model: get_delete executes successfully",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockCfServer({
      "/challenges/widgets/test-id-123": { result: { id: "test-id-123" } },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: {
          id: "test-id",
          name: "test-turnstile",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).get_delete.execute({ "sitekey": "test-id-123" }, context);
      assertEquals(result.dataHandles.length, 0);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});
