/**
 * Cloudflare Vectorize — vector indexes, insert/query/delete operations
 *
 * Auto-generated by scripts/cloudflare-codegen — do not edit manually.
 *
 * @module
 */
// SPDX-License-Identifier: AGPL-3.0-or-later WITH Swamp-Extension-Exception

import { z } from "npm:zod@4.4.3";
import { cfApi, cfApiPaginated } from "./_lib/api.ts";

// =============================================================================
// Schemas
// =============================================================================

const GlobalArgsSchema = z.object({
  apiToken: z.string().meta({ sensitive: true }).describe(
    "Cloudflare API token",
  ),
  accountId: z.string().describe("Cloudflare account ID"),
});

const VectorizeIndexesItemSchema = z.object({
  config: z.unknown().optional(),
  created_on: z.string().optional().describe(
    "Specifies the timestamp the resource was created as an ISO8601 string.",
  ),
  description: z.unknown().optional(),
  modified_on: z.string().optional().describe(
    "Specifies the timestamp the resource was modified as an ISO8601 string.",
  ),
  name: z.unknown().optional(),
});

const ListVectorizeIndexesSchema = z.object({
  items: z.array(VectorizeIndexesItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateVectorizeIndexSchema = z.object({
  config: z.unknown().optional(),
  created_on: z.string().optional().describe(
    "Specifies the timestamp the resource was created as an ISO8601 string.",
  ),
  description: z.unknown().optional(),
  modified_on: z.string().optional().describe(
    "Specifies the timestamp the resource was modified as an ISO8601 string.",
  ),
  name: z.unknown().optional(),
});

const DeleteVectorsByIdSchema = z.object({
  mutationId: z.unknown().optional(),
});

const GetVectorsByIdSchema = z.object({
  id: z.unknown().optional(),
  metadata: z.object({}).optional(),
  namespace: z.string().nullable().optional(),
  values: z.array(z.number()).optional(),
});

const GetVectorizeIndexInfoSchema = z.object({
  dimensions: z.unknown().optional(),
  processedUpToDatetime: z.string().nullable().optional().describe(
    "Specifies the timestamp the last mutation batch was processed as an ISO8601 string.",
  ),
  processedUpToMutation: z.unknown().optional(),
  vectorCount: z.number().int().optional().describe(
    "Specifies the number of vectors present in the index",
  ),
});

const VectorizeInsertVectorSchema = z.object({
  mutationId: z.unknown().optional(),
});

const ListVectorsSchema = z.object({
  count: z.number().int().describe(
    "Number of vectors returned in this response",
  ),
  cursorExpirationTimestamp: z.string().nullable().optional().describe(
    "When the cursor expires as an ISO8601 string",
  ),
  isTruncated: z.boolean().describe(
    "Whether there are more vectors available beyond this response",
  ),
  nextCursor: z.string().nullable().optional().describe(
    "Cursor for the next page of results",
  ),
  totalCount: z.number().int().describe("Total number of vectors in the index"),
  vectors: z.array(z.unknown()).describe("Array of vector items"),
});

const CreateMetadataIndexSchema = z.object({
  mutationId: z.unknown().optional(),
});

const DeleteMetadataIndexSchema = z.object({
  mutationId: z.unknown().optional(),
});

const ListMetadataIndexesSchema = z.object({
  metadataIndexes: z.array(z.object({
    indexType: z.enum(["string", "number", "boolean"]).optional(),
    propertyName: z.string().optional(),
  })).optional().describe("Array of indexed metadata properties."),
});

const CreateVectorizeQueryVectorSchema = z.object({
  count: z.number().int().optional().describe(
    "Specifies the count of vectors returned by the search",
  ),
  matches: z.array(z.object({
    id: z.unknown().optional(),
    metadata: z.object({}).nullable().optional(),
    namespace: z.string().nullable().optional(),
    score: z.number().optional(),
    values: z.array(z.number()).nullable().optional(),
  })).optional().describe("Array of vectors matched by the search"),
});

const VectorizeUpsertVectorSchema = z.object({
  mutationId: z.unknown().optional(),
});

// =============================================================================
// Model Definition
// =============================================================================

/** Cloudflare Vectorize — vector indexes, insert/query/delete operations */
export const model = {
  type: "@webframp/cloudflare/vectorize",
  version: "2026.07.19.1",
  globalArguments: GlobalArgsSchema,

  upgrades: [],

  resources: {
    "vectorize_indexes": {
      description: "List Vectorize Indexes",
      schema: ListVectorizeIndexesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "vectorize_index": {
      description: "Create Vectorize Index",
      schema: CreateVectorizeIndexSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "delete_vectors_by_id": {
      description: "Delete Vectors By Identifier",
      schema: DeleteVectorsByIdSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "vectors_by_id": {
      description: "Get Vectors By Identifier",
      schema: GetVectorsByIdSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "vectorize_index_info": {
      description: "Get Vectorize Index Info",
      schema: GetVectorizeIndexInfoSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "vectorize_insert_vector": {
      description: "Insert Vectors",
      schema: VectorizeInsertVectorSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "list_vectors": {
      description: "List Vectors",
      schema: ListVectorsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "metadata_index": {
      description: "Create Metadata Index",
      schema: CreateMetadataIndexSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "delete_metadata_index": {
      description: "Delete Metadata Index",
      schema: DeleteMetadataIndexSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "list_metadata_indexes": {
      description: "List Metadata Indexes",
      schema: ListMetadataIndexesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "vectorize_query_vector": {
      description: "Query Vectors",
      schema: CreateVectorizeQueryVectorSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "vectorize_upsert_vector": {
      description: "Upsert Vectors",
      schema: VectorizeUpsertVectorSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
  },

  methods: {
    list_vectorize_indexes: {
      description: "List Vectorize Indexes",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/vectorize/v2/indexes`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "vectorize_indexes",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} vectorize_indexes", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_vectorize_index: {
      description: "Create Vectorize Index",
      arguments: z.object({
        config: z.unknown(),
        description: z.unknown().optional(),
        name: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/vectorize/v2/indexes`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "vectorize_index",
          id,
          result,
        );
        context.logger.info("Created vectorize_index {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_vectorize_index: {
      description: "Get Vectorize Index",
      arguments: z.object({
        index_name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/vectorize/v2/indexes/${args.index_name}`,
        );

        const handle = await context.writeResource(
          "vectorize_index",
          String(args.index_name),
          result,
        );
        context.logger.info("Fetched vectorize_index", {});
        return { dataHandles: [handle] };
      },
    },
    delete_vectorize_index: {
      description: "Delete Vectorize Index",
      arguments: z.object({
        index_name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/vectorize/v2/indexes/${args.index_name}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.index_name });
        return { dataHandles: [] };
      },
    },
    delete_vectors_by_id: {
      description: "Delete Vectors By Identifier",
      arguments: z.object({
        index_name: z.string(),
        ids: z.array(z.unknown()).optional().describe(
          "A list of vector identifiers to delete from the index indicated by the path.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/vectorize/v2/indexes/${args.index_name}/delete_by_ids`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "delete_vectors_by_id",
          id,
          result,
        );
        context.logger.info("Created delete_vectors_by_id {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_vectors_by_id: {
      description: "Get Vectors By Identifier",
      arguments: z.object({
        index_name: z.string(),
        ids: z.array(z.unknown()).optional().describe(
          "A list of vector identifiers to retrieve from the index indicated by the path.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/vectorize/v2/indexes/${args.index_name}/get_by_ids`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "get_vectors_by_id",
          id,
          result,
        );
        context.logger.info("Created get_vectors_by_id {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_vectorize_index_info: {
      description: "Get Vectorize Index Info",
      arguments: z.object({
        index_name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/vectorize/v2/indexes/${args.index_name}/info`,
        );

        const handle = await context.writeResource(
          "vectorize_index_info",
          String(args.index_name),
          result,
        );
        context.logger.info("Fetched vectorize_index_info", {});
        return { dataHandles: [handle] };
      },
    },
    vectorize_insert_vector: {
      description: "Insert Vectors",
      arguments: z.object({
        index_name: z.string(),
        unparsable_behavior: z.enum(["error", "discard"]).optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/vectorize/v2/indexes/${args.index_name}/insert`,
        );

        const handle = await context.writeResource(
          "vectorize_insert_vector",
          "latest",
          result,
        );
        context.logger.info("Executed vectorize_insert_vector", {});
        return { dataHandles: [handle] };
      },
    },
    list_vectors: {
      description: "List Vectors",
      arguments: z.object({
        index_name: z.string(),
        count: z.number().optional(),
        cursor: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/vectorize/v2/indexes/${args.index_name}/list`,
        );

        const handle = await context.writeResource(
          "list_vectors",
          String(args.index_name),
          result,
        );
        context.logger.info("Fetched list_vectors", {});
        return { dataHandles: [handle] };
      },
    },
    create_metadata_index: {
      description: "Create Metadata Index",
      arguments: z.object({
        index_name: z.string(),
        indexType: z.enum(["string", "number", "boolean"]).describe(
          "Specifies the type of metadata property to index.",
        ),
        propertyName: z.string().describe(
          "Specifies the metadata property to index.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/vectorize/v2/indexes/${args.index_name}/metadata_index/create`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "metadata_index",
          id,
          result,
        );
        context.logger.info("Created metadata_index {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    delete_metadata_index: {
      description: "Delete Metadata Index",
      arguments: z.object({
        index_name: z.string(),
        propertyName: z.string().describe(
          "Specifies the metadata property for which the index must be deleted.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/vectorize/v2/indexes/${args.index_name}/metadata_index/delete`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "delete_metadata_index",
          id,
          result,
        );
        context.logger.info("Created delete_metadata_index {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    list_metadata_indexes: {
      description: "List Metadata Indexes",
      arguments: z.object({
        index_name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/vectorize/v2/indexes/${args.index_name}/metadata_index/list`,
        );

        const handle = await context.writeResource(
          "list_metadata_indexes",
          String(args.index_name),
          result,
        );
        context.logger.info("Fetched list_metadata_indexes", {});
        return { dataHandles: [handle] };
      },
    },
    create_vectorize_query_vector: {
      description: "Query Vectors",
      arguments: z.object({
        index_name: z.string(),
        filter: z.object({}).optional().describe(
          "A metadata filter expression used to limit nearest neighbor results.",
        ),
        returnMetadata: z.enum(["none", "indexed", "all"]).optional().describe(
          "Whether to return no metadata, indexed metadata or all metadata associated wi...",
        ),
        returnValues: z.boolean().optional().describe(
          "Whether to return the values associated with the closest vectors.",
        ),
        topK: z.number().optional().describe(
          "The number of nearest neighbors to find.",
        ),
        vector: z.array(z.number()).describe(
          "The search vector that will be used to find the nearest neighbors.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/vectorize/v2/indexes/${args.index_name}/query`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "vectorize_query_vector",
          id,
          result,
        );
        context.logger.info("Created vectorize_query_vector {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    vectorize_upsert_vector: {
      description: "Upsert Vectors",
      arguments: z.object({
        index_name: z.string(),
        unparsable_behavior: z.enum(["error", "discard"]).optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/vectorize/v2/indexes/${args.index_name}/upsert`,
        );

        const handle = await context.writeResource(
          "vectorize_upsert_vector",
          "latest",
          result,
        );
        context.logger.info("Executed vectorize_upsert_vector", {});
        return { dataHandles: [handle] };
      },
    },
  },
};
