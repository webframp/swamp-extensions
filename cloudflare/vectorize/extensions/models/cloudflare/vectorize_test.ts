// Cloudflare Vectorize — vector indexes, insert/query/delete operations — Tests
// Auto-generated by scripts/cloudflare-codegen — do not edit manually.
// SPDX-License-Identifier: Apache-2.0

import { assertEquals, assertExists } from "jsr:@std/assert@1";
import { createModelTestContext } from "@systeminit/swamp-testing";
import { model } from "./vectorize.ts";

// ---------------------------------------------------------------------------
// Model Export Structure Tests
// ---------------------------------------------------------------------------

Deno.test("vectorize model: has correct type", () => {
  assertEquals(model.type, "@webframp/cloudflare/vectorize");
});

Deno.test("vectorize model: has valid version format", () => {
  const versionPattern = /^\d{4}\.\d{2}\.\d{2}\.\d+$/;
  assertEquals(versionPattern.test(model.version), true);
});

Deno.test("vectorize model: has globalArguments with apiToken", () => {
  assertExists(model.globalArguments);
  const shape = model.globalArguments.shape;
  assertExists(shape.apiToken);
  assertExists(shape.accountId);
});

Deno.test("vectorize model: has expected methods", () => {
  assertExists(model.methods);
  assertExists(model.methods.list_vectorize_indexes);
  assertExists(model.methods.create_vectorize_index);
  assertExists(model.methods.get_vectorize_index);
  assertExists(model.methods.delete_vectorize_index);
  assertExists(model.methods.delete_vectors_by_id);
  assertExists(model.methods.get_vectors_by_id);
  assertExists(model.methods.get_vectorize_index_info);
  assertExists(model.methods.vectorize_insert_vector);
  assertExists(model.methods.list_vectors);
  assertExists(model.methods.create_metadata_index);
  assertExists(model.methods.delete_metadata_index);
  assertExists(model.methods.list_metadata_indexes);
  assertExists(model.methods.create_vectorize_query_vector);
  assertExists(model.methods.vectorize_upsert_vector);
});

Deno.test("vectorize model: has expected resources", () => {
  assertExists(model.resources);
  assertExists(model.resources["vectorize_indexes"]);
  assertExists(model.resources["vectorize_index"]);
  assertExists(model.resources["delete_vectors_by_id"]);
  assertExists(model.resources["vectors_by_id"]);
  assertExists(model.resources["vectorize_index_info"]);
  assertExists(model.resources["vectorize_insert_vector"]);
  assertExists(model.resources["list_vectors"]);
  assertExists(model.resources["metadata_index"]);
  assertExists(model.resources["delete_metadata_index"]);
  assertExists(model.resources["list_metadata_indexes"]);
  assertExists(model.resources["vectorize_query_vector"]);
  assertExists(model.resources["vectorize_upsert_vector"]);
});

// ---------------------------------------------------------------------------
// Mock Cloudflare API Server
// ---------------------------------------------------------------------------

function startMockCfServer(
  responses: Record<string, { result: unknown; isCollection?: boolean }>,
): { url: string; server: Deno.HttpServer } {
  const server = Deno.serve({ port: 0, onListen() {} }, (req) => {
    const url = new URL(req.url);
    const path = url.pathname;

    for (
      const [pattern, { result, isCollection }] of Object.entries(responses)
    ) {
      if (path.includes(pattern)) {
        return Response.json({
          success: true,
          errors: [],
          messages: [],
          result,
          result_info: isCollection
            ? {
              page: 1,
              per_page: 50,
              total_count: Array.isArray(result) ? result.length : 1,
              total_pages: 1,
            }
            : undefined,
        });
      }
    }

    return Response.json({
      success: false,
      errors: [{ code: 404, message: "Not found" }],
      messages: [],
      result: null,
    });
  });

  const addr = server.addr as Deno.NetAddr;
  return { url: `http://localhost:${addr.port}`, server };
}

function installFetchMock(mockUrl: string): () => void {
  const originalFetch = globalThis.fetch;
  globalThis.fetch = (input, init) => {
    const reqUrl = typeof input === "string" ? input : input.toString();
    const newUrl = reqUrl.replace(
      "https://api.cloudflare.com/client/v4",
      mockUrl,
    );
    return originalFetch(newUrl, init);
  };
  return () => {
    globalThis.fetch = originalFetch;
  };
}

Deno.test({
  name: "vectorize model: list_vectorize_indexes fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = [{
      "config": null,
      "created_on": "2022-11-15T18:25:44.442097Z",
      "description": null,
      "modified_on": "2022-11-15T18:25:44.442097Z",
      "name": null,
    }];
    const { url, server } = startMockCfServer({
      "/vectorize/v2/indexes": { result: mockData, isCollection: true },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: {
          id: "test-id",
          name: "test-vectorize",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).list_vectorize_indexes.execute({}, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "vectorize model: create_vectorize_index creates and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "id": "new-123",
      "config": null,
      "created_on": "2022-11-15T18:25:44.442097Z",
      "description": null,
      "modified_on": "2022-11-15T18:25:44.442097Z",
      "name": null,
    };
    const { url, server } = startMockCfServer({
      "/vectorize/v2/indexes": { result: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: {
          id: "test-id",
          name: "test-vectorize",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).create_vectorize_index.execute({
        "config": null,
        "created_on": "2022-11-15T18:25:44.442097Z",
        "description": null,
        "modified_on": "2022-11-15T18:25:44.442097Z",
        "name": null,
      }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "vectorize model: get_vectorize_index fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "config": null,
      "created_on": "2022-11-15T18:25:44.442097Z",
      "description": null,
      "modified_on": "2022-11-15T18:25:44.442097Z",
      "name": null,
    };
    const { url, server } = startMockCfServer({
      "/vectorize/v2/indexes/test-id-123": { result: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: {
          id: "test-id",
          name: "test-vectorize",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).get_vectorize_index.execute(
        { "index_name": "test-id-123" },
        context,
      );
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "vectorize model: delete_vectorize_index executes successfully",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockCfServer({
      "/vectorize/v2/indexes/test-id-123": { result: { id: "test-id-123" } },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: {
          id: "test-id",
          name: "test-vectorize",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).delete_vectorize_index.execute(
        { "index_name": "test-id-123" },
        context,
      );
      assertEquals(result.dataHandles.length, 0);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "vectorize model: vectorize_insert_vector executes and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = { "mutationId": null };
    const { url, server } = startMockCfServer({
      "/vectorize/v2/indexes/test-id-123/insert": { result: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: {
          id: "test-id",
          name: "test-vectorize",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).vectorize_insert_vector.execute({
        "index_name": "test-id-123",
        "mutationId": null,
      }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});
