/**
 * Cloudflare Workers AI — model inference, fine-tuning, LoRA adapters
 *
 * Auto-generated by scripts/cloudflare-codegen — do not edit manually.
 *
 * @module
 */
// SPDX-License-Identifier: AGPL-3.0-or-later WITH Swamp-Extension-Exception

import { z } from "npm:zod@4.4.3";
import { cfApi, cfApiPaginated } from "./_lib/api.ts";

// =============================================================================
// Schemas
// =============================================================================

const GlobalArgsSchema = z.object({
  apiToken: z.string().meta({ sensitive: true }).describe(
    "Cloudflare API token",
  ),
  accountId: z.string().describe("Cloudflare account ID"),
});

const GetCreditBalanceSchema = z.object({
  balance: z.number(),
  first_topup_success: z.boolean().optional(),
  has_default_payment_method: z.boolean(),
  payment_method: z.object({
    brand: z.string().optional(),
    last4: z.string().optional(),
  }).nullable(),
  topup_config: z.object({
    amount: z.number().nullable(),
    disabledReason: z.string().nullable(),
    error: z.string().nullable(),
    lastFailedAt: z.number().nullable(),
    threshold: z.number().nullable(),
  }),
});

const GetInvoiceHistorySchema = z.object({
  invoices: z.array(z.object({
    amount_due: z.number(),
    amount_paid: z.number(),
    amount_remaining: z.number(),
    attempt_count: z.number().optional(),
    attempted: z.boolean().optional(),
    auto_advance: z.boolean().nullable().optional(),
    created: z.number().optional(),
    created_by: z.string().optional(),
    currency: z.string(),
    description: z.string().nullable().optional(),
    id: z.string().nullable().optional(),
    invoice_origin: z.string().optional(),
    invoice_pdf: z.string().nullable().optional(),
    status: z.string().nullable().optional(),
  })),
  pagination: z.object({
    has_more: z.boolean(),
    page: z.number(),
    per_page: z.number(),
    total_count: z.number(),
  }),
});

const GetInvoicePreviewSchema = z.object({
  amount_due: z.number(),
  amount_paid: z.number(),
  amount_remaining: z.number(),
  currency: z.string(),
  id: z.string(),
  invoice_lines: z.array(z.object({
    amount: z.number(),
    currency: z.string(),
    description: z.string().nullable(),
    period: z.object({
      end: z.number(),
      start: z.number(),
    }),
    pretax_credit_amounts: z.array(z.object({
      amount: z.number(),
      credit_balance_transaction: z.string().nullable().optional(),
      discount: z.string().nullable().optional(),
      type: z.string(),
    })).optional(),
    pricing: z.object({
      unit_amount_decimal: z.string().nullable(),
    }),
    quantity: z.number(),
  })),
  period_end: z.number(),
  period_start: z.number(),
  status: z.enum(["draft", "open", "paid", "uncollectible", "void"]),
});

const GetSpendingLimitSchema = z.object({
  config: z.object({
    amount: z.number().nullable(),
    duration: z.string().nullable(),
    strategy: z.string().nullable(),
  }),
  enabled: z.boolean(),
});

const CreateTopupSchema = z.object({
  brand: z.string().optional().describe("Card brand (visa, mastercard, etc.)."),
  client_secret: z.string().nullable().describe(
    "Stripe PaymentIntent client secret.",
  ),
  last4: z.string().optional().describe("Last 4 digits of card."),
  onboarding: z.boolean().describe("Whether the user was already onboarded."),
  payment_intent_id: z.string().describe("Stripe invoice ID."),
});

const GetTopupConfigSchema = z.object({
  amount: z.number().nullable(),
  disabledReason: z.string().nullable(),
  error: z.string().nullable(),
  lastFailedAt: z.number().nullable(),
  threshold: z.number().nullable(),
});

const CreateAigBillingSetTopupConfigSchema = z.object({
  amount: z.number(),
  threshold: z.number(),
});

const GetTopupLimitsSchema = z.object({
  currency: z.string().describe("ISO 4217 currency code."),
  max_cents: z.number().int(),
  min_cents: z.number().int().describe(
    "Minimum allowed top-up amount in cents.",
  ),
});

const CreateAigBillingCheckTopupStatusSchema = z.object({
  payment_intent_id: z.string(),
  status: z.enum(["completed", "pending"]),
});

const GetUsageHistorySchema = z.object({
  history: z.array(z.object({
    aggregated_value: z.number(),
    end_time: z.number(),
    id: z.string(),
    start_time: z.number(),
  })),
});

const AccountProviderItemSchema = z.object({
  base_url: z.string(),
  beta: z.boolean().optional(),
  created_at: z.string(),
  curl_example: z.string().optional(),
  description: z.string().optional(),
  enable: z.boolean().optional(),
  headers: z.string().max(8192).optional(),
  id: z.string(),
  js_example: z.string().optional(),
  link: z.string().optional(),
  logo: z.string().optional(),
  modified_at: z.string(),
  name: z.string(),
  position: z.number().int().optional(),
  slug: z.string(),
});

const ListAccountProviderSchema = z.object({
  items: z.array(AccountProviderItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const AccountProviderCostItemSchema = z.object({
  account_provider_id: z.string(),
  changed_by: z.string().optional(),
  cost_in: z.number().optional(),
  cost_out: z.number().optional(),
  cost_type: z.string().optional(),
  created_at: z.string(),
  enable: z.boolean().optional(),
  id: z.string(),
  model: z.string(),
  model_rule: z.enum(["equals", "starts-with", "contains"]).optional(),
  modified_at: z.string(),
  token_pricing: z.string().optional(),
  weight: z.number().int().optional(),
});

const ListAccountProviderCostSchema = z.object({
  items: z.array(AccountProviderCostItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetAigConfigFetchAccountProviderCostSchema = z.object({
  account_provider_id: z.string(),
  changed_by: z.string().optional(),
  cost_in: z.number().optional(),
  cost_out: z.number().optional(),
  cost_type: z.string().optional(),
  created_at: z.string(),
  enable: z.boolean().optional(),
  id: z.string(),
  model: z.string(),
  model_rule: z.enum(["equals", "starts-with", "contains"]).optional(),
  modified_at: z.string(),
  token_pricing: z.string().optional(),
  weight: z.number().int().optional(),
});

const GetAigConfigFetchAccountProviderSchema = z.object({
  base_url: z.string(),
  beta: z.boolean().optional(),
  created_at: z.string(),
  curl_example: z.string().optional(),
  description: z.string().optional(),
  enable: z.boolean().optional(),
  headers: z.string().max(8192).optional(),
  id: z.string(),
  js_example: z.string().optional(),
  link: z.string().optional(),
  logo: z.string().optional(),
  modified_at: z.string(),
  name: z.string(),
  position: z.number().int().optional(),
  slug: z.string(),
});

const EvaluatorsItemSchema = z.object({
  created_at: z.string(),
  description: z.string(),
  enable: z.boolean(),
  id: z.string(),
  mandatory: z.boolean(),
  modified_at: z.string(),
  name: z.string(),
  type: z.string(),
});

const ListEvaluatorsSchema = z.object({
  items: z.array(EvaluatorsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GatewayItemSchema = z.object({
  authentication: z.boolean().optional(),
  cache_invalidate_on_update: z.boolean(),
  cache_ttl: z.number().int().min(0).nullable(),
  collect_logs: z.boolean(),
  created_at: z.string(),
  dlp: z.union([
    z.object({
      action: z.enum(["BLOCK", "FLAG"]),
      enabled: z.boolean(),
      profiles: z.array(z.string()),
    }),
    z.object({
      enabled: z.boolean(),
      policies: z.array(z.object({
        action: z.enum(["FLAG", "BLOCK"]),
        check: z.array(z.enum(["REQUEST", "RESPONSE"])),
        enabled: z.boolean(),
        id: z.string(),
        profiles: z.array(z.string()),
      })),
    }),
  ]).optional(),
  guardrails: z.object({
    prompt: z.object({
      P1: z.enum(["FLAG", "BLOCK"]).optional(),
      S1: z.enum(["FLAG", "BLOCK"]).optional(),
      S10: z.enum(["FLAG", "BLOCK"]).optional(),
      S11: z.enum(["FLAG", "BLOCK"]).optional(),
      S12: z.enum(["FLAG", "BLOCK"]).optional(),
      S13: z.enum(["FLAG", "BLOCK"]).optional(),
      S2: z.enum(["FLAG", "BLOCK"]).optional(),
      S3: z.enum(["FLAG", "BLOCK"]).optional(),
      S4: z.enum(["FLAG", "BLOCK"]).optional(),
      S5: z.enum(["FLAG", "BLOCK"]).optional(),
      S6: z.enum(["FLAG", "BLOCK"]).optional(),
      S7: z.enum(["FLAG", "BLOCK"]).optional(),
      S8: z.enum(["FLAG", "BLOCK"]).optional(),
      S9: z.enum(["FLAG", "BLOCK"]).optional(),
    }),
    response: z.object({
      P1: z.enum(["FLAG", "BLOCK"]).optional(),
      S1: z.enum(["FLAG", "BLOCK"]).optional(),
      S10: z.enum(["FLAG", "BLOCK"]).optional(),
      S11: z.enum(["FLAG", "BLOCK"]).optional(),
      S12: z.enum(["FLAG", "BLOCK"]).optional(),
      S13: z.enum(["FLAG", "BLOCK"]).optional(),
      S2: z.enum(["FLAG", "BLOCK"]).optional(),
      S3: z.enum(["FLAG", "BLOCK"]).optional(),
      S4: z.enum(["FLAG", "BLOCK"]).optional(),
      S5: z.enum(["FLAG", "BLOCK"]).optional(),
      S6: z.enum(["FLAG", "BLOCK"]).optional(),
      S7: z.enum(["FLAG", "BLOCK"]).optional(),
      S8: z.enum(["FLAG", "BLOCK"]).optional(),
      S9: z.enum(["FLAG", "BLOCK"]).optional(),
    }),
  }).nullable().optional(),
  id: z.string().min(1).max(64).describe("gateway id"),
  is_default: z.boolean().optional(),
  log_management: z.number().int().min(10000).max(10000000).nullable()
    .optional(),
  log_management_strategy: z.enum(["STOP_INSERTING", "DELETE_OLDEST"])
    .nullable().optional(),
  logpush: z.boolean().optional(),
  logpush_public_key: z.string().min(16).max(1024).nullable().optional(),
  modified_at: z.string(),
  otel: z.array(z.object({
    authorization: z.string().max(256).optional(),
    content_type: z.enum(["json", "protobuf"]).optional(),
    headers: z.record(z.string(), z.string().max(4096)),
    url: z.string().max(2048),
  })).nullable().optional(),
  rate_limiting_interval: z.number().int().min(0).nullable(),
  rate_limiting_limit: z.number().int().min(0).nullable(),
  rate_limiting_technique: z.enum(["fixed", "sliding"]).nullable().optional(),
  retry_backoff: z.enum(["constant", "linear", "exponential"]).nullable()
    .optional().describe("Backoff strategy for retry delays"),
  retry_delay: z.number().int().min(0).max(5000).nullable().optional().describe(
    "Delay between retry attempts in milliseconds (0-5000)",
  ),
  retry_max_attempts: z.number().int().min(1).max(5).nullable().optional()
    .describe("Maximum number of retry attempts for failed requests (1-5)"),
  spend_limits: z.object({
    enabled: z.boolean().optional(),
    rules: z.array(z.object({
      enabled: z.boolean().optional(),
      id: z.string().min(1).optional(),
      limit: z.number().min(0),
      limitType: z.enum(["cost"]),
      metadata: z.record(
        z.string(),
        z.union([
          z.object({
            mode: z.enum(["partition"]),
          }),
          z.object({
            mode: z.enum(["filter"]),
            values: z.array(z.unknown()),
          }),
        ]),
      ).optional(),
      model: z.object({
        mode: z.enum(["filter"]),
        values: z.array(z.string()),
      }).optional(),
      provider: z.object({
        mode: z.enum(["filter"]),
        values: z.array(z.string()),
      }).optional(),
      technique: z.enum(["fixed", "sliding"]).optional(),
      window: z.number().int().min(0),
    })).optional(),
  }).nullable().optional(),
  store_id: z.string().nullable().optional(),
  stripe: z.object({
    authorization: z.string(),
    usage_events: z.array(z.object({
      payload: z.string(),
    })),
  }).nullable().optional(),
  workers_ai_billing_mode: z.enum(["postpaid"]).optional().describe(
    "Controls how Workers AI inference calls routed through this gateway are billed. Only 'postpaid' i...",
  ),
  zdr: z.boolean().optional(),
});

const ListGatewaySchema = z.object({
  items: z.array(GatewayItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const DatasetItemSchema = z.object({
  created_at: z.string(),
  enable: z.boolean(),
  filters: z.array(z.object({
    key: z.enum([
      "created_at",
      "request_content_type",
      "response_content_type",
      "success",
      "cached",
      "provider",
      "model",
      "cost",
      "tokens",
      "tokens_in",
      "tokens_out",
      "duration",
      "feedback",
    ]),
    operator: z.enum(["eq", "contains", "lt", "gt"]),
    value: z.array(z.union([z.string(), z.number(), z.boolean()])),
  })),
  gateway_id: z.string().min(1).max(64).describe("gateway id"),
  id: z.string(),
  modified_at: z.string(),
  name: z.string(),
});

const ListDatasetSchema = z.object({
  items: z.array(DatasetItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetAigConfigFetchDatasetSchema = z.object({
  created_at: z.string(),
  enable: z.boolean(),
  filters: z.array(z.object({
    key: z.enum([
      "created_at",
      "request_content_type",
      "response_content_type",
      "success",
      "cached",
      "provider",
      "model",
      "cost",
      "tokens",
      "tokens_in",
      "tokens_out",
      "duration",
      "feedback",
    ]),
    operator: z.enum(["eq", "contains", "lt", "gt"]),
    value: z.array(z.union([z.string(), z.number(), z.boolean()])),
  })),
  gateway_id: z.string().min(1).max(64).describe("gateway id"),
  id: z.string(),
  modified_at: z.string(),
  name: z.string(),
});

const EvaluationsItemSchema = z.object({
  created_at: z.string(),
  datasets: z.array(z.object({
    account_id: z.string(),
    account_tag: z.string(),
    created_at: z.string(),
    enable: z.boolean(),
    filters: z.array(z.object({
      key: z.enum([
        "created_at",
        "request_content_type",
        "response_content_type",
        "success",
        "cached",
        "provider",
        "model",
        "cost",
        "tokens",
        "tokens_in",
        "tokens_out",
        "duration",
        "feedback",
      ]),
      operator: z.enum(["eq", "contains", "lt", "gt"]),
      value: z.array(z.union([z.string(), z.number(), z.boolean()])),
    })),
    gateway_id: z.string().min(1).max(64),
    id: z.string(),
    modified_at: z.string(),
    name: z.string(),
  })),
  gateway_id: z.string().min(1).max(64).describe("gateway id"),
  id: z.string(),
  modified_at: z.string(),
  name: z.string(),
  processed: z.boolean(),
  results: z.array(z.object({
    created_at: z.string(),
    evaluation_id: z.string(),
    evaluation_type_id: z.string(),
    id: z.string(),
    modified_at: z.string(),
    result: z.string(),
    status: z.number(),
    status_description: z.string(),
    total_logs: z.number(),
  })),
  total_logs: z.number(),
});

const ListEvaluationsSchema = z.object({
  items: z.array(EvaluationsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetAigConfigFetchEvaluationsSchema = z.object({
  created_at: z.string(),
  datasets: z.array(z.object({
    account_id: z.string(),
    account_tag: z.string(),
    created_at: z.string(),
    enable: z.boolean(),
    filters: z.array(z.object({
      key: z.enum([
        "created_at",
        "request_content_type",
        "response_content_type",
        "success",
        "cached",
        "provider",
        "model",
        "cost",
        "tokens",
        "tokens_in",
        "tokens_out",
        "duration",
        "feedback",
      ]),
      operator: z.enum(["eq", "contains", "lt", "gt"]),
      value: z.array(z.union([z.string(), z.number(), z.boolean()])),
    })),
    gateway_id: z.string().min(1).max(64),
    id: z.string(),
    modified_at: z.string(),
    name: z.string(),
  })),
  gateway_id: z.string().min(1).max(64).describe("gateway id"),
  id: z.string(),
  modified_at: z.string(),
  name: z.string(),
  processed: z.boolean(),
  results: z.array(z.object({
    created_at: z.string(),
    evaluation_id: z.string(),
    evaluation_type_id: z.string(),
    id: z.string(),
    modified_at: z.string(),
    result: z.string(),
    status: z.number(),
    status_description: z.string(),
    total_logs: z.number(),
  })),
  total_logs: z.number(),
});

const GatewayLogsItemSchema = z.object({
  cached: z.boolean(),
  cost: z.number().optional(),
  created_at: z.string(),
  custom_cost: z.boolean().optional(),
  duration: z.number().int(),
  id: z.string(),
  metadata: z.string().optional(),
  model: z.string(),
  model_type: z.string().optional(),
  path: z.string(),
  provider: z.string(),
  request_content_type: z.string().optional(),
  request_type: z.string().optional(),
  response_content_type: z.string().optional(),
  status_code: z.number().int().optional(),
  step: z.number().int().optional(),
  success: z.boolean(),
  tokens_in: z.number().int().nullable(),
  tokens_out: z.number().int().nullable(),
});

const ListGatewayLogsSchema = z.object({
  items: z.array(GatewayLogsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetGatewayLogDetailSchema = z.object({
  cached: z.boolean(),
  cost: z.number().optional(),
  created_at: z.string(),
  custom_cost: z.boolean().optional(),
  duration: z.number().int(),
  id: z.string(),
  metadata: z.string().optional(),
  model: z.string(),
  model_type: z.string().optional(),
  path: z.string(),
  provider: z.string(),
  request_content_type: z.string().optional(),
  request_head: z.string().optional(),
  request_head_complete: z.boolean().optional(),
  request_size: z.number().int().optional(),
  request_type: z.string().optional(),
  response_content_type: z.string().optional(),
  response_head: z.string().optional(),
  response_head_complete: z.boolean().optional(),
  response_size: z.number().int().optional(),
  status_code: z.number().int().optional(),
  step: z.number().int().optional(),
  success: z.boolean(),
  tokens_in: z.number().int().nullable(),
  tokens_out: z.number().int().nullable(),
});

const PatchGatewayLogSchema = z.object({});

const GetGatewayLogRequestSchema = z.object({});

const GetGatewayLogResponseSchema = z.object({});

const ProvidersItemSchema = z.object({
  alias: z.string(),
  default_config: z.boolean(),
  gateway_id: z.string().min(1).max(64).describe("gateway id"),
  id: z.string(),
  modified_at: z.string(),
  provider_slug: z.string(),
  rate_limit: z.number().optional(),
  rate_limit_period: z.number().optional(),
  secret_id: z.string(),
  secret_preview: z.string(),
});

const ListProvidersSchema = z.object({
  items: z.array(ProvidersItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const ListGatewayDynamicRoutesSchema = z.object({
  data: z.object({
    order_by: z.string(),
    order_by_direction: z.string(),
    page: z.number(),
    per_page: z.number(),
    routes: z.array(z.object({
      account_tag: z.string(),
      created_at: z.string(),
      deployment: z.object({
        created_at: z.string(),
        deployment_id: z.string(),
        version_id: z.string(),
      }),
      elements: z.array(z.union([
        z.object({
          id: z.string(),
          outputs: z.object({
            next: z.unknown(),
          }),
          type: z.enum(["start"]),
        }),
        z.object({
          id: z.string(),
          outputs: z.object({
            false: z.unknown(),
            true: z.unknown(),
          }),
          properties: z.object({
            conditions: z.unknown().optional(),
          }),
          type: z.enum(["conditional"]),
        }),
        z.object({
          id: z.string(),
          outputs: z.record(z.string(), z.unknown()),
          type: z.enum(["percentage"]),
        }),
        z.object({
          id: z.string(),
          outputs: z.object({
            fallback: z.unknown(),
            success: z.unknown(),
          }),
          properties: z.object({
            key: z.unknown(),
            limit: z.unknown(),
            limitType: z.unknown(),
            window: z.unknown(),
          }),
          type: z.enum(["rate"]),
        }),
        z.object({
          id: z.string(),
          outputs: z.object({
            fallback: z.unknown(),
            success: z.unknown(),
          }),
          properties: z.object({
            model: z.unknown(),
            provider: z.unknown(),
            retries: z.unknown(),
            timeout: z.unknown(),
          }),
          type: z.enum(["model"]),
        }),
        z.object({
          id: z.string(),
          outputs: z.record(z.string(), z.unknown()),
          type: z.enum(["end"]),
        }),
      ])),
      gateway_id: z.string(),
      id: z.string(),
      modified_at: z.string(),
      name: z.string(),
      version: z.object({
        active: z.enum(["true", "false"]),
        created_at: z.string(),
        data: z.string(),
        is_valid: z.boolean().optional(),
        version_id: z.string(),
      }),
    })),
  }),
  success: z.boolean(),
});

const CreateAigConfigPostGatewayDynamicRouteSchema = z.object({
  created_at: z.string(),
  deployment: z.object({
    created_at: z.string(),
    deployment_id: z.string(),
    version_id: z.string(),
  }),
  elements: z.array(z.union([
    z.object({
      id: z.string(),
      outputs: z.object({
        next: z.object({
          elementId: z.string(),
        }),
      }),
      type: z.enum(["start"]),
    }),
    z.object({
      id: z.string(),
      outputs: z.object({
        false: z.object({
          elementId: z.string(),
        }),
        true: z.object({
          elementId: z.string(),
        }),
      }),
      properties: z.object({
        conditions: z.object({}).optional(),
      }),
      type: z.enum(["conditional"]),
    }),
    z.object({
      id: z.string(),
      outputs: z.record(
        z.string(),
        z.object({
          elementId: z.string(),
        }),
      ),
      type: z.enum(["percentage"]),
    }),
    z.object({
      id: z.string(),
      outputs: z.object({
        fallback: z.object({
          elementId: z.string(),
        }),
        success: z.object({
          elementId: z.string(),
        }),
      }),
      properties: z.object({
        key: z.string(),
        limit: z.number(),
        limitType: z.enum(["count", "cost"]),
        window: z.number(),
      }),
      type: z.enum(["rate"]),
    }),
    z.object({
      id: z.string(),
      outputs: z.object({
        fallback: z.object({
          elementId: z.string(),
        }),
        success: z.object({
          elementId: z.string(),
        }),
      }),
      properties: z.object({
        model: z.string(),
        provider: z.string(),
        retries: z.number(),
        timeout: z.number(),
      }),
      type: z.enum(["model"]),
    }),
    z.object({
      id: z.string(),
      outputs: z.record(
        z.string(),
        z.object({
          elementId: z.string(),
        }),
      ),
      type: z.enum(["end"]),
    }),
  ])),
  gateway_id: z.string(),
  id: z.string(),
  modified_at: z.string(),
  name: z.string(),
  version: z.object({
    active: z.enum(["true", "false"]),
    created_at: z.string(),
    data: z.string(),
    is_valid: z.boolean().optional(),
    version_id: z.string(),
  }),
});

const GetGatewayDynamicRouteSchema = z.object({
  created_at: z.string(),
  deployment: z.object({
    created_at: z.string(),
    deployment_id: z.string(),
    version_id: z.string(),
  }),
  elements: z.array(z.union([
    z.object({
      id: z.string(),
      outputs: z.object({
        next: z.object({
          elementId: z.string(),
        }),
      }),
      type: z.enum(["start"]),
    }),
    z.object({
      id: z.string(),
      outputs: z.object({
        false: z.object({
          elementId: z.string(),
        }),
        true: z.object({
          elementId: z.string(),
        }),
      }),
      properties: z.object({
        conditions: z.object({}).optional(),
      }),
      type: z.enum(["conditional"]),
    }),
    z.object({
      id: z.string(),
      outputs: z.record(
        z.string(),
        z.object({
          elementId: z.string(),
        }),
      ),
      type: z.enum(["percentage"]),
    }),
    z.object({
      id: z.string(),
      outputs: z.object({
        fallback: z.object({
          elementId: z.string(),
        }),
        success: z.object({
          elementId: z.string(),
        }),
      }),
      properties: z.object({
        key: z.string(),
        limit: z.number(),
        limitType: z.enum(["count", "cost"]),
        window: z.number(),
      }),
      type: z.enum(["rate"]),
    }),
    z.object({
      id: z.string(),
      outputs: z.object({
        fallback: z.object({
          elementId: z.string(),
        }),
        success: z.object({
          elementId: z.string(),
        }),
      }),
      properties: z.object({
        model: z.string(),
        provider: z.string(),
        retries: z.number(),
        timeout: z.number(),
      }),
      type: z.enum(["model"]),
    }),
    z.object({
      id: z.string(),
      outputs: z.record(
        z.string(),
        z.object({
          elementId: z.string(),
        }),
      ),
      type: z.enum(["end"]),
    }),
  ])),
  gateway_id: z.string(),
  id: z.string(),
  modified_at: z.string(),
  name: z.string(),
  version: z.object({
    active: z.enum(["true", "false"]),
    created_at: z.string(),
    data: z.string(),
    is_valid: z.boolean().optional(),
    version_id: z.string(),
  }),
});

const ListGatewayDynamicRouteDeploymentsSchema = z.object({
  data: z.object({
    deployments: z.array(z.object({
      created_at: z.string(),
      deployment_id: z.string(),
      version_id: z.string(),
    })),
    order_by: z.string(),
    order_by_direction: z.string(),
    page: z.number(),
    per_page: z.number(),
  }),
  success: z.boolean(),
});

const CreateAigConfigPostGatewayDynamicRouteDeploymentSchema = z.object({
  created_at: z.string(),
  elements: z.array(z.union([
    z.object({
      id: z.string(),
      outputs: z.object({
        next: z.object({
          elementId: z.string(),
        }),
      }),
      type: z.enum(["start"]),
    }),
    z.object({
      id: z.string(),
      outputs: z.object({
        false: z.object({
          elementId: z.string(),
        }),
        true: z.object({
          elementId: z.string(),
        }),
      }),
      properties: z.object({
        conditions: z.object({}).optional(),
      }),
      type: z.enum(["conditional"]),
    }),
    z.object({
      id: z.string(),
      outputs: z.record(
        z.string(),
        z.object({
          elementId: z.string(),
        }),
      ),
      type: z.enum(["percentage"]),
    }),
    z.object({
      id: z.string(),
      outputs: z.object({
        fallback: z.object({
          elementId: z.string(),
        }),
        success: z.object({
          elementId: z.string(),
        }),
      }),
      properties: z.object({
        key: z.string(),
        limit: z.number(),
        limitType: z.enum(["count", "cost"]),
        window: z.number(),
      }),
      type: z.enum(["rate"]),
    }),
    z.object({
      id: z.string(),
      outputs: z.object({
        fallback: z.object({
          elementId: z.string(),
        }),
        success: z.object({
          elementId: z.string(),
        }),
      }),
      properties: z.object({
        model: z.string(),
        provider: z.string(),
        retries: z.number(),
        timeout: z.number(),
      }),
      type: z.enum(["model"]),
    }),
    z.object({
      id: z.string(),
      outputs: z.record(
        z.string(),
        z.object({
          elementId: z.string(),
        }),
      ),
      type: z.enum(["end"]),
    }),
  ])),
  gateway_id: z.string(),
  id: z.string(),
  modified_at: z.string(),
  name: z.string(),
});

const ListGatewayDynamicRouteVersionsSchema = z.object({
  data: z.object({
    order_by: z.string(),
    order_by_direction: z.string(),
    page: z.number(),
    per_page: z.number(),
    versions: z.array(z.object({
      active: z.enum(["true", "false"]),
      created_at: z.string(),
      data: z.string(),
      is_valid: z.boolean().optional(),
      version_id: z.string(),
    })),
  }),
  success: z.boolean(),
});

const CreateAigConfigPostGatewayDynamicRouteVersionSchema = z.object({
  created_at: z.string(),
  elements: z.array(z.union([
    z.object({
      id: z.string(),
      outputs: z.object({
        next: z.object({
          elementId: z.string(),
        }),
      }),
      type: z.enum(["start"]),
    }),
    z.object({
      id: z.string(),
      outputs: z.object({
        false: z.object({
          elementId: z.string(),
        }),
        true: z.object({
          elementId: z.string(),
        }),
      }),
      properties: z.object({
        conditions: z.object({}).optional(),
      }),
      type: z.enum(["conditional"]),
    }),
    z.object({
      id: z.string(),
      outputs: z.record(
        z.string(),
        z.object({
          elementId: z.string(),
        }),
      ),
      type: z.enum(["percentage"]),
    }),
    z.object({
      id: z.string(),
      outputs: z.object({
        fallback: z.object({
          elementId: z.string(),
        }),
        success: z.object({
          elementId: z.string(),
        }),
      }),
      properties: z.object({
        key: z.string(),
        limit: z.number(),
        limitType: z.enum(["count", "cost"]),
        window: z.number(),
      }),
      type: z.enum(["rate"]),
    }),
    z.object({
      id: z.string(),
      outputs: z.object({
        fallback: z.object({
          elementId: z.string(),
        }),
        success: z.object({
          elementId: z.string(),
        }),
      }),
      properties: z.object({
        model: z.string(),
        provider: z.string(),
        retries: z.number(),
        timeout: z.number(),
      }),
      type: z.enum(["model"]),
    }),
    z.object({
      id: z.string(),
      outputs: z.record(
        z.string(),
        z.object({
          elementId: z.string(),
        }),
      ),
      type: z.enum(["end"]),
    }),
  ])),
  gateway_id: z.string(),
  id: z.string(),
  modified_at: z.string(),
  name: z.string(),
});

const GetGatewayDynamicRouteVersionSchema = z.object({
  active: z.enum(["true", "false"]),
  created_at: z.string(),
  data: z.string(),
  elements: z.array(z.union([
    z.object({
      id: z.string(),
      outputs: z.object({
        next: z.object({
          elementId: z.string(),
        }),
      }),
      type: z.enum(["start"]),
    }),
    z.object({
      id: z.string(),
      outputs: z.object({
        false: z.object({
          elementId: z.string(),
        }),
        true: z.object({
          elementId: z.string(),
        }),
      }),
      properties: z.object({
        conditions: z.object({}).optional(),
      }),
      type: z.enum(["conditional"]),
    }),
    z.object({
      id: z.string(),
      outputs: z.record(
        z.string(),
        z.object({
          elementId: z.string(),
        }),
      ),
      type: z.enum(["percentage"]),
    }),
    z.object({
      id: z.string(),
      outputs: z.object({
        fallback: z.object({
          elementId: z.string(),
        }),
        success: z.object({
          elementId: z.string(),
        }),
      }),
      properties: z.object({
        key: z.string(),
        limit: z.number(),
        limitType: z.enum(["count", "cost"]),
        window: z.number(),
      }),
      type: z.enum(["rate"]),
    }),
    z.object({
      id: z.string(),
      outputs: z.object({
        fallback: z.object({
          elementId: z.string(),
        }),
        success: z.object({
          elementId: z.string(),
        }),
      }),
      properties: z.object({
        model: z.string(),
        provider: z.string(),
        retries: z.number(),
        timeout: z.number(),
      }),
      type: z.enum(["model"]),
    }),
    z.object({
      id: z.string(),
      outputs: z.record(
        z.string(),
        z.object({
          elementId: z.string(),
        }),
      ),
      type: z.enum(["end"]),
    }),
  ])),
  gateway_id: z.string(),
  id: z.string(),
  is_valid: z.boolean().optional(),
  modified_at: z.string(),
  name: z.string(),
  version_id: z.string(),
});

const GetGatewayUrlSchema = z.string();

const GetAigConfigFetchGatewaySchema = z.object({
  authentication: z.boolean().optional(),
  cache_invalidate_on_update: z.boolean(),
  cache_ttl: z.number().int().min(0).nullable(),
  collect_logs: z.boolean(),
  created_at: z.string(),
  dlp: z.union([
    z.object({
      action: z.enum(["BLOCK", "FLAG"]),
      enabled: z.boolean(),
      profiles: z.array(z.string()),
    }),
    z.object({
      enabled: z.boolean(),
      policies: z.array(z.object({
        action: z.enum(["FLAG", "BLOCK"]),
        check: z.array(z.enum(["REQUEST", "RESPONSE"])),
        enabled: z.boolean(),
        id: z.string(),
        profiles: z.array(z.string()),
      })),
    }),
  ]).optional(),
  guardrails: z.object({
    prompt: z.object({
      P1: z.enum(["FLAG", "BLOCK"]).optional(),
      S1: z.enum(["FLAG", "BLOCK"]).optional(),
      S10: z.enum(["FLAG", "BLOCK"]).optional(),
      S11: z.enum(["FLAG", "BLOCK"]).optional(),
      S12: z.enum(["FLAG", "BLOCK"]).optional(),
      S13: z.enum(["FLAG", "BLOCK"]).optional(),
      S2: z.enum(["FLAG", "BLOCK"]).optional(),
      S3: z.enum(["FLAG", "BLOCK"]).optional(),
      S4: z.enum(["FLAG", "BLOCK"]).optional(),
      S5: z.enum(["FLAG", "BLOCK"]).optional(),
      S6: z.enum(["FLAG", "BLOCK"]).optional(),
      S7: z.enum(["FLAG", "BLOCK"]).optional(),
      S8: z.enum(["FLAG", "BLOCK"]).optional(),
      S9: z.enum(["FLAG", "BLOCK"]).optional(),
    }),
    response: z.object({
      P1: z.enum(["FLAG", "BLOCK"]).optional(),
      S1: z.enum(["FLAG", "BLOCK"]).optional(),
      S10: z.enum(["FLAG", "BLOCK"]).optional(),
      S11: z.enum(["FLAG", "BLOCK"]).optional(),
      S12: z.enum(["FLAG", "BLOCK"]).optional(),
      S13: z.enum(["FLAG", "BLOCK"]).optional(),
      S2: z.enum(["FLAG", "BLOCK"]).optional(),
      S3: z.enum(["FLAG", "BLOCK"]).optional(),
      S4: z.enum(["FLAG", "BLOCK"]).optional(),
      S5: z.enum(["FLAG", "BLOCK"]).optional(),
      S6: z.enum(["FLAG", "BLOCK"]).optional(),
      S7: z.enum(["FLAG", "BLOCK"]).optional(),
      S8: z.enum(["FLAG", "BLOCK"]).optional(),
      S9: z.enum(["FLAG", "BLOCK"]).optional(),
    }),
  }).nullable().optional(),
  id: z.string().min(1).max(64).describe("gateway id"),
  is_default: z.boolean().optional(),
  log_management: z.number().int().min(10000).max(10000000).nullable()
    .optional(),
  log_management_strategy: z.enum(["STOP_INSERTING", "DELETE_OLDEST"])
    .nullable().optional(),
  logpush: z.boolean().optional(),
  logpush_public_key: z.string().min(16).max(1024).nullable().optional(),
  modified_at: z.string(),
  otel: z.array(z.object({
    authorization: z.string().max(256).optional(),
    content_type: z.enum(["json", "protobuf"]).optional(),
    headers: z.record(z.string(), z.string().max(4096)),
    url: z.string().max(2048),
  })).nullable().optional(),
  rate_limiting_interval: z.number().int().min(0).nullable(),
  rate_limiting_limit: z.number().int().min(0).nullable(),
  rate_limiting_technique: z.enum(["fixed", "sliding"]).nullable().optional(),
  retry_backoff: z.enum(["constant", "linear", "exponential"]).nullable()
    .optional().describe("Backoff strategy for retry delays"),
  retry_delay: z.number().int().min(0).max(5000).nullable().optional().describe(
    "Delay between retry attempts in milliseconds (0-5000)",
  ),
  retry_max_attempts: z.number().int().min(1).max(5).nullable().optional()
    .describe("Maximum number of retry attempts for failed requests (1-5)"),
  spend_limits: z.object({
    enabled: z.boolean().optional(),
    rules: z.array(z.object({
      enabled: z.boolean().optional(),
      id: z.string().min(1).optional(),
      limit: z.number().min(0),
      limitType: z.enum(["cost"]),
      metadata: z.record(
        z.string(),
        z.union([
          z.object({
            mode: z.enum(["partition"]),
          }),
          z.object({
            mode: z.enum(["filter"]),
            values: z.array(z.unknown()),
          }),
        ]),
      ).optional(),
      model: z.object({
        mode: z.enum(["filter"]),
        values: z.array(z.string()),
      }).optional(),
      provider: z.object({
        mode: z.enum(["filter"]),
        values: z.array(z.string()),
      }).optional(),
      technique: z.enum(["fixed", "sliding"]).optional(),
      window: z.number().int().min(0),
    })).optional(),
  }).nullable().optional(),
  store_id: z.string().nullable().optional(),
  stripe: z.object({
    authorization: z.string(),
    usage_events: z.array(z.object({
      payload: z.string(),
    })),
  }).nullable().optional(),
  workers_ai_billing_mode: z.enum(["postpaid"]).optional().describe(
    "Controls how Workers AI inference calls routed through this gateway are billed. Only 'postpaid' i...",
  ),
  zdr: z.boolean().optional(),
});

const InstancesItemSchema = z.object({
  ai_gateway_id: z.string().nullable().optional(),
  ai_search_model: z.union([
    z.literal("@cf/meta/llama-3.3-70b-instruct-fp8-fast"),
    z.literal("@cf/zai-org/glm-4.7-flash"),
    z.literal("@cf/meta/llama-3.1-8b-instruct-fast"),
    z.literal("@cf/meta/llama-3.1-8b-instruct-fp8"),
    z.literal("@cf/meta/llama-4-scout-17b-16e-instruct"),
    z.literal("@cf/qwen/qwen3-30b-a3b-fp8"),
    z.literal("@cf/deepseek-ai/deepseek-r1-distill-qwen-32b"),
    z.literal("@cf/moonshotai/kimi-k2-instruct"),
    z.literal("@cf/google/gemma-3-12b-it"),
    z.literal("@cf/google/gemma-4-26b-a4b-it"),
    z.literal("@cf/moonshotai/kimi-k2.5"),
    z.literal("anthropic/claude-3-7-sonnet"),
    z.literal("anthropic/claude-sonnet-4"),
    z.literal("anthropic/claude-opus-4"),
    z.literal("anthropic/claude-3-5-haiku"),
    z.literal("cerebras/qwen-3-235b-a22b-instruct"),
    z.literal("cerebras/qwen-3-235b-a22b-thinking"),
    z.literal("cerebras/llama-3.3-70b"),
    z.literal("cerebras/llama-4-maverick-17b-128e-instruct"),
    z.literal("cerebras/llama-4-scout-17b-16e-instruct"),
    z.literal("cerebras/gpt-oss-120b"),
    z.literal("google-ai-studio/gemini-2.5-flash"),
    z.literal("google-ai-studio/gemini-2.5-pro"),
    z.literal("grok/grok-4"),
    z.literal("groq/llama-3.3-70b-versatile"),
    z.literal("groq/llama-3.1-8b-instant"),
    z.literal("openai/gpt-5"),
    z.literal("openai/gpt-5-mini"),
    z.literal("openai/gpt-5-nano"),
    z.literal(""),
    z.literal(null),
  ]).nullable().optional(),
  cache: z.boolean().optional(),
  cache_threshold: z.enum([
    "super_strict_match",
    "close_enough",
    "flexible_friend",
    "anything_goes",
  ]).optional(),
  cache_ttl: z.union([
    z.union([z.literal(600)]),
    z.union([z.literal(1800)]),
    z.union([z.literal(3600)]),
    z.union([z.literal(7200)]),
    z.union([z.literal(21600)]),
    z.union([z.literal(43200)]),
    z.union([z.literal(86400)]),
    z.union([z.literal(172800)]),
    z.union([z.literal(259200)]),
    z.union([z.literal(518400)]),
  ]).optional().describe(
    "Cache entry TTL in seconds. Allowed values: 600 (10min), 1800 (30min), 3600 (1h), 7200 (2h), 2160...",
  ),
  chunk_overlap: z.number().int().min(0).max(30).optional(),
  chunk_size: z.number().int().min(64).optional(),
  created_at: z.string(),
  created_by: z.string().nullable().optional(),
  custom_metadata: z.array(z.object({
    data_type: z.enum(["text", "number", "boolean", "datetime"]),
    field_name: z.string().min(1).max(64),
  })).optional(),
  embedding_model: z.union([
    z.literal("@cf/qwen/qwen3-embedding-0.6b"),
    z.literal("@cf/qwen/qwen3-vl-embedding-2b"),
    z.literal("@cf/baai/bge-m3"),
    z.literal("@cf/baai/bge-large-en-v1.5"),
    z.literal("@cf/google/embeddinggemma-300m"),
    z.literal("google-ai-studio/gemini-embedding-001"),
    z.literal("google-ai-studio/gemini-embedding-2-preview"),
    z.literal("google-ai-studio/gemini-embedding-2"),
    z.literal("openai/text-embedding-3-small"),
    z.literal("openai/text-embedding-3-large"),
    z.literal(""),
    z.literal(null),
  ]).nullable().optional(),
  enable: z.boolean().optional(),
  engine_version: z.number().optional(),
  fusion_method: z.enum(["max", "rrf"]).optional(),
  hybrid_search_enabled: z.boolean().optional().describe(
    "Deprecated — use index_method instead.",
  ),
  id: z.string().min(1).max(64).describe(
    "AI Search instance ID. Lowercase alphanumeric, hyphens, and underscores.",
  ),
  index_method: z.object({
    keyword: z.boolean(),
    vector: z.boolean(),
  }).optional().describe(
    "Controls which storage backends are used during indexing. Defaults to vector-only.",
  ),
  indexing_options: z.object({
    keyword_tokenizer: z.enum(["porter", "trigram"]).optional(),
  }).nullable().optional(),
  last_activity: z.string().nullable().optional(),
  max_num_results: z.number().int().min(1).max(50).optional(),
  metadata: z.object({
    created_from_aisearch_wizard: z.boolean().optional(),
    worker_domain: z.string().optional(),
  }).optional(),
  modified_at: z.string(),
  modified_by: z.string().nullable().optional(),
  namespace: z.string().nullable().optional(),
  paused: z.boolean().optional(),
  public_endpoint_id: z.string().nullable().optional(),
  public_endpoint_params: z.object({
    authorized_hosts: z.array(z.string()).optional(),
    chat_completions_endpoint: z.object({
      disabled: z.boolean().optional(),
    }).optional(),
    custom_domains: z.array(z.string().min(1).max(253)).nullable().optional(),
    enabled: z.boolean().optional(),
    mcp: z.object({
      description: z.string().optional(),
      disabled: z.boolean().optional(),
    }).optional(),
    rate_limit: z.object({
      period_ms: z.number().int().min(60000).max(3600000).optional(),
      requests: z.number().int().min(1).optional(),
      technique: z.enum(["fixed", "sliding"]).optional(),
    }).optional(),
    search_endpoint: z.object({
      disabled: z.boolean().optional(),
    }).optional(),
  }).optional(),
  reranking: z.boolean().optional(),
  reranking_model: z.union([
    z.literal("@cf/baai/bge-reranker-base"),
    z.literal(""),
    z.literal(null),
  ]).nullable().optional(),
  retrieval_options: z.object({
    boost_by: z.array(z.object({
      direction: z.enum(["asc", "desc", "exists", "not_exists"]).optional(),
      field: z.string().min(1).max(64),
    })).optional(),
    keyword_match_mode: z.enum(["and", "or"]).optional(),
  }).nullable().optional(),
  rewrite_model: z.union([
    z.literal("@cf/meta/llama-3.3-70b-instruct-fp8-fast"),
    z.literal("@cf/zai-org/glm-4.7-flash"),
    z.literal("@cf/meta/llama-3.1-8b-instruct-fast"),
    z.literal("@cf/meta/llama-3.1-8b-instruct-fp8"),
    z.literal("@cf/meta/llama-4-scout-17b-16e-instruct"),
    z.literal("@cf/qwen/qwen3-30b-a3b-fp8"),
    z.literal("@cf/deepseek-ai/deepseek-r1-distill-qwen-32b"),
    z.literal("@cf/moonshotai/kimi-k2-instruct"),
    z.literal("@cf/google/gemma-3-12b-it"),
    z.literal("@cf/google/gemma-4-26b-a4b-it"),
    z.literal("@cf/moonshotai/kimi-k2.5"),
    z.literal("anthropic/claude-3-7-sonnet"),
    z.literal("anthropic/claude-sonnet-4"),
    z.literal("anthropic/claude-opus-4"),
    z.literal("anthropic/claude-3-5-haiku"),
    z.literal("cerebras/qwen-3-235b-a22b-instruct"),
    z.literal("cerebras/qwen-3-235b-a22b-thinking"),
    z.literal("cerebras/llama-3.3-70b"),
    z.literal("cerebras/llama-4-maverick-17b-128e-instruct"),
    z.literal("cerebras/llama-4-scout-17b-16e-instruct"),
    z.literal("cerebras/gpt-oss-120b"),
    z.literal("google-ai-studio/gemini-2.5-flash"),
    z.literal("google-ai-studio/gemini-2.5-pro"),
    z.literal("grok/grok-4"),
    z.literal("groq/llama-3.3-70b-versatile"),
    z.literal("groq/llama-3.1-8b-instant"),
    z.literal("openai/gpt-5"),
    z.literal("openai/gpt-5-mini"),
    z.literal("openai/gpt-5-nano"),
    z.literal(""),
    z.literal(null),
  ]).nullable().optional(),
  rewrite_query: z.boolean().optional(),
  score_threshold: z.number().min(0).max(1).optional(),
  source: z.string().nullable().optional(),
  source_params: z.object({
    exclude_items: z.array(z.string().max(512)).optional(),
    include_items: z.array(z.string().max(512)).optional(),
    prefix: z.string().optional(),
    r2_jurisdiction: z.string().optional(),
    web_crawler: z.object({
      parse_options: z.object({
        content_selector: z.array(z.object({
          path: z.string().min(1).max(200),
          selector: z.string().min(1).max(200),
        })).optional(),
        include_headers: z.record(z.string(), z.string().max(8192)).optional(),
        include_images: z.boolean().optional(),
        specific_sitemaps: z.array(z.string()).optional(),
        use_browser_rendering: z.boolean().optional(),
      }).optional(),
      parse_type: z.enum(["sitemap", "crawl"]).optional(),
    }).optional(),
  }).nullable().optional(),
  status: z.string().optional(),
  sync_interval: z.union([
    z.union([z.literal(900)]),
    z.union([z.literal(1800)]),
    z.union([z.literal(3600)]),
    z.union([z.literal(7200)]),
    z.union([z.literal(14400)]),
    z.union([z.literal(21600)]),
    z.union([z.literal(43200)]),
    z.union([z.literal(86400)]),
  ]).optional().describe(
    "Interval between automatic syncs, in seconds. Allowed values: 900 (15min), 1800 (30min), 3600 (1h...",
  ),
  token_id: z.string().optional(),
  type: z.union([z.literal("r2"), z.literal("web-crawler"), z.literal(null)])
    .nullable().optional(),
});

const ListInstancesSchema = z.object({
  items: z.array(InstancesItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateInstanceSchema = z.object({
  ai_gateway_id: z.string().nullable().optional(),
  ai_search_model: z.union([
    z.literal("@cf/meta/llama-3.3-70b-instruct-fp8-fast"),
    z.literal("@cf/zai-org/glm-4.7-flash"),
    z.literal("@cf/meta/llama-3.1-8b-instruct-fast"),
    z.literal("@cf/meta/llama-3.1-8b-instruct-fp8"),
    z.literal("@cf/meta/llama-4-scout-17b-16e-instruct"),
    z.literal("@cf/qwen/qwen3-30b-a3b-fp8"),
    z.literal("@cf/deepseek-ai/deepseek-r1-distill-qwen-32b"),
    z.literal("@cf/moonshotai/kimi-k2-instruct"),
    z.literal("@cf/google/gemma-3-12b-it"),
    z.literal("@cf/google/gemma-4-26b-a4b-it"),
    z.literal("@cf/moonshotai/kimi-k2.5"),
    z.literal("anthropic/claude-3-7-sonnet"),
    z.literal("anthropic/claude-sonnet-4"),
    z.literal("anthropic/claude-opus-4"),
    z.literal("anthropic/claude-3-5-haiku"),
    z.literal("cerebras/qwen-3-235b-a22b-instruct"),
    z.literal("cerebras/qwen-3-235b-a22b-thinking"),
    z.literal("cerebras/llama-3.3-70b"),
    z.literal("cerebras/llama-4-maverick-17b-128e-instruct"),
    z.literal("cerebras/llama-4-scout-17b-16e-instruct"),
    z.literal("cerebras/gpt-oss-120b"),
    z.literal("google-ai-studio/gemini-2.5-flash"),
    z.literal("google-ai-studio/gemini-2.5-pro"),
    z.literal("grok/grok-4"),
    z.literal("groq/llama-3.3-70b-versatile"),
    z.literal("groq/llama-3.1-8b-instant"),
    z.literal("openai/gpt-5"),
    z.literal("openai/gpt-5-mini"),
    z.literal("openai/gpt-5-nano"),
    z.literal(""),
    z.literal(null),
  ]).nullable().optional(),
  cache: z.boolean().optional(),
  cache_threshold: z.enum([
    "super_strict_match",
    "close_enough",
    "flexible_friend",
    "anything_goes",
  ]).optional(),
  cache_ttl: z.union([
    z.union([z.literal(600)]),
    z.union([z.literal(1800)]),
    z.union([z.literal(3600)]),
    z.union([z.literal(7200)]),
    z.union([z.literal(21600)]),
    z.union([z.literal(43200)]),
    z.union([z.literal(86400)]),
    z.union([z.literal(172800)]),
    z.union([z.literal(259200)]),
    z.union([z.literal(518400)]),
  ]).optional().describe(
    "Cache entry TTL in seconds. Allowed values: 600 (10min), 1800 (30min), 3600 (1h), 7200 (2h), 2160...",
  ),
  chunk_overlap: z.number().int().min(0).max(30).optional(),
  chunk_size: z.number().int().min(64).optional(),
  created_at: z.string(),
  created_by: z.string().nullable().optional(),
  custom_metadata: z.array(z.object({
    data_type: z.enum(["text", "number", "boolean", "datetime"]),
    field_name: z.string().min(1).max(64),
  })).optional(),
  embedding_model: z.union([
    z.literal("@cf/qwen/qwen3-embedding-0.6b"),
    z.literal("@cf/qwen/qwen3-vl-embedding-2b"),
    z.literal("@cf/baai/bge-m3"),
    z.literal("@cf/baai/bge-large-en-v1.5"),
    z.literal("@cf/google/embeddinggemma-300m"),
    z.literal("google-ai-studio/gemini-embedding-001"),
    z.literal("google-ai-studio/gemini-embedding-2-preview"),
    z.literal("google-ai-studio/gemini-embedding-2"),
    z.literal("openai/text-embedding-3-small"),
    z.literal("openai/text-embedding-3-large"),
    z.literal(""),
    z.literal(null),
  ]).nullable().optional(),
  enable: z.boolean().optional(),
  engine_version: z.number().optional(),
  fusion_method: z.enum(["max", "rrf"]).optional(),
  hybrid_search_enabled: z.boolean().optional().describe(
    "Deprecated — use index_method instead.",
  ),
  id: z.string().min(1).max(64).describe(
    "AI Search instance ID. Lowercase alphanumeric, hyphens, and underscores.",
  ),
  index_method: z.object({
    keyword: z.boolean(),
    vector: z.boolean(),
  }).optional().describe(
    "Controls which storage backends are used during indexing. Defaults to vector-only.",
  ),
  indexing_options: z.object({
    keyword_tokenizer: z.enum(["porter", "trigram"]).optional(),
  }).nullable().optional(),
  last_activity: z.string().nullable().optional(),
  max_num_results: z.number().int().min(1).max(50).optional(),
  metadata: z.object({
    created_from_aisearch_wizard: z.boolean().optional(),
    worker_domain: z.string().optional(),
  }).optional(),
  modified_at: z.string(),
  modified_by: z.string().nullable().optional(),
  namespace: z.string().nullable().optional(),
  paused: z.boolean().optional(),
  public_endpoint_id: z.string().nullable().optional(),
  public_endpoint_params: z.object({
    authorized_hosts: z.array(z.string()).optional(),
    chat_completions_endpoint: z.object({
      disabled: z.boolean().optional(),
    }).optional(),
    custom_domains: z.array(z.string().min(1).max(253)).nullable().optional(),
    enabled: z.boolean().optional(),
    mcp: z.object({
      description: z.string().optional(),
      disabled: z.boolean().optional(),
    }).optional(),
    rate_limit: z.object({
      period_ms: z.number().int().min(60000).max(3600000).optional(),
      requests: z.number().int().min(1).optional(),
      technique: z.enum(["fixed", "sliding"]).optional(),
    }).optional(),
    search_endpoint: z.object({
      disabled: z.boolean().optional(),
    }).optional(),
  }).optional(),
  reranking: z.boolean().optional(),
  reranking_model: z.union([
    z.literal("@cf/baai/bge-reranker-base"),
    z.literal(""),
    z.literal(null),
  ]).nullable().optional(),
  retrieval_options: z.object({
    boost_by: z.array(z.object({
      direction: z.enum(["asc", "desc", "exists", "not_exists"]).optional(),
      field: z.string().min(1).max(64),
    })).optional(),
    keyword_match_mode: z.enum(["and", "or"]).optional(),
  }).nullable().optional(),
  rewrite_model: z.union([
    z.literal("@cf/meta/llama-3.3-70b-instruct-fp8-fast"),
    z.literal("@cf/zai-org/glm-4.7-flash"),
    z.literal("@cf/meta/llama-3.1-8b-instruct-fast"),
    z.literal("@cf/meta/llama-3.1-8b-instruct-fp8"),
    z.literal("@cf/meta/llama-4-scout-17b-16e-instruct"),
    z.literal("@cf/qwen/qwen3-30b-a3b-fp8"),
    z.literal("@cf/deepseek-ai/deepseek-r1-distill-qwen-32b"),
    z.literal("@cf/moonshotai/kimi-k2-instruct"),
    z.literal("@cf/google/gemma-3-12b-it"),
    z.literal("@cf/google/gemma-4-26b-a4b-it"),
    z.literal("@cf/moonshotai/kimi-k2.5"),
    z.literal("anthropic/claude-3-7-sonnet"),
    z.literal("anthropic/claude-sonnet-4"),
    z.literal("anthropic/claude-opus-4"),
    z.literal("anthropic/claude-3-5-haiku"),
    z.literal("cerebras/qwen-3-235b-a22b-instruct"),
    z.literal("cerebras/qwen-3-235b-a22b-thinking"),
    z.literal("cerebras/llama-3.3-70b"),
    z.literal("cerebras/llama-4-maverick-17b-128e-instruct"),
    z.literal("cerebras/llama-4-scout-17b-16e-instruct"),
    z.literal("cerebras/gpt-oss-120b"),
    z.literal("google-ai-studio/gemini-2.5-flash"),
    z.literal("google-ai-studio/gemini-2.5-pro"),
    z.literal("grok/grok-4"),
    z.literal("groq/llama-3.3-70b-versatile"),
    z.literal("groq/llama-3.1-8b-instant"),
    z.literal("openai/gpt-5"),
    z.literal("openai/gpt-5-mini"),
    z.literal("openai/gpt-5-nano"),
    z.literal(""),
    z.literal(null),
  ]).nullable().optional(),
  rewrite_query: z.boolean().optional(),
  score_threshold: z.number().min(0).max(1).optional(),
  source: z.string().nullable().optional(),
  source_params: z.object({
    exclude_items: z.array(z.string().max(512)).optional(),
    include_items: z.array(z.string().max(512)).optional(),
    prefix: z.string().optional(),
    r2_jurisdiction: z.string().optional(),
    web_crawler: z.object({
      parse_options: z.object({
        content_selector: z.array(z.object({
          path: z.string().min(1).max(200),
          selector: z.string().min(1).max(200),
        })).optional(),
        include_headers: z.record(z.string(), z.string().max(8192)).optional(),
        include_images: z.boolean().optional(),
        specific_sitemaps: z.array(z.string()).optional(),
        use_browser_rendering: z.boolean().optional(),
      }).optional(),
      parse_type: z.enum(["sitemap", "crawl"]).optional(),
    }).optional(),
  }).nullable().optional(),
  status: z.string().optional(),
  sync_interval: z.union([
    z.union([z.literal(900)]),
    z.union([z.literal(1800)]),
    z.union([z.literal(3600)]),
    z.union([z.literal(7200)]),
    z.union([z.literal(14400)]),
    z.union([z.literal(21600)]),
    z.union([z.literal(43200)]),
    z.union([z.literal(86400)]),
  ]).optional().describe(
    "Interval between automatic syncs, in seconds. Allowed values: 900 (15min), 1800 (30min), 3600 (1h...",
  ),
  token_id: z.string().optional(),
  type: z.union([z.literal("r2"), z.literal("web-crawler"), z.literal(null)])
    .nullable().optional(),
});

const GetAiSearchFetchInstanceSchema = z.object({
  ai_gateway_id: z.string().nullable().optional(),
  ai_search_model: z.union([
    z.literal("@cf/meta/llama-3.3-70b-instruct-fp8-fast"),
    z.literal("@cf/zai-org/glm-4.7-flash"),
    z.literal("@cf/meta/llama-3.1-8b-instruct-fast"),
    z.literal("@cf/meta/llama-3.1-8b-instruct-fp8"),
    z.literal("@cf/meta/llama-4-scout-17b-16e-instruct"),
    z.literal("@cf/qwen/qwen3-30b-a3b-fp8"),
    z.literal("@cf/deepseek-ai/deepseek-r1-distill-qwen-32b"),
    z.literal("@cf/moonshotai/kimi-k2-instruct"),
    z.literal("@cf/google/gemma-3-12b-it"),
    z.literal("@cf/google/gemma-4-26b-a4b-it"),
    z.literal("@cf/moonshotai/kimi-k2.5"),
    z.literal("anthropic/claude-3-7-sonnet"),
    z.literal("anthropic/claude-sonnet-4"),
    z.literal("anthropic/claude-opus-4"),
    z.literal("anthropic/claude-3-5-haiku"),
    z.literal("cerebras/qwen-3-235b-a22b-instruct"),
    z.literal("cerebras/qwen-3-235b-a22b-thinking"),
    z.literal("cerebras/llama-3.3-70b"),
    z.literal("cerebras/llama-4-maverick-17b-128e-instruct"),
    z.literal("cerebras/llama-4-scout-17b-16e-instruct"),
    z.literal("cerebras/gpt-oss-120b"),
    z.literal("google-ai-studio/gemini-2.5-flash"),
    z.literal("google-ai-studio/gemini-2.5-pro"),
    z.literal("grok/grok-4"),
    z.literal("groq/llama-3.3-70b-versatile"),
    z.literal("groq/llama-3.1-8b-instant"),
    z.literal("openai/gpt-5"),
    z.literal("openai/gpt-5-mini"),
    z.literal("openai/gpt-5-nano"),
    z.literal(""),
    z.literal(null),
  ]).nullable().optional(),
  cache: z.boolean().optional(),
  cache_threshold: z.enum([
    "super_strict_match",
    "close_enough",
    "flexible_friend",
    "anything_goes",
  ]).optional(),
  cache_ttl: z.union([
    z.union([z.literal(600)]),
    z.union([z.literal(1800)]),
    z.union([z.literal(3600)]),
    z.union([z.literal(7200)]),
    z.union([z.literal(21600)]),
    z.union([z.literal(43200)]),
    z.union([z.literal(86400)]),
    z.union([z.literal(172800)]),
    z.union([z.literal(259200)]),
    z.union([z.literal(518400)]),
  ]).optional().describe(
    "Cache entry TTL in seconds. Allowed values: 600 (10min), 1800 (30min), 3600 (1h), 7200 (2h), 2160...",
  ),
  chunk_overlap: z.number().int().min(0).max(30).optional(),
  chunk_size: z.number().int().min(64).optional(),
  created_at: z.string(),
  created_by: z.string().nullable().optional(),
  custom_metadata: z.array(z.object({
    data_type: z.enum(["text", "number", "boolean", "datetime"]),
    field_name: z.string().min(1).max(64),
  })).optional(),
  embedding_model: z.union([
    z.literal("@cf/qwen/qwen3-embedding-0.6b"),
    z.literal("@cf/qwen/qwen3-vl-embedding-2b"),
    z.literal("@cf/baai/bge-m3"),
    z.literal("@cf/baai/bge-large-en-v1.5"),
    z.literal("@cf/google/embeddinggemma-300m"),
    z.literal("google-ai-studio/gemini-embedding-001"),
    z.literal("google-ai-studio/gemini-embedding-2-preview"),
    z.literal("google-ai-studio/gemini-embedding-2"),
    z.literal("openai/text-embedding-3-small"),
    z.literal("openai/text-embedding-3-large"),
    z.literal(""),
    z.literal(null),
  ]).nullable().optional(),
  enable: z.boolean().optional(),
  engine_version: z.number().optional(),
  fusion_method: z.enum(["max", "rrf"]).optional(),
  hybrid_search_enabled: z.boolean().optional().describe(
    "Deprecated — use index_method instead.",
  ),
  id: z.string().min(1).max(64).describe(
    "AI Search instance ID. Lowercase alphanumeric, hyphens, and underscores.",
  ),
  index_method: z.object({
    keyword: z.boolean(),
    vector: z.boolean(),
  }).optional().describe(
    "Controls which storage backends are used during indexing. Defaults to vector-only.",
  ),
  indexing_options: z.object({
    keyword_tokenizer: z.enum(["porter", "trigram"]).optional(),
  }).nullable().optional(),
  last_activity: z.string().nullable().optional(),
  max_num_results: z.number().int().min(1).max(50).optional(),
  metadata: z.object({
    created_from_aisearch_wizard: z.boolean().optional(),
    worker_domain: z.string().optional(),
  }).optional(),
  modified_at: z.string(),
  modified_by: z.string().nullable().optional(),
  namespace: z.string().nullable().optional(),
  paused: z.boolean().optional(),
  public_endpoint_id: z.string().nullable().optional(),
  public_endpoint_params: z.object({
    authorized_hosts: z.array(z.string()).optional(),
    chat_completions_endpoint: z.object({
      disabled: z.boolean().optional(),
    }).optional(),
    custom_domains: z.array(z.string().min(1).max(253)).nullable().optional(),
    enabled: z.boolean().optional(),
    mcp: z.object({
      description: z.string().optional(),
      disabled: z.boolean().optional(),
    }).optional(),
    rate_limit: z.object({
      period_ms: z.number().int().min(60000).max(3600000).optional(),
      requests: z.number().int().min(1).optional(),
      technique: z.enum(["fixed", "sliding"]).optional(),
    }).optional(),
    search_endpoint: z.object({
      disabled: z.boolean().optional(),
    }).optional(),
  }).optional(),
  reranking: z.boolean().optional(),
  reranking_model: z.union([
    z.literal("@cf/baai/bge-reranker-base"),
    z.literal(""),
    z.literal(null),
  ]).nullable().optional(),
  retrieval_options: z.object({
    boost_by: z.array(z.object({
      direction: z.enum(["asc", "desc", "exists", "not_exists"]).optional(),
      field: z.string().min(1).max(64),
    })).optional(),
    keyword_match_mode: z.enum(["and", "or"]).optional(),
  }).nullable().optional(),
  rewrite_model: z.union([
    z.literal("@cf/meta/llama-3.3-70b-instruct-fp8-fast"),
    z.literal("@cf/zai-org/glm-4.7-flash"),
    z.literal("@cf/meta/llama-3.1-8b-instruct-fast"),
    z.literal("@cf/meta/llama-3.1-8b-instruct-fp8"),
    z.literal("@cf/meta/llama-4-scout-17b-16e-instruct"),
    z.literal("@cf/qwen/qwen3-30b-a3b-fp8"),
    z.literal("@cf/deepseek-ai/deepseek-r1-distill-qwen-32b"),
    z.literal("@cf/moonshotai/kimi-k2-instruct"),
    z.literal("@cf/google/gemma-3-12b-it"),
    z.literal("@cf/google/gemma-4-26b-a4b-it"),
    z.literal("@cf/moonshotai/kimi-k2.5"),
    z.literal("anthropic/claude-3-7-sonnet"),
    z.literal("anthropic/claude-sonnet-4"),
    z.literal("anthropic/claude-opus-4"),
    z.literal("anthropic/claude-3-5-haiku"),
    z.literal("cerebras/qwen-3-235b-a22b-instruct"),
    z.literal("cerebras/qwen-3-235b-a22b-thinking"),
    z.literal("cerebras/llama-3.3-70b"),
    z.literal("cerebras/llama-4-maverick-17b-128e-instruct"),
    z.literal("cerebras/llama-4-scout-17b-16e-instruct"),
    z.literal("cerebras/gpt-oss-120b"),
    z.literal("google-ai-studio/gemini-2.5-flash"),
    z.literal("google-ai-studio/gemini-2.5-pro"),
    z.literal("grok/grok-4"),
    z.literal("groq/llama-3.3-70b-versatile"),
    z.literal("groq/llama-3.1-8b-instant"),
    z.literal("openai/gpt-5"),
    z.literal("openai/gpt-5-mini"),
    z.literal("openai/gpt-5-nano"),
    z.literal(""),
    z.literal(null),
  ]).nullable().optional(),
  rewrite_query: z.boolean().optional(),
  score_threshold: z.number().min(0).max(1).optional(),
  source: z.string().nullable().optional(),
  source_params: z.object({
    exclude_items: z.array(z.string().max(512)).optional(),
    include_items: z.array(z.string().max(512)).optional(),
    prefix: z.string().optional(),
    r2_jurisdiction: z.string().optional(),
    web_crawler: z.object({
      parse_options: z.object({
        content_selector: z.array(z.object({
          path: z.string().min(1).max(200),
          selector: z.string().min(1).max(200),
        })).optional(),
        include_headers: z.record(z.string(), z.string().max(8192)).optional(),
        include_images: z.boolean().optional(),
        specific_sitemaps: z.array(z.string()).optional(),
        use_browser_rendering: z.boolean().optional(),
      }).optional(),
      parse_type: z.enum(["sitemap", "crawl"]).optional(),
    }).optional(),
  }).nullable().optional(),
  status: z.string().optional(),
  sync_interval: z.union([
    z.union([z.literal(900)]),
    z.union([z.literal(1800)]),
    z.union([z.literal(3600)]),
    z.union([z.literal(7200)]),
    z.union([z.literal(14400)]),
    z.union([z.literal(21600)]),
    z.union([z.literal(43200)]),
    z.union([z.literal(86400)]),
  ]).optional().describe(
    "Interval between automatic syncs, in seconds. Allowed values: 900 (15min), 1800 (30min), 3600 (1h...",
  ),
  token_id: z.string().optional(),
  type: z.union([z.literal("r2"), z.literal("web-crawler"), z.literal(null)])
    .nullable().optional(),
});

const CreateAiSearchInstanceChatCompletionSchema = z.object({
  choices: z.array(z.object({
    index: z.number().int().optional(),
    message: z.object({
      content: z.union([
        z.string(),
        z.array(z.union([
          z.object({
            text: z.unknown(),
            type: z.unknown(),
          }),
          z.object({
            image_url: z.unknown(),
            type: z.unknown(),
          }),
        ])),
        z.unknown().nullable(),
      ]),
      role: z.enum(["system", "developer", "user", "assistant", "tool"]),
    }),
  })),
  chunks: z.array(z.object({
    id: z.string(),
    item: z.object({
      key: z.string(),
      metadata: z.record(z.string(), z.unknown()).optional(),
      timestamp: z.number().optional(),
    }).optional(),
    score: z.number().min(0).max(1),
    scoring_details: z.object({
      fusion_method: z.enum(["rrf", "max"]).optional(),
      keyword_rank: z.number().optional(),
      keyword_score: z.number().min(0).optional(),
      reranking_score: z.number().min(0).max(1).optional(),
      vector_rank: z.number().optional(),
      vector_score: z.number().min(0).max(1).optional(),
    }).optional(),
    text: z.string(),
    type: z.string(),
  })),
  id: z.string().optional(),
  model: z.string().optional(),
  object: z.string().optional(),
});

const JobsItemSchema = z.object({
  description: z.string().optional(),
  end_reason: z.string().optional(),
  ended_at: z.string().optional(),
  id: z.string(),
  last_seen_at: z.string().optional(),
  source: z.enum(["user", "schedule"]),
  started_at: z.string().optional(),
});

const ListJobsSchema = z.object({
  items: z.array(JobsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateJobSchema = z.object({
  description: z.string().optional(),
  end_reason: z.string().optional(),
  ended_at: z.string().optional(),
  id: z.string(),
  last_seen_at: z.string().optional(),
  source: z.enum(["user", "schedule"]),
  started_at: z.string().optional(),
});

const UpdateAiSearchInstanceChangeJobStatusSchema = z.object({
  description: z.string().optional(),
  end_reason: z.string().optional(),
  ended_at: z.string().optional(),
  id: z.string(),
  last_seen_at: z.string().optional(),
  source: z.enum(["user", "schedule"]),
  started_at: z.string().optional(),
});

const JobLogsItemSchema = z.object({
  created_at: z.number(),
  id: z.number().int(),
  message: z.string(),
  message_type: z.number().int(),
});

const ListJobLogsSchema = z.object({
  items: z.array(JobLogsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateAiSearchInstanceSearchSchema = z.object({
  chunks: z.array(z.object({
    id: z.string(),
    item: z.object({
      key: z.string(),
      metadata: z.record(z.string(), z.unknown()).optional(),
      timestamp: z.number().optional(),
    }).optional(),
    score: z.number().min(0).max(1),
    scoring_details: z.object({
      fusion_method: z.enum(["rrf", "max"]).optional(),
      keyword_rank: z.number().optional(),
      keyword_score: z.number().min(0).optional(),
      reranking_score: z.number().min(0).max(1).optional(),
      vector_rank: z.number().optional(),
      vector_score: z.number().min(0).max(1).optional(),
    }).optional(),
    text: z.string(),
    type: z.string(),
  })),
  query_kind: z.enum(["text", "image", "multimodal"]),
  search_query: z.string().optional(),
});

const GetAiSearchStatsSchema = z.object({
  completed: z.number().int().optional(),
  degraded: z.boolean().optional().describe(
    "True when status counts are unavailable (e.g. legacy stats query exceeded D1 statement-size limit...",
  ),
  engine: z.object({
    r2: z.object({
      metadataSizeBytes: z.number().int(),
      objectCount: z.number().int(),
      payloadSizeBytes: z.number().int(),
    }).optional(),
    vectorize: z.object({
      dimensions: z.number().int(),
      vectorsCount: z.number().int(),
    }).optional(),
  }).optional().describe(
    "Engine-specific metadata. Present only for managed (v3) instances.",
  ),
  error: z.number().int().optional(),
  file_embed_errors: z.record(z.string(), z.unknown()).optional(),
  index_source_errors: z.record(z.string(), z.unknown()).optional(),
  last_activity: z.string().optional(),
  outdated: z.number().int().optional(),
  queued: z.number().int().optional(),
  running: z.number().int().optional(),
  skipped: z.number().int().optional(),
});

const NamespacesItemSchema = z.object({
  created_at: z.string(),
  description: z.string().max(256).nullable().optional().describe(
    "Optional description for the namespace. Max 256 characters.",
  ),
  name: z.string(),
});

const ListNamespacesSchema = z.object({
  items: z.array(NamespacesItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateNamespaceSchema = z.object({
  created_at: z.string(),
  description: z.string().max(256).nullable().optional().describe(
    "Optional description for the namespace. Max 256 characters.",
  ),
  name: z.string(),
});

const GetAiSearchFetchNamespaceSchema = z.object({
  created_at: z.string(),
  description: z.string().max(256).nullable().optional().describe(
    "Optional description for the namespace. Max 256 characters.",
  ),
  name: z.string(),
});

const CreateAiSearchNamespaceMultiInstanceChatCompletionSchema = z.object({
  choices: z.array(z.object({
    index: z.number().int().optional(),
    message: z.object({
      content: z.union([
        z.string(),
        z.array(z.union([
          z.object({
            text: z.unknown(),
            type: z.unknown(),
          }),
          z.object({
            image_url: z.unknown(),
            type: z.unknown(),
          }),
        ])),
        z.unknown().nullable(),
      ]),
      role: z.enum(["system", "developer", "user", "assistant", "tool"]),
    }),
  })),
  chunks: z.array(z.object({
    id: z.string(),
    instance_id: z.string(),
    item: z.object({
      key: z.string(),
      metadata: z.record(z.string(), z.unknown()).optional(),
      timestamp: z.number().optional(),
    }).optional(),
    score: z.number().min(0).max(1),
    scoring_details: z.object({
      fusion_method: z.enum(["rrf", "max"]).optional(),
      keyword_rank: z.number().optional(),
      keyword_score: z.number().min(0).optional(),
      reranking_score: z.number().min(0).max(1).optional(),
      vector_rank: z.number().optional(),
      vector_score: z.number().min(0).max(1).optional(),
    }).optional(),
    text: z.string(),
    type: z.string(),
  })),
  errors: z.array(z.object({
    instance_id: z.string(),
    message: z.string(),
  })).optional(),
  id: z.string().optional(),
  model: z.string().optional(),
  object: z.string().optional(),
});

const GetAiSearchNamespaceFetchInstanceSchema = z.object({
  ai_gateway_id: z.string().nullable().optional(),
  ai_search_model: z.union([
    z.literal("@cf/meta/llama-3.3-70b-instruct-fp8-fast"),
    z.literal("@cf/zai-org/glm-4.7-flash"),
    z.literal("@cf/meta/llama-3.1-8b-instruct-fast"),
    z.literal("@cf/meta/llama-3.1-8b-instruct-fp8"),
    z.literal("@cf/meta/llama-4-scout-17b-16e-instruct"),
    z.literal("@cf/qwen/qwen3-30b-a3b-fp8"),
    z.literal("@cf/deepseek-ai/deepseek-r1-distill-qwen-32b"),
    z.literal("@cf/moonshotai/kimi-k2-instruct"),
    z.literal("@cf/google/gemma-3-12b-it"),
    z.literal("@cf/google/gemma-4-26b-a4b-it"),
    z.literal("@cf/moonshotai/kimi-k2.5"),
    z.literal("anthropic/claude-3-7-sonnet"),
    z.literal("anthropic/claude-sonnet-4"),
    z.literal("anthropic/claude-opus-4"),
    z.literal("anthropic/claude-3-5-haiku"),
    z.literal("cerebras/qwen-3-235b-a22b-instruct"),
    z.literal("cerebras/qwen-3-235b-a22b-thinking"),
    z.literal("cerebras/llama-3.3-70b"),
    z.literal("cerebras/llama-4-maverick-17b-128e-instruct"),
    z.literal("cerebras/llama-4-scout-17b-16e-instruct"),
    z.literal("cerebras/gpt-oss-120b"),
    z.literal("google-ai-studio/gemini-2.5-flash"),
    z.literal("google-ai-studio/gemini-2.5-pro"),
    z.literal("grok/grok-4"),
    z.literal("groq/llama-3.3-70b-versatile"),
    z.literal("groq/llama-3.1-8b-instant"),
    z.literal("openai/gpt-5"),
    z.literal("openai/gpt-5-mini"),
    z.literal("openai/gpt-5-nano"),
    z.literal(""),
    z.literal(null),
  ]).nullable().optional(),
  cache: z.boolean().optional(),
  cache_threshold: z.enum([
    "super_strict_match",
    "close_enough",
    "flexible_friend",
    "anything_goes",
  ]).optional(),
  cache_ttl: z.union([
    z.union([z.literal(600)]),
    z.union([z.literal(1800)]),
    z.union([z.literal(3600)]),
    z.union([z.literal(7200)]),
    z.union([z.literal(21600)]),
    z.union([z.literal(43200)]),
    z.union([z.literal(86400)]),
    z.union([z.literal(172800)]),
    z.union([z.literal(259200)]),
    z.union([z.literal(518400)]),
  ]).optional().describe(
    "Cache entry TTL in seconds. Allowed values: 600 (10min), 1800 (30min), 3600 (1h), 7200 (2h), 2160...",
  ),
  chunk_overlap: z.number().int().min(0).max(30).optional(),
  chunk_size: z.number().int().min(64).optional(),
  created_at: z.string(),
  created_by: z.string().nullable().optional(),
  custom_metadata: z.array(z.object({
    data_type: z.enum(["text", "number", "boolean", "datetime"]),
    field_name: z.string().min(1).max(64),
  })).optional(),
  embedding_model: z.union([
    z.literal("@cf/qwen/qwen3-embedding-0.6b"),
    z.literal("@cf/qwen/qwen3-vl-embedding-2b"),
    z.literal("@cf/baai/bge-m3"),
    z.literal("@cf/baai/bge-large-en-v1.5"),
    z.literal("@cf/google/embeddinggemma-300m"),
    z.literal("google-ai-studio/gemini-embedding-001"),
    z.literal("google-ai-studio/gemini-embedding-2-preview"),
    z.literal("google-ai-studio/gemini-embedding-2"),
    z.literal("openai/text-embedding-3-small"),
    z.literal("openai/text-embedding-3-large"),
    z.literal(""),
    z.literal(null),
  ]).nullable().optional(),
  enable: z.boolean().optional(),
  engine_version: z.number().optional(),
  fusion_method: z.enum(["max", "rrf"]).optional(),
  hybrid_search_enabled: z.boolean().optional().describe(
    "Deprecated — use index_method instead.",
  ),
  id: z.string().min(1).max(64).describe(
    "AI Search instance ID. Lowercase alphanumeric, hyphens, and underscores.",
  ),
  index_method: z.object({
    keyword: z.boolean(),
    vector: z.boolean(),
  }).optional().describe(
    "Controls which storage backends are used during indexing. Defaults to vector-only.",
  ),
  indexing_options: z.object({
    keyword_tokenizer: z.enum(["porter", "trigram"]).optional(),
  }).nullable().optional(),
  last_activity: z.string().nullable().optional(),
  max_num_results: z.number().int().min(1).max(50).optional(),
  metadata: z.object({
    created_from_aisearch_wizard: z.boolean().optional(),
    worker_domain: z.string().optional(),
  }).optional(),
  modified_at: z.string(),
  modified_by: z.string().nullable().optional(),
  namespace: z.string().nullable().optional(),
  paused: z.boolean().optional(),
  public_endpoint_id: z.string().nullable().optional(),
  public_endpoint_params: z.object({
    authorized_hosts: z.array(z.string()).optional(),
    chat_completions_endpoint: z.object({
      disabled: z.boolean().optional(),
    }).optional(),
    custom_domains: z.array(z.string().min(1).max(253)).nullable().optional(),
    enabled: z.boolean().optional(),
    mcp: z.object({
      description: z.string().optional(),
      disabled: z.boolean().optional(),
    }).optional(),
    rate_limit: z.object({
      period_ms: z.number().int().min(60000).max(3600000).optional(),
      requests: z.number().int().min(1).optional(),
      technique: z.enum(["fixed", "sliding"]).optional(),
    }).optional(),
    search_endpoint: z.object({
      disabled: z.boolean().optional(),
    }).optional(),
  }).optional(),
  reranking: z.boolean().optional(),
  reranking_model: z.union([
    z.literal("@cf/baai/bge-reranker-base"),
    z.literal(""),
    z.literal(null),
  ]).nullable().optional(),
  retrieval_options: z.object({
    boost_by: z.array(z.object({
      direction: z.enum(["asc", "desc", "exists", "not_exists"]).optional(),
      field: z.string().min(1).max(64),
    })).optional(),
    keyword_match_mode: z.enum(["and", "or"]).optional(),
  }).nullable().optional(),
  rewrite_model: z.union([
    z.literal("@cf/meta/llama-3.3-70b-instruct-fp8-fast"),
    z.literal("@cf/zai-org/glm-4.7-flash"),
    z.literal("@cf/meta/llama-3.1-8b-instruct-fast"),
    z.literal("@cf/meta/llama-3.1-8b-instruct-fp8"),
    z.literal("@cf/meta/llama-4-scout-17b-16e-instruct"),
    z.literal("@cf/qwen/qwen3-30b-a3b-fp8"),
    z.literal("@cf/deepseek-ai/deepseek-r1-distill-qwen-32b"),
    z.literal("@cf/moonshotai/kimi-k2-instruct"),
    z.literal("@cf/google/gemma-3-12b-it"),
    z.literal("@cf/google/gemma-4-26b-a4b-it"),
    z.literal("@cf/moonshotai/kimi-k2.5"),
    z.literal("anthropic/claude-3-7-sonnet"),
    z.literal("anthropic/claude-sonnet-4"),
    z.literal("anthropic/claude-opus-4"),
    z.literal("anthropic/claude-3-5-haiku"),
    z.literal("cerebras/qwen-3-235b-a22b-instruct"),
    z.literal("cerebras/qwen-3-235b-a22b-thinking"),
    z.literal("cerebras/llama-3.3-70b"),
    z.literal("cerebras/llama-4-maverick-17b-128e-instruct"),
    z.literal("cerebras/llama-4-scout-17b-16e-instruct"),
    z.literal("cerebras/gpt-oss-120b"),
    z.literal("google-ai-studio/gemini-2.5-flash"),
    z.literal("google-ai-studio/gemini-2.5-pro"),
    z.literal("grok/grok-4"),
    z.literal("groq/llama-3.3-70b-versatile"),
    z.literal("groq/llama-3.1-8b-instant"),
    z.literal("openai/gpt-5"),
    z.literal("openai/gpt-5-mini"),
    z.literal("openai/gpt-5-nano"),
    z.literal(""),
    z.literal(null),
  ]).nullable().optional(),
  rewrite_query: z.boolean().optional(),
  score_threshold: z.number().min(0).max(1).optional(),
  source: z.string().nullable().optional(),
  source_params: z.object({
    exclude_items: z.array(z.string().max(512)).optional(),
    include_items: z.array(z.string().max(512)).optional(),
    prefix: z.string().optional(),
    r2_jurisdiction: z.string().optional(),
    web_crawler: z.object({
      parse_options: z.object({
        content_selector: z.array(z.object({
          path: z.string().min(1).max(200),
          selector: z.string().min(1).max(200),
        })).optional(),
        include_headers: z.record(z.string(), z.string().max(8192)).optional(),
        include_images: z.boolean().optional(),
        specific_sitemaps: z.array(z.string()).optional(),
        use_browser_rendering: z.boolean().optional(),
      }).optional(),
      parse_type: z.enum(["sitemap", "crawl"]).optional(),
    }).optional(),
  }).nullable().optional(),
  status: z.string().optional(),
  sync_interval: z.union([
    z.union([z.literal(900)]),
    z.union([z.literal(1800)]),
    z.union([z.literal(3600)]),
    z.union([z.literal(7200)]),
    z.union([z.literal(14400)]),
    z.union([z.literal(21600)]),
    z.union([z.literal(43200)]),
    z.union([z.literal(86400)]),
  ]).optional().describe(
    "Interval between automatic syncs, in seconds. Allowed values: 900 (15min), 1800 (30min), 3600 (1h...",
  ),
  token_id: z.string().optional(),
  type: z.union([z.literal("r2"), z.literal("web-crawler"), z.literal(null)])
    .nullable().optional(),
});

const UpdateAiSearchMoveInstanceSchema = z.object({});

const CreateAiSearchNamespaceInstanceChatCompletionSchema = z.object({
  choices: z.array(z.object({
    index: z.number().int().optional(),
    message: z.object({
      content: z.union([
        z.string(),
        z.array(z.union([
          z.object({
            text: z.unknown(),
            type: z.unknown(),
          }),
          z.object({
            image_url: z.unknown(),
            type: z.unknown(),
          }),
        ])),
        z.unknown().nullable(),
      ]),
      role: z.enum(["system", "developer", "user", "assistant", "tool"]),
    }),
  })),
  chunks: z.array(z.object({
    id: z.string(),
    item: z.object({
      key: z.string(),
      metadata: z.record(z.string(), z.unknown()).optional(),
      timestamp: z.number().optional(),
    }).optional(),
    score: z.number().min(0).max(1),
    scoring_details: z.object({
      fusion_method: z.enum(["rrf", "max"]).optional(),
      keyword_rank: z.number().optional(),
      keyword_score: z.number().min(0).optional(),
      reranking_score: z.number().min(0).max(1).optional(),
      vector_rank: z.number().optional(),
      vector_score: z.number().min(0).max(1).optional(),
    }).optional(),
    text: z.string(),
    type: z.string(),
  })),
  id: z.string().optional(),
  model: z.string().optional(),
  object: z.string().optional(),
});

const ItemsItemSchema = z.object({
  checksum: z.string(),
  chunks_count: z.number().int().nullable(),
  created_at: z.string(),
  error: z.string().optional(),
  file_size: z.number().nullable(),
  id: z.string(),
  key: z.string(),
  last_seen_at: z.string(),
  namespace: z.string(),
  next_action: z.union([
    z.literal("INDEX"),
    z.literal("DELETE"),
    z.literal(null),
  ]).nullable(),
  source_id: z.string().nullable().describe(
    'Identifies which data source this item belongs to. "builtin" for uploaded files, "{type}:{source}...',
  ),
  status: z.enum([
    "queued",
    "running",
    "completed",
    "error",
    "skipped",
    "outdated",
  ]),
});

const ListItemsSchema = z.object({
  items: z.array(ItemsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const AiSearchNamespaceInstanceUploadItemSchema = z.object({
  checksum: z.string(),
  chunks_count: z.number().int().nullable(),
  created_at: z.string(),
  error: z.string().optional(),
  file_size: z.number().nullable(),
  id: z.string(),
  key: z.string(),
  last_seen_at: z.string(),
  namespace: z.string(),
  next_action: z.union([
    z.literal("INDEX"),
    z.literal("DELETE"),
    z.literal(null),
  ]).nullable(),
  source_id: z.string().nullable().describe(
    'Identifies which data source this item belongs to. "builtin" for uploaded files, "{type}:{source}...',
  ),
  status: z.enum([
    "queued",
    "running",
    "completed",
    "error",
    "skipped",
    "outdated",
  ]),
});

const CreateOrUpdateItemSchema = z.object({
  checksum: z.string(),
  chunks_count: z.number().int().nullable(),
  created_at: z.string(),
  error: z.string().optional(),
  file_size: z.number().nullable(),
  id: z.string(),
  key: z.string(),
  last_seen_at: z.string(),
  namespace: z.string(),
  next_action: z.union([
    z.literal("INDEX"),
    z.literal("DELETE"),
    z.literal(null),
  ]).nullable(),
  source_id: z.string().nullable().describe(
    'Identifies which data source this item belongs to. "builtin" for uploaded files, "{type}:{source}...',
  ),
  status: z.enum([
    "queued",
    "running",
    "completed",
    "error",
    "skipped",
    "outdated",
  ]),
});

const GetItemSchema = z.object({
  checksum: z.string(),
  chunks_count: z.number().int().nullable(),
  created_at: z.string(),
  error: z.string().optional(),
  file_size: z.number().nullable(),
  id: z.string(),
  key: z.string(),
  last_seen_at: z.string(),
  namespace: z.string(),
  next_action: z.union([
    z.literal("INDEX"),
    z.literal("DELETE"),
    z.literal(null),
  ]).nullable(),
  source_id: z.string().nullable().describe(
    'Identifies which data source this item belongs to. "builtin" for uploaded files, "{type}:{source}...',
  ),
  status: z.enum([
    "queued",
    "running",
    "completed",
    "error",
    "skipped",
    "outdated",
  ]),
});

const UpdateAiSearchNamespaceInstanceSyncItemSchema = z.object({
  checksum: z.string(),
  chunks_count: z.number().int().nullable(),
  created_at: z.string(),
  error: z.string().optional(),
  file_size: z.number().nullable(),
  id: z.string(),
  key: z.string(),
  last_seen_at: z.string(),
  namespace: z.string(),
  next_action: z.union([
    z.literal("INDEX"),
    z.literal("DELETE"),
    z.literal(null),
  ]).nullable(),
  source_id: z.string().nullable().describe(
    'Identifies which data source this item belongs to. "builtin" for uploaded files, "{type}:{source}...',
  ),
  status: z.enum([
    "queued",
    "running",
    "completed",
    "error",
    "skipped",
    "outdated",
  ]),
});

const ItemChunksItemSchema = z.object({
  end_byte: z.number().optional(),
  id: z.string(),
  item: z.object({
    key: z.string(),
    metadata: z.record(z.string(), z.unknown()).optional(),
    timestamp: z.number().optional(),
  }),
  start_byte: z.number().optional(),
  text: z.string(),
});

const ListItemChunksSchema = z.object({
  items: z.array(ItemChunksItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const AiSearchNamespaceInstanceLogsItemItemSchema = z.object({
  action: z.string(),
  chunkCount: z.number().int().nullable(),
  errorType: z.string().nullable(),
  fileKey: z.string(),
  message: z.string().nullable(),
  processingTimeMs: z.number().int().nullable(),
  timestamp: z.string(),
});

const ListAiSearchNamespaceInstanceLogsItemSchema = z.object({
  items: z.array(AiSearchNamespaceInstanceLogsItemItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const UpdateAiSearchNamespaceInstanceChangeJobStatusSchema = z.object({
  description: z.string().optional(),
  end_reason: z.string().optional(),
  ended_at: z.string().optional(),
  id: z.string(),
  last_seen_at: z.string().optional(),
  source: z.enum(["user", "schedule"]),
  started_at: z.string().optional(),
});

const CreateAiSearchNamespaceInstanceSearchSchema = z.object({
  chunks: z.array(z.object({
    id: z.string(),
    item: z.object({
      key: z.string(),
      metadata: z.record(z.string(), z.unknown()).optional(),
      timestamp: z.number().optional(),
    }).optional(),
    score: z.number().min(0).max(1),
    scoring_details: z.object({
      fusion_method: z.enum(["rrf", "max"]).optional(),
      keyword_rank: z.number().optional(),
      keyword_score: z.number().min(0).optional(),
      reranking_score: z.number().min(0).max(1).optional(),
      vector_rank: z.number().optional(),
      vector_score: z.number().min(0).max(1).optional(),
    }).optional(),
    text: z.string(),
    type: z.string(),
  })),
  query_kind: z.enum(["text", "image", "multimodal"]),
  search_query: z.string().optional(),
});

const GetAiSearchNamespaceStatsSchema = z.object({
  completed: z.number().int().optional(),
  degraded: z.boolean().optional().describe(
    "True when status counts are unavailable (e.g. legacy stats query exceeded D1 statement-size limit...",
  ),
  engine: z.object({
    r2: z.object({
      metadataSizeBytes: z.number().int(),
      objectCount: z.number().int(),
      payloadSizeBytes: z.number().int(),
    }).optional(),
    vectorize: z.object({
      dimensions: z.number().int(),
      vectorsCount: z.number().int(),
    }).optional(),
  }).optional().describe(
    "Engine-specific metadata. Present only for managed (v3) instances.",
  ),
  error: z.number().int().optional(),
  file_embed_errors: z.record(z.string(), z.unknown()).optional(),
  index_source_errors: z.record(z.string(), z.unknown()).optional(),
  last_activity: z.string().optional(),
  outdated: z.number().int().optional(),
  queued: z.number().int().optional(),
  running: z.number().int().optional(),
  skipped: z.number().int().optional(),
});

const CreateAiSearchNamespaceMultiInstanceSearchSchema = z.object({
  chunks: z.array(z.object({
    id: z.string(),
    instance_id: z.string(),
    item: z.object({
      key: z.string(),
      metadata: z.record(z.string(), z.unknown()).optional(),
      timestamp: z.number().optional(),
    }).optional(),
    score: z.number().min(0).max(1),
    scoring_details: z.object({
      fusion_method: z.enum(["rrf", "max"]).optional(),
      keyword_rank: z.number().optional(),
      keyword_score: z.number().min(0).optional(),
      reranking_score: z.number().min(0).max(1).optional(),
      vector_rank: z.number().optional(),
      vector_score: z.number().min(0).max(1).optional(),
    }).optional(),
    text: z.string(),
    type: z.string(),
  })),
  errors: z.array(z.object({
    instance_id: z.string(),
    message: z.string(),
  })).optional(),
  query_kind: z.enum(["text", "image", "multimodal"]),
  search_query: z.string().optional(),
});

const TokensItemSchema = z.object({
  cf_api_id: z.string(),
  created_at: z.string(),
  created_by: z.string().nullable().optional(),
  enabled: z.boolean().optional(),
  id: z.string(),
  legacy: z.boolean().optional(),
  modified_at: z.string(),
  modified_by: z.string().nullable().optional(),
  name: z.string(),
});

const ListTokensSchema = z.object({
  items: z.array(TokensItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetAiSearchFetchTokensSchema = z.object({
  cf_api_id: z.string(),
  created_at: z.string(),
  created_by: z.string().nullable().optional(),
  enabled: z.boolean().optional(),
  id: z.string(),
  legacy: z.boolean().optional(),
  modified_at: z.string(),
  modified_by: z.string().nullable().optional(),
  name: z.string(),
});

const WorkersAiSearchAuthorItemSchema = z.object({});

const ListWorkersAiSearchAuthorSchema = z.object({
  items: z.array(WorkersAiSearchAuthorItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const ListFinetunesSchema = z.object({
  created_at: z.string(),
  description: z.string().optional(),
  id: z.string(),
  model: z.string(),
  modified_at: z.string(),
  name: z.string(),
});

const CreateFinetuneSchema = z.object({
  created_at: z.string(),
  description: z.string().optional(),
  id: z.string(),
  model: z.string(),
  modified_at: z.string(),
  name: z.string(),
  public: z.boolean(),
});

const PublicFinetunesItemSchema = z.object({
  created_at: z.string(),
  description: z.string().optional(),
  id: z.string(),
  model: z.string(),
  modified_at: z.string(),
  name: z.string(),
  public: z.boolean(),
});

const ListPublicFinetunesSchema = z.object({
  items: z.array(PublicFinetunesItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const WorkersAiUploadFinetuneAssetSchema = z.object({
  success: z.boolean(),
});

const GetModelSchemaSchema = z.object({
  input: z.object({
    additionalProperties: z.boolean(),
    description: z.string(),
    type: z.string(),
  }),
  output: z.object({
    additionalProperties: z.boolean(),
    description: z.string(),
    type: z.string(),
  }),
});

const GetWorkersAiSearchModelSchema = z.union([
  z.object({
    errors: z.array(z.object({})),
    messages: z.array(z.string()),
    result: z.array(z.object({})),
    success: z.boolean(),
  }),
  z.object({
    data: z.array(z.object({})),
  }),
]);

const CreateWorkersAiPostRunGenericSchema = z.object({});

const CreateWorkersAiPostRunCfAi4bharatIndictrans2EnIndic1bSchema = z.object(
  {},
);

const CreateWorkersAiPostRunCfAi4bharatNonomniIndictrans2EnIndic1bSchema = z
  .object({});

const WorkersAiPostRunCfAisingaporeGemmaSeaLionV427bItSchema = z.object({});

const WorkersAiPostRunCfBaaiBgeBaseEnV15Schema = z.object({});

const WorkersAiPostRunCfBaaiBgeLargeEnV15Schema = z.object({});

const WorkersAiPostRunCfBaaiBgeM3Schema = z.object({});

const CreateWorkersAiPostRunCfBaaiBgeRerankerBaseSchema = z.object({});

const WorkersAiPostRunCfBaaiBgeSmallEnV15Schema = z.object({});

const WorkersAiPostRunCfBaaiNonomniBgeBaseEnV15Schema = z.object({});

const WorkersAiPostRunCfBaaiNonomniBgeLargeEnV15Schema = z.object({});

const WorkersAiPostRunCfBaaiNonomniBgeM3Schema = z.object({});

const WorkersAiPostRunCfBaaiNonomniBgeSmallEnV15Schema = z.object({});

const CreateWorkersAiPostRunCfBlackForestLabsFlux1SchnellSchema = z.object({});

const CreateWorkersAiPostRunCfBlackForestLabsFlux2DevSchema = z.object({});

const CreateWorkersAiPostRunCfBlackForestLabsFlux2Klein4bSchema = z.object({});

const CreateWorkersAiPostRunCfBlackForestLabsFlux2Klein9bSchema = z.object({});

const CreateWorkersAiPostRunCfBytedanceStableDiffusionXlLightningSchema = z
  .object({});

const CreateWorkersAiPostRunCfDeepgramAura1Schema = z.object({});

const CreateWorkersAiPostRunCfDeepgramAura2EnSchema = z.object({});

const CreateWorkersAiPostRunCfDeepgramAura2EsSchema = z.object({});

const CreateWorkersAiPostRunCfDeepgramFluxSchema = z.object({});

const CreateWorkersAiPostRunCfDeepgramNova3Schema = z.object({});

const WorkersAiPostRunCfDeepseekAiDeepseekMath7bInstructSchema = z.object({});

const WorkersAiPostRunCfDeepseekAiDeepseekR1DistillQwen32bSchema = z.object({});

const WorkersAiPostRunCfDefogSqlcoder7b2Schema = z.object({});

const CreateWorkersAiPostRunCfFacebookBartLargeCnnSchema = z.object({});

const CreateWorkersAiPostRunCfFacebookNonomniBartLargeCnnSchema = z.object({});

const WorkersAiPostRunCfFacebookNonomniDetrResnet50Schema = z.object({});

const WorkersAiPostRunCfFblgitUnaCybertron7bV2Bf16Schema = z.object({});

const CreateWorkersAiPostRunCfGoogleEmbeddinggemma300mSchema = z.object({});

const WorkersAiPostRunCfGoogleGemma2bItLoraSchema = z.object({});

const WorkersAiPostRunCfGoogleGemma312bItSchema = z.object({});

const WorkersAiPostRunCfGoogleGemma426bA4bItSchema = z.object({});

const WorkersAiPostRunCfGoogleGemma7bItLoraSchema = z.object({});

const CreateWorkersAiPostRunCfGoogleNonomniEmbeddinggemma300mSchema = z.object(
  {},
);

const CreateWorkersAiPostRunCfHuggingfaceDistilbertSst2Int8Schema = z.object(
  {},
);

const CreateWorkersAiPostRunCfHuggingfaceNonomniDistilbertSst2Int8Schema = z
  .object({});

const WorkersAiPostRunCfIbmGraniteGranite40HMicroSchema = z.object({});

const CreateWorkersAiPostRunCfLeonardoLucidOriginSchema = z.object({});

const CreateWorkersAiPostRunCfLeonardoPhoenix10Schema = z.object({});

const CreateWorkersAiPostRunCfLykonDreamshaper8LcmSchema = z.object({});

const WorkersAiPostRunCfMetaLlamaLlama27bChatHfLoraSchema = z.object({});

const WorkersAiPostRunCfMetaLlama27bChatFp16Schema = z.object({});

const WorkersAiPostRunCfMetaLlama27bChatInt8Schema = z.object({});

const WorkersAiPostRunCfMetaLlama38bInstructSchema = z.object({});

const WorkersAiPostRunCfMetaLlama38bInstructAwqSchema = z.object({});

const WorkersAiPostRunCfMetaLlama3170bInstructFp8FastSchema = z.object({});

const WorkersAiPostRunCfMetaLlama318bInstructAwqSchema = z.object({});

const WorkersAiPostRunCfMetaLlama318bInstructFp8Schema = z.object({});

const WorkersAiPostRunCfMetaLlama318bInstructFp8FastSchema = z.object({});

const WorkersAiPostRunCfMetaLlama3211bVisionInstructSchema = z.object({});

const WorkersAiPostRunCfMetaLlama321bInstructSchema = z.object({});

const WorkersAiPostRunCfMetaLlama323bInstructSchema = z.object({});

const WorkersAiPostRunCfMetaLlama3370bInstructFp8FastSchema = z.object({});

const WorkersAiPostRunCfMetaLlama4Scout17b16eInstructSchema = z.object({});

const CreateWorkersAiPostRunCfMetaLlamaGuard38bSchema = z.object({});

const WorkersAiPostRunCfMetaM2m10012bSchema = z.object({});

const WorkersAiPostRunCfMicrosoftNonomniResnet50Schema = z.object({});

const WorkersAiPostRunCfMicrosoftPhi2Schema = z.object({});

const WorkersAiPostRunCfMicrosoftResnet50Schema = z.object({});

const WorkersAiPostRunCfMistralMistral7bInstructV01Schema = z.object({});

const WorkersAiPostRunCfMistralMistral7bInstructV02LoraSchema = z.object({});

const WorkersAiPostRunCfMistralaiMistralSmall3124bInstructSchema = z.object({});

const WorkersAiPostRunCfMoonshotaiKimiK25Schema = z.object({});

const WorkersAiPostRunCfMoonshotaiKimiK26Schema = z.object({});

const WorkersAiPostRunCfMoonshotaiKimiK27CodeSchema = z.object({});

const CreateWorkersAiPostRunCfMyshellAiMelottsSchema = z.object({});

const WorkersAiPostRunCfNvidiaNemotron3120bA12bSchema = z.object({});

const WorkersAiPostRunCfNvidiaNemotronSpeechStreamingEn06bSchema = z.object({});

const WorkersAiPostRunCfOpenaiGptOss120bSchema = z.object({});

const WorkersAiPostRunCfOpenaiGptOss20bSchema = z.object({});

const WorkersAiPostRunCfOpenaiWhisperSchema = z.object({});

const CreateWorkersAiPostRunCfOpenaiWhisperLargeV3TurboSchema = z.object({});

const WorkersAiPostRunCfOpenaiWhisperTinyEnSchema = z.object({});

const WorkersAiPostRunCfOpenchatOpenchat350106Schema = z.object({});

const CreateWorkersAiPostRunCfPfnetPlamoEmbedding1bSchema = z.object({});

const WorkersAiPostRunCfQwenQwen1505bChatSchema = z.object({});

const WorkersAiPostRunCfQwenQwen1518bChatSchema = z.object({});

const WorkersAiPostRunCfQwenQwen1514bChatAwqSchema = z.object({});

const WorkersAiPostRunCfQwenQwen157bChatAwqSchema = z.object({});

const WorkersAiPostRunCfQwenQwen25Coder32bInstructSchema = z.object({});

const WorkersAiPostRunCfQwenQwen330bA3bFp8Schema = z.object({});

const CreateWorkersAiPostRunCfQwenQwen3Embedding06bSchema = z.object({});

const WorkersAiPostRunCfQwenQwq32bSchema = z.object({});

const CreateWorkersAiPostRunCfRunwaymlStableDiffusionV15Img2imgSchema = z
  .object({});

const CreateWorkersAiPostRunCfRunwaymlStableDiffusionV15InpaintingSchema = z
  .object({});

const CreateWorkersAiPostRunCfStabilityaiStableDiffusionXlBase10Schema = z
  .object({});

const WorkersAiPostRunCfTheblokeDiscolmGerman7bV1AwqSchema = z.object({});

const WorkersAiPostRunCfTiiuaeFalcon7bInstructSchema = z.object({});

const WorkersAiPostRunCfTinyllamaTinyllama11bChatV10Schema = z.object({});

const WorkersAiPostRunCfZaiOrgGlm47FlashSchema = z.object({});

const WorkersAiPostRunCfZaiOrgGlm52Schema = z.object({});

const WorkersAiPostRunHfGoogleGemma7bItSchema = z.object({});

const WorkersAiPostRunHfMistralMistral7bInstructV02Schema = z.object({});

const WorkersAiPostRunHfNexusflowStarlingLm7bBetaSchema = z.object({});

const WorkersAiPostRunHfNousresearchHermes2ProMistral7bSchema = z.object({});

const WorkersAiPostRunHfTheblokeDeepseekCoder67bBaseAwqSchema = z.object({});

const WorkersAiPostRunHfTheblokeDeepseekCoder67bInstructAwqSchema = z.object(
  {},
);

const WorkersAiPostRunHfTheblokeLlama213bChatAwqSchema = z.object({});

const WorkersAiPostRunHfTheblokeMistral7bInstructV01AwqSchema = z.object({});

const WorkersAiPostRunHfTheblokeNeuralChat7bV31AwqSchema = z.object({});

const WorkersAiPostRunHfTheblokeOpenhermes25Mistral7bAwqSchema = z.object({});

const WorkersAiPostRunHfTheblokeZephyr7bBetaAwqSchema = z.object({});

const WorkersAiPostRunModelSchema = z.union([
  z.array(z.object({
    label: z.string().optional(),
    score: z.number().optional(),
  })),
  z.string(),
  z.union([
    z.object({
      audio: z.string().optional(),
    }),
    z.string(),
  ]),
  z.object({
    data: z.array(z.array(z.number())).optional(),
    shape: z.array(z.number()).optional(),
  }),
  z.object({
    text: z.string(),
    vtt: z.string().optional(),
    word_count: z.number().optional(),
    words: z.array(z.object({
      end: z.number().optional(),
      start: z.number().optional(),
      word: z.string().optional(),
    })).optional(),
  }),
  z.array(z.object({
    label: z.string().optional(),
    score: z.number().optional(),
  })),
  z.array(z.object({
    box: z.object({
      xmax: z.number().optional(),
      xmin: z.number().optional(),
      ymax: z.number().optional(),
      ymin: z.number().optional(),
    }).optional(),
    label: z.string().optional(),
    score: z.number().optional(),
  })),
  z.union([
    z.object({
      response: z.string(),
      tool_calls: z.array(z.object({
        arguments: z.object({}).optional(),
        name: z.string().optional(),
      })).optional(),
      usage: z.object({
        completion_tokens: z.number().optional(),
        prompt_tokens: z.number().optional(),
        total_tokens: z.number().optional(),
      }).optional(),
    }),
    z.string(),
  ]),
  z.object({
    translated_text: z.string().optional(),
  }),
  z.object({
    summary: z.string().optional(),
  }),
  z.object({
    description: z.string().optional(),
  }),
  z.object({
    description: z.string().optional(),
  }),
  z.object({
    data: z.array(z.array(z.number())).optional(),
    shape: z.array(z.number()).optional(),
  }),
]);

const WorkersAiSearchTaskItemSchema = z.object({});

const ListWorkersAiSearchTaskSchema = z.object({
  items: z.array(WorkersAiSearchTaskItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const WorkersAiPostToMarkdownSchema = z.object({
  data: z.string(),
  format: z.string(),
  mimeType: z.string(),
  name: z.string(),
  tokens: z.string(),
});

const GetToMarkdownSupportedItemSchema = z.object({
  extension: z.string(),
  mimeType: z.string(),
});

const GetToMarkdownSupportedSchema = z.object({
  items: z.array(GetToMarkdownSupportedItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

// =============================================================================
// Model Definition
// =============================================================================

/** Cloudflare Workers AI — model inference, fine-tuning, LoRA adapters */
export const model = {
  type: "@webframp/cloudflare/workers-ai",
  version: "2026.07.19.1",
  globalArguments: GlobalArgsSchema,

  upgrades: [],

  resources: {
    "credit_balance": {
      description: "Get credit balance",
      schema: GetCreditBalanceSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "invoice_history": {
      description: "Get invoice history",
      schema: GetInvoiceHistorySchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "invoice_preview": {
      description: "Get invoice preview",
      schema: GetInvoicePreviewSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "spending_limit": {
      description: "Get spending limit",
      schema: GetSpendingLimitSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "topup": {
      description: "Create a top-up",
      schema: CreateTopupSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "topup_config": {
      description: "Get auto top-up configuration",
      schema: GetTopupConfigSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "aig_billing_set_topup_config": {
      description: "Set auto top-up configuration",
      schema: CreateAigBillingSetTopupConfigSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "topup_limits": {
      description: "Get account top-up limits",
      schema: GetTopupLimitsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "aig_billing_check_topup_status": {
      description: "Check top-up status",
      schema: CreateAigBillingCheckTopupStatusSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "usage_history": {
      description: "Get usage history",
      schema: GetUsageHistorySchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "account_provider": {
      description: "List Account Providers",
      schema: ListAccountProviderSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "account_provider_cost": {
      description: "List Account Provider Costs",
      schema: ListAccountProviderCostSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "aig_config_fetch_account_provider_cost": {
      description: "Fetch a Account Provider Cost",
      schema: GetAigConfigFetchAccountProviderCostSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "aig_config_fetch_account_provider": {
      description: "Fetch a Account Provider",
      schema: GetAigConfigFetchAccountProviderSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "evaluators": {
      description: "List Evaluators",
      schema: ListEvaluatorsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "gateway": {
      description: "List Gateways",
      schema: ListGatewaySchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "dataset": {
      description: "List Datasets",
      schema: ListDatasetSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "aig_config_fetch_dataset": {
      description: "Fetch a Dataset",
      schema: GetAigConfigFetchDatasetSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "evaluations": {
      description: "List Evaluations",
      schema: ListEvaluationsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "aig_config_fetch_evaluations": {
      description: "Fetch a Evaluation",
      schema: GetAigConfigFetchEvaluationsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "gateway_logs": {
      description: "List Gateway Logs",
      schema: ListGatewayLogsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "gateway_log_detail": {
      description: "Get Gateway Log Detail",
      schema: GetGatewayLogDetailSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "patch_gateway_log": {
      description: "Patch Gateway Log",
      schema: PatchGatewayLogSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "gateway_log_request": {
      description: "Get Gateway Log Request",
      schema: GetGatewayLogRequestSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "gateway_log_response": {
      description: "Get Gateway Log Response",
      schema: GetGatewayLogResponseSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "providers": {
      description: "List Provider Configs",
      schema: ListProvidersSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "list_gateway_dynamic_routes": {
      description: "List all AI Gateway Dynamic Routes.",
      schema: ListGatewayDynamicRoutesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "aig_config_post_gateway_dynamic_route": {
      description: "Create a new AI Gateway Dynamic Route.",
      schema: CreateAigConfigPostGatewayDynamicRouteSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "gateway_dynamic_route": {
      description: "Get an AI Gateway Dynamic Route.",
      schema: GetGatewayDynamicRouteSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "list_gateway_dynamic_route_deployments": {
      description: "List all AI Gateway Dynamic Route Deployments.",
      schema: ListGatewayDynamicRouteDeploymentsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "aig_config_post_gateway_dynamic_route_deployment": {
      description: "Create a new AI Gateway Dynamic Route Deployment.",
      schema: CreateAigConfigPostGatewayDynamicRouteDeploymentSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "list_gateway_dynamic_route_versions": {
      description: "List all AI Gateway Dynamic Route Versions.",
      schema: ListGatewayDynamicRouteVersionsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "aig_config_post_gateway_dynamic_route_version": {
      description: "Create a new AI Gateway Dynamic Route Version.",
      schema: CreateAigConfigPostGatewayDynamicRouteVersionSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "gateway_dynamic_route_version": {
      description: "Get an AI Gateway Dynamic Route Version.",
      schema: GetGatewayDynamicRouteVersionSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "gateway_url": {
      description: "Get Gateway URL",
      schema: GetGatewayUrlSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "aig_config_fetch_gateway": {
      description: "Fetch a Gateway",
      schema: GetAigConfigFetchGatewaySchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "instances": {
      description: "List AI Search instances.",
      schema: ListInstancesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "instance": {
      description: "Create an AI Search instance.",
      schema: CreateInstanceSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "ai_search_fetch_instance": {
      description: "Get an AI Search instance.",
      schema: GetAiSearchFetchInstanceSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "ai_search_instance_chat_completion": {
      description: "Chat Completions",
      schema: CreateAiSearchInstanceChatCompletionSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "jobs": {
      description: "List Jobs",
      schema: ListJobsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "job": {
      description: "Create new job",
      schema: CreateJobSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "ai_search_instance_change_job_status": {
      description: "Cancel an indexing job.",
      schema: UpdateAiSearchInstanceChangeJobStatusSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "job_logs": {
      description: "List Job Logs",
      schema: ListJobLogsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "ai_search_instance_search": {
      description: "Search",
      schema: CreateAiSearchInstanceSearchSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "ai_search_stats": {
      description: "Get instance statistics.",
      schema: GetAiSearchStatsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "namespaces": {
      description: "List namespaces.",
      schema: ListNamespacesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "namespace": {
      description: "Create namespace.",
      schema: CreateNamespaceSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "ai_search_fetch_namespace": {
      description: "Read namespace.",
      schema: GetAiSearchFetchNamespaceSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "ai_search_namespace_multi_instance_chat_completion": {
      description: "Multi-Instance Chat Completions",
      schema: CreateAiSearchNamespaceMultiInstanceChatCompletionSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "ai_search_namespace_fetch_instance": {
      description: "Get an AI Search instance.",
      schema: GetAiSearchNamespaceFetchInstanceSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "ai_search_move_instance": {
      description: "Move an instance to a different namespace.",
      schema: UpdateAiSearchMoveInstanceSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "ai_search_namespace_instance_chat_completion": {
      description: "Chat Completions",
      schema: CreateAiSearchNamespaceInstanceChatCompletionSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "items": {
      description: "Items List.",
      schema: ListItemsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "ai_search_namespace_instance_upload_item": {
      description: "Upload Item.",
      schema: AiSearchNamespaceInstanceUploadItemSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "or_update_item": {
      description: "Create or Update Item.",
      schema: CreateOrUpdateItemSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "item": {
      description: "Get Item.",
      schema: GetItemSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "ai_search_namespace_instance_sync_item": {
      description: "Sync Item.",
      schema: UpdateAiSearchNamespaceInstanceSyncItemSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "item_chunks": {
      description: "List Item Chunks.",
      schema: ListItemChunksSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "item_content": {
      description: "Download Item Content.",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "ai_search_namespace_instance_logs_item": {
      description: "Item Logs.",
      schema: ListAiSearchNamespaceInstanceLogsItemSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "ai_search_namespace_instance_change_job_status": {
      description: "Cancel an indexing job.",
      schema: UpdateAiSearchNamespaceInstanceChangeJobStatusSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "ai_search_namespace_instance_search": {
      description: "Search",
      schema: CreateAiSearchNamespaceInstanceSearchSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "ai_search_namespace_stats": {
      description: "Get instance statistics.",
      schema: GetAiSearchNamespaceStatsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "ai_search_namespace_multi_instance_search": {
      description: "Multi-Instance Search",
      schema: CreateAiSearchNamespaceMultiInstanceSearchSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "tokens": {
      description: "List tokens.",
      schema: ListTokensSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "ai_search_fetch_tokens": {
      description: "Read token.",
      schema: GetAiSearchFetchTokensSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_search_author": {
      description: "Author Search",
      schema: ListWorkersAiSearchAuthorSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "list_finetunes": {
      description: "List Finetunes",
      schema: ListFinetunesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "finetune": {
      description: "Create a new Finetune",
      schema: CreateFinetuneSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "public_finetunes": {
      description: "List Public Finetunes",
      schema: ListPublicFinetunesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "workers_ai_upload_finetune_asset": {
      description: "Upload a Finetune Asset",
      schema: WorkersAiUploadFinetuneAssetSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "model_schema": {
      description: "Get Model Schema",
      schema: GetModelSchemaSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_search_model": {
      description: "Model Search",
      schema: GetWorkersAiSearchModelSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_generic": {
      description: "Execute AI Model (Generic)",
      schema: CreateWorkersAiPostRunGenericSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_ai4bharat_indictrans2_en_indic_1b": {
      description: "Execute @cf/ai4bharat/indictrans2-en-indic-1B model.",
      schema: CreateWorkersAiPostRunCfAi4bharatIndictrans2EnIndic1bSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_ai4bharat_nonomni_indictrans2_en_indic_1b": {
      description:
        "Execute @cf/ai4bharat/nonomni-indictrans2-en-indic-1b model.",
      schema:
        CreateWorkersAiPostRunCfAi4bharatNonomniIndictrans2EnIndic1bSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_aisingapore_gemma_sea_lion_v4_27b_it": {
      description: "Execute @cf/aisingapore/gemma-sea-lion-v4-27b-it model.",
      schema: WorkersAiPostRunCfAisingaporeGemmaSeaLionV427bItSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_baai_bge_base_en_v1_5": {
      description: "Execute @cf/baai/bge-base-en-v1.5 model.",
      schema: WorkersAiPostRunCfBaaiBgeBaseEnV15Schema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_baai_bge_large_en_v1_5": {
      description: "Execute @cf/baai/bge-large-en-v1.5 model.",
      schema: WorkersAiPostRunCfBaaiBgeLargeEnV15Schema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_baai_bge_m3": {
      description: "Execute @cf/baai/bge-m3 model.",
      schema: WorkersAiPostRunCfBaaiBgeM3Schema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_baai_bge_reranker_base": {
      description: "Execute @cf/baai/bge-reranker-base model.",
      schema: CreateWorkersAiPostRunCfBaaiBgeRerankerBaseSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_baai_bge_small_en_v1_5": {
      description: "Execute @cf/baai/bge-small-en-v1.5 model.",
      schema: WorkersAiPostRunCfBaaiBgeSmallEnV15Schema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_baai_nonomni_bge_base_en_v1_5": {
      description: "Execute @cf/baai/nonomni-bge-base-en-v1.5 model.",
      schema: WorkersAiPostRunCfBaaiNonomniBgeBaseEnV15Schema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_baai_nonomni_bge_large_en_v1_5": {
      description: "Execute @cf/baai/nonomni-bge-large-en-v1.5 model.",
      schema: WorkersAiPostRunCfBaaiNonomniBgeLargeEnV15Schema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_baai_nonomni_bge_m3": {
      description: "Execute @cf/baai/nonomni-bge-m3 model.",
      schema: WorkersAiPostRunCfBaaiNonomniBgeM3Schema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_baai_nonomni_bge_small_en_v1_5": {
      description: "Execute @cf/baai/nonomni-bge-small-en-v1.5 model.",
      schema: WorkersAiPostRunCfBaaiNonomniBgeSmallEnV15Schema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_black_forest_labs_flux_1_schnell": {
      description: "Execute @cf/black-forest-labs/flux-1-schnell model.",
      schema: CreateWorkersAiPostRunCfBlackForestLabsFlux1SchnellSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_black_forest_labs_flux_2_dev": {
      description: "Execute @cf/black-forest-labs/flux-2-dev model.",
      schema: CreateWorkersAiPostRunCfBlackForestLabsFlux2DevSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_black_forest_labs_flux_2_klein_4b": {
      description: "Execute @cf/black-forest-labs/flux-2-klein-4b model.",
      schema: CreateWorkersAiPostRunCfBlackForestLabsFlux2Klein4bSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_black_forest_labs_flux_2_klein_9b": {
      description: "Execute @cf/black-forest-labs/flux-2-klein-9b model.",
      schema: CreateWorkersAiPostRunCfBlackForestLabsFlux2Klein9bSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_bytedance_stable_diffusion_xl_lightning": {
      description: "Execute @cf/bytedance/stable-diffusion-xl-lightning model.",
      schema: CreateWorkersAiPostRunCfBytedanceStableDiffusionXlLightningSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_websocket_run_cf_deepgram_aura": {
      description: "Open Websocket connection with @cf/deepgram/aura model.",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_websocket_run_cf_deepgram_aura_1": {
      description: "Open Websocket connection with @cf/deepgram/aura-1 model.",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_deepgram_aura_1": {
      description: "Execute @cf/deepgram/aura-1 model.",
      schema: CreateWorkersAiPostRunCfDeepgramAura1Schema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_websocket_run_cf_deepgram_aura_1_internal": {
      description:
        "Open Websocket connection with @cf/deepgram/aura-1-internal model.",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_websocket_run_cf_deepgram_aura_2": {
      description: "Open Websocket connection with @cf/deepgram/aura-2 model.",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_websocket_run_cf_deepgram_aura_2_en": {
      description:
        "Open Websocket connection with @cf/deepgram/aura-2-en model.",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_deepgram_aura_2_en": {
      description: "Execute @cf/deepgram/aura-2-en model.",
      schema: CreateWorkersAiPostRunCfDeepgramAura2EnSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_websocket_run_cf_deepgram_aura_2_en_ws": {
      description:
        "Open Websocket connection with @cf/deepgram/aura-2-en-ws model.",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_websocket_run_cf_deepgram_aura_2_es": {
      description:
        "Open Websocket connection with @cf/deepgram/aura-2-es model.",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_deepgram_aura_2_es": {
      description: "Execute @cf/deepgram/aura-2-es model.",
      schema: CreateWorkersAiPostRunCfDeepgramAura2EsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_websocket_run_cf_deepgram_flux": {
      description: "Open Websocket connection with @cf/deepgram/flux model.",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_deepgram_flux": {
      description: "Execute @cf/deepgram/flux model.",
      schema: CreateWorkersAiPostRunCfDeepgramFluxSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_websocket_run_cf_deepgram_nova_3": {
      description: "Open Websocket connection with @cf/deepgram/nova-3 model.",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_deepgram_nova_3": {
      description: "Execute @cf/deepgram/nova-3 model.",
      schema: CreateWorkersAiPostRunCfDeepgramNova3Schema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_websocket_run_cf_deepgram_nova_3_internal": {
      description:
        "Open Websocket connection with @cf/deepgram/nova-3-internal model.",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_websocket_run_cf_deepgram_nova_3_ws": {
      description:
        "Open Websocket connection with @cf/deepgram/nova-3-ws model.",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_deepseek_ai_deepseek_math_7b_instruct": {
      description: "Execute @cf/deepseek-ai/deepseek-math-7b-instruct model.",
      schema: WorkersAiPostRunCfDeepseekAiDeepseekMath7bInstructSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_deepseek_ai_deepseek_r1_distill_qwen_32b": {
      description:
        "Execute @cf/deepseek-ai/deepseek-r1-distill-qwen-32b model.",
      schema: WorkersAiPostRunCfDeepseekAiDeepseekR1DistillQwen32bSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_defog_sqlcoder_7b_2": {
      description: "Execute @cf/defog/sqlcoder-7b-2 model.",
      schema: WorkersAiPostRunCfDefogSqlcoder7b2Schema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_facebook_bart_large_cnn": {
      description: "Execute @cf/facebook/bart-large-cnn model.",
      schema: CreateWorkersAiPostRunCfFacebookBartLargeCnnSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_facebook_nonomni_bart_large_cnn": {
      description: "Execute @cf/facebook/nonomni-bart-large-cnn model.",
      schema: CreateWorkersAiPostRunCfFacebookNonomniBartLargeCnnSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_facebook_nonomni_detr_resnet_50": {
      description: "Execute @cf/facebook/nonomni-detr-resnet-50 model.",
      schema: WorkersAiPostRunCfFacebookNonomniDetrResnet50Schema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_fblgit_una_cybertron_7b_v2_bf16": {
      description: "Execute @cf/fblgit/una-cybertron-7b-v2-bf16 model.",
      schema: WorkersAiPostRunCfFblgitUnaCybertron7bV2Bf16Schema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_google_embeddinggemma_300m": {
      description: "Execute @cf/google/embeddinggemma-300m model.",
      schema: CreateWorkersAiPostRunCfGoogleEmbeddinggemma300mSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_google_gemma_2b_it_lora": {
      description: "Execute @cf/google/gemma-2b-it-lora model.",
      schema: WorkersAiPostRunCfGoogleGemma2bItLoraSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_google_gemma_3_12b_it": {
      description: "Execute @cf/google/gemma-3-12b-it model.",
      schema: WorkersAiPostRunCfGoogleGemma312bItSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_google_gemma_4_26b_a4b_it": {
      description: "Execute @cf/google/gemma-4-26b-a4b-it model.",
      schema: WorkersAiPostRunCfGoogleGemma426bA4bItSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_google_gemma_7b_it_lora": {
      description: "Execute @cf/google/gemma-7b-it-lora model.",
      schema: WorkersAiPostRunCfGoogleGemma7bItLoraSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_google_nonomni_embeddinggemma_300m": {
      description: "Execute @cf/google/nonomni-embeddinggemma-300m model.",
      schema: CreateWorkersAiPostRunCfGoogleNonomniEmbeddinggemma300mSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_huggingface_distilbert_sst_2_int8": {
      description: "Execute @cf/huggingface/distilbert-sst-2-int8 model.",
      schema: CreateWorkersAiPostRunCfHuggingfaceDistilbertSst2Int8Schema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_huggingface_nonomni_distilbert_sst_2_int8": {
      description:
        "Execute @cf/huggingface/nonomni-distilbert-sst-2-int8 model.",
      schema:
        CreateWorkersAiPostRunCfHuggingfaceNonomniDistilbertSst2Int8Schema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_ibm_granite_granite_4_0_h_micro": {
      description: "Execute @cf/ibm-granite/granite-4.0-h-micro model.",
      schema: WorkersAiPostRunCfIbmGraniteGranite40HMicroSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_leonardo_lucid_origin": {
      description: "Execute @cf/leonardo/lucid-origin model.",
      schema: CreateWorkersAiPostRunCfLeonardoLucidOriginSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_leonardo_phoenix_1_0": {
      description: "Execute @cf/leonardo/phoenix-1.0 model.",
      schema: CreateWorkersAiPostRunCfLeonardoPhoenix10Schema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_lykon_dreamshaper_8_lcm": {
      description: "Execute @cf/lykon/dreamshaper-8-lcm model.",
      schema: CreateWorkersAiPostRunCfLykonDreamshaper8LcmSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_meta_llama_llama_2_7b_chat_hf_lora": {
      description: "Execute @cf/meta-llama/llama-2-7b-chat-hf-lora model.",
      schema: WorkersAiPostRunCfMetaLlamaLlama27bChatHfLoraSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_meta_llama_2_7b_chat_fp16": {
      description: "Execute @cf/meta/llama-2-7b-chat-fp16 model.",
      schema: WorkersAiPostRunCfMetaLlama27bChatFp16Schema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_meta_llama_2_7b_chat_int8": {
      description: "Execute @cf/meta/llama-2-7b-chat-int8 model.",
      schema: WorkersAiPostRunCfMetaLlama27bChatInt8Schema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_meta_llama_3_8b_instruct": {
      description: "Execute @cf/meta/llama-3-8b-instruct model.",
      schema: WorkersAiPostRunCfMetaLlama38bInstructSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_meta_llama_3_8b_instruct_awq": {
      description: "Execute @cf/meta/llama-3-8b-instruct-awq model.",
      schema: WorkersAiPostRunCfMetaLlama38bInstructAwqSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_meta_llama_3_1_70b_instruct_fp8_fast": {
      description: "Execute @cf/meta/llama-3.1-70b-instruct-fp8-fast model.",
      schema: WorkersAiPostRunCfMetaLlama3170bInstructFp8FastSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_meta_llama_3_1_8b_instruct_awq": {
      description: "Execute @cf/meta/llama-3.1-8b-instruct-awq model.",
      schema: WorkersAiPostRunCfMetaLlama318bInstructAwqSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_meta_llama_3_1_8b_instruct_fp8": {
      description: "Execute @cf/meta/llama-3.1-8b-instruct-fp8 model.",
      schema: WorkersAiPostRunCfMetaLlama318bInstructFp8Schema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_meta_llama_3_1_8b_instruct_fp8_fast": {
      description: "Execute @cf/meta/llama-3.1-8b-instruct-fp8-fast model.",
      schema: WorkersAiPostRunCfMetaLlama318bInstructFp8FastSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_meta_llama_3_2_11b_vision_instruct": {
      description: "Execute @cf/meta/llama-3.2-11b-vision-instruct model.",
      schema: WorkersAiPostRunCfMetaLlama3211bVisionInstructSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_meta_llama_3_2_1b_instruct": {
      description: "Execute @cf/meta/llama-3.2-1b-instruct model.",
      schema: WorkersAiPostRunCfMetaLlama321bInstructSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_meta_llama_3_2_3b_instruct": {
      description: "Execute @cf/meta/llama-3.2-3b-instruct model.",
      schema: WorkersAiPostRunCfMetaLlama323bInstructSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_meta_llama_3_3_70b_instruct_fp8_fast": {
      description: "Execute @cf/meta/llama-3.3-70b-instruct-fp8-fast model.",
      schema: WorkersAiPostRunCfMetaLlama3370bInstructFp8FastSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_meta_llama_4_scout_17b_16e_instruct": {
      description: "Execute @cf/meta/llama-4-scout-17b-16e-instruct model.",
      schema: WorkersAiPostRunCfMetaLlama4Scout17b16eInstructSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_meta_llama_guard_3_8b": {
      description: "Execute @cf/meta/llama-guard-3-8b model.",
      schema: CreateWorkersAiPostRunCfMetaLlamaGuard38bSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_meta_m2m100_1_2b": {
      description: "Execute @cf/meta/m2m100-1.2b model.",
      schema: WorkersAiPostRunCfMetaM2m10012bSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_microsoft_nonomni_resnet_50": {
      description: "Execute @cf/microsoft/nonomni-resnet-50 model.",
      schema: WorkersAiPostRunCfMicrosoftNonomniResnet50Schema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_microsoft_phi_2": {
      description: "Execute @cf/microsoft/phi-2 model.",
      schema: WorkersAiPostRunCfMicrosoftPhi2Schema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_microsoft_resnet_50": {
      description: "Execute @cf/microsoft/resnet-50 model.",
      schema: WorkersAiPostRunCfMicrosoftResnet50Schema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_mistral_mistral_7b_instruct_v0_1": {
      description: "Execute @cf/mistral/mistral-7b-instruct-v0.1 model.",
      schema: WorkersAiPostRunCfMistralMistral7bInstructV01Schema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_mistral_mistral_7b_instruct_v0_2_lora": {
      description: "Execute @cf/mistral/mistral-7b-instruct-v0.2-lora model.",
      schema: WorkersAiPostRunCfMistralMistral7bInstructV02LoraSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_mistralai_mistral_small_3_1_24b_instruct": {
      description:
        "Execute @cf/mistralai/mistral-small-3.1-24b-instruct model.",
      schema: WorkersAiPostRunCfMistralaiMistralSmall3124bInstructSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_moonshotai_kimi_k2_5": {
      description: "Execute @cf/moonshotai/kimi-k2.5 model.",
      schema: WorkersAiPostRunCfMoonshotaiKimiK25Schema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_moonshotai_kimi_k2_6": {
      description: "Execute @cf/moonshotai/kimi-k2.6 model.",
      schema: WorkersAiPostRunCfMoonshotaiKimiK26Schema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_moonshotai_kimi_k2_7_code": {
      description: "Execute @cf/moonshotai/kimi-k2.7-code model.",
      schema: WorkersAiPostRunCfMoonshotaiKimiK27CodeSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_myshell_ai_melotts": {
      description: "Execute @cf/myshell-ai/melotts model.",
      schema: CreateWorkersAiPostRunCfMyshellAiMelottsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_nvidia_nemotron_3_120b_a12b": {
      description: "Execute @cf/nvidia/nemotron-3-120b-a12b model.",
      schema: WorkersAiPostRunCfNvidiaNemotron3120bA12bSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_websocket_run_cf_nvidia_nemotron_speech_streaming_en_0_6b":
      {
        description:
          "Open Websocket connection with @cf/nvidia/nemotron-speech-streaming-en-0.6b model.",
        schema: z.object({}),
        lifetime: "infinite" as const,
        garbageCollection: 20,
      },
    "workers_ai_post_run_cf_nvidia_nemotron_speech_streaming_en_0_6b": {
      description:
        "Execute @cf/nvidia/nemotron-speech-streaming-en-0.6b model.",
      schema: WorkersAiPostRunCfNvidiaNemotronSpeechStreamingEn06bSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_openai_gpt_oss_120b": {
      description: "Execute @cf/openai/gpt-oss-120b model.",
      schema: WorkersAiPostRunCfOpenaiGptOss120bSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_openai_gpt_oss_20b": {
      description: "Execute @cf/openai/gpt-oss-20b model.",
      schema: WorkersAiPostRunCfOpenaiGptOss20bSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_openai_whisper": {
      description: "Execute @cf/openai/whisper model.",
      schema: WorkersAiPostRunCfOpenaiWhisperSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_openai_whisper_large_v3_turbo": {
      description: "Execute @cf/openai/whisper-large-v3-turbo model.",
      schema: CreateWorkersAiPostRunCfOpenaiWhisperLargeV3TurboSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_openai_whisper_tiny_en": {
      description: "Execute @cf/openai/whisper-tiny-en model.",
      schema: WorkersAiPostRunCfOpenaiWhisperTinyEnSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_openchat_openchat_3_5_0106": {
      description: "Execute @cf/openchat/openchat-3.5-0106 model.",
      schema: WorkersAiPostRunCfOpenchatOpenchat350106Schema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_pfnet_plamo_embedding_1b": {
      description: "Execute @cf/pfnet/plamo-embedding-1b model.",
      schema: CreateWorkersAiPostRunCfPfnetPlamoEmbedding1bSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_websocket_run_cf_pipecat_ai_smart_turn_v2": {
      description:
        "Open Websocket connection with @cf/pipecat-ai/smart-turn-v2 model.",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_websocket_run_cf_pipecat_ai_smart_turn_v3": {
      description:
        "Open Websocket connection with @cf/pipecat-ai/smart-turn-v3 model.",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_qwen_qwen1_5_0_5b_chat": {
      description: "Execute @cf/qwen/qwen1.5-0.5b-chat model.",
      schema: WorkersAiPostRunCfQwenQwen1505bChatSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_qwen_qwen1_5_1_8b_chat": {
      description: "Execute @cf/qwen/qwen1.5-1.8b-chat model.",
      schema: WorkersAiPostRunCfQwenQwen1518bChatSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_qwen_qwen1_5_14b_chat_awq": {
      description: "Execute @cf/qwen/qwen1.5-14b-chat-awq model.",
      schema: WorkersAiPostRunCfQwenQwen1514bChatAwqSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_qwen_qwen1_5_7b_chat_awq": {
      description: "Execute @cf/qwen/qwen1.5-7b-chat-awq model.",
      schema: WorkersAiPostRunCfQwenQwen157bChatAwqSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_qwen_qwen2_5_coder_32b_instruct": {
      description: "Execute @cf/qwen/qwen2.5-coder-32b-instruct model.",
      schema: WorkersAiPostRunCfQwenQwen25Coder32bInstructSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_qwen_qwen3_30b_a3b_fp8": {
      description: "Execute @cf/qwen/qwen3-30b-a3b-fp8 model.",
      schema: WorkersAiPostRunCfQwenQwen330bA3bFp8Schema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_qwen_qwen3_embedding_0_6b": {
      description: "Execute @cf/qwen/qwen3-embedding-0.6b model.",
      schema: CreateWorkersAiPostRunCfQwenQwen3Embedding06bSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_qwen_qwq_32b": {
      description: "Execute @cf/qwen/qwq-32b model.",
      schema: WorkersAiPostRunCfQwenQwq32bSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_runwayml_stable_diffusion_v1_5_img2img": {
      description: "Execute @cf/runwayml/stable-diffusion-v1-5-img2img model.",
      schema: CreateWorkersAiPostRunCfRunwaymlStableDiffusionV15Img2imgSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_runwayml_stable_diffusion_v1_5_inpainting": {
      description:
        "Execute @cf/runwayml/stable-diffusion-v1-5-inpainting model.",
      schema:
        CreateWorkersAiPostRunCfRunwaymlStableDiffusionV15InpaintingSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_stabilityai_stable_diffusion_xl_base_1_0": {
      description:
        "Execute @cf/stabilityai/stable-diffusion-xl-base-1.0 model.",
      schema: CreateWorkersAiPostRunCfStabilityaiStableDiffusionXlBase10Schema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_websocket_run_cf_sven_test_pipe_http": {
      description:
        "Open Websocket connection with @cf/sven/test-pipe-http model.",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_websocket_run_cf_test_hello_world_cog": {
      description:
        "Open Websocket connection with @cf/test/hello-world-cog model.",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_thebloke_discolm_german_7b_v1_awq": {
      description: "Execute @cf/thebloke/discolm-german-7b-v1-awq model.",
      schema: WorkersAiPostRunCfTheblokeDiscolmGerman7bV1AwqSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_tiiuae_falcon_7b_instruct": {
      description: "Execute @cf/tiiuae/falcon-7b-instruct model.",
      schema: WorkersAiPostRunCfTiiuaeFalcon7bInstructSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_tinyllama_tinyllama_1_1b_chat_v1_0": {
      description: "Execute @cf/tinyllama/tinyllama-1.1b-chat-v1.0 model.",
      schema: WorkersAiPostRunCfTinyllamaTinyllama11bChatV10Schema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_zai_org_glm_4_7_flash": {
      description: "Execute @cf/zai-org/glm-4.7-flash model.",
      schema: WorkersAiPostRunCfZaiOrgGlm47FlashSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_cf_zai_org_glm_5_2": {
      description: "Execute @cf/zai-org/glm-5.2 model.",
      schema: WorkersAiPostRunCfZaiOrgGlm52Schema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_hf_google_gemma_7b_it": {
      description: "Execute @hf/google/gemma-7b-it model.",
      schema: WorkersAiPostRunHfGoogleGemma7bItSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_hf_mistral_mistral_7b_instruct_v0_2": {
      description: "Execute @hf/mistral/mistral-7b-instruct-v0.2 model.",
      schema: WorkersAiPostRunHfMistralMistral7bInstructV02Schema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_hf_nexusflow_starling_lm_7b_beta": {
      description: "Execute @hf/nexusflow/starling-lm-7b-beta model.",
      schema: WorkersAiPostRunHfNexusflowStarlingLm7bBetaSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_hf_nousresearch_hermes_2_pro_mistral_7b": {
      description: "Execute @hf/nousresearch/hermes-2-pro-mistral-7b model.",
      schema: WorkersAiPostRunHfNousresearchHermes2ProMistral7bSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_hf_thebloke_deepseek_coder_6_7b_base_awq": {
      description: "Execute @hf/thebloke/deepseek-coder-6.7b-base-awq model.",
      schema: WorkersAiPostRunHfTheblokeDeepseekCoder67bBaseAwqSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_hf_thebloke_deepseek_coder_6_7b_instruct_awq": {
      description:
        "Execute @hf/thebloke/deepseek-coder-6.7b-instruct-awq model.",
      schema: WorkersAiPostRunHfTheblokeDeepseekCoder67bInstructAwqSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_hf_thebloke_llama_2_13b_chat_awq": {
      description: "Execute @hf/thebloke/llama-2-13b-chat-awq model.",
      schema: WorkersAiPostRunHfTheblokeLlama213bChatAwqSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_hf_thebloke_mistral_7b_instruct_v0_1_awq": {
      description: "Execute @hf/thebloke/mistral-7b-instruct-v0.1-awq model.",
      schema: WorkersAiPostRunHfTheblokeMistral7bInstructV01AwqSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_hf_thebloke_neural_chat_7b_v3_1_awq": {
      description: "Execute @hf/thebloke/neural-chat-7b-v3-1-awq model.",
      schema: WorkersAiPostRunHfTheblokeNeuralChat7bV31AwqSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_hf_thebloke_openhermes_2_5_mistral_7b_awq": {
      description: "Execute @hf/thebloke/openhermes-2.5-mistral-7b-awq model.",
      schema: WorkersAiPostRunHfTheblokeOpenhermes25Mistral7bAwqSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_hf_thebloke_zephyr_7b_beta_awq": {
      description: "Execute @hf/thebloke/zephyr-7b-beta-awq model.",
      schema: WorkersAiPostRunHfTheblokeZephyr7bBetaAwqSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_post_run_model": {
      description: "Execute AI model",
      schema: WorkersAiPostRunModelSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "workers_ai_search_task": {
      description: "Task Search",
      schema: ListWorkersAiSearchTaskSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "workers_ai_post_to_markdown": {
      description: "Convert Files into Markdown",
      schema: WorkersAiPostToMarkdownSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "get_to_markdown_supported": {
      description: "Get all converted formats supported",
      schema: GetToMarkdownSupportedSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
  },

  methods: {
    get_credit_balance: {
      description: "Get credit balance",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai-gateway/billing/credit-balance`,
        );

        const handle = await context.writeResource(
          "credit_balance",
          "latest",
          result,
        );
        context.logger.info("Fetched credit_balance", {});
        return { dataHandles: [handle] };
      },
    },
    get_invoice_history: {
      description: "Get invoice history",
      arguments: z.object({
        type: z.enum(["auto", "all", "manual"]).optional().describe(
          "Filter invoice type: auto, manual, or all.",
        ),
      }),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai-gateway/billing/invoice-history`,
        );

        const handle = await context.writeResource(
          "invoice_history",
          "latest",
          result,
        );
        context.logger.info("Fetched invoice_history", {});
        return { dataHandles: [handle] };
      },
    },
    get_invoice_preview: {
      description: "Get invoice preview",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai-gateway/billing/invoice-preview`,
        );

        const handle = await context.writeResource(
          "invoice_preview",
          "latest",
          result,
        );
        context.logger.info("Fetched invoice_preview", {});
        return { dataHandles: [handle] };
      },
    },
    get_spending_limit: {
      description: "Get spending limit",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai-gateway/billing/spending-limit`,
        );

        const handle = await context.writeResource(
          "spending_limit",
          "latest",
          result,
        );
        context.logger.info("Fetched spending_limit", {});
        return { dataHandles: [handle] };
      },
    },
    delete_spending_limit: {
      description: "Delete spending limit",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/ai-gateway/billing/spending-limit`,
        );

        context.logger.info("Deleted resource {id}", { id: "unknown" });
        return { dataHandles: [] };
      },
    },
    create_topup: {
      description: "Create a top-up",
      arguments: z.object({
        amount: z.number().int().min(1000).describe(
          "Top-up amount in cents (min 1000).",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai-gateway/billing/topup`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource("topup", id, result);
        context.logger.info("Created topup {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_topup_config: {
      description: "Get auto top-up configuration",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai-gateway/billing/topup/config`,
        );

        const handle = await context.writeResource(
          "topup_config",
          "latest",
          result,
        );
        context.logger.info("Fetched topup_config", {});
        return { dataHandles: [handle] };
      },
    },
    create_aig_billing_set_topup_config: {
      description: "Set auto top-up configuration",
      arguments: z.object({
        amount: z.number().int().min(1000).describe(
          "Auto top-up amount in cents (min 1000).",
        ),
        threshold: z.number().int().min(500).describe(
          "Balance threshold in cents that triggers auto top-up (min 500).",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai-gateway/billing/topup/config`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "aig_billing_set_topup_config",
          id,
          result,
        );
        context.logger.info("Created aig_billing_set_topup_config {id}", {
          id,
        });
        return { dataHandles: [handle] };
      },
    },
    delete_topup_config: {
      description: "Delete auto top-up configuration",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/ai-gateway/billing/topup/config`,
        );

        context.logger.info("Deleted resource {id}", { id: "unknown" });
        return { dataHandles: [] };
      },
    },
    get_topup_limits: {
      description: "Get account top-up limits",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai-gateway/billing/topup/limits`,
        );

        const handle = await context.writeResource(
          "topup_limits",
          "latest",
          result,
        );
        context.logger.info("Fetched topup_limits", {});
        return { dataHandles: [handle] };
      },
    },
    create_aig_billing_check_topup_status: {
      description: "Check top-up status",
      arguments: z.object({
        payment_intent_id: z.string().describe(
          "Stripe invoice ID to check status for.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai-gateway/billing/topup/status`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "aig_billing_check_topup_status",
          id,
          result,
        );
        context.logger.info("Created aig_billing_check_topup_status {id}", {
          id,
        });
        return { dataHandles: [handle] };
      },
    },
    get_usage_history: {
      description: "Get usage history",
      arguments: z.object({
        value_grouping_window: z.enum(["day", "hour"]).optional().describe(
          "Grouping window for usage data.",
        ),
        start_time: z.number().optional().describe(
          "Start time as Unix timestamp in milliseconds.",
        ),
        end_time: z.number().optional().describe(
          "End time as Unix timestamp in milliseconds.",
        ),
      }),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai-gateway/billing/usage-history`,
        );

        const handle = await context.writeResource(
          "usage_history",
          "latest",
          result,
        );
        context.logger.info("Fetched usage_history", {});
        return { dataHandles: [handle] };
      },
    },
    list_account_provider: {
      description: "List Account Providers",
      arguments: z.object({
        page: z.number().optional(),
        per_page: z.number().optional(),
        beta: z.boolean().optional(),
        enable: z.boolean().optional(),
        search: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/ai-gateway/custom-providers`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("account_provider", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} account_provider", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_account_provider: {
      description: "Create a new Account Provider",
      arguments: z.object({
        base_url: z.string(),
        beta: z.boolean().optional(),
        curl_example: z.string().optional(),
        description: z.string().optional(),
        enable: z.boolean().optional(),
        headers: z.string().max(8192).optional(),
        js_example: z.string().optional(),
        link: z.string().optional(),
        name: z.string(),
        position: z.number().int().optional(),
        slug: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai-gateway/custom-providers`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "account_provider",
          id,
          result,
        );
        context.logger.info("Created account_provider {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    list_account_provider_cost: {
      description: "List Account Provider Costs",
      arguments: z.object({
        page: z.number().optional(),
        per_page: z.number().optional(),
        enable: z.boolean().optional(),
        account_provider_id: z.string().optional(),
        model_rule: z.enum(["equals", "starts-with", "contains"]).optional(),
        cost_type: z.string().optional(),
        search: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/ai-gateway/custom-providers/costs`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "account_provider_cost",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} account_provider_cost", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_account_provider_cost: {
      description: "Create a new Account Provider Cost",
      arguments: z.object({
        account_provider_id: z.string(),
        cost_in: z.number().optional(),
        cost_out: z.number().optional(),
        cost_type: z.string().optional(),
        enable: z.boolean().optional(),
        model: z.string(),
        model_rule: z.enum(["equals", "starts-with", "contains"]).optional(),
        token_pricing: z.object({
          input_audio_tokens: z.number().optional(),
          input_cache_creation_tokens: z.number().optional(),
          input_cached_tokens: z.number().optional(),
          input_image_count: z.number().optional(),
          input_image_tokens: z.number().optional(),
          input_text_tokens: z.number().optional(),
          input_tokens: z.number().optional(),
          input_video_tokens: z.number().optional(),
          output_image_count: z.number().optional(),
          output_reasoning_tokens: z.number().optional(),
          output_tokens: z.number().optional(),
          total_tokens: z.number().optional(),
        }).optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai-gateway/custom-providers/costs`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "account_provider_cost",
          id,
          result,
        );
        context.logger.info("Created account_provider_cost {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_aig_config_fetch_account_provider_cost: {
      description: "Fetch a Account Provider Cost",
      arguments: z.object({
        id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai-gateway/custom-providers/costs/${args.id}`,
        );

        const handle = await context.writeResource(
          "aig_config_fetch_account_provider_cost",
          String(args.id),
          result,
        );
        context.logger.info(
          "Fetched aig_config_fetch_account_provider_cost",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    update_account_provider_cost: {
      description: "Update a Account Provider Cost",
      arguments: z.object({
        id: z.string(),
        cost_in: z.number().optional(),
        cost_out: z.number().optional(),
        cost_type: z.string().optional(),
        enable: z.boolean().optional(),
        model: z.string().optional(),
        model_rule: z.enum(["equals", "starts-with", "contains"]).optional(),
        token_pricing: z.object({
          input_audio_tokens: z.number().optional(),
          input_cache_creation_tokens: z.number().optional(),
          input_cached_tokens: z.number().optional(),
          input_image_count: z.number().optional(),
          input_image_tokens: z.number().optional(),
          input_text_tokens: z.number().optional(),
          input_tokens: z.number().optional(),
          input_video_tokens: z.number().optional(),
          output_image_count: z.number().optional(),
          output_reasoning_tokens: z.number().optional(),
          output_tokens: z.number().optional(),
          total_tokens: z.number().optional(),
        }).optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/accounts/${accountId}/ai-gateway/custom-providers/costs/${args.id}`,
          body,
        );

        const handle = await context.writeResource(
          "account_provider_cost",
          String(args.id),
          result,
        );
        context.logger.info("Updated account_provider_cost", {});
        return { dataHandles: [handle] };
      },
    },
    delete_account_provider_cost: {
      description: "Delete a Account Provider Cost",
      arguments: z.object({
        id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/ai-gateway/custom-providers/costs/${args.id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.id });
        return { dataHandles: [] };
      },
    },
    get_aig_config_fetch_account_provider: {
      description: "Fetch a Account Provider",
      arguments: z.object({
        id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai-gateway/custom-providers/${args.id}`,
        );

        const handle = await context.writeResource(
          "aig_config_fetch_account_provider",
          String(args.id),
          result,
        );
        context.logger.info("Fetched aig_config_fetch_account_provider", {});
        return { dataHandles: [handle] };
      },
    },
    update_account_provider: {
      description: "Update a Account Provider",
      arguments: z.object({
        id: z.string(),
        base_url: z.string().optional(),
        beta: z.boolean().optional(),
        curl_example: z.string().optional(),
        description: z.string().optional(),
        enable: z.boolean().optional(),
        headers: z.string().max(8192).optional(),
        js_example: z.string().optional(),
        link: z.string().optional(),
        logo: z.string().optional(),
        name: z.string().optional(),
        position: z.number().int().optional(),
        slug: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/accounts/${accountId}/ai-gateway/custom-providers/${args.id}`,
          body,
        );

        const handle = await context.writeResource(
          "account_provider",
          String(args.id),
          result,
        );
        context.logger.info("Updated account_provider", {});
        return { dataHandles: [handle] };
      },
    },
    delete_account_provider: {
      description: "Delete a Account Provider",
      arguments: z.object({
        id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/ai-gateway/custom-providers/${args.id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.id });
        return { dataHandles: [] };
      },
    },
    list_evaluators: {
      description: "List Evaluators",
      arguments: z.object({
        page: z.number().optional(),
        per_page: z.number().optional(),
        order_by: z.string().optional(),
        order_by_direction: z.enum(["asc", "desc"]).optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/ai-gateway/evaluation-types`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("evaluators", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} evaluators", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    list_gateway: {
      description: "List Gateways",
      arguments: z.object({
        page: z.number().optional(),
        per_page: z.number().optional(),
        search: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/ai-gateway/gateways`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("gateway", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} gateway", { count: results.length });
        return { dataHandles: [handle] };
      },
    },
    create_gateway: {
      description: "Create a new Gateway",
      arguments: z.object({
        authentication: z.boolean().optional(),
        cache_invalidate_on_update: z.boolean(),
        cache_ttl: z.number().int().min(0).nullable(),
        collect_logs: z.boolean(),
        log_management: z.number().int().min(10000).max(10000000).nullable()
          .optional(),
        log_management_strategy: z.enum(["STOP_INSERTING", "DELETE_OLDEST"])
          .nullable().optional(),
        logpush: z.boolean().optional(),
        logpush_public_key: z.string().min(16).max(1024).nullable().optional(),
        rate_limiting_interval: z.number().int().min(0).nullable(),
        rate_limiting_limit: z.number().int().min(0).nullable(),
        rate_limiting_technique: z.enum(["fixed", "sliding"]).nullable()
          .optional(),
        retry_backoff: z.enum(["constant", "linear", "exponential"]).nullable()
          .optional().describe("Backoff strategy for retry delays"),
        retry_delay: z.number().int().min(0).max(5000).nullable().optional()
          .describe("Delay between retry attempts in milliseconds (0-5000)"),
        retry_max_attempts: z.number().int().min(1).max(5).nullable().optional()
          .describe(
            "Maximum number of retry attempts for failed requests (1-5)",
          ),
        workers_ai_billing_mode: z.enum(["postpaid"]).optional().describe(
          "Controls how Workers AI inference calls routed through this gateway are bille...",
        ),
        zdr: z.boolean().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai-gateway/gateways`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource("gateway", id, result);
        context.logger.info("Created gateway {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    list_dataset: {
      description: "List Datasets",
      arguments: z.object({
        gateway_id: z.string(),
        page: z.number().optional(),
        per_page: z.number().optional(),
        name: z.string().optional(),
        enable: z.boolean().optional(),
        search: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["gateway_id", "page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/ai-gateway/gateways/${args.gateway_id}/datasets`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("dataset", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} dataset", { count: results.length });
        return { dataHandles: [handle] };
      },
    },
    create_dataset: {
      description: "Create a new Dataset",
      arguments: z.object({
        gateway_id: z.string(),
        enable: z.boolean(),
        filters: z.array(z.object({
          key: z.enum([
            "created_at",
            "request_content_type",
            "response_content_type",
            "success",
            "cached",
            "provider",
            "model",
            "cost",
            "tokens",
            "tokens_in",
            "tokens_out",
            "duration",
            "feedback",
          ]),
          operator: z.enum(["eq", "contains", "lt", "gt"]),
          value: z.array(z.union([z.string(), z.number(), z.boolean()])),
        })),
        name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["gateway_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai-gateway/gateways/${args.gateway_id}/datasets`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource("dataset", id, result);
        context.logger.info("Created dataset {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_aig_config_fetch_dataset: {
      description: "Fetch a Dataset",
      arguments: z.object({
        gateway_id: z.string(),
        id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai-gateway/gateways/${args.gateway_id}/datasets/${args.id}`,
        );

        const handle = await context.writeResource(
          "aig_config_fetch_dataset",
          String(args.id),
          result,
        );
        context.logger.info("Fetched aig_config_fetch_dataset", {});
        return { dataHandles: [handle] };
      },
    },
    update_dataset: {
      description: "Update a Dataset",
      arguments: z.object({
        gateway_id: z.string(),
        id: z.string(),
        enable: z.boolean(),
        filters: z.array(z.object({
          key: z.enum([
            "created_at",
            "request_content_type",
            "response_content_type",
            "success",
            "cached",
            "provider",
            "model",
            "cost",
            "tokens",
            "tokens_in",
            "tokens_out",
            "duration",
            "feedback",
          ]),
          operator: z.enum(["eq", "contains", "lt", "gt"]),
          value: z.array(z.union([z.string(), z.number(), z.boolean()])),
        })),
        name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["gateway_id", "id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/ai-gateway/gateways/${args.gateway_id}/datasets/${args.id}`,
          body,
        );

        const handle = await context.writeResource(
          "dataset",
          String(args.id),
          result,
        );
        context.logger.info("Updated dataset", {});
        return { dataHandles: [handle] };
      },
    },
    delete_dataset: {
      description: "Delete a Dataset",
      arguments: z.object({
        gateway_id: z.string(),
        id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/ai-gateway/gateways/${args.gateway_id}/datasets/${args.id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.id });
        return { dataHandles: [] };
      },
    },
    list_evaluations: {
      description: "List Evaluations",
      arguments: z.object({
        gateway_id: z.string(),
        page: z.number().optional(),
        per_page: z.number().optional(),
        name: z.string().optional(),
        processed: z.boolean().optional(),
        search: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["gateway_id", "page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/ai-gateway/gateways/${args.gateway_id}/evaluations`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("evaluations", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} evaluations", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_evaluations: {
      description: "Create a new Evaluation",
      arguments: z.object({
        gateway_id: z.string(),
        dataset_ids: z.array(z.string()),
        evaluation_type_ids: z.array(z.string()),
        name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["gateway_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai-gateway/gateways/${args.gateway_id}/evaluations`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource("evaluations", id, result);
        context.logger.info("Created evaluations {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_aig_config_fetch_evaluations: {
      description: "Fetch a Evaluation",
      arguments: z.object({
        gateway_id: z.string(),
        id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai-gateway/gateways/${args.gateway_id}/evaluations/${args.id}`,
        );

        const handle = await context.writeResource(
          "aig_config_fetch_evaluations",
          String(args.id),
          result,
        );
        context.logger.info("Fetched aig_config_fetch_evaluations", {});
        return { dataHandles: [handle] };
      },
    },
    delete_evaluations: {
      description: "Delete a Evaluation",
      arguments: z.object({
        gateway_id: z.string(),
        id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/ai-gateway/gateways/${args.gateway_id}/evaluations/${args.id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.id });
        return { dataHandles: [] };
      },
    },
    list_gateway_logs: {
      description: "List Gateway Logs",
      arguments: z.object({
        gateway_id: z.string(),
        search: z.string().optional(),
        page: z.number().optional(),
        per_page: z.number().optional(),
        order_by: z.enum([
          "created_at",
          "provider",
          "model",
          "model_type",
          "success",
          "cached",
        ]).optional(),
        order_by_direction: z.enum(["asc", "desc"]).optional(),
        filters: z.string().optional(),
        meta_info: z.boolean().optional(),
        direction: z.enum(["asc", "desc"]).optional(),
        start_date: z.string().optional(),
        end_date: z.string().optional(),
        min_cost: z.number().optional(),
        max_cost: z.number().optional(),
        min_tokens_in: z.number().optional(),
        max_tokens_in: z.number().optional(),
        min_tokens_out: z.number().optional(),
        max_tokens_out: z.number().optional(),
        min_total_tokens: z.number().optional(),
        max_total_tokens: z.number().optional(),
        min_duration: z.number().optional(),
        max_duration: z.number().optional(),
        feedback: z.string().optional(),
        success: z.boolean().optional(),
        cached: z.boolean().optional(),
        model: z.string().optional(),
        model_type: z.string().optional(),
        provider: z.string().optional(),
        request_content_type: z.string().optional(),
        response_content_type: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["gateway_id", "page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/ai-gateway/gateways/${args.gateway_id}/logs`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("gateway_logs", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} gateway_logs", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    delete_gateway_logs: {
      description: "Delete Gateway Logs",
      arguments: z.object({
        gateway_id: z.string(),
        order_by: z.enum([
          "created_at",
          "provider",
          "model",
          "model_type",
          "success",
          "cached",
          "cost",
          "tokens_in",
          "tokens_out",
          "duration",
          "feedback",
        ]).optional(),
        order_by_direction: z.enum(["asc", "desc"]).optional(),
        filters: z.string().optional(),
        limit: z.number().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/ai-gateway/gateways/${args.gateway_id}/logs`,
        );

        context.logger.info("Deleted resource {id}", { id: args.gateway_id });
        return { dataHandles: [] };
      },
    },
    get_gateway_log_detail: {
      description: "Get Gateway Log Detail",
      arguments: z.object({
        id: z.string(),
        gateway_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai-gateway/gateways/${args.gateway_id}/logs/${args.id}`,
        );

        const handle = await context.writeResource(
          "gateway_log_detail",
          String(args.gateway_id),
          result,
        );
        context.logger.info("Fetched gateway_log_detail", {});
        return { dataHandles: [handle] };
      },
    },
    patch_gateway_log: {
      description: "Patch Gateway Log",
      arguments: z.object({
        id: z.string(),
        gateway_id: z.string(),
        feedback: z.number().min(-1).max(1).nullable().optional(),
        metadata: z.record(
          z.string(),
          z.union([z.string(), z.number(), z.boolean()]),
        ).nullable().optional(),
        score: z.number().min(0).max(100).nullable().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["id", "gateway_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/accounts/${accountId}/ai-gateway/gateways/${args.gateway_id}/logs/${args.id}`,
          body,
        );

        const handle = await context.writeResource(
          "patch_gateway_log",
          String(args.gateway_id),
          result,
        );
        context.logger.info("Updated patch_gateway_log", {});
        return { dataHandles: [handle] };
      },
    },
    get_gateway_log_request: {
      description: "Get Gateway Log Request",
      arguments: z.object({
        id: z.string(),
        gateway_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai-gateway/gateways/${args.gateway_id}/logs/${args.id}/request`,
        );

        const handle = await context.writeResource(
          "gateway_log_request",
          String(args.gateway_id),
          result,
        );
        context.logger.info("Fetched gateway_log_request", {});
        return { dataHandles: [handle] };
      },
    },
    get_gateway_log_response: {
      description: "Get Gateway Log Response",
      arguments: z.object({
        id: z.string(),
        gateway_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai-gateway/gateways/${args.gateway_id}/logs/${args.id}/response`,
        );

        const handle = await context.writeResource(
          "gateway_log_response",
          String(args.gateway_id),
          result,
        );
        context.logger.info("Fetched gateway_log_response", {});
        return { dataHandles: [handle] };
      },
    },
    list_providers: {
      description: "List Provider Configs",
      arguments: z.object({
        gateway_id: z.string(),
        page: z.number().optional(),
        per_page: z.number().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["gateway_id", "page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/ai-gateway/gateways/${args.gateway_id}/provider_configs`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("providers", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} providers", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_providers: {
      description: "Create a new Provider Configs",
      arguments: z.object({
        gateway_id: z.string(),
        alias: z.string(),
        default_config: z.boolean(),
        provider_slug: z.string(),
        rate_limit: z.number().optional(),
        rate_limit_period: z.number().optional(),
        secret: z.string().optional(),
        secret_id: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["gateway_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai-gateway/gateways/${args.gateway_id}/provider_configs`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource("providers", id, result);
        context.logger.info("Created providers {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    update_providers: {
      description: "Update a Provider Configs",
      arguments: z.object({
        gateway_id: z.string(),
        id: z.string(),
        secret: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["gateway_id", "id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/ai-gateway/gateways/${args.gateway_id}/provider_configs/${args.id}`,
          body,
        );

        const handle = await context.writeResource(
          "providers",
          String(args.id),
          result,
        );
        context.logger.info("Updated providers", {});
        return { dataHandles: [handle] };
      },
    },
    delete_providers: {
      description: "Delete a Provider Configs",
      arguments: z.object({
        gateway_id: z.string(),
        id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/ai-gateway/gateways/${args.gateway_id}/provider_configs/${args.id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.id });
        return { dataHandles: [] };
      },
    },
    list_gateway_dynamic_routes: {
      description: "List all AI Gateway Dynamic Routes.",
      arguments: z.object({
        gateway_id: z.string(),
        page: z.number().optional().describe("Page number"),
        per_page: z.number().optional().describe("Number of routes per page"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai-gateway/gateways/${args.gateway_id}/routes`,
        );

        const handle = await context.writeResource(
          "list_gateway_dynamic_routes",
          String(args.gateway_id),
          result,
        );
        context.logger.info("Fetched list_gateway_dynamic_routes", {});
        return { dataHandles: [handle] };
      },
    },
    create_aig_config_post_gateway_dynamic_route: {
      description: "Create a new AI Gateway Dynamic Route.",
      arguments: z.object({
        gateway_id: z.string(),
        elements: z.array(z.union([
          z.object({
            id: z.string(),
            outputs: z.object({
              next: z.object({
                elementId: z.string(),
              }),
            }),
            type: z.enum(["start"]),
          }),
          z.object({
            id: z.string(),
            outputs: z.object({
              false: z.object({
                elementId: z.string(),
              }),
              true: z.object({
                elementId: z.string(),
              }),
            }),
            properties: z.object({
              conditions: z.object({}).optional(),
            }),
            type: z.enum(["conditional"]),
          }),
          z.object({
            id: z.string(),
            outputs: z.record(
              z.string(),
              z.object({
                elementId: z.string(),
              }),
            ),
            type: z.enum(["percentage"]),
          }),
          z.object({
            id: z.string(),
            outputs: z.object({
              fallback: z.object({
                elementId: z.string(),
              }),
              success: z.object({
                elementId: z.string(),
              }),
            }),
            properties: z.object({
              key: z.string(),
              limit: z.number(),
              limitType: z.enum(["count", "cost"]),
              window: z.number(),
            }),
            type: z.enum(["rate"]),
          }),
          z.object({
            id: z.string(),
            outputs: z.object({
              fallback: z.object({
                elementId: z.string(),
              }),
              success: z.object({
                elementId: z.string(),
              }),
            }),
            properties: z.object({
              model: z.string(),
              provider: z.string(),
              retries: z.number(),
              timeout: z.number(),
            }),
            type: z.enum(["model"]),
          }),
          z.object({
            id: z.string(),
            outputs: z.record(
              z.string(),
              z.object({
                elementId: z.string(),
              }),
            ),
            type: z.enum(["end"]),
          }),
        ])),
        name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["gateway_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai-gateway/gateways/${args.gateway_id}/routes`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "aig_config_post_gateway_dynamic_route",
          id,
          result,
        );
        context.logger.info(
          "Created aig_config_post_gateway_dynamic_route {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    get_gateway_dynamic_route: {
      description: "Get an AI Gateway Dynamic Route.",
      arguments: z.object({
        gateway_id: z.string(),
        id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai-gateway/gateways/${args.gateway_id}/routes/${args.id}`,
        );

        const handle = await context.writeResource(
          "gateway_dynamic_route",
          String(args.id),
          result,
        );
        context.logger.info("Fetched gateway_dynamic_route", {});
        return { dataHandles: [handle] };
      },
    },
    update_gateway_dynamic_route: {
      description: "Update an AI Gateway Dynamic Route.",
      arguments: z.object({
        gateway_id: z.string(),
        id: z.string(),
        name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["gateway_id", "id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/accounts/${accountId}/ai-gateway/gateways/${args.gateway_id}/routes/${args.id}`,
          body,
        );

        const handle = await context.writeResource(
          "gateway_dynamic_route",
          String(args.id),
          result,
        );
        context.logger.info("Updated gateway_dynamic_route", {});
        return { dataHandles: [handle] };
      },
    },
    delete_gateway_dynamic_route: {
      description: "Delete an AI Gateway Dynamic Route.",
      arguments: z.object({
        gateway_id: z.string(),
        id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/ai-gateway/gateways/${args.gateway_id}/routes/${args.id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.id });
        return { dataHandles: [] };
      },
    },
    list_gateway_dynamic_route_deployments: {
      description: "List all AI Gateway Dynamic Route Deployments.",
      arguments: z.object({
        gateway_id: z.string(),
        id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai-gateway/gateways/${args.gateway_id}/routes/${args.id}/deployments`,
        );

        const handle = await context.writeResource(
          "list_gateway_dynamic_route_deployments",
          String(args.id),
          result,
        );
        context.logger.info(
          "Fetched list_gateway_dynamic_route_deployments",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    create_aig_config_post_gateway_dynamic_route_deployment: {
      description: "Create a new AI Gateway Dynamic Route Deployment.",
      arguments: z.object({
        gateway_id: z.string(),
        id: z.string(),
        version_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["gateway_id", "id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai-gateway/gateways/${args.gateway_id}/routes/${args.id}/deployments`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "aig_config_post_gateway_dynamic_route_deployment",
          id,
          result,
        );
        context.logger.info(
          "Created aig_config_post_gateway_dynamic_route_deployment {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    list_gateway_dynamic_route_versions: {
      description: "List all AI Gateway Dynamic Route Versions.",
      arguments: z.object({
        gateway_id: z.string(),
        id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai-gateway/gateways/${args.gateway_id}/routes/${args.id}/versions`,
        );

        const handle = await context.writeResource(
          "list_gateway_dynamic_route_versions",
          String(args.id),
          result,
        );
        context.logger.info("Fetched list_gateway_dynamic_route_versions", {});
        return { dataHandles: [handle] };
      },
    },
    create_aig_config_post_gateway_dynamic_route_version: {
      description: "Create a new AI Gateway Dynamic Route Version.",
      arguments: z.object({
        gateway_id: z.string(),
        id: z.string(),
        elements: z.array(z.union([
          z.object({
            id: z.string(),
            outputs: z.object({
              next: z.object({
                elementId: z.string(),
              }),
            }),
            type: z.enum(["start"]),
          }),
          z.object({
            id: z.string(),
            outputs: z.object({
              false: z.object({
                elementId: z.string(),
              }),
              true: z.object({
                elementId: z.string(),
              }),
            }),
            properties: z.object({
              conditions: z.object({}).optional(),
            }),
            type: z.enum(["conditional"]),
          }),
          z.object({
            id: z.string(),
            outputs: z.record(
              z.string(),
              z.object({
                elementId: z.string(),
              }),
            ),
            type: z.enum(["percentage"]),
          }),
          z.object({
            id: z.string(),
            outputs: z.object({
              fallback: z.object({
                elementId: z.string(),
              }),
              success: z.object({
                elementId: z.string(),
              }),
            }),
            properties: z.object({
              key: z.string(),
              limit: z.number(),
              limitType: z.enum(["count", "cost"]),
              window: z.number(),
            }),
            type: z.enum(["rate"]),
          }),
          z.object({
            id: z.string(),
            outputs: z.object({
              fallback: z.object({
                elementId: z.string(),
              }),
              success: z.object({
                elementId: z.string(),
              }),
            }),
            properties: z.object({
              model: z.string(),
              provider: z.string(),
              retries: z.number(),
              timeout: z.number(),
            }),
            type: z.enum(["model"]),
          }),
          z.object({
            id: z.string(),
            outputs: z.record(
              z.string(),
              z.object({
                elementId: z.string(),
              }),
            ),
            type: z.enum(["end"]),
          }),
        ])),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["gateway_id", "id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai-gateway/gateways/${args.gateway_id}/routes/${args.id}/versions`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "aig_config_post_gateway_dynamic_route_version",
          id,
          result,
        );
        context.logger.info(
          "Created aig_config_post_gateway_dynamic_route_version {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    get_gateway_dynamic_route_version: {
      description: "Get an AI Gateway Dynamic Route Version.",
      arguments: z.object({
        gateway_id: z.string(),
        id: z.string(),
        version_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai-gateway/gateways/${args.gateway_id}/routes/${args.id}/versions/${args.version_id}`,
        );

        const handle = await context.writeResource(
          "gateway_dynamic_route_version",
          String(args.version_id),
          result,
        );
        context.logger.info("Fetched gateway_dynamic_route_version", {});
        return { dataHandles: [handle] };
      },
    },
    get_gateway_url: {
      description: "Get Gateway URL",
      arguments: z.object({
        gateway_id: z.string(),
        provider: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai-gateway/gateways/${args.gateway_id}/url/${args.provider}`,
        );

        const handle = await context.writeResource(
          "gateway_url",
          String(args.provider),
          result,
        );
        context.logger.info("Fetched gateway_url", {});
        return { dataHandles: [handle] };
      },
    },
    get_aig_config_fetch_gateway: {
      description: "Fetch a Gateway",
      arguments: z.object({
        id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai-gateway/gateways/${args.id}`,
        );

        const handle = await context.writeResource(
          "aig_config_fetch_gateway",
          String(args.id),
          result,
        );
        context.logger.info("Fetched aig_config_fetch_gateway", {});
        return { dataHandles: [handle] };
      },
    },
    update_gateway: {
      description: "Update a Gateway",
      arguments: z.object({
        id: z.string(),
        authentication: z.boolean().optional(),
        cache_invalidate_on_update: z.boolean(),
        cache_ttl: z.number().int().min(0).nullable(),
        collect_logs: z.boolean(),
        dlp: z.union([
          z.object({
            action: z.enum(["BLOCK", "FLAG"]),
            enabled: z.boolean(),
            profiles: z.array(z.string()),
          }),
          z.object({
            enabled: z.boolean(),
            policies: z.array(z.object({
              action: z.enum(["FLAG", "BLOCK"]),
              check: z.array(z.enum(["REQUEST", "RESPONSE"])),
              enabled: z.boolean(),
              id: z.string(),
              profiles: z.array(z.string()),
            })),
          }),
        ]).optional(),
        guardrails: z.object({
          prompt: z.object({
            P1: z.enum(["FLAG", "BLOCK"]).optional(),
            S1: z.enum(["FLAG", "BLOCK"]).optional(),
            S10: z.enum(["FLAG", "BLOCK"]).optional(),
            S11: z.enum(["FLAG", "BLOCK"]).optional(),
            S12: z.enum(["FLAG", "BLOCK"]).optional(),
            S13: z.enum(["FLAG", "BLOCK"]).optional(),
            S2: z.enum(["FLAG", "BLOCK"]).optional(),
            S3: z.enum(["FLAG", "BLOCK"]).optional(),
            S4: z.enum(["FLAG", "BLOCK"]).optional(),
            S5: z.enum(["FLAG", "BLOCK"]).optional(),
            S6: z.enum(["FLAG", "BLOCK"]).optional(),
            S7: z.enum(["FLAG", "BLOCK"]).optional(),
            S8: z.enum(["FLAG", "BLOCK"]).optional(),
            S9: z.enum(["FLAG", "BLOCK"]).optional(),
          }),
          response: z.object({
            P1: z.enum(["FLAG", "BLOCK"]).optional(),
            S1: z.enum(["FLAG", "BLOCK"]).optional(),
            S10: z.enum(["FLAG", "BLOCK"]).optional(),
            S11: z.enum(["FLAG", "BLOCK"]).optional(),
            S12: z.enum(["FLAG", "BLOCK"]).optional(),
            S13: z.enum(["FLAG", "BLOCK"]).optional(),
            S2: z.enum(["FLAG", "BLOCK"]).optional(),
            S3: z.enum(["FLAG", "BLOCK"]).optional(),
            S4: z.enum(["FLAG", "BLOCK"]).optional(),
            S5: z.enum(["FLAG", "BLOCK"]).optional(),
            S6: z.enum(["FLAG", "BLOCK"]).optional(),
            S7: z.enum(["FLAG", "BLOCK"]).optional(),
            S8: z.enum(["FLAG", "BLOCK"]).optional(),
            S9: z.enum(["FLAG", "BLOCK"]).optional(),
          }),
        }).nullable().optional(),
        log_management: z.number().int().min(10000).max(10000000).nullable()
          .optional(),
        log_management_strategy: z.enum(["STOP_INSERTING", "DELETE_OLDEST"])
          .nullable().optional(),
        logpush: z.boolean().optional(),
        logpush_public_key: z.string().min(16).max(1024).nullable().optional(),
        otel: z.array(z.object({
          authorization: z.string().max(256).optional(),
          content_type: z.enum(["json", "protobuf"]).optional(),
          headers: z.record(z.string(), z.string().max(4096)),
          url: z.string().max(2048),
        })).nullable().optional(),
        rate_limiting_interval: z.number().int().min(0).nullable(),
        rate_limiting_limit: z.number().int().min(0).nullable(),
        rate_limiting_technique: z.enum(["fixed", "sliding"]).nullable()
          .optional(),
        retry_backoff: z.enum(["constant", "linear", "exponential"]).nullable()
          .optional().describe("Backoff strategy for retry delays"),
        retry_delay: z.number().int().min(0).max(5000).nullable().optional()
          .describe("Delay between retry attempts in milliseconds (0-5000)"),
        retry_max_attempts: z.number().int().min(1).max(5).nullable().optional()
          .describe(
            "Maximum number of retry attempts for failed requests (1-5)",
          ),
        spend_limits: z.object({
          enabled: z.boolean().optional(),
          rules: z.array(z.object({
            enabled: z.boolean().optional(),
            id: z.string().min(1).optional(),
            limit: z.number().min(0),
            limitType: z.enum(["cost"]),
            metadata: z.record(
              z.string(),
              z.union([
                z.object({
                  mode: z.enum(["partition"]),
                }),
                z.object({
                  mode: z.enum(["filter"]),
                  values: z.array(z.unknown()),
                }),
              ]),
            ).optional(),
            model: z.object({
              mode: z.enum(["filter"]),
              values: z.array(z.string()),
            }).optional(),
            provider: z.object({
              mode: z.enum(["filter"]),
              values: z.array(z.string()),
            }).optional(),
            technique: z.enum(["fixed", "sliding"]).optional(),
            window: z.number().int().min(0),
          })).optional(),
        }).nullable().optional(),
        store_id: z.string().nullable().optional(),
        stripe: z.object({
          authorization: z.string(),
          usage_events: z.array(z.object({
            payload: z.string(),
          })),
        }).nullable().optional(),
        workers_ai_billing_mode: z.enum(["postpaid"]).optional().describe(
          "Controls how Workers AI inference calls routed through this gateway are bille...",
        ),
        zdr: z.boolean().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/ai-gateway/gateways/${args.id}`,
          body,
        );

        const handle = await context.writeResource(
          "gateway",
          String(args.id),
          result,
        );
        context.logger.info("Updated gateway", {});
        return { dataHandles: [handle] };
      },
    },
    delete_gateway: {
      description: "Delete a Gateway",
      arguments: z.object({
        id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/ai-gateway/gateways/${args.id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.id });
        return { dataHandles: [] };
      },
    },
    list_instances: {
      description: "List AI Search instances.",
      arguments: z.object({
        page: z.number().optional().describe("Page number (1-indexed)."),
        per_page: z.number().optional().describe("Number of results per page."),
        search: z.string().optional().describe(
          "Filter instances whose id contains this string (case-insensitive).",
        ),
        namespace: z.string().optional().describe("Filter by namespace."),
        order_by: z.enum(["created_at"]).optional().describe(
          "Field to order results by.",
        ),
        order_by_direction: z.enum(["asc", "desc"]).optional().describe(
          "Order direction.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/ai-search/instances`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("instances", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} instances", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_instance: {
      description: "Create an AI Search instance.",
      arguments: z.object({
        ai_gateway_id: z.string().nullable().optional(),
        ai_search_model: z.union([
          z.literal("@cf/meta/llama-3.3-70b-instruct-fp8-fast"),
          z.literal("@cf/zai-org/glm-4.7-flash"),
          z.literal("@cf/meta/llama-3.1-8b-instruct-fast"),
          z.literal("@cf/meta/llama-3.1-8b-instruct-fp8"),
          z.literal("@cf/meta/llama-4-scout-17b-16e-instruct"),
          z.literal("@cf/qwen/qwen3-30b-a3b-fp8"),
          z.literal("@cf/deepseek-ai/deepseek-r1-distill-qwen-32b"),
          z.literal("@cf/moonshotai/kimi-k2-instruct"),
          z.literal("@cf/google/gemma-3-12b-it"),
          z.literal("@cf/google/gemma-4-26b-a4b-it"),
          z.literal("@cf/moonshotai/kimi-k2.5"),
          z.literal("anthropic/claude-3-7-sonnet"),
          z.literal("anthropic/claude-sonnet-4"),
          z.literal("anthropic/claude-opus-4"),
          z.literal("anthropic/claude-3-5-haiku"),
          z.literal("cerebras/qwen-3-235b-a22b-instruct"),
          z.literal("cerebras/qwen-3-235b-a22b-thinking"),
          z.literal("cerebras/llama-3.3-70b"),
          z.literal("cerebras/llama-4-maverick-17b-128e-instruct"),
          z.literal("cerebras/llama-4-scout-17b-16e-instruct"),
          z.literal("cerebras/gpt-oss-120b"),
          z.literal("google-ai-studio/gemini-2.5-flash"),
          z.literal("google-ai-studio/gemini-2.5-pro"),
          z.literal("grok/grok-4"),
          z.literal("groq/llama-3.3-70b-versatile"),
          z.literal("groq/llama-3.1-8b-instant"),
          z.literal("openai/gpt-5"),
          z.literal("openai/gpt-5-mini"),
          z.literal("openai/gpt-5-nano"),
          z.literal(""),
          z.literal(null),
        ]).nullable().optional(),
        cache: z.boolean().optional(),
        cache_threshold: z.enum([
          "super_strict_match",
          "close_enough",
          "flexible_friend",
          "anything_goes",
        ]).optional(),
        cache_ttl: z.union([
          z.union([z.literal(600)]),
          z.union([z.literal(1800)]),
          z.union([z.literal(3600)]),
          z.union([z.literal(7200)]),
          z.union([z.literal(21600)]),
          z.union([z.literal(43200)]),
          z.union([z.literal(86400)]),
          z.union([z.literal(172800)]),
          z.union([z.literal(259200)]),
          z.union([z.literal(518400)]),
        ]).optional().describe(
          "Cache entry TTL in seconds. Allowed values: 600 (10min), 1800 (30min), 3600 (...",
        ),
        chunk: z.boolean().optional(),
        chunk_overlap: z.number().int().min(0).max(30).optional(),
        chunk_size: z.number().int().min(64).optional(),
        custom_metadata: z.array(z.object({
          data_type: z.enum(["text", "number", "boolean", "datetime"]),
          field_name: z.string().min(1).max(64),
        })).optional(),
        embedding_model: z.union([
          z.literal("@cf/qwen/qwen3-embedding-0.6b"),
          z.literal("@cf/qwen/qwen3-vl-embedding-2b"),
          z.literal("@cf/baai/bge-m3"),
          z.literal("@cf/baai/bge-large-en-v1.5"),
          z.literal("@cf/google/embeddinggemma-300m"),
          z.literal("google-ai-studio/gemini-embedding-001"),
          z.literal("google-ai-studio/gemini-embedding-2-preview"),
          z.literal("google-ai-studio/gemini-embedding-2"),
          z.literal("openai/text-embedding-3-small"),
          z.literal("openai/text-embedding-3-large"),
          z.literal(""),
          z.literal(null),
        ]).nullable().optional(),
        fusion_method: z.enum(["max", "rrf"]).optional(),
        hybrid_search_enabled: z.boolean().optional().describe(
          "Deprecated — use index_method instead.",
        ),
        index_method: z.object({
          keyword: z.boolean(),
          vector: z.boolean(),
        }).optional().describe(
          "Controls which storage backends are used during indexing. Defaults to vector-...",
        ),
        indexing_options: z.object({
          keyword_tokenizer: z.enum(["porter", "trigram"]).optional(),
        }).nullable().optional(),
        max_num_results: z.number().int().min(1).max(50).optional(),
        metadata: z.object({
          created_from_aisearch_wizard: z.boolean().optional(),
          worker_domain: z.string().optional(),
        }).optional(),
        public_endpoint_params: z.object({
          authorized_hosts: z.array(z.string()).optional(),
          chat_completions_endpoint: z.object({
            disabled: z.boolean().optional(),
          }).optional(),
          custom_domains: z.array(z.string().min(1).max(253)).nullable()
            .optional(),
          enabled: z.boolean().optional(),
          mcp: z.object({
            description: z.string().optional(),
            disabled: z.boolean().optional(),
          }).optional(),
          rate_limit: z.object({
            period_ms: z.number().int().min(60000).max(3600000).optional(),
            requests: z.number().int().min(1).optional(),
            technique: z.enum(["fixed", "sliding"]).optional(),
          }).optional(),
          search_endpoint: z.object({
            disabled: z.boolean().optional(),
          }).optional(),
        }).optional(),
        reranking: z.boolean().optional(),
        reranking_model: z.union([
          z.literal("@cf/baai/bge-reranker-base"),
          z.literal(""),
          z.literal(null),
        ]).nullable().optional(),
        retrieval_options: z.object({
          boost_by: z.array(z.object({
            direction: z.enum(["asc", "desc", "exists", "not_exists"])
              .optional(),
            field: z.string().min(1).max(64),
          })).optional(),
          keyword_match_mode: z.enum(["and", "or"]).optional(),
        }).nullable().optional(),
        rewrite_model: z.union([
          z.literal("@cf/meta/llama-3.3-70b-instruct-fp8-fast"),
          z.literal("@cf/zai-org/glm-4.7-flash"),
          z.literal("@cf/meta/llama-3.1-8b-instruct-fast"),
          z.literal("@cf/meta/llama-3.1-8b-instruct-fp8"),
          z.literal("@cf/meta/llama-4-scout-17b-16e-instruct"),
          z.literal("@cf/qwen/qwen3-30b-a3b-fp8"),
          z.literal("@cf/deepseek-ai/deepseek-r1-distill-qwen-32b"),
          z.literal("@cf/moonshotai/kimi-k2-instruct"),
          z.literal("@cf/google/gemma-3-12b-it"),
          z.literal("@cf/google/gemma-4-26b-a4b-it"),
          z.literal("@cf/moonshotai/kimi-k2.5"),
          z.literal("anthropic/claude-3-7-sonnet"),
          z.literal("anthropic/claude-sonnet-4"),
          z.literal("anthropic/claude-opus-4"),
          z.literal("anthropic/claude-3-5-haiku"),
          z.literal("cerebras/qwen-3-235b-a22b-instruct"),
          z.literal("cerebras/qwen-3-235b-a22b-thinking"),
          z.literal("cerebras/llama-3.3-70b"),
          z.literal("cerebras/llama-4-maverick-17b-128e-instruct"),
          z.literal("cerebras/llama-4-scout-17b-16e-instruct"),
          z.literal("cerebras/gpt-oss-120b"),
          z.literal("google-ai-studio/gemini-2.5-flash"),
          z.literal("google-ai-studio/gemini-2.5-pro"),
          z.literal("grok/grok-4"),
          z.literal("groq/llama-3.3-70b-versatile"),
          z.literal("groq/llama-3.1-8b-instant"),
          z.literal("openai/gpt-5"),
          z.literal("openai/gpt-5-mini"),
          z.literal("openai/gpt-5-nano"),
          z.literal(""),
          z.literal(null),
        ]).nullable().optional(),
        rewrite_query: z.boolean().optional(),
        score_threshold: z.number().min(0).max(1).optional(),
        source: z.string().nullable().optional(),
        source_params: z.object({
          exclude_items: z.array(z.string().max(512)).optional(),
          include_items: z.array(z.string().max(512)).optional(),
          prefix: z.string().optional(),
          r2_jurisdiction: z.string().optional(),
          web_crawler: z.object({
            parse_options: z.object({
              content_selector: z.array(z.object({
                path: z.string().min(1).max(200),
                selector: z.string().min(1).max(200),
              })).optional(),
              include_headers: z.record(z.string(), z.string().max(8192))
                .optional(),
              include_images: z.boolean().optional(),
              specific_sitemaps: z.array(z.string()).optional(),
              use_browser_rendering: z.boolean().optional(),
            }).optional(),
            parse_type: z.enum(["sitemap", "crawl"]).optional(),
          }).optional(),
        }).nullable().optional(),
        sync_interval: z.union([
          z.union([z.literal(900)]),
          z.union([z.literal(1800)]),
          z.union([z.literal(3600)]),
          z.union([z.literal(7200)]),
          z.union([z.literal(14400)]),
          z.union([z.literal(21600)]),
          z.union([z.literal(43200)]),
          z.union([z.literal(86400)]),
        ]).optional().describe(
          "Interval between automatic syncs, in seconds. Allowed values: 900 (15min), 18...",
        ),
        token_id: z.string().optional(),
        type: z.union([
          z.literal("r2"),
          z.literal("web-crawler"),
          z.literal(null),
        ]).nullable().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai-search/instances`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource("instance", id, result);
        context.logger.info("Created instance {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_ai_search_fetch_instance: {
      description: "Get an AI Search instance.",
      arguments: z.object({
        id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai-search/instances/${args.id}`,
        );

        const handle = await context.writeResource(
          "ai_search_fetch_instance",
          String(args.id),
          result,
        );
        context.logger.info("Fetched ai_search_fetch_instance", {});
        return { dataHandles: [handle] };
      },
    },
    update_instance: {
      description: "Update an AI Search instance.",
      arguments: z.object({
        id: z.string(),
        ai_gateway_id: z.string().nullable().optional(),
        ai_search_model: z.union([
          z.literal("@cf/meta/llama-3.3-70b-instruct-fp8-fast"),
          z.literal("@cf/zai-org/glm-4.7-flash"),
          z.literal("@cf/meta/llama-3.1-8b-instruct-fast"),
          z.literal("@cf/meta/llama-3.1-8b-instruct-fp8"),
          z.literal("@cf/meta/llama-4-scout-17b-16e-instruct"),
          z.literal("@cf/qwen/qwen3-30b-a3b-fp8"),
          z.literal("@cf/deepseek-ai/deepseek-r1-distill-qwen-32b"),
          z.literal("@cf/moonshotai/kimi-k2-instruct"),
          z.literal("@cf/google/gemma-3-12b-it"),
          z.literal("@cf/google/gemma-4-26b-a4b-it"),
          z.literal("@cf/moonshotai/kimi-k2.5"),
          z.literal("anthropic/claude-3-7-sonnet"),
          z.literal("anthropic/claude-sonnet-4"),
          z.literal("anthropic/claude-opus-4"),
          z.literal("anthropic/claude-3-5-haiku"),
          z.literal("cerebras/qwen-3-235b-a22b-instruct"),
          z.literal("cerebras/qwen-3-235b-a22b-thinking"),
          z.literal("cerebras/llama-3.3-70b"),
          z.literal("cerebras/llama-4-maverick-17b-128e-instruct"),
          z.literal("cerebras/llama-4-scout-17b-16e-instruct"),
          z.literal("cerebras/gpt-oss-120b"),
          z.literal("google-ai-studio/gemini-2.5-flash"),
          z.literal("google-ai-studio/gemini-2.5-pro"),
          z.literal("grok/grok-4"),
          z.literal("groq/llama-3.3-70b-versatile"),
          z.literal("groq/llama-3.1-8b-instant"),
          z.literal("openai/gpt-5"),
          z.literal("openai/gpt-5-mini"),
          z.literal("openai/gpt-5-nano"),
          z.literal(""),
          z.literal(null),
        ]).nullable().optional(),
        cache: z.boolean().optional(),
        cache_threshold: z.enum([
          "super_strict_match",
          "close_enough",
          "flexible_friend",
          "anything_goes",
        ]).optional(),
        cache_ttl: z.union([
          z.union([z.literal(600)]),
          z.union([z.literal(1800)]),
          z.union([z.literal(3600)]),
          z.union([z.literal(7200)]),
          z.union([z.literal(21600)]),
          z.union([z.literal(43200)]),
          z.union([z.literal(86400)]),
          z.union([z.literal(172800)]),
          z.union([z.literal(259200)]),
          z.union([z.literal(518400)]),
        ]).optional().describe(
          "Cache entry TTL in seconds. Allowed values: 600 (10min), 1800 (30min), 3600 (...",
        ),
        chunk: z.boolean().optional(),
        chunk_overlap: z.number().int().min(0).max(30).optional(),
        chunk_size: z.number().int().min(64).optional(),
        custom_metadata: z.array(z.object({
          data_type: z.enum(["text", "number", "boolean", "datetime"]),
          field_name: z.string().min(1).max(64),
        })).optional(),
        embedding_model: z.union([
          z.literal("@cf/qwen/qwen3-embedding-0.6b"),
          z.literal("@cf/qwen/qwen3-vl-embedding-2b"),
          z.literal("@cf/baai/bge-m3"),
          z.literal("@cf/baai/bge-large-en-v1.5"),
          z.literal("@cf/google/embeddinggemma-300m"),
          z.literal("google-ai-studio/gemini-embedding-001"),
          z.literal("google-ai-studio/gemini-embedding-2-preview"),
          z.literal("google-ai-studio/gemini-embedding-2"),
          z.literal("openai/text-embedding-3-small"),
          z.literal("openai/text-embedding-3-large"),
          z.literal(""),
          z.literal(null),
        ]).nullable().optional(),
        fusion_method: z.enum(["max", "rrf"]).optional(),
        index_method: z.object({
          keyword: z.boolean(),
          vector: z.boolean(),
        }).optional().describe(
          "Controls which storage backends are used during indexing. Defaults to vector-...",
        ),
        indexing_options: z.object({
          keyword_tokenizer: z.enum(["porter", "trigram"]).optional(),
        }).nullable().optional(),
        max_num_results: z.number().int().min(1).max(50).optional(),
        metadata: z.object({
          created_from_aisearch_wizard: z.boolean().optional(),
          worker_domain: z.string().optional(),
        }).optional(),
        paused: z.boolean().optional(),
        public_endpoint_params: z.object({
          authorized_hosts: z.array(z.string()).optional(),
          chat_completions_endpoint: z.object({
            disabled: z.boolean().optional(),
          }).optional(),
          custom_domains: z.array(z.string().min(1).max(253)).nullable()
            .optional(),
          enabled: z.boolean().optional(),
          mcp: z.object({
            description: z.string().optional(),
            disabled: z.boolean().optional(),
          }).optional(),
          rate_limit: z.object({
            period_ms: z.number().int().min(60000).max(3600000).optional(),
            requests: z.number().int().min(1).optional(),
            technique: z.enum(["fixed", "sliding"]).optional(),
          }).optional(),
          search_endpoint: z.object({
            disabled: z.boolean().optional(),
          }).optional(),
        }).optional(),
        reranking: z.boolean().optional(),
        reranking_model: z.union([
          z.literal("@cf/baai/bge-reranker-base"),
          z.literal(""),
          z.literal(null),
        ]).nullable().optional(),
        retrieval_options: z.object({
          boost_by: z.array(z.object({
            direction: z.enum(["asc", "desc", "exists", "not_exists"])
              .optional(),
            field: z.string().min(1).max(64),
          })).optional(),
          keyword_match_mode: z.enum(["and", "or"]).optional(),
        }).nullable().optional(),
        rewrite_model: z.union([
          z.literal("@cf/meta/llama-3.3-70b-instruct-fp8-fast"),
          z.literal("@cf/zai-org/glm-4.7-flash"),
          z.literal("@cf/meta/llama-3.1-8b-instruct-fast"),
          z.literal("@cf/meta/llama-3.1-8b-instruct-fp8"),
          z.literal("@cf/meta/llama-4-scout-17b-16e-instruct"),
          z.literal("@cf/qwen/qwen3-30b-a3b-fp8"),
          z.literal("@cf/deepseek-ai/deepseek-r1-distill-qwen-32b"),
          z.literal("@cf/moonshotai/kimi-k2-instruct"),
          z.literal("@cf/google/gemma-3-12b-it"),
          z.literal("@cf/google/gemma-4-26b-a4b-it"),
          z.literal("@cf/moonshotai/kimi-k2.5"),
          z.literal("anthropic/claude-3-7-sonnet"),
          z.literal("anthropic/claude-sonnet-4"),
          z.literal("anthropic/claude-opus-4"),
          z.literal("anthropic/claude-3-5-haiku"),
          z.literal("cerebras/qwen-3-235b-a22b-instruct"),
          z.literal("cerebras/qwen-3-235b-a22b-thinking"),
          z.literal("cerebras/llama-3.3-70b"),
          z.literal("cerebras/llama-4-maverick-17b-128e-instruct"),
          z.literal("cerebras/llama-4-scout-17b-16e-instruct"),
          z.literal("cerebras/gpt-oss-120b"),
          z.literal("google-ai-studio/gemini-2.5-flash"),
          z.literal("google-ai-studio/gemini-2.5-pro"),
          z.literal("grok/grok-4"),
          z.literal("groq/llama-3.3-70b-versatile"),
          z.literal("groq/llama-3.1-8b-instant"),
          z.literal("openai/gpt-5"),
          z.literal("openai/gpt-5-mini"),
          z.literal("openai/gpt-5-nano"),
          z.literal(""),
          z.literal(null),
        ]).nullable().optional(),
        rewrite_query: z.boolean().optional(),
        score_threshold: z.number().min(0).max(1).optional(),
        source: z.string().nullable().optional(),
        source_params: z.object({
          exclude_items: z.array(z.string().max(512)).optional(),
          include_items: z.array(z.string().max(512)).optional(),
          prefix: z.string().optional(),
          r2_jurisdiction: z.string().optional(),
          web_crawler: z.object({
            parse_options: z.object({
              content_selector: z.array(z.object({
                path: z.string().min(1).max(200),
                selector: z.string().min(1).max(200),
              })).optional(),
              include_headers: z.record(z.string(), z.string().max(8192))
                .optional(),
              include_images: z.boolean().optional(),
              specific_sitemaps: z.array(z.string()).optional(),
              use_browser_rendering: z.boolean().optional(),
            }).optional(),
            parse_type: z.enum(["sitemap", "crawl"]).optional(),
          }).optional(),
        }).nullable().optional(),
        summarization: z.boolean().optional(),
        summarization_model: z.union([
          z.literal("@cf/meta/llama-3.3-70b-instruct-fp8-fast"),
          z.literal("@cf/zai-org/glm-4.7-flash"),
          z.literal("@cf/meta/llama-3.1-8b-instruct-fast"),
          z.literal("@cf/meta/llama-3.1-8b-instruct-fp8"),
          z.literal("@cf/meta/llama-4-scout-17b-16e-instruct"),
          z.literal("@cf/qwen/qwen3-30b-a3b-fp8"),
          z.literal("@cf/deepseek-ai/deepseek-r1-distill-qwen-32b"),
          z.literal("@cf/moonshotai/kimi-k2-instruct"),
          z.literal("@cf/google/gemma-3-12b-it"),
          z.literal("@cf/google/gemma-4-26b-a4b-it"),
          z.literal("@cf/moonshotai/kimi-k2.5"),
          z.literal("anthropic/claude-3-7-sonnet"),
          z.literal("anthropic/claude-sonnet-4"),
          z.literal("anthropic/claude-opus-4"),
          z.literal("anthropic/claude-3-5-haiku"),
          z.literal("cerebras/qwen-3-235b-a22b-instruct"),
          z.literal("cerebras/qwen-3-235b-a22b-thinking"),
          z.literal("cerebras/llama-3.3-70b"),
          z.literal("cerebras/llama-4-maverick-17b-128e-instruct"),
          z.literal("cerebras/llama-4-scout-17b-16e-instruct"),
          z.literal("cerebras/gpt-oss-120b"),
          z.literal("google-ai-studio/gemini-2.5-flash"),
          z.literal("google-ai-studio/gemini-2.5-pro"),
          z.literal("grok/grok-4"),
          z.literal("groq/llama-3.3-70b-versatile"),
          z.literal("groq/llama-3.1-8b-instant"),
          z.literal("openai/gpt-5"),
          z.literal("openai/gpt-5-mini"),
          z.literal("openai/gpt-5-nano"),
          z.literal(""),
          z.literal(null),
        ]).nullable().optional(),
        sync_interval: z.union([
          z.union([z.literal(900)]),
          z.union([z.literal(1800)]),
          z.union([z.literal(3600)]),
          z.union([z.literal(7200)]),
          z.union([z.literal(14400)]),
          z.union([z.literal(21600)]),
          z.union([z.literal(43200)]),
          z.union([z.literal(86400)]),
        ]).optional().describe(
          "Interval between automatic syncs, in seconds. Allowed values: 900 (15min), 18...",
        ),
        system_prompt_ai_search: z.string().nullable().optional(),
        system_prompt_index_summarization: z.string().nullable().optional(),
        system_prompt_rewrite_query: z.string().nullable().optional(),
        token_id: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/ai-search/instances/${args.id}`,
          body,
        );

        const handle = await context.writeResource(
          "instance",
          String(args.id),
          result,
        );
        context.logger.info("Updated instance", {});
        return { dataHandles: [handle] };
      },
    },
    delete_instance: {
      description: "Delete an AI Search instance.",
      arguments: z.object({
        id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/ai-search/instances/${args.id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.id });
        return { dataHandles: [] };
      },
    },
    create_ai_search_instance_chat_completion: {
      description: "Chat Completions",
      arguments: z.object({
        id: z.string().describe(
          "AI Search instance ID. Lowercase alphanumeric, hyphens, and underscores.",
        ),
        ai_search_options: z.object({
          cache: z.object({
            cache_threshold: z.enum([
              "super_strict_match",
              "close_enough",
              "flexible_friend",
              "anything_goes",
            ]).optional(),
            enabled: z.boolean().optional(),
          }).optional(),
          query_rewrite: z.object({
            enabled: z.boolean().optional(),
            model: z.union([
              z.enum([
                "@cf/meta/llama-3.3-70b-instruct-fp8-fast",
                "@cf/zai-org/glm-4.7-flash",
                "@cf/meta/llama-3.1-8b-instruct-fast",
                "@cf/meta/llama-3.1-8b-instruct-fp8",
                "@cf/meta/llama-4-scout-17b-16e-instruct",
                "@cf/qwen/qwen3-30b-a3b-fp8",
                "@cf/deepseek-ai/deepseek-r1-distill-qwen-32b",
                "@cf/moonshotai/kimi-k2-instruct",
                "@cf/google/gemma-3-12b-it",
                "@cf/google/gemma-4-26b-a4b-it",
                "@cf/moonshotai/kimi-k2.5",
                "anthropic/claude-3-7-sonnet",
                "anthropic/claude-sonnet-4",
                "anthropic/claude-opus-4",
                "anthropic/claude-3-5-haiku",
                "cerebras/qwen-3-235b-a22b-instruct",
                "cerebras/qwen-3-235b-a22b-thinking",
                "cerebras/llama-3.3-70b",
                "cerebras/llama-4-maverick-17b-128e-instruct",
                "cerebras/llama-4-scout-17b-16e-instruct",
                "cerebras/gpt-oss-120b",
                "google-ai-studio/gemini-2.5-flash",
                "google-ai-studio/gemini-2.5-pro",
                "grok/grok-4",
                "groq/llama-3.3-70b-versatile",
                "groq/llama-3.1-8b-instant",
                "openai/gpt-5",
                "openai/gpt-5-mini",
                "openai/gpt-5-nano",
              ]),
              z.enum([""]),
            ]).optional(),
            rewrite_prompt: z.string().optional(),
          }).optional(),
          reranking: z.object({
            enabled: z.boolean().optional(),
            match_threshold: z.number().min(0).max(1).optional(),
            model: z.union([
              z.enum(["@cf/baai/bge-reranker-base"]),
              z.enum([""]),
            ]).optional(),
          }).optional(),
          retrieval: z.object({
            boost_by: z.array(z.object({
              direction: z.enum(["asc", "desc", "exists", "not_exists"])
                .optional(),
              field: z.string().min(1).max(64),
            })).optional(),
            context_expansion: z.number().int().min(0).max(3).optional(),
            filters: z.record(z.string(), z.unknown()).optional(),
            fusion_method: z.enum(["max", "rrf"]).optional(),
            keyword_match_mode: z.enum(["and", "or"]).optional(),
            match_threshold: z.number().min(0).max(1).optional(),
            max_num_results: z.number().int().min(1).max(50).optional(),
            retrieval_type: z.enum(["vector", "keyword", "hybrid"]).optional(),
            return_on_failure: z.boolean().optional(),
          }).optional(),
        }).optional(),
        messages: z.array(z.object({
          content: z.union([
            z.string(),
            z.array(z.union([
              z.object({
                text: z.string().min(1),
                type: z.enum(["text"]),
              }),
              z.object({
                image_url: z.object({
                  url: z.unknown(),
                }),
                type: z.enum(["image_url"]),
              }),
            ])),
            z.unknown().nullable(),
          ]),
          role: z.enum(["system", "developer", "user", "assistant", "tool"]),
        })),
        model: z.union([
          z.enum([
            "@cf/meta/llama-3.3-70b-instruct-fp8-fast",
            "@cf/zai-org/glm-4.7-flash",
            "@cf/meta/llama-3.1-8b-instruct-fast",
            "@cf/meta/llama-3.1-8b-instruct-fp8",
            "@cf/meta/llama-4-scout-17b-16e-instruct",
            "@cf/qwen/qwen3-30b-a3b-fp8",
            "@cf/deepseek-ai/deepseek-r1-distill-qwen-32b",
            "@cf/moonshotai/kimi-k2-instruct",
            "@cf/google/gemma-3-12b-it",
            "@cf/google/gemma-4-26b-a4b-it",
            "@cf/moonshotai/kimi-k2.5",
            "anthropic/claude-3-7-sonnet",
            "anthropic/claude-sonnet-4",
            "anthropic/claude-opus-4",
            "anthropic/claude-3-5-haiku",
            "cerebras/qwen-3-235b-a22b-instruct",
            "cerebras/qwen-3-235b-a22b-thinking",
            "cerebras/llama-3.3-70b",
            "cerebras/llama-4-maverick-17b-128e-instruct",
            "cerebras/llama-4-scout-17b-16e-instruct",
            "cerebras/gpt-oss-120b",
            "google-ai-studio/gemini-2.5-flash",
            "google-ai-studio/gemini-2.5-pro",
            "grok/grok-4",
            "groq/llama-3.3-70b-versatile",
            "groq/llama-3.1-8b-instant",
            "openai/gpt-5",
            "openai/gpt-5-mini",
            "openai/gpt-5-nano",
          ]),
          z.enum([""]),
        ]).optional(),
        stream: z.boolean().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai-search/instances/${args.id}/chat/completions`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "ai_search_instance_chat_completion",
          id,
          result,
        );
        context.logger.info("Created ai_search_instance_chat_completion {id}", {
          id,
        });
        return { dataHandles: [handle] };
      },
    },
    list_jobs: {
      description: "List Jobs",
      arguments: z.object({
        id: z.string().describe(
          "AI Search instance ID. Lowercase alphanumeric, hyphens, and underscores.",
        ),
        page: z.number().optional(),
        per_page: z.number().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["id", "page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/ai-search/instances/${args.id}/jobs`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("jobs", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} jobs", { count: results.length });
        return { dataHandles: [handle] };
      },
    },
    create_job: {
      description: "Create new job",
      arguments: z.object({
        id: z.string().describe(
          "AI Search instance ID. Lowercase alphanumeric, hyphens, and underscores.",
        ),
        description: z.string().max(255).optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai-search/instances/${args.id}/jobs`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource("job", id, result);
        context.logger.info("Created job {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_job: {
      description: "Get a Job Details",
      arguments: z.object({
        id: z.string().describe(
          "AI Search instance ID. Lowercase alphanumeric, hyphens, and underscores.",
        ),
        job_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai-search/instances/${args.id}/jobs/${args.job_id}`,
        );

        const handle = await context.writeResource(
          "job",
          String(args.job_id),
          result,
        );
        context.logger.info("Fetched job", {});
        return { dataHandles: [handle] };
      },
    },
    update_ai_search_instance_change_job_status: {
      description: "Cancel an indexing job.",
      arguments: z.object({
        id: z.string().describe(
          "AI Search instance ID. Lowercase alphanumeric, hyphens, and underscores.",
        ),
        job_id: z.string(),
        action: z.enum(["cancel"]),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["id", "job_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/accounts/${accountId}/ai-search/instances/${args.id}/jobs/${args.job_id}`,
          body,
        );

        const handle = await context.writeResource(
          "ai_search_instance_change_job_status",
          String(args.job_id),
          result,
        );
        context.logger.info("Updated ai_search_instance_change_job_status", {});
        return { dataHandles: [handle] };
      },
    },
    list_job_logs: {
      description: "List Job Logs",
      arguments: z.object({
        id: z.string().describe(
          "AI Search instance ID. Lowercase alphanumeric, hyphens, and underscores.",
        ),
        job_id: z.string(),
        page: z.number().optional(),
        per_page: z.number().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["id", "job_id", "page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/ai-search/instances/${args.id}/jobs/${args.job_id}/logs`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("job_logs", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} job_logs", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_ai_search_instance_search: {
      description: "Search",
      arguments: z.object({
        id: z.string().describe(
          "AI Search instance ID. Lowercase alphanumeric, hyphens, and underscores.",
        ),
        ai_search_options: z.object({
          cache: z.object({
            cache_threshold: z.enum([
              "super_strict_match",
              "close_enough",
              "flexible_friend",
              "anything_goes",
            ]).optional(),
            enabled: z.boolean().optional(),
          }).optional(),
          query_rewrite: z.object({
            enabled: z.boolean().optional(),
            model: z.union([
              z.enum([
                "@cf/meta/llama-3.3-70b-instruct-fp8-fast",
                "@cf/zai-org/glm-4.7-flash",
                "@cf/meta/llama-3.1-8b-instruct-fast",
                "@cf/meta/llama-3.1-8b-instruct-fp8",
                "@cf/meta/llama-4-scout-17b-16e-instruct",
                "@cf/qwen/qwen3-30b-a3b-fp8",
                "@cf/deepseek-ai/deepseek-r1-distill-qwen-32b",
                "@cf/moonshotai/kimi-k2-instruct",
                "@cf/google/gemma-3-12b-it",
                "@cf/google/gemma-4-26b-a4b-it",
                "@cf/moonshotai/kimi-k2.5",
                "anthropic/claude-3-7-sonnet",
                "anthropic/claude-sonnet-4",
                "anthropic/claude-opus-4",
                "anthropic/claude-3-5-haiku",
                "cerebras/qwen-3-235b-a22b-instruct",
                "cerebras/qwen-3-235b-a22b-thinking",
                "cerebras/llama-3.3-70b",
                "cerebras/llama-4-maverick-17b-128e-instruct",
                "cerebras/llama-4-scout-17b-16e-instruct",
                "cerebras/gpt-oss-120b",
                "google-ai-studio/gemini-2.5-flash",
                "google-ai-studio/gemini-2.5-pro",
                "grok/grok-4",
                "groq/llama-3.3-70b-versatile",
                "groq/llama-3.1-8b-instant",
                "openai/gpt-5",
                "openai/gpt-5-mini",
                "openai/gpt-5-nano",
              ]),
              z.enum([""]),
            ]).optional(),
            rewrite_prompt: z.string().optional(),
          }).optional(),
          reranking: z.object({
            enabled: z.boolean().optional(),
            match_threshold: z.number().min(0).max(1).optional(),
            model: z.union([
              z.enum(["@cf/baai/bge-reranker-base"]),
              z.enum([""]),
            ]).optional(),
          }).optional(),
          retrieval: z.object({
            boost_by: z.array(z.object({
              direction: z.enum(["asc", "desc", "exists", "not_exists"])
                .optional(),
              field: z.string().min(1).max(64),
            })).optional(),
            context_expansion: z.number().int().min(0).max(3).optional(),
            filters: z.record(z.string(), z.unknown()).optional(),
            fusion_method: z.enum(["max", "rrf"]).optional(),
            keyword_match_mode: z.enum(["and", "or"]).optional(),
            match_threshold: z.number().min(0).max(1).optional(),
            max_num_results: z.number().int().min(1).max(50).optional(),
            retrieval_type: z.enum(["vector", "keyword", "hybrid"]).optional(),
            return_on_failure: z.boolean().optional(),
          }).optional(),
        }).optional(),
        messages: z.array(z.object({
          content: z.union([
            z.string(),
            z.array(z.union([
              z.object({
                text: z.string().min(1),
                type: z.enum(["text"]),
              }),
              z.object({
                image_url: z.object({
                  url: z.unknown(),
                }),
                type: z.enum(["image_url"]),
              }),
            ])),
            z.unknown().nullable(),
          ]),
          role: z.enum(["system", "developer", "user", "assistant", "tool"]),
        })).optional().describe(
          "OpenAI-compatible message array. For multimodal queries, set the last user me...",
        ),
        query: z.string().min(1).optional().describe(
          "A simple text query string. Alternative to 'messages' — provide either this o...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai-search/instances/${args.id}/search`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "ai_search_instance_search",
          id,
          result,
        );
        context.logger.info("Created ai_search_instance_search {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_ai_search_stats: {
      description: "Get instance statistics.",
      arguments: z.object({
        id: z.string().describe(
          "AI Search instance ID. Lowercase alphanumeric, hyphens, and underscores.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai-search/instances/${args.id}/stats`,
        );

        const handle = await context.writeResource(
          "ai_search_stats",
          String(args.id),
          result,
        );
        context.logger.info("Fetched ai_search_stats", {});
        return { dataHandles: [handle] };
      },
    },
    list_namespaces: {
      description: "List namespaces.",
      arguments: z.object({
        page: z.number().optional().describe("Page number (1-indexed)."),
        per_page: z.number().optional().describe("Number of results per page."),
        search: z.string().optional().describe(
          "Filter namespaces whose name or description contains this string (case-insensitive).",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/ai-search/namespaces`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("namespaces", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} namespaces", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_namespace: {
      description: "Create namespace.",
      arguments: z.object({
        description: z.string().max(256).nullable().optional().describe(
          "Optional description for the namespace. Max 256 characters.",
        ),
        name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai-search/namespaces`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource("namespace", id, result);
        context.logger.info("Created namespace {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_ai_search_fetch_namespace: {
      description: "Read namespace.",
      arguments: z.object({
        name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai-search/namespaces/${args.name}`,
        );

        const handle = await context.writeResource(
          "ai_search_fetch_namespace",
          String(args.name),
          result,
        );
        context.logger.info("Fetched ai_search_fetch_namespace", {});
        return { dataHandles: [handle] };
      },
    },
    update_namespace: {
      description: "Update namespace.",
      arguments: z.object({
        name: z.string(),
        description: z.string().max(256).nullable().optional().describe(
          "Optional description for the namespace. Max 256 characters.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["name"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/ai-search/namespaces/${args.name}`,
          body,
        );

        const handle = await context.writeResource(
          "namespace",
          String(args.name),
          result,
        );
        context.logger.info("Updated namespace", {});
        return { dataHandles: [handle] };
      },
    },
    delete_namespace: {
      description: "Delete namespace.",
      arguments: z.object({
        name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/ai-search/namespaces/${args.name}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.name });
        return { dataHandles: [] };
      },
    },
    create_ai_search_namespace_multi_instance_chat_completion: {
      description: "Multi-Instance Chat Completions",
      arguments: z.object({
        name: z.string().describe("Namespace name"),
        ai_search_options: z.object({
          cache: z.object({
            cache_threshold: z.enum([
              "super_strict_match",
              "close_enough",
              "flexible_friend",
              "anything_goes",
            ]).optional(),
            enabled: z.boolean().optional(),
          }).optional(),
          instance_ids: z.array(z.string().min(1).max(64)),
          query_rewrite: z.object({
            enabled: z.boolean().optional(),
            model: z.union([
              z.enum([
                "@cf/meta/llama-3.3-70b-instruct-fp8-fast",
                "@cf/zai-org/glm-4.7-flash",
                "@cf/meta/llama-3.1-8b-instruct-fast",
                "@cf/meta/llama-3.1-8b-instruct-fp8",
                "@cf/meta/llama-4-scout-17b-16e-instruct",
                "@cf/qwen/qwen3-30b-a3b-fp8",
                "@cf/deepseek-ai/deepseek-r1-distill-qwen-32b",
                "@cf/moonshotai/kimi-k2-instruct",
                "@cf/google/gemma-3-12b-it",
                "@cf/google/gemma-4-26b-a4b-it",
                "@cf/moonshotai/kimi-k2.5",
                "anthropic/claude-3-7-sonnet",
                "anthropic/claude-sonnet-4",
                "anthropic/claude-opus-4",
                "anthropic/claude-3-5-haiku",
                "cerebras/qwen-3-235b-a22b-instruct",
                "cerebras/qwen-3-235b-a22b-thinking",
                "cerebras/llama-3.3-70b",
                "cerebras/llama-4-maverick-17b-128e-instruct",
                "cerebras/llama-4-scout-17b-16e-instruct",
                "cerebras/gpt-oss-120b",
                "google-ai-studio/gemini-2.5-flash",
                "google-ai-studio/gemini-2.5-pro",
                "grok/grok-4",
                "groq/llama-3.3-70b-versatile",
                "groq/llama-3.1-8b-instant",
                "openai/gpt-5",
                "openai/gpt-5-mini",
                "openai/gpt-5-nano",
              ]),
              z.enum([""]),
            ]).optional(),
            rewrite_prompt: z.string().optional(),
          }).optional(),
          reranking: z.object({
            enabled: z.boolean().optional(),
            match_threshold: z.number().min(0).max(1).optional(),
            model: z.union([
              z.enum(["@cf/baai/bge-reranker-base"]),
              z.enum([""]),
            ]).optional(),
          }).optional(),
          retrieval: z.object({
            boost_by: z.array(z.object({
              direction: z.enum(["asc", "desc", "exists", "not_exists"])
                .optional(),
              field: z.string().min(1).max(64),
            })).optional(),
            context_expansion: z.number().int().min(0).max(3).optional(),
            filters: z.record(z.string(), z.unknown()).optional(),
            fusion_method: z.enum(["max", "rrf"]).optional(),
            keyword_match_mode: z.enum(["and", "or"]).optional(),
            match_threshold: z.number().min(0).max(1).optional(),
            max_num_results: z.number().int().min(1).max(50).optional(),
            retrieval_type: z.enum(["vector", "keyword", "hybrid"]).optional(),
            return_on_failure: z.boolean().optional(),
          }).optional(),
        }),
        messages: z.array(z.object({
          content: z.union([
            z.string(),
            z.array(z.union([
              z.object({
                text: z.string().min(1),
                type: z.enum(["text"]),
              }),
              z.object({
                image_url: z.object({
                  url: z.unknown(),
                }),
                type: z.enum(["image_url"]),
              }),
            ])),
            z.unknown().nullable(),
          ]),
          role: z.enum(["system", "developer", "user", "assistant", "tool"]),
        })),
        model: z.union([
          z.enum([
            "@cf/meta/llama-3.3-70b-instruct-fp8-fast",
            "@cf/zai-org/glm-4.7-flash",
            "@cf/meta/llama-3.1-8b-instruct-fast",
            "@cf/meta/llama-3.1-8b-instruct-fp8",
            "@cf/meta/llama-4-scout-17b-16e-instruct",
            "@cf/qwen/qwen3-30b-a3b-fp8",
            "@cf/deepseek-ai/deepseek-r1-distill-qwen-32b",
            "@cf/moonshotai/kimi-k2-instruct",
            "@cf/google/gemma-3-12b-it",
            "@cf/google/gemma-4-26b-a4b-it",
            "@cf/moonshotai/kimi-k2.5",
            "anthropic/claude-3-7-sonnet",
            "anthropic/claude-sonnet-4",
            "anthropic/claude-opus-4",
            "anthropic/claude-3-5-haiku",
            "cerebras/qwen-3-235b-a22b-instruct",
            "cerebras/qwen-3-235b-a22b-thinking",
            "cerebras/llama-3.3-70b",
            "cerebras/llama-4-maverick-17b-128e-instruct",
            "cerebras/llama-4-scout-17b-16e-instruct",
            "cerebras/gpt-oss-120b",
            "google-ai-studio/gemini-2.5-flash",
            "google-ai-studio/gemini-2.5-pro",
            "grok/grok-4",
            "groq/llama-3.3-70b-versatile",
            "groq/llama-3.1-8b-instant",
            "openai/gpt-5",
            "openai/gpt-5-mini",
            "openai/gpt-5-nano",
          ]),
          z.enum([""]),
        ]).optional(),
        stream: z.boolean().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["name"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai-search/namespaces/${args.name}/chat/completions`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "ai_search_namespace_multi_instance_chat_completion",
          id,
          result,
        );
        context.logger.info(
          "Created ai_search_namespace_multi_instance_chat_completion {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    get_ai_search_namespace_fetch_instance: {
      description: "Get an AI Search instance.",
      arguments: z.object({
        id: z.string(),
        name: z.string().describe("Namespace name"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai-search/namespaces/${args.name}/instances/${args.id}`,
        );

        const handle = await context.writeResource(
          "ai_search_namespace_fetch_instance",
          String(args.name),
          result,
        );
        context.logger.info("Fetched ai_search_namespace_fetch_instance", {});
        return { dataHandles: [handle] };
      },
    },
    update_ai_search_move_instance: {
      description: "Move an instance to a different namespace.",
      arguments: z.object({
        name: z.string().describe("Current namespace of the instance."),
        id: z.string().describe("Instance id."),
        new_namespace: z.string().describe(
          "Target namespace to move the instance into.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["name", "id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/accounts/${accountId}/ai-search/namespaces/${args.name}/instances/${args.id}`,
          body,
        );

        const handle = await context.writeResource(
          "ai_search_move_instance",
          String(args.id),
          result,
        );
        context.logger.info("Updated ai_search_move_instance", {});
        return { dataHandles: [handle] };
      },
    },
    create_ai_search_namespace_instance_chat_completion: {
      description: "Chat Completions",
      arguments: z.object({
        id: z.string().describe(
          "AI Search instance ID. Lowercase alphanumeric, hyphens, and underscores.",
        ),
        name: z.string().describe("Namespace name"),
        ai_search_options: z.object({
          cache: z.object({
            cache_threshold: z.enum([
              "super_strict_match",
              "close_enough",
              "flexible_friend",
              "anything_goes",
            ]).optional(),
            enabled: z.boolean().optional(),
          }).optional(),
          query_rewrite: z.object({
            enabled: z.boolean().optional(),
            model: z.union([
              z.enum([
                "@cf/meta/llama-3.3-70b-instruct-fp8-fast",
                "@cf/zai-org/glm-4.7-flash",
                "@cf/meta/llama-3.1-8b-instruct-fast",
                "@cf/meta/llama-3.1-8b-instruct-fp8",
                "@cf/meta/llama-4-scout-17b-16e-instruct",
                "@cf/qwen/qwen3-30b-a3b-fp8",
                "@cf/deepseek-ai/deepseek-r1-distill-qwen-32b",
                "@cf/moonshotai/kimi-k2-instruct",
                "@cf/google/gemma-3-12b-it",
                "@cf/google/gemma-4-26b-a4b-it",
                "@cf/moonshotai/kimi-k2.5",
                "anthropic/claude-3-7-sonnet",
                "anthropic/claude-sonnet-4",
                "anthropic/claude-opus-4",
                "anthropic/claude-3-5-haiku",
                "cerebras/qwen-3-235b-a22b-instruct",
                "cerebras/qwen-3-235b-a22b-thinking",
                "cerebras/llama-3.3-70b",
                "cerebras/llama-4-maverick-17b-128e-instruct",
                "cerebras/llama-4-scout-17b-16e-instruct",
                "cerebras/gpt-oss-120b",
                "google-ai-studio/gemini-2.5-flash",
                "google-ai-studio/gemini-2.5-pro",
                "grok/grok-4",
                "groq/llama-3.3-70b-versatile",
                "groq/llama-3.1-8b-instant",
                "openai/gpt-5",
                "openai/gpt-5-mini",
                "openai/gpt-5-nano",
              ]),
              z.enum([""]),
            ]).optional(),
            rewrite_prompt: z.string().optional(),
          }).optional(),
          reranking: z.object({
            enabled: z.boolean().optional(),
            match_threshold: z.number().min(0).max(1).optional(),
            model: z.union([
              z.enum(["@cf/baai/bge-reranker-base"]),
              z.enum([""]),
            ]).optional(),
          }).optional(),
          retrieval: z.object({
            boost_by: z.array(z.object({
              direction: z.enum(["asc", "desc", "exists", "not_exists"])
                .optional(),
              field: z.string().min(1).max(64),
            })).optional(),
            context_expansion: z.number().int().min(0).max(3).optional(),
            filters: z.record(z.string(), z.unknown()).optional(),
            fusion_method: z.enum(["max", "rrf"]).optional(),
            keyword_match_mode: z.enum(["and", "or"]).optional(),
            match_threshold: z.number().min(0).max(1).optional(),
            max_num_results: z.number().int().min(1).max(50).optional(),
            retrieval_type: z.enum(["vector", "keyword", "hybrid"]).optional(),
            return_on_failure: z.boolean().optional(),
          }).optional(),
        }).optional(),
        messages: z.array(z.object({
          content: z.union([
            z.string(),
            z.array(z.union([
              z.object({
                text: z.string().min(1),
                type: z.enum(["text"]),
              }),
              z.object({
                image_url: z.object({
                  url: z.unknown(),
                }),
                type: z.enum(["image_url"]),
              }),
            ])),
            z.unknown().nullable(),
          ]),
          role: z.enum(["system", "developer", "user", "assistant", "tool"]),
        })),
        model: z.union([
          z.enum([
            "@cf/meta/llama-3.3-70b-instruct-fp8-fast",
            "@cf/zai-org/glm-4.7-flash",
            "@cf/meta/llama-3.1-8b-instruct-fast",
            "@cf/meta/llama-3.1-8b-instruct-fp8",
            "@cf/meta/llama-4-scout-17b-16e-instruct",
            "@cf/qwen/qwen3-30b-a3b-fp8",
            "@cf/deepseek-ai/deepseek-r1-distill-qwen-32b",
            "@cf/moonshotai/kimi-k2-instruct",
            "@cf/google/gemma-3-12b-it",
            "@cf/google/gemma-4-26b-a4b-it",
            "@cf/moonshotai/kimi-k2.5",
            "anthropic/claude-3-7-sonnet",
            "anthropic/claude-sonnet-4",
            "anthropic/claude-opus-4",
            "anthropic/claude-3-5-haiku",
            "cerebras/qwen-3-235b-a22b-instruct",
            "cerebras/qwen-3-235b-a22b-thinking",
            "cerebras/llama-3.3-70b",
            "cerebras/llama-4-maverick-17b-128e-instruct",
            "cerebras/llama-4-scout-17b-16e-instruct",
            "cerebras/gpt-oss-120b",
            "google-ai-studio/gemini-2.5-flash",
            "google-ai-studio/gemini-2.5-pro",
            "grok/grok-4",
            "groq/llama-3.3-70b-versatile",
            "groq/llama-3.1-8b-instant",
            "openai/gpt-5",
            "openai/gpt-5-mini",
            "openai/gpt-5-nano",
          ]),
          z.enum([""]),
        ]).optional(),
        stream: z.boolean().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["id", "name"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai-search/namespaces/${args.name}/instances/${args.id}/chat/completions`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "ai_search_namespace_instance_chat_completion",
          id,
          result,
        );
        context.logger.info(
          "Created ai_search_namespace_instance_chat_completion {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    list_items: {
      description: "Items List.",
      arguments: z.object({
        id: z.string().describe(
          "AI Search instance ID. Lowercase alphanumeric, hyphens, and underscores.",
        ),
        name: z.string().describe("Namespace name"),
        page: z.number().optional(),
        per_page: z.number().optional(),
        search: z.string().optional(),
        sort_by: z.enum(["status", "modified_at"]).optional().describe(
          'Sort order for items. "status" (default) sorts by status priority then last_seen_at. "modified_at" sorts by file modification time (most recent first), falling back to created_at.',
        ),
        status: z.enum([
          "queued",
          "running",
          "completed",
          "error",
          "skipped",
          "outdated",
        ]).optional(),
        source: z.string().optional().describe(
          'Filter items by source_id. Use "builtin" for uploaded files, or a source identifier like "web-crawler:https://example.com".',
        ),
        metadata_filter: z.string().optional().describe(
          'JSON-encoded metadata filter using Vectorize filter syntax. Examples: {"folder":"reports/"}, {"timestamp":{"$gte":1700000000000}}, {"folder":{"$in":["docs/","reports/"]}}',
        ),
        item_id: z.string().optional().describe(
          "Filter items by their unique ID. Returns at most one item.",
        ),
        key: z.string().optional().describe(
          "Filter items by their exact key (object key / filename). Keys are unique per source, so combine with `source` to disambiguate across data sources.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["id", "name", "page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/ai-search/namespaces/${args.name}/instances/${args.id}/items`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("items", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} items", { count: results.length });
        return { dataHandles: [handle] };
      },
    },
    ai_search_namespace_instance_upload_item: {
      description: "Upload Item.",
      arguments: z.object({
        id: z.string().describe(
          "AI Search instance ID. Lowercase alphanumeric, hyphens, and underscores.",
        ),
        name: z.string().describe("Namespace name"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai-search/namespaces/${args.name}/instances/${args.id}/items`,
        );

        const handle = await context.writeResource(
          "ai_search_namespace_instance_upload_item",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed ai_search_namespace_instance_upload_item",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    create_or_update_item: {
      description: "Create or Update Item.",
      arguments: z.object({
        id: z.string().describe(
          "AI Search instance ID. Lowercase alphanumeric, hyphens, and underscores.",
        ),
        name: z.string().describe("Namespace name"),
        key: z.string().describe(
          "Item key / filename. Must not exceed 128 characters.",
        ),
        next_action: z.enum(["INDEX"]),
        wait_for_completion: z.boolean().optional().describe(
          "Wait for indexing to fully complete before responding. On RAGs with vector in...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["id", "name"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/ai-search/namespaces/${args.name}/instances/${args.id}/items`,
          body,
        );

        const handle = await context.writeResource(
          "create_or_update_item",
          String(args.name),
          result,
        );
        context.logger.info("Updated create_or_update_item", {});
        return { dataHandles: [handle] };
      },
    },
    get_item: {
      description: "Get Item.",
      arguments: z.object({
        id: z.string().describe(
          "AI Search instance ID. Lowercase alphanumeric, hyphens, and underscores.",
        ),
        item_id: z.string(),
        name: z.string().describe("Namespace name"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai-search/namespaces/${args.name}/instances/${args.id}/items/${args.item_id}`,
        );

        const handle = await context.writeResource(
          "item",
          String(args.name),
          result,
        );
        context.logger.info("Fetched item", {});
        return { dataHandles: [handle] };
      },
    },
    update_ai_search_namespace_instance_sync_item: {
      description: "Sync Item.",
      arguments: z.object({
        id: z.string().describe(
          "AI Search instance ID. Lowercase alphanumeric, hyphens, and underscores.",
        ),
        item_id: z.string(),
        name: z.string().describe("Namespace name"),
        next_action: z.enum(["INDEX"]),
        wait_for_completion: z.boolean().optional().describe(
          "Wait for indexing to fully complete before responding. On RAGs with vector in...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["id", "item_id", "name"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/accounts/${accountId}/ai-search/namespaces/${args.name}/instances/${args.id}/items/${args.item_id}`,
          body,
        );

        const handle = await context.writeResource(
          "ai_search_namespace_instance_sync_item",
          String(args.name),
          result,
        );
        context.logger.info(
          "Updated ai_search_namespace_instance_sync_item",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    delete_item: {
      description: "Delete Item.",
      arguments: z.object({
        id: z.string().describe(
          "AI Search instance ID. Lowercase alphanumeric, hyphens, and underscores.",
        ),
        item_id: z.string(),
        name: z.string().describe("Namespace name"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/ai-search/namespaces/${args.name}/instances/${args.id}/items/${args.item_id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.name });
        return { dataHandles: [] };
      },
    },
    list_item_chunks: {
      description: "List Item Chunks.",
      arguments: z.object({
        id: z.string().describe(
          "AI Search instance ID. Lowercase alphanumeric, hyphens, and underscores.",
        ),
        item_id: z.string(),
        name: z.string().describe("Namespace name"),
        limit: z.number().optional(),
        offset: z.number().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set([
          "id",
          "item_id",
          "name",
          "page",
          "per_page",
        ]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/ai-search/namespaces/${args.name}/instances/${args.id}/items/${args.item_id}/chunks`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("item_chunks", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} item_chunks", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    get_item_content: {
      description: "Download Item Content.",
      arguments: z.object({
        id: z.string().describe(
          "AI Search instance ID. Lowercase alphanumeric, hyphens, and underscores.",
        ),
        item_id: z.string(),
        name: z.string().describe("Namespace name"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai-search/namespaces/${args.name}/instances/${args.id}/items/${args.item_id}/download`,
        );

        const handle = await context.writeResource(
          "item_content",
          String(args.name),
          result,
        );
        context.logger.info("Fetched item_content", {});
        return { dataHandles: [handle] };
      },
    },
    list_ai_search_namespace_instance_logs_item: {
      description: "Item Logs.",
      arguments: z.object({
        id: z.string().describe(
          "AI Search instance ID. Lowercase alphanumeric, hyphens, and underscores.",
        ),
        item_id: z.string(),
        name: z.string().describe("Namespace name"),
        limit: z.number().optional(),
        cursor: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set([
          "id",
          "item_id",
          "name",
          "page",
          "per_page",
        ]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/ai-search/namespaces/${args.name}/instances/${args.id}/items/${args.item_id}/logs`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "ai_search_namespace_instance_logs_item",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info(
          "Found {count} ai_search_namespace_instance_logs_item",
          { count: results.length },
        );
        return { dataHandles: [handle] };
      },
    },
    update_ai_search_namespace_instance_change_job_status: {
      description: "Cancel an indexing job.",
      arguments: z.object({
        id: z.string().describe(
          "AI Search instance ID. Lowercase alphanumeric, hyphens, and underscores.",
        ),
        job_id: z.string(),
        name: z.string().describe("Namespace name"),
        action: z.enum(["cancel"]),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["id", "job_id", "name"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/accounts/${accountId}/ai-search/namespaces/${args.name}/instances/${args.id}/jobs/${args.job_id}`,
          body,
        );

        const handle = await context.writeResource(
          "ai_search_namespace_instance_change_job_status",
          String(args.name),
          result,
        );
        context.logger.info(
          "Updated ai_search_namespace_instance_change_job_status",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    create_ai_search_namespace_instance_search: {
      description: "Search",
      arguments: z.object({
        id: z.string().describe(
          "AI Search instance ID. Lowercase alphanumeric, hyphens, and underscores.",
        ),
        name: z.string().describe("Namespace name"),
        ai_search_options: z.object({
          cache: z.object({
            cache_threshold: z.enum([
              "super_strict_match",
              "close_enough",
              "flexible_friend",
              "anything_goes",
            ]).optional(),
            enabled: z.boolean().optional(),
          }).optional(),
          query_rewrite: z.object({
            enabled: z.boolean().optional(),
            model: z.union([
              z.enum([
                "@cf/meta/llama-3.3-70b-instruct-fp8-fast",
                "@cf/zai-org/glm-4.7-flash",
                "@cf/meta/llama-3.1-8b-instruct-fast",
                "@cf/meta/llama-3.1-8b-instruct-fp8",
                "@cf/meta/llama-4-scout-17b-16e-instruct",
                "@cf/qwen/qwen3-30b-a3b-fp8",
                "@cf/deepseek-ai/deepseek-r1-distill-qwen-32b",
                "@cf/moonshotai/kimi-k2-instruct",
                "@cf/google/gemma-3-12b-it",
                "@cf/google/gemma-4-26b-a4b-it",
                "@cf/moonshotai/kimi-k2.5",
                "anthropic/claude-3-7-sonnet",
                "anthropic/claude-sonnet-4",
                "anthropic/claude-opus-4",
                "anthropic/claude-3-5-haiku",
                "cerebras/qwen-3-235b-a22b-instruct",
                "cerebras/qwen-3-235b-a22b-thinking",
                "cerebras/llama-3.3-70b",
                "cerebras/llama-4-maverick-17b-128e-instruct",
                "cerebras/llama-4-scout-17b-16e-instruct",
                "cerebras/gpt-oss-120b",
                "google-ai-studio/gemini-2.5-flash",
                "google-ai-studio/gemini-2.5-pro",
                "grok/grok-4",
                "groq/llama-3.3-70b-versatile",
                "groq/llama-3.1-8b-instant",
                "openai/gpt-5",
                "openai/gpt-5-mini",
                "openai/gpt-5-nano",
              ]),
              z.enum([""]),
            ]).optional(),
            rewrite_prompt: z.string().optional(),
          }).optional(),
          reranking: z.object({
            enabled: z.boolean().optional(),
            match_threshold: z.number().min(0).max(1).optional(),
            model: z.union([
              z.enum(["@cf/baai/bge-reranker-base"]),
              z.enum([""]),
            ]).optional(),
          }).optional(),
          retrieval: z.object({
            boost_by: z.array(z.object({
              direction: z.enum(["asc", "desc", "exists", "not_exists"])
                .optional(),
              field: z.string().min(1).max(64),
            })).optional(),
            context_expansion: z.number().int().min(0).max(3).optional(),
            filters: z.record(z.string(), z.unknown()).optional(),
            fusion_method: z.enum(["max", "rrf"]).optional(),
            keyword_match_mode: z.enum(["and", "or"]).optional(),
            match_threshold: z.number().min(0).max(1).optional(),
            max_num_results: z.number().int().min(1).max(50).optional(),
            retrieval_type: z.enum(["vector", "keyword", "hybrid"]).optional(),
            return_on_failure: z.boolean().optional(),
          }).optional(),
        }).optional(),
        messages: z.array(z.object({
          content: z.union([
            z.string(),
            z.array(z.union([
              z.object({
                text: z.string().min(1),
                type: z.enum(["text"]),
              }),
              z.object({
                image_url: z.object({
                  url: z.unknown(),
                }),
                type: z.enum(["image_url"]),
              }),
            ])),
            z.unknown().nullable(),
          ]),
          role: z.enum(["system", "developer", "user", "assistant", "tool"]),
        })).optional().describe(
          "OpenAI-compatible message array. For multimodal queries, set the last user me...",
        ),
        query: z.string().min(1).optional().describe(
          "A simple text query string. Alternative to 'messages' — provide either this o...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["id", "name"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai-search/namespaces/${args.name}/instances/${args.id}/search`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "ai_search_namespace_instance_search",
          id,
          result,
        );
        context.logger.info(
          "Created ai_search_namespace_instance_search {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    get_ai_search_namespace_stats: {
      description: "Get instance statistics.",
      arguments: z.object({
        id: z.string().describe(
          "AI Search instance ID. Lowercase alphanumeric, hyphens, and underscores.",
        ),
        name: z.string().describe("Namespace name"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai-search/namespaces/${args.name}/instances/${args.id}/stats`,
        );

        const handle = await context.writeResource(
          "ai_search_namespace_stats",
          String(args.name),
          result,
        );
        context.logger.info("Fetched ai_search_namespace_stats", {});
        return { dataHandles: [handle] };
      },
    },
    create_ai_search_namespace_multi_instance_search: {
      description: "Multi-Instance Search",
      arguments: z.object({
        name: z.string().describe("Namespace name"),
        ai_search_options: z.object({
          cache: z.object({
            cache_threshold: z.enum([
              "super_strict_match",
              "close_enough",
              "flexible_friend",
              "anything_goes",
            ]).optional(),
            enabled: z.boolean().optional(),
          }).optional(),
          instance_ids: z.array(z.string().min(1).max(64)),
          query_rewrite: z.object({
            enabled: z.boolean().optional(),
            model: z.union([
              z.enum([
                "@cf/meta/llama-3.3-70b-instruct-fp8-fast",
                "@cf/zai-org/glm-4.7-flash",
                "@cf/meta/llama-3.1-8b-instruct-fast",
                "@cf/meta/llama-3.1-8b-instruct-fp8",
                "@cf/meta/llama-4-scout-17b-16e-instruct",
                "@cf/qwen/qwen3-30b-a3b-fp8",
                "@cf/deepseek-ai/deepseek-r1-distill-qwen-32b",
                "@cf/moonshotai/kimi-k2-instruct",
                "@cf/google/gemma-3-12b-it",
                "@cf/google/gemma-4-26b-a4b-it",
                "@cf/moonshotai/kimi-k2.5",
                "anthropic/claude-3-7-sonnet",
                "anthropic/claude-sonnet-4",
                "anthropic/claude-opus-4",
                "anthropic/claude-3-5-haiku",
                "cerebras/qwen-3-235b-a22b-instruct",
                "cerebras/qwen-3-235b-a22b-thinking",
                "cerebras/llama-3.3-70b",
                "cerebras/llama-4-maverick-17b-128e-instruct",
                "cerebras/llama-4-scout-17b-16e-instruct",
                "cerebras/gpt-oss-120b",
                "google-ai-studio/gemini-2.5-flash",
                "google-ai-studio/gemini-2.5-pro",
                "grok/grok-4",
                "groq/llama-3.3-70b-versatile",
                "groq/llama-3.1-8b-instant",
                "openai/gpt-5",
                "openai/gpt-5-mini",
                "openai/gpt-5-nano",
              ]),
              z.enum([""]),
            ]).optional(),
            rewrite_prompt: z.string().optional(),
          }).optional(),
          reranking: z.object({
            enabled: z.boolean().optional(),
            match_threshold: z.number().min(0).max(1).optional(),
            model: z.union([
              z.enum(["@cf/baai/bge-reranker-base"]),
              z.enum([""]),
            ]).optional(),
          }).optional(),
          retrieval: z.object({
            boost_by: z.array(z.object({
              direction: z.enum(["asc", "desc", "exists", "not_exists"])
                .optional(),
              field: z.string().min(1).max(64),
            })).optional(),
            context_expansion: z.number().int().min(0).max(3).optional(),
            filters: z.record(z.string(), z.unknown()).optional(),
            fusion_method: z.enum(["max", "rrf"]).optional(),
            keyword_match_mode: z.enum(["and", "or"]).optional(),
            match_threshold: z.number().min(0).max(1).optional(),
            max_num_results: z.number().int().min(1).max(50).optional(),
            retrieval_type: z.enum(["vector", "keyword", "hybrid"]).optional(),
            return_on_failure: z.boolean().optional(),
          }).optional(),
        }),
        messages: z.array(z.object({
          content: z.union([
            z.string(),
            z.array(z.union([
              z.object({
                text: z.string().min(1),
                type: z.enum(["text"]),
              }),
              z.object({
                image_url: z.object({
                  url: z.unknown(),
                }),
                type: z.enum(["image_url"]),
              }),
            ])),
            z.unknown().nullable(),
          ]),
          role: z.enum(["system", "developer", "user", "assistant", "tool"]),
        })).optional().describe(
          "OpenAI-compatible message array. For multimodal queries, set the last user me...",
        ),
        query: z.string().min(1).optional().describe(
          "A simple text query string. Alternative to 'messages' — provide either this o...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["name"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai-search/namespaces/${args.name}/search`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "ai_search_namespace_multi_instance_search",
          id,
          result,
        );
        context.logger.info(
          "Created ai_search_namespace_multi_instance_search {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    list_tokens: {
      description: "List tokens.",
      arguments: z.object({
        page: z.number().optional().describe("Page number (1-indexed)."),
        per_page: z.number().optional().describe("Number of results per page."),
        search: z.string().optional().describe(
          "Filter tokens whose name contains this string (case-insensitive).",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/ai-search/tokens`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("tokens", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} tokens", { count: results.length });
        return { dataHandles: [handle] };
      },
    },
    create_tokens: {
      description: "Create token.",
      arguments: z.object({
        cf_api_id: z.string(),
        cf_api_key: z.string(),
        legacy: z.boolean().optional(),
        name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai-search/tokens`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource("tokens", id, result);
        context.logger.info("Created tokens {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_ai_search_fetch_tokens: {
      description: "Read token.",
      arguments: z.object({
        id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai-search/tokens/${args.id}`,
        );

        const handle = await context.writeResource(
          "ai_search_fetch_tokens",
          String(args.id),
          result,
        );
        context.logger.info("Fetched ai_search_fetch_tokens", {});
        return { dataHandles: [handle] };
      },
    },
    update_tokens: {
      description: "Update token.",
      arguments: z.object({
        id: z.string(),
        cf_api_id: z.string(),
        cf_api_key: z.string(),
        legacy: z.boolean().optional(),
        name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/ai-search/tokens/${args.id}`,
          body,
        );

        const handle = await context.writeResource(
          "tokens",
          String(args.id),
          result,
        );
        context.logger.info("Updated tokens", {});
        return { dataHandles: [handle] };
      },
    },
    delete_tokens: {
      description: "Delete token.",
      arguments: z.object({
        id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/ai-search/tokens/${args.id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.id });
        return { dataHandles: [] };
      },
    },
    list_workers_ai_search_author: {
      description: "Author Search",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/ai/authors/search`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "workers_ai_search_author",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} workers_ai_search_author", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    list_finetunes: {
      description: "List Finetunes",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai/finetunes`,
        );

        const handle = await context.writeResource(
          "list_finetunes",
          "latest",
          result,
        );
        context.logger.info("Fetched list_finetunes", {});
        return { dataHandles: [handle] };
      },
    },
    create_finetune: {
      description: "Create a new Finetune",
      arguments: z.object({
        description: z.string().optional(),
        model: z.string(),
        name: z.string(),
        public: z.boolean().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/finetunes`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource("finetune", id, result);
        context.logger.info("Created finetune {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    list_public_finetunes: {
      description: "List Public Finetunes",
      arguments: z.object({
        limit: z.number().optional().describe("Pagination Limit"),
        offset: z.number().optional().describe("Pagination Offset"),
        orderBy: z.string().optional().describe("Order By Column Name"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/ai/finetunes/public`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("public_finetunes", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} public_finetunes", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    workers_ai_upload_finetune_asset: {
      description: "Upload a Finetune Asset",
      arguments: z.object({
        finetune_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/finetunes/${args.finetune_id}/finetune-assets`,
        );

        const handle = await context.writeResource(
          "workers_ai_upload_finetune_asset",
          "latest",
          result ?? {},
        );
        context.logger.info("Executed workers_ai_upload_finetune_asset", {});
        return { dataHandles: [handle] };
      },
    },
    get_model_schema: {
      description: "Get Model Schema",
      arguments: z.object({
        model: z.string().optional().describe("Model Name"),
      }),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai/models/schema`,
        );

        const handle = await context.writeResource(
          "model_schema",
          "latest",
          result,
        );
        context.logger.info("Fetched model_schema", {});
        return { dataHandles: [handle] };
      },
    },
    get_workers_ai_search_model: {
      description: "Model Search",
      arguments: z.object({
        per_page: z.number().optional(),
        page: z.number().optional(),
        task: z.string().optional().describe("Filter by Task Name"),
        author: z.string().optional().describe("Filter by Author"),
        source: z.number().optional().describe("Filter by Source Id"),
        hide_experimental: z.boolean().optional().describe(
          "Filter to hide experimental models",
        ),
        search: z.string().optional().describe("Search"),
        include_deprecated: z.boolean().optional().describe(
          "If true, include models whose planned_deprecation_date is in the past — but only within a three-month grace window after that date. Models whose planned_deprecation_date is more than three months in the past remain hidden regardless of this flag. Future planned-deprecation dates are always included regardless of this flag. Defaults to false, preserving the existing behavior of hiding all past-dated deprecations.",
        ),
        format: z.enum(["openrouter"]).optional().describe(
          "If set, return models in the requested marketplace format instead of the default response.",
        ),
      }),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai/models/search`,
        );

        const handle = await context.writeResource(
          "workers_ai_search_model",
          "latest",
          result,
        );
        context.logger.info("Fetched workers_ai_search_model", {});
        return { dataHandles: [handle] };
      },
    },
    create_workers_ai_post_run_generic: {
      description: "Execute AI Model (Generic)",
      arguments: z.object({
        input: z.object({}).describe(
          "Model-specific input data. Format varies by model type.",
        ),
        model: z.string().describe(
          "The AI model to execute (e.g., openai/gpt-5.5, anthropic/claude-opus-4.7)",
        ),
        options: z.object({
          extraHeaders: z.record(z.string(), z.string()).optional(),
          gateway: z.object({
            cacheTtl: z.number().optional(),
            id: z.string().optional(),
            skipCache: z.boolean().optional(),
          }).optional(),
        }).optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "workers_ai_post_run_generic",
          id,
          result,
        );
        context.logger.info("Created workers_ai_post_run_generic {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    create_workers_ai_post_run_cf_ai4bharat_indictrans2_en_indic_1b: {
      description: "Execute @cf/ai4bharat/indictrans2-en-indic-1B model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
        target_language: z.enum([
          "asm_Beng",
          "awa_Deva",
          "ben_Beng",
          "bho_Deva",
          "brx_Deva",
          "doi_Deva",
          "eng_Latn",
          "gom_Deva",
          "gon_Deva",
          "guj_Gujr",
          "hin_Deva",
          "hne_Deva",
          "kan_Knda",
          "kas_Arab",
          "kas_Deva",
          "kha_Latn",
          "lus_Latn",
          "mag_Deva",
          "mai_Deva",
          "mal_Mlym",
          "mar_Deva",
          "mni_Beng",
          "mni_Mtei",
          "npi_Deva",
          "ory_Orya",
          "pan_Guru",
          "san_Deva",
          "sat_Olck",
          "snd_Arab",
          "snd_Deva",
          "tam_Taml",
          "tel_Telu",
          "urd_Arab",
          "unr_Deva",
        ]).describe("Target langauge to translate to"),
        text: z.union([z.string(), z.array(z.string())]).describe(
          "Input text to translate. Can be a single string or a list of strings.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }
        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/ai4bharat/indictrans2-en-indic-1B${qs}`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "workers_ai_post_run_cf_ai4bharat_indictrans2_en_indic_1b",
          id,
          result,
        );
        context.logger.info(
          "Created workers_ai_post_run_cf_ai4bharat_indictrans2_en_indic_1b {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    create_workers_ai_post_run_cf_ai4bharat_nonomni_indictrans2_en_indic_1b: {
      description:
        "Execute @cf/ai4bharat/nonomni-indictrans2-en-indic-1b model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
        target_language: z.enum([
          "asm_Beng",
          "awa_Deva",
          "ben_Beng",
          "bho_Deva",
          "brx_Deva",
          "doi_Deva",
          "eng_Latn",
          "gom_Deva",
          "gon_Deva",
          "guj_Gujr",
          "hin_Deva",
          "hne_Deva",
          "kan_Knda",
          "kas_Arab",
          "kas_Deva",
          "kha_Latn",
          "lus_Latn",
          "mag_Deva",
          "mai_Deva",
          "mal_Mlym",
          "mar_Deva",
          "mni_Beng",
          "mni_Mtei",
          "npi_Deva",
          "ory_Orya",
          "pan_Guru",
          "san_Deva",
          "sat_Olck",
          "snd_Arab",
          "snd_Deva",
          "tam_Taml",
          "tel_Telu",
          "urd_Arab",
          "unr_Deva",
        ]).describe("Target langauge to translate to"),
        text: z.union([z.string(), z.array(z.string())]).describe(
          "Input text to translate. Can be a single string or a list of strings.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }
        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/ai4bharat/nonomni-indictrans2-en-indic-1b${qs}`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "workers_ai_post_run_cf_ai4bharat_nonomni_indictrans2_en_indic_1b",
          id,
          result,
        );
        context.logger.info(
          "Created workers_ai_post_run_cf_ai4bharat_nonomni_indictrans2_en_indic_1b {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_aisingapore_gemma_sea_lion_v4_27b_it: {
      description: "Execute @cf/aisingapore/gemma-sea-lion-v4-27b-it model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/aisingapore/gemma-sea-lion-v4-27b-it${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_aisingapore_gemma_sea_lion_v4_27b_it",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_aisingapore_gemma_sea_lion_v4_27b_it",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_baai_bge_base_en_v1_5: {
      description: "Execute @cf/baai/bge-base-en-v1.5 model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/baai/bge-base-en-v1.5${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_baai_bge_base_en_v1_5",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_baai_bge_base_en_v1_5",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_baai_bge_large_en_v1_5: {
      description: "Execute @cf/baai/bge-large-en-v1.5 model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/baai/bge-large-en-v1.5${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_baai_bge_large_en_v1_5",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_baai_bge_large_en_v1_5",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_baai_bge_m3: {
      description: "Execute @cf/baai/bge-m3 model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/baai/bge-m3${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_baai_bge_m3",
          "latest",
          result ?? {},
        );
        context.logger.info("Executed workers_ai_post_run_cf_baai_bge_m3", {});
        return { dataHandles: [handle] };
      },
    },
    create_workers_ai_post_run_cf_baai_bge_reranker_base: {
      description: "Execute @cf/baai/bge-reranker-base model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
        contexts: z.array(z.object({
          text: z.string().min(1).optional(),
        })).describe(
          "List of provided contexts. Note that the index in this array is important, as...",
        ),
        query: z.string().min(1).describe(
          "A query you wish to perform against the provided contexts.",
        ),
        top_k: z.number().int().min(1).optional().describe(
          "Number of returned results starting with the best score.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }
        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/baai/bge-reranker-base${qs}`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "workers_ai_post_run_cf_baai_bge_reranker_base",
          id,
          result,
        );
        context.logger.info(
          "Created workers_ai_post_run_cf_baai_bge_reranker_base {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_baai_bge_small_en_v1_5: {
      description: "Execute @cf/baai/bge-small-en-v1.5 model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/baai/bge-small-en-v1.5${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_baai_bge_small_en_v1_5",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_baai_bge_small_en_v1_5",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_baai_nonomni_bge_base_en_v1_5: {
      description: "Execute @cf/baai/nonomni-bge-base-en-v1.5 model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/baai/nonomni-bge-base-en-v1.5${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_baai_nonomni_bge_base_en_v1_5",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_baai_nonomni_bge_base_en_v1_5",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_baai_nonomni_bge_large_en_v1_5: {
      description: "Execute @cf/baai/nonomni-bge-large-en-v1.5 model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/baai/nonomni-bge-large-en-v1.5${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_baai_nonomni_bge_large_en_v1_5",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_baai_nonomni_bge_large_en_v1_5",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_baai_nonomni_bge_m3: {
      description: "Execute @cf/baai/nonomni-bge-m3 model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/baai/nonomni-bge-m3${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_baai_nonomni_bge_m3",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_baai_nonomni_bge_m3",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_baai_nonomni_bge_small_en_v1_5: {
      description: "Execute @cf/baai/nonomni-bge-small-en-v1.5 model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/baai/nonomni-bge-small-en-v1.5${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_baai_nonomni_bge_small_en_v1_5",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_baai_nonomni_bge_small_en_v1_5",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    create_workers_ai_post_run_cf_black_forest_labs_flux_1_schnell: {
      description: "Execute @cf/black-forest-labs/flux-1-schnell model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
        prompt: z.string().min(1).max(2048).describe(
          "A text description of the image you want to generate.",
        ),
        steps: z.number().int().max(8).optional().describe(
          "The number of diffusion steps; higher values can improve quality but take lon...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }
        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/black-forest-labs/flux-1-schnell${qs}`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "workers_ai_post_run_cf_black_forest_labs_flux_1_schnell",
          id,
          result,
        );
        context.logger.info(
          "Created workers_ai_post_run_cf_black_forest_labs_flux_1_schnell {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    create_workers_ai_post_run_cf_black_forest_labs_flux_2_dev: {
      description: "Execute @cf/black-forest-labs/flux-2-dev model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
        multipart: z.object({
          body: z.object({}).optional(),
          contentType: z.string().optional(),
        }),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }
        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/black-forest-labs/flux-2-dev${qs}`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "workers_ai_post_run_cf_black_forest_labs_flux_2_dev",
          id,
          result,
        );
        context.logger.info(
          "Created workers_ai_post_run_cf_black_forest_labs_flux_2_dev {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    create_workers_ai_post_run_cf_black_forest_labs_flux_2_klein_4b: {
      description: "Execute @cf/black-forest-labs/flux-2-klein-4b model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
        multipart: z.object({
          body: z.object({}).optional(),
          contentType: z.string().optional(),
        }),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }
        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/black-forest-labs/flux-2-klein-4b${qs}`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "workers_ai_post_run_cf_black_forest_labs_flux_2_klein_4b",
          id,
          result,
        );
        context.logger.info(
          "Created workers_ai_post_run_cf_black_forest_labs_flux_2_klein_4b {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    create_workers_ai_post_run_cf_black_forest_labs_flux_2_klein_9b: {
      description: "Execute @cf/black-forest-labs/flux-2-klein-9b model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
        multipart: z.object({
          body: z.object({}).optional(),
          contentType: z.string().optional(),
        }),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }
        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/black-forest-labs/flux-2-klein-9b${qs}`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "workers_ai_post_run_cf_black_forest_labs_flux_2_klein_9b",
          id,
          result,
        );
        context.logger.info(
          "Created workers_ai_post_run_cf_black_forest_labs_flux_2_klein_9b {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    create_workers_ai_post_run_cf_bytedance_stable_diffusion_xl_lightning: {
      description: "Execute @cf/bytedance/stable-diffusion-xl-lightning model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
        guidance: z.number().optional().describe(
          "Controls how closely the generated image should adhere to the prompt; higher ...",
        ),
        height: z.number().int().min(256).max(2048).optional().describe(
          "The height of the generated image in pixels",
        ),
        image: z.array(z.number()).optional().describe(
          "For use with img2img tasks. An array of integers that represent the image dat...",
        ),
        image_b64: z.string().optional().describe(
          "For use with img2img tasks. A base64-encoded string of the input image",
        ),
        mask: z.array(z.number()).optional().describe(
          "An array representing An array of integers that represent mask image data for...",
        ),
        negative_prompt: z.string().optional().describe(
          "Text describing elements to avoid in the generated image",
        ),
        num_steps: z.number().int().max(20).optional().describe(
          "The number of diffusion steps; higher values can improve quality but take longer",
        ),
        prompt: z.string().min(1).describe(
          "A text description of the image you want to generate",
        ),
        seed: z.number().int().optional().describe(
          "Random seed for reproducibility of the image generation",
        ),
        strength: z.number().optional().describe(
          "A value between 0 and 1 indicating how strongly to apply the transformation d...",
        ),
        width: z.number().int().min(256).max(2048).optional().describe(
          "The width of the generated image in pixels",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }
        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/bytedance/stable-diffusion-xl-lightning${qs}`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "workers_ai_post_run_cf_bytedance_stable_diffusion_xl_lightning",
          id,
          result,
        );
        context.logger.info(
          "Created workers_ai_post_run_cf_bytedance_stable_diffusion_xl_lightning {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    get_workers_ai_post_websocket_run_cf_deepgram_aura: {
      description: "Open Websocket connection with @cf/deepgram/aura model.",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai/run/@cf/deepgram/aura`,
        );

        const handle = await context.writeResource(
          "workers_ai_post_websocket_run_cf_deepgram_aura",
          "latest",
          result,
        );
        context.logger.info(
          "Fetched workers_ai_post_websocket_run_cf_deepgram_aura",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    get_workers_ai_post_websocket_run_cf_deepgram_aura_1: {
      description: "Open Websocket connection with @cf/deepgram/aura-1 model.",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai/run/@cf/deepgram/aura-1`,
        );

        const handle = await context.writeResource(
          "workers_ai_post_websocket_run_cf_deepgram_aura_1",
          "latest",
          result,
        );
        context.logger.info(
          "Fetched workers_ai_post_websocket_run_cf_deepgram_aura_1",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    create_workers_ai_post_run_cf_deepgram_aura_1: {
      description: "Execute @cf/deepgram/aura-1 model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
        bit_rate: z.number().optional().describe(
          "The bitrate of the audio in bits per second. Choose from predefined ranges or...",
        ),
        container: z.enum(["none", "wav", "ogg"]).optional().describe(
          "Container specifies the file format wrapper for the output audio. The availab...",
        ),
        encoding: z.enum([
          "linear16",
          "flac",
          "mulaw",
          "alaw",
          "mp3",
          "opus",
          "aac",
        ]).optional().describe("Encoding of the output audio."),
        sample_rate: z.number().optional().describe(
          "Sample Rate specifies the sample rate for the output audio. Based on the enco...",
        ),
        speaker: z.enum([
          "angus",
          "asteria",
          "arcas",
          "orion",
          "orpheus",
          "athena",
          "luna",
          "zeus",
          "perseus",
          "helios",
          "hera",
          "stella",
        ]).optional().describe("Speaker used to produce the audio."),
        text: z.string().describe("The text content to be converted to speech"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }
        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/deepgram/aura-1${qs}`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "workers_ai_post_run_cf_deepgram_aura_1",
          id,
          result,
        );
        context.logger.info(
          "Created workers_ai_post_run_cf_deepgram_aura_1 {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    get_workers_ai_post_websocket_run_cf_deepgram_aura_1_internal: {
      description:
        "Open Websocket connection with @cf/deepgram/aura-1-internal model.",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai/run/@cf/deepgram/aura-1-internal`,
        );

        const handle = await context.writeResource(
          "workers_ai_post_websocket_run_cf_deepgram_aura_1_internal",
          "latest",
          result,
        );
        context.logger.info(
          "Fetched workers_ai_post_websocket_run_cf_deepgram_aura_1_internal",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    get_workers_ai_post_websocket_run_cf_deepgram_aura_2: {
      description: "Open Websocket connection with @cf/deepgram/aura-2 model.",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai/run/@cf/deepgram/aura-2`,
        );

        const handle = await context.writeResource(
          "workers_ai_post_websocket_run_cf_deepgram_aura_2",
          "latest",
          result,
        );
        context.logger.info(
          "Fetched workers_ai_post_websocket_run_cf_deepgram_aura_2",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    get_workers_ai_post_websocket_run_cf_deepgram_aura_2_en: {
      description:
        "Open Websocket connection with @cf/deepgram/aura-2-en model.",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai/run/@cf/deepgram/aura-2-en`,
        );

        const handle = await context.writeResource(
          "workers_ai_post_websocket_run_cf_deepgram_aura_2_en",
          "latest",
          result,
        );
        context.logger.info(
          "Fetched workers_ai_post_websocket_run_cf_deepgram_aura_2_en",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    create_workers_ai_post_run_cf_deepgram_aura_2_en: {
      description: "Execute @cf/deepgram/aura-2-en model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
        bit_rate: z.number().optional().describe(
          "The bitrate of the audio in bits per second. Choose from predefined ranges or...",
        ),
        container: z.enum(["none", "wav", "ogg"]).optional().describe(
          "Container specifies the file format wrapper for the output audio. The availab...",
        ),
        encoding: z.enum([
          "linear16",
          "flac",
          "mulaw",
          "alaw",
          "mp3",
          "opus",
          "aac",
        ]).optional().describe("Encoding of the output audio."),
        sample_rate: z.number().optional().describe(
          "Sample Rate specifies the sample rate for the output audio. Based on the enco...",
        ),
        speaker: z.enum([
          "amalthea",
          "andromeda",
          "apollo",
          "arcas",
          "aries",
          "asteria",
          "athena",
          "atlas",
          "aurora",
          "callista",
          "cora",
          "cordelia",
          "delia",
          "draco",
          "electra",
          "harmonia",
          "helena",
          "hera",
          "hermes",
          "hyperion",
          "iris",
          "janus",
          "juno",
          "jupiter",
          "luna",
          "mars",
          "minerva",
          "neptune",
          "odysseus",
          "ophelia",
          "orion",
          "orpheus",
          "pandora",
          "phoebe",
          "pluto",
          "saturn",
          "thalia",
          "theia",
          "vesta",
          "zeus",
        ]).optional().describe("Speaker used to produce the audio."),
        text: z.string().optional().describe(
          "The text content to be converted to speech",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }
        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/deepgram/aura-2-en${qs}`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "workers_ai_post_run_cf_deepgram_aura_2_en",
          id,
          result,
        );
        context.logger.info(
          "Created workers_ai_post_run_cf_deepgram_aura_2_en {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    get_workers_ai_post_websocket_run_cf_deepgram_aura_2_en_ws: {
      description:
        "Open Websocket connection with @cf/deepgram/aura-2-en-ws model.",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai/run/@cf/deepgram/aura-2-en-ws`,
        );

        const handle = await context.writeResource(
          "workers_ai_post_websocket_run_cf_deepgram_aura_2_en_ws",
          "latest",
          result,
        );
        context.logger.info(
          "Fetched workers_ai_post_websocket_run_cf_deepgram_aura_2_en_ws",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    get_workers_ai_post_websocket_run_cf_deepgram_aura_2_es: {
      description:
        "Open Websocket connection with @cf/deepgram/aura-2-es model.",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai/run/@cf/deepgram/aura-2-es`,
        );

        const handle = await context.writeResource(
          "workers_ai_post_websocket_run_cf_deepgram_aura_2_es",
          "latest",
          result,
        );
        context.logger.info(
          "Fetched workers_ai_post_websocket_run_cf_deepgram_aura_2_es",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    create_workers_ai_post_run_cf_deepgram_aura_2_es: {
      description: "Execute @cf/deepgram/aura-2-es model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
        bit_rate: z.number().optional().describe(
          "The bitrate of the audio in bits per second. Choose from predefined ranges or...",
        ),
        container: z.enum(["none", "wav", "ogg"]).optional().describe(
          "Container specifies the file format wrapper for the output audio. The availab...",
        ),
        encoding: z.enum([
          "linear16",
          "flac",
          "mulaw",
          "alaw",
          "mp3",
          "opus",
          "aac",
        ]).optional().describe("Encoding of the output audio."),
        sample_rate: z.number().optional().describe(
          "Sample Rate specifies the sample rate for the output audio. Based on the enco...",
        ),
        speaker: z.enum([
          "sirio",
          "nestor",
          "carina",
          "celeste",
          "alvaro",
          "diana",
          "aquila",
          "selena",
          "estrella",
          "javier",
        ]).optional().describe("Speaker used to produce the audio."),
        text: z.string().optional().describe(
          "The text content to be converted to speech",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }
        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/deepgram/aura-2-es${qs}`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "workers_ai_post_run_cf_deepgram_aura_2_es",
          id,
          result,
        );
        context.logger.info(
          "Created workers_ai_post_run_cf_deepgram_aura_2_es {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    get_workers_ai_post_websocket_run_cf_deepgram_flux: {
      description: "Open Websocket connection with @cf/deepgram/flux model.",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai/run/@cf/deepgram/flux`,
        );

        const handle = await context.writeResource(
          "workers_ai_post_websocket_run_cf_deepgram_flux",
          "latest",
          result,
        );
        context.logger.info(
          "Fetched workers_ai_post_websocket_run_cf_deepgram_flux",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    create_workers_ai_post_run_cf_deepgram_flux: {
      description: "Execute @cf/deepgram/flux model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
        eager_eot_threshold: z.string().optional().describe(
          "End-of-turn confidence required to fire an eager end-of-turn event. When set,...",
        ),
        encoding: z.enum(["linear16"]).optional().describe(
          "Encoding of the audio stream. Currently only supports raw signed little-endia...",
        ),
        eot_threshold: z.string().optional().describe(
          "End-of-turn confidence required to finish a turn. Valid Values 0.5 - 0.9.",
        ),
        eot_timeout_ms: z.string().optional().describe(
          "A turn will be finished when this much time has passed after speech, regardle...",
        ),
        keyterm: z.string().optional().describe(
          "Keyterm prompting can improve recognition of specialized terminology. Pass mu...",
        ),
        mip_opt_out: z.enum(["true", "false"]).optional().describe(
          "Opts out requests from the Deepgram Model Improvement Program. Refer to Deepg...",
        ),
        sample_rate: z.string().optional().describe(
          "Sample rate of the audio stream in Hz.",
        ),
        tag: z.string().optional().describe(
          "Label your requests for the purpose of identification during usage reporting",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }
        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/deepgram/flux${qs}`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "workers_ai_post_run_cf_deepgram_flux",
          id,
          result,
        );
        context.logger.info(
          "Created workers_ai_post_run_cf_deepgram_flux {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    get_workers_ai_post_websocket_run_cf_deepgram_nova_3: {
      description: "Open Websocket connection with @cf/deepgram/nova-3 model.",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai/run/@cf/deepgram/nova-3`,
        );

        const handle = await context.writeResource(
          "workers_ai_post_websocket_run_cf_deepgram_nova_3",
          "latest",
          result,
        );
        context.logger.info(
          "Fetched workers_ai_post_websocket_run_cf_deepgram_nova_3",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    create_workers_ai_post_run_cf_deepgram_nova_3: {
      description: "Execute @cf/deepgram/nova-3 model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
        audio: z.object({
          body: z.object({}),
          contentType: z.string(),
        }),
        channels: z.number().optional().describe(
          "The number of channels in the submitted audio",
        ),
        custom_intent: z.string().optional().describe(
          "Custom intents you want the model to detect within your input audio if present",
        ),
        custom_intent_mode: z.enum(["extended", "strict"]).optional().describe(
          "Sets how the model will interpret intents submitted to the custom_intent para...",
        ),
        custom_topic: z.string().optional().describe(
          "Custom topics you want the model to detect within your input audio or text if...",
        ),
        custom_topic_mode: z.enum(["extended", "strict"]).optional().describe(
          "Sets how the model will interpret strings submitted to the custom_topic param...",
        ),
        detect_entities: z.boolean().optional().describe(
          "Identifies and extracts key entities from content in submitted audio",
        ),
        detect_language: z.boolean().optional().describe(
          "Identifies the dominant language spoken in submitted audio",
        ),
        diarize: z.boolean().optional().describe(
          "Recognize speaker changes. Each word in the transcript will be assigned a spe...",
        ),
        dictation: z.boolean().optional().describe(
          "Identify and extract key entities from content in submitted audio",
        ),
        encoding: z.enum([
          "linear16",
          "flac",
          "mulaw",
          "amr-nb",
          "amr-wb",
          "opus",
          "speex",
          "g729",
        ]).optional().describe(
          "Specify the expected encoding of your submitted audio",
        ),
        endpointing: z.string().optional().describe(
          "Indicates how long model will wait to detect whether a speaker has finished s...",
        ),
        extra: z.string().optional().describe(
          "Arbitrary key-value pairs that are attached to the API response for usage in ...",
        ),
        filler_words: z.boolean().optional().describe(
          "Filler Words can help transcribe interruptions in your audio, like 'uh' and 'um'",
        ),
        interim_results: z.boolean().optional().describe(
          "Specifies whether the streaming endpoint should provide ongoing transcription...",
        ),
        keyterm: z.string().optional().describe(
          "Key term prompting can boost or suppress specialized terminology and brands.",
        ),
        keywords: z.string().optional().describe(
          "Keywords can boost or suppress specialized terminology and brands.",
        ),
        language: z.string().optional().describe(
          "The BCP-47 language tag that hints at the primary spoken language. Depending ...",
        ),
        measurements: z.boolean().optional().describe(
          "Spoken measurements will be converted to their corresponding abbreviations.",
        ),
        mip_opt_out: z.boolean().optional().describe(
          "Opts out requests from the Deepgram Model Improvement Program. Refer to our D...",
        ),
        mode: z.enum(["general", "medical", "finance"]).optional().describe(
          "Mode of operation for the model representing broad area of topic that will be...",
        ),
        multichannel: z.boolean().optional().describe(
          "Transcribe each audio channel independently.",
        ),
        numerals: z.boolean().optional().describe(
          "Numerals converts numbers from written format to numerical format.",
        ),
        paragraphs: z.boolean().optional().describe(
          "Splits audio into paragraphs to improve transcript readability.",
        ),
        profanity_filter: z.boolean().optional().describe(
          "Profanity Filter looks for recognized profanity and converts it to the neares...",
        ),
        punctuate: z.boolean().optional().describe(
          "Add punctuation and capitalization to the transcript.",
        ),
        redact: z.string().optional().describe(
          "Redaction removes sensitive information from your transcripts.",
        ),
        replace: z.string().optional().describe(
          "Search for terms or phrases in submitted audio and replaces them.",
        ),
        search: z.string().optional().describe(
          "Search for terms or phrases in submitted audio.",
        ),
        sentiment: z.boolean().optional().describe(
          "Recognizes the sentiment throughout a transcript or text.",
        ),
        smart_format: z.boolean().optional().describe(
          "Apply formatting to transcript output. When set to true, additional formattin...",
        ),
        topics: z.boolean().optional().describe(
          "Detect topics throughout a transcript or text.",
        ),
        utt_split: z.number().optional().describe(
          "Seconds to wait before detecting a pause between words in submitted audio.",
        ),
        utterance_end_ms: z.boolean().optional().describe(
          "Indicates how long model will wait to send an UtteranceEnd message after a wo...",
        ),
        utterances: z.boolean().optional().describe(
          "Segments speech into meaningful semantic units.",
        ),
        vad_events: z.boolean().optional().describe(
          "Indicates that speech has started. You'll begin receiving Speech Started mess...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }
        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/deepgram/nova-3${qs}`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "workers_ai_post_run_cf_deepgram_nova_3",
          id,
          result,
        );
        context.logger.info(
          "Created workers_ai_post_run_cf_deepgram_nova_3 {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    get_workers_ai_post_websocket_run_cf_deepgram_nova_3_internal: {
      description:
        "Open Websocket connection with @cf/deepgram/nova-3-internal model.",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai/run/@cf/deepgram/nova-3-internal`,
        );

        const handle = await context.writeResource(
          "workers_ai_post_websocket_run_cf_deepgram_nova_3_internal",
          "latest",
          result,
        );
        context.logger.info(
          "Fetched workers_ai_post_websocket_run_cf_deepgram_nova_3_internal",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    get_workers_ai_post_websocket_run_cf_deepgram_nova_3_ws: {
      description:
        "Open Websocket connection with @cf/deepgram/nova-3-ws model.",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai/run/@cf/deepgram/nova-3-ws`,
        );

        const handle = await context.writeResource(
          "workers_ai_post_websocket_run_cf_deepgram_nova_3_ws",
          "latest",
          result,
        );
        context.logger.info(
          "Fetched workers_ai_post_websocket_run_cf_deepgram_nova_3_ws",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_deepseek_ai_deepseek_math_7b_instruct: {
      description: "Execute @cf/deepseek-ai/deepseek-math-7b-instruct model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/deepseek-ai/deepseek-math-7b-instruct${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_deepseek_ai_deepseek_math_7b_instruct",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_deepseek_ai_deepseek_math_7b_instruct",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_deepseek_ai_deepseek_r1_distill_qwen_32b: {
      description:
        "Execute @cf/deepseek-ai/deepseek-r1-distill-qwen-32b model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/deepseek-ai/deepseek-r1-distill-qwen-32b${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_deepseek_ai_deepseek_r1_distill_qwen_32b",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_deepseek_ai_deepseek_r1_distill_qwen_32b",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_defog_sqlcoder_7b_2: {
      description: "Execute @cf/defog/sqlcoder-7b-2 model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/defog/sqlcoder-7b-2${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_defog_sqlcoder_7b_2",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_defog_sqlcoder_7b_2",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    create_workers_ai_post_run_cf_facebook_bart_large_cnn: {
      description: "Execute @cf/facebook/bart-large-cnn model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
        input_text: z.string().min(1).describe(
          "The text that you want the model to summarize",
        ),
        max_length: z.number().int().optional().describe(
          "The maximum length of the generated summary in tokens",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }
        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/facebook/bart-large-cnn${qs}`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "workers_ai_post_run_cf_facebook_bart_large_cnn",
          id,
          result,
        );
        context.logger.info(
          "Created workers_ai_post_run_cf_facebook_bart_large_cnn {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    create_workers_ai_post_run_cf_facebook_nonomni_bart_large_cnn: {
      description: "Execute @cf/facebook/nonomni-bart-large-cnn model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
        input_text: z.string().min(1).describe(
          "The text that you want the model to summarize",
        ),
        max_length: z.number().int().optional().describe(
          "The maximum length of the generated summary in tokens",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }
        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/facebook/nonomni-bart-large-cnn${qs}`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "workers_ai_post_run_cf_facebook_nonomni_bart_large_cnn",
          id,
          result,
        );
        context.logger.info(
          "Created workers_ai_post_run_cf_facebook_nonomni_bart_large_cnn {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_facebook_nonomni_detr_resnet_50: {
      description: "Execute @cf/facebook/nonomni-detr-resnet-50 model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/facebook/nonomni-detr-resnet-50${qs}`,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_facebook_nonomni_detr_resnet_50",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_facebook_nonomni_detr_resnet_50",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_fblgit_una_cybertron_7b_v2_bf16: {
      description: "Execute @cf/fblgit/una-cybertron-7b-v2-bf16 model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/fblgit/una-cybertron-7b-v2-bf16${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_fblgit_una_cybertron_7b_v2_bf16",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_fblgit_una_cybertron_7b_v2_bf16",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    create_workers_ai_post_run_cf_google_embeddinggemma_300m: {
      description: "Execute @cf/google/embeddinggemma-300m model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
        text: z.union([z.string().min(1), z.array(z.string().min(1))]),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }
        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/google/embeddinggemma-300m${qs}`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "workers_ai_post_run_cf_google_embeddinggemma_300m",
          id,
          result,
        );
        context.logger.info(
          "Created workers_ai_post_run_cf_google_embeddinggemma_300m {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_google_gemma_2b_it_lora: {
      description: "Execute @cf/google/gemma-2b-it-lora model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/google/gemma-2b-it-lora${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_google_gemma_2b_it_lora",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_google_gemma_2b_it_lora",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_google_gemma_3_12b_it: {
      description: "Execute @cf/google/gemma-3-12b-it model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/google/gemma-3-12b-it${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_google_gemma_3_12b_it",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_google_gemma_3_12b_it",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_google_gemma_4_26b_a4b_it: {
      description: "Execute @cf/google/gemma-4-26b-a4b-it model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/google/gemma-4-26b-a4b-it${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_google_gemma_4_26b_a4b_it",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_google_gemma_4_26b_a4b_it",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_google_gemma_7b_it_lora: {
      description: "Execute @cf/google/gemma-7b-it-lora model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/google/gemma-7b-it-lora${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_google_gemma_7b_it_lora",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_google_gemma_7b_it_lora",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    create_workers_ai_post_run_cf_google_nonomni_embeddinggemma_300m: {
      description: "Execute @cf/google/nonomni-embeddinggemma-300m model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
        text: z.union([z.string(), z.array(z.string())]).describe(
          "Input text to embed. Can be a single string or a list of strings.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }
        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/google/nonomni-embeddinggemma-300m${qs}`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "workers_ai_post_run_cf_google_nonomni_embeddinggemma_300m",
          id,
          result,
        );
        context.logger.info(
          "Created workers_ai_post_run_cf_google_nonomni_embeddinggemma_300m {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    create_workers_ai_post_run_cf_huggingface_distilbert_sst_2_int8: {
      description: "Execute @cf/huggingface/distilbert-sst-2-int8 model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
        text: z.string().min(1).describe("The text that you want to classify"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }
        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/huggingface/distilbert-sst-2-int8${qs}`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "workers_ai_post_run_cf_huggingface_distilbert_sst_2_int8",
          id,
          result,
        );
        context.logger.info(
          "Created workers_ai_post_run_cf_huggingface_distilbert_sst_2_int8 {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    create_workers_ai_post_run_cf_huggingface_nonomni_distilbert_sst_2_int8: {
      description:
        "Execute @cf/huggingface/nonomni-distilbert-sst-2-int8 model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
        text: z.string().min(1).describe("The text that you want to classify"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }
        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/huggingface/nonomni-distilbert-sst-2-int8${qs}`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "workers_ai_post_run_cf_huggingface_nonomni_distilbert_sst_2_int8",
          id,
          result,
        );
        context.logger.info(
          "Created workers_ai_post_run_cf_huggingface_nonomni_distilbert_sst_2_int8 {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_ibm_granite_granite_4_0_h_micro: {
      description: "Execute @cf/ibm-granite/granite-4.0-h-micro model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/ibm-granite/granite-4.0-h-micro${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_ibm_granite_granite_4_0_h_micro",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_ibm_granite_granite_4_0_h_micro",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    create_workers_ai_post_run_cf_leonardo_lucid_origin: {
      description: "Execute @cf/leonardo/lucid-origin model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
        guidance: z.number().min(0).max(10).optional().describe(
          "Controls how closely the generated image should adhere to the prompt; higher ...",
        ),
        height: z.number().int().min(0).max(2500).optional().describe(
          "The height of the generated image in pixels",
        ),
        num_steps: z.number().int().min(1).max(40).optional().describe(
          "The number of diffusion steps; higher values can improve quality but take longer",
        ),
        prompt: z.string().min(1).describe(
          "A text description of the image you want to generate.",
        ),
        seed: z.number().int().min(0).optional().describe(
          "Random seed for reproducibility of the image generation",
        ),
        steps: z.number().int().min(1).max(40).optional().describe(
          "The number of diffusion steps; higher values can improve quality but take longer",
        ),
        width: z.number().int().min(0).max(2500).optional().describe(
          "The width of the generated image in pixels",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }
        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/leonardo/lucid-origin${qs}`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "workers_ai_post_run_cf_leonardo_lucid_origin",
          id,
          result,
        );
        context.logger.info(
          "Created workers_ai_post_run_cf_leonardo_lucid_origin {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    create_workers_ai_post_run_cf_leonardo_phoenix_1_0: {
      description: "Execute @cf/leonardo/phoenix-1.0 model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
        guidance: z.number().min(2).max(10).optional().describe(
          "Controls how closely the generated image should adhere to the prompt; higher ...",
        ),
        height: z.number().int().min(0).max(2048).optional().describe(
          "The height of the generated image in pixels",
        ),
        negative_prompt: z.string().min(1).optional().describe(
          "Specify what to exclude from the generated images",
        ),
        num_steps: z.number().int().min(1).max(50).optional().describe(
          "The number of diffusion steps; higher values can improve quality but take longer",
        ),
        prompt: z.string().min(1).describe(
          "A text description of the image you want to generate.",
        ),
        seed: z.number().int().min(0).optional().describe(
          "Random seed for reproducibility of the image generation",
        ),
        width: z.number().int().min(0).max(2048).optional().describe(
          "The width of the generated image in pixels",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }
        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/leonardo/phoenix-1.0${qs}`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "workers_ai_post_run_cf_leonardo_phoenix_1_0",
          id,
          result,
        );
        context.logger.info(
          "Created workers_ai_post_run_cf_leonardo_phoenix_1_0 {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    create_workers_ai_post_run_cf_lykon_dreamshaper_8_lcm: {
      description: "Execute @cf/lykon/dreamshaper-8-lcm model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
        guidance: z.number().optional().describe(
          "Controls how closely the generated image should adhere to the prompt; higher ...",
        ),
        height: z.number().int().min(256).max(2048).optional().describe(
          "The height of the generated image in pixels",
        ),
        image: z.array(z.number()).optional().describe(
          "For use with img2img tasks. An array of integers that represent the image dat...",
        ),
        image_b64: z.string().optional().describe(
          "For use with img2img tasks. A base64-encoded string of the input image",
        ),
        mask: z.array(z.number()).optional().describe(
          "An array representing An array of integers that represent mask image data for...",
        ),
        negative_prompt: z.string().optional().describe(
          "Text describing elements to avoid in the generated image",
        ),
        num_steps: z.number().int().max(20).optional().describe(
          "The number of diffusion steps; higher values can improve quality but take longer",
        ),
        prompt: z.string().min(1).describe(
          "A text description of the image you want to generate",
        ),
        seed: z.number().int().optional().describe(
          "Random seed for reproducibility of the image generation",
        ),
        strength: z.number().optional().describe(
          "A value between 0 and 1 indicating how strongly to apply the transformation d...",
        ),
        width: z.number().int().min(256).max(2048).optional().describe(
          "The width of the generated image in pixels",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }
        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/lykon/dreamshaper-8-lcm${qs}`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "workers_ai_post_run_cf_lykon_dreamshaper_8_lcm",
          id,
          result,
        );
        context.logger.info(
          "Created workers_ai_post_run_cf_lykon_dreamshaper_8_lcm {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_meta_llama_llama_2_7b_chat_hf_lora: {
      description: "Execute @cf/meta-llama/llama-2-7b-chat-hf-lora model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/meta-llama/llama-2-7b-chat-hf-lora${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_meta_llama_llama_2_7b_chat_hf_lora",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_meta_llama_llama_2_7b_chat_hf_lora",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_meta_llama_2_7b_chat_fp16: {
      description: "Execute @cf/meta/llama-2-7b-chat-fp16 model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/meta/llama-2-7b-chat-fp16${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_meta_llama_2_7b_chat_fp16",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_meta_llama_2_7b_chat_fp16",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_meta_llama_2_7b_chat_int8: {
      description: "Execute @cf/meta/llama-2-7b-chat-int8 model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/meta/llama-2-7b-chat-int8${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_meta_llama_2_7b_chat_int8",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_meta_llama_2_7b_chat_int8",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_meta_llama_3_8b_instruct: {
      description: "Execute @cf/meta/llama-3-8b-instruct model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/meta/llama-3-8b-instruct${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_meta_llama_3_8b_instruct",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_meta_llama_3_8b_instruct",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_meta_llama_3_8b_instruct_awq: {
      description: "Execute @cf/meta/llama-3-8b-instruct-awq model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/meta/llama-3-8b-instruct-awq${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_meta_llama_3_8b_instruct_awq",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_meta_llama_3_8b_instruct_awq",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_meta_llama_3_1_70b_instruct_fp8_fast: {
      description: "Execute @cf/meta/llama-3.1-70b-instruct-fp8-fast model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/meta/llama-3.1-70b-instruct-fp8-fast${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_meta_llama_3_1_70b_instruct_fp8_fast",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_meta_llama_3_1_70b_instruct_fp8_fast",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_meta_llama_3_1_8b_instruct_awq: {
      description: "Execute @cf/meta/llama-3.1-8b-instruct-awq model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/meta/llama-3.1-8b-instruct-awq${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_meta_llama_3_1_8b_instruct_awq",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_meta_llama_3_1_8b_instruct_awq",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_meta_llama_3_1_8b_instruct_fp8: {
      description: "Execute @cf/meta/llama-3.1-8b-instruct-fp8 model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/meta/llama-3.1-8b-instruct-fp8${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_meta_llama_3_1_8b_instruct_fp8",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_meta_llama_3_1_8b_instruct_fp8",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_meta_llama_3_1_8b_instruct_fp8_fast: {
      description: "Execute @cf/meta/llama-3.1-8b-instruct-fp8-fast model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/meta/llama-3.1-8b-instruct-fp8-fast${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_meta_llama_3_1_8b_instruct_fp8_fast",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_meta_llama_3_1_8b_instruct_fp8_fast",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_meta_llama_3_2_11b_vision_instruct: {
      description: "Execute @cf/meta/llama-3.2-11b-vision-instruct model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/meta/llama-3.2-11b-vision-instruct${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_meta_llama_3_2_11b_vision_instruct",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_meta_llama_3_2_11b_vision_instruct",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_meta_llama_3_2_1b_instruct: {
      description: "Execute @cf/meta/llama-3.2-1b-instruct model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/meta/llama-3.2-1b-instruct${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_meta_llama_3_2_1b_instruct",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_meta_llama_3_2_1b_instruct",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_meta_llama_3_2_3b_instruct: {
      description: "Execute @cf/meta/llama-3.2-3b-instruct model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/meta/llama-3.2-3b-instruct${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_meta_llama_3_2_3b_instruct",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_meta_llama_3_2_3b_instruct",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_meta_llama_3_3_70b_instruct_fp8_fast: {
      description: "Execute @cf/meta/llama-3.3-70b-instruct-fp8-fast model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/meta/llama-3.3-70b-instruct-fp8-fast${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_meta_llama_3_3_70b_instruct_fp8_fast",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_meta_llama_3_3_70b_instruct_fp8_fast",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_meta_llama_4_scout_17b_16e_instruct: {
      description: "Execute @cf/meta/llama-4-scout-17b-16e-instruct model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/meta/llama-4-scout-17b-16e-instruct${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_meta_llama_4_scout_17b_16e_instruct",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_meta_llama_4_scout_17b_16e_instruct",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    create_workers_ai_post_run_cf_meta_llama_guard_3_8b: {
      description: "Execute @cf/meta/llama-guard-3-8b model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
        max_tokens: z.number().int().optional().describe(
          "The maximum number of tokens to generate in the response.",
        ),
        messages: z.array(z.object({
          content: z.string(),
          role: z.enum(["user", "assistant"]),
        })).describe(
          "An array of message objects representing the conversation history.",
        ),
        response_format: z.object({
          type: z.string().optional(),
        }).optional().describe(
          "Dictate the output format of the generated response.",
        ),
        temperature: z.number().min(0).max(5).optional().describe(
          "Controls the randomness of the output; higher values produce more random resu...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }
        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/meta/llama-guard-3-8b${qs}`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "workers_ai_post_run_cf_meta_llama_guard_3_8b",
          id,
          result,
        );
        context.logger.info(
          "Created workers_ai_post_run_cf_meta_llama_guard_3_8b {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_meta_m2m100_1_2b: {
      description: "Execute @cf/meta/m2m100-1.2b model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/meta/m2m100-1.2b${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_meta_m2m100_1_2b",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_meta_m2m100_1_2b",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_microsoft_nonomni_resnet_50: {
      description: "Execute @cf/microsoft/nonomni-resnet-50 model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/microsoft/nonomni-resnet-50${qs}`,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_microsoft_nonomni_resnet_50",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_microsoft_nonomni_resnet_50",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_microsoft_phi_2: {
      description: "Execute @cf/microsoft/phi-2 model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/microsoft/phi-2${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_microsoft_phi_2",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_microsoft_phi_2",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_microsoft_resnet_50: {
      description: "Execute @cf/microsoft/resnet-50 model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/microsoft/resnet-50${qs}`,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_microsoft_resnet_50",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_microsoft_resnet_50",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_mistral_mistral_7b_instruct_v0_1: {
      description: "Execute @cf/mistral/mistral-7b-instruct-v0.1 model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/mistral/mistral-7b-instruct-v0.1${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_mistral_mistral_7b_instruct_v0_1",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_mistral_mistral_7b_instruct_v0_1",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_mistral_mistral_7b_instruct_v0_2_lora: {
      description: "Execute @cf/mistral/mistral-7b-instruct-v0.2-lora model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/mistral/mistral-7b-instruct-v0.2-lora${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_mistral_mistral_7b_instruct_v0_2_lora",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_mistral_mistral_7b_instruct_v0_2_lora",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_mistralai_mistral_small_3_1_24b_instruct: {
      description:
        "Execute @cf/mistralai/mistral-small-3.1-24b-instruct model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/mistralai/mistral-small-3.1-24b-instruct${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_mistralai_mistral_small_3_1_24b_instruct",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_mistralai_mistral_small_3_1_24b_instruct",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_moonshotai_kimi_k2_5: {
      description: "Execute @cf/moonshotai/kimi-k2.5 model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/moonshotai/kimi-k2.5${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_moonshotai_kimi_k2_5",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_moonshotai_kimi_k2_5",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_moonshotai_kimi_k2_6: {
      description: "Execute @cf/moonshotai/kimi-k2.6 model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/moonshotai/kimi-k2.6${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_moonshotai_kimi_k2_6",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_moonshotai_kimi_k2_6",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_moonshotai_kimi_k2_7_code: {
      description: "Execute @cf/moonshotai/kimi-k2.7-code model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/moonshotai/kimi-k2.7-code${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_moonshotai_kimi_k2_7_code",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_moonshotai_kimi_k2_7_code",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    create_workers_ai_post_run_cf_myshell_ai_melotts: {
      description: "Execute @cf/myshell-ai/melotts model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
        lang: z.string().optional().describe(
          "The speech language (e.g., 'en' for English, 'fr' for French). Defaults to 'e...",
        ),
        prompt: z.string().min(1).describe(
          "A text description of the audio you want to generate",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }
        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/myshell-ai/melotts${qs}`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "workers_ai_post_run_cf_myshell_ai_melotts",
          id,
          result,
        );
        context.logger.info(
          "Created workers_ai_post_run_cf_myshell_ai_melotts {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_nvidia_nemotron_3_120b_a12b: {
      description: "Execute @cf/nvidia/nemotron-3-120b-a12b model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/nvidia/nemotron-3-120b-a12b${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_nvidia_nemotron_3_120b_a12b",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_nvidia_nemotron_3_120b_a12b",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    get_workers_ai_post_websocket_run_cf_nvidia_nemotron_speech_streaming_en_0_6b:
      {
        description:
          "Open Websocket connection with @cf/nvidia/nemotron-speech-streaming-en-0.6b model.",
        arguments: z.object({}),
        execute: async (
          _args: Record<string, unknown>,
          context: {
            globalArgs: Record<string, string>;
            writeResource: (
              spec: string,
              instance: string,
              data: unknown,
            ) => Promise<{ name: string }>;
            logger: {
              info: (msg: string, props: Record<string, unknown>) => void;
            };
          },
        ) => {
          const { apiToken, accountId } = context.globalArgs;
          const result = await cfApi<Record<string, unknown>>(
            apiToken,
            "GET",
            `/accounts/${accountId}/ai/run/@cf/nvidia/nemotron-speech-streaming-en-0.6b`,
          );

          const handle = await context.writeResource(
            "workers_ai_post_websocket_run_cf_nvidia_nemotron_speech_streaming_en_0_6b",
            "latest",
            result,
          );
          context.logger.info(
            "Fetched workers_ai_post_websocket_run_cf_nvidia_nemotron_speech_streaming_en_0_6b",
            {},
          );
          return { dataHandles: [handle] };
        },
      },
    workers_ai_post_run_cf_nvidia_nemotron_speech_streaming_en_0_6b: {
      description:
        "Execute @cf/nvidia/nemotron-speech-streaming-en-0.6b model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/nvidia/nemotron-speech-streaming-en-0.6b${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_nvidia_nemotron_speech_streaming_en_0_6b",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_nvidia_nemotron_speech_streaming_en_0_6b",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_openai_gpt_oss_120b: {
      description: "Execute @cf/openai/gpt-oss-120b model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/openai/gpt-oss-120b${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_openai_gpt_oss_120b",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_openai_gpt_oss_120b",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_openai_gpt_oss_20b: {
      description: "Execute @cf/openai/gpt-oss-20b model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/openai/gpt-oss-20b${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_openai_gpt_oss_20b",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_openai_gpt_oss_20b",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_openai_whisper: {
      description: "Execute @cf/openai/whisper model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/openai/whisper${qs}`,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_openai_whisper",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_openai_whisper",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    create_workers_ai_post_run_cf_openai_whisper_large_v3_turbo: {
      description: "Execute @cf/openai/whisper-large-v3-turbo model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
        audio: z.union([
          z.string(),
          z.object({
            body: z.object({}).optional(),
            contentType: z.string().optional(),
          }),
        ]),
        beam_size: z.number().int().optional().describe(
          "The number of beams to use in beam search decoding. Higher values may improve...",
        ),
        compression_ratio_threshold: z.number().optional().describe(
          "Threshold for filtering out segments with high compression ratio, which often...",
        ),
        condition_on_previous_text: z.boolean().optional().describe(
          "Whether to condition on previous text during transcription. Setting to false ...",
        ),
        hallucination_silence_threshold: z.number().optional().describe(
          "Optional threshold (in seconds) to skip silent periods that may cause halluci...",
        ),
        initial_prompt: z.string().optional().describe(
          "A text prompt to help provide context to the model on the contents of the audio.",
        ),
        language: z.string().optional().describe(
          "The language of the audio being transcribed or translated.",
        ),
        log_prob_threshold: z.number().optional().describe(
          "Threshold for filtering out segments with low average log probability, indica...",
        ),
        no_speech_threshold: z.number().optional().describe(
          "Threshold for detecting no-speech segments. Segments with no-speech probabili...",
        ),
        prefix: z.string().optional().describe(
          "The prefix appended to the beginning of the output of the transcription and c...",
        ),
        task: z.string().optional().describe(
          "Supported tasks are 'translate' or 'transcribe'.",
        ),
        vad_filter: z.boolean().optional().describe(
          "Preprocess the audio with a voice activity detection model.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }
        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/openai/whisper-large-v3-turbo${qs}`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "workers_ai_post_run_cf_openai_whisper_large_v3_turbo",
          id,
          result,
        );
        context.logger.info(
          "Created workers_ai_post_run_cf_openai_whisper_large_v3_turbo {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_openai_whisper_tiny_en: {
      description: "Execute @cf/openai/whisper-tiny-en model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/openai/whisper-tiny-en${qs}`,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_openai_whisper_tiny_en",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_openai_whisper_tiny_en",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_openchat_openchat_3_5_0106: {
      description: "Execute @cf/openchat/openchat-3.5-0106 model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/openchat/openchat-3.5-0106${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_openchat_openchat_3_5_0106",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_openchat_openchat_3_5_0106",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    create_workers_ai_post_run_cf_pfnet_plamo_embedding_1b: {
      description: "Execute @cf/pfnet/plamo-embedding-1b model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
        text: z.union([z.string(), z.array(z.string())]).describe(
          "Input text to embed. Can be a single string or a list of strings.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }
        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/pfnet/plamo-embedding-1b${qs}`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "workers_ai_post_run_cf_pfnet_plamo_embedding_1b",
          id,
          result,
        );
        context.logger.info(
          "Created workers_ai_post_run_cf_pfnet_plamo_embedding_1b {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    get_workers_ai_post_websocket_run_cf_pipecat_ai_smart_turn_v2: {
      description:
        "Open Websocket connection with @cf/pipecat-ai/smart-turn-v2 model.",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai/run/@cf/pipecat-ai/smart-turn-v2`,
        );

        const handle = await context.writeResource(
          "workers_ai_post_websocket_run_cf_pipecat_ai_smart_turn_v2",
          "latest",
          result,
        );
        context.logger.info(
          "Fetched workers_ai_post_websocket_run_cf_pipecat_ai_smart_turn_v2",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    get_workers_ai_post_websocket_run_cf_pipecat_ai_smart_turn_v3: {
      description:
        "Open Websocket connection with @cf/pipecat-ai/smart-turn-v3 model.",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai/run/@cf/pipecat-ai/smart-turn-v3`,
        );

        const handle = await context.writeResource(
          "workers_ai_post_websocket_run_cf_pipecat_ai_smart_turn_v3",
          "latest",
          result,
        );
        context.logger.info(
          "Fetched workers_ai_post_websocket_run_cf_pipecat_ai_smart_turn_v3",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_qwen_qwen1_5_0_5b_chat: {
      description: "Execute @cf/qwen/qwen1.5-0.5b-chat model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/qwen/qwen1.5-0.5b-chat${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_qwen_qwen1_5_0_5b_chat",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_qwen_qwen1_5_0_5b_chat",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_qwen_qwen1_5_1_8b_chat: {
      description: "Execute @cf/qwen/qwen1.5-1.8b-chat model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/qwen/qwen1.5-1.8b-chat${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_qwen_qwen1_5_1_8b_chat",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_qwen_qwen1_5_1_8b_chat",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_qwen_qwen1_5_14b_chat_awq: {
      description: "Execute @cf/qwen/qwen1.5-14b-chat-awq model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/qwen/qwen1.5-14b-chat-awq${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_qwen_qwen1_5_14b_chat_awq",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_qwen_qwen1_5_14b_chat_awq",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_qwen_qwen1_5_7b_chat_awq: {
      description: "Execute @cf/qwen/qwen1.5-7b-chat-awq model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/qwen/qwen1.5-7b-chat-awq${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_qwen_qwen1_5_7b_chat_awq",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_qwen_qwen1_5_7b_chat_awq",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_qwen_qwen2_5_coder_32b_instruct: {
      description: "Execute @cf/qwen/qwen2.5-coder-32b-instruct model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/qwen/qwen2.5-coder-32b-instruct${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_qwen_qwen2_5_coder_32b_instruct",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_qwen_qwen2_5_coder_32b_instruct",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_qwen_qwen3_30b_a3b_fp8: {
      description: "Execute @cf/qwen/qwen3-30b-a3b-fp8 model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/qwen/qwen3-30b-a3b-fp8${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_qwen_qwen3_30b_a3b_fp8",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_qwen_qwen3_30b_a3b_fp8",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    create_workers_ai_post_run_cf_qwen_qwen3_embedding_0_6b: {
      description: "Execute @cf/qwen/qwen3-embedding-0.6b model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
        documents: z.union([z.string().min(1), z.array(z.string().min(1))])
          .optional(),
        instruction: z.string().optional().describe(
          "Optional instruction for the task",
        ),
        queries: z.union([z.string().min(1), z.array(z.string().min(1))])
          .optional(),
        text: z.union([z.string().min(1), z.array(z.string().min(1))])
          .optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }
        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/qwen/qwen3-embedding-0.6b${qs}`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "workers_ai_post_run_cf_qwen_qwen3_embedding_0_6b",
          id,
          result,
        );
        context.logger.info(
          "Created workers_ai_post_run_cf_qwen_qwen3_embedding_0_6b {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_qwen_qwq_32b: {
      description: "Execute @cf/qwen/qwq-32b model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/qwen/qwq-32b${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_qwen_qwq_32b",
          "latest",
          result ?? {},
        );
        context.logger.info("Executed workers_ai_post_run_cf_qwen_qwq_32b", {});
        return { dataHandles: [handle] };
      },
    },
    create_workers_ai_post_run_cf_runwayml_stable_diffusion_v1_5_img2img: {
      description: "Execute @cf/runwayml/stable-diffusion-v1-5-img2img model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
        guidance: z.number().optional().describe(
          "Controls how closely the generated image should adhere to the prompt; higher ...",
        ),
        height: z.number().int().min(256).max(2048).optional().describe(
          "The height of the generated image in pixels",
        ),
        image: z.array(z.number()).optional().describe(
          "For use with img2img tasks. An array of integers that represent the image dat...",
        ),
        image_b64: z.string().optional().describe(
          "For use with img2img tasks. A base64-encoded string of the input image",
        ),
        mask: z.array(z.number()).optional().describe(
          "An array representing An array of integers that represent mask image data for...",
        ),
        negative_prompt: z.string().optional().describe(
          "Text describing elements to avoid in the generated image",
        ),
        num_steps: z.number().int().max(20).optional().describe(
          "The number of diffusion steps; higher values can improve quality but take longer",
        ),
        prompt: z.string().min(1).describe(
          "A text description of the image you want to generate",
        ),
        seed: z.number().int().optional().describe(
          "Random seed for reproducibility of the image generation",
        ),
        strength: z.number().optional().describe(
          "A value between 0 and 1 indicating how strongly to apply the transformation d...",
        ),
        width: z.number().int().min(256).max(2048).optional().describe(
          "The width of the generated image in pixels",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }
        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/runwayml/stable-diffusion-v1-5-img2img${qs}`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "workers_ai_post_run_cf_runwayml_stable_diffusion_v1_5_img2img",
          id,
          result,
        );
        context.logger.info(
          "Created workers_ai_post_run_cf_runwayml_stable_diffusion_v1_5_img2img {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    create_workers_ai_post_run_cf_runwayml_stable_diffusion_v1_5_inpainting: {
      description:
        "Execute @cf/runwayml/stable-diffusion-v1-5-inpainting model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
        guidance: z.number().optional().describe(
          "Controls how closely the generated image should adhere to the prompt; higher ...",
        ),
        height: z.number().int().min(256).max(2048).optional().describe(
          "The height of the generated image in pixels",
        ),
        image: z.array(z.number()).optional().describe(
          "For use with img2img tasks. An array of integers that represent the image dat...",
        ),
        image_b64: z.string().optional().describe(
          "For use with img2img tasks. A base64-encoded string of the input image",
        ),
        mask: z.array(z.number()).optional().describe(
          "An array representing An array of integers that represent mask image data for...",
        ),
        negative_prompt: z.string().optional().describe(
          "Text describing elements to avoid in the generated image",
        ),
        num_steps: z.number().int().max(20).optional().describe(
          "The number of diffusion steps; higher values can improve quality but take longer",
        ),
        prompt: z.string().min(1).describe(
          "A text description of the image you want to generate",
        ),
        seed: z.number().int().optional().describe(
          "Random seed for reproducibility of the image generation",
        ),
        strength: z.number().optional().describe(
          "A value between 0 and 1 indicating how strongly to apply the transformation d...",
        ),
        width: z.number().int().min(256).max(2048).optional().describe(
          "The width of the generated image in pixels",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }
        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/runwayml/stable-diffusion-v1-5-inpainting${qs}`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "workers_ai_post_run_cf_runwayml_stable_diffusion_v1_5_inpainting",
          id,
          result,
        );
        context.logger.info(
          "Created workers_ai_post_run_cf_runwayml_stable_diffusion_v1_5_inpainting {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    create_workers_ai_post_run_cf_stabilityai_stable_diffusion_xl_base_1_0: {
      description:
        "Execute @cf/stabilityai/stable-diffusion-xl-base-1.0 model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
        guidance: z.number().optional().describe(
          "Controls how closely the generated image should adhere to the prompt; higher ...",
        ),
        height: z.number().int().min(256).max(2048).optional().describe(
          "The height of the generated image in pixels",
        ),
        image: z.array(z.number()).optional().describe(
          "For use with img2img tasks. An array of integers that represent the image dat...",
        ),
        image_b64: z.string().optional().describe(
          "For use with img2img tasks. A base64-encoded string of the input image",
        ),
        mask: z.array(z.number()).optional().describe(
          "An array representing An array of integers that represent mask image data for...",
        ),
        negative_prompt: z.string().optional().describe(
          "Text describing elements to avoid in the generated image",
        ),
        num_steps: z.number().int().max(20).optional().describe(
          "The number of diffusion steps; higher values can improve quality but take longer",
        ),
        prompt: z.string().min(1).describe(
          "A text description of the image you want to generate",
        ),
        seed: z.number().int().optional().describe(
          "Random seed for reproducibility of the image generation",
        ),
        strength: z.number().optional().describe(
          "A value between 0 and 1 indicating how strongly to apply the transformation d...",
        ),
        width: z.number().int().min(256).max(2048).optional().describe(
          "The width of the generated image in pixels",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }
        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/stabilityai/stable-diffusion-xl-base-1.0${qs}`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "workers_ai_post_run_cf_stabilityai_stable_diffusion_xl_base_1_0",
          id,
          result,
        );
        context.logger.info(
          "Created workers_ai_post_run_cf_stabilityai_stable_diffusion_xl_base_1_0 {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    get_workers_ai_post_websocket_run_cf_sven_test_pipe_http: {
      description:
        "Open Websocket connection with @cf/sven/test-pipe-http model.",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai/run/@cf/sven/test-pipe-http`,
        );

        const handle = await context.writeResource(
          "workers_ai_post_websocket_run_cf_sven_test_pipe_http",
          "latest",
          result,
        );
        context.logger.info(
          "Fetched workers_ai_post_websocket_run_cf_sven_test_pipe_http",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    get_workers_ai_post_websocket_run_cf_test_hello_world_cog: {
      description:
        "Open Websocket connection with @cf/test/hello-world-cog model.",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/ai/run/@cf/test/hello-world-cog`,
        );

        const handle = await context.writeResource(
          "workers_ai_post_websocket_run_cf_test_hello_world_cog",
          "latest",
          result,
        );
        context.logger.info(
          "Fetched workers_ai_post_websocket_run_cf_test_hello_world_cog",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_thebloke_discolm_german_7b_v1_awq: {
      description: "Execute @cf/thebloke/discolm-german-7b-v1-awq model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/thebloke/discolm-german-7b-v1-awq${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_thebloke_discolm_german_7b_v1_awq",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_thebloke_discolm_german_7b_v1_awq",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_tiiuae_falcon_7b_instruct: {
      description: "Execute @cf/tiiuae/falcon-7b-instruct model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/tiiuae/falcon-7b-instruct${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_tiiuae_falcon_7b_instruct",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_tiiuae_falcon_7b_instruct",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_tinyllama_tinyllama_1_1b_chat_v1_0: {
      description: "Execute @cf/tinyllama/tinyllama-1.1b-chat-v1.0 model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/tinyllama/tinyllama-1.1b-chat-v1.0${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_tinyllama_tinyllama_1_1b_chat_v1_0",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_tinyllama_tinyllama_1_1b_chat_v1_0",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_zai_org_glm_4_7_flash: {
      description: "Execute @cf/zai-org/glm-4.7-flash model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/zai-org/glm-4.7-flash${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_zai_org_glm_4_7_flash",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_zai_org_glm_4_7_flash",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_cf_zai_org_glm_5_2: {
      description: "Execute @cf/zai-org/glm-5.2 model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@cf/zai-org/glm-5.2${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_cf_zai_org_glm_5_2",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_cf_zai_org_glm_5_2",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_hf_google_gemma_7b_it: {
      description: "Execute @hf/google/gemma-7b-it model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@hf/google/gemma-7b-it${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_hf_google_gemma_7b_it",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_hf_google_gemma_7b_it",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_hf_mistral_mistral_7b_instruct_v0_2: {
      description: "Execute @hf/mistral/mistral-7b-instruct-v0.2 model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@hf/mistral/mistral-7b-instruct-v0.2${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_hf_mistral_mistral_7b_instruct_v0_2",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_hf_mistral_mistral_7b_instruct_v0_2",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_hf_nexusflow_starling_lm_7b_beta: {
      description: "Execute @hf/nexusflow/starling-lm-7b-beta model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@hf/nexusflow/starling-lm-7b-beta${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_hf_nexusflow_starling_lm_7b_beta",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_hf_nexusflow_starling_lm_7b_beta",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_hf_nousresearch_hermes_2_pro_mistral_7b: {
      description: "Execute @hf/nousresearch/hermes-2-pro-mistral-7b model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@hf/nousresearch/hermes-2-pro-mistral-7b${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_hf_nousresearch_hermes_2_pro_mistral_7b",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_hf_nousresearch_hermes_2_pro_mistral_7b",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_hf_thebloke_deepseek_coder_6_7b_base_awq: {
      description: "Execute @hf/thebloke/deepseek-coder-6.7b-base-awq model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@hf/thebloke/deepseek-coder-6.7b-base-awq${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_hf_thebloke_deepseek_coder_6_7b_base_awq",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_hf_thebloke_deepseek_coder_6_7b_base_awq",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_hf_thebloke_deepseek_coder_6_7b_instruct_awq: {
      description:
        "Execute @hf/thebloke/deepseek-coder-6.7b-instruct-awq model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@hf/thebloke/deepseek-coder-6.7b-instruct-awq${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_hf_thebloke_deepseek_coder_6_7b_instruct_awq",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_hf_thebloke_deepseek_coder_6_7b_instruct_awq",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_hf_thebloke_llama_2_13b_chat_awq: {
      description: "Execute @hf/thebloke/llama-2-13b-chat-awq model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@hf/thebloke/llama-2-13b-chat-awq${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_hf_thebloke_llama_2_13b_chat_awq",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_hf_thebloke_llama_2_13b_chat_awq",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_hf_thebloke_mistral_7b_instruct_v0_1_awq: {
      description: "Execute @hf/thebloke/mistral-7b-instruct-v0.1-awq model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@hf/thebloke/mistral-7b-instruct-v0.1-awq${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_hf_thebloke_mistral_7b_instruct_v0_1_awq",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_hf_thebloke_mistral_7b_instruct_v0_1_awq",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_hf_thebloke_neural_chat_7b_v3_1_awq: {
      description: "Execute @hf/thebloke/neural-chat-7b-v3-1-awq model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@hf/thebloke/neural-chat-7b-v3-1-awq${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_hf_thebloke_neural_chat_7b_v3_1_awq",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_hf_thebloke_neural_chat_7b_v3_1_awq",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_hf_thebloke_openhermes_2_5_mistral_7b_awq: {
      description: "Execute @hf/thebloke/openhermes-2.5-mistral-7b-awq model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@hf/thebloke/openhermes-2.5-mistral-7b-awq${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_hf_thebloke_openhermes_2_5_mistral_7b_awq",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_hf_thebloke_openhermes_2_5_mistral_7b_awq",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_hf_thebloke_zephyr_7b_beta_awq: {
      description: "Execute @hf/thebloke/zephyr-7b-beta-awq model.",
      arguments: z.object({
        queueRequest: z.string().optional(),
        tags: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const queryParts: string[] = [];
        const queryKeys = new Set(["queueRequest", "tags"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && queryKeys.has(k)) {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/@hf/thebloke/zephyr-7b-beta-awq${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_hf_thebloke_zephyr_7b_beta_awq",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed workers_ai_post_run_hf_thebloke_zephyr_7b_beta_awq",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_run_model: {
      description: "Execute AI model",
      arguments: z.object({
        model_name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["model_name"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/run/${args.model_name}`,
          body,
        );

        const handle = await context.writeResource(
          "workers_ai_post_run_model",
          "latest",
          result ?? {},
        );
        context.logger.info("Executed workers_ai_post_run_model", {});
        return { dataHandles: [handle] };
      },
    },
    list_workers_ai_search_task: {
      description: "Task Search",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/ai/tasks/search`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "workers_ai_search_task",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} workers_ai_search_task", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    workers_ai_post_to_markdown: {
      description: "Convert Files into Markdown",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/ai/tomarkdown`,
        );

        const handle = await context.writeResource(
          "workers_ai_post_to_markdown",
          "latest",
          result ?? {},
        );
        context.logger.info("Executed workers_ai_post_to_markdown", {});
        return { dataHandles: [handle] };
      },
    },
    get_to_markdown_supported: {
      description: "Get all converted formats supported",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/ai/tomarkdown/supported`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "get_to_markdown_supported",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} get_to_markdown_supported", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
  },
};
