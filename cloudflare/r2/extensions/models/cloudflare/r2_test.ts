// Cloudflare R2 object storage — buckets, objects, multipart uploads, notifications — Tests
// Auto-generated by scripts/cloudflare-codegen — do not edit manually.
// SPDX-License-Identifier: Apache-2.0

import { assertEquals, assertExists } from "jsr:@std/assert@1";
import { createModelTestContext } from "@systeminit/swamp-testing";
import { model } from "./r2.ts";

// ---------------------------------------------------------------------------
// Model Export Structure Tests
// ---------------------------------------------------------------------------

Deno.test("r2 model: has correct type", () => {
  assertEquals(model.type, "@webframp/cloudflare/r2");
});

Deno.test("r2 model: has valid version format", () => {
  const versionPattern = /^\d{4}\.\d{2}\.\d{2}\.\d+$/;
  assertEquals(versionPattern.test(model.version), true);
});

Deno.test("r2 model: has globalArguments with apiToken", () => {
  assertExists(model.globalArguments);
  const shape = model.globalArguments.shape;
  assertExists(shape.apiToken);
  assertExists(shape.accountId);
});

Deno.test("r2 model: has expected methods", () => {
  assertExists(model.methods);
  assertExists(model.methods.list_catalogs);
  assertExists(model.methods.get_catalog_details);
  assertExists(model.methods.create_store_credentials);
  assertExists(model.methods.disable_catalog);
  assertExists(model.methods.enable_catalog);
  assertExists(model.methods.get_maintenance_config);
  assertExists(model.methods.update_maintenance_config);
  assertExists(model.methods.list_namespaces);
  assertExists(model.methods.list_tables);
  assertExists(model.methods.get_table);
  assertExists(model.methods.get_table_maintenance_config);
  assertExists(model.methods.update_table_maintenance_config);
  assertExists(model.methods.list_buckets);
  assertExists(model.methods.create_bucket);
  assertExists(model.methods.get_bucket);
  assertExists(model.methods.patch_bucket);
  assertExists(model.methods.delete_bucket);
  assertExists(model.methods.get_bucket_cors_policy);
  assertExists(model.methods.put_bucket_cors_policy);
  assertExists(model.methods.delete_bucket_cors_policy);
  assertExists(model.methods.list_custom_domains);
  assertExists(model.methods.create_r2_add_custom_domain);
  assertExists(model.methods.get_custom_domain_settings);
  assertExists(model.methods.update_r2_edit_custom_domain_settings);
  assertExists(model.methods.delete_custom_domain);
  assertExists(model.methods.get_bucket_public_policy);
  assertExists(model.methods.put_bucket_public_policy);
  assertExists(model.methods.get_bucket_lifecycle_configuration);
  assertExists(model.methods.put_bucket_lifecycle_configuration);
  assertExists(model.methods.get_bucket_local_uploads_configuration);
  assertExists(model.methods.put_bucket_local_uploads_configuration);
  assertExists(model.methods.get_bucket_lock_configuration);
  assertExists(model.methods.put_bucket_lock_configuration);
  assertExists(model.methods.list_objects);
  assertExists(model.methods.delete_objects);
  assertExists(model.methods.get_object);
  assertExists(model.methods.put_object);
  assertExists(model.methods.delete_object);
  assertExists(model.methods.get_bucket_sippy_config);
  assertExists(model.methods.put_bucket_sippy_config);
  assertExists(model.methods.delete_bucket_sippy_config);
  assertExists(model.methods.get_account_level_metrics);
  assertExists(model.methods.create_temp_access_credentials);
});

Deno.test("r2 model: has expected resources", () => {
  assertExists(model.resources);
  assertExists(model.resources["list_catalogs"]);
  assertExists(model.resources["catalog_details"]);
  assertExists(model.resources["store_credentials"]);
  assertExists(model.resources["disable_catalog"]);
  assertExists(model.resources["enable_catalog"]);
  assertExists(model.resources["maintenance_config"]);
  assertExists(model.resources["list_namespaces"]);
  assertExists(model.resources["list_tables"]);
  assertExists(model.resources["table"]);
  assertExists(model.resources["table_maintenance_config"]);
  assertExists(model.resources["list_buckets"]);
  assertExists(model.resources["bucket"]);
  assertExists(model.resources["patch_bucket"]);
  assertExists(model.resources["bucket_cors_policy"]);
  assertExists(model.resources["put_bucket_cors_policy"]);
  assertExists(model.resources["list_custom_domains"]);
  assertExists(model.resources["r2_add_custom_domain"]);
  assertExists(model.resources["custom_domain_settings"]);
  assertExists(model.resources["r2_edit_custom_domain_settings"]);
  assertExists(model.resources["bucket_public_policy"]);
  assertExists(model.resources["put_bucket_public_policy"]);
  assertExists(model.resources["bucket_lifecycle_configuration"]);
  assertExists(model.resources["put_bucket_lifecycle_configuration"]);
  assertExists(model.resources["bucket_local_uploads_configuration"]);
  assertExists(model.resources["put_bucket_local_uploads_configuration"]);
  assertExists(model.resources["bucket_lock_configuration"]);
  assertExists(model.resources["put_bucket_lock_configuration"]);
  assertExists(model.resources["objects"]);
  assertExists(model.resources["object"]);
  assertExists(model.resources["put_object"]);
  assertExists(model.resources["bucket_sippy_config"]);
  assertExists(model.resources["put_bucket_sippy_config"]);
  assertExists(model.resources["account_level_metrics"]);
  assertExists(model.resources["temp_access_credentials"]);
});

// ---------------------------------------------------------------------------
// Mock Cloudflare API Server
// ---------------------------------------------------------------------------

function startMockCfServer(
  responses: Record<string, { result: unknown; isCollection?: boolean }>,
): { url: string; server: Deno.HttpServer } {
  const server = Deno.serve({ port: 0, onListen() {} }, (req) => {
    const url = new URL(req.url);
    const path = url.pathname;

    for (
      const [pattern, { result, isCollection }] of Object.entries(responses)
    ) {
      if (path.includes(pattern)) {
        return Response.json({
          success: true,
          errors: [],
          messages: [],
          result,
          result_info: isCollection
            ? {
              page: 1,
              per_page: 50,
              total_count: Array.isArray(result) ? result.length : 1,
              total_pages: 1,
            }
            : undefined,
        });
      }
    }

    return Response.json({
      success: false,
      errors: [{ code: 404, message: "Not found" }],
      messages: [],
      result: null,
    });
  });

  const addr = server.addr as Deno.NetAddr;
  return { url: `http://localhost:${addr.port}`, server };
}

function installFetchMock(mockUrl: string): () => void {
  const originalFetch = globalThis.fetch;
  globalThis.fetch = (input, init) => {
    const reqUrl = typeof input === "string" ? input : input.toString();
    const newUrl = reqUrl.replace(
      "https://api.cloudflare.com/client/v4",
      mockUrl,
    );
    return originalFetch(newUrl, init);
  };
  return () => {
    globalThis.fetch = originalFetch;
  };
}

Deno.test({
  name: "r2 model: list_catalogs fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = { "warehouses": [null] };
    const { url, server } = startMockCfServer({
      "/r2-catalog": { result: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: { id: "test-id", name: "test-r2", version: 1, tags: {} },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).list_catalogs.execute({}, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "r2 model: create_store_credentials creates and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = { "id": "new-123" };
    const { url, server } = startMockCfServer({
      "/r2-catalog/test-id-123/credential": { result: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: { id: "test-id", name: "test-r2", version: 1, tags: {} },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).create_store_credentials.execute(
        { "bucket_name": "test-id-123" },
        context,
      );
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "r2 model: disable_catalog executes and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = { "id": "fixture-123" };
    const { url, server } = startMockCfServer({
      "/r2-catalog/test-id-123/disable": { result: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: { id: "test-id", name: "test-r2", version: 1, tags: {} },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).disable_catalog.execute({
        "bucket_name": "test-id-123",
        "id": "fixture-123",
      }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "r2 model: patch_bucket executes and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "creation_date": "test-value",
      "jurisdiction": null,
      "location": null,
      "name": null,
      "storage_class": null,
    };
    const { url, server } = startMockCfServer({
      "/r2/buckets/test-id-123": { result: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: { id: "test-id", name: "test-r2", version: 1, tags: {} },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).patch_bucket.execute({
        "bucket_name": "test-id-123",
        "creation_date": "test-value",
        "jurisdiction": null,
        "location": null,
        "name": null,
        "storage_class": null,
      }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "r2 model: delete_bucket executes successfully",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockCfServer({
      "/r2/buckets/test-id-123": { result: { id: "test-id-123" } },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: { id: "test-id", name: "test-r2", version: 1, tags: {} },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).delete_bucket.execute({ "bucket_name": "test-id-123" }, context);
      assertEquals(result.dataHandles.length, 0);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "r2 model: list_objects fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = [{
      "custom_metadata": {},
      "etag": "d41d8cd98f00b204e9800998ecf8427e",
      "http_metadata": null,
      "key": "path/to/my-object.txt",
      "last_modified": "2024-01-15T10:30:00Z",
      "size": 1048576,
      "ssec": false,
      "storage_class": null,
    }];
    const { url, server } = startMockCfServer({
      "/r2/buckets/test-id-123/objects": {
        result: mockData,
        isCollection: true,
      },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: { id: "test-id", name: "test-r2", version: 1, tags: {} },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).list_objects.execute({ "bucket_name": "test-id-123" }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});
