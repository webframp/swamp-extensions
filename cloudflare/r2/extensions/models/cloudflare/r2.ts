/**
 * Cloudflare R2 object storage — buckets, objects, multipart uploads, notifications
 *
 * Auto-generated by scripts/cloudflare-codegen — do not edit manually.
 *
 * @module
 */
// SPDX-License-Identifier: AGPL-3.0-or-later WITH Swamp-Extension-Exception

import { z } from "npm:zod@4.4.3";
import { cfApi, cfApiPaginated } from "./_lib/api.ts";

// =============================================================================
// Schemas
// =============================================================================

const GlobalArgsSchema = z.object({
  apiToken: z.string().meta({ sensitive: true }).describe(
    "Cloudflare API token",
  ),
  accountId: z.string().describe("Cloudflare account ID"),
});

const ListCatalogsSchema = z.object({
  warehouses: z.array(z.unknown()).describe("Lists catalogs in the account."),
});

const GetCatalogDetailsSchema = z.object({
  bucket: z.string().describe("Specifies the associated R2 bucket name."),
  credential_status: z.string().nullable().optional().describe(
    "Shows the credential configuration status.",
  ),
  id: z.string().describe("Use this to uniquely identify the catalog."),
  maintenance_config: z.object({}).nullable().optional().describe(
    "Configures maintenance for the catalog.",
  ),
  name: z.string().describe(
    "Specifies the catalog name (generated from account and bucket name).",
  ),
  status: z.unknown(),
});

const CreateStoreCredentialsSchema = z.object({}).nullable();

const EnableCatalogSchema = z.object({
  id: z.string().describe(
    "Use this to uniquely identify the activated catalog.",
  ),
  name: z.string().describe("Specifies the name of the activated catalog."),
});

const GetMaintenanceConfigSchema = z.object({
  credential_status: z.unknown(),
  maintenance_config: z.unknown(),
});

const ListNamespacesSchema = z.object({
  details: z.array(z.unknown()).nullable().optional().describe(
    "Contains detailed metadata for each namespace when return_details is true. Each object includes t...",
  ),
  namespace_uuids: z.array(z.string()).nullable().optional().describe(
    "Contains UUIDs for each namespace when return_uuids is true. The order corresponds to the namespa...",
  ),
  namespaces: z.array(z.unknown()).describe("Lists namespaces in the catalog."),
  next_page_token: z.string().nullable().optional().describe(
    "Use this opaque token to fetch the next page of results. A null or absent value indicates the las...",
  ),
});

const ListTablesSchema = z.object({
  details: z.array(z.unknown()).nullable().optional().describe(
    "Contains detailed metadata for each table when return_details is true. Each object includes ident...",
  ),
  identifiers: z.array(z.unknown()).describe("Lists tables in the namespace."),
  next_page_token: z.string().nullable().optional().describe(
    "Use this opaque token to fetch the next page of results. A null or absent value indicates the las...",
  ),
  table_uuids: z.array(z.string()).nullable().optional().describe(
    "Contains UUIDs for each table when return_uuids is true. The order corresponds to the identifiers...",
  ),
});

const GetTableSchema = z.object({
  identifier: z.unknown(),
  metadata: z.unknown(),
  metadata_location: z.string().optional().describe(
    "Specifies the S3-compatible URI to the current Iceberg metadata file in R2. Omitted for staged ta...",
  ),
  returned_snapshots: z.number().int().min(0).describe(
    "Describes the number of snapshots that appear in `metadata.snapshots`. Caps the list at 10 (the m...",
  ),
  table_uuid: z.string().describe(
    "Contains the Iceberg table UUID, stable across renames.",
  ),
  total_snapshots: z.number().int().min(0).describe(
    "Indicates the total number of snapshots stored for the table, before pruning.",
  ),
});

const GetTableMaintenanceConfigSchema = z.object({
  maintenance_config: z.unknown(),
});

const ListBucketsSchema = z.object({
  buckets: z.array(z.unknown()).optional(),
});

const CreateBucketSchema = z.object({
  creation_date: z.string().optional().describe("Creation timestamp."),
  jurisdiction: z.unknown().optional(),
  location: z.unknown().optional(),
  name: z.unknown().optional(),
  storage_class: z.unknown().optional(),
});

const PatchBucketSchema = z.object({
  creation_date: z.string().optional().describe("Creation timestamp."),
  jurisdiction: z.unknown().optional(),
  location: z.unknown().optional(),
  name: z.unknown().optional(),
  storage_class: z.unknown().optional(),
});

const GetBucketCorsPolicySchema = z.object({
  rules: z.array(z.unknown()).optional(),
});

const PutBucketCorsPolicySchema = z.object({});

const ListCustomDomainsSchema = z.object({
  domains: z.array(z.object({
    ciphers: z.array(z.string()).optional(),
    domain: z.string(),
    enabled: z.boolean(),
    minTLS: z.enum(["1.0", "1.1", "1.2", "1.3"]).optional(),
    status: z.object({
      ownership: z.enum([
        "pending",
        "active",
        "deactivated",
        "blocked",
        "error",
        "unknown",
      ]),
      ssl: z.enum([
        "initializing",
        "pending",
        "active",
        "deactivated",
        "error",
        "unknown",
      ]),
    }),
    zoneId: z.string().optional(),
    zoneName: z.string().optional(),
  })),
});

const CreateR2AddCustomDomainSchema = z.object({
  ciphers: z.array(z.string()).optional().describe(
    "An allowlist of ciphers for TLS termination. These ciphers must be in the BoringSSL format.",
  ),
  domain: z.string().describe("Domain name of the affected custom domain."),
  enabled: z.boolean().describe(
    "Whether this bucket is publicly accessible at the specified custom domain.",
  ),
  minTLS: z.enum(["1.0", "1.1", "1.2", "1.3"]).optional().describe(
    "Minimum TLS Version the custom domain will accept for incoming connections. If not set, defaults ...",
  ),
  zoneId: z.string().describe("Zone ID of the custom domain."),
});

const GetCustomDomainSettingsSchema = z.object({
  ciphers: z.array(z.string()).optional().describe(
    "An allowlist of ciphers for TLS termination. These ciphers must be in the BoringSSL format.",
  ),
  domain: z.string().describe("Domain name of the custom domain to be added."),
  enabled: z.boolean().describe(
    "Whether this bucket is publicly accessible at the specified custom domain.",
  ),
  minTLS: z.enum(["1.0", "1.1", "1.2", "1.3"]).optional().describe(
    "Minimum TLS Version the custom domain will accept for incoming connections. If not set, defaults ...",
  ),
  status: z.object({
    ownership: z.enum([
      "pending",
      "active",
      "deactivated",
      "blocked",
      "error",
      "unknown",
    ]),
    ssl: z.enum([
      "initializing",
      "pending",
      "active",
      "deactivated",
      "error",
      "unknown",
    ]),
  }),
  zoneId: z.string().optional().describe(
    "Zone ID of the custom domain resides in.",
  ),
  zoneName: z.string().optional().describe(
    "Zone that the custom domain resides in.",
  ),
});

const UpdateR2EditCustomDomainSettingsSchema = z.object({
  ciphers: z.array(z.string()).optional().describe(
    "An allowlist of ciphers for TLS termination. These ciphers must be in the BoringSSL format.",
  ),
  domain: z.string().describe("Domain name of the affected custom domain."),
  enabled: z.boolean().optional().describe(
    "Whether this bucket is publicly accessible at the specified custom domain.",
  ),
  minTLS: z.enum(["1.0", "1.1", "1.2", "1.3"]).optional().describe(
    "Minimum TLS Version the custom domain will accept for incoming connections. If not set, defaults ...",
  ),
});

const GetBucketPublicPolicySchema = z.object({
  bucketId: z.string().max(32).describe("Bucket ID."),
  domain: z.string().describe("Domain name of the bucket's r2.dev domain."),
  enabled: z.boolean().describe(
    "Whether this bucket is publicly accessible at the r2.dev domain.",
  ),
});

const PutBucketPublicPolicySchema = z.object({
  bucketId: z.string().max(32).describe("Bucket ID."),
  domain: z.string().describe("Domain name of the bucket's r2.dev domain."),
  enabled: z.boolean().describe(
    "Whether this bucket is publicly accessible at the r2.dev domain.",
  ),
});

const GetBucketLifecycleConfigurationSchema = z.object({
  rules: z.array(z.unknown()).optional(),
});

const PutBucketLifecycleConfigurationSchema = z.object({});

const GetBucketLocalUploadsConfigurationSchema = z.object({
  enabled: z.boolean().optional().describe(
    "Whether local uploads is enabled for this bucket. When enabled, object's data is written to the n...",
  ),
});

const PutBucketLocalUploadsConfigurationSchema = z.object({});

const GetBucketLockConfigurationSchema = z.object({
  rules: z.array(z.unknown()).optional(),
});

const PutBucketLockConfigurationSchema = z.object({});

const ObjectsItemSchema = z.object({
  custom_metadata: z.record(z.string(), z.string()).optional().describe(
    "Custom metadata key-value pairs associated with the object.",
  ),
  etag: z.string().optional().describe(
    "The entity tag for the object. In JSON list/get responses this is the raw hex digest (without sur...",
  ),
  http_metadata: z.unknown().optional(),
  key: z.string().optional().describe("The object key (name)."),
  last_modified: z.string().optional().describe(
    "The date and time the object was last modified.",
  ),
  size: z.number().int().optional().describe(
    "The size of the object in bytes.",
  ),
  ssec: z.boolean().optional().describe(
    "Whether the object is encrypted with a customer-supplied encryption key.",
  ),
  storage_class: z.unknown().optional(),
});

const ListObjectsSchema = z.object({
  items: z.array(ObjectsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const PutObjectSchema = z.object({
  etag: z.string().optional().describe(
    "The entity tag for the uploaded object.",
  ),
  key: z.string().optional().describe("The key (name) of the uploaded object."),
  size: z.string().optional().describe(
    "The size of the uploaded object in bytes (as a string).",
  ),
  storage_class: z.unknown().optional(),
  uploaded: z.string().optional().describe(
    "The date and time the object was uploaded.",
  ),
  version: z.string().optional().describe(
    "The version UUID of the uploaded object.",
  ),
});

const GetBucketSippyConfigSchema = z.object({
  destination: z.object({
    accessKeyId: z.string().optional(),
    account: z.string().optional(),
    bucket: z.string().optional(),
    provider: z.enum(["r2"]).optional(),
  }).optional().describe("Details about the configured destination bucket."),
  enabled: z.boolean().optional().describe("State of Sippy for this bucket."),
  source: z.object({
    bucket: z.string().nullable().optional(),
    bucketUrl: z.string().nullable().optional(),
    provider: z.enum(["aws", "gcs", "s3"]).optional(),
    region: z.string().nullable().optional(),
  }).optional().describe("Details about the configured source bucket."),
});

const PutBucketSippyConfigSchema = z.object({
  destination: z.object({
    accessKeyId: z.string().optional(),
    account: z.string().optional(),
    bucket: z.string().optional(),
    provider: z.enum(["r2"]).optional(),
  }).optional().describe("Details about the configured destination bucket."),
  enabled: z.boolean().optional().describe("State of Sippy for this bucket."),
  source: z.object({
    bucket: z.string().nullable().optional(),
    bucketUrl: z.string().nullable().optional(),
    provider: z.enum(["aws", "gcs", "s3"]).optional(),
    region: z.string().nullable().optional(),
  }).optional().describe("Details about the configured source bucket."),
});

const GetAccountLevelMetricsSchema = z.object({
  infrequentAccess: z.unknown().optional(),
  standard: z.unknown().optional(),
});

const CreateTempAccessCredentialsSchema = z.object({
  accessKeyId: z.string().optional().describe("ID for new access key."),
  secretAccessKey: z.string().optional().describe("Secret access key."),
  sessionToken: z.string().optional().describe("Security token."),
});

// =============================================================================
// Model Definition
// =============================================================================

/** Cloudflare R2 object storage — buckets, objects, multipart uploads, notifications */
export const model = {
  type: "@webframp/cloudflare/r2",
  version: "2026.07.19.1",
  globalArguments: GlobalArgsSchema,

  upgrades: [],

  resources: {
    "list_catalogs": {
      description: "List R2 catalogs",
      schema: ListCatalogsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "catalog_details": {
      description: "Get R2 catalog details",
      schema: GetCatalogDetailsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "store_credentials": {
      description: "Store catalog credentials",
      schema: CreateStoreCredentialsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "disable_catalog": {
      description: "Disable R2 catalog",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "enable_catalog": {
      description: "Enable R2 bucket as a catalog",
      schema: EnableCatalogSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "maintenance_config": {
      description: "Get catalog maintenance configuration",
      schema: GetMaintenanceConfigSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "list_namespaces": {
      description: "List namespaces in catalog",
      schema: ListNamespacesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "list_tables": {
      description: "List tables in namespace",
      schema: ListTablesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "table": {
      description: "Get table details",
      schema: GetTableSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "table_maintenance_config": {
      description: "Get table maintenance configuration",
      schema: GetTableMaintenanceConfigSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "list_buckets": {
      description: "List Buckets",
      schema: ListBucketsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "bucket": {
      description: "Create Bucket",
      schema: CreateBucketSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "patch_bucket": {
      description: "Patch Bucket",
      schema: PatchBucketSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "bucket_cors_policy": {
      description: "Get Bucket CORS Policy",
      schema: GetBucketCorsPolicySchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "put_bucket_cors_policy": {
      description: "Put Bucket CORS Policy",
      schema: PutBucketCorsPolicySchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "list_custom_domains": {
      description: "List Custom Domains of Bucket",
      schema: ListCustomDomainsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "r2_add_custom_domain": {
      description: "Attach Custom Domain To Bucket",
      schema: CreateR2AddCustomDomainSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "custom_domain_settings": {
      description: "Get Custom Domain Settings",
      schema: GetCustomDomainSettingsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "r2_edit_custom_domain_settings": {
      description: "Configure Custom Domain Settings",
      schema: UpdateR2EditCustomDomainSettingsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "bucket_public_policy": {
      description: "Get r2.dev Domain of Bucket",
      schema: GetBucketPublicPolicySchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "put_bucket_public_policy": {
      description: "Update r2.dev Domain of Bucket",
      schema: PutBucketPublicPolicySchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "bucket_lifecycle_configuration": {
      description: "Get Object Lifecycle Rules",
      schema: GetBucketLifecycleConfigurationSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "put_bucket_lifecycle_configuration": {
      description: "Put Object Lifecycle Rules",
      schema: PutBucketLifecycleConfigurationSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "bucket_local_uploads_configuration": {
      description: "Get Local Uploads Configuration",
      schema: GetBucketLocalUploadsConfigurationSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "put_bucket_local_uploads_configuration": {
      description: "Put Local Uploads Configuration",
      schema: PutBucketLocalUploadsConfigurationSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "bucket_lock_configuration": {
      description: "Get Bucket Lock Rules",
      schema: GetBucketLockConfigurationSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "put_bucket_lock_configuration": {
      description: "Put Bucket Lock Rules",
      schema: PutBucketLockConfigurationSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "objects": {
      description: "List Objects",
      schema: ListObjectsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "object": {
      description: "Get Object",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "put_object": {
      description: "Upload Object",
      schema: PutObjectSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "bucket_sippy_config": {
      description: "Get Sippy Configuration",
      schema: GetBucketSippyConfigSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "put_bucket_sippy_config": {
      description: "Enable Sippy",
      schema: PutBucketSippyConfigSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "account_level_metrics": {
      description: "Get Account-Level Metrics",
      schema: GetAccountLevelMetricsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "temp_access_credentials": {
      description: "Create Temporary Access Credentials",
      schema: CreateTempAccessCredentialsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
  },

  methods: {
    list_catalogs: {
      description: "List R2 catalogs",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/r2-catalog`,
        );

        const handle = await context.writeResource(
          "list_catalogs",
          "latest",
          result,
        );
        context.logger.info("Fetched list_catalogs", {});
        return { dataHandles: [handle] };
      },
    },
    get_catalog_details: {
      description: "Get R2 catalog details",
      arguments: z.object({
        bucket_name: z.string().describe("Specifies the R2 bucket name."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/r2-catalog/${args.bucket_name}`,
        );

        const handle = await context.writeResource(
          "catalog_details",
          String(args.bucket_name),
          result,
        );
        context.logger.info("Fetched catalog_details", {});
        return { dataHandles: [handle] };
      },
    },
    create_store_credentials: {
      description: "Store catalog credentials",
      arguments: z.object({
        bucket_name: z.string().describe("Specifies the R2 bucket name."),
        token: z.string().describe(
          "Provides the Cloudflare API token for accessing R2.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const pathKeys = new Set(["bucket_name"]);
        for (const [k, v] of Object.entries(args)) {
          if (!pathKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/r2-catalog/${args.bucket_name}/credential`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "store_credentials",
          id,
          result,
        );
        context.logger.info("Created store_credentials {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    disable_catalog: {
      description: "Disable R2 catalog",
      arguments: z.object({
        bucket_name: z.string().describe(
          "Specifies the R2 bucket name to disable as catalog.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/r2-catalog/${args.bucket_name}/disable`,
        );

        const handle = await context.writeResource(
          "disable_catalog",
          "latest",
          result,
        );
        context.logger.info("Executed disable_catalog", {});
        return { dataHandles: [handle] };
      },
    },
    enable_catalog: {
      description: "Enable R2 bucket as a catalog",
      arguments: z.object({
        bucket_name: z.string().describe(
          "Specifies the R2 bucket name to enable as catalog.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/r2-catalog/${args.bucket_name}/enable`,
        );

        const handle = await context.writeResource(
          "enable_catalog",
          "latest",
          result,
        );
        context.logger.info("Executed enable_catalog", {});
        return { dataHandles: [handle] };
      },
    },
    get_maintenance_config: {
      description: "Get catalog maintenance configuration",
      arguments: z.object({
        bucket_name: z.string().describe("Specifies the R2 bucket name."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/r2-catalog/${args.bucket_name}/maintenance-configs`,
        );

        const handle = await context.writeResource(
          "maintenance_config",
          String(args.bucket_name),
          result,
        );
        context.logger.info("Fetched maintenance_config", {});
        return { dataHandles: [handle] };
      },
    },
    update_maintenance_config: {
      description: "Update catalog maintenance configuration",
      arguments: z.object({
        bucket_name: z.string().describe("Specifies the R2 bucket name."),
        compaction: z.unknown().optional(),
        snapshot_expiration: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const pathKeys = new Set(["bucket_name"]);
        for (const [k, v] of Object.entries(args)) {
          if (!pathKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/r2-catalog/${args.bucket_name}/maintenance-configs`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "update_maintenance_config",
          id,
          result,
        );
        context.logger.info("Created update_maintenance_config {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    list_namespaces: {
      description: "List namespaces in catalog",
      arguments: z.object({
        bucket_name: z.string().describe("Specifies the R2 bucket name."),
        page_token: z.string().optional().describe(
          "Opaque pagination token from a previous response. Use this to fetch the next page of results. ",
        ),
        page_size: z.number().optional().describe(
          "Maximum number of namespaces to return per page. Defaults to 100, maximum 1000. ",
        ),
        parent: z.string().optional().describe(
          'Parent namespace to filter by. Only returns direct children of this namespace. For nested namespaces, use %1F as separator (e.g., "bronze%1Fanalytics"). Omit this parameter to list top-level namespaces. ',
        ),
        return_uuids: z.boolean().optional().describe(
          "Whether to include namespace UUIDs in the response. Set to true to receive the namespace_uuids array. ",
        ),
        return_details: z.boolean().optional().describe(
          "Whether to include additional metadata (timestamps). When true, response includes created_at and updated_at arrays. ",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/r2-catalog/${args.bucket_name}/namespaces`,
        );

        const handle = await context.writeResource(
          "list_namespaces",
          String(args.bucket_name),
          result,
        );
        context.logger.info("Fetched list_namespaces", {});
        return { dataHandles: [handle] };
      },
    },
    list_tables: {
      description: "List tables in namespace",
      arguments: z.object({
        bucket_name: z.string().describe("Specifies the R2 bucket name."),
        namespace: z.string().describe(
          'The namespace identifier. For nested namespaces, use %1F as separator (e.g., "bronze%1Fanalytics"). ',
        ),
        page_token: z.string().optional().describe(
          "Opaque pagination token from a previous response. Use this to fetch the next page of results. ",
        ),
        page_size: z.number().optional().describe(
          "Maximum number of tables to return per page. Defaults to 100, maximum 1000. ",
        ),
        return_uuids: z.boolean().optional().describe(
          "Whether to include table UUIDs in the response. Set to true to receive the table_uuids array. ",
        ),
        return_details: z.boolean().optional().describe(
          "Whether to include additional metadata (timestamps, locations). When true, response includes created_at, updated_at, metadata_locations, and locations arrays. ",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/r2-catalog/${args.bucket_name}/namespaces/${args.namespace}/tables`,
        );

        const handle = await context.writeResource(
          "list_tables",
          String(args.namespace),
          result,
        );
        context.logger.info("Fetched list_tables", {});
        return { dataHandles: [handle] };
      },
    },
    get_table: {
      description: "Get table details",
      arguments: z.object({
        bucket_name: z.string().describe("Specifies the R2 bucket name."),
        namespace: z.string().describe(
          'The namespace identifier. For nested namespaces, use %1F as separator (e.g., "bronze%1Fanalytics"). ',
        ),
        table_name: z.string().describe(
          "The table name within the given namespace.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/r2-catalog/${args.bucket_name}/namespaces/${args.namespace}/tables/${args.table_name}`,
        );

        const handle = await context.writeResource(
          "table",
          String(args.table_name),
          result,
        );
        context.logger.info("Fetched table", {});
        return { dataHandles: [handle] };
      },
    },
    get_table_maintenance_config: {
      description: "Get table maintenance configuration",
      arguments: z.object({
        bucket_name: z.string().describe("Specifies the R2 bucket name."),
        namespace: z.string().describe(
          "The namespace identifier (use %1F as separator for nested namespaces).",
        ),
        table_name: z.string().describe("The table name."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/r2-catalog/${args.bucket_name}/namespaces/${args.namespace}/tables/${args.table_name}/maintenance-configs`,
        );

        const handle = await context.writeResource(
          "table_maintenance_config",
          String(args.table_name),
          result,
        );
        context.logger.info("Fetched table_maintenance_config", {});
        return { dataHandles: [handle] };
      },
    },
    update_table_maintenance_config: {
      description: "Update table maintenance configuration",
      arguments: z.object({
        bucket_name: z.string().describe("Specifies the R2 bucket name."),
        namespace: z.string().describe(
          "The namespace identifier (use %1F as separator for nested namespaces).",
        ),
        table_name: z.string().describe("The table name."),
        compaction: z.unknown().optional(),
        snapshot_expiration: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const pathKeys = new Set(["bucket_name", "namespace", "table_name"]);
        for (const [k, v] of Object.entries(args)) {
          if (!pathKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/r2-catalog/${args.bucket_name}/namespaces/${args.namespace}/tables/${args.table_name}/maintenance-configs`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "update_table_maintenance_config",
          id,
          result,
        );
        context.logger.info("Created update_table_maintenance_config {id}", {
          id,
        });
        return { dataHandles: [handle] };
      },
    },
    list_buckets: {
      description: "List Buckets",
      arguments: z.object({
        name_contains: z.string().optional(),
        start_after: z.string().optional(),
        per_page: z.number().optional(),
        order: z.enum(["name"]).optional(),
        direction: z.enum(["asc", "desc"]).optional(),
        cursor: z.string().optional(),
      }),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/r2/buckets`,
        );

        const handle = await context.writeResource(
          "list_buckets",
          "latest",
          result,
        );
        context.logger.info("Fetched list_buckets", {});
        return { dataHandles: [handle] };
      },
    },
    create_bucket: {
      description: "Create Bucket",
      arguments: z.object({
        locationHint: z.unknown().optional(),
        name: z.unknown(),
        storageClass: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/r2/buckets`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource("bucket", id, result);
        context.logger.info("Created bucket {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_bucket: {
      description: "Get Bucket",
      arguments: z.object({
        bucket_name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/r2/buckets/${args.bucket_name}`,
        );

        const handle = await context.writeResource(
          "bucket",
          String(args.bucket_name),
          result,
        );
        context.logger.info("Fetched bucket", {});
        return { dataHandles: [handle] };
      },
    },
    patch_bucket: {
      description: "Patch Bucket",
      arguments: z.object({
        bucket_name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const pathKeys = new Set(["bucket_name"]);
        for (const [k, v] of Object.entries(args)) {
          if (!pathKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/accounts/${accountId}/r2/buckets/${args.bucket_name}`,
          body,
        );

        const handle = await context.writeResource(
          "patch_bucket",
          String(args.bucket_name),
          result,
        );
        context.logger.info("Updated patch_bucket", {});
        return { dataHandles: [handle] };
      },
    },
    delete_bucket: {
      description: "Delete Bucket",
      arguments: z.object({
        bucket_name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/r2/buckets/${args.bucket_name}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.bucket_name });
        return { dataHandles: [] };
      },
    },
    get_bucket_cors_policy: {
      description: "Get Bucket CORS Policy",
      arguments: z.object({
        bucket_name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/r2/buckets/${args.bucket_name}/cors`,
        );

        const handle = await context.writeResource(
          "bucket_cors_policy",
          String(args.bucket_name),
          result,
        );
        context.logger.info("Fetched bucket_cors_policy", {});
        return { dataHandles: [handle] };
      },
    },
    put_bucket_cors_policy: {
      description: "Put Bucket CORS Policy",
      arguments: z.object({
        bucket_name: z.string(),
        rules: z.array(z.unknown()).optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const pathKeys = new Set(["bucket_name"]);
        for (const [k, v] of Object.entries(args)) {
          if (!pathKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/r2/buckets/${args.bucket_name}/cors`,
          body,
        );

        const handle = await context.writeResource(
          "put_bucket_cors_policy",
          String(args.bucket_name),
          result,
        );
        context.logger.info("Updated put_bucket_cors_policy", {});
        return { dataHandles: [handle] };
      },
    },
    delete_bucket_cors_policy: {
      description: "Delete Bucket CORS Policy",
      arguments: z.object({
        bucket_name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/r2/buckets/${args.bucket_name}/cors`,
        );

        context.logger.info("Deleted resource {id}", { id: args.bucket_name });
        return { dataHandles: [] };
      },
    },
    list_custom_domains: {
      description: "List Custom Domains of Bucket",
      arguments: z.object({
        bucket_name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/r2/buckets/${args.bucket_name}/domains/custom`,
        );

        const handle = await context.writeResource(
          "list_custom_domains",
          String(args.bucket_name),
          result,
        );
        context.logger.info("Fetched list_custom_domains", {});
        return { dataHandles: [handle] };
      },
    },
    create_r2_add_custom_domain: {
      description: "Attach Custom Domain To Bucket",
      arguments: z.object({
        bucket_name: z.string(),
        ciphers: z.array(z.string()).optional().describe(
          "An allowlist of ciphers for TLS termination. These ciphers must be in the Bor...",
        ),
        domain: z.string().describe("Name of the custom domain to be added."),
        enabled: z.boolean().describe(
          "Whether to enable public bucket access at the custom domain. If undefined, th...",
        ),
        minTLS: z.enum(["1.0", "1.1", "1.2", "1.3"]).optional().describe(
          "Minimum TLS Version the custom domain will accept for incoming connections. I...",
        ),
        zoneId: z.string().describe("Zone ID of the custom domain."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const pathKeys = new Set(["bucket_name"]);
        for (const [k, v] of Object.entries(args)) {
          if (!pathKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/r2/buckets/${args.bucket_name}/domains/custom`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "r2_add_custom_domain",
          id,
          result,
        );
        context.logger.info("Created r2_add_custom_domain {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_custom_domain_settings: {
      description: "Get Custom Domain Settings",
      arguments: z.object({
        bucket_name: z.string(),
        domain: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/r2/buckets/${args.bucket_name}/domains/custom/${args.domain}`,
        );

        const handle = await context.writeResource(
          "custom_domain_settings",
          String(args.domain),
          result,
        );
        context.logger.info("Fetched custom_domain_settings", {});
        return { dataHandles: [handle] };
      },
    },
    update_r2_edit_custom_domain_settings: {
      description: "Configure Custom Domain Settings",
      arguments: z.object({
        bucket_name: z.string(),
        domain: z.string(),
        ciphers: z.array(z.string()).optional().describe(
          "An allowlist of ciphers for TLS termination. These ciphers must be in the Bor...",
        ),
        enabled: z.boolean().optional().describe(
          "Whether to enable public bucket access at the specified custom domain.",
        ),
        minTLS: z.enum(["1.0", "1.1", "1.2", "1.3"]).optional().describe(
          "Minimum TLS Version the custom domain will accept for incoming connections. I...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const pathKeys = new Set(["bucket_name", "domain"]);
        for (const [k, v] of Object.entries(args)) {
          if (!pathKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/r2/buckets/${args.bucket_name}/domains/custom/${args.domain}`,
          body,
        );

        const handle = await context.writeResource(
          "r2_edit_custom_domain_settings",
          String(args.domain),
          result,
        );
        context.logger.info("Updated r2_edit_custom_domain_settings", {});
        return { dataHandles: [handle] };
      },
    },
    delete_custom_domain: {
      description: "Remove Custom Domain From Bucket",
      arguments: z.object({
        bucket_name: z.string(),
        domain: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/r2/buckets/${args.bucket_name}/domains/custom/${args.domain}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.domain });
        return { dataHandles: [] };
      },
    },
    get_bucket_public_policy: {
      description: "Get r2.dev Domain of Bucket",
      arguments: z.object({
        bucket_name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/r2/buckets/${args.bucket_name}/domains/managed`,
        );

        const handle = await context.writeResource(
          "bucket_public_policy",
          String(args.bucket_name),
          result,
        );
        context.logger.info("Fetched bucket_public_policy", {});
        return { dataHandles: [handle] };
      },
    },
    put_bucket_public_policy: {
      description: "Update r2.dev Domain of Bucket",
      arguments: z.object({
        bucket_name: z.string(),
        enabled: z.boolean().describe(
          "Whether to enable public bucket access at the r2.dev domain.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const pathKeys = new Set(["bucket_name"]);
        for (const [k, v] of Object.entries(args)) {
          if (!pathKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/r2/buckets/${args.bucket_name}/domains/managed`,
          body,
        );

        const handle = await context.writeResource(
          "put_bucket_public_policy",
          String(args.bucket_name),
          result,
        );
        context.logger.info("Updated put_bucket_public_policy", {});
        return { dataHandles: [handle] };
      },
    },
    get_bucket_lifecycle_configuration: {
      description: "Get Object Lifecycle Rules",
      arguments: z.object({
        bucket_name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/r2/buckets/${args.bucket_name}/lifecycle`,
        );

        const handle = await context.writeResource(
          "bucket_lifecycle_configuration",
          String(args.bucket_name),
          result,
        );
        context.logger.info("Fetched bucket_lifecycle_configuration", {});
        return { dataHandles: [handle] };
      },
    },
    put_bucket_lifecycle_configuration: {
      description: "Put Object Lifecycle Rules",
      arguments: z.object({
        bucket_name: z.string(),
        rules: z.array(z.unknown()).optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const pathKeys = new Set(["bucket_name"]);
        for (const [k, v] of Object.entries(args)) {
          if (!pathKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/r2/buckets/${args.bucket_name}/lifecycle`,
          body,
        );

        const handle = await context.writeResource(
          "put_bucket_lifecycle_configuration",
          String(args.bucket_name),
          result,
        );
        context.logger.info("Updated put_bucket_lifecycle_configuration", {});
        return { dataHandles: [handle] };
      },
    },
    get_bucket_local_uploads_configuration: {
      description: "Get Local Uploads Configuration",
      arguments: z.object({
        bucket_name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/r2/buckets/${args.bucket_name}/local-uploads`,
        );

        const handle = await context.writeResource(
          "bucket_local_uploads_configuration",
          String(args.bucket_name),
          result,
        );
        context.logger.info("Fetched bucket_local_uploads_configuration", {});
        return { dataHandles: [handle] };
      },
    },
    put_bucket_local_uploads_configuration: {
      description: "Put Local Uploads Configuration",
      arguments: z.object({
        bucket_name: z.string(),
        enabled: z.boolean().describe(
          "Whether to enable local uploads for this bucket.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const pathKeys = new Set(["bucket_name"]);
        for (const [k, v] of Object.entries(args)) {
          if (!pathKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/r2/buckets/${args.bucket_name}/local-uploads`,
          body,
        );

        const handle = await context.writeResource(
          "put_bucket_local_uploads_configuration",
          String(args.bucket_name),
          result,
        );
        context.logger.info(
          "Updated put_bucket_local_uploads_configuration",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    get_bucket_lock_configuration: {
      description: "Get Bucket Lock Rules",
      arguments: z.object({
        bucket_name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/r2/buckets/${args.bucket_name}/lock`,
        );

        const handle = await context.writeResource(
          "bucket_lock_configuration",
          String(args.bucket_name),
          result,
        );
        context.logger.info("Fetched bucket_lock_configuration", {});
        return { dataHandles: [handle] };
      },
    },
    put_bucket_lock_configuration: {
      description: "Put Bucket Lock Rules",
      arguments: z.object({
        bucket_name: z.string(),
        rules: z.array(z.unknown()).optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const pathKeys = new Set(["bucket_name"]);
        for (const [k, v] of Object.entries(args)) {
          if (!pathKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/r2/buckets/${args.bucket_name}/lock`,
          body,
        );

        const handle = await context.writeResource(
          "put_bucket_lock_configuration",
          String(args.bucket_name),
          result,
        );
        context.logger.info("Updated put_bucket_lock_configuration", {});
        return { dataHandles: [handle] };
      },
    },
    list_objects: {
      description: "List Objects",
      arguments: z.object({
        bucket_name: z.string(),
        per_page: z.number().optional(),
        prefix: z.string().optional(),
        delimiter: z.string().optional(),
        cursor: z.string().optional(),
        start_after: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["bucket_name", "page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/r2/buckets/${args.bucket_name}/objects`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("objects", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} objects", { count: results.length });
        return { dataHandles: [handle] };
      },
    },
    delete_objects: {
      description: "Delete Objects",
      arguments: z.object({
        bucket_name: z.string(),
        prefix: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/r2/buckets/${args.bucket_name}/objects`,
        );

        context.logger.info("Deleted resource {id}", { id: args.bucket_name });
        return { dataHandles: [] };
      },
    },
    get_object: {
      description: "Get Object",
      arguments: z.object({
        bucket_name: z.string(),
        object_key: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/r2/buckets/${args.bucket_name}/objects/${args.object_key}`,
        );

        const handle = await context.writeResource(
          "object",
          String(args.object_key),
          result,
        );
        context.logger.info("Fetched object", {});
        return { dataHandles: [handle] };
      },
    },
    put_object: {
      description: "Upload Object",
      arguments: z.object({
        bucket_name: z.string(),
        object_key: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const pathKeys = new Set(["bucket_name", "object_key"]);
        for (const [k, v] of Object.entries(args)) {
          if (!pathKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/r2/buckets/${args.bucket_name}/objects/${args.object_key}`,
          body,
        );

        const handle = await context.writeResource(
          "put_object",
          String(args.object_key),
          result,
        );
        context.logger.info("Updated put_object", {});
        return { dataHandles: [handle] };
      },
    },
    delete_object: {
      description: "Delete Object",
      arguments: z.object({
        bucket_name: z.string(),
        object_key: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/r2/buckets/${args.bucket_name}/objects/${args.object_key}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.object_key });
        return { dataHandles: [] };
      },
    },
    get_bucket_sippy_config: {
      description: "Get Sippy Configuration",
      arguments: z.object({
        bucket_name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/r2/buckets/${args.bucket_name}/sippy`,
        );

        const handle = await context.writeResource(
          "bucket_sippy_config",
          String(args.bucket_name),
          result,
        );
        context.logger.info("Fetched bucket_sippy_config", {});
        return { dataHandles: [handle] };
      },
    },
    put_bucket_sippy_config: {
      description: "Enable Sippy",
      arguments: z.object({
        bucket_name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const pathKeys = new Set(["bucket_name"]);
        for (const [k, v] of Object.entries(args)) {
          if (!pathKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/r2/buckets/${args.bucket_name}/sippy`,
          body,
        );

        const handle = await context.writeResource(
          "put_bucket_sippy_config",
          String(args.bucket_name),
          result,
        );
        context.logger.info("Updated put_bucket_sippy_config", {});
        return { dataHandles: [handle] };
      },
    },
    delete_bucket_sippy_config: {
      description: "Disable Sippy",
      arguments: z.object({
        bucket_name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/r2/buckets/${args.bucket_name}/sippy`,
        );

        context.logger.info("Deleted resource {id}", { id: args.bucket_name });
        return { dataHandles: [] };
      },
    },
    get_account_level_metrics: {
      description: "Get Account-Level Metrics",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/r2/metrics`,
        );

        const handle = await context.writeResource(
          "account_level_metrics",
          "latest",
          result,
        );
        context.logger.info("Fetched account_level_metrics", {});
        return { dataHandles: [handle] };
      },
    },
    create_temp_access_credentials: {
      description: "Create Temporary Access Credentials",
      arguments: z.object({
        bucket: z.string().describe("Name of the R2 bucket."),
        objects: z.array(z.string()).optional().describe(
          "Optional object paths to scope the credentials to.",
        ),
        parentAccessKeyId: z.string().describe(
          "The parent access key id to use for signing.",
        ),
        permission: z.enum([
          "admin-read-write",
          "admin-read-only",
          "object-read-write",
          "object-read-only",
        ]).describe("Permissions allowed on the credentials."),
        prefixes: z.array(z.string()).optional().describe(
          "Optional prefix paths to scope the credentials to.",
        ),
        ttlSeconds: z.number().max(604800).describe(
          "How long the credentials will live for in seconds.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/r2/temp-access-credentials`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "temp_access_credentials",
          id,
          result,
        );
        context.logger.info("Created temp_access_credentials {id}", { id });
        return { dataHandles: [handle] };
      },
    },
  },
};
