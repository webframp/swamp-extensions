/**
 * Cloudflare Spectrum — TCP/UDP proxying for non-HTTP applications
 *
 * Auto-generated by scripts/cloudflare-codegen — do not edit manually.
 *
 * @module
 */
// SPDX-License-Identifier: Apache-2.0

import { z } from "npm:zod@4.4.3";
import { cfApi, cfApiPaginated, sanitizeInstanceName } from "./_lib/api.ts";

const EXTENSION_NAME = "@webframp/cloudflare/spectrum";

// =============================================================================
// Schemas
// =============================================================================

const GlobalArgsSchema = z.object({
  apiToken: z.string().meta({ sensitive: true }).describe(
    "Cloudflare API token; overrides the CLOUDFLARE_API_TOKEN environment variable. Wire with a vault.get(...) expression to source it from a vault.",
  ).optional(),
  zoneId: z.string().describe("Cloudflare zone ID"),
});

const GetCurrentAggregatedAnalyticsItemSchema = z.object({
  appID: z.unknown(),
  bytesEgress: z.number().describe("Number of bytes sent."),
  bytesIngress: z.number().describe("Number of bytes received."),
  connections: z.number().describe("Number of connections."),
  durationAvg: z.number().describe("Average duration of connections."),
}).passthrough();

const GetCurrentAggregatedAnalyticsSchema = z.object({
  items: z.array(GetCurrentAggregatedAnalyticsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const GetAnalyticsByTimeSchema = z.object({
  data: z.array(z.unknown()).describe(
    "List of columns returned by the analytics query.",
  ),
  data_lag: z.number().min(0).describe(
    "Number of seconds between current time and last processed event, i.e. how many seconds of data co...",
  ),
  max: z.unknown(),
  min: z.unknown(),
  query: z.unknown(),
  rows: z.number().min(0).describe("Total number of rows in the result."),
  time_intervals: z.array(z.array(z.unknown())).optional().describe(
    "List of time interval buckets: [start, end].",
  ),
  totals: z.unknown(),
}).passthrough();

const GetAnalyticsSummarySchema = z.object({
  data: z.array(z.unknown()).describe(
    "List of columns returned by the analytics query.",
  ),
  data_lag: z.number().min(0).describe(
    "Number of seconds between current time and last processed event, i.e. how many seconds of data co...",
  ),
  max: z.unknown(),
  min: z.unknown(),
  query: z.unknown(),
  rows: z.number().min(0).describe("Total number of rows in the result."),
  time_intervals: z.array(z.array(z.unknown())).optional().describe(
    "List of time interval buckets: [start, end].",
  ),
  totals: z.unknown(),
}).passthrough();

const ListSpectrumApplicationsSchema = z.union([
  z.array(z.unknown()),
  z.array(z.unknown()),
]);

const CreateSpectrumApplicationUsingANameForTheOriginSchema = z.union([
  z.object({
    created_on: z.unknown(),
    id: z.unknown(),
    modified_on: z.unknown(),
    argo_smart_routing: z.unknown().optional(),
    dns: z.unknown(),
    edge_ips: z.unknown().optional(),
    ip_firewall: z.unknown().optional(),
    origin_direct: z.unknown().optional(),
    origin_dns: z.unknown().optional(),
    origin_port: z.unknown().optional(),
    protocol: z.unknown(),
    proxy_protocol: z.unknown().optional(),
    tls: z.unknown().optional(),
    traffic_type: z.unknown(),
    virtual_network_id: z.unknown().optional(),
  }),
  z.object({
    created_on: z.unknown(),
    id: z.unknown(),
    modified_on: z.unknown(),
    dns: z.unknown(),
    origin_direct: z.unknown().optional(),
    protocol: z.unknown(),
  }),
]);

const GetSpectrumApplicationConfigurationSchema = z.union([
  z.object({
    created_on: z.unknown(),
    id: z.unknown(),
    modified_on: z.unknown(),
    argo_smart_routing: z.unknown().optional(),
    dns: z.unknown(),
    edge_ips: z.unknown().optional(),
    ip_firewall: z.unknown().optional(),
    origin_direct: z.unknown().optional(),
    origin_dns: z.unknown().optional(),
    origin_port: z.unknown().optional(),
    protocol: z.unknown(),
    proxy_protocol: z.unknown().optional(),
    tls: z.unknown().optional(),
    traffic_type: z.unknown(),
    virtual_network_id: z.unknown().optional(),
  }),
  z.object({
    created_on: z.unknown(),
    id: z.unknown(),
    modified_on: z.unknown(),
    dns: z.unknown(),
    origin_direct: z.unknown().optional(),
    protocol: z.unknown(),
  }),
]);

const UpdateSpectrumApplicationConfigurationUsingANameForTheOriginSchema = z
  .union([
    z.object({
      created_on: z.unknown(),
      id: z.unknown(),
      modified_on: z.unknown(),
      argo_smart_routing: z.unknown().optional(),
      dns: z.unknown(),
      edge_ips: z.unknown().optional(),
      ip_firewall: z.unknown().optional(),
      origin_direct: z.unknown().optional(),
      origin_dns: z.unknown().optional(),
      origin_port: z.unknown().optional(),
      protocol: z.unknown(),
      proxy_protocol: z.unknown().optional(),
      tls: z.unknown().optional(),
      traffic_type: z.unknown(),
      virtual_network_id: z.unknown().optional(),
    }),
    z.object({
      created_on: z.unknown(),
      id: z.unknown(),
      modified_on: z.unknown(),
      dns: z.unknown(),
      origin_direct: z.unknown().optional(),
      protocol: z.unknown(),
    }),
  ]);

// =============================================================================
// Model Definition
// =============================================================================

/** Cloudflare Spectrum — TCP/UDP proxying for non-HTTP applications */
export const model = {
  type: "@webframp/cloudflare/spectrum",
  version: "2026.08.28.2",
  globalArguments: GlobalArgsSchema,

  upgrades: [
    {
      toVersion: "2026.08.24.1",
      description:
        "Added optional durationMs, collectedBy, and fetchedAt output metadata fields",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.08.25.1",
      description: "Label metadata update, no schema changes",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.08.26.1",
      description:
        "No schema changes — restored inline npm:zod specifier for registry scoring; retained strict mode",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.08.26.2",
      description:
        "No schema changes — restored inline npm:zod specifier for registry scoring; retained strict mode",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.08.28.1",
      description:
        "No schema changes — normalized license to Apache-2.0 and corrected copyright holder to Sean Escriva",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.08.28.2",
      description: "Regenerated from updated API spec; no migration required",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
  ],

  resources: {
    "get_current_aggregated_analytics": {
      description: "Get current aggregated analytics",
      schema: GetCurrentAggregatedAnalyticsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "analytics_by_time": {
      description: "Get analytics by time",
      schema: GetAnalyticsByTimeSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "analytics_summary": {
      description: "Get analytics summary",
      schema: GetAnalyticsSummarySchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "list_spectrum_applications": {
      description: "List Spectrum applications",
      schema: ListSpectrumApplicationsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "spectrum_application_using_a_name_for_the_origin": {
      description: "Create Spectrum application using a name for the origin",
      schema: CreateSpectrumApplicationUsingANameForTheOriginSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "spectrum_application_configuration": {
      description: "Get Spectrum application configuration",
      schema: GetSpectrumApplicationConfigurationSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "spectrum_application_configuration_using_a_name_for_the_origin": {
      description:
        "Update Spectrum application configuration using a name for the origin",
      schema:
        UpdateSpectrumApplicationConfigurationUsingANameForTheOriginSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
  },

  methods: {
    get_current_aggregated_analytics: {
      description: "Get current aggregated analytics",
      arguments: z.object({
        appID: z.string().optional(),
        colo_name: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;
        const startMs = Date.now();
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/zones/${zoneId}/spectrum/analytics/aggregate/current`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "get_current_aggregated_analytics",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );

        context.logger.info("Found {count} get_current_aggregated_analytics", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    get_analytics_by_time: {
      description: "Get analytics by time",
      arguments: z.object({
        dimensions: z.string().optional(),
        sort: z.string().optional(),
        until: z.string().optional(),
        metrics: z.string().optional(),
        filters: z.string().optional(),
        since: z.string().optional(),
        time_delta: z.enum([
          "year",
          "quarter",
          "month",
          "week",
          "day",
          "hour",
          "dekaminute",
          "minute",
        ]).optional(),
      }),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/zones/${zoneId}/spectrum/analytics/events/bytime`,
        );

        const handle = await context.writeResource(
          "analytics_by_time",
          "latest",
          result,
        );
        context.logger.info("Fetched analytics_by_time", {});
        return { dataHandles: [handle] };
      },
    },
    get_analytics_summary: {
      description: "Get analytics summary",
      arguments: z.object({
        dimensions: z.string().optional(),
        sort: z.string().optional(),
        until: z.string().optional(),
        metrics: z.string().optional(),
        filters: z.string().optional(),
        since: z.string().optional(),
      }),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/zones/${zoneId}/spectrum/analytics/events/summary`,
        );

        const handle = await context.writeResource(
          "analytics_summary",
          "latest",
          result,
        );
        context.logger.info("Fetched analytics_summary", {});
        return { dataHandles: [handle] };
      },
    },
    list_spectrum_applications: {
      description: "List Spectrum applications",
      arguments: z.object({
        page: z.number().optional(),
        per_page: z.number().optional(),
        direction: z.enum(["asc", "desc"]).optional(),
        order: z.enum([
          "protocol",
          "app_id",
          "created_on",
          "modified_on",
          "dns",
        ]).optional(),
      }),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/zones/${zoneId}/spectrum/apps`,
        );

        const handle = await context.writeResource(
          "list_spectrum_applications",
          "latest",
          result,
        );
        context.logger.info("Fetched list_spectrum_applications", {});
        return { dataHandles: [handle] };
      },
    },
    create_spectrum_application_using_a_name_for_the_origin: {
      description: "Create Spectrum application using a name for the origin",
      arguments: z.object({
        body: z.union([
          z.object({
            created_on: z.unknown(),
            id: z.unknown(),
            modified_on: z.unknown(),
            argo_smart_routing: z.unknown().optional(),
            dns: z.unknown(),
            edge_ips: z.unknown().optional(),
            ip_firewall: z.unknown().optional(),
            origin_direct: z.unknown().optional(),
            origin_dns: z.unknown().optional(),
            origin_port: z.unknown().optional(),
            protocol: z.unknown(),
            proxy_protocol: z.unknown().optional(),
            tls: z.unknown().optional(),
            traffic_type: z.unknown(),
            virtual_network_id: z.unknown().optional(),
          }),
          z.object({
            created_on: z.unknown(),
            id: z.unknown(),
            modified_on: z.unknown(),
            dns: z.unknown(),
            origin_direct: z.unknown().optional(),
            protocol: z.unknown(),
          }),
        ]),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;

        const body = args.body;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/zones/${zoneId}/spectrum/apps`,
          body,
        );

        const handle = await context.writeResource(
          "create_spectrum_application_using_a_name_for_the_origin",
          "latest",
          result ?? {},
        );
        context.logger.info(
          "Executed create_spectrum_application_using_a_name_for_the_origin",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    get_spectrum_application_configuration: {
      description: "Get Spectrum application configuration",
      arguments: z.object({
        app_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/zones/${zoneId}/spectrum/apps/${args.app_id}`,
        );

        const handle = await context.writeResource(
          "spectrum_application_configuration",
          sanitizeInstanceName(String(args.app_id)),
          result,
        );
        context.logger.info("Fetched spectrum_application_configuration", {});
        return { dataHandles: [handle] };
      },
    },
    update_spectrum_application_configuration_using_a_name_for_the_origin: {
      description:
        "Update Spectrum application configuration using a name for the origin",
      arguments: z.object({
        app_id: z.string(),
        body: z.union([
          z.object({
            created_on: z.unknown(),
            id: z.unknown(),
            modified_on: z.unknown(),
            argo_smart_routing: z.unknown().optional(),
            dns: z.unknown(),
            edge_ips: z.unknown().optional(),
            ip_firewall: z.unknown().optional(),
            origin_direct: z.unknown().optional(),
            origin_dns: z.unknown().optional(),
            origin_port: z.unknown().optional(),
            protocol: z.unknown(),
            proxy_protocol: z.unknown().optional(),
            tls: z.unknown().optional(),
            traffic_type: z.unknown(),
            virtual_network_id: z.unknown().optional(),
          }),
          z.object({
            created_on: z.unknown(),
            id: z.unknown(),
            modified_on: z.unknown(),
            dns: z.unknown(),
            origin_direct: z.unknown().optional(),
            protocol: z.unknown(),
          }),
        ]),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;

        const body = args.body;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/zones/${zoneId}/spectrum/apps/${args.app_id}`,
          body,
        );

        const handle = await context.writeResource(
          "spectrum_application_configuration_using_a_name_for_the_origin",
          sanitizeInstanceName(String(args.app_id)),
          result,
        );
        context.logger.info(
          "Updated spectrum_application_configuration_using_a_name_for_the_origin",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    delete_spectrum_application: {
      description: "Delete Spectrum application",
      arguments: z.object({
        app_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;

        await cfApi(
          apiToken,
          "DELETE",
          `/zones/${zoneId}/spectrum/apps/${args.app_id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.app_id });
        return { dataHandles: [] };
      },
    },
  },
};
