// Cloudflare Spectrum — TCP/UDP proxying for non-HTTP applications — Tests
// Auto-generated by scripts/cloudflare-codegen — do not edit manually.
// SPDX-License-Identifier: Apache-2.0

import { assertEquals, assertExists } from "jsr:@std/assert@1.0.19";
import { createModelTestContext } from "@systeminit/swamp-testing";
import { model } from "./spectrum.ts";

// ---------------------------------------------------------------------------
// Model Export Structure Tests
// ---------------------------------------------------------------------------

Deno.test("spectrum model: has correct type", () => {
  assertEquals(model.type, "@webframp/cloudflare/spectrum");
});

Deno.test("spectrum model: has valid version format", () => {
  const versionPattern = /^\d{4}\.\d{2}\.\d{2}\.\d+$/;
  assertEquals(versionPattern.test(model.version), true);
});

Deno.test("spectrum model: has globalArguments with apiToken", () => {
  assertExists(model.globalArguments);
  const shape = model.globalArguments.shape;
  assertExists(shape.apiToken);
  assertExists(shape.zoneId);
});

Deno.test("spectrum model: has expected methods", () => {
  assertExists(model.methods);
  assertExists(model.methods.get_current_aggregated_analytics);
  assertExists(model.methods.get_analytics_by_time);
  assertExists(model.methods.get_analytics_summary);
  assertExists(model.methods.list_spectrum_applications);
  assertExists(
    model.methods.create_spectrum_application_using_a_name_for_the_origin,
  );
  assertExists(model.methods.get_spectrum_application_configuration);
  assertExists(
    model.methods
      .update_spectrum_application_configuration_using_a_name_for_the_origin,
  );
  assertExists(model.methods.delete_spectrum_application);
});

Deno.test("spectrum model: has expected resources", () => {
  assertExists(model.resources);
  assertExists(model.resources["get_current_aggregated_analytics"]);
  assertExists(model.resources["analytics_by_time"]);
  assertExists(model.resources["analytics_summary"]);
  assertExists(model.resources["list_spectrum_applications"]);
  assertExists(
    model.resources["spectrum_application_using_a_name_for_the_origin"],
  );
  assertExists(model.resources["spectrum_application_configuration"]);
  assertExists(
    model
      .resources[
        "spectrum_application_configuration_using_a_name_for_the_origin"
      ],
  );
});

// ---------------------------------------------------------------------------
// Mock Cloudflare API Server
// ---------------------------------------------------------------------------

function startMockCfServer(
  responses: Record<string, { result: unknown; isCollection?: boolean }>,
): { url: string; server: Deno.HttpServer } {
  const server = Deno.serve({ port: 0, onListen() {} }, (req) => {
    const url = new URL(req.url);
    const path = url.pathname;

    for (
      const [pattern, { result, isCollection }] of Object.entries(responses)
    ) {
      if (path.includes(pattern)) {
        return Response.json({
          success: true,
          errors: [],
          messages: [],
          result,
          result_info: isCollection
            ? {
              page: 1,
              per_page: 50,
              total_count: Array.isArray(result) ? result.length : 1,
              total_pages: 1,
            }
            : undefined,
        });
      }
    }

    return Response.json({
      success: false,
      errors: [{ code: 404, message: "Not found" }],
      messages: [],
      result: null,
    });
  });

  const addr = server.addr as Deno.NetAddr;
  return { url: `http://localhost:${addr.port}`, server };
}

function installFetchMock(mockUrl: string): () => void {
  const originalFetch = globalThis.fetch;
  globalThis.fetch = (input, init) => {
    const reqUrl = typeof input === "string" ? input : input.toString();
    const newUrl = reqUrl.replace(
      "https://api.cloudflare.com/client/v4",
      mockUrl,
    );
    return originalFetch(newUrl, init);
  };
  return () => {
    globalThis.fetch = originalFetch;
  };
}

Deno.test({
  name:
    "spectrum model: get_current_aggregated_analytics fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = [{
      "appID": null,
      "bytesEgress": 1,
      "bytesIngress": 1,
      "connections": 1,
      "durationAvg": 1,
    }];
    const { url, server } = startMockCfServer({
      "/spectrum/analytics/aggregate/current": {
        result: mockData,
        isCollection: true,
      },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "zoneId": "zone-123" },
        definition: {
          id: "test-id",
          name: "test-spectrum",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).get_current_aggregated_analytics.execute({}, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "spectrum model: get_analytics_by_time fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "data": [null],
      "data_lag": 3,
      "max": null,
      "min": null,
      "query": null,
      "rows": 5,
      "time_intervals": [[null]],
      "totals": null,
    };
    const { url, server } = startMockCfServer({
      "/spectrum/analytics/events/bytime": { result: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "zoneId": "zone-123" },
        definition: {
          id: "test-id",
          name: "test-spectrum",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).get_analytics_by_time.execute({}, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name:
    "spectrum model: create_spectrum_application_using_a_name_for_the_origin executes and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = null;
    const { url, server } = startMockCfServer({
      "/spectrum/apps": { result: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "zoneId": "zone-123" },
        definition: {
          id: "test-id",
          name: "test-spectrum",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).create_spectrum_application_using_a_name_for_the_origin.execute(
        {},
        context,
      );
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name:
    "spectrum model: update_spectrum_application_configuration_using_a_name_for_the_origin executes and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = null;
    const { url, server } = startMockCfServer({
      "/spectrum/apps/test-id-123": { result: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "zoneId": "zone-123" },
        definition: {
          id: "test-id",
          name: "test-spectrum",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).update_spectrum_application_configuration_using_a_name_for_the_origin
        .execute({ "app_id": "test-id-123" }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "spectrum model: delete_spectrum_application executes successfully",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockCfServer({
      "/spectrum/apps/test-id-123": { result: { id: "test-id-123" } },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "zoneId": "zone-123" },
        definition: {
          id: "test-id",
          name: "test-spectrum",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).delete_spectrum_application.execute(
        { "app_id": "test-id-123" },
        context,
      );
      assertEquals(result.dataHandles.length, 0);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});
