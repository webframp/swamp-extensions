// Cloudflare Tunnel — tunnel management, configurations, connections — Tests
// Auto-generated by scripts/cloudflare-codegen — do not edit manually.
// SPDX-License-Identifier: Apache-2.0

import { assertEquals, assertExists } from "jsr:@std/assert@1.0.19";
import { createModelTestContext } from "@systeminit/swamp-testing";
import { model } from "./tunnel.ts";

// ---------------------------------------------------------------------------
// Model Export Structure Tests
// ---------------------------------------------------------------------------

Deno.test("tunnel model: has correct type", () => {
  assertEquals(model.type, "@webframp/cloudflare/tunnel");
});

Deno.test("tunnel model: has valid version format", () => {
  const versionPattern = /^\d{4}\.\d{2}\.\d{2}\.\d+$/;
  assertEquals(versionPattern.test(model.version), true);
});

Deno.test("tunnel model: has globalArguments with apiToken", () => {
  assertExists(model.globalArguments);
  const shape = model.globalArguments.shape;
  assertExists(shape.apiToken);
  assertExists(shape.accountId);
});

Deno.test("tunnel model: has expected methods", () => {
  assertExists(model.methods);
  assertExists(model.methods.list_cloudflare_tunnels);
  assertExists(model.methods.create_a_cloudflare_tunnel);
  assertExists(model.methods.get_a_cloudflare_tunnel);
  assertExists(model.methods.update_a_cloudflare_tunnel);
  assertExists(model.methods.delete_a_cloudflare_tunnel);
  assertExists(model.methods.get_configuration);
  assertExists(model.methods.put_configuration);
  assertExists(model.methods.list_cloudflare_tunnel_connections);
  assertExists(
    model.methods
      .delete_cloudflare_tunnel_clean_up_cloudflare_tunnel_connections,
  );
  assertExists(model.methods.get_cloudflare_tunnel_connector);
  assertExists(model.methods.get_a_cloudflare_tunnel_management_token);
  assertExists(model.methods.get_a_cloudflare_tunnel_token);
  assertExists(model.methods.list_all_tunnels);
});

Deno.test("tunnel model: has expected resources", () => {
  assertExists(model.resources);
  assertExists(model.resources["cloudflare_tunnels"]);
  assertExists(model.resources["a_cloudflare_tunnel"]);
  assertExists(model.resources["configuration"]);
  assertExists(model.resources["put_configuration"]);
  assertExists(model.resources["cloudflare_tunnel_connections"]);
  assertExists(model.resources["cloudflare_tunnel_connector"]);
  assertExists(model.resources["a_cloudflare_tunnel_management_token"]);
  assertExists(model.resources["a_cloudflare_tunnel_token"]);
  assertExists(model.resources["all_tunnels"]);
});

// ---------------------------------------------------------------------------
// Mock Cloudflare API Server
// ---------------------------------------------------------------------------

function startMockCfServer(
  responses: Record<string, { result: unknown; isCollection?: boolean }>,
): { url: string; server: Deno.HttpServer } {
  const server = Deno.serve({ port: 0, onListen() {} }, (req) => {
    const url = new URL(req.url);
    const path = url.pathname;

    for (
      const [pattern, { result, isCollection }] of Object.entries(responses)
    ) {
      if (path.includes(pattern)) {
        return Response.json({
          success: true,
          errors: [],
          messages: [],
          result,
          result_info: isCollection
            ? {
              page: 1,
              per_page: 50,
              total_count: Array.isArray(result) ? result.length : 1,
              total_pages: 1,
            }
            : undefined,
        });
      }
    }

    return Response.json({
      success: false,
      errors: [{ code: 404, message: "Not found" }],
      messages: [],
      result: null,
    });
  });

  const addr = server.addr as Deno.NetAddr;
  return { url: `http://localhost:${addr.port}`, server };
}

function installFetchMock(mockUrl: string): () => void {
  const originalFetch = globalThis.fetch;
  globalThis.fetch = (input, init) => {
    const reqUrl = typeof input === "string" ? input : input.toString();
    const newUrl = reqUrl.replace(
      "https://api.cloudflare.com/client/v4",
      mockUrl,
    );
    return originalFetch(newUrl, init);
  };
  return () => {
    globalThis.fetch = originalFetch;
  };
}

Deno.test({
  name: "tunnel model: list_cloudflare_tunnels fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = [{
      "account_tag": null,
      "config_src": null,
      "connections": null,
      "conns_active_at": null,
      "conns_inactive_at": null,
      "created_at": null,
      "deleted_at": null,
      "id": null,
      "metadata": null,
      "name": null,
      "remote_config": null,
      "status": null,
      "tun_type": null,
    }];
    const { url, server } = startMockCfServer({
      "/cfd_tunnel": { result: mockData, isCollection: true },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: {
          id: "test-id",
          name: "test-tunnel",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).list_cloudflare_tunnels.execute({}, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "tunnel model: create_a_cloudflare_tunnel creates and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "id": null,
      "account_tag": null,
      "config_src": null,
      "connections": null,
      "conns_active_at": null,
      "conns_inactive_at": null,
      "created_at": null,
      "deleted_at": null,
      "metadata": null,
      "name": null,
      "remote_config": null,
      "status": null,
      "tun_type": null,
    };
    const { url, server } = startMockCfServer({
      "/cfd_tunnel": { result: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: {
          id: "test-id",
          name: "test-tunnel",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).create_a_cloudflare_tunnel.execute({
        "account_tag": null,
        "config_src": null,
        "connections": null,
        "conns_active_at": null,
        "conns_inactive_at": null,
        "created_at": null,
        "deleted_at": null,
        "id": null,
        "metadata": null,
        "name": null,
        "remote_config": null,
        "status": null,
        "tun_type": null,
      }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "tunnel model: get_a_cloudflare_tunnel fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "account_tag": null,
      "config_src": null,
      "connections": null,
      "conns_active_at": null,
      "conns_inactive_at": null,
      "created_at": null,
      "deleted_at": null,
      "id": null,
      "metadata": null,
      "name": null,
      "remote_config": null,
      "status": null,
      "tun_type": null,
    };
    const { url, server } = startMockCfServer({
      "/cfd_tunnel/test-id-123": { result: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: {
          id: "test-id",
          name: "test-tunnel",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).get_a_cloudflare_tunnel.execute(
        { "tunnel_id": "test-id-123" },
        context,
      );
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "tunnel model: update_a_cloudflare_tunnel executes and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "account_tag": null,
      "config_src": null,
      "connections": null,
      "conns_active_at": null,
      "conns_inactive_at": null,
      "created_at": null,
      "deleted_at": null,
      "id": null,
      "metadata": null,
      "name": null,
      "remote_config": null,
      "status": null,
      "tun_type": null,
    };
    const { url, server } = startMockCfServer({
      "/cfd_tunnel/test-id-123": { result: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: {
          id: "test-id",
          name: "test-tunnel",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).update_a_cloudflare_tunnel.execute({
        "tunnel_id": "test-id-123",
        "account_tag": null,
        "config_src": null,
        "connections": null,
        "conns_active_at": null,
        "conns_inactive_at": null,
        "created_at": null,
        "deleted_at": null,
        "id": null,
        "metadata": null,
        "name": null,
        "remote_config": null,
        "status": null,
        "tun_type": null,
      }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "tunnel model: delete_a_cloudflare_tunnel executes successfully",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockCfServer({
      "/cfd_tunnel/test-id-123": { result: { id: "test-id-123" } },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: {
          id: "test-id",
          name: "test-tunnel",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).delete_a_cloudflare_tunnel.execute(
        { "tunnel_id": "test-id-123" },
        context,
      );
      assertEquals(result.dataHandles.length, 0);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});
