/**
 * Cloudflare Tunnel — tunnel management, configurations, connections
 *
 * Auto-generated by scripts/cloudflare-codegen — do not edit manually.
 *
 * @module
 */
// SPDX-License-Identifier: AGPL-3.0-or-later WITH Swamp-Extension-Exception

import { z } from "npm:zod@4.4.3";
import { cfApi, cfApiPaginated } from "./_lib/api.ts";

// =============================================================================
// Schemas
// =============================================================================

const GlobalArgsSchema = z.object({
  apiToken: z.string().meta({ sensitive: true }).describe(
    "Cloudflare API token",
  ),
  accountId: z.string().describe("Cloudflare account ID"),
});

const CloudflareTunnelsItemSchema = z.object({
  account_tag: z.unknown().optional(),
  config_src: z.unknown().optional(),
  connections: z.unknown().optional(),
  conns_active_at: z.unknown().optional(),
  conns_inactive_at: z.unknown().optional(),
  created_at: z.unknown().optional(),
  deleted_at: z.unknown().optional(),
  id: z.unknown().optional(),
  metadata: z.unknown().optional(),
  name: z.unknown().optional(),
  remote_config: z.unknown().optional(),
  status: z.unknown().optional(),
  tun_type: z.unknown().optional(),
});

const ListCloudflareTunnelsSchema = z.object({
  items: z.array(CloudflareTunnelsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateACloudflareTunnelSchema = z.object({
  account_tag: z.unknown().optional(),
  config_src: z.unknown().optional(),
  connections: z.unknown().optional(),
  conns_active_at: z.unknown().optional(),
  conns_inactive_at: z.unknown().optional(),
  created_at: z.unknown().optional(),
  deleted_at: z.unknown().optional(),
  id: z.unknown().optional(),
  metadata: z.unknown().optional(),
  name: z.unknown().optional(),
  remote_config: z.unknown().optional(),
  status: z.unknown().optional(),
  tun_type: z.unknown().optional(),
});

const GetConfigurationSchema = z.object({
  account_id: z.unknown().optional(),
  config: z.unknown().optional(),
  created_at: z.unknown().optional(),
  source: z.unknown().optional(),
  tunnel_id: z.unknown().optional(),
  version: z.unknown().optional(),
});

const PutConfigurationSchema = z.object({
  account_id: z.unknown().optional(),
  config: z.unknown().optional(),
  created_at: z.unknown().optional(),
  source: z.unknown().optional(),
  tunnel_id: z.unknown().optional(),
  version: z.unknown().optional(),
});

const CloudflareTunnelConnectionsItemSchema = z.object({
  arch: z.unknown().optional(),
  config_version: z.unknown().optional(),
  conns: z.unknown().optional(),
  features: z.unknown().optional(),
  id: z.unknown().optional(),
  run_at: z.unknown().optional(),
  version: z.unknown().optional(),
});

const ListCloudflareTunnelConnectionsSchema = z.object({
  items: z.array(CloudflareTunnelConnectionsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetCloudflareTunnelConnectorSchema = z.object({
  arch: z.unknown().optional(),
  config_version: z.unknown().optional(),
  conns: z.unknown().optional(),
  features: z.unknown().optional(),
  id: z.unknown().optional(),
  run_at: z.unknown().optional(),
  version: z.unknown().optional(),
});

const GetACloudflareTunnelManagementTokenSchema = z.string();

const GetACloudflareTunnelTokenSchema = z.string();

const AllTunnelsItemSchema = z.union([z.unknown(), z.unknown()]);

const ListAllTunnelsSchema = z.object({
  items: z.array(AllTunnelsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

// =============================================================================
// Model Definition
// =============================================================================

/** Cloudflare Tunnel — tunnel management, configurations, connections */
export const model = {
  type: "@webframp/cloudflare/tunnel",
  version: "2026.07.19.1",
  globalArguments: GlobalArgsSchema,

  upgrades: [],

  resources: {
    "cloudflare_tunnels": {
      description: "List Cloudflare Tunnels",
      schema: ListCloudflareTunnelsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "a_cloudflare_tunnel": {
      description: "Create a Cloudflare Tunnel",
      schema: CreateACloudflareTunnelSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "configuration": {
      description: "Get configuration",
      schema: GetConfigurationSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "put_configuration": {
      description: "Put configuration",
      schema: PutConfigurationSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "cloudflare_tunnel_connections": {
      description: "List Cloudflare Tunnel connections",
      schema: ListCloudflareTunnelConnectionsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "cloudflare_tunnel_connector": {
      description: "Get Cloudflare Tunnel connector",
      schema: GetCloudflareTunnelConnectorSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "a_cloudflare_tunnel_management_token": {
      description: "Get a Cloudflare Tunnel management token",
      schema: GetACloudflareTunnelManagementTokenSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "a_cloudflare_tunnel_token": {
      description: "Get a Cloudflare Tunnel token",
      schema: GetACloudflareTunnelTokenSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "all_tunnels": {
      description: "List All Tunnels",
      schema: ListAllTunnelsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
  },

  methods: {
    list_cloudflare_tunnels: {
      description: "List Cloudflare Tunnels",
      arguments: z.object({
        name: z.string().optional(),
        is_deleted: z.boolean().optional(),
        existed_at: z.string().optional(),
        uuid: z.string().optional(),
        was_active_at: z.string().optional(),
        was_inactive_at: z.string().optional(),
        include_prefix: z.string().optional(),
        exclude_prefix: z.string().optional(),
        status: z.string().optional(),
        per_page: z.string().optional(),
        page: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/cfd_tunnel`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "cloudflare_tunnels",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} cloudflare_tunnels", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_a_cloudflare_tunnel: {
      description: "Create a Cloudflare Tunnel",
      arguments: z.object({
        config_src: z.unknown().optional(),
        name: z.unknown(),
        tunnel_secret: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/cfd_tunnel`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "a_cloudflare_tunnel",
          id,
          result,
        );
        context.logger.info("Created a_cloudflare_tunnel {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_a_cloudflare_tunnel: {
      description: "Get a Cloudflare Tunnel",
      arguments: z.object({
        tunnel_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/cfd_tunnel/${args.tunnel_id}`,
        );

        const handle = await context.writeResource(
          "a_cloudflare_tunnel",
          String(args.tunnel_id),
          result,
        );
        context.logger.info("Fetched a_cloudflare_tunnel", {});
        return { dataHandles: [handle] };
      },
    },
    update_a_cloudflare_tunnel: {
      description: "Update a Cloudflare Tunnel",
      arguments: z.object({
        tunnel_id: z.string(),
        name: z.unknown().optional(),
        tunnel_secret: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/accounts/${accountId}/cfd_tunnel/${args.tunnel_id}`,
          args,
        );

        const handle = await context.writeResource(
          "a_cloudflare_tunnel",
          String(args.tunnel_id),
          result,
        );
        context.logger.info("Updated a_cloudflare_tunnel", {});
        return { dataHandles: [handle] };
      },
    },
    delete_a_cloudflare_tunnel: {
      description: "Delete a Cloudflare Tunnel",
      arguments: z.object({
        tunnel_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/cfd_tunnel/${args.tunnel_id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.tunnel_id });
        return { dataHandles: [] };
      },
    },
    get_configuration: {
      description: "Get configuration",
      arguments: z.object({
        tunnel_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/cfd_tunnel/${args.tunnel_id}/configurations`,
        );

        const handle = await context.writeResource(
          "configuration",
          String(args.tunnel_id),
          result,
        );
        context.logger.info("Fetched configuration", {});
        return { dataHandles: [handle] };
      },
    },
    put_configuration: {
      description: "Put configuration",
      arguments: z.object({
        tunnel_id: z.string(),
        config: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/cfd_tunnel/${args.tunnel_id}/configurations`,
          args,
        );

        const handle = await context.writeResource(
          "put_configuration",
          String(args.tunnel_id),
          result,
        );
        context.logger.info("Updated put_configuration", {});
        return { dataHandles: [handle] };
      },
    },
    list_cloudflare_tunnel_connections: {
      description: "List Cloudflare Tunnel connections",
      arguments: z.object({
        tunnel_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const pathParams = new Set(["tunnel_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !pathParams.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/cfd_tunnel/${args.tunnel_id}/connections`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "cloudflare_tunnel_connections",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} cloudflare_tunnel_connections", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    delete_cloudflare_tunnel_clean_up_cloudflare_tunnel_connections: {
      description: "Clean up Cloudflare Tunnel connections",
      arguments: z.object({
        tunnel_id: z.string(),
        client_id: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/cfd_tunnel/${args.tunnel_id}/connections`,
        );

        context.logger.info("Deleted resource {id}", { id: args.tunnel_id });
        return { dataHandles: [] };
      },
    },
    get_cloudflare_tunnel_connector: {
      description: "Get Cloudflare Tunnel connector",
      arguments: z.object({
        tunnel_id: z.string(),
        connector_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/cfd_tunnel/${args.tunnel_id}/connectors/${args.connector_id}`,
        );

        const handle = await context.writeResource(
          "cloudflare_tunnel_connector",
          String(args.connector_id),
          result,
        );
        context.logger.info("Fetched cloudflare_tunnel_connector", {});
        return { dataHandles: [handle] };
      },
    },
    get_a_cloudflare_tunnel_management_token: {
      description: "Get a Cloudflare Tunnel management token",
      arguments: z.object({
        tunnel_id: z.string(),
        resources: z.array(z.unknown()),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/cfd_tunnel/${args.tunnel_id}/management`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "get_a_cloudflare_tunnel_management_token",
          id,
          result,
        );
        context.logger.info(
          "Created get_a_cloudflare_tunnel_management_token {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    get_a_cloudflare_tunnel_token: {
      description: "Get a Cloudflare Tunnel token",
      arguments: z.object({
        tunnel_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/cfd_tunnel/${args.tunnel_id}/token`,
        );

        const handle = await context.writeResource(
          "a_cloudflare_tunnel_token",
          String(args.tunnel_id),
          result,
        );
        context.logger.info("Fetched a_cloudflare_tunnel_token", {});
        return { dataHandles: [handle] };
      },
    },
    list_all_tunnels: {
      description: "List All Tunnels",
      arguments: z.object({
        name: z.string().optional(),
        is_deleted: z.boolean().optional(),
        existed_at: z.string().optional(),
        uuid: z.string().optional(),
        was_active_at: z.string().optional(),
        was_inactive_at: z.string().optional(),
        include_prefix: z.string().optional(),
        exclude_prefix: z.string().optional(),
        tun_types: z.string().optional(),
        status: z.string().optional(),
        per_page: z.string().optional(),
        page: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/tunnels`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("all_tunnels", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} all_tunnels", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
  },
};
