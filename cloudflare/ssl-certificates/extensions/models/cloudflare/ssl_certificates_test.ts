// Cloudflare SSL/TLS — certificate packs, custom certificates, certificate authority — Tests
// Auto-generated by scripts/cloudflare-codegen — do not edit manually.
// SPDX-License-Identifier: Apache-2.0

import { assertEquals, assertExists } from "jsr:@std/assert@1";
import { createModelTestContext } from "@systeminit/swamp-testing";
import { model } from "./ssl_certificates.ts";

// ---------------------------------------------------------------------------
// Model Export Structure Tests
// ---------------------------------------------------------------------------

Deno.test("ssl-certificates model: has correct type", () => {
  assertEquals(model.type, "@webframp/cloudflare/ssl-certificates");
});

Deno.test("ssl-certificates model: has valid version format", () => {
  const versionPattern = /^\d{4}\.\d{2}\.\d{2}\.\d+$/;
  assertEquals(versionPattern.test(model.version), true);
});

Deno.test("ssl-certificates model: has globalArguments with apiToken", () => {
  assertExists(model.globalArguments);
  const shape = model.globalArguments.shape;
  assertExists(shape.apiToken);
  assertExists(shape.zoneId);
});

Deno.test("ssl-certificates model: has expected methods", () => {
  assertExists(model.methods);
  assertExists(model.methods.list_certificates);
  assertExists(model.methods.create_certificate);
  assertExists(model.methods.get_certificate);
  assertExists(model.methods.delete_origin_ca_revoke_certificate);
  assertExists(model.methods.list_ssl_configurations);
  assertExists(model.methods.create_ssl_configuration);
  assertExists(
    model.methods.update_custom_ssl_for_a_zone_re_prioritize_ssl_certificates,
  );
  assertExists(
    model.methods.get_custom_ssl_for_a_zone_ssl_configuration_details,
  );
  assertExists(
    model.methods.update_custom_ssl_for_a_zone_edit_ssl_configuration,
  );
  assertExists(model.methods.delete_ssl_configuration);
  assertExists(model.methods.create_analyze_certificate_analyze_certificate);
  assertExists(model.methods.list_certificate_packs);
  assertExists(
    model.methods
      .create_certificate_packs_order_advanced_certificate_manager_certificate_pack,
  );
  assertExists(model.methods.get_certificate_pack_quotas);
  assertExists(model.methods.get_certificate_pack);
  assertExists(
    model.methods
      .update_certificate_packs_restart_validation_for_advanced_certificate_manager_certificate_pack,
  );
  assertExists(
    model.methods.delete_advanced_certificate_manager_certificate_pack,
  );
  assertExists(
    model.methods
      .get_universal_ssl_settings_for_a_zone_universal_ssl_settings_details,
  );
  assertExists(
    model.methods
      .update_universal_ssl_settings_for_a_zone_edit_universal_ssl_settings,
  );
  assertExists(model.methods.list_ssl_verification_ssl_verification_details);
  assertExists(
    model.methods
      .update_ssl_verification_edit_ssl_certificate_pack_validation_method,
  );
});

Deno.test("ssl-certificates model: has expected resources", () => {
  assertExists(model.resources);
  assertExists(model.resources["certificates"]);
  assertExists(model.resources["certificate"]);
  assertExists(model.resources["ssl_configurations"]);
  assertExists(model.resources["ssl_configuration"]);
  assertExists(
    model.resources["custom_ssl_for_a_zone_re_prioritize_ssl_certificates"],
  );
  assertExists(
    model.resources["custom_ssl_for_a_zone_ssl_configuration_details"],
  );
  assertExists(model.resources["custom_ssl_for_a_zone_edit_ssl_configuration"]);
  assertExists(model.resources["analyze_certificate_analyze_certificate"]);
  assertExists(model.resources["certificate_packs"]);
  assertExists(
    model
      .resources[
        "certificate_packs_order_advanced_certificate_manager_certificate_pack"
      ],
  );
  assertExists(model.resources["certificate_pack_quotas"]);
  assertExists(model.resources["certificate_pack"]);
  assertExists(
    model
      .resources[
        "certificate_packs_restart_validation_for_advanced_certificate_manager_certificate_pack"
      ],
  );
  assertExists(
    model
      .resources[
        "universal_ssl_settings_for_a_zone_universal_ssl_settings_details"
      ],
  );
  assertExists(
    model
      .resources[
        "universal_ssl_settings_for_a_zone_edit_universal_ssl_settings"
      ],
  );
  assertExists(model.resources["ssl_verification_ssl_verification_details"]);
  assertExists(
    model
      .resources[
        "ssl_verification_edit_ssl_certificate_pack_validation_method"
      ],
  );
});

// ---------------------------------------------------------------------------
// Mock Cloudflare API Server
// ---------------------------------------------------------------------------

function startMockCfServer(
  responses: Record<string, { result: unknown; isCollection?: boolean }>,
): { url: string; server: Deno.HttpServer } {
  const server = Deno.serve({ port: 0, onListen() {} }, (req) => {
    const url = new URL(req.url);
    const path = url.pathname;

    for (
      const [pattern, { result, isCollection }] of Object.entries(responses)
    ) {
      if (path.includes(pattern)) {
        return Response.json({
          success: true,
          errors: [],
          messages: [],
          result,
          result_info: isCollection
            ? {
              page: 1,
              per_page: 50,
              total_count: Array.isArray(result) ? result.length : 1,
              total_pages: 1,
            }
            : undefined,
        });
      }
    }

    return Response.json({
      success: false,
      errors: [{ code: 404, message: "Not found" }],
      messages: [],
      result: null,
    });
  });

  const addr = server.addr as Deno.NetAddr;
  return { url: `http://localhost:${addr.port}`, server };
}

function installFetchMock(mockUrl: string): () => void {
  const originalFetch = globalThis.fetch;
  globalThis.fetch = (input, init) => {
    const reqUrl = typeof input === "string" ? input : input.toString();
    const newUrl = reqUrl.replace(
      "https://api.cloudflare.com/client/v4",
      mockUrl,
    );
    return originalFetch(newUrl, init);
  };
  return () => {
    globalThis.fetch = originalFetch;
  };
}

Deno.test({
  name: "ssl-certificates model: list_certificates fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = [{
      "certificate": null,
      "csr": null,
      "expires_on": null,
      "hostnames": null,
      "id": null,
      "request_type": null,
      "requested_validity": null,
    }];
    const { url, server } = startMockCfServer({
      "/certificates": { result: mockData, isCollection: true },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "zoneId": "zone-123" },
        definition: {
          id: "test-id",
          name: "test-ssl-certificates",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).list_certificates.execute({}, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name:
    "ssl-certificates model: create_certificate creates and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "id": null,
      "certificate": null,
      "csr": null,
      "expires_on": null,
      "hostnames": null,
      "request_type": null,
      "requested_validity": null,
    };
    const { url, server } = startMockCfServer({
      "/certificates": { result: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "zoneId": "zone-123" },
        definition: {
          id: "test-id",
          name: "test-ssl-certificates",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).create_certificate.execute({
        "certificate": null,
        "csr": null,
        "expires_on": null,
        "hostnames": null,
        "id": null,
        "request_type": null,
        "requested_validity": null,
      }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "ssl-certificates model: get_certificate fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "certificate": null,
      "csr": null,
      "expires_on": null,
      "hostnames": null,
      "id": null,
      "request_type": null,
      "requested_validity": null,
    };
    const { url, server } = startMockCfServer({
      "/certificates/test-id-123": { result: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "zoneId": "zone-123" },
        definition: {
          id: "test-id",
          name: "test-ssl-certificates",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).get_certificate.execute(
        { "certificate_id": "test-id-123" },
        context,
      );
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name:
    "ssl-certificates model: delete_origin_ca_revoke_certificate executes successfully",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockCfServer({
      "/certificates/test-id-123": { result: { id: "test-id-123" } },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "zoneId": "zone-123" },
        definition: {
          id: "test-id",
          name: "test-ssl-certificates",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).delete_origin_ca_revoke_certificate.execute({
        "certificate_id": "test-id-123",
      }, context);
      assertEquals(result.dataHandles.length, 0);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name:
    "ssl-certificates model: update_custom_ssl_for_a_zone_re_prioritize_ssl_certificates executes and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "bundle_method": null,
      "custom_csr_id": null,
      "expires_on": null,
      "geo_restrictions": null,
      "hosts": null,
      "id": null,
      "issuer": null,
      "keyless_server": null,
      "modified_on": null,
      "policy_restrictions": null,
      "priority": null,
      "signature": null,
      "status": null,
      "uploaded_on": null,
      "zone_id": null,
    };
    const { url, server } = startMockCfServer({
      "/custom_certificates/prioritize": { result: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "zoneId": "zone-123" },
        definition: {
          id: "test-id",
          name: "test-ssl-certificates",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).update_custom_ssl_for_a_zone_re_prioritize_ssl_certificates.execute({
        "bundle_method": null,
        "custom_csr_id": null,
        "expires_on": null,
        "geo_restrictions": null,
        "hosts": null,
        "id": null,
        "issuer": null,
        "keyless_server": null,
        "modified_on": null,
        "policy_restrictions": null,
        "priority": null,
        "signature": null,
        "status": null,
        "uploaded_on": null,
        "zone_id": null,
      }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});
