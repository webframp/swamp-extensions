/**
 * Cloudflare SSL/TLS — certificate packs, custom certificates, certificate authority
 *
 * Auto-generated by scripts/cloudflare-codegen — do not edit manually.
 *
 * @module
 */
// SPDX-License-Identifier: AGPL-3.0-or-later WITH Swamp-Extension-Exception

import { z } from "npm:zod@4.4.3";
import { cfApi, cfApiPaginated } from "./_lib/api.ts";

// =============================================================================
// Schemas
// =============================================================================

const GlobalArgsSchema = z.object({
  apiToken: z.string().meta({ sensitive: true }).describe(
    "Cloudflare API token",
  ),
  zoneId: z.string().describe("Cloudflare zone ID"),
});

const CertificatesItemSchema = z.object({
  certificate: z.unknown().optional(),
  csr: z.unknown(),
  expires_on: z.unknown().optional(),
  hostnames: z.unknown(),
  id: z.unknown().optional(),
  request_type: z.unknown(),
  requested_validity: z.unknown(),
});

const ListCertificatesSchema = z.object({
  items: z.array(CertificatesItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateCertificateSchema = z.object({
  certificate: z.unknown().optional(),
  csr: z.unknown(),
  expires_on: z.unknown().optional(),
  hostnames: z.unknown(),
  id: z.unknown().optional(),
  request_type: z.unknown(),
  requested_validity: z.unknown(),
});

const SslConfigurationsItemSchema = z.object({
  bundle_method: z.unknown().optional(),
  custom_csr_id: z.unknown().optional(),
  expires_on: z.unknown().optional(),
  geo_restrictions: z.unknown().optional(),
  hosts: z.unknown().optional(),
  id: z.unknown(),
  issuer: z.unknown().optional(),
  keyless_server: z.unknown().optional(),
  modified_on: z.unknown().optional(),
  policy_restrictions: z.unknown().optional(),
  priority: z.unknown().optional(),
  signature: z.unknown().optional(),
  status: z.unknown().optional(),
  uploaded_on: z.unknown().optional(),
  zone_id: z.unknown(),
});

const ListSslConfigurationsSchema = z.object({
  items: z.array(SslConfigurationsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateSslConfigurationSchema = z.object({
  bundle_method: z.unknown().optional(),
  custom_csr_id: z.unknown().optional(),
  expires_on: z.unknown().optional(),
  geo_restrictions: z.unknown().optional(),
  hosts: z.unknown().optional(),
  id: z.unknown(),
  issuer: z.unknown().optional(),
  keyless_server: z.unknown().optional(),
  modified_on: z.unknown().optional(),
  policy_restrictions: z.unknown().optional(),
  priority: z.unknown().optional(),
  signature: z.unknown().optional(),
  status: z.unknown().optional(),
  uploaded_on: z.unknown().optional(),
  zone_id: z.unknown(),
});

const UpdateCustomSslForAZoneRePrioritizeSslCertificatesSchema = z.object({
  bundle_method: z.unknown().optional(),
  custom_csr_id: z.unknown().optional(),
  expires_on: z.unknown().optional(),
  geo_restrictions: z.unknown().optional(),
  hosts: z.unknown().optional(),
  id: z.unknown(),
  issuer: z.unknown().optional(),
  keyless_server: z.unknown().optional(),
  modified_on: z.unknown().optional(),
  policy_restrictions: z.unknown().optional(),
  priority: z.unknown().optional(),
  signature: z.unknown().optional(),
  status: z.unknown().optional(),
  uploaded_on: z.unknown().optional(),
  zone_id: z.unknown(),
});

const GetCustomSslForAZoneSslConfigurationDetailsSchema = z.object({
  bundle_method: z.unknown().optional(),
  custom_csr_id: z.unknown().optional(),
  expires_on: z.unknown().optional(),
  geo_restrictions: z.unknown().optional(),
  hosts: z.unknown().optional(),
  id: z.unknown(),
  issuer: z.unknown().optional(),
  keyless_server: z.unknown().optional(),
  modified_on: z.unknown().optional(),
  policy_restrictions: z.unknown().optional(),
  priority: z.unknown().optional(),
  signature: z.unknown().optional(),
  status: z.unknown().optional(),
  uploaded_on: z.unknown().optional(),
  zone_id: z.unknown(),
});

const UpdateCustomSslForAZoneEditSslConfigurationSchema = z.object({
  bundle_method: z.unknown().optional(),
  custom_csr_id: z.unknown().optional(),
  expires_on: z.unknown().optional(),
  geo_restrictions: z.unknown().optional(),
  hosts: z.unknown().optional(),
  id: z.unknown(),
  issuer: z.unknown().optional(),
  keyless_server: z.unknown().optional(),
  modified_on: z.unknown().optional(),
  policy_restrictions: z.unknown().optional(),
  priority: z.unknown().optional(),
  signature: z.unknown().optional(),
  status: z.unknown().optional(),
  uploaded_on: z.unknown().optional(),
  zone_id: z.unknown(),
});

const CreateAnalyzeCertificateAnalyzeCertificateSchema = z.object({});

const CertificatePacksItemSchema = z.object({
  certificate_authority: z.unknown().optional(),
  certificates: z.array(z.unknown()).describe(
    "Array of certificates in this pack.",
  ),
  cloudflare_branding: z.unknown().optional(),
  dcv_delegation_records: z.array(z.unknown()).optional().describe(
    "DCV Delegation records for domain validation.",
  ),
  hosts: z.unknown(),
  id: z.unknown(),
  primary_certificate: z.unknown().optional(),
  status: z.unknown(),
  type: z.unknown(),
  validation_errors: z.array(z.object({
    message: z.string().optional(),
  })).optional().describe(
    "Domain validation errors that have been received by the certificate authority (CA).",
  ),
  validation_method: z.unknown().optional(),
  validation_records: z.array(z.unknown()).optional().describe(
    "Certificates' validation records.",
  ),
  validity_days: z.unknown().optional(),
});

const ListCertificatePacksSchema = z.object({
  items: z.array(CertificatePacksItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateCertificatePacksOrderAdvancedCertificateManagerCertificatePackSchema =
  z.object({
    certificate_authority: z.unknown().optional(),
    certificates: z.array(z.unknown()).describe(
      "Array of certificates in this pack.",
    ),
    cloudflare_branding: z.unknown().optional(),
    dcv_delegation_records: z.array(z.unknown()).optional().describe(
      "DCV Delegation records for domain validation.",
    ),
    hosts: z.unknown(),
    id: z.unknown(),
    primary_certificate: z.unknown().optional(),
    status: z.unknown(),
    type: z.unknown(),
    validation_errors: z.array(z.object({
      message: z.string().optional(),
    })).optional().describe(
      "Domain validation errors that have been received by the certificate authority (CA).",
    ),
    validation_method: z.unknown().optional(),
    validation_records: z.array(z.unknown()).optional().describe(
      "Certificates' validation records.",
    ),
    validity_days: z.unknown().optional(),
  });

const GetCertificatePackQuotasSchema = z.object({
  advanced: z.unknown().optional(),
});

const GetCertificatePackSchema = z.object({
  certificate_authority: z.unknown().optional(),
  certificates: z.array(z.unknown()).describe(
    "Array of certificates in this pack.",
  ),
  cloudflare_branding: z.unknown().optional(),
  dcv_delegation_records: z.array(z.unknown()).optional().describe(
    "DCV Delegation records for domain validation.",
  ),
  hosts: z.unknown(),
  id: z.unknown(),
  primary_certificate: z.unknown().optional(),
  status: z.unknown(),
  type: z.unknown(),
  validation_errors: z.array(z.object({
    message: z.string().optional(),
  })).optional().describe(
    "Domain validation errors that have been received by the certificate authority (CA).",
  ),
  validation_method: z.unknown().optional(),
  validation_records: z.array(z.unknown()).optional().describe(
    "Certificates' validation records.",
  ),
  validity_days: z.unknown().optional(),
});

const UpdateCertificatePacksRestartValidationForAdvancedCertificateManagerCertificatePackSchema =
  z.object({
    certificate_authority: z.unknown().optional(),
    certificates: z.array(z.unknown()).describe(
      "Array of certificates in this pack.",
    ),
    cloudflare_branding: z.unknown().optional(),
    dcv_delegation_records: z.array(z.unknown()).optional().describe(
      "DCV Delegation records for domain validation.",
    ),
    hosts: z.unknown(),
    id: z.unknown(),
    primary_certificate: z.unknown().optional(),
    status: z.unknown(),
    type: z.unknown(),
    validation_errors: z.array(z.object({
      message: z.string().optional(),
    })).optional().describe(
      "Domain validation errors that have been received by the certificate authority (CA).",
    ),
    validation_method: z.unknown().optional(),
    validation_records: z.array(z.unknown()).optional().describe(
      "Certificates' validation records.",
    ),
    validity_days: z.unknown().optional(),
  });

const GetUniversalSslSettingsForAZoneUniversalSslSettingsDetailsSchema = z
  .object({
    enabled: z.unknown().optional(),
  });

const UpdateUniversalSslSettingsForAZoneEditUniversalSslSettingsSchema = z
  .object({
    enabled: z.unknown().optional(),
  });

const SslVerificationSslVerificationDetailsItemSchema = z.object({
  brand_check: z.unknown().optional(),
  cert_pack_uuid: z.unknown().optional(),
  certificate_status: z.unknown(),
  signature: z.unknown().optional(),
  validation_method: z.unknown().optional(),
  verification_info: z.unknown().optional(),
  verification_status: z.unknown().optional(),
  verification_type: z.unknown().optional(),
});

const ListSslVerificationSslVerificationDetailsSchema = z.object({
  items: z.array(SslVerificationSslVerificationDetailsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const UpdateSslVerificationEditSslCertificatePackValidationMethodSchema = z
  .object({
    status: z.unknown().optional(),
    validation_method: z.unknown().optional(),
  });

// =============================================================================
// Model Definition
// =============================================================================

/** Cloudflare SSL/TLS — certificate packs, custom certificates, certificate authority */
export const model = {
  type: "@webframp/cloudflare/ssl-certificates",
  version: "2026.07.19.1",
  globalArguments: GlobalArgsSchema,

  upgrades: [],

  resources: {
    "certificates": {
      description: "List Certificates",
      schema: ListCertificatesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "certificate": {
      description: "Create Certificate",
      schema: CreateCertificateSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "ssl_configurations": {
      description: "List SSL Configurations",
      schema: ListSslConfigurationsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "ssl_configuration": {
      description: "Create SSL Configuration",
      schema: CreateSslConfigurationSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "custom_ssl_for_a_zone_re_prioritize_ssl_certificates": {
      description: "Re-prioritize SSL Certificates",
      schema: UpdateCustomSslForAZoneRePrioritizeSslCertificatesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "custom_ssl_for_a_zone_ssl_configuration_details": {
      description: "SSL Configuration Details",
      schema: GetCustomSslForAZoneSslConfigurationDetailsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "custom_ssl_for_a_zone_edit_ssl_configuration": {
      description: "Edit SSL Configuration",
      schema: UpdateCustomSslForAZoneEditSslConfigurationSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "analyze_certificate_analyze_certificate": {
      description: "Analyze Certificate",
      schema: CreateAnalyzeCertificateAnalyzeCertificateSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "certificate_packs": {
      description: "List Certificate Packs",
      schema: ListCertificatePacksSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "certificate_packs_order_advanced_certificate_manager_certificate_pack": {
      description: "Order Advanced Certificate Manager Certificate Pack",
      schema:
        CreateCertificatePacksOrderAdvancedCertificateManagerCertificatePackSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "certificate_pack_quotas": {
      description: "Get Certificate Pack Quotas",
      schema: GetCertificatePackQuotasSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "certificate_pack": {
      description: "Get Certificate Pack",
      schema: GetCertificatePackSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "certificate_packs_restart_validation_for_advanced_certificate_manager_certificate_pack":
      {
        description:
          "Restart Validation or Update Advanced Certificate Manager Certificate Pack",
        schema:
          UpdateCertificatePacksRestartValidationForAdvancedCertificateManagerCertificatePackSchema,
        lifetime: "infinite" as const,
        garbageCollection: 20,
      },
    "universal_ssl_settings_for_a_zone_universal_ssl_settings_details": {
      description: "Universal SSL Settings Details",
      schema: GetUniversalSslSettingsForAZoneUniversalSslSettingsDetailsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "universal_ssl_settings_for_a_zone_edit_universal_ssl_settings": {
      description: "Edit Universal SSL Settings",
      schema: UpdateUniversalSslSettingsForAZoneEditUniversalSslSettingsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "ssl_verification_ssl_verification_details": {
      description: "SSL Verification Details",
      schema: ListSslVerificationSslVerificationDetailsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "ssl_verification_edit_ssl_certificate_pack_validation_method": {
      description: "Edit SSL Certificate Pack Validation Method",
      schema: UpdateSslVerificationEditSslCertificatePackValidationMethodSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
  },

  methods: {
    list_certificates: {
      description: "List Certificates",
      arguments: z.object({
        zone_id: z.string().optional(),
        page: z.number().optional(),
        per_page: z.number().optional(),
        limit: z.number().optional(),
        offset: z.number().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;
        const params: Record<string, string> = {};
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/certificates`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("certificates", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} certificates", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_certificate: {
      description: "Create Certificate",
      arguments: z.object({
        csr: z.unknown(),
        hostnames: z.unknown(),
        request_type: z.unknown(),
        requested_validity: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/certificates`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource("certificate", id, result);
        context.logger.info("Created certificate {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_certificate: {
      description: "Get Certificate",
      arguments: z.object({
        certificate_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/certificates/${args.certificate_id}`,
        );

        const handle = await context.writeResource(
          "certificate",
          String(args.certificate_id),
          result,
        );
        context.logger.info("Fetched certificate", {});
        return { dataHandles: [handle] };
      },
    },
    delete_origin_ca_revoke_certificate: {
      description: "Revoke Certificate",
      arguments: z.object({
        certificate_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/certificates/${args.certificate_id}`,
        );

        context.logger.info("Deleted resource {id}", {
          id: args.certificate_id,
        });
        return { dataHandles: [] };
      },
    },
    list_ssl_configurations: {
      description: "List SSL Configurations",
      arguments: z.object({
        page: z.number().optional(),
        per_page: z.number().optional(),
        match: z.enum(["any", "all"]).optional(),
        status: z.enum([
          "active",
          "expired",
          "deleted",
          "pending",
          "initializing",
        ]).optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;
        const params: Record<string, string> = {};
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/zones/${zoneId}/custom_certificates`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "ssl_configurations",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} ssl_configurations", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_ssl_configuration: {
      description: "Create SSL Configuration",
      arguments: z.object({
        bundle_method: z.unknown().optional(),
        certificate: z.unknown(),
        custom_csr_id: z.unknown().optional(),
        deploy: z.unknown().optional(),
        geo_restrictions: z.unknown().optional(),
        policy: z.unknown().optional(),
        private_key: z.unknown().optional(),
        type: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/zones/${zoneId}/custom_certificates`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "ssl_configuration",
          id,
          result,
        );
        context.logger.info("Created ssl_configuration {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    update_custom_ssl_for_a_zone_re_prioritize_ssl_certificates: {
      description: "Re-prioritize SSL Certificates",
      arguments: z.object({
        certificates: z.array(z.object({
          id: z.unknown().optional(),
          priority: z.unknown().optional(),
        })).describe("Array of ordered certificates."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/zones/${zoneId}/custom_certificates/prioritize`,
          args,
        );

        const handle = await context.writeResource(
          "custom_ssl_for_a_zone_re_prioritize_ssl_certificates",
          "updated",
          result,
        );
        context.logger.info(
          "Updated custom_ssl_for_a_zone_re_prioritize_ssl_certificates",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    get_custom_ssl_for_a_zone_ssl_configuration_details: {
      description: "SSL Configuration Details",
      arguments: z.object({
        custom_certificate_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/zones/${zoneId}/custom_certificates/${args.custom_certificate_id}`,
        );

        const handle = await context.writeResource(
          "custom_ssl_for_a_zone_ssl_configuration_details",
          String(args.custom_certificate_id),
          result,
        );
        context.logger.info(
          "Fetched custom_ssl_for_a_zone_ssl_configuration_details",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    update_custom_ssl_for_a_zone_edit_ssl_configuration: {
      description: "Edit SSL Configuration",
      arguments: z.object({
        custom_certificate_id: z.string(),
        bundle_method: z.unknown().optional(),
        certificate: z.unknown().optional(),
        custom_csr_id: z.unknown().optional(),
        deploy: z.unknown().optional(),
        geo_restrictions: z.unknown().optional(),
        policy: z.unknown().optional(),
        private_key: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/zones/${zoneId}/custom_certificates/${args.custom_certificate_id}`,
          args,
        );

        const handle = await context.writeResource(
          "custom_ssl_for_a_zone_edit_ssl_configuration",
          String(args.custom_certificate_id),
          result,
        );
        context.logger.info(
          "Updated custom_ssl_for_a_zone_edit_ssl_configuration",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    delete_ssl_configuration: {
      description: "Delete SSL Configuration",
      arguments: z.object({
        custom_certificate_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/zones/${zoneId}/custom_certificates/${args.custom_certificate_id}`,
        );

        context.logger.info("Deleted resource {id}", {
          id: args.custom_certificate_id,
        });
        return { dataHandles: [] };
      },
    },
    create_analyze_certificate_analyze_certificate: {
      description: "Analyze Certificate",
      arguments: z.object({
        bundle_method: z.unknown().optional(),
        certificate: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/zones/${zoneId}/ssl/analyze`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "analyze_certificate_analyze_certificate",
          id,
          result,
        );
        context.logger.info(
          "Created analyze_certificate_analyze_certificate {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    list_certificate_packs: {
      description: "List Certificate Packs",
      arguments: z.object({
        page: z.number().optional(),
        per_page: z.number().optional(),
        status: z.enum(["all"]).optional(),
        deploy: z.enum(["staging", "production"]).optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;
        const params: Record<string, string> = {};
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/zones/${zoneId}/ssl/certificate_packs`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "certificate_packs",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} certificate_packs", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_certificate_packs_order_advanced_certificate_manager_certificate_pack:
      {
        description: "Order Advanced Certificate Manager Certificate Pack",
        arguments: z.object({
          certificate_authority: z.unknown(),
          cloudflare_branding: z.unknown().optional(),
          hosts: z.unknown(),
          type: z.unknown(),
          validation_method: z.unknown(),
          validity_days: z.unknown(),
        }),
        execute: async (
          args: Record<string, unknown>,
          context: {
            globalArgs: Record<string, string>;
            writeResource: (
              spec: string,
              instance: string,
              data: unknown,
            ) => Promise<{ name: string }>;
            logger: {
              info: (msg: string, props: Record<string, unknown>) => void;
            };
          },
        ) => {
          const { apiToken, zoneId } = context.globalArgs;
          const result = await cfApi<Record<string, unknown>>(
            apiToken,
            "POST",
            `/zones/${zoneId}/ssl/certificate_packs/order`,
            args,
          );

          const id = (result as { id?: string }).id ?? "created";
          const handle = await context.writeResource(
            "certificate_packs_order_advanced_certificate_manager_certificate_pack",
            id,
            result,
          );
          context.logger.info(
            "Created certificate_packs_order_advanced_certificate_manager_certificate_pack {id}",
            { id },
          );
          return { dataHandles: [handle] };
        },
      },
    get_certificate_pack_quotas: {
      description: "Get Certificate Pack Quotas",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/zones/${zoneId}/ssl/certificate_packs/quota`,
        );

        const handle = await context.writeResource(
          "certificate_pack_quotas",
          "latest",
          result,
        );
        context.logger.info("Fetched certificate_pack_quotas", {});
        return { dataHandles: [handle] };
      },
    },
    get_certificate_pack: {
      description: "Get Certificate Pack",
      arguments: z.object({
        certificate_pack_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/zones/${zoneId}/ssl/certificate_packs/${args.certificate_pack_id}`,
        );

        const handle = await context.writeResource(
          "certificate_pack",
          String(args.certificate_pack_id),
          result,
        );
        context.logger.info("Fetched certificate_pack", {});
        return { dataHandles: [handle] };
      },
    },
    update_certificate_packs_restart_validation_for_advanced_certificate_manager_certificate_pack:
      {
        description:
          "Restart Validation or Update Advanced Certificate Manager Certificate Pack",
        arguments: z.object({
          certificate_pack_id: z.string(),
          cloudflare_branding: z.unknown().optional(),
        }),
        execute: async (
          args: Record<string, unknown>,
          context: {
            globalArgs: Record<string, string>;
            writeResource: (
              spec: string,
              instance: string,
              data: unknown,
            ) => Promise<{ name: string }>;
            logger: {
              info: (msg: string, props: Record<string, unknown>) => void;
            };
          },
        ) => {
          const { apiToken, zoneId } = context.globalArgs;
          const result = await cfApi<Record<string, unknown>>(
            apiToken,
            "PATCH",
            `/zones/${zoneId}/ssl/certificate_packs/${args.certificate_pack_id}`,
            args,
          );

          const handle = await context.writeResource(
            "certificate_packs_restart_validation_for_advanced_certificate_manager_certificate_pack",
            String(args.certificate_pack_id),
            result,
          );
          context.logger.info(
            "Updated certificate_packs_restart_validation_for_advanced_certificate_manager_certificate_pack",
            {},
          );
          return { dataHandles: [handle] };
        },
      },
    delete_advanced_certificate_manager_certificate_pack: {
      description: "Delete Advanced Certificate Manager Certificate Pack",
      arguments: z.object({
        certificate_pack_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/zones/${zoneId}/ssl/certificate_packs/${args.certificate_pack_id}`,
        );

        context.logger.info("Deleted resource {id}", {
          id: args.certificate_pack_id,
        });
        return { dataHandles: [] };
      },
    },
    get_universal_ssl_settings_for_a_zone_universal_ssl_settings_details: {
      description: "Universal SSL Settings Details",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/zones/${zoneId}/ssl/universal/settings`,
        );

        const handle = await context.writeResource(
          "universal_ssl_settings_for_a_zone_universal_ssl_settings_details",
          "latest",
          result,
        );
        context.logger.info(
          "Fetched universal_ssl_settings_for_a_zone_universal_ssl_settings_details",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    update_universal_ssl_settings_for_a_zone_edit_universal_ssl_settings: {
      description: "Edit Universal SSL Settings",
      arguments: z.object({
        enabled: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/zones/${zoneId}/ssl/universal/settings`,
          args,
        );

        const handle = await context.writeResource(
          "universal_ssl_settings_for_a_zone_edit_universal_ssl_settings",
          "updated",
          result,
        );
        context.logger.info(
          "Updated universal_ssl_settings_for_a_zone_edit_universal_ssl_settings",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    list_ssl_verification_ssl_verification_details: {
      description: "SSL Verification Details",
      arguments: z.object({
        retry: z.boolean().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;
        const params: Record<string, string> = {};
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/zones/${zoneId}/ssl/verification`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "ssl_verification_ssl_verification_details",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info(
          "Found {count} ssl_verification_ssl_verification_details",
          { count: results.length },
        );
        return { dataHandles: [handle] };
      },
    },
    update_ssl_verification_edit_ssl_certificate_pack_validation_method: {
      description: "Edit SSL Certificate Pack Validation Method",
      arguments: z.object({
        certificate_pack_id: z.string(),
        validation_method: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, zoneId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/zones/${zoneId}/ssl/verification/${args.certificate_pack_id}`,
          args,
        );

        const handle = await context.writeResource(
          "ssl_verification_edit_ssl_certificate_pack_validation_method",
          String(args.certificate_pack_id),
          result,
        );
        context.logger.info(
          "Updated ssl_verification_edit_ssl_certificate_pack_validation_method",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
  },
};
