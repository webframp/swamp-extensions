// Cloudflare Hyperdrive — database connection pooling configurations — Tests
// Auto-generated by scripts/cloudflare-codegen — do not edit manually.
// SPDX-License-Identifier: Apache-2.0

import { assertEquals, assertExists } from "jsr:@std/assert@1.0.19";
import { createModelTestContext } from "@systeminit/swamp-testing";
import { model } from "./hyperdrive.ts";

// ---------------------------------------------------------------------------
// Model Export Structure Tests
// ---------------------------------------------------------------------------

Deno.test("hyperdrive model: has correct type", () => {
  assertEquals(model.type, "@webframp/cloudflare/hyperdrive");
});

Deno.test("hyperdrive model: has valid version format", () => {
  const versionPattern = /^\d{4}\.\d{2}\.\d{2}\.\d+$/;
  assertEquals(versionPattern.test(model.version), true);
});

Deno.test("hyperdrive model: has globalArguments with apiToken", () => {
  assertExists(model.globalArguments);
  const shape = model.globalArguments.shape;
  assertExists(shape.apiToken);
  assertExists(shape.accountId);
});

Deno.test("hyperdrive model: has expected methods", () => {
  assertExists(model.methods);
  assertExists(model.methods.list_hyperdrive);
  assertExists(model.methods.create_hyperdrive);
  assertExists(model.methods.get_hyperdrive);
  assertExists(model.methods.update_hyperdrive);
  assertExists(model.methods.patch_hyperdrive);
  assertExists(model.methods.delete_hyperdrive);
});

Deno.test("hyperdrive model: has expected resources", () => {
  assertExists(model.resources);
  assertExists(model.resources["hyperdrive"]);
  assertExists(model.resources["patch_hyperdrive"]);
});

// ---------------------------------------------------------------------------
// Mock Cloudflare API Server
// ---------------------------------------------------------------------------

function startMockCfServer(
  responses: Record<string, { result: unknown; isCollection?: boolean }>,
): { url: string; server: Deno.HttpServer } {
  const server = Deno.serve({ port: 0, onListen() {} }, (req) => {
    const url = new URL(req.url);
    const path = url.pathname;

    for (
      const [pattern, { result, isCollection }] of Object.entries(responses)
    ) {
      if (path.includes(pattern)) {
        return Response.json({
          success: true,
          errors: [],
          messages: [],
          result,
          result_info: isCollection
            ? {
              page: 1,
              per_page: 50,
              total_count: Array.isArray(result) ? result.length : 1,
              total_pages: 1,
            }
            : undefined,
        });
      }
    }

    return Response.json({
      success: false,
      errors: [{ code: 404, message: "Not found" }],
      messages: [],
      result: null,
    });
  });

  const addr = server.addr as Deno.NetAddr;
  return { url: `http://localhost:${addr.port}`, server };
}

function installFetchMock(mockUrl: string): () => void {
  const originalFetch = globalThis.fetch;
  globalThis.fetch = (input, init) => {
    const reqUrl = typeof input === "string" ? input : input.toString();
    const newUrl = reqUrl.replace(
      "https://api.cloudflare.com/client/v4",
      mockUrl,
    );
    return originalFetch(newUrl, init);
  };
  return () => {
    globalThis.fetch = originalFetch;
  };
}

Deno.test({
  name: "hyperdrive model: list_hyperdrive fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = [{
      "caching": null,
      "created_on": "2017-01-01T00:00:00Z",
      "id": null,
      "modified_on": "2017-01-01T00:00:00Z",
      "mtls": null,
      "name": null,
      "origin": {},
      "origin_connection_limit": null,
    }];
    const { url, server } = startMockCfServer({
      "/hyperdrive/configs": { result: mockData, isCollection: true },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: {
          id: "test-id",
          name: "test-hyperdrive",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).list_hyperdrive.execute({}, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "hyperdrive model: create_hyperdrive creates and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "id": null,
      "caching": null,
      "created_on": "2017-01-01T00:00:00Z",
      "modified_on": "2017-01-01T00:00:00Z",
      "mtls": null,
      "name": null,
      "origin": {},
      "origin_connection_limit": null,
    };
    const { url, server } = startMockCfServer({
      "/hyperdrive/configs": { result: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: {
          id: "test-id",
          name: "test-hyperdrive",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).create_hyperdrive.execute({
        "caching": null,
        "created_on": "2017-01-01T00:00:00Z",
        "id": null,
        "modified_on": "2017-01-01T00:00:00Z",
        "mtls": null,
        "name": null,
        "origin": {},
        "origin_connection_limit": null,
      }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "hyperdrive model: get_hyperdrive fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "caching": null,
      "created_on": "2017-01-01T00:00:00Z",
      "id": null,
      "modified_on": "2017-01-01T00:00:00Z",
      "mtls": null,
      "name": null,
      "origin": {},
      "origin_connection_limit": null,
    };
    const { url, server } = startMockCfServer({
      "/hyperdrive/configs/test-id-123": { result: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: {
          id: "test-id",
          name: "test-hyperdrive",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).get_hyperdrive.execute({ "hyperdrive_id": "test-id-123" }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "hyperdrive model: update_hyperdrive executes and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "caching": null,
      "created_on": "2017-01-01T00:00:00Z",
      "id": null,
      "modified_on": "2017-01-01T00:00:00Z",
      "mtls": null,
      "name": null,
      "origin": {},
      "origin_connection_limit": null,
    };
    const { url, server } = startMockCfServer({
      "/hyperdrive/configs/test-id-123": { result: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: {
          id: "test-id",
          name: "test-hyperdrive",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).update_hyperdrive.execute({
        "hyperdrive_id": "test-id-123",
        "caching": null,
        "created_on": "2017-01-01T00:00:00Z",
        "id": null,
        "modified_on": "2017-01-01T00:00:00Z",
        "mtls": null,
        "name": null,
        "origin": {},
        "origin_connection_limit": null,
      }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "hyperdrive model: delete_hyperdrive executes successfully",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockCfServer({
      "/hyperdrive/configs/test-id-123": { result: { id: "test-id-123" } },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: {
          id: "test-id",
          name: "test-hyperdrive",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).delete_hyperdrive.execute(
        { "hyperdrive_id": "test-id-123" },
        context,
      );
      assertEquals(result.dataHandles.length, 0);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});
