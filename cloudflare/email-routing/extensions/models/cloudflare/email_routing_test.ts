// Cloudflare Email Routing — rules, addresses, catch-all, DNS setup — Tests
// Auto-generated by scripts/cloudflare-codegen — do not edit manually.
// SPDX-License-Identifier: Apache-2.0

import { assertEquals, assertExists } from "jsr:@std/assert@1";
import { createModelTestContext } from "@systeminit/swamp-testing";
import { model } from "./email_routing.ts";

// ---------------------------------------------------------------------------
// Model Export Structure Tests
// ---------------------------------------------------------------------------

Deno.test("email-routing model: has correct type", () => {
  assertEquals(model.type, "@webframp/cloudflare/email-routing");
});

Deno.test("email-routing model: has valid version format", () => {
  const versionPattern = /^\d{4}\.\d{2}\.\d{2}\.\d+$/;
  assertEquals(versionPattern.test(model.version), true);
});

Deno.test("email-routing model: has globalArguments with apiToken", () => {
  assertExists(model.globalArguments);
  const shape = model.globalArguments.shape;
  assertExists(shape.apiToken);
  assertExists(shape.zoneId);
});

Deno.test("email-routing model: has expected methods", () => {
  assertExists(model.methods);
  assertExists(model.methods.get_dmarc_reports_status);
  assertExists(model.methods.update_configure_dmarc_reports);
  assertExists(model.methods.get_inspect_spf);
  assertExists(model.methods.get_email_routing_settings);
  assertExists(
    model.methods.update_email_routing_settings_replace_email_routing_settings,
  );
  assertExists(model.methods.update_email_routing_settings);
  assertExists(
    model.methods.get_email_routing_settings_email_routing_dns_settings,
  );
  assertExists(
    model.methods.create_email_routing_settings_enable_email_routing_dns,
  );
  assertExists(
    model.methods.update_email_routing_settings_unlock_email_routing_dns,
  );
  assertExists(
    model.methods.delete_email_routing_settings_disable_email_routing_dns,
  );
  assertExists(model.methods.list_routing_rules);
  assertExists(model.methods.create_routing_rule);
  assertExists(model.methods.get_catch_all_rule);
  assertExists(model.methods.update_catch_all_rule);
  assertExists(model.methods.get_routing_rule);
  assertExists(model.methods.update_routing_rule);
  assertExists(model.methods.delete_routing_rule);
  assertExists(model.methods.get_publiclistsuppressionzonerouting);
  assertExists(model.methods.create_post_publicnewsuppressionzonerouting);
  assertExists(model.methods.get_publicgetsuppressionzonerouting);
  assertExists(model.methods.delete_publicdeletesuppressionzonerouting);
  assertExists(model.methods.list_sending_subdomains);
  assertExists(model.methods.create_sending_subdomain);
  assertExists(
    model.methods.create_email_sending_subdomains_preview_sending_subdomain,
  );
  assertExists(model.methods.get_sending_subdomain);
  assertExists(model.methods.update_sending_subdomain);
  assertExists(model.methods.delete_sending_subdomain);
  assertExists(model.methods.get_sending_subdomain_dns);
  assertExists(
    model.methods.email_sending_subdomains_fix_sending_subdomain_dns,
  );
  assertExists(model.methods.get_sending_subdomain_dns_status);
  assertExists(model.methods.get_publiclistsuppressionzonesending);
  assertExists(model.methods.create_post_publicnewsuppressionzonesending);
  assertExists(model.methods.get_publicgetsuppressionzonesending);
  assertExists(model.methods.delete_publicdeletesuppressionzonesending);
});

Deno.test("email-routing model: has expected resources", () => {
  assertExists(model.resources);
  assertExists(model.resources["dmarc_reports_status"]);
  assertExists(model.resources["configure_dmarc_reports"]);
  assertExists(model.resources["inspect_spf"]);
  assertExists(model.resources["email_routing_settings"]);
  assertExists(
    model.resources["email_routing_settings_replace_email_routing_settings"],
  );
  assertExists(
    model.resources["email_routing_settings_email_routing_dns_settings"],
  );
  assertExists(
    model.resources["email_routing_settings_enable_email_routing_dns"],
  );
  assertExists(
    model.resources["email_routing_settings_unlock_email_routing_dns"],
  );
  assertExists(model.resources["routing_rules"]);
  assertExists(model.resources["routing_rule"]);
  assertExists(model.resources["catch_all_rule"]);
  assertExists(model.resources["get_publiclistsuppressionzonerouting"]);
  assertExists(model.resources["post_publicnewsuppressionzonerouting"]);
  assertExists(model.resources["publicgetsuppressionzonerouting"]);
  assertExists(model.resources["sending_subdomains"]);
  assertExists(model.resources["sending_subdomain"]);
  assertExists(
    model.resources["email_sending_subdomains_preview_sending_subdomain"],
  );
  assertExists(model.resources["get_sending_subdomain_dns"]);
  assertExists(
    model.resources["email_sending_subdomains_fix_sending_subdomain_dns"],
  );
  assertExists(model.resources["sending_subdomain_dns_status"]);
  assertExists(model.resources["get_publiclistsuppressionzonesending"]);
  assertExists(model.resources["post_publicnewsuppressionzonesending"]);
  assertExists(model.resources["publicgetsuppressionzonesending"]);
});

// ---------------------------------------------------------------------------
// Mock Cloudflare API Server
// ---------------------------------------------------------------------------

function startMockCfServer(
  responses: Record<string, { result: unknown; isCollection?: boolean }>,
): { url: string; server: Deno.HttpServer } {
  const server = Deno.serve({ port: 0, onListen() {} }, (req) => {
    const url = new URL(req.url);
    const path = url.pathname;

    for (
      const [pattern, { result, isCollection }] of Object.entries(responses)
    ) {
      if (path.includes(pattern)) {
        return Response.json({
          success: true,
          errors: [],
          messages: [],
          result,
          result_info: isCollection
            ? {
              page: 1,
              per_page: 50,
              total_count: Array.isArray(result) ? result.length : 1,
              total_pages: 1,
            }
            : undefined,
        });
      }
    }

    return Response.json({
      success: false,
      errors: [{ code: 404, message: "Not found" }],
      messages: [],
      result: null,
    });
  });

  const addr = server.addr as Deno.NetAddr;
  return { url: `http://localhost:${addr.port}`, server };
}

function installFetchMock(mockUrl: string): () => void {
  const originalFetch = globalThis.fetch;
  globalThis.fetch = (input, init) => {
    const reqUrl = typeof input === "string" ? input : input.toString();
    const newUrl = reqUrl.replace(
      "https://api.cloudflare.com/client/v4",
      mockUrl,
    );
    return originalFetch(newUrl, init);
  };
  return () => {
    globalThis.fetch = originalFetch;
  };
}

Deno.test({
  name:
    "email-routing model: get_dmarc_reports_status fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "approved_sources": [null],
      "created": "2024-01-15T10:30:00.12345Z",
      "created_at": "2024-01-15T10:30:00.12345Z",
      "enabled": true,
      "modified": "2024-01-15T11:45:00.12345Z",
      "modified_at": "2024-01-15T11:45:00.12345Z",
      "records": null,
      "rua_prefix": "9233c80fc89f43e3a7b749605f651868",
      "skip_wizard": false,
      "status": null,
      "tag": "023e105f4ecef8ad9ca31a8372d0c353",
      "zone_id": "023e105f4ecef8ad9ca31a8372d0c353",
    };
    const { url, server } = startMockCfServer({
      "/email/auth/dmarc-reports": { result: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "zoneId": "zone-123" },
        definition: {
          id: "test-id",
          name: "test-email-routing",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).get_dmarc_reports_status.execute({}, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name:
    "email-routing model: update_configure_dmarc_reports executes and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "approved_sources": [null],
      "created": "2024-01-15T10:30:00.12345Z",
      "created_at": "2024-01-15T10:30:00.12345Z",
      "enabled": true,
      "modified": "2024-01-15T11:45:00.12345Z",
      "modified_at": "2024-01-15T11:45:00.12345Z",
      "records": null,
      "rua_prefix": "9233c80fc89f43e3a7b749605f651868",
      "skip_wizard": false,
      "status": null,
      "tag": "023e105f4ecef8ad9ca31a8372d0c353",
      "zone_id": "023e105f4ecef8ad9ca31a8372d0c353",
    };
    const { url, server } = startMockCfServer({
      "/email/auth/dmarc-reports": { result: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "zoneId": "zone-123" },
        definition: {
          id: "test-id",
          name: "test-email-routing",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).update_configure_dmarc_reports.execute({
        "approved_sources": [null],
        "created": "2024-01-15T10:30:00.12345Z",
        "created_at": "2024-01-15T10:30:00.12345Z",
        "enabled": true,
        "modified": "2024-01-15T11:45:00.12345Z",
        "modified_at": "2024-01-15T11:45:00.12345Z",
        "records": null,
        "rua_prefix": "9233c80fc89f43e3a7b749605f651868",
        "skip_wizard": false,
        "status": null,
        "tag": "023e105f4ecef8ad9ca31a8372d0c353",
        "zone_id": "023e105f4ecef8ad9ca31a8372d0c353",
      }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name:
    "email-routing model: create_email_routing_settings_enable_email_routing_dns creates and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "id": null,
      "created": null,
      "enabled": null,
      "modified": null,
      "name": null,
      "skip_wizard": null,
      "status": null,
      "support_subaddress": null,
      "tag": null,
    };
    const { url, server } = startMockCfServer({
      "/email/routing/dns": { result: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "zoneId": "zone-123" },
        definition: {
          id: "test-id",
          name: "test-email-routing",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).create_email_routing_settings_enable_email_routing_dns.execute({
        "created": null,
        "enabled": null,
        "id": null,
        "modified": null,
        "name": null,
        "skip_wizard": null,
        "status": null,
        "support_subaddress": null,
        "tag": null,
      }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name:
    "email-routing model: delete_email_routing_settings_disable_email_routing_dns executes successfully",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockCfServer({
      "/email/routing/dns": { result: { id: "test-id-123" } },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "zoneId": "zone-123" },
        definition: {
          id: "test-id",
          name: "test-email-routing",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).delete_email_routing_settings_disable_email_routing_dns.execute(
        {},
        context,
      );
      assertEquals(result.dataHandles.length, 0);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "email-routing model: list_routing_rules fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = [{
      "actions": null,
      "enabled": null,
      "id": null,
      "matchers": null,
      "name": null,
      "priority": null,
      "source": null,
      "tag": null,
    }];
    const { url, server } = startMockCfServer({
      "/email/routing/rules": { result: mockData, isCollection: true },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "zoneId": "zone-123" },
        definition: {
          id: "test-id",
          name: "test-email-routing",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).list_routing_rules.execute({}, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name:
    "email-routing model: email_sending_subdomains_fix_sending_subdomain_dns executes and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = { "errors": [null], "records": [null], "status": "ready" };
    const { url, server } = startMockCfServer({
      "/email/sending/subdomains/test-id-123/dns": { result: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "zoneId": "zone-123" },
        definition: {
          id: "test-id",
          name: "test-email-routing",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).email_sending_subdomains_fix_sending_subdomain_dns.execute({
        "subdomain_id": "test-id-123",
        "errors": [null],
        "records": [null],
        "status": "ready",
      }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});
