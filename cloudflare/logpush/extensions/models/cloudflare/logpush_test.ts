// Cloudflare Logpush — log jobs, destinations, field configurations — Tests
// Auto-generated by scripts/cloudflare-codegen — do not edit manually.
// SPDX-License-Identifier: Apache-2.0

import { assertEquals, assertExists } from "jsr:@std/assert@1";
import { createModelTestContext } from "@systeminit/swamp-testing";
import { model } from "./logpush.ts";

// ---------------------------------------------------------------------------
// Model Export Structure Tests
// ---------------------------------------------------------------------------

Deno.test("logpush model: has correct type", () => {
  assertEquals(model.type, "@webframp/cloudflare/logpush");
});

Deno.test("logpush model: has valid version format", () => {
  const versionPattern = /^\d{4}\.\d{2}\.\d{2}\.\d+$/;
  assertEquals(versionPattern.test(model.version), true);
});

Deno.test("logpush model: has globalArguments with apiToken", () => {
  assertExists(model.globalArguments);
  const shape = model.globalArguments.shape;
  assertExists(shape.apiToken);
  assertExists(shape.accountId);
});

Deno.test("logpush model: has expected methods", () => {
  assertExists(model.methods);
  assertExists(
    model.methods.get_accounts_account_id_logpush_datasets_dataset_id_fields,
  );
  assertExists(
    model.methods.get_accounts_account_id_logpush_datasets_dataset_id_jobs,
  );
  assertExists(model.methods.get_accounts_account_id_logpush_jobs);
  assertExists(model.methods.create_post_accounts_account_id_logpush_jobs);
  assertExists(model.methods.get_accounts_account_id_logpush_jobs_job_id);
  assertExists(model.methods.put_accounts_account_id_logpush_jobs_job_id);
  assertExists(model.methods.delete_accounts_account_id_logpush_jobs_job_id);
  assertExists(model.methods.create_post_accounts_account_id_logpush_ownership);
  assertExists(
    model.methods.create_post_accounts_account_id_logpush_ownership_validate,
  );
  assertExists(
    model.methods.delete_accounts_account_id_logpush_validate_destination,
  );
  assertExists(
    model.methods
      .delete_accounts_account_id_logpush_validate_destination_exists,
  );
  assertExists(
    model.methods.create_post_accounts_account_id_logpush_validate_origin,
  );
  assertExists(
    model.methods.get_zones_zone_id_logpush_datasets_dataset_id_fields,
  );
  assertExists(
    model.methods.get_zones_zone_id_logpush_datasets_dataset_id_jobs,
  );
  assertExists(model.methods.get_zones_zone_id_logpush_edge_jobs);
  assertExists(model.methods.create_post_zones_zone_id_logpush_edge_jobs);
  assertExists(model.methods.get_zones_zone_id_logpush_jobs);
  assertExists(model.methods.create_post_zones_zone_id_logpush_jobs);
  assertExists(model.methods.get_zones_zone_id_logpush_jobs_job_id);
  assertExists(model.methods.put_zones_zone_id_logpush_jobs_job_id);
  assertExists(model.methods.delete_zones_zone_id_logpush_jobs_job_id);
  assertExists(model.methods.create_post_zones_zone_id_logpush_ownership);
  assertExists(
    model.methods.create_post_zones_zone_id_logpush_ownership_validate,
  );
  assertExists(
    model.methods.create_post_zones_zone_id_logpush_validate_destination,
  );
  assertExists(
    model.methods.create_post_zones_zone_id_logpush_validate_destination_exists,
  );
  assertExists(model.methods.create_post_zones_zone_id_logpush_validate_origin);
});

Deno.test("logpush model: has expected resources", () => {
  assertExists(model.resources);
  assertExists(
    model.resources["accounts_account_id_logpush_datasets_dataset_id_fields"],
  );
  assertExists(
    model.resources["get_accounts_account_id_logpush_datasets_dataset_id_jobs"],
  );
  assertExists(model.resources["get_accounts_account_id_logpush_jobs"]);
  assertExists(model.resources["post_accounts_account_id_logpush_jobs"]);
  assertExists(model.resources["accounts_account_id_logpush_jobs_job_id"]);
  assertExists(model.resources["put_accounts_account_id_logpush_jobs_job_id"]);
  assertExists(model.resources["post_accounts_account_id_logpush_ownership"]);
  assertExists(
    model.resources["post_accounts_account_id_logpush_ownership_validate"],
  );
  assertExists(
    model.resources["delete_accounts_account_id_logpush_validate_destination"],
  );
  assertExists(
    model
      .resources[
        "delete_accounts_account_id_logpush_validate_destination_exists"
      ],
  );
  assertExists(
    model.resources["post_accounts_account_id_logpush_validate_origin"],
  );
  assertExists(
    model.resources["zones_zone_id_logpush_datasets_dataset_id_fields"],
  );
  assertExists(
    model.resources["get_zones_zone_id_logpush_datasets_dataset_id_jobs"],
  );
  assertExists(model.resources["get_zones_zone_id_logpush_edge_jobs"]);
  assertExists(model.resources["post_zones_zone_id_logpush_edge_jobs"]);
  assertExists(model.resources["get_zones_zone_id_logpush_jobs"]);
  assertExists(model.resources["post_zones_zone_id_logpush_jobs"]);
  assertExists(model.resources["zones_zone_id_logpush_jobs_job_id"]);
  assertExists(model.resources["put_zones_zone_id_logpush_jobs_job_id"]);
  assertExists(model.resources["post_zones_zone_id_logpush_ownership"]);
  assertExists(
    model.resources["post_zones_zone_id_logpush_ownership_validate"],
  );
  assertExists(
    model.resources["post_zones_zone_id_logpush_validate_destination"],
  );
  assertExists(
    model.resources["post_zones_zone_id_logpush_validate_destination_exists"],
  );
  assertExists(model.resources["post_zones_zone_id_logpush_validate_origin"]);
});

// ---------------------------------------------------------------------------
// Mock Cloudflare API Server
// ---------------------------------------------------------------------------

function startMockCfServer(
  responses: Record<string, { result: unknown; isCollection?: boolean }>,
): { url: string; server: Deno.HttpServer } {
  const server = Deno.serve({ port: 0, onListen() {} }, (req) => {
    const url = new URL(req.url);
    const path = url.pathname;

    for (
      const [pattern, { result, isCollection }] of Object.entries(responses)
    ) {
      if (path.includes(pattern)) {
        return Response.json({
          success: true,
          errors: [],
          messages: [],
          result,
          result_info: isCollection
            ? {
              page: 1,
              per_page: 50,
              total_count: Array.isArray(result) ? result.length : 1,
              total_pages: 1,
            }
            : undefined,
        });
      }
    }

    return Response.json({
      success: false,
      errors: [{ code: 404, message: "Not found" }],
      messages: [],
      result: null,
    });
  });

  const addr = server.addr as Deno.NetAddr;
  return { url: `http://localhost:${addr.port}`, server };
}

function installFetchMock(mockUrl: string): () => void {
  const originalFetch = globalThis.fetch;
  globalThis.fetch = (input, init) => {
    const reqUrl = typeof input === "string" ? input : input.toString();
    const newUrl = reqUrl.replace(
      "https://api.cloudflare.com/client/v4",
      mockUrl,
    );
    return originalFetch(newUrl, init);
  };
  return () => {
    globalThis.fetch = originalFetch;
  };
}

Deno.test({
  name:
    "logpush model: get_accounts_account_id_logpush_datasets_dataset_id_fields fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {};
    const { url, server } = startMockCfServer({
      "/logpush/datasets/test-id-123/fields": { result: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: {
          id: "test-id",
          name: "test-logpush",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).get_accounts_account_id_logpush_datasets_dataset_id_fields.execute({
        "dataset_id": "test-id-123",
      }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name:
    "logpush model: get_accounts_account_id_logpush_datasets_dataset_id_jobs fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = [{
      "dataset": null,
      "destination_conf": null,
      "enabled": null,
      "error_message": null,
      "frequency": null,
      "id": null,
      "kind": null,
      "last_complete": null,
      "last_error": null,
      "logpull_options": null,
      "max_upload_bytes": null,
      "max_upload_interval_seconds": null,
      "max_upload_records": null,
      "name": null,
      "output_options": null,
    }];
    const { url, server } = startMockCfServer({
      "/logpush/datasets/test-id-123/jobs": {
        result: mockData,
        isCollection: true,
      },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: {
          id: "test-id",
          name: "test-logpush",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).get_accounts_account_id_logpush_datasets_dataset_id_jobs.execute({
        "dataset_id": "test-id-123",
      }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name:
    "logpush model: create_post_accounts_account_id_logpush_jobs creates and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "id": null,
      "dataset": null,
      "destination_conf": null,
      "enabled": null,
      "error_message": null,
      "frequency": null,
      "kind": null,
      "last_complete": null,
      "last_error": null,
      "logpull_options": null,
      "max_upload_bytes": null,
      "max_upload_interval_seconds": null,
      "max_upload_records": null,
      "name": null,
      "output_options": null,
    };
    const { url, server } = startMockCfServer({
      "/logpush/jobs": { result: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: {
          id: "test-id",
          name: "test-logpush",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).create_post_accounts_account_id_logpush_jobs.execute({
        "dataset": null,
        "destination_conf": null,
        "enabled": null,
        "error_message": null,
        "frequency": null,
        "id": null,
        "kind": null,
        "last_complete": null,
        "last_error": null,
        "logpull_options": null,
        "max_upload_bytes": null,
        "max_upload_interval_seconds": null,
        "max_upload_records": null,
        "name": null,
        "output_options": null,
      }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name:
    "logpush model: put_accounts_account_id_logpush_jobs_job_id executes and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockData = {
      "dataset": null,
      "destination_conf": null,
      "enabled": null,
      "error_message": null,
      "frequency": null,
      "id": null,
      "kind": null,
      "last_complete": null,
      "last_error": null,
      "logpull_options": null,
      "max_upload_bytes": null,
      "max_upload_interval_seconds": null,
      "max_upload_records": null,
      "name": null,
      "output_options": null,
    };
    const { url, server } = startMockCfServer({
      "/logpush/jobs/test-id-123": { result: mockData },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: {
          id: "test-id",
          name: "test-logpush",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).put_accounts_account_id_logpush_jobs_job_id.execute({
        "job_id": "test-id-123",
        "dataset": null,
        "destination_conf": null,
        "enabled": null,
        "error_message": null,
        "frequency": null,
        "id": null,
        "kind": null,
        "last_complete": null,
        "last_error": null,
        "logpull_options": null,
        "max_upload_bytes": null,
        "max_upload_interval_seconds": null,
        "max_upload_records": null,
        "name": null,
        "output_options": null,
      }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name:
    "logpush model: delete_accounts_account_id_logpush_jobs_job_id executes successfully",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockCfServer({
      "/logpush/jobs/test-id-123": { result: { id: "test-id-123" } },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context } = createModelTestContext({
        globalArgs: { "apiToken": "test-token", "accountId": "acct-123" },
        definition: {
          id: "test-id",
          name: "test-logpush",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).delete_accounts_account_id_logpush_jobs_job_id.execute({
        "job_id": "test-id-123",
      }, context);
      assertEquals(result.dataHandles.length, 0);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});
