/**
 * Cloudflare Logpush — log jobs, destinations, field configurations
 *
 * Auto-generated by scripts/cloudflare-codegen — do not edit manually.
 *
 * @module
 */
// SPDX-License-Identifier: AGPL-3.0-or-later WITH Swamp-Extension-Exception

import { z } from "npm:zod@4.4.3";
import { cfApi, cfApiPaginated } from "./_lib/api.ts";

// =============================================================================
// Schemas
// =============================================================================

const GlobalArgsSchema = z.object({
  apiToken: z.string().meta({ sensitive: true }).describe(
    "Cloudflare API token",
  ),
  accountId: z.string().describe("Cloudflare account ID"),
});

const GetAccountsAccountIdLogpushDatasetsDatasetIdFieldsSchema = z.object({});

const GetAccountsAccountIdLogpushDatasetsDatasetIdJobsItemSchema = z.object({
  dataset: z.unknown().optional(),
  destination_conf: z.unknown().optional(),
  enabled: z.unknown().optional(),
  error_message: z.unknown().optional(),
  frequency: z.unknown().optional(),
  id: z.unknown().optional(),
  kind: z.unknown().optional(),
  last_complete: z.unknown().optional(),
  last_error: z.unknown().optional(),
  logpull_options: z.unknown().optional(),
  max_upload_bytes: z.unknown().optional(),
  max_upload_interval_seconds: z.unknown().optional(),
  max_upload_records: z.unknown().optional(),
  name: z.unknown().optional(),
  output_options: z.unknown().optional(),
}).nullable();

const GetAccountsAccountIdLogpushDatasetsDatasetIdJobsSchema = z.object({
  items: z.array(GetAccountsAccountIdLogpushDatasetsDatasetIdJobsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetAccountsAccountIdLogpushJobsItemSchema = z.object({
  dataset: z.unknown().optional(),
  destination_conf: z.unknown().optional(),
  enabled: z.unknown().optional(),
  error_message: z.unknown().optional(),
  frequency: z.unknown().optional(),
  id: z.unknown().optional(),
  kind: z.unknown().optional(),
  last_complete: z.unknown().optional(),
  last_error: z.unknown().optional(),
  logpull_options: z.unknown().optional(),
  max_upload_bytes: z.unknown().optional(),
  max_upload_interval_seconds: z.unknown().optional(),
  max_upload_records: z.unknown().optional(),
  name: z.unknown().optional(),
  output_options: z.unknown().optional(),
}).nullable();

const GetAccountsAccountIdLogpushJobsSchema = z.object({
  items: z.array(GetAccountsAccountIdLogpushJobsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreatePostAccountsAccountIdLogpushJobsSchema = z.object({
  dataset: z.unknown().optional(),
  destination_conf: z.unknown().optional(),
  enabled: z.unknown().optional(),
  error_message: z.unknown().optional(),
  frequency: z.unknown().optional(),
  id: z.unknown().optional(),
  kind: z.unknown().optional(),
  last_complete: z.unknown().optional(),
  last_error: z.unknown().optional(),
  logpull_options: z.unknown().optional(),
  max_upload_bytes: z.unknown().optional(),
  max_upload_interval_seconds: z.unknown().optional(),
  max_upload_records: z.unknown().optional(),
  name: z.unknown().optional(),
  output_options: z.unknown().optional(),
}).nullable();

const GetAccountsAccountIdLogpushJobsJobIdSchema = z.object({
  dataset: z.unknown().optional(),
  destination_conf: z.unknown().optional(),
  enabled: z.unknown().optional(),
  error_message: z.unknown().optional(),
  frequency: z.unknown().optional(),
  id: z.unknown().optional(),
  kind: z.unknown().optional(),
  last_complete: z.unknown().optional(),
  last_error: z.unknown().optional(),
  logpull_options: z.unknown().optional(),
  max_upload_bytes: z.unknown().optional(),
  max_upload_interval_seconds: z.unknown().optional(),
  max_upload_records: z.unknown().optional(),
  name: z.unknown().optional(),
  output_options: z.unknown().optional(),
}).nullable();

const PutAccountsAccountIdLogpushJobsJobIdSchema = z.object({
  dataset: z.unknown().optional(),
  destination_conf: z.unknown().optional(),
  enabled: z.unknown().optional(),
  error_message: z.unknown().optional(),
  frequency: z.unknown().optional(),
  id: z.unknown().optional(),
  kind: z.unknown().optional(),
  last_complete: z.unknown().optional(),
  last_error: z.unknown().optional(),
  logpull_options: z.unknown().optional(),
  max_upload_bytes: z.unknown().optional(),
  max_upload_interval_seconds: z.unknown().optional(),
  max_upload_records: z.unknown().optional(),
  name: z.unknown().optional(),
  output_options: z.unknown().optional(),
}).nullable();

const CreatePostAccountsAccountIdLogpushOwnershipSchema = z.object({
  filename: z.string().optional(),
  message: z.string().optional(),
  valid: z.boolean().optional(),
}).nullable();

const CreatePostAccountsAccountIdLogpushOwnershipValidateSchema = z.object({
  valid: z.boolean().optional(),
}).nullable();

const DeleteAccountsAccountIdLogpushValidateDestinationSchema = z.object({
  message: z.string().optional(),
  valid: z.boolean().optional(),
}).nullable();

const DeleteAccountsAccountIdLogpushValidateDestinationExistsSchema = z.object({
  exists: z.boolean().optional(),
}).nullable();

const CreatePostAccountsAccountIdLogpushValidateOriginSchema = z.object({
  message: z.string().optional(),
  valid: z.boolean().optional(),
}).nullable();

const GetZonesZoneIdLogpushDatasetsDatasetIdFieldsSchema = z.object({});

const GetZonesZoneIdLogpushDatasetsDatasetIdJobsItemSchema = z.object({
  dataset: z.unknown().optional(),
  destination_conf: z.unknown().optional(),
  enabled: z.unknown().optional(),
  error_message: z.unknown().optional(),
  frequency: z.unknown().optional(),
  id: z.unknown().optional(),
  kind: z.unknown().optional(),
  last_complete: z.unknown().optional(),
  last_error: z.unknown().optional(),
  logpull_options: z.unknown().optional(),
  max_upload_bytes: z.unknown().optional(),
  max_upload_interval_seconds: z.unknown().optional(),
  max_upload_records: z.unknown().optional(),
  name: z.unknown().optional(),
  output_options: z.unknown().optional(),
}).nullable();

const GetZonesZoneIdLogpushDatasetsDatasetIdJobsSchema = z.object({
  items: z.array(GetZonesZoneIdLogpushDatasetsDatasetIdJobsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetZonesZoneIdLogpushEdgeJobsItemSchema = z.object({
  destination_conf: z.unknown().optional(),
  fields: z.unknown().optional(),
  filter: z.unknown().optional(),
  sample: z.unknown().optional(),
  session_id: z.unknown().optional(),
}).nullable();

const GetZonesZoneIdLogpushEdgeJobsSchema = z.object({
  items: z.array(GetZonesZoneIdLogpushEdgeJobsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreatePostZonesZoneIdLogpushEdgeJobsSchema = z.object({
  destination_conf: z.unknown().optional(),
  fields: z.unknown().optional(),
  filter: z.unknown().optional(),
  sample: z.unknown().optional(),
  session_id: z.unknown().optional(),
}).nullable();

const GetZonesZoneIdLogpushJobsItemSchema = z.object({
  dataset: z.unknown().optional(),
  destination_conf: z.unknown().optional(),
  enabled: z.unknown().optional(),
  error_message: z.unknown().optional(),
  frequency: z.unknown().optional(),
  id: z.unknown().optional(),
  kind: z.unknown().optional(),
  last_complete: z.unknown().optional(),
  last_error: z.unknown().optional(),
  logpull_options: z.unknown().optional(),
  max_upload_bytes: z.unknown().optional(),
  max_upload_interval_seconds: z.unknown().optional(),
  max_upload_records: z.unknown().optional(),
  name: z.unknown().optional(),
  output_options: z.unknown().optional(),
}).nullable();

const GetZonesZoneIdLogpushJobsSchema = z.object({
  items: z.array(GetZonesZoneIdLogpushJobsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreatePostZonesZoneIdLogpushJobsSchema = z.object({
  dataset: z.unknown().optional(),
  destination_conf: z.unknown().optional(),
  enabled: z.unknown().optional(),
  error_message: z.unknown().optional(),
  frequency: z.unknown().optional(),
  id: z.unknown().optional(),
  kind: z.unknown().optional(),
  last_complete: z.unknown().optional(),
  last_error: z.unknown().optional(),
  logpull_options: z.unknown().optional(),
  max_upload_bytes: z.unknown().optional(),
  max_upload_interval_seconds: z.unknown().optional(),
  max_upload_records: z.unknown().optional(),
  name: z.unknown().optional(),
  output_options: z.unknown().optional(),
}).nullable();

const GetZonesZoneIdLogpushJobsJobIdSchema = z.object({
  dataset: z.unknown().optional(),
  destination_conf: z.unknown().optional(),
  enabled: z.unknown().optional(),
  error_message: z.unknown().optional(),
  frequency: z.unknown().optional(),
  id: z.unknown().optional(),
  kind: z.unknown().optional(),
  last_complete: z.unknown().optional(),
  last_error: z.unknown().optional(),
  logpull_options: z.unknown().optional(),
  max_upload_bytes: z.unknown().optional(),
  max_upload_interval_seconds: z.unknown().optional(),
  max_upload_records: z.unknown().optional(),
  name: z.unknown().optional(),
  output_options: z.unknown().optional(),
}).nullable();

const PutZonesZoneIdLogpushJobsJobIdSchema = z.object({
  dataset: z.unknown().optional(),
  destination_conf: z.unknown().optional(),
  enabled: z.unknown().optional(),
  error_message: z.unknown().optional(),
  frequency: z.unknown().optional(),
  id: z.unknown().optional(),
  kind: z.unknown().optional(),
  last_complete: z.unknown().optional(),
  last_error: z.unknown().optional(),
  logpull_options: z.unknown().optional(),
  max_upload_bytes: z.unknown().optional(),
  max_upload_interval_seconds: z.unknown().optional(),
  max_upload_records: z.unknown().optional(),
  name: z.unknown().optional(),
  output_options: z.unknown().optional(),
}).nullable();

const CreatePostZonesZoneIdLogpushOwnershipSchema = z.object({
  filename: z.string().optional(),
  message: z.string().optional(),
  valid: z.boolean().optional(),
}).nullable();

const CreatePostZonesZoneIdLogpushOwnershipValidateSchema = z.object({
  valid: z.boolean().optional(),
}).nullable();

const CreatePostZonesZoneIdLogpushValidateDestinationSchema = z.object({
  message: z.string().optional(),
  valid: z.boolean().optional(),
}).nullable();

const CreatePostZonesZoneIdLogpushValidateDestinationExistsSchema = z.object({
  exists: z.boolean().optional(),
}).nullable();

const CreatePostZonesZoneIdLogpushValidateOriginSchema = z.object({
  message: z.string().optional(),
  valid: z.boolean().optional(),
}).nullable();

// =============================================================================
// Model Definition
// =============================================================================

/** Cloudflare Logpush — log jobs, destinations, field configurations */
export const model = {
  type: "@webframp/cloudflare/logpush",
  version: "2026.07.19.1",
  globalArguments: GlobalArgsSchema,

  upgrades: [],

  resources: {
    "accounts_account_id_logpush_datasets_dataset_id_fields": {
      description: "List fields",
      schema: GetAccountsAccountIdLogpushDatasetsDatasetIdFieldsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "get_accounts_account_id_logpush_datasets_dataset_id_jobs": {
      description: "List Logpush jobs for a dataset",
      schema: GetAccountsAccountIdLogpushDatasetsDatasetIdJobsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "get_accounts_account_id_logpush_jobs": {
      description: "List Logpush jobs",
      schema: GetAccountsAccountIdLogpushJobsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "post_accounts_account_id_logpush_jobs": {
      description: "Create Logpush job",
      schema: CreatePostAccountsAccountIdLogpushJobsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "accounts_account_id_logpush_jobs_job_id": {
      description: "Get Logpush job details",
      schema: GetAccountsAccountIdLogpushJobsJobIdSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "put_accounts_account_id_logpush_jobs_job_id": {
      description: "Update Logpush job",
      schema: PutAccountsAccountIdLogpushJobsJobIdSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "post_accounts_account_id_logpush_ownership": {
      description: "Get ownership challenge",
      schema: CreatePostAccountsAccountIdLogpushOwnershipSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "post_accounts_account_id_logpush_ownership_validate": {
      description: "Validate ownership challenge",
      schema: CreatePostAccountsAccountIdLogpushOwnershipValidateSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "delete_accounts_account_id_logpush_validate_destination": {
      description: "Validate destination",
      schema: DeleteAccountsAccountIdLogpushValidateDestinationSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "delete_accounts_account_id_logpush_validate_destination_exists": {
      description: "Check destination exists",
      schema: DeleteAccountsAccountIdLogpushValidateDestinationExistsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "post_accounts_account_id_logpush_validate_origin": {
      description: "Validate origin",
      schema: CreatePostAccountsAccountIdLogpushValidateOriginSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "zones_zone_id_logpush_datasets_dataset_id_fields": {
      description: "List fields",
      schema: GetZonesZoneIdLogpushDatasetsDatasetIdFieldsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "get_zones_zone_id_logpush_datasets_dataset_id_jobs": {
      description: "List Logpush jobs for a dataset",
      schema: GetZonesZoneIdLogpushDatasetsDatasetIdJobsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "get_zones_zone_id_logpush_edge_jobs": {
      description: "List Instant Logs jobs",
      schema: GetZonesZoneIdLogpushEdgeJobsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "post_zones_zone_id_logpush_edge_jobs": {
      description: "Create Instant Logs job",
      schema: CreatePostZonesZoneIdLogpushEdgeJobsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "get_zones_zone_id_logpush_jobs": {
      description: "List Logpush jobs",
      schema: GetZonesZoneIdLogpushJobsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "post_zones_zone_id_logpush_jobs": {
      description: "Create Logpush job",
      schema: CreatePostZonesZoneIdLogpushJobsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "zones_zone_id_logpush_jobs_job_id": {
      description: "Get Logpush job details",
      schema: GetZonesZoneIdLogpushJobsJobIdSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "put_zones_zone_id_logpush_jobs_job_id": {
      description: "Update Logpush job",
      schema: PutZonesZoneIdLogpushJobsJobIdSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "post_zones_zone_id_logpush_ownership": {
      description: "Get ownership challenge",
      schema: CreatePostZonesZoneIdLogpushOwnershipSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "post_zones_zone_id_logpush_ownership_validate": {
      description: "Validate ownership challenge",
      schema: CreatePostZonesZoneIdLogpushOwnershipValidateSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "post_zones_zone_id_logpush_validate_destination": {
      description: "Validate destination",
      schema: CreatePostZonesZoneIdLogpushValidateDestinationSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "post_zones_zone_id_logpush_validate_destination_exists": {
      description: "Check destination exists",
      schema: CreatePostZonesZoneIdLogpushValidateDestinationExistsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "post_zones_zone_id_logpush_validate_origin": {
      description: "Validate origin",
      schema: CreatePostZonesZoneIdLogpushValidateOriginSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
  },

  methods: {
    get_accounts_account_id_logpush_datasets_dataset_id_fields: {
      description: "List fields",
      arguments: z.object({
        dataset_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/logpush/datasets/${args.dataset_id}/fields`,
        );

        const handle = await context.writeResource(
          "accounts_account_id_logpush_datasets_dataset_id_fields",
          String(args.dataset_id),
          result,
        );
        context.logger.info(
          "Fetched accounts_account_id_logpush_datasets_dataset_id_fields",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    get_accounts_account_id_logpush_datasets_dataset_id_jobs: {
      description: "List Logpush jobs for a dataset",
      arguments: z.object({
        dataset_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const pathParams = new Set(["dataset_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !pathParams.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/logpush/datasets/${args.dataset_id}/jobs`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "get_accounts_account_id_logpush_datasets_dataset_id_jobs",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info(
          "Found {count} get_accounts_account_id_logpush_datasets_dataset_id_jobs",
          { count: results.length },
        );
        return { dataHandles: [handle] };
      },
    },
    get_accounts_account_id_logpush_jobs: {
      description: "List Logpush jobs",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/logpush/jobs`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "get_accounts_account_id_logpush_jobs",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info(
          "Found {count} get_accounts_account_id_logpush_jobs",
          { count: results.length },
        );
        return { dataHandles: [handle] };
      },
    },
    create_post_accounts_account_id_logpush_jobs: {
      description: "Create Logpush job",
      arguments: z.object({
        dataset: z.unknown().optional(),
        destination_conf: z.unknown(),
        enabled: z.unknown().optional(),
        filter: z.unknown().optional(),
        frequency: z.unknown().optional(),
        kind: z.unknown().optional(),
        logpull_options: z.unknown().optional(),
        max_upload_bytes: z.unknown().optional(),
        max_upload_interval_seconds: z.unknown().optional(),
        max_upload_records: z.unknown().optional(),
        name: z.unknown().optional(),
        output_options: z.unknown().optional(),
        ownership_challenge: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/logpush/jobs`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "post_accounts_account_id_logpush_jobs",
          id,
          result,
        );
        context.logger.info(
          "Created post_accounts_account_id_logpush_jobs {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    get_accounts_account_id_logpush_jobs_job_id: {
      description: "Get Logpush job details",
      arguments: z.object({
        job_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/logpush/jobs/${args.job_id}`,
        );

        const handle = await context.writeResource(
          "accounts_account_id_logpush_jobs_job_id",
          String(args.job_id),
          result,
        );
        context.logger.info(
          "Fetched accounts_account_id_logpush_jobs_job_id",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    put_accounts_account_id_logpush_jobs_job_id: {
      description: "Update Logpush job",
      arguments: z.object({
        job_id: z.string(),
        destination_conf: z.unknown().optional(),
        enabled: z.unknown().optional(),
        filter: z.unknown().optional(),
        frequency: z.unknown().optional(),
        kind: z.unknown().optional(),
        logpull_options: z.unknown().optional(),
        max_upload_bytes: z.unknown().optional(),
        max_upload_interval_seconds: z.unknown().optional(),
        max_upload_records: z.unknown().optional(),
        name: z.unknown().optional(),
        output_options: z.unknown().optional(),
        ownership_challenge: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/logpush/jobs/${args.job_id}`,
          args,
        );

        const handle = await context.writeResource(
          "put_accounts_account_id_logpush_jobs_job_id",
          String(args.job_id),
          result,
        );
        context.logger.info(
          "Updated put_accounts_account_id_logpush_jobs_job_id",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    delete_accounts_account_id_logpush_jobs_job_id: {
      description: "Delete Logpush job",
      arguments: z.object({
        job_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/logpush/jobs/${args.job_id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.job_id });
        return { dataHandles: [] };
      },
    },
    create_post_accounts_account_id_logpush_ownership: {
      description: "Get ownership challenge",
      arguments: z.object({
        destination_conf: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/logpush/ownership`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "post_accounts_account_id_logpush_ownership",
          id,
          result,
        );
        context.logger.info(
          "Created post_accounts_account_id_logpush_ownership {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    create_post_accounts_account_id_logpush_ownership_validate: {
      description: "Validate ownership challenge",
      arguments: z.object({
        destination_conf: z.unknown(),
        ownership_challenge: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/logpush/ownership/validate`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "post_accounts_account_id_logpush_ownership_validate",
          id,
          result,
        );
        context.logger.info(
          "Created post_accounts_account_id_logpush_ownership_validate {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    delete_accounts_account_id_logpush_validate_destination: {
      description: "Validate destination",
      arguments: z.object({
        destination_conf: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/logpush/validate/destination`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "delete_accounts_account_id_logpush_validate_destination",
          id,
          result,
        );
        context.logger.info(
          "Created delete_accounts_account_id_logpush_validate_destination {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    delete_accounts_account_id_logpush_validate_destination_exists: {
      description: "Check destination exists",
      arguments: z.object({
        destination_conf: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/logpush/validate/destination/exists`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "delete_accounts_account_id_logpush_validate_destination_exists",
          id,
          result,
        );
        context.logger.info(
          "Created delete_accounts_account_id_logpush_validate_destination_exists {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    create_post_accounts_account_id_logpush_validate_origin: {
      description: "Validate origin",
      arguments: z.object({
        logpull_options: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/logpush/validate/origin`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "post_accounts_account_id_logpush_validate_origin",
          id,
          result,
        );
        context.logger.info(
          "Created post_accounts_account_id_logpush_validate_origin {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    get_zones_zone_id_logpush_datasets_dataset_id_fields: {
      description: "List fields",
      arguments: z.object({
        dataset_id: z.string(),
        zone_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/zones/${args.zone_id}/logpush/datasets/${args.dataset_id}/fields`,
        );

        const handle = await context.writeResource(
          "zones_zone_id_logpush_datasets_dataset_id_fields",
          String(args.zone_id),
          result,
        );
        context.logger.info(
          "Fetched zones_zone_id_logpush_datasets_dataset_id_fields",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    get_zones_zone_id_logpush_datasets_dataset_id_jobs: {
      description: "List Logpush jobs for a dataset",
      arguments: z.object({
        dataset_id: z.string(),
        zone_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        const params: Record<string, string> = {};
        const pathParams = new Set(["dataset_id", "zone_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !pathParams.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/zones/${args.zone_id}/logpush/datasets/${args.dataset_id}/jobs`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "get_zones_zone_id_logpush_datasets_dataset_id_jobs",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info(
          "Found {count} get_zones_zone_id_logpush_datasets_dataset_id_jobs",
          { count: results.length },
        );
        return { dataHandles: [handle] };
      },
    },
    get_zones_zone_id_logpush_edge_jobs: {
      description: "List Instant Logs jobs",
      arguments: z.object({
        zone_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        const params: Record<string, string> = {};
        const pathParams = new Set(["zone_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !pathParams.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/zones/${args.zone_id}/logpush/edge/jobs`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "get_zones_zone_id_logpush_edge_jobs",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info(
          "Found {count} get_zones_zone_id_logpush_edge_jobs",
          { count: results.length },
        );
        return { dataHandles: [handle] };
      },
    },
    create_post_zones_zone_id_logpush_edge_jobs: {
      description: "Create Instant Logs job",
      arguments: z.object({
        zone_id: z.string(),
        fields: z.unknown().optional(),
        filter: z.unknown().optional(),
        sample: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/zones/${args.zone_id}/logpush/edge/jobs`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "post_zones_zone_id_logpush_edge_jobs",
          id,
          result,
        );
        context.logger.info(
          "Created post_zones_zone_id_logpush_edge_jobs {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    get_zones_zone_id_logpush_jobs: {
      description: "List Logpush jobs",
      arguments: z.object({
        zone_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        const params: Record<string, string> = {};
        const pathParams = new Set(["zone_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !pathParams.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/zones/${args.zone_id}/logpush/jobs`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "get_zones_zone_id_logpush_jobs",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} get_zones_zone_id_logpush_jobs", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_post_zones_zone_id_logpush_jobs: {
      description: "Create Logpush job",
      arguments: z.object({
        zone_id: z.string(),
        dataset: z.unknown().optional(),
        destination_conf: z.unknown(),
        enabled: z.unknown().optional(),
        filter: z.unknown().optional(),
        frequency: z.unknown().optional(),
        kind: z.unknown().optional(),
        logpull_options: z.unknown().optional(),
        max_upload_bytes: z.unknown().optional(),
        max_upload_interval_seconds: z.unknown().optional(),
        max_upload_records: z.unknown().optional(),
        name: z.unknown().optional(),
        output_options: z.unknown().optional(),
        ownership_challenge: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/zones/${args.zone_id}/logpush/jobs`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "post_zones_zone_id_logpush_jobs",
          id,
          result,
        );
        context.logger.info("Created post_zones_zone_id_logpush_jobs {id}", {
          id,
        });
        return { dataHandles: [handle] };
      },
    },
    get_zones_zone_id_logpush_jobs_job_id: {
      description: "Get Logpush job details",
      arguments: z.object({
        job_id: z.string(),
        zone_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/zones/${args.zone_id}/logpush/jobs/${args.job_id}`,
        );

        const handle = await context.writeResource(
          "zones_zone_id_logpush_jobs_job_id",
          String(args.zone_id),
          result,
        );
        context.logger.info("Fetched zones_zone_id_logpush_jobs_job_id", {});
        return { dataHandles: [handle] };
      },
    },
    put_zones_zone_id_logpush_jobs_job_id: {
      description: "Update Logpush job",
      arguments: z.object({
        job_id: z.string(),
        zone_id: z.string(),
        destination_conf: z.unknown().optional(),
        enabled: z.unknown().optional(),
        filter: z.unknown().optional(),
        frequency: z.unknown().optional(),
        kind: z.unknown().optional(),
        logpull_options: z.unknown().optional(),
        max_upload_bytes: z.unknown().optional(),
        max_upload_interval_seconds: z.unknown().optional(),
        max_upload_records: z.unknown().optional(),
        name: z.unknown().optional(),
        output_options: z.unknown().optional(),
        ownership_challenge: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/zones/${args.zone_id}/logpush/jobs/${args.job_id}`,
          args,
        );

        const handle = await context.writeResource(
          "put_zones_zone_id_logpush_jobs_job_id",
          String(args.zone_id),
          result,
        );
        context.logger.info(
          "Updated put_zones_zone_id_logpush_jobs_job_id",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    delete_zones_zone_id_logpush_jobs_job_id: {
      description: "Delete Logpush job",
      arguments: z.object({
        job_id: z.string(),
        zone_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/zones/${args.zone_id}/logpush/jobs/${args.job_id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.zone_id });
        return { dataHandles: [] };
      },
    },
    create_post_zones_zone_id_logpush_ownership: {
      description: "Get ownership challenge",
      arguments: z.object({
        zone_id: z.string(),
        destination_conf: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/zones/${args.zone_id}/logpush/ownership`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "post_zones_zone_id_logpush_ownership",
          id,
          result,
        );
        context.logger.info(
          "Created post_zones_zone_id_logpush_ownership {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    create_post_zones_zone_id_logpush_ownership_validate: {
      description: "Validate ownership challenge",
      arguments: z.object({
        zone_id: z.string(),
        destination_conf: z.unknown(),
        ownership_challenge: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/zones/${args.zone_id}/logpush/ownership/validate`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "post_zones_zone_id_logpush_ownership_validate",
          id,
          result,
        );
        context.logger.info(
          "Created post_zones_zone_id_logpush_ownership_validate {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    create_post_zones_zone_id_logpush_validate_destination: {
      description: "Validate destination",
      arguments: z.object({
        zone_id: z.string(),
        destination_conf: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/zones/${args.zone_id}/logpush/validate/destination`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "post_zones_zone_id_logpush_validate_destination",
          id,
          result,
        );
        context.logger.info(
          "Created post_zones_zone_id_logpush_validate_destination {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    create_post_zones_zone_id_logpush_validate_destination_exists: {
      description: "Check destination exists",
      arguments: z.object({
        zone_id: z.string(),
        destination_conf: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/zones/${args.zone_id}/logpush/validate/destination/exists`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "post_zones_zone_id_logpush_validate_destination_exists",
          id,
          result,
        );
        context.logger.info(
          "Created post_zones_zone_id_logpush_validate_destination_exists {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    create_post_zones_zone_id_logpush_validate_origin: {
      description: "Validate origin",
      arguments: z.object({
        zone_id: z.string(),
        logpull_options: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/zones/${args.zone_id}/logpush/validate/origin`,
          args,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "post_zones_zone_id_logpush_validate_origin",
          id,
          result,
        );
        context.logger.info(
          "Created post_zones_zone_id_logpush_validate_origin {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
  },
};
