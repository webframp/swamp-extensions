/**
 * Cloudflare Registrar — domain registration, transfers, contacts
 *
 * Auto-generated by scripts/cloudflare-codegen — do not edit manually.
 *
 * @module
 */
// SPDX-License-Identifier: AGPL-3.0-or-later WITH Swamp-Extension-Exception

import { z } from "npm:zod@4.4.3";
import { cfApi, cfApiPaginated } from "./_lib/api.ts";

// =============================================================================
// Schemas
// =============================================================================

const GlobalArgsSchema = z.object({
  apiToken: z.string().meta({ sensitive: true }).describe(
    "Cloudflare API token",
  ),
  accountId: z.string().describe("Cloudflare account ID"),
});

const CreateSandboxRegistrarDomainDiscoveryCheckSchema = z.object({
  domains: z.array(z.unknown()).describe(
    "Array of domain availability results. Domains on unsupported extensions are included with `regist...",
  ),
});

const GetSandboxRegistrarDomainDiscoverySearchSchema = z.object({
  domains: z.array(z.unknown()).describe(
    "Array of domain suggestions sorted by relevance. May be empty if no domains match the search crit...",
  ),
});

const ListItemSchema = z.object({
  auto_renew: z.boolean().describe(
    "Whether the domain will be automatically renewed before expiration.",
  ),
  created_at: z.string().describe(
    "When the domain was registered. Present when the registration resource exists.",
  ),
  domain_name: z.unknown(),
  expires_at: z.string().nullable().describe(
    "When the domain registration expires. Present when the registration is ready; may be null only wh...",
  ),
  locked: z.boolean().describe("Whether the domain is locked for transfer."),
  privacy_mode: z.union([z.literal(false), z.literal("redaction")]).describe(
    "Current WHOIS privacy mode for the registration.",
  ),
  status: z.enum([
    "active",
    "registration_pending",
    "expired",
    "suspended",
    "redemption_period",
    "pending_delete",
  ]).describe(
    "Current registration status. - `active`: Domain is registered and operational - `registration_pen...",
  ),
});

const ListSchema = z.object({
  items: z.array(ListItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateSchema = z.object({
  completed: z.boolean().describe(
    "Whether the workflow has reached a terminal state. `true` when `state` is `succeeded` or `failed`...",
  ),
  context: z.record(z.string(), z.unknown()).optional().describe(
    "Workflow-specific data for this workflow. The workflow subject is identified by `context.domain_n...",
  ),
  created_at: z.string(),
  error: z.unknown().nullable().optional(),
  links: z.unknown(),
  state: z.enum([
    "pending",
    "in_progress",
    "action_required",
    "blocked",
    "succeeded",
    "failed",
  ]).describe(
    "Workflow lifecycle state. - `pending`: Workflow has been created but not yet started processing. ...",
  ),
  updated_at: z.string(),
});

const GetSchema = z.object({
  auto_renew: z.boolean().describe(
    "Whether the domain will be automatically renewed before expiration.",
  ),
  created_at: z.string().describe(
    "When the domain was registered. Present when the registration resource exists.",
  ),
  domain_name: z.unknown(),
  expires_at: z.string().nullable().describe(
    "When the domain registration expires. Present when the registration is ready; may be null only wh...",
  ),
  locked: z.boolean().describe("Whether the domain is locked for transfer."),
  privacy_mode: z.union([z.literal(false), z.literal("redaction")]).describe(
    "Current WHOIS privacy mode for the registration.",
  ),
  status: z.enum([
    "active",
    "registration_pending",
    "expired",
    "suspended",
    "redemption_period",
    "pending_delete",
  ]).describe(
    "Current registration status. - `active`: Domain is registered and operational - `registration_pen...",
  ),
});

const UpdateSchema = z.object({
  completed: z.boolean().describe(
    "Whether the workflow has reached a terminal state. `true` when `state` is `succeeded` or `failed`...",
  ),
  context: z.record(z.string(), z.unknown()).optional().describe(
    "Workflow-specific data for this workflow. The workflow subject is identified by `context.domain_n...",
  ),
  created_at: z.string(),
  error: z.unknown().nullable().optional(),
  links: z.unknown(),
  state: z.enum([
    "pending",
    "in_progress",
    "action_required",
    "blocked",
    "succeeded",
    "failed",
  ]).describe(
    "Workflow lifecycle state. - `pending`: Workflow has been created but not yet started processing. ...",
  ),
  updated_at: z.string(),
});

const GetStatusSchema = z.object({
  completed: z.boolean().describe(
    "Whether the workflow has reached a terminal state. `true` when `state` is `succeeded` or `failed`...",
  ),
  context: z.record(z.string(), z.unknown()).optional().describe(
    "Workflow-specific data for this workflow. The workflow subject is identified by `context.domain_n...",
  ),
  created_at: z.string(),
  error: z.unknown().nullable().optional(),
  links: z.unknown(),
  state: z.enum([
    "pending",
    "in_progress",
    "action_required",
    "blocked",
    "succeeded",
    "failed",
  ]).describe(
    "Workflow lifecycle state. - `pending`: Workflow has been created but not yet started processing. ...",
  ),
  updated_at: z.string(),
});

const GetUpdateStatusSchema = z.object({
  completed: z.boolean().describe(
    "Whether the workflow has reached a terminal state. `true` when `state` is `succeeded` or `failed`...",
  ),
  context: z.record(z.string(), z.unknown()).optional().describe(
    "Workflow-specific data for this workflow. The workflow subject is identified by `context.domain_n...",
  ),
  created_at: z.string(),
  error: z.unknown().nullable().optional(),
  links: z.unknown(),
  state: z.enum([
    "pending",
    "in_progress",
    "action_required",
    "blocked",
    "succeeded",
    "failed",
  ]).describe(
    "Workflow lifecycle state. - `pending`: Workflow has been created but not yet started processing. ...",
  ),
  updated_at: z.string(),
});

const CreateRegistrarDomainDiscoveryCheckSchema = z.object({
  domains: z.array(z.unknown()).describe(
    "Array of domain availability results. Domains on unsupported extensions are included with `regist...",
  ),
});

const GetRegistrarDomainDiscoverySearchSchema = z.object({
  domains: z.array(z.unknown()).describe(
    "Array of domain suggestions sorted by relevance. May be empty if no domains match the search crit...",
  ),
});

// =============================================================================
// Model Definition
// =============================================================================

/** Cloudflare Registrar — domain registration, transfers, contacts */
export const model = {
  type: "@webframp/cloudflare/registrar",
  version: "2026.07.19.1",
  globalArguments: GlobalArgsSchema,

  upgrades: [],

  resources: {
    "sandbox_registrar_domain_discovery_check": {
      description: "Check domain availability",
      schema: CreateSandboxRegistrarDomainDiscoveryCheckSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "sandbox_registrar_domain_discovery_search": {
      description: "Search for available domains",
      schema: GetSandboxRegistrarDomainDiscoverySearchSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "list": {
      description: "List Registrations",
      schema: ListSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "create": {
      description: "Create Registration",
      schema: CreateSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "get": {
      description: "Get Registration",
      schema: GetSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "update": {
      description: "Update Registration",
      schema: UpdateSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "status": {
      description: "Get Registration Status",
      schema: GetStatusSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "update_status": {
      description: "Get Update Status",
      schema: GetUpdateStatusSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "registrar_domain_discovery_check": {
      description: "Check domain availability",
      schema: CreateRegistrarDomainDiscoveryCheckSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "registrar_domain_discovery_search": {
      description: "Search for available domains",
      schema: GetRegistrarDomainDiscoverySearchSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
  },

  methods: {
    create_sandbox_registrar_domain_discovery_check: {
      description: "Check domain availability",
      arguments: z.object({
        domains: z.array(z.string()).describe(
          "List of fully qualified domain names (FQDNs) to check for availability. Each ...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/registrar-sandbox/domain-check`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "sandbox_registrar_domain_discovery_check",
          id,
          result,
        );
        context.logger.info(
          "Created sandbox_registrar_domain_discovery_check {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    get_sandbox_registrar_domain_discovery_search: {
      description: "Search for available domains",
      arguments: z.object({
        q: z.string().optional().describe(
          'The search term to find domain suggestions. Accepts keywords, phrases, or full domain names. - Phrases: "coffee shop" returns coffeeshop.com, mycoffeeshop.net, etc. - Domain names: "example.com" returns example.com and variations across extensions ',
        ),
        extensions: z.string().optional().describe(
          "Limits results to specific domain extensions from the supported set. If not specified, returns results across all supported extensions. Extensions not in the supported set are silently ignored. ",
        ),
        limit: z.number().optional().describe(
          "Maximum number of domain suggestions to return. Defaults to 20 if not specified. ",
        ),
      }),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/registrar-sandbox/domain-search`,
        );

        const handle = await context.writeResource(
          "sandbox_registrar_domain_discovery_search",
          "latest",
          result,
        );
        context.logger.info(
          "Fetched sandbox_registrar_domain_discovery_search",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    list: {
      description: "List Registrations",
      arguments: z.object({
        sort_by: z.enum(["registry_created_at", "registry_expires_at", "name"])
          .optional().describe(
            "Column to sort results by. Defaults to registration date (`registry_created_at`) when omitted. ",
          ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "per_page"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await cfApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/accounts/${accountId}/registrar-sandbox/registrations`,
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("list", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} list", { count: results.length });
        return { dataHandles: [handle] };
      },
    },
    create: {
      description: "Create Registration",
      arguments: z.object({
        auto_renew: z.boolean().optional().describe(
          "Enable or disable automatic renewal. Defaults to `false` if omitted. Setting ...",
        ),
        contacts: z.unknown().optional(),
        domain_name: z.unknown(),
        privacy_mode: z.union([z.literal(false), z.literal("redaction")])
          .optional().describe(
            "WHOIS privacy mode for the registration. Defaults to `redaction`. - `off`: Do...",
          ),
        years: z.number().int().min(1).max(10).optional().describe(
          "Number of years to register (1–10). If omitted, defaults to the minimum regis...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/registrar-sandbox/registrations`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource("create", id, result);
        context.logger.info("Created create {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get: {
      description: "Get Registration",
      arguments: z.object({
        domain_name: z.string().describe("Domain name to retrieve."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/registrar-sandbox/registrations/${args.domain_name}`,
        );

        const handle = await context.writeResource(
          "get",
          String(args.domain_name),
          result,
        );
        context.logger.info("Fetched get", {});
        return { dataHandles: [handle] };
      },
    },
    update: {
      description: "Update Registration",
      arguments: z.object({
        domain_name: z.string().describe("Domain name to update."),
        auto_renew: z.boolean().optional().describe(
          "Enable or disable automatic renewal. Setting this field to `true` authorizes ...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const pathKeys = new Set(["domain_name"]);
        for (const [k, v] of Object.entries(args)) {
          if (!pathKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/accounts/${accountId}/registrar-sandbox/registrations/${args.domain_name}`,
          body,
        );

        const handle = await context.writeResource(
          "update",
          String(args.domain_name),
          result,
        );
        context.logger.info("Updated update", {});
        return { dataHandles: [handle] };
      },
    },
    get_status: {
      description: "Get Registration Status",
      arguments: z.object({
        domain_name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/registrar-sandbox/registrations/${args.domain_name}/registration-status`,
        );

        const handle = await context.writeResource(
          "status",
          String(args.domain_name),
          result,
        );
        context.logger.info("Fetched status", {});
        return { dataHandles: [handle] };
      },
    },
    get_update_status: {
      description: "Get Update Status",
      arguments: z.object({
        domain_name: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/registrar-sandbox/registrations/${args.domain_name}/update-status`,
        );

        const handle = await context.writeResource(
          "update_status",
          String(args.domain_name),
          result,
        );
        context.logger.info("Fetched update_status", {});
        return { dataHandles: [handle] };
      },
    },
    create_registrar_domain_discovery_check: {
      description: "Check domain availability",
      arguments: z.object({
        domains: z.array(z.string()).describe(
          "List of fully qualified domain names (FQDNs) to check for availability. Each ...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body = args;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/registrar/domain-check`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "registrar_domain_discovery_check",
          id,
          result,
        );
        context.logger.info("Created registrar_domain_discovery_check {id}", {
          id,
        });
        return { dataHandles: [handle] };
      },
    },
    get_registrar_domain_discovery_search: {
      description: "Search for available domains",
      arguments: z.object({
        q: z.string().optional().describe(
          'The search term to find domain suggestions. Accepts keywords, phrases, or full domain names. - Phrases: "coffee shop" returns coffeeshop.com, mycoffeeshop.net, etc. - Domain names: "example.com" returns example.com and variations across extensions ',
        ),
        extensions: z.string().optional().describe(
          "Limits results to specific domain extensions from the supported set. If not specified, returns results across all supported extensions. Extensions not in the supported set are silently ignored. ",
        ),
        limit: z.number().optional().describe(
          "Maximum number of domain suggestions to return. Defaults to 20 if not specified. ",
        ),
      }),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/registrar/domain-search`,
        );

        const handle = await context.writeResource(
          "registrar_domain_discovery_search",
          "latest",
          result,
        );
        context.logger.info("Fetched registrar_domain_discovery_search", {});
        return { dataHandles: [handle] };
      },
    },
  },
};
