/**
 * Cloudflare Rulesets — WAF custom rules, transform rules, managed rulesets
 *
 * Auto-generated by scripts/cloudflare-codegen — do not edit manually.
 *
 * @module
 */
// SPDX-License-Identifier: AGPL-3.0-or-later WITH Swamp-Extension-Exception

import { z } from "npm:zod@4.4.3";
import { cfApi } from "./_lib/api.ts";

// =============================================================================
// Schemas
// =============================================================================

const GlobalArgsSchema = z.object({
  apiToken: z.string().meta({ sensitive: true }).describe(
    "Cloudflare API token",
  ),
  accountId: z.string().describe("Cloudflare account ID"),
});

// =============================================================================
// Model Definition
// =============================================================================

/** Cloudflare Rulesets — WAF custom rules, transform rules, managed rulesets */
export const model = {
  type: "@webframp/cloudflare/rulesets",
  version: "2026.07.19.1",
  globalArguments: GlobalArgsSchema,

  upgrades: [],

  resources: {
    "listaccountrulesets": {
      description: "List account rulesets",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "createaccountruleset": {
      description: "Create an account ruleset",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "getaccountentrypointruleset": {
      description: "Get an account entry point ruleset",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "updateaccountentrypointruleset": {
      description: "Update an account entry point ruleset",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "listaccountentrypointrulesetversions": {
      description: "List an account entry point ruleset's versions",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "getaccountentrypointrulesetversion": {
      description: "Get an account entry point ruleset version",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "getaccountruleset": {
      description: "Get an account ruleset",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "updateaccountruleset": {
      description: "Update an account ruleset",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "createaccountrulesetrule": {
      description: "Create an account ruleset rule",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "updateaccountrulesetrule": {
      description: "Update an account ruleset rule",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "listaccountrulesetversions": {
      description: "List an account ruleset's versions",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "getaccountrulesetversion": {
      description: "Get an account ruleset version",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "listaccountrulesetversionrulesbytag": {
      description: "List an account ruleset version's rules by tag",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "listzonerulesets": {
      description: "List zone rulesets",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "createzoneruleset": {
      description: "Create a zone ruleset",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "getzoneentrypointruleset": {
      description: "Get a zone entry point ruleset",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "updatezoneentrypointruleset": {
      description: "Update a zone entry point ruleset",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "listzoneentrypointrulesetversions": {
      description: "List a zone entry point ruleset's versions",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "getzoneentrypointrulesetversion": {
      description: "Get a zone entry point ruleset version",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "getzoneruleset": {
      description: "Get a zone ruleset",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "updatezoneruleset": {
      description: "Update a zone ruleset",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "createzonerulesetrule": {
      description: "Create a zone ruleset rule",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "updatezonerulesetrule": {
      description: "Update a zone ruleset rule",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "listzonerulesetversions": {
      description: "List a zone ruleset's versions",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "getzonerulesetversion": {
      description: "Get a zone ruleset version",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "listzonerulesetversionrulesbytag": {
      description: "List a zone ruleset version's rules by tag",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
  },

  methods: {
    listaccountrulesets: {
      description: "List account rulesets",
      arguments: z.object({
        cursor: z.string().optional(),
        per_page: z.string().optional(),
      }),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/rulesets`,
        );

        const handle = await context.writeResource(
          "listaccountrulesets",
          "latest",
          result,
        );
        context.logger.info("Fetched listaccountrulesets", {});
        return { dataHandles: [handle] };
      },
    },
    createaccountruleset: {
      description: "Create an account ruleset",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/rulesets`,
        );

        const handle = await context.writeResource(
          "createaccountruleset",
          "latest",
          result ?? {},
        );
        context.logger.info("Executed createaccountruleset", {});
        return { dataHandles: [handle] };
      },
    },
    getaccountentrypointruleset: {
      description: "Get an account entry point ruleset",
      arguments: z.object({
        ruleset_phase: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/rulesets/phases/${args.ruleset_phase}/entrypoint`,
        );

        const handle = await context.writeResource(
          "getaccountentrypointruleset",
          String(args.ruleset_phase),
          result,
        );
        context.logger.info("Fetched getaccountentrypointruleset", {});
        return { dataHandles: [handle] };
      },
    },
    updateaccountentrypointruleset: {
      description: "Update an account entry point ruleset",
      arguments: z.object({
        ruleset_phase: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["ruleset_phase"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/rulesets/phases/${args.ruleset_phase}/entrypoint`,
          body,
        );

        const handle = await context.writeResource(
          "updateaccountentrypointruleset",
          String(args.ruleset_phase),
          result,
        );
        context.logger.info("Updated updateaccountentrypointruleset", {});
        return { dataHandles: [handle] };
      },
    },
    listaccountentrypointrulesetversions: {
      description: "List an account entry point ruleset's versions",
      arguments: z.object({
        ruleset_phase: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/rulesets/phases/${args.ruleset_phase}/entrypoint/versions`,
        );

        const handle = await context.writeResource(
          "listaccountentrypointrulesetversions",
          String(args.ruleset_phase),
          result,
        );
        context.logger.info("Fetched listaccountentrypointrulesetversions", {});
        return { dataHandles: [handle] };
      },
    },
    getaccountentrypointrulesetversion: {
      description: "Get an account entry point ruleset version",
      arguments: z.object({
        ruleset_version: z.string(),
        ruleset_phase: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/rulesets/phases/${args.ruleset_phase}/entrypoint/versions/${args.ruleset_version}`,
        );

        const handle = await context.writeResource(
          "getaccountentrypointrulesetversion",
          String(args.ruleset_phase),
          result,
        );
        context.logger.info("Fetched getaccountentrypointrulesetversion", {});
        return { dataHandles: [handle] };
      },
    },
    getaccountruleset: {
      description: "Get an account ruleset",
      arguments: z.object({
        ruleset_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/rulesets/${args.ruleset_id}`,
        );

        const handle = await context.writeResource(
          "getaccountruleset",
          String(args.ruleset_id),
          result,
        );
        context.logger.info("Fetched getaccountruleset", {});
        return { dataHandles: [handle] };
      },
    },
    updateaccountruleset: {
      description: "Update an account ruleset",
      arguments: z.object({
        ruleset_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["ruleset_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/accounts/${accountId}/rulesets/${args.ruleset_id}`,
          body,
        );

        const handle = await context.writeResource(
          "updateaccountruleset",
          String(args.ruleset_id),
          result,
        );
        context.logger.info("Updated updateaccountruleset", {});
        return { dataHandles: [handle] };
      },
    },
    deleteaccountruleset: {
      description: "Delete an account ruleset",
      arguments: z.object({
        ruleset_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/rulesets/${args.ruleset_id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.ruleset_id });
        return { dataHandles: [] };
      },
    },
    createaccountrulesetrule: {
      description: "Create an account ruleset rule",
      arguments: z.object({
        ruleset_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/accounts/${accountId}/rulesets/${args.ruleset_id}/rules`,
        );

        const handle = await context.writeResource(
          "createaccountrulesetrule",
          "latest",
          result ?? {},
        );
        context.logger.info("Executed createaccountrulesetrule", {});
        return { dataHandles: [handle] };
      },
    },
    updateaccountrulesetrule: {
      description: "Update an account ruleset rule",
      arguments: z.object({
        rule_id: z.string(),
        ruleset_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["rule_id", "ruleset_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/accounts/${accountId}/rulesets/${args.ruleset_id}/rules/${args.rule_id}`,
          body,
        );

        const handle = await context.writeResource(
          "updateaccountrulesetrule",
          String(args.ruleset_id),
          result,
        );
        context.logger.info("Updated updateaccountrulesetrule", {});
        return { dataHandles: [handle] };
      },
    },
    deleteaccountrulesetrule: {
      description: "Delete an account ruleset rule",
      arguments: z.object({
        rule_id: z.string(),
        ruleset_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/rulesets/${args.ruleset_id}/rules/${args.rule_id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.ruleset_id });
        return { dataHandles: [] };
      },
    },
    listaccountrulesetversions: {
      description: "List an account ruleset's versions",
      arguments: z.object({
        ruleset_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/rulesets/${args.ruleset_id}/versions`,
        );

        const handle = await context.writeResource(
          "listaccountrulesetversions",
          String(args.ruleset_id),
          result,
        );
        context.logger.info("Fetched listaccountrulesetversions", {});
        return { dataHandles: [handle] };
      },
    },
    getaccountrulesetversion: {
      description: "Get an account ruleset version",
      arguments: z.object({
        ruleset_version: z.string(),
        ruleset_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/rulesets/${args.ruleset_id}/versions/${args.ruleset_version}`,
        );

        const handle = await context.writeResource(
          "getaccountrulesetversion",
          String(args.ruleset_id),
          result,
        );
        context.logger.info("Fetched getaccountrulesetversion", {});
        return { dataHandles: [handle] };
      },
    },
    deleteaccountrulesetversion: {
      description: "Delete an account ruleset version",
      arguments: z.object({
        ruleset_version: z.string(),
        ruleset_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/accounts/${accountId}/rulesets/${args.ruleset_id}/versions/${args.ruleset_version}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.ruleset_id });
        return { dataHandles: [] };
      },
    },
    listaccountrulesetversionrulesbytag: {
      description: "List an account ruleset version's rules by tag",
      arguments: z.object({
        rule_tag: z.string(),
        ruleset_version: z.string(),
        ruleset_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken, accountId } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/accounts/${accountId}/rulesets/${args.ruleset_id}/versions/${args.ruleset_version}/by_tag/${args.rule_tag}`,
        );

        const handle = await context.writeResource(
          "listaccountrulesetversionrulesbytag",
          String(args.ruleset_id),
          result,
        );
        context.logger.info("Fetched listaccountrulesetversionrulesbytag", {});
        return { dataHandles: [handle] };
      },
    },
    listzonerulesets: {
      description: "List zone rulesets",
      arguments: z.object({
        zone_id: z.string(),
        cursor: z.string().optional(),
        per_page: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/zones/${args.zone_id}/rulesets`,
        );

        const handle = await context.writeResource(
          "listzonerulesets",
          String(args.zone_id),
          result,
        );
        context.logger.info("Fetched listzonerulesets", {});
        return { dataHandles: [handle] };
      },
    },
    createzoneruleset: {
      description: "Create a zone ruleset",
      arguments: z.object({
        zone_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/zones/${args.zone_id}/rulesets`,
        );

        const handle = await context.writeResource(
          "createzoneruleset",
          "latest",
          result ?? {},
        );
        context.logger.info("Executed createzoneruleset", {});
        return { dataHandles: [handle] };
      },
    },
    getzoneentrypointruleset: {
      description: "Get a zone entry point ruleset",
      arguments: z.object({
        ruleset_phase: z.string(),
        zone_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/zones/${args.zone_id}/rulesets/phases/${args.ruleset_phase}/entrypoint`,
        );

        const handle = await context.writeResource(
          "getzoneentrypointruleset",
          String(args.zone_id),
          result,
        );
        context.logger.info("Fetched getzoneentrypointruleset", {});
        return { dataHandles: [handle] };
      },
    },
    updatezoneentrypointruleset: {
      description: "Update a zone entry point ruleset",
      arguments: z.object({
        ruleset_phase: z.string(),
        zone_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["ruleset_phase", "zone_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/zones/${args.zone_id}/rulesets/phases/${args.ruleset_phase}/entrypoint`,
          body,
        );

        const handle = await context.writeResource(
          "updatezoneentrypointruleset",
          String(args.zone_id),
          result,
        );
        context.logger.info("Updated updatezoneentrypointruleset", {});
        return { dataHandles: [handle] };
      },
    },
    listzoneentrypointrulesetversions: {
      description: "List a zone entry point ruleset's versions",
      arguments: z.object({
        ruleset_phase: z.string(),
        zone_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/zones/${args.zone_id}/rulesets/phases/${args.ruleset_phase}/entrypoint/versions`,
        );

        const handle = await context.writeResource(
          "listzoneentrypointrulesetversions",
          String(args.zone_id),
          result,
        );
        context.logger.info("Fetched listzoneentrypointrulesetversions", {});
        return { dataHandles: [handle] };
      },
    },
    getzoneentrypointrulesetversion: {
      description: "Get a zone entry point ruleset version",
      arguments: z.object({
        ruleset_version: z.string(),
        ruleset_phase: z.string(),
        zone_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/zones/${args.zone_id}/rulesets/phases/${args.ruleset_phase}/entrypoint/versions/${args.ruleset_version}`,
        );

        const handle = await context.writeResource(
          "getzoneentrypointrulesetversion",
          String(args.zone_id),
          result,
        );
        context.logger.info("Fetched getzoneentrypointrulesetversion", {});
        return { dataHandles: [handle] };
      },
    },
    getzoneruleset: {
      description: "Get a zone ruleset",
      arguments: z.object({
        ruleset_id: z.string(),
        zone_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/zones/${args.zone_id}/rulesets/${args.ruleset_id}`,
        );

        const handle = await context.writeResource(
          "getzoneruleset",
          String(args.zone_id),
          result,
        );
        context.logger.info("Fetched getzoneruleset", {});
        return { dataHandles: [handle] };
      },
    },
    updatezoneruleset: {
      description: "Update a zone ruleset",
      arguments: z.object({
        ruleset_id: z.string(),
        zone_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["ruleset_id", "zone_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/zones/${args.zone_id}/rulesets/${args.ruleset_id}`,
          body,
        );

        const handle = await context.writeResource(
          "updatezoneruleset",
          String(args.zone_id),
          result,
        );
        context.logger.info("Updated updatezoneruleset", {});
        return { dataHandles: [handle] };
      },
    },
    deletezoneruleset: {
      description: "Delete a zone ruleset",
      arguments: z.object({
        ruleset_id: z.string(),
        zone_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/zones/${args.zone_id}/rulesets/${args.ruleset_id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.zone_id });
        return { dataHandles: [] };
      },
    },
    createzonerulesetrule: {
      description: "Create a zone ruleset rule",
      arguments: z.object({
        ruleset_id: z.string(),
        zone_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/zones/${args.zone_id}/rulesets/${args.ruleset_id}/rules`,
        );

        const handle = await context.writeResource(
          "createzonerulesetrule",
          "latest",
          result ?? {},
        );
        context.logger.info("Executed createzonerulesetrule", {});
        return { dataHandles: [handle] };
      },
    },
    updatezonerulesetrule: {
      description: "Update a zone ruleset rule",
      arguments: z.object({
        rule_id: z.string(),
        ruleset_id: z.string(),
        zone_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["rule_id", "ruleset_id", "zone_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/zones/${args.zone_id}/rulesets/${args.ruleset_id}/rules/${args.rule_id}`,
          body,
        );

        const handle = await context.writeResource(
          "updatezonerulesetrule",
          String(args.zone_id),
          result,
        );
        context.logger.info("Updated updatezonerulesetrule", {});
        return { dataHandles: [handle] };
      },
    },
    deletezonerulesetrule: {
      description: "Delete a zone ruleset rule",
      arguments: z.object({
        rule_id: z.string(),
        ruleset_id: z.string(),
        zone_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/zones/${args.zone_id}/rulesets/${args.ruleset_id}/rules/${args.rule_id}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.zone_id });
        return { dataHandles: [] };
      },
    },
    listzonerulesetversions: {
      description: "List a zone ruleset's versions",
      arguments: z.object({
        ruleset_id: z.string(),
        zone_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/zones/${args.zone_id}/rulesets/${args.ruleset_id}/versions`,
        );

        const handle = await context.writeResource(
          "listzonerulesetversions",
          String(args.zone_id),
          result,
        );
        context.logger.info("Fetched listzonerulesetversions", {});
        return { dataHandles: [handle] };
      },
    },
    getzonerulesetversion: {
      description: "Get a zone ruleset version",
      arguments: z.object({
        ruleset_version: z.string(),
        ruleset_id: z.string(),
        zone_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/zones/${args.zone_id}/rulesets/${args.ruleset_id}/versions/${args.ruleset_version}`,
        );

        const handle = await context.writeResource(
          "getzonerulesetversion",
          String(args.zone_id),
          result,
        );
        context.logger.info("Fetched getzonerulesetversion", {});
        return { dataHandles: [handle] };
      },
    },
    deletezonerulesetversion: {
      description: "Delete a zone ruleset version",
      arguments: z.object({
        ruleset_version: z.string(),
        ruleset_id: z.string(),
        zone_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        await cfApi(
          apiToken,
          "DELETE",
          `/zones/${args.zone_id}/rulesets/${args.ruleset_id}/versions/${args.ruleset_version}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.zone_id });
        return { dataHandles: [] };
      },
    },
    listzonerulesetversionrulesbytag: {
      description: "List a zone ruleset version's rules by tag",
      arguments: z.object({
        rule_tag: z.string(),
        ruleset_version: z.string(),
        ruleset_id: z.string(),
        zone_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        const result = await cfApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/zones/${args.zone_id}/rulesets/${args.ruleset_id}/versions/${args.ruleset_version}/by_tag/${args.rule_tag}`,
        );

        const handle = await context.writeResource(
          "listzonerulesetversionrulesbytag",
          String(args.zone_id),
          result,
        );
        context.logger.info("Fetched listzonerulesetversionrulesbytag", {});
        return { dataHandles: [handle] };
      },
    },
  },
};
