// Datadog On-Call — on-call schedules, escalation policies, and routing — Tests
// Auto-generated by scripts/datadog-codegen — do not edit manually.
// SPDX-License-Identifier: Apache-2.0

import { assertEquals, assertExists } from "jsr:@std/assert@1";
import { createModelTestContext } from "@systeminit/swamp-testing";
import { model } from "./on_call.ts";

// ---------------------------------------------------------------------------
// Model Export Structure Tests
// ---------------------------------------------------------------------------

Deno.test("on-call model: has correct type", () => {
  assertEquals(model.type, "@webframp/datadog/on-call");
});

Deno.test("on-call model: has valid version format", () => {
  const versionPattern = /^\d{4}\.\d{2}\.\d{2}\.\d+$/;
  assertEquals(versionPattern.test(model.version), true);
});

Deno.test("on-call model: has globalArguments with apiKey and appKey", () => {
  assertExists(model.globalArguments);
  const shape = model.globalArguments.shape;
  assertExists(shape.apiKey);
  assertExists(shape.appKey);
  assertExists(shape.site);
});

Deno.test("on-call model: has expected methods", () => {
  assertExists(model.methods);
  assertExists(model.methods.create_on_call_escalation_policy);
  assertExists(model.methods.get_on_call_escalation_policy);
  assertExists(model.methods.update_on_call_escalation_policy);
  assertExists(model.methods.delete_on_call_escalation_policy);
  assertExists(model.methods.create_on_call_schedule);
  assertExists(model.methods.get_on_call_schedule);
  assertExists(model.methods.update_on_call_schedule);
  assertExists(model.methods.delete_on_call_schedule);
  assertExists(model.methods.get_schedule_on_call_responders);
  assertExists(model.methods.get_team_on_call_users);
  assertExists(model.methods.get_on_call_team_routing_rules);
  assertExists(model.methods.set_on_call_team_routing_rules);
  assertExists(model.methods.list_user_notification_channels);
  assertExists(model.methods.create_user_notification_channel);
  assertExists(model.methods.get_user_notification_channel);
  assertExists(model.methods.delete_user_notification_channel);
  assertExists(model.methods.list_user_notification_rules);
  assertExists(model.methods.create_user_notification_rule);
  assertExists(model.methods.get_user_notification_rule);
  assertExists(model.methods.update_user_notification_rule);
  assertExists(model.methods.delete_user_notification_rule);
});

Deno.test("on-call model: has expected resources", () => {
  assertExists(model.resources);
  assertExists(model.resources["on_call_escalation_policy"]);
  assertExists(model.resources["on_call_schedule"]);
  assertExists(model.resources["schedule_on_call_responders"]);
  assertExists(model.resources["team_on_call_users"]);
  assertExists(model.resources["on_call_team_routing_rules"]);
  assertExists(model.resources["set_on_call_team_routing_rules"]);
  assertExists(model.resources["user_notification_channels"]);
  assertExists(model.resources["user_notification_channel"]);
  assertExists(model.resources["user_notification_rules"]);
  assertExists(model.resources["user_notification_rule"]);
});

// ---------------------------------------------------------------------------
// Mock Datadog API Server
// ---------------------------------------------------------------------------

function startMockDdServer(
  responses: Record<string, { body: unknown; status?: number }>,
): { url: string; server: Deno.HttpServer } {
  const server = Deno.serve({ port: 0, onListen() {} }, (req) => {
    const url = new URL(req.url);
    const path = url.pathname;

    for (const [pattern, { body, status }] of Object.entries(responses)) {
      if (path.includes(pattern)) {
        return Response.json(body, { status: status ?? 200 });
      }
    }

    return Response.json(
      { errors: [{ status: "404", title: "Not found" }] },
      { status: 404 },
    );
  });

  const addr = server.addr as Deno.NetAddr;
  return { url: `http://localhost:${addr.port}`, server };
}

function installFetchMock(mockUrl: string): () => void {
  const originalFetch = globalThis.fetch;
  globalThis.fetch = (input, init) => {
    const reqUrl = typeof input === "string" ? input : input.toString();
    // Replace any DD site URL with the mock
    const newUrl = reqUrl
      .replace("https://api.datadoghq.com", mockUrl)
      .replace("https://us3.datadoghq.com", mockUrl)
      .replace("https://us5.datadoghq.com", mockUrl)
      .replace("https://api.datadoghq.eu", mockUrl)
      .replace("https://ap1.datadoghq.com", mockUrl)
      .replace("https://api.ddog-gov.com", mockUrl);
    return originalFetch(newUrl, init);
  };
  return () => {
    globalThis.fetch = originalFetch;
  };
}

Deno.test({
  name:
    "on-call model: create_on_call_escalation_policy creates and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockDdServer({
      "/on-call/escalation-policies": {
        body: {
          "data": {
            "id": "new-123",
            "type": "resource",
            "attributes": {
              "name": "On-Call Escalation Policy",
              "resolve_page_on_policy_end": true,
              "retries": 0,
              "steps_id": "test-value",
              "teams_id": "test-value",
            },
          },
        },
      },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: {
          "apiKey": "test-api-key",
          "appKey": "test-app-key",
          "site": "us1",
        },
        definition: {
          id: "test-id",
          name: "test-on-call",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).create_on_call_escalation_policy.execute(
        { "name": "test-resource" },
        context,
      );
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name:
    "on-call model: get_on_call_escalation_policy fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockDdServer({
      "/escalation-policies/test-id-123": {
        body: {
          "data": {
            "id": "fixture-123",
            "type": "resource",
            "attributes": {
              "name": "On-Call Escalation Policy",
              "resolve_page_on_policy_end": true,
              "retries": 0,
              "steps_id": "test-value",
              "teams_id": "test-value",
            },
          },
        },
      },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: {
          "apiKey": "test-api-key",
          "appKey": "test-app-key",
          "site": "us1",
        },
        definition: {
          id: "test-id",
          name: "test-on-call",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).get_on_call_escalation_policy.execute(
        { "policy_id": "test-id-123" },
        context,
      );
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name:
    "on-call model: update_on_call_escalation_policy executes and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockDdServer({
      "/escalation-policies/test-id-123": {
        body: {
          "data": {
            "id": "fixture-123",
            "type": "resource",
            "attributes": {
              "name": "On-Call Escalation Policy",
              "resolve_page_on_policy_end": true,
              "retries": 0,
              "steps_id": "test-value",
              "teams_id": "test-value",
            },
          },
        },
      },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: {
          "apiKey": "test-api-key",
          "appKey": "test-app-key",
          "site": "us1",
        },
        definition: {
          id: "test-id",
          name: "test-on-call",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).update_on_call_escalation_policy.execute({
        "policy_id": "test-id-123",
        "name": "test-resource",
      }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "on-call model: delete_on_call_escalation_policy executes successfully",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockDdServer({
      "/escalation-policies/test-id-123": { body: {}, status: 204 },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context } = createModelTestContext({
        globalArgs: {
          "apiKey": "test-api-key",
          "appKey": "test-app-key",
          "site": "us1",
        },
        definition: {
          id: "test-id",
          name: "test-on-call",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).delete_on_call_escalation_policy.execute({
        "policy_id": "test-id-123",
      }, context);
      assertEquals(result.dataHandles.length, 0);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name:
    "on-call model: list_user_notification_channels fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockDdServer({
      "/test-id-123/notification-channels": {
        body: {
          "data": [{
            "id": "fixture-123",
            "type": "resource",
            "attributes": { "active": true, "config": null },
          }],
          "meta": { "page": {} },
        },
      },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: {
          "apiKey": "test-api-key",
          "appKey": "test-app-key",
          "site": "us1",
        },
        definition: {
          id: "test-id",
          name: "test-on-call",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).list_user_notification_channels.execute(
        { "user_id": "test-id-123" },
        context,
      );
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});
