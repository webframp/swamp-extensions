/**
 * Datadog Security Signals — signal search, triage, and archiving
 *
 * Auto-generated by scripts/datadog-codegen — do not edit manually.
 *
 * @module
 */
// SPDX-License-Identifier: Apache-2.0

import { z } from "npm:zod@4.4.3";
import {
  ddApi,
  ddApiPaginated,
  ddApiPostPaginated,
  sanitizeInstanceName,
} from "./_lib/api.ts";

const EXTENSION_NAME = "@webframp/datadog/security-signals";

// =============================================================================
// Schemas
// =============================================================================

const GlobalArgsSchema = z.object({
  apiKey: z.string().meta({ sensitive: true }).describe(
    "Datadog API key (DD-API-KEY)",
  ),
  appKey: z.string().meta({ sensitive: true }).describe(
    "Datadog application key (DD-APPLICATION-KEY)",
  ),
  site: z.enum(["us1", "us3", "us5", "eu1", "ap1", "us1-fed"]).default("us1")
    .describe("Datadog site"),
});

const SecurityMonitoringSignalsItemSchema = z.object({
  id: z.string().describe("The unique ID of the security signal."),
  type: z.enum(["signal"]).optional().default("signal").describe(
    "The type of event.",
  ),
  custom: z.record(z.string(), z.unknown()).optional().describe(
    "A JSON object of attributes in the security signal.",
  ),
  message: z.string().optional().describe(
    "The message in the security signal defined by the rule that generated the signal.",
  ),
  tags: z.array(z.string()).optional().describe(
    "An array of tags associated with the security signal.",
  ),
  timestamp: z.string().optional().describe(
    "The timestamp of the security signal.",
  ),
}).passthrough();

const ListSecurityMonitoringSignalsSchema = z.object({
  items: z.array(SecurityMonitoringSignalsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const BulkEditSecurityMonitoringSignalsAssigneeSchema = z.object({
  result: z.unknown(),
  status: z.string().describe("The status of the bulk operation."),
  type: z.string().describe("The type of the response."),
}).passthrough();

const BulkEditSecurityMonitoringSignalsStateSchema = z.object({
  result: z.unknown(),
  status: z.string().describe("The status of the bulk operation."),
  type: z.string().describe("The type of the response."),
}).passthrough();

const BulkEditSecurityMonitoringSignalsSchema = z.object({
  result: z.unknown(),
  status: z.string().describe("The status of the bulk operation."),
  type: z.string().describe("The type of the response."),
}).passthrough();

const SearchSecurityMonitoringSignalsItemSchema = z.object({
  id: z.string().describe("The unique ID of the security signal."),
  type: z.enum(["signal"]).optional().default("signal").describe(
    "The type of event.",
  ),
  custom: z.record(z.string(), z.unknown()).optional().describe(
    "A JSON object of attributes in the security signal.",
  ),
  message: z.string().optional().describe(
    "The message in the security signal defined by the rule that generated the signal.",
  ),
  tags: z.array(z.string()).optional().describe(
    "An array of tags associated with the security signal.",
  ),
  timestamp: z.string().optional().describe(
    "The timestamp of the security signal.",
  ),
}).passthrough();

const SearchSecurityMonitoringSignalsSchema = z.object({
  items: z.array(SearchSecurityMonitoringSignalsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const GetSecurityMonitoringSignalSchema = z.object({
  id: z.string().describe("The unique ID of the security signal."),
  type: z.enum(["signal"]).optional().default("signal").describe(
    "The type of event.",
  ),
  custom: z.record(z.string(), z.unknown()).optional().describe(
    "A JSON object of attributes in the security signal.",
  ),
  message: z.string().optional().describe(
    "The message in the security signal defined by the rule that generated the signal.",
  ),
  tags: z.array(z.string()).optional().describe(
    "An array of tags associated with the security signal.",
  ),
  timestamp: z.string().optional().describe(
    "The timestamp of the security signal.",
  ),
}).passthrough();

const EditSecurityMonitoringSignalAssigneeSchema = z.object({
  id: z.string().describe("The unique ID of the security signal."),
  type: z.enum(["signal_metadata"]).optional().default("signal_metadata")
    .describe("The type of event."),
  archive_comment: z.string().optional().describe(
    "Optional comment to display on archived signals.",
  ),
  archive_comment_timestamp: z.number().int().min(0).optional().describe(
    "Timestamp of the last edit to the comment.",
  ),
  archive_comment_user: z.object({
    handle: z.string().optional(),
    icon: z.string().optional(),
    id: z.number().int().optional(),
    name: z.string().nullable().optional(),
    uuid: z.string(),
  }).optional().describe("Object representing a given user entity."),
  archive_reason: z.enum([
    "none",
    "false_positive",
    "testing_or_maintenance",
    "remediated",
    "investigated_case_opened",
    "true_positive_benign",
    "true_positive_malicious",
    "other",
  ]).optional().describe("Reason a signal is archived."),
  assignee: z.object({
    handle: z.string().optional(),
    icon: z.string().optional(),
    id: z.number().int().optional(),
    name: z.string().nullable().optional(),
    uuid: z.string(),
  }).describe("Object representing a given user entity."),
  incident_ids: z.array(z.number().int()).describe(
    "Array of incidents that are associated with this signal.",
  ),
  state: z.enum(["open", "archived", "under_review"]).describe(
    "The new triage state of the signal.",
  ),
  state_update_timestamp: z.number().int().min(0).optional().describe(
    "Timestamp of the last update to the signal state.",
  ),
  state_update_user: z.object({
    handle: z.string().optional(),
    icon: z.string().optional(),
    id: z.number().int().optional(),
    name: z.string().nullable().optional(),
    uuid: z.string(),
  }).optional().describe("Object representing a given user entity."),
}).passthrough();

const EditSecurityMonitoringSignalIncidentsSchema = z.object({
  id: z.string().describe("The unique ID of the security signal."),
  type: z.enum(["signal_metadata"]).optional().default("signal_metadata")
    .describe("The type of event."),
  archive_comment: z.string().optional().describe(
    "Optional comment to display on archived signals.",
  ),
  archive_comment_timestamp: z.number().int().min(0).optional().describe(
    "Timestamp of the last edit to the comment.",
  ),
  archive_comment_user: z.object({
    handle: z.string().optional(),
    icon: z.string().optional(),
    id: z.number().int().optional(),
    name: z.string().nullable().optional(),
    uuid: z.string(),
  }).optional().describe("Object representing a given user entity."),
  archive_reason: z.enum([
    "none",
    "false_positive",
    "testing_or_maintenance",
    "remediated",
    "investigated_case_opened",
    "true_positive_benign",
    "true_positive_malicious",
    "other",
  ]).optional().describe("Reason a signal is archived."),
  assignee: z.object({
    handle: z.string().optional(),
    icon: z.string().optional(),
    id: z.number().int().optional(),
    name: z.string().nullable().optional(),
    uuid: z.string(),
  }).describe("Object representing a given user entity."),
  incident_ids: z.array(z.number().int()).describe(
    "Array of incidents that are associated with this signal.",
  ),
  state: z.enum(["open", "archived", "under_review"]).describe(
    "The new triage state of the signal.",
  ),
  state_update_timestamp: z.number().int().min(0).optional().describe(
    "Timestamp of the last update to the signal state.",
  ),
  state_update_user: z.object({
    handle: z.string().optional(),
    icon: z.string().optional(),
    id: z.number().int().optional(),
    name: z.string().nullable().optional(),
    uuid: z.string(),
  }).optional().describe("Object representing a given user entity."),
}).passthrough();

const InvestigationLogQueriesMatchingSignalItemSchema = z.object({
  id: z.string().describe("The unique ID of the suggested action."),
  type: z.enum(["investigation_log_queries", "recommended_blog_posts"])
    .optional().describe("The type of the suggested action resource."),
  name: z.string().optional().describe(
    "The name of the investigation log query.",
  ),
  query_filter: z.string().optional().describe(
    "The log query filter for the investigation.",
  ),
  template_variables: z.record(z.string(), z.array(z.string())).optional()
    .describe(
      "Template variables applied to the investigation log query, mapping attribute paths to values extr...",
    ),
  title: z.string().optional().describe(
    "The title of the recommended blog post.",
  ),
  url: z.string().optional().describe("The URL of the suggested action."),
}).passthrough();

const GetInvestigationLogQueriesMatchingSignalSchema = z.object({
  items: z.array(InvestigationLogQueriesMatchingSignalItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const EditSecurityMonitoringSignalStateSchema = z.object({
  id: z.string().describe("The unique ID of the security signal."),
  type: z.enum(["signal_metadata"]).optional().default("signal_metadata")
    .describe("The type of event."),
  archive_comment: z.string().optional().describe(
    "Optional comment to display on archived signals.",
  ),
  archive_comment_timestamp: z.number().int().min(0).optional().describe(
    "Timestamp of the last edit to the comment.",
  ),
  archive_comment_user: z.object({
    handle: z.string().optional(),
    icon: z.string().optional(),
    id: z.number().int().optional(),
    name: z.string().nullable().optional(),
    uuid: z.string(),
  }).optional().describe("Object representing a given user entity."),
  archive_reason: z.enum([
    "none",
    "false_positive",
    "testing_or_maintenance",
    "remediated",
    "investigated_case_opened",
    "true_positive_benign",
    "true_positive_malicious",
    "other",
  ]).optional().describe("Reason a signal is archived."),
  assignee: z.object({
    handle: z.string().optional(),
    icon: z.string().optional(),
    id: z.number().int().optional(),
    name: z.string().nullable().optional(),
    uuid: z.string(),
  }).describe("Object representing a given user entity."),
  incident_ids: z.array(z.number().int()).describe(
    "Array of incidents that are associated with this signal.",
  ),
  state: z.enum(["open", "archived", "under_review"]).describe(
    "The new triage state of the signal.",
  ),
  state_update_timestamp: z.number().int().min(0).optional().describe(
    "Timestamp of the last update to the signal state.",
  ),
  state_update_user: z.object({
    handle: z.string().optional(),
    icon: z.string().optional(),
    id: z.number().int().optional(),
    name: z.string().nullable().optional(),
    uuid: z.string(),
  }).optional().describe("Object representing a given user entity."),
}).passthrough();

const SuggestedActionsMatchingSignalItemSchema = z.object({
  id: z.string().describe("The unique ID of the suggested action."),
  type: z.enum(["investigation_log_queries", "recommended_blog_posts"])
    .optional().describe("The type of the suggested action resource."),
  name: z.string().optional().describe(
    "The name of the investigation log query.",
  ),
  query_filter: z.string().optional().describe(
    "The log query filter for the investigation.",
  ),
  template_variables: z.record(z.string(), z.array(z.string())).optional()
    .describe(
      "Template variables applied to the investigation log query, mapping attribute paths to values extr...",
    ),
  title: z.string().optional().describe(
    "The title of the recommended blog post.",
  ),
  url: z.string().optional().describe("The URL of the suggested action."),
}).passthrough();

const GetSuggestedActionsMatchingSignalSchema = z.object({
  items: z.array(SuggestedActionsMatchingSignalItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const EditSecurityMonitoringSignalSchema = z.object({
  id: z.string().describe("The unique ID of the security signal."),
  type: z.enum(["signal_metadata"]).optional().default("signal_metadata")
    .describe("The type of event."),
  archive_comment: z.string().optional().describe(
    "Optional comment to display on archived signals.",
  ),
  archive_comment_timestamp: z.number().int().min(0).optional().describe(
    "Timestamp of the last edit to the comment.",
  ),
  archive_comment_user: z.object({
    handle: z.string().optional(),
    icon: z.string().optional(),
    id: z.number().int().optional(),
    name: z.string().nullable().optional(),
    uuid: z.string(),
  }).optional().describe("Object representing a given user entity."),
  archive_reason: z.enum([
    "none",
    "false_positive",
    "testing_or_maintenance",
    "remediated",
    "investigated_case_opened",
    "true_positive_benign",
    "true_positive_malicious",
    "other",
  ]).optional().describe("Reason a signal is archived."),
  assignee: z.object({
    handle: z.string().optional(),
    icon: z.string().optional(),
    id: z.number().int().optional(),
    name: z.string().nullable().optional(),
    uuid: z.string(),
  }).describe("Object representing a given user entity."),
  incident_ids: z.array(z.number().int()).describe(
    "Array of incidents that are associated with this signal.",
  ),
  state: z.enum(["open", "archived", "under_review"]).describe(
    "The new triage state of the signal.",
  ),
  state_update_timestamp: z.number().int().min(0).optional().describe(
    "Timestamp of the last update to the signal state.",
  ),
  state_update_user: z.object({
    handle: z.string().optional(),
    icon: z.string().optional(),
    id: z.number().int().optional(),
    name: z.string().nullable().optional(),
    uuid: z.string(),
  }).optional().describe("Object representing a given user entity."),
}).passthrough();

// =============================================================================
// Model Definition
// =============================================================================

/** Datadog Security Signals — signal search, triage, and archiving */
export const model = {
  type: "@webframp/datadog/security-signals",
  version: "2026.08.28.2",
  globalArguments: GlobalArgsSchema,

  upgrades: [
    {
      toVersion: "2026.08.24.1",
      description:
        "Added optional durationMs, collectedBy, and fetchedAt output metadata fields",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.08.25.1",
      description: "Label metadata update, no schema changes",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.08.26.1",
      description:
        "No schema changes — restored inline npm:zod specifier for registry scoring; retained strict mode",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.08.26.2",
      description:
        "No schema changes — restored inline npm:zod specifier for registry scoring; retained strict mode",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.08.28.1",
      description:
        "No schema changes — normalized license to Apache-2.0 and corrected copyright holder to Sean Escriva",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.08.28.2",
      description: "Regenerated from updated API spec; no migration required",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
  ],

  resources: {
    "security_monitoring_signals": {
      description: "Get a quick list of security signals",
      schema: ListSecurityMonitoringSignalsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "bulk_edit_security_monitoring_signals_assignee": {
      description: "Bulk update triage assignee of security signals",
      schema: BulkEditSecurityMonitoringSignalsAssigneeSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "bulk_edit_security_monitoring_signals_state": {
      description: "Bulk update triage state of security signals",
      schema: BulkEditSecurityMonitoringSignalsStateSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "bulk_edit_security_monitoring_signals": {
      description: "Bulk update security signals",
      schema: BulkEditSecurityMonitoringSignalsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "search_security_monitoring_signals": {
      description: "Get a list of security signals",
      schema: SearchSecurityMonitoringSignalsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "security_monitoring_signal": {
      description: "Get a signal's details",
      schema: GetSecurityMonitoringSignalSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "edit_security_monitoring_signal_assignee": {
      description: "Modify the triage assignee of a security signal",
      schema: EditSecurityMonitoringSignalAssigneeSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "edit_security_monitoring_signal_incidents": {
      description: "Change the related incidents of a security signal",
      schema: EditSecurityMonitoringSignalIncidentsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "investigation_log_queries_matching_signal": {
      description: "Get investigation queries for a signal",
      schema: GetInvestigationLogQueriesMatchingSignalSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "edit_security_monitoring_signal_state": {
      description: "Change the triage state of a security signal",
      schema: EditSecurityMonitoringSignalStateSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "suggested_actions_matching_signal": {
      description: "Get suggested actions for a signal",
      schema: GetSuggestedActionsMatchingSignalSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "edit_security_monitoring_signal": {
      description: "Update security signal triage state or assignee",
      schema: EditSecurityMonitoringSignalSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
  },

  methods: {
    list_security_monitoring_signals: {
      description: "Get a quick list of security signals",
      arguments: z.object({
        filter_query: z.string().optional().describe(
          "The search query for security signals.",
        ),
        filter_from: z.string().optional().describe(
          "The minimum timestamp for requested security signals.",
        ),
        filter_to: z.string().optional().describe(
          "The maximum timestamp for requested security signals.",
        ),
        sort: z.string().optional().describe(
          "The order of the security signals in results.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const startMs = Date.now();
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>([]);
        const paramNameMap: Record<string, string> = {
          "filter_query": "filter[query]",
          "filter_from": "filter[from]",
          "filter_to": "filter[to]",
        };
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) {
            const paramKey = paramNameMap[k] ?? k;
            params[paramKey] = String(v);
          }
        }

        const { results, truncated } = await ddApiPaginated(
          apiKey,
          appKey,
          site,
          `/api/v2/security_monitoring/signals`,
          {
            "style": "cursor",
            "limitParam": "page[limit]",
            "limitDefault": 100,
            "cursorParam": "page[cursor]",
            "cursorResponsePath": "meta.page.after",
          },
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "security_monitoring_signals",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );

        context.logger.info("Found {count} security_monitoring_signals", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    bulk_edit_security_monitoring_signals_assignee: {
      description: "Bulk update triage assignee of security signals",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const attrs: Record<string, unknown> = {};
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) attrs[k] = v;
        }
        const body = { data: { attributes: attrs } };

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "PATCH",
          `/api/v2/security_monitoring/signals/bulk/assignee`,
          body,
        );

        const handle = await context.writeResource(
          "bulk_edit_security_monitoring_signals_assignee",
          "updated",
          result,
        );
        context.logger.info(
          "Updated bulk_edit_security_monitoring_signals_assignee",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    bulk_edit_security_monitoring_signals_state: {
      description: "Bulk update triage state of security signals",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const attrs: Record<string, unknown> = {};
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) attrs[k] = v;
        }
        const body = { data: { attributes: attrs } };

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "PATCH",
          `/api/v2/security_monitoring/signals/bulk/state`,
          body,
        );

        const handle = await context.writeResource(
          "bulk_edit_security_monitoring_signals_state",
          "updated",
          result,
        );
        context.logger.info(
          "Updated bulk_edit_security_monitoring_signals_state",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    bulk_edit_security_monitoring_signals: {
      description: "Bulk update security signals",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const attrs: Record<string, unknown> = {};
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) attrs[k] = v;
        }
        const body = { data: { attributes: attrs } };

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "PATCH",
          `/api/v2/security_monitoring/signals/bulk/update`,
          body,
        );

        const handle = await context.writeResource(
          "bulk_edit_security_monitoring_signals",
          "updated",
          result,
        );
        context.logger.info(
          "Updated bulk_edit_security_monitoring_signals",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    search_security_monitoring_signals: {
      description: "Get a list of security signals",
      arguments: z.object({
        filter: z.unknown().optional(),
        page: z.unknown().optional(),
        sort: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const startMs = Date.now();
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) body[k] = v;
        }

        const { results, truncated } = await ddApiPostPaginated(
          apiKey,
          appKey,
          site,
          `/api/v2/security_monitoring/signals/search`,
          body,
          "meta.page.after",
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "search_security_monitoring_signals",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );

        context.logger.info(
          "Found {count} search_security_monitoring_signals",
          { count: results.length },
        );
        return { dataHandles: [handle] };
      },
    },
    get_security_monitoring_signal: {
      description: "Get a signal's details",
      arguments: z.object({
        signal_id: z.string().describe("The ID of the signal."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "GET",
          `/api/v2/security_monitoring/signals/${
            encodeURIComponent(String(args.signal_id))
          }`,
        );

        const handle = await context.writeResource(
          "security_monitoring_signal",
          sanitizeInstanceName(String(args.signal_id)),
          result,
        );
        context.logger.info("Fetched security_monitoring_signal", {});
        return { dataHandles: [handle] };
      },
    },
    edit_security_monitoring_signal_assignee: {
      description: "Modify the triage assignee of a security signal",
      arguments: z.object({
        signal_id: z.string().describe("The ID of the signal."),
        assignee: z.unknown(),
        version: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const attrs: Record<string, unknown> = {};
        const excludeKeys = new Set<string>(["signal_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) attrs[k] = v;
        }
        const body = { data: { attributes: attrs } };

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "PATCH",
          `/api/v2/security_monitoring/signals/${
            encodeURIComponent(String(args.signal_id))
          }/assignee`,
          body,
        );

        const handle = await context.writeResource(
          "edit_security_monitoring_signal_assignee",
          sanitizeInstanceName(String(args.signal_id)),
          result,
        );
        context.logger.info(
          "Updated edit_security_monitoring_signal_assignee",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    edit_security_monitoring_signal_incidents: {
      description: "Change the related incidents of a security signal",
      arguments: z.object({
        signal_id: z.string().describe("The ID of the signal."),
        incident_ids: z.unknown(),
        version: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const attrs: Record<string, unknown> = {};
        const excludeKeys = new Set<string>(["signal_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) attrs[k] = v;
        }
        const body = { data: { attributes: attrs } };

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "PATCH",
          `/api/v2/security_monitoring/signals/${
            encodeURIComponent(String(args.signal_id))
          }/incidents`,
          body,
        );

        const handle = await context.writeResource(
          "edit_security_monitoring_signal_incidents",
          sanitizeInstanceName(String(args.signal_id)),
          result,
        );
        context.logger.info(
          "Updated edit_security_monitoring_signal_incidents",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    get_investigation_log_queries_matching_signal: {
      description: "Get investigation queries for a signal",
      arguments: z.object({
        signal_id: z.string().describe("The ID of the signal."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const startMs = Date.now();
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>(["signal_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) {
            const paramKey = k;
            params[paramKey] = String(v);
          }
        }

        const { results, truncated } = await ddApiPaginated(
          apiKey,
          appKey,
          site,
          `/api/v2/security_monitoring/signals/${
            encodeURIComponent(String(args.signal_id))
          }/investigation_queries`,
          { "style": "none", "limitParam": "", "limitDefault": 0 },
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "investigation_log_queries_matching_signal",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );

        context.logger.info(
          "Found {count} investigation_log_queries_matching_signal",
          { count: results.length },
        );
        return { dataHandles: [handle] };
      },
    },
    edit_security_monitoring_signal_state: {
      description: "Change the triage state of a security signal",
      arguments: z.object({
        signal_id: z.string().describe("The ID of the signal."),
        archive_comment: z.unknown().optional(),
        archive_reason: z.unknown().optional(),
        state: z.unknown(),
        version: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const attrs: Record<string, unknown> = {};
        const excludeKeys = new Set<string>(["signal_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) attrs[k] = v;
        }
        const body = { data: { type: "signal_metadata", attributes: attrs } };

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "PATCH",
          `/api/v2/security_monitoring/signals/${
            encodeURIComponent(String(args.signal_id))
          }/state`,
          body,
        );

        const handle = await context.writeResource(
          "edit_security_monitoring_signal_state",
          sanitizeInstanceName(String(args.signal_id)),
          result,
        );
        context.logger.info(
          "Updated edit_security_monitoring_signal_state",
          {},
        );
        return { dataHandles: [handle] };
      },
    },
    get_suggested_actions_matching_signal: {
      description: "Get suggested actions for a signal",
      arguments: z.object({
        signal_id: z.string().describe("The ID of the signal."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const startMs = Date.now();
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>(["signal_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) {
            const paramKey = k;
            params[paramKey] = String(v);
          }
        }

        const { results, truncated } = await ddApiPaginated(
          apiKey,
          appKey,
          site,
          `/api/v2/security_monitoring/signals/${
            encodeURIComponent(String(args.signal_id))
          }/suggested_actions`,
          { "style": "none", "limitParam": "", "limitDefault": 0 },
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "suggested_actions_matching_signal",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );

        context.logger.info("Found {count} suggested_actions_matching_signal", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    edit_security_monitoring_signal: {
      description: "Update security signal triage state or assignee",
      arguments: z.object({
        signal_id: z.string().describe("The ID of the signal."),
        archive_comment: z.unknown().optional(),
        archive_reason: z.unknown().optional(),
        assignee: z.unknown().optional(),
        state: z.unknown().optional(),
        version: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const attrs: Record<string, unknown> = {};
        const excludeKeys = new Set<string>(["signal_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) attrs[k] = v;
        }
        const body = { data: { type: "signal_metadata", attributes: attrs } };

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "PATCH",
          `/api/v2/security_monitoring/signals/${
            encodeURIComponent(String(args.signal_id))
          }/update`,
          body,
        );

        const handle = await context.writeResource(
          "edit_security_monitoring_signal",
          sanitizeInstanceName(String(args.signal_id)),
          result,
        );
        context.logger.info("Updated edit_security_monitoring_signal", {});
        return { dataHandles: [handle] };
      },
    },
  },
};
