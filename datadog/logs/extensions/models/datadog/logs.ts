/**
 * Datadog Logs — log search, aggregation, and analytics
 *
 * Auto-generated by scripts/datadog-codegen — do not edit manually.
 *
 * @module
 */
// SPDX-License-Identifier: AGPL-3.0-or-later WITH Swamp-Extension-Exception

import { z } from "npm:zod@4.4.3";
import { ddApi, ddApiPaginated, ddApiPostPaginated } from "./_lib/api.ts";

// =============================================================================
// Schemas
// =============================================================================

const GlobalArgsSchema = z.object({
  apiKey: z.string().meta({ sensitive: true }).describe(
    "Datadog API key (DD-API-KEY)",
  ),
  appKey: z.string().meta({ sensitive: true }).describe(
    "Datadog application key (DD-APPLICATION-KEY)",
  ),
  site: z.enum(["us1", "us3", "us5", "eu1", "ap1", "us1-fed"]).default("us1")
    .describe("Datadog site"),
});

const SubmitLogSchema = z.object({});

const AggregateLogsSchema = z.object({
  buckets: z.array(z.unknown()).optional().describe(
    "The list of matching buckets, one item per bucket",
  ),
});

const LogsGetItemSchema = z.object({
  id: z.string().describe("Unique ID of the Log."),
  type: z.enum(["log"]).optional().describe("Type of the event."),
  attributes: z.record(z.string(), z.unknown()).optional().describe(
    "JSON object of attributes from your log.",
  ),
  host: z.string().optional().describe(
    "Name of the machine from where the logs are being sent.",
  ),
  message: z.string().optional().describe(
    "The message [reserved attribute](https://docs.datadoghq.com/logs/log_collection/#reserved-attribu...",
  ),
  service: z.string().optional().describe(
    "The name of the application or service generating the log events. It is used to switch from Logs ...",
  ),
  status: z.string().optional().describe(
    "Status of the message associated with your log.",
  ),
  tags: z.array(z.string()).optional().describe(
    "Array of tags associated with your log.",
  ),
  timestamp: z.string().optional().describe("Timestamp of your log."),
});

const ListLogsGetSchema = z.object({
  items: z.array(LogsGetItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const LogsItemSchema = z.object({
  id: z.string().describe("Unique ID of the Log."),
  type: z.enum(["log"]).optional().describe("Type of the event."),
  attributes: z.record(z.string(), z.unknown()).optional().describe(
    "JSON object of attributes from your log.",
  ),
  host: z.string().optional().describe(
    "Name of the machine from where the logs are being sent.",
  ),
  message: z.string().optional().describe(
    "The message [reserved attribute](https://docs.datadoghq.com/logs/log_collection/#reserved-attribu...",
  ),
  service: z.string().optional().describe(
    "The name of the application or service generating the log events. It is used to switch from Logs ...",
  ),
  status: z.string().optional().describe(
    "Status of the message associated with your log.",
  ),
  tags: z.array(z.string()).optional().describe(
    "Array of tags associated with your log.",
  ),
  timestamp: z.string().optional().describe("Timestamp of your log."),
});

const ListLogsSchema = z.object({
  items: z.array(LogsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

// =============================================================================
// Model Definition
// =============================================================================

/** Datadog Logs — log search, aggregation, and analytics */
export const model = {
  type: "@webframp/datadog/logs",
  version: "2026.07.20.10",
  globalArguments: GlobalArgsSchema,

  upgrades: [],

  resources: {
    "submit_log": {
      description: "Send logs",
      schema: SubmitLogSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "aggregate_logs": {
      description: "Aggregate events",
      schema: AggregateLogsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "logs_get": {
      description: "Search logs (GET)",
      schema: ListLogsGetSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "logs": {
      description: "Search logs (POST)",
      schema: ListLogsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
  },

  methods: {
    submit_log: {
      description: "Send logs",
      arguments: z.object({
        ddtags: z.string().optional().describe(
          "Log tags can be passed as query parameters with `text/plain` content type.",
        ),
        entries: z.array(z.record(z.string(), z.unknown())).describe(
          "Array of items to submit",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const queryParts: string[] = [];
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && ["ddtags"].includes(k)) {
            const apiName = k;
            queryParts.push(
              `${encodeURIComponent(apiName)}=${encodeURIComponent(String(v))}`,
            );
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "POST",
          `/api/v2/logs${qs}`,
          args.entries,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource("submit_log", id, result);
        context.logger.info("Created submit_log {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    aggregate_logs: {
      description: "Aggregate events",
      arguments: z.object({
        compute: z.array(z.unknown()).optional().describe(
          "The list of metrics or timeseries to compute for the retrieved buckets.",
        ),
        filter: z.unknown().optional(),
        group_by: z.array(z.unknown()).optional().describe(
          "The rules for the group by",
        ),
        options: z.unknown().optional(),
        page: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "POST",
          `/api/v2/logs/analytics/aggregate`,
          body,
        );

        const id = (result as { id?: string }).id ?? "aggregate_logs";
        const handle = await context.writeResource(
          "aggregate_logs",
          id,
          result ?? {},
        );
        context.logger.info("Executed aggregate_logs", {});
        return { dataHandles: [handle] };
      },
    },
    list_logs_get: {
      description: "Search logs (GET)",
      arguments: z.object({
        filter_query: z.string().optional().describe(
          "Search query following logs syntax.",
        ),
        filter_indexes: z.string().optional().describe(
          "For customers with multiple indexes, the indexes to search. Defaults to '*' w...",
        ),
        filter_from: z.string().optional().describe(
          "Minimum timestamp for requested logs.",
        ),
        filter_to: z.string().optional().describe(
          "Maximum timestamp for requested logs.",
        ),
        filter_storage_tier: z.string().optional().describe(
          "Specifies the storage type to be used",
        ),
        sort: z.string().optional().describe("Order of logs in results."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>([]);
        const paramNameMap: Record<string, string> = {
          "filter_query": "filter[query]",
          "filter_indexes": "filter[indexes]",
          "filter_from": "filter[from]",
          "filter_to": "filter[to]",
          "filter_storage_tier": "filter[storage_tier]",
        };
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) {
            const apiKey = paramNameMap[k] ?? k;
            params[apiKey] = String(v);
          }
        }

        const { results, truncated } = await ddApiPaginated(
          apiKey,
          appKey,
          site,
          `/api/v2/logs/events`,
          {
            "style": "cursor",
            "limitParam": "page[limit]",
            "limitDefault": 100,
            "cursorParam": "page[cursor]",
            "cursorResponsePath": "meta.page.after",
          },
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("logs_get", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} logs_get", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    list_logs: {
      description: "Search logs (POST)",
      arguments: z.object({
        filter: z.unknown().optional(),
        options: z.unknown().optional(),
        page: z.unknown().optional(),
        sort: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) body[k] = v;
        }

        const { results, truncated } = await ddApiPostPaginated(
          apiKey,
          appKey,
          site,
          `/api/v2/logs/events/search`,
          body,
          "meta.page.after",
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("logs", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
        });

        context.logger.info("Found {count} logs", { count: results.length });
        return { dataHandles: [handle] };
      },
    },
  },
};
