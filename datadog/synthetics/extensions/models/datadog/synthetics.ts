/**
 * Datadog Synthetics — synthetic monitoring tests, results, and locations
 *
 * Auto-generated by scripts/datadog-codegen — do not edit manually.
 *
 * @module
 */
// SPDX-License-Identifier: AGPL-3.0-or-later WITH Swamp-Extension-Exception

import { z } from "npm:zod@4.4.3";
import { ddApi, ddApiPaginated } from "./_lib/api.ts";

// =============================================================================
// Schemas
// =============================================================================

const GlobalArgsSchema = z.object({
  apiKey: z.string().meta({ sensitive: true }).describe(
    "Datadog API key (DD-API-KEY)",
  ),
  appKey: z.string().meta({ sensitive: true }).describe(
    "Datadog application key (DD-APPLICATION-KEY)",
  ),
  site: z.enum(["us1", "us3", "us5", "eu1", "ap1", "us1-fed"]).default("us1")
    .describe("Datadog site"),
});

const ApiMultistepSubtestsItemSchema = z.object({
  id: z.string().describe("The public ID of the subtest."),
  type: z.enum(["subtest"]).optional().describe(
    "Type of the subtest resource.",
  ),
  name: z.string().optional().describe("Name of the subtest."),
  public_id: z.string().optional().describe("The public ID of the subtest."),
});

const GetApiMultistepSubtestsSchema = z.object({
  items: z.array(ApiMultistepSubtestsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const ApiMultistepSubtestParentsItemSchema = z.object({
  id: z.string().describe("The public ID of the parent test."),
  type: z.enum(["parent_test"]).optional().describe(
    "Type of the parent test resource.",
  ),
  child_name: z.string().optional().describe("The name of the child subtest."),
  child_public_id: z.string().optional().describe(
    "The public ID of the child subtest.",
  ),
  monitor_id: z.number().int().optional().describe(
    "The associated monitor ID.",
  ),
  name: z.string().optional().describe("Name of the parent test."),
  overall_state: z.number().int().optional().describe(
    "The overall state of the parent test.",
  ),
  overall_state_modified: z.string().optional().describe(
    "Timestamp of when the overall state was last modified.",
  ),
  public_id: z.string().optional().describe(
    "The public ID of the parent test.",
  ),
});

const GetApiMultistepSubtestParentsSchema = z.object({
  items: z.array(ApiMultistepSubtestParentsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const SyntheticsDowntimesItemSchema = z.object({
  id: z.string().describe("The unique identifier of the downtime."),
  type: z.enum(["downtime"]).optional().describe(
    "The resource type for a Synthetics downtime.",
  ),
  createdAt: z.string().describe(
    "The timestamp when the downtime was created.",
  ),
  createdBy: z.string().describe(
    "The UUID of the user who created the downtime.",
  ),
  createdByName: z.string().describe(
    "The display name of the user who created the downtime.",
  ),
  description: z.string().describe("The description of the downtime."),
  isEnabled: z.boolean().describe("Whether the downtime is enabled."),
  name: z.string().describe("The name of the downtime."),
  tags: z.array(z.string()).describe(
    "List of tags associated with a Synthetics downtime.",
  ),
  testIds: z.array(z.string()).describe(
    "List of Synthetics test public IDs associated with a downtime.",
  ),
  timeSlots: z.array(z.unknown()).describe(
    "List of time slots in a Synthetics downtime response.",
  ),
  updatedAt: z.string().describe(
    "The timestamp when the downtime was last updated.",
  ),
  updatedBy: z.string().describe(
    "The UUID of the user who last updated the downtime.",
  ),
  updatedByName: z.string().describe(
    "The display name of the user who last updated the downtime.",
  ),
});

const ListSyntheticsDowntimesSchema = z.object({
  items: z.array(SyntheticsDowntimesItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateSyntheticsDowntimeSchema = z.object({
  id: z.string().describe("The unique identifier of the downtime."),
  type: z.enum(["downtime"]).optional().describe(
    "The resource type for a Synthetics downtime.",
  ),
  createdAt: z.string().describe(
    "The timestamp when the downtime was created.",
  ),
  createdBy: z.string().describe(
    "The UUID of the user who created the downtime.",
  ),
  createdByName: z.string().describe(
    "The display name of the user who created the downtime.",
  ),
  description: z.string().describe("The description of the downtime."),
  isEnabled: z.boolean().describe("Whether the downtime is enabled."),
  name: z.string().describe("The name of the downtime."),
  tags: z.array(z.string()).describe(
    "List of tags associated with a Synthetics downtime.",
  ),
  testIds: z.array(z.string()).describe(
    "List of Synthetics test public IDs associated with a downtime.",
  ),
  timeSlots: z.array(z.unknown()).describe(
    "List of time slots in a Synthetics downtime response.",
  ),
  updatedAt: z.string().describe(
    "The timestamp when the downtime was last updated.",
  ),
  updatedBy: z.string().describe(
    "The UUID of the user who last updated the downtime.",
  ),
  updatedByName: z.string().describe(
    "The display name of the user who last updated the downtime.",
  ),
});

const AddTestToSyntheticsDowntimeSchema = z.object({
  id: z.string().describe("The unique identifier of the downtime."),
  type: z.enum(["downtime"]).optional().describe(
    "The resource type for a Synthetics downtime.",
  ),
  createdAt: z.string().describe(
    "The timestamp when the downtime was created.",
  ),
  createdBy: z.string().describe(
    "The UUID of the user who created the downtime.",
  ),
  createdByName: z.string().describe(
    "The display name of the user who created the downtime.",
  ),
  description: z.string().describe("The description of the downtime."),
  isEnabled: z.boolean().describe("Whether the downtime is enabled."),
  name: z.string().describe("The name of the downtime."),
  tags: z.array(z.string()).describe(
    "List of tags associated with a Synthetics downtime.",
  ),
  testIds: z.array(z.string()).describe(
    "List of Synthetics test public IDs associated with a downtime.",
  ),
  timeSlots: z.array(z.unknown()).describe(
    "List of time slots in a Synthetics downtime response.",
  ),
  updatedAt: z.string().describe(
    "The timestamp when the downtime was last updated.",
  ),
  updatedBy: z.string().describe(
    "The UUID of the user who last updated the downtime.",
  ),
  updatedByName: z.string().describe(
    "The display name of the user who last updated the downtime.",
  ),
});

const GetOnDemandConcurrencyCapSchema = z.object({
  attributes: z.unknown().optional(),
  type: z.unknown().optional(),
});

const SetOnDemandConcurrencyCapSchema = z.object({
  attributes: z.unknown().optional(),
  type: z.unknown().optional(),
});

const CreateSyntheticsSuiteSchema = z.object({
  id: z.string().describe("The public ID for the suite."),
  type: z.enum(["suite"]).describe("Type of the Synthetic suite, `suite`."),
  message: z.string().optional().describe(
    "Notification message associated with the suite.",
  ),
  monitor_id: z.number().int().optional().describe(
    "The associated monitor ID.",
  ),
  name: z.string().describe("Name of the suite."),
  options: z.object({
    alerting_threshold: z.number().min(0).max(1).optional(),
  }).describe("Object describing the extra options for a Synthetic suite."),
  public_id: z.string().optional().describe("The public ID for the test."),
  tags: z.array(z.string()).optional().describe(
    "Array of tags attached to the suite.",
  ),
  tests: z.array(z.unknown()).describe(
    "Array of Synthetic tests included in the suite.",
  ),
});

const DeleteSyntheticsSuitesSchema = z.object({
  id: z.string().describe("The public ID of the deleted Synthetic test suite."),
  type: z.enum(["suites"]).optional().describe(
    "Type for the Synthetics suites responses, `suites`.",
  ),
  deleted_at: z.string().optional().describe(
    "Deletion timestamp of the Synthetic suite ID.",
  ),
  public_id: z.string().optional().describe("The Synthetic suite ID deleted."),
});

const SearchSuitesSchema = z.object({
  id: z.string().describe(
    "The unique identifier of the suite search response data.",
  ),
  type: z.enum(["suites_search"]).optional().describe(
    "Type for the Synthetics suites search response, `suites_search`.",
  ),
  suites: z.array(z.unknown()).optional().describe(
    "List of Synthetic suites matching the search query.",
  ),
  total: z.number().int().max(2147483647).optional().describe(
    "Total number of Synthetic suites matching the search query.",
  ),
});

const EditSyntheticsSuiteSchema = z.object({
  id: z.string().describe("The public ID for the suite."),
  type: z.enum(["suite"]).describe("Type of the Synthetic suite, `suite`."),
  message: z.string().optional().describe(
    "Notification message associated with the suite.",
  ),
  monitor_id: z.number().int().optional().describe(
    "The associated monitor ID.",
  ),
  name: z.string().describe("Name of the suite."),
  options: z.object({
    alerting_threshold: z.number().min(0).max(1).optional(),
  }).describe("Object describing the extra options for a Synthetic suite."),
  public_id: z.string().optional().describe("The public ID for the test."),
  tags: z.array(z.string()).optional().describe(
    "Array of tags attached to the suite.",
  ),
  tests: z.array(z.unknown()).describe(
    "Array of Synthetic tests included in the suite.",
  ),
});

const PatchTestSuiteSchema = z.object({
  id: z.string().describe("The public ID for the suite."),
  type: z.enum(["suite"]).describe("Type of the Synthetic suite, `suite`."),
  message: z.string().optional().describe(
    "Notification message associated with the suite.",
  ),
  monitor_id: z.number().int().optional().describe(
    "The associated monitor ID.",
  ),
  name: z.string().describe("Name of the suite."),
  options: z.object({
    alerting_threshold: z.number().min(0).max(1).optional(),
  }).describe("Object describing the extra options for a Synthetic suite."),
  public_id: z.string().optional().describe("The public ID for the test."),
  tags: z.array(z.string()).optional().describe(
    "Array of tags attached to the suite.",
  ),
  tests: z.array(z.unknown()).describe(
    "Array of Synthetic tests included in the suite.",
  ),
});

const SyntheticsBrowserTestLatestResultsItemSchema = z.object({
  id: z.string().describe("The result ID."),
  type: z.enum(["result_summary"]).optional().describe(
    "Type of the Synthetic test result summary resource, `result_summary`.",
  ),
  device: z.object({
    browser: z.unknown().optional(),
    id: z.string().optional(),
    name: z.string().optional(),
    platform: z.unknown().optional(),
    resolution: z.unknown().optional(),
    type: z.string().optional(),
  }).optional().describe(
    "Device information for the test result (browser and mobile tests).",
  ),
  execution_info: z.object({
    duration: z.unknown().optional(),
    error_message: z.string().optional(),
    is_fast_retry: z.boolean().optional(),
    timings: z.record(z.string(), z.unknown()).optional(),
    tunnel: z.boolean().optional(),
    unhealthy: z.boolean().optional(),
  }).optional().describe("Execution details for a Synthetic test result."),
  finished_at: z.number().int().optional().describe(
    "Timestamp of when the test finished (in milliseconds).",
  ),
  location: z.object({
    id: z.string().optional(),
    name: z.string().optional(),
    version: z.string().optional(),
    worker_id: z.string().optional(),
  }).optional().describe("Location information for a Synthetic test result."),
  run_type: z.enum(["scheduled", "fast", "ci", "triggered"]).optional()
    .describe("The type of run for a Synthetic test result."),
  started_at: z.number().int().optional().describe(
    "Timestamp of when the test started (in milliseconds).",
  ),
  status: z.enum(["passed", "failed", "no_data"]).optional().describe(
    "Status of a Synthetic test result.",
  ),
  steps_info: z.object({
    completed: z.number().int().optional(),
    errors: z.number().int().optional(),
    total: z.number().int().optional(),
  }).optional().describe("Step execution summary for a Synthetic test result."),
  test_sub_type: z.enum([
    "dns",
    "grpc",
    "http",
    "icmp",
    "mcp",
    "multi",
    "ssl",
    "tcp",
    "udp",
    "websocket",
  ]).optional().describe(
    "Subtype of the Synthetic test that produced this result.",
  ),
  test_type: z.enum(["api", "browser", "mobile", "network"]).optional()
    .describe("Type of the Synthetic test that produced this result."),
  test_id: z.string().optional().describe("Related test ID"),
});

const ListSyntheticsBrowserTestLatestResultsSchema = z.object({
  items: z.array(SyntheticsBrowserTestLatestResultsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetSyntheticsBrowserTestResultSchema = z.object({
  id: z.string().describe("The result ID."),
  type: z.enum(["result"]).optional().describe(
    "Type of the Synthetic test result resource, `result`.",
  ),
  batch: z.object({
    id: z.string().optional(),
  }).optional().describe("Batch information for the test result."),
  ci: z.object({
    pipeline: z.unknown().optional(),
    provider: z.unknown().optional(),
    stage: z.unknown().optional(),
    workspace_path: z.string().optional(),
  }).optional().describe("CI information associated with the test result."),
  device: z.object({
    browser: z.unknown().optional(),
    id: z.string().optional(),
    name: z.string().optional(),
    platform: z.unknown().optional(),
    resolution: z.unknown().optional(),
    type: z.string().optional(),
  }).optional().describe(
    "Device information for the test result (browser and mobile tests).",
  ),
  git: z.object({
    branch: z.string().optional(),
    commit: z.unknown().optional(),
    repository_url: z.string().optional(),
  }).optional().describe("Git information associated with the test result."),
  location: z.object({
    id: z.string().optional(),
    name: z.string().optional(),
    version: z.string().optional(),
    worker_id: z.string().optional(),
  }).optional().describe("Location information for a Synthetic test result."),
  result: z.object({
    assertions: z.array(z.unknown()).optional(),
    bucket_keys: z.unknown().optional(),
    call_type: z.string().optional(),
    cert: z.unknown().optional(),
    compressed_json_descriptor: z.string().optional(),
    compressed_steps: z.string().optional(),
    connection_outcome: z.string().optional(),
    dns_resolution: z.unknown().optional(),
    duration: z.number().optional(),
    exited_on_step_success: z.boolean().optional(),
    failure: z.unknown().optional(),
    finished_at: z.number().int().optional(),
    handshake: z.unknown().optional(),
    id: z.string().optional(),
    initial_id: z.string().optional(),
    is_fast_retry: z.boolean().optional(),
    is_last_retry: z.boolean().optional(),
    netpath: z.unknown().optional(),
    netstats: z.unknown().optional(),
    ocsp: z.unknown().optional(),
    ping: z.unknown().optional(),
    received_email_count: z.number().int().optional(),
    received_message: z.string().optional(),
    request: z.unknown().optional(),
    resolved_ip: z.string().optional(),
    response: z.unknown().optional(),
    run_type: z.unknown().optional(),
    sent_message: z.string().optional(),
    start_url: z.string().optional(),
    started_at: z.number().int().optional(),
    status: z.unknown().optional(),
    steps: z.array(z.unknown()).optional(),
    time_to_interactive: z.number().int().optional(),
    timings: z.record(z.string(), z.unknown()).optional(),
    trace: z.unknown().optional(),
    traceroute: z.array(z.unknown()).optional(),
    triggered_at: z.number().int().optional(),
    tunnel: z.boolean().optional(),
    turns: z.array(z.unknown()).optional(),
    unhealthy: z.boolean().optional(),
    variables: z.unknown().optional(),
  }).optional().describe("Full result details for a Synthetic test execution."),
  test_sub_type: z.enum([
    "dns",
    "grpc",
    "http",
    "icmp",
    "mcp",
    "multi",
    "ssl",
    "tcp",
    "udp",
    "websocket",
  ]).optional().describe(
    "Subtype of the Synthetic test that produced this result.",
  ),
  test_type: z.enum(["api", "browser", "mobile", "network"]).optional()
    .describe("Type of the Synthetic test that produced this result."),
  test_id: z.string().optional().describe("Related test ID"),
});

const DeleteSyntheticsTestsSchema = z.object({
  id: z.string().describe("The public ID of the deleted Synthetic test."),
  type: z.enum(["delete_tests"]).optional().describe(
    "Type for the bulk delete Synthetic tests response, `delete_tests`.",
  ),
  deleted_at: z.string().optional().describe(
    "Deletion timestamp of the Synthetic test ID.",
  ),
  public_id: z.string().optional().describe("The Synthetic test ID deleted."),
});

const GetSyntheticsFastTestResultSchema = z.object({
  id: z.string().describe(
    "The UUID of the fast test, used as the result identifier.",
  ),
  type: z.enum(["result"]).optional().describe(
    "JSON:API type for a fast test result.",
  ),
  device: z.object({
    browser: z.unknown().optional(),
    id: z.string().optional(),
    name: z.string().optional(),
    platform: z.unknown().optional(),
    resolution: z.unknown().optional(),
    type: z.string().optional(),
  }).optional().describe(
    "Device information for the test result (browser and mobile tests).",
  ),
  location: z.object({
    id: z.string().optional(),
    name: z.string().optional(),
    version: z.string().optional(),
    worker_id: z.string().optional(),
  }).optional().describe("Location information for a Synthetic test result."),
  result: z.object({
    assertions: z.array(z.unknown()).optional(),
    call_type: z.string().optional(),
    cert: z.unknown().optional(),
    duration: z.number().optional(),
    failure: z.unknown().optional(),
    finished_at: z.number().int().optional(),
    id: z.string().optional(),
    is_fast_retry: z.boolean().optional(),
    request: z.unknown().optional(),
    resolved_ip: z.string().optional(),
    response: z.unknown().optional(),
    run_type: z.unknown().optional(),
    started_at: z.number().int().optional(),
    status: z.string().optional(),
    steps: z.array(z.unknown()).optional(),
    timings: z.record(z.string(), z.unknown()).optional(),
    traceroute: z.array(z.unknown()).optional(),
    triggered_at: z.number().int().optional(),
    tunnel: z.boolean().optional(),
  }).optional().describe(
    "Detailed result data for the fast test run. The exact shape of nested fields (`request`, `respons...",
  ),
  test_sub_type: z.enum([
    "dns",
    "grpc",
    "http",
    "icmp",
    "mcp",
    "multi",
    "ssl",
    "tcp",
    "udp",
    "websocket",
  ]).optional().describe(
    "Subtype of the Synthetic test that produced this result.",
  ),
  test_type: z.enum(["fast-api", "fast-browser"]).optional().describe(
    "Type of the Synthetic fast test that produced this result.",
  ),
  test_version: z.number().int().optional().describe(
    "Version of the test at the time the fast test was triggered.",
  ),
});

const CreateSyntheticsNetworkTestSchema = z.object({
  id: z.string().describe("The public ID of the Network Path test."),
  type: z.enum(["network"]).describe("Type of the Synthetic test, `network`."),
  config: z.object({
    assertions: z.array(z.unknown()).optional(),
    request: z.unknown().optional(),
  }).describe("Configuration object for a Network Path test."),
  locations: z.array(z.string()).describe(
    "Array of locations used to run the test. Network Path tests can be run from managed locations to ...",
  ),
  message: z.string().describe(
    "Notification message associated with the test.",
  ),
  monitor_id: z.number().int().optional().describe(
    "The associated monitor ID.",
  ),
  name: z.string().describe("Name of the test."),
  options: z.object({
    min_failure_duration: z.number().int().optional(),
    min_location_failed: z.number().int().optional(),
    monitor_name: z.string().optional(),
    monitor_options: z.unknown().optional(),
    monitor_priority: z.number().int().min(1).max(5).optional(),
    restricted_roles: z.unknown().optional(),
    retry: z.unknown().optional(),
    scheduling: z.unknown().optional(),
    tick_every: z.number().int().min(30).max(604800).optional(),
  }).describe("Object describing the extra options for a Synthetic test."),
  public_id: z.string().optional().describe("The public ID for the test."),
  status: z.enum(["live", "paused"]).optional().describe(
    "Define whether you want to start (`live`) or pause (`paused`) a Synthetic test.",
  ),
  subtype: z.enum(["tcp", "udp", "icmp"]).optional().describe(
    "Subtype of the Synthetic Network Path test: `tcp`, `udp`, or `icmp`.",
  ),
  tags: z.array(z.string()).optional().describe(
    "Array of tags attached to the test.",
  ),
});

const PollSyntheticsTestResultsItemSchema = z.object({
  id: z.string().describe("The result ID."),
  type: z.enum(["result"]).optional().describe(
    "Type of the Synthetic test result resource, `result`.",
  ),
  batch: z.object({
    id: z.string().optional(),
  }).optional().describe("Batch information for the test result."),
  ci: z.object({
    pipeline: z.unknown().optional(),
    provider: z.unknown().optional(),
    stage: z.unknown().optional(),
    workspace_path: z.string().optional(),
  }).optional().describe("CI information associated with the test result."),
  device: z.object({
    browser: z.unknown().optional(),
    id: z.string().optional(),
    name: z.string().optional(),
    platform: z.unknown().optional(),
    resolution: z.unknown().optional(),
    type: z.string().optional(),
  }).optional().describe(
    "Device information for the test result (browser and mobile tests).",
  ),
  git: z.object({
    branch: z.string().optional(),
    commit: z.unknown().optional(),
    repository_url: z.string().optional(),
  }).optional().describe("Git information associated with the test result."),
  location: z.object({
    id: z.string().optional(),
    name: z.string().optional(),
    version: z.string().optional(),
    worker_id: z.string().optional(),
  }).optional().describe("Location information for a Synthetic test result."),
  result: z.object({
    assertions: z.array(z.unknown()).optional(),
    bucket_keys: z.unknown().optional(),
    call_type: z.string().optional(),
    cert: z.unknown().optional(),
    compressed_json_descriptor: z.string().optional(),
    compressed_steps: z.string().optional(),
    connection_outcome: z.string().optional(),
    dns_resolution: z.unknown().optional(),
    duration: z.number().optional(),
    exited_on_step_success: z.boolean().optional(),
    failure: z.unknown().optional(),
    finished_at: z.number().int().optional(),
    handshake: z.unknown().optional(),
    id: z.string().optional(),
    initial_id: z.string().optional(),
    is_fast_retry: z.boolean().optional(),
    is_last_retry: z.boolean().optional(),
    netpath: z.unknown().optional(),
    netstats: z.unknown().optional(),
    ocsp: z.unknown().optional(),
    ping: z.unknown().optional(),
    received_email_count: z.number().int().optional(),
    received_message: z.string().optional(),
    request: z.unknown().optional(),
    resolved_ip: z.string().optional(),
    response: z.unknown().optional(),
    run_type: z.unknown().optional(),
    sent_message: z.string().optional(),
    start_url: z.string().optional(),
    started_at: z.number().int().optional(),
    status: z.unknown().optional(),
    steps: z.array(z.unknown()).optional(),
    time_to_interactive: z.number().int().optional(),
    timings: z.record(z.string(), z.unknown()).optional(),
    trace: z.unknown().optional(),
    traceroute: z.array(z.unknown()).optional(),
    triggered_at: z.number().int().optional(),
    tunnel: z.boolean().optional(),
    turns: z.array(z.unknown()).optional(),
    unhealthy: z.boolean().optional(),
    variables: z.unknown().optional(),
  }).optional().describe("Full result details for a Synthetic test execution."),
  test_sub_type: z.enum([
    "dns",
    "grpc",
    "http",
    "icmp",
    "mcp",
    "multi",
    "ssl",
    "tcp",
    "udp",
    "websocket",
  ]).optional().describe(
    "Subtype of the Synthetic test that produced this result.",
  ),
  test_type: z.enum(["api", "browser", "mobile", "network"]).optional()
    .describe("Type of the Synthetic test that produced this result."),
  test_id: z.string().optional().describe("Related test ID"),
});

const PollSyntheticsTestResultsSchema = z.object({
  items: z.array(PollSyntheticsTestResultsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetTestFileDownloadUrlSchema = z.object({
  url: z.string().optional().describe(
    "A presigned URL to download the file. The URL expires after a short period.",
  ),
});

const GetTestFileMultipartPresignedUrlsSchema = z.object({
  bucketKey: z.string().optional().describe(
    "The bucket key that references the uploaded file after completion.",
  ),
  multipart_presigned_urls_params: z.unknown().optional(),
});

const TestParentSuitesItemSchema = z.object({
  id: z.string().describe("The public ID of the parent suite."),
  type: z.enum(["parent_suite"]).optional().describe(
    "Type of the parent suite resource.",
  ),
  child_name: z.string().optional().describe(
    "The name of the child test within the suite.",
  ),
  child_public_id: z.string().optional().describe(
    "The public ID of the child test within the suite.",
  ),
  monitor_id: z.number().int().optional().describe(
    "The associated monitor ID.",
  ),
  name: z.string().optional().describe("Name of the parent suite."),
  overall_state: z.number().int().optional().describe(
    "The overall state of the parent suite.",
  ),
  overall_state_modified: z.string().optional().describe(
    "Timestamp of when the overall state was last modified.",
  ),
  public_id: z.string().optional().describe(
    "The public ID of the parent suite.",
  ),
});

const GetTestParentSuitesSchema = z.object({
  items: z.array(TestParentSuitesItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const SyntheticsTestLatestResultsItemSchema = z.object({
  id: z.string().describe("The result ID."),
  type: z.enum(["result_summary"]).optional().describe(
    "Type of the Synthetic test result summary resource, `result_summary`.",
  ),
  device: z.object({
    browser: z.unknown().optional(),
    id: z.string().optional(),
    name: z.string().optional(),
    platform: z.unknown().optional(),
    resolution: z.unknown().optional(),
    type: z.string().optional(),
  }).optional().describe(
    "Device information for the test result (browser and mobile tests).",
  ),
  execution_info: z.object({
    duration: z.unknown().optional(),
    error_message: z.string().optional(),
    is_fast_retry: z.boolean().optional(),
    timings: z.record(z.string(), z.unknown()).optional(),
    tunnel: z.boolean().optional(),
    unhealthy: z.boolean().optional(),
  }).optional().describe("Execution details for a Synthetic test result."),
  finished_at: z.number().int().optional().describe(
    "Timestamp of when the test finished (in milliseconds).",
  ),
  location: z.object({
    id: z.string().optional(),
    name: z.string().optional(),
    version: z.string().optional(),
    worker_id: z.string().optional(),
  }).optional().describe("Location information for a Synthetic test result."),
  run_type: z.enum(["scheduled", "fast", "ci", "triggered"]).optional()
    .describe("The type of run for a Synthetic test result."),
  started_at: z.number().int().optional().describe(
    "Timestamp of when the test started (in milliseconds).",
  ),
  status: z.enum(["passed", "failed", "no_data"]).optional().describe(
    "Status of a Synthetic test result.",
  ),
  steps_info: z.object({
    completed: z.number().int().optional(),
    errors: z.number().int().optional(),
    total: z.number().int().optional(),
  }).optional().describe("Step execution summary for a Synthetic test result."),
  test_sub_type: z.enum([
    "dns",
    "grpc",
    "http",
    "icmp",
    "mcp",
    "multi",
    "ssl",
    "tcp",
    "udp",
    "websocket",
  ]).optional().describe(
    "Subtype of the Synthetic test that produced this result.",
  ),
  test_type: z.enum(["api", "browser", "mobile", "network"]).optional()
    .describe("Type of the Synthetic test that produced this result."),
  test_id: z.string().optional().describe("Related test ID"),
});

const ListSyntheticsTestLatestResultsSchema = z.object({
  items: z.array(SyntheticsTestLatestResultsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetSyntheticsTestResultSchema = z.object({
  id: z.string().describe("The result ID."),
  type: z.enum(["result"]).optional().describe(
    "Type of the Synthetic test result resource, `result`.",
  ),
  batch: z.object({
    id: z.string().optional(),
  }).optional().describe("Batch information for the test result."),
  ci: z.object({
    pipeline: z.unknown().optional(),
    provider: z.unknown().optional(),
    stage: z.unknown().optional(),
    workspace_path: z.string().optional(),
  }).optional().describe("CI information associated with the test result."),
  device: z.object({
    browser: z.unknown().optional(),
    id: z.string().optional(),
    name: z.string().optional(),
    platform: z.unknown().optional(),
    resolution: z.unknown().optional(),
    type: z.string().optional(),
  }).optional().describe(
    "Device information for the test result (browser and mobile tests).",
  ),
  git: z.object({
    branch: z.string().optional(),
    commit: z.unknown().optional(),
    repository_url: z.string().optional(),
  }).optional().describe("Git information associated with the test result."),
  location: z.object({
    id: z.string().optional(),
    name: z.string().optional(),
    version: z.string().optional(),
    worker_id: z.string().optional(),
  }).optional().describe("Location information for a Synthetic test result."),
  result: z.object({
    assertions: z.array(z.unknown()).optional(),
    bucket_keys: z.unknown().optional(),
    call_type: z.string().optional(),
    cert: z.unknown().optional(),
    compressed_json_descriptor: z.string().optional(),
    compressed_steps: z.string().optional(),
    connection_outcome: z.string().optional(),
    dns_resolution: z.unknown().optional(),
    duration: z.number().optional(),
    exited_on_step_success: z.boolean().optional(),
    failure: z.unknown().optional(),
    finished_at: z.number().int().optional(),
    handshake: z.unknown().optional(),
    id: z.string().optional(),
    initial_id: z.string().optional(),
    is_fast_retry: z.boolean().optional(),
    is_last_retry: z.boolean().optional(),
    netpath: z.unknown().optional(),
    netstats: z.unknown().optional(),
    ocsp: z.unknown().optional(),
    ping: z.unknown().optional(),
    received_email_count: z.number().int().optional(),
    received_message: z.string().optional(),
    request: z.unknown().optional(),
    resolved_ip: z.string().optional(),
    response: z.unknown().optional(),
    run_type: z.unknown().optional(),
    sent_message: z.string().optional(),
    start_url: z.string().optional(),
    started_at: z.number().int().optional(),
    status: z.unknown().optional(),
    steps: z.array(z.unknown()).optional(),
    time_to_interactive: z.number().int().optional(),
    timings: z.record(z.string(), z.unknown()).optional(),
    trace: z.unknown().optional(),
    traceroute: z.array(z.unknown()).optional(),
    triggered_at: z.number().int().optional(),
    tunnel: z.boolean().optional(),
    turns: z.array(z.unknown()).optional(),
    unhealthy: z.boolean().optional(),
    variables: z.unknown().optional(),
  }).optional().describe("Full result details for a Synthetic test execution."),
  test_sub_type: z.enum([
    "dns",
    "grpc",
    "http",
    "icmp",
    "mcp",
    "multi",
    "ssl",
    "tcp",
    "udp",
    "websocket",
  ]).optional().describe(
    "Subtype of the Synthetic test that produced this result.",
  ),
  test_type: z.enum(["api", "browser", "mobile", "network"]).optional()
    .describe("Type of the Synthetic test that produced this result."),
  test_id: z.string().optional().describe("Related test ID"),
});

const SyntheticsTestVersionsItemSchema = z.object({
  id: z.string().describe("UUID of the version change record."),
  type: z.enum(["version_metadata"]).optional().describe(
    "Type of the version metadata resource.",
  ),
  author_uuid: z.string().optional().describe(
    "UUID of the user who created this version.",
  ),
  change_metadata: z.array(z.unknown()).optional().describe(
    "List of metadata describing individual changes in this version.",
  ),
  version_number: z.number().int().optional().describe(
    "The sequential version number.",
  ),
  version_payload_created_at: z.string().optional().describe(
    "Timestamp of when this version was created.",
  ),
});

const ListSyntheticsTestVersionsSchema = z.object({
  items: z.array(SyntheticsTestVersionsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const GetSyntheticsTestVersionSchema = z.object({
  id: z.string().describe("UUID of the version record."),
  type: z.enum(["version"]).optional().describe(
    "Type of the version resource.",
  ),
  author: z.object({
    email: z.string().optional(),
    handle: z.string().optional(),
    id: z.string().optional(),
    name: z.string().optional(),
  }).optional().describe("Object describing the author of a test version."),
  change_metadata: z.array(z.unknown()).optional().describe(
    "List of metadata describing individual changes in this version. Only returned when the `include_c...",
  ),
  payload: z.record(z.string(), z.unknown()).optional().describe(
    "The full test configuration at this version.",
  ),
  version_payload_created_at: z.string().optional().describe(
    "Timestamp of when this version was created.",
  ),
});

const PatchGlobalVariableSchema = z.object({
  id: z.string().describe("Unique identifier of the global variable."),
  type: z.enum(["global_variables"]).optional().describe(
    "Global variable type.",
  ),
  attributes: z.object({
    restricted_roles: z.unknown().optional(),
  }).optional().describe("Attributes of the global variable."),
  description: z.string().describe("Description of the global variable."),
  is_fido: z.boolean().optional().describe(
    "Determines if the global variable is a FIDO variable.",
  ),
  is_totp: z.boolean().optional().describe(
    "Determines if the global variable is a TOTP/MFA variable.",
  ),
  name: z.string().describe(
    "Name of the global variable. Unique across Synthetic global variables.",
  ),
  parse_test_options: z.object({
    field: z.string().optional(),
    localVariableName: z.string().optional(),
    parser: z.unknown().optional(),
    type: z.unknown(),
  }).optional().describe(
    "Parser options to use for retrieving a Synthetic global variable from a Synthetic test. Used in c...",
  ),
  parse_test_public_id: z.string().optional().describe(
    "A Synthetic test ID to use as a test to generate the variable value.",
  ),
  tags: z.array(z.string()).describe("Tags of the global variable."),
  value: z.object({
    options: z.unknown().optional(),
    secure: z.boolean().optional(),
    value: z.string().optional(),
  }).describe("Value of the global variable."),
});

// =============================================================================
// Model Definition
// =============================================================================

/** Datadog Synthetics — synthetic monitoring tests, results, and locations */
export const model = {
  type: "@webframp/datadog/synthetics",
  version: "2026.07.20.3",
  globalArguments: GlobalArgsSchema,

  upgrades: [],

  resources: {
    "api_multistep_subtests": {
      description: "Get available subtests for a multistep test",
      schema: GetApiMultistepSubtestsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "api_multistep_subtest_parents": {
      description: "Get parent tests for a subtest",
      schema: GetApiMultistepSubtestParentsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "synthetics_downtimes": {
      description: "List Synthetics downtimes",
      schema: ListSyntheticsDowntimesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "synthetics_downtime": {
      description: "Create a Synthetics downtime",
      schema: CreateSyntheticsDowntimeSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "add_test_to_synthetics_downtime": {
      description: "Add a test to a Synthetics downtime",
      schema: AddTestToSyntheticsDowntimeSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "on_demand_concurrency_cap": {
      description: "Get the on-demand concurrency cap",
      schema: GetOnDemandConcurrencyCapSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "set_on_demand_concurrency_cap": {
      description: "Save new value for on-demand concurrency cap",
      schema: SetOnDemandConcurrencyCapSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "synthetics_suite": {
      description: "Create a test suite",
      schema: CreateSyntheticsSuiteSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "synthetics_suites": {
      description: "Bulk delete suites",
      schema: DeleteSyntheticsSuitesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "search_suites": {
      description: "Search test suites",
      schema: SearchSuitesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "edit_synthetics_suite": {
      description: "Edit a test suite",
      schema: EditSyntheticsSuiteSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "patch_test_suite": {
      description: "Patch a test suite",
      schema: PatchTestSuiteSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "synthetics_browser_test_latest_results": {
      description: "Get a browser test's latest results",
      schema: ListSyntheticsBrowserTestLatestResultsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "synthetics_browser_test_result": {
      description: "Get a browser test result",
      schema: GetSyntheticsBrowserTestResultSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "synthetics_tests": {
      description: "Bulk delete tests",
      schema: DeleteSyntheticsTestsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "synthetics_fast_test_result": {
      description: "Get a fast test result",
      schema: GetSyntheticsFastTestResultSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "synthetics_network_test": {
      description: "Create a Network Path test",
      schema: CreateSyntheticsNetworkTestSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "poll_synthetics_test_results": {
      description: "Poll for test results",
      schema: PollSyntheticsTestResultsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "test_file_download_url": {
      description: "Get a presigned URL for downloading a test file",
      schema: GetTestFileDownloadUrlSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "test_file_multipart_presigned_urls": {
      description: "Get presigned URLs for uploading a test file",
      schema: GetTestFileMultipartPresignedUrlsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "abort_test_file_multipart_upload": {
      description: "Abort a multipart upload of a test file",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "complete_test_file_multipart_upload": {
      description: "Complete a multipart upload of a test file",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "test_parent_suites": {
      description: "Get parent suites for a test",
      schema: GetTestParentSuitesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "synthetics_test_latest_results": {
      description: "Get a test's latest results",
      schema: ListSyntheticsTestLatestResultsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "synthetics_test_result": {
      description: "Get a test result",
      schema: GetSyntheticsTestResultSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "synthetics_test_versions": {
      description: "Get version history of a test",
      schema: ListSyntheticsTestVersionsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "synthetics_test_version": {
      description: "Get a specific version of a test",
      schema: GetSyntheticsTestVersionSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "patch_global_variable": {
      description: "Patch a global variable",
      schema: PatchGlobalVariableSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
  },

  methods: {
    get_api_multistep_subtests: {
      description: "Get available subtests for a multistep test",
      arguments: z.object({
        public_id: z.string().describe(
          "The public ID of the API multistep test.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>(["public_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await ddApiPaginated(
          apiKey,
          appKey,
          site,
          `/api/v2/synthetics/api-multistep/subtests/${
            encodeURIComponent(String(args.public_id))
          }`,
          { "style": "none", "limitParam": "", "limitDefault": 0 },
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "api_multistep_subtests",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} api_multistep_subtests", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    get_api_multistep_subtest_parents: {
      description: "Get parent tests for a subtest",
      arguments: z.object({
        public_id: z.string().describe("The public ID of the subtest."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>(["public_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await ddApiPaginated(
          apiKey,
          appKey,
          site,
          `/api/v2/synthetics/api-multistep/subtests/${
            encodeURIComponent(String(args.public_id))
          }/parents`,
          { "style": "none", "limitParam": "", "limitDefault": 0 },
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "api_multistep_subtest_parents",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} api_multistep_subtest_parents", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    list_synthetics_downtimes: {
      description: "List Synthetics downtimes",
      arguments: z.object({
        filter_test_ids: z.string().optional().describe(
          "Comma-separated list of Synthetics test public IDs to filter downtimes by.",
        ),
        filter_active: z.string().optional().describe(
          "If set to `true`, return only downtimes that are currently active.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await ddApiPaginated(
          apiKey,
          appKey,
          site,
          `/api/v2/synthetics/downtimes`,
          { "style": "none", "limitParam": "", "limitDefault": 0 },
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "synthetics_downtimes",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} synthetics_downtimes", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_synthetics_downtime: {
      description: "Create a Synthetics downtime",
      arguments: z.object({
        description: z.string().optional().describe(
          "An optional description of the downtime.",
        ),
        isEnabled: z.boolean().describe("Whether the downtime is enabled."),
        name: z.string().describe("The name of the downtime."),
        tags: z.unknown().optional(),
        testIds: z.unknown(),
        timeSlots: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const attrs: Record<string, unknown> = {};
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) attrs[k] = v;
        }
        const body = { data: { type: "downtime", attributes: attrs } };

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "POST",
          `/api/v2/synthetics/downtimes`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "synthetics_downtime",
          id,
          result,
        );
        context.logger.info("Created synthetics_downtime {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_synthetics_downtime: {
      description: "Get a Synthetics downtime",
      arguments: z.object({
        downtime_id: z.string().describe("The ID of the downtime to retrieve."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "GET",
          `/api/v2/synthetics/downtimes/${
            encodeURIComponent(String(args.downtime_id))
          }`,
        );

        const handle = await context.writeResource(
          "synthetics_downtime",
          String(args.downtime_id),
          result,
        );
        context.logger.info("Fetched synthetics_downtime", {});
        return { dataHandles: [handle] };
      },
    },
    update_synthetics_downtime: {
      description: "Update a Synthetics downtime",
      arguments: z.object({
        downtime_id: z.string().describe("The ID of the downtime to update."),
        description: z.string().optional().describe(
          "An optional description of the downtime.",
        ),
        isEnabled: z.boolean().describe("Whether the downtime is enabled."),
        name: z.string().describe("The name of the downtime."),
        tags: z.unknown().optional(),
        testIds: z.unknown(),
        timeSlots: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const attrs: Record<string, unknown> = {};
        const excludeKeys = new Set<string>(["downtime_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) attrs[k] = v;
        }
        const body = { data: { type: "downtime", attributes: attrs } };

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "PUT",
          `/api/v2/synthetics/downtimes/${
            encodeURIComponent(String(args.downtime_id))
          }`,
          body,
        );

        const handle = await context.writeResource(
          "synthetics_downtime",
          String(args.downtime_id),
          result,
        );
        context.logger.info("Updated synthetics_downtime", {});
        return { dataHandles: [handle] };
      },
    },
    delete_synthetics_downtime: {
      description: "Delete a Synthetics downtime",
      arguments: z.object({
        downtime_id: z.string().describe("The ID of the downtime to delete."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        await ddApi(
          apiKey,
          appKey,
          site,
          "DELETE",
          `/api/v2/synthetics/downtimes/${
            encodeURIComponent(String(args.downtime_id))
          }`,
        );

        context.logger.info("Deleted resource {id}", { id: args.downtime_id });
        return { dataHandles: [] };
      },
    },
    add_test_to_synthetics_downtime: {
      description: "Add a test to a Synthetics downtime",
      arguments: z.object({
        downtime_id: z.string().describe("The ID of the downtime."),
        test_id: z.string().describe(
          "The public ID of the Synthetics test to associate with the downtime.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>(["downtime_id", "test_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "PUT",
          `/api/v2/synthetics/downtimes/${
            encodeURIComponent(String(args.downtime_id))
          }/tests/${encodeURIComponent(String(args.test_id))}`,
          body,
        );

        const handle = await context.writeResource(
          "add_test_to_synthetics_downtime",
          String(args.test_id),
          result,
        );
        context.logger.info("Updated add_test_to_synthetics_downtime", {});
        return { dataHandles: [handle] };
      },
    },
    remove_test_from_synthetics_downtime: {
      description: "Remove a test from a Synthetics downtime",
      arguments: z.object({
        downtime_id: z.string().describe("The ID of the downtime."),
        test_id: z.string().describe(
          "The public ID of the Synthetics test to disassociate from the downtime.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        await ddApi(
          apiKey,
          appKey,
          site,
          "DELETE",
          `/api/v2/synthetics/downtimes/${
            encodeURIComponent(String(args.downtime_id))
          }/tests/${encodeURIComponent(String(args.test_id))}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.test_id });
        return { dataHandles: [] };
      },
    },
    get_on_demand_concurrency_cap: {
      description: "Get the on-demand concurrency cap",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "GET",
          `/api/v2/synthetics/settings/on_demand_concurrency_cap`,
        );

        const handle = await context.writeResource(
          "on_demand_concurrency_cap",
          "latest",
          result,
        );
        context.logger.info("Fetched on_demand_concurrency_cap", {});
        return { dataHandles: [handle] };
      },
    },
    set_on_demand_concurrency_cap: {
      description: "Save new value for on-demand concurrency cap",
      arguments: z.object({
        on_demand_concurrency_cap: z.number().optional().describe(
          "Value of the on-demand concurrency cap.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "POST",
          `/api/v2/synthetics/settings/on_demand_concurrency_cap`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "set_on_demand_concurrency_cap",
          id,
          result,
        );
        context.logger.info("Created set_on_demand_concurrency_cap {id}", {
          id,
        });
        return { dataHandles: [handle] };
      },
    },
    create_synthetics_suite: {
      description: "Create a test suite",
      arguments: z.object({
        message: z.string().optional().describe(
          "Notification message associated with the suite.",
        ),
        monitor_id: z.number().int().optional().describe(
          "The associated monitor ID.",
        ),
        name: z.string().describe("Name of the suite."),
        options: z.unknown(),
        public_id: z.string().optional().describe(
          "The public ID for the test.",
        ),
        tags: z.array(z.string()).optional().describe(
          "Array of tags attached to the suite.",
        ),
        tests: z.array(z.unknown()).describe(
          "Array of Synthetic tests included in the suite.",
        ),
        type: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const attrs: Record<string, unknown> = {};
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) attrs[k] = v;
        }
        const body = { data: { type: "suites", attributes: attrs } };

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "POST",
          `/api/v2/synthetics/suites`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "synthetics_suite",
          id,
          result,
        );
        context.logger.info("Created synthetics_suite {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    delete_synthetics_suites: {
      description: "Bulk delete suites",
      arguments: z.object({
        force_delete_dependencies: z.boolean().optional().describe(
          "Whether to force deletion of suites that have dependent resources.",
        ),
        public_ids: z.array(z.string()).describe(
          "List of public IDs of the Synthetic test suites to delete.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "POST",
          `/api/v2/synthetics/suites/bulk-delete`,
          body,
        );

        const id = (result as { id?: string }).id ?? "latest";
        const handle = await context.writeResource(
          "synthetics_suites",
          id,
          result ?? {},
        );
        context.logger.info("Executed delete_synthetics_suites", {});
        return { dataHandles: [handle] };
      },
    },
    search_suites: {
      description: "Search test suites",
      arguments: z.object({
        query: z.string().optional().describe("The search query."),
        sort: z.string().optional().describe(
          "The sort order for the results (e.g., `name,asc` or `name,desc`).",
        ),
        facets_only: z.boolean().optional().describe(
          "If true, return only facets instead of full test details.",
        ),
        start: z.number().optional().describe(
          "The offset from which to start returning results.",
        ),
        count: z.number().optional().describe(
          "The maximum number of results to return.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const queryParts: string[] = [];
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) {
            queryParts.push(
              `${encodeURIComponent(k)}=${encodeURIComponent(String(v))}`,
            );
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "GET",
          `/api/v2/synthetics/suites/search${qs}`,
        );

        const handle = await context.writeResource(
          "search_suites",
          "latest",
          result,
        );
        context.logger.info("Fetched search_suites", {});
        return { dataHandles: [handle] };
      },
    },
    get_synthetics_suite: {
      description: "Get a suite",
      arguments: z.object({
        public_id: z.string().describe(
          "The public ID of the suite to get details from.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "GET",
          `/api/v2/synthetics/suites/${
            encodeURIComponent(String(args.public_id))
          }`,
        );

        const handle = await context.writeResource(
          "synthetics_suite",
          String(args.public_id),
          result,
        );
        context.logger.info("Fetched synthetics_suite", {});
        return { dataHandles: [handle] };
      },
    },
    edit_synthetics_suite: {
      description: "Edit a test suite",
      arguments: z.object({
        public_id: z.string().describe("The public ID of the suite to edit."),
        message: z.string().optional().describe(
          "Notification message associated with the suite.",
        ),
        monitor_id: z.number().int().optional().describe(
          "The associated monitor ID.",
        ),
        name: z.string().describe("Name of the suite."),
        options: z.unknown(),
        tags: z.array(z.string()).optional().describe(
          "Array of tags attached to the suite.",
        ),
        tests: z.array(z.unknown()).describe(
          "Array of Synthetic tests included in the suite.",
        ),
        type: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const attrs: Record<string, unknown> = {};
        const excludeKeys = new Set<string>(["public_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) attrs[k] = v;
        }
        const body = { data: { type: "suites", attributes: attrs } };

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "PUT",
          `/api/v2/synthetics/suites/${
            encodeURIComponent(String(args.public_id))
          }`,
          body,
        );

        const handle = await context.writeResource(
          "edit_synthetics_suite",
          String(args.public_id),
          result,
        );
        context.logger.info("Updated edit_synthetics_suite", {});
        return { dataHandles: [handle] };
      },
    },
    patch_test_suite: {
      description: "Patch a test suite",
      arguments: z.object({
        public_id: z.string().describe(
          "The public ID of the Synthetic test suite to patch.",
        ),
        json_patch: z.array(z.unknown()).optional().describe(
          "JSON Patch operations following RFC 6902.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const attrs: Record<string, unknown> = {};
        const excludeKeys = new Set<string>(["public_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) attrs[k] = v;
        }
        const body = { data: { type: "suites_json_patch", attributes: attrs } };

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "PATCH",
          `/api/v2/synthetics/suites/${
            encodeURIComponent(String(args.public_id))
          }/jsonpatch`,
          body,
        );

        const handle = await context.writeResource(
          "patch_test_suite",
          String(args.public_id),
          result,
        );
        context.logger.info("Updated patch_test_suite", {});
        return { dataHandles: [handle] };
      },
    },
    list_synthetics_browser_test_latest_results: {
      description: "Get a browser test's latest results",
      arguments: z.object({
        public_id: z.string().describe(
          "The public ID of the Synthetic browser test for which to search results.",
        ),
        from_ts: z.number().optional().describe(
          "Timestamp in milliseconds from which to start querying results.",
        ),
        to_ts: z.number().optional().describe(
          "Timestamp in milliseconds up to which to query results.",
        ),
        status: z.string().optional().describe("Filter results by status."),
        runType: z.string().optional().describe("Filter results by run type."),
        probe_dc: z.string().optional().describe(
          "Locations for which to query results.",
        ),
        device_id: z.string().optional().describe(
          "Device IDs for which to query results.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>(["public_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await ddApiPaginated(
          apiKey,
          appKey,
          site,
          `/api/v2/synthetics/tests/browser/${
            encodeURIComponent(String(args.public_id))
          }/results`,
          { "style": "none", "limitParam": "", "limitDefault": 0 },
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "synthetics_browser_test_latest_results",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info(
          "Found {count} synthetics_browser_test_latest_results",
          { count: results.length },
        );
        return { dataHandles: [handle] };
      },
    },
    get_synthetics_browser_test_result: {
      description: "Get a browser test result",
      arguments: z.object({
        public_id: z.string().describe(
          "The public ID of the Synthetic browser test to which the target result belongs.",
        ),
        result_id: z.string().describe("The ID of the result to get."),
        event_id: z.string().optional().describe(
          "The event ID used to look up the result in the event store.",
        ),
        timestamp: z.number().optional().describe(
          "Timestamp in seconds to look up the result.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const queryParts: string[] = [];
        const excludeKeys = new Set<string>(["public_id", "result_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) {
            queryParts.push(
              `${encodeURIComponent(k)}=${encodeURIComponent(String(v))}`,
            );
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "GET",
          `/api/v2/synthetics/tests/browser/${
            encodeURIComponent(String(args.public_id))
          }/results/${encodeURIComponent(String(args.result_id))}${qs}`,
        );

        const handle = await context.writeResource(
          "synthetics_browser_test_result",
          String(args.result_id),
          result,
        );
        context.logger.info("Fetched synthetics_browser_test_result", {});
        return { dataHandles: [handle] };
      },
    },
    delete_synthetics_tests: {
      description: "Bulk delete tests",
      arguments: z.object({
        force_delete_dependencies: z.boolean().optional().describe(
          "Whether to force deletion of tests that have dependent resources.",
        ),
        public_ids: z.array(z.string()).describe(
          "List of public IDs of the Synthetic tests to delete.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "POST",
          `/api/v2/synthetics/tests/bulk-delete`,
          body,
        );

        const id = (result as { id?: string }).id ?? "latest";
        const handle = await context.writeResource(
          "synthetics_tests",
          id,
          result ?? {},
        );
        context.logger.info("Executed delete_synthetics_tests", {});
        return { dataHandles: [handle] };
      },
    },
    get_synthetics_fast_test_result: {
      description: "Get a fast test result",
      arguments: z.object({
        id: z.string().describe(
          "The UUID of the fast test to retrieve the result for.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "GET",
          `/api/v2/synthetics/tests/fast/${
            encodeURIComponent(String(args.id))
          }`,
        );

        const handle = await context.writeResource(
          "synthetics_fast_test_result",
          String(args.id),
          result,
        );
        context.logger.info("Fetched synthetics_fast_test_result", {});
        return { dataHandles: [handle] };
      },
    },
    create_synthetics_network_test: {
      description: "Create a Network Path test",
      arguments: z.object({
        config: z.unknown(),
        locations: z.array(z.string()).describe(
          "Array of locations used to run the test. Network Path tests can be run from m...",
        ),
        message: z.string().describe(
          "Notification message associated with the test.",
        ),
        monitor_id: z.number().int().optional().describe(
          "The associated monitor ID.",
        ),
        name: z.string().describe("Name of the test."),
        options: z.unknown(),
        public_id: z.string().optional().describe(
          "The public ID for the test.",
        ),
        status: z.unknown().optional(),
        subtype: z.unknown().optional(),
        tags: z.array(z.string()).optional().describe(
          "Array of tags attached to the test.",
        ),
        type: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const attrs: Record<string, unknown> = {};
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) attrs[k] = v;
        }
        const body = { data: { type: "network", attributes: attrs } };

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "POST",
          `/api/v2/synthetics/tests/network`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "synthetics_network_test",
          id,
          result,
        );
        context.logger.info("Created synthetics_network_test {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_synthetics_network_test: {
      description: "Get a Network Path test",
      arguments: z.object({
        public_id: z.string().describe(
          "The public ID of the Network Path test to get details from.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "GET",
          `/api/v2/synthetics/tests/network/${
            encodeURIComponent(String(args.public_id))
          }`,
        );

        const handle = await context.writeResource(
          "synthetics_network_test",
          String(args.public_id),
          result,
        );
        context.logger.info("Fetched synthetics_network_test", {});
        return { dataHandles: [handle] };
      },
    },
    update_synthetics_network_test: {
      description: "Edit a Network Path test",
      arguments: z.object({
        public_id: z.string().describe(
          "The public ID of the Network Path test to edit.",
        ),
        config: z.unknown(),
        locations: z.array(z.string()).describe(
          "Array of locations used to run the test. Network Path tests can be run from m...",
        ),
        message: z.string().describe(
          "Notification message associated with the test.",
        ),
        monitor_id: z.number().int().optional().describe(
          "The associated monitor ID.",
        ),
        name: z.string().describe("Name of the test."),
        options: z.unknown(),
        status: z.unknown().optional(),
        subtype: z.unknown().optional(),
        tags: z.array(z.string()).optional().describe(
          "Array of tags attached to the test.",
        ),
        type: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const attrs: Record<string, unknown> = {};
        const excludeKeys = new Set<string>(["public_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) attrs[k] = v;
        }
        const body = { data: { type: "network", attributes: attrs } };

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "PUT",
          `/api/v2/synthetics/tests/network/${
            encodeURIComponent(String(args.public_id))
          }`,
          body,
        );

        const handle = await context.writeResource(
          "synthetics_network_test",
          String(args.public_id),
          result,
        );
        context.logger.info("Updated synthetics_network_test", {});
        return { dataHandles: [handle] };
      },
    },
    poll_synthetics_test_results: {
      description: "Poll for test results",
      arguments: z.object({
        result_ids: z.string().optional().describe(
          "A JSON-encoded array of result IDs to poll for.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await ddApiPaginated(
          apiKey,
          appKey,
          site,
          `/api/v2/synthetics/tests/poll_results`,
          { "style": "none", "limitParam": "", "limitDefault": 0 },
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "poll_synthetics_test_results",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} poll_synthetics_test_results", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    get_test_file_download_url: {
      description: "Get a presigned URL for downloading a test file",
      arguments: z.object({
        public_id: z.string().describe("The public ID of the Synthetic test."),
        bucketKey: z.string().min(1).describe(
          "The bucket key referencing the file to download.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>(["public_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "POST",
          `/api/v2/synthetics/tests/${
            encodeURIComponent(String(args.public_id))
          }/files/download`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "get_test_file_download_url",
          id,
          result,
        );
        context.logger.info("Created get_test_file_download_url {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_test_file_multipart_presigned_urls: {
      description: "Get presigned URLs for uploading a test file",
      arguments: z.object({
        public_id: z.string().describe("The public ID of the Synthetic test."),
        bucketKeyPrefix: z.unknown(),
        parts: z.array(z.unknown()).describe(
          "Array of part descriptors for the multipart upload.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>(["public_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "POST",
          `/api/v2/synthetics/tests/${
            encodeURIComponent(String(args.public_id))
          }/files/multipart-presigned-urls`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "get_test_file_multipart_presigned_urls",
          id,
          result,
        );
        context.logger.info(
          "Created get_test_file_multipart_presigned_urls {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    abort_test_file_multipart_upload: {
      description: "Abort a multipart upload of a test file",
      arguments: z.object({
        public_id: z.string().describe("The public ID of the Synthetic test."),
        key: z.string().describe(
          "The full storage path of the file whose upload should be aborted.",
        ),
        uploadId: z.string().describe(
          "The upload ID of the multipart upload to abort.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>(["public_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "POST",
          `/api/v2/synthetics/tests/${
            encodeURIComponent(String(args.public_id))
          }/files/multipart-upload-abort`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "abort_test_file_multipart_upload",
          id,
          result,
        );
        context.logger.info("Created abort_test_file_multipart_upload {id}", {
          id,
        });
        return { dataHandles: [handle] };
      },
    },
    complete_test_file_multipart_upload: {
      description: "Complete a multipart upload of a test file",
      arguments: z.object({
        public_id: z.string().describe("The public ID of the Synthetic test."),
        key: z.string().describe(
          "The full storage path for the uploaded file.",
        ),
        parts: z.array(z.unknown()).describe(
          "Array of completed parts with their ETags.",
        ),
        uploadId: z.string().describe(
          "The upload ID returned when the multipart upload was initiated.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>(["public_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "POST",
          `/api/v2/synthetics/tests/${
            encodeURIComponent(String(args.public_id))
          }/files/multipart-upload-complete`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "complete_test_file_multipart_upload",
          id,
          result,
        );
        context.logger.info(
          "Created complete_test_file_multipart_upload {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    get_test_parent_suites: {
      description: "Get parent suites for a test",
      arguments: z.object({
        public_id: z.string().describe("The public ID of the Synthetic test."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>(["public_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await ddApiPaginated(
          apiKey,
          appKey,
          site,
          `/api/v2/synthetics/tests/${
            encodeURIComponent(String(args.public_id))
          }/parent-suites`,
          { "style": "none", "limitParam": "", "limitDefault": 0 },
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "test_parent_suites",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} test_parent_suites", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    list_synthetics_test_latest_results: {
      description: "Get a test's latest results",
      arguments: z.object({
        public_id: z.string().describe(
          "The public ID of the Synthetic test for which to search results.",
        ),
        from_ts: z.number().optional().describe(
          "Timestamp in milliseconds from which to start querying results.",
        ),
        to_ts: z.number().optional().describe(
          "Timestamp in milliseconds up to which to query results.",
        ),
        status: z.string().optional().describe("Filter results by status."),
        runType: z.string().optional().describe("Filter results by run type."),
        probe_dc: z.string().optional().describe(
          "Locations for which to query results.",
        ),
        device_id: z.string().optional().describe(
          "Device IDs for which to query results.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>(["public_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await ddApiPaginated(
          apiKey,
          appKey,
          site,
          `/api/v2/synthetics/tests/${
            encodeURIComponent(String(args.public_id))
          }/results`,
          { "style": "none", "limitParam": "", "limitDefault": 0 },
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "synthetics_test_latest_results",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} synthetics_test_latest_results", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    get_synthetics_test_result: {
      description: "Get a test result",
      arguments: z.object({
        public_id: z.string().describe(
          "The public ID of the Synthetic test to which the target result belongs.",
        ),
        result_id: z.string().describe("The ID of the result to get."),
        event_id: z.string().optional().describe(
          "The event ID used to look up the result in the event store.",
        ),
        timestamp: z.number().optional().describe(
          "Timestamp in seconds to look up the result.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const queryParts: string[] = [];
        const excludeKeys = new Set<string>(["public_id", "result_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) {
            queryParts.push(
              `${encodeURIComponent(k)}=${encodeURIComponent(String(v))}`,
            );
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "GET",
          `/api/v2/synthetics/tests/${
            encodeURIComponent(String(args.public_id))
          }/results/${encodeURIComponent(String(args.result_id))}${qs}`,
        );

        const handle = await context.writeResource(
          "synthetics_test_result",
          String(args.result_id),
          result,
        );
        context.logger.info("Fetched synthetics_test_result", {});
        return { dataHandles: [handle] };
      },
    },
    list_synthetics_test_versions: {
      description: "Get version history of a test",
      arguments: z.object({
        public_id: z.string().describe("The public ID of the Synthetic test."),
        last_version_number: z.number().optional().describe(
          "The version number of the last item from the previous page. Omit to get the f...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>(["public_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await ddApiPaginated(
          apiKey,
          appKey,
          site,
          `/api/v2/synthetics/tests/${
            encodeURIComponent(String(args.public_id))
          }/version_history`,
          {
            "style": "offset",
            "limitParam": "limit",
            "limitDefault": 100,
            "offsetParam": "page[offset]",
          },
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "synthetics_test_versions",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} synthetics_test_versions", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    get_synthetics_test_version: {
      description: "Get a specific version of a test",
      arguments: z.object({
        public_id: z.string().describe("The public ID of the Synthetic test."),
        version_number: z.string().describe("The version number to retrieve."),
        include_change_metadata: z.boolean().optional().describe(
          "If `true`, include change metadata in the response.",
        ),
        only_check_existence: z.boolean().optional().describe(
          "If `true`, only check whether the version exists without returning its full p...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const queryParts: string[] = [];
        const excludeKeys = new Set<string>(["public_id", "version_number"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) {
            queryParts.push(
              `${encodeURIComponent(k)}=${encodeURIComponent(String(v))}`,
            );
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "GET",
          `/api/v2/synthetics/tests/${
            encodeURIComponent(String(args.public_id))
          }/version_history/${
            encodeURIComponent(String(args.version_number))
          }${qs}`,
        );

        const handle = await context.writeResource(
          "synthetics_test_version",
          String(args.version_number),
          result,
        );
        context.logger.info("Fetched synthetics_test_version", {});
        return { dataHandles: [handle] };
      },
    },
    patch_global_variable: {
      description: "Patch a global variable",
      arguments: z.object({
        variable_id: z.string().describe("The ID of the global variable."),
        json_patch: z.array(z.unknown()).optional().describe(
          "JSON Patch operations following RFC 6902.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const attrs: Record<string, unknown> = {};
        const excludeKeys = new Set<string>(["variable_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) attrs[k] = v;
        }
        const body = {
          data: { type: "global_variables_json_patch", attributes: attrs },
        };

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "PATCH",
          `/api/v2/synthetics/variables/${
            encodeURIComponent(String(args.variable_id))
          }/jsonpatch`,
          body,
        );

        const handle = await context.writeResource(
          "patch_global_variable",
          String(args.variable_id),
          result,
        );
        context.logger.info("Updated patch_global_variable", {});
        return { dataHandles: [handle] };
      },
    },
  },
};
