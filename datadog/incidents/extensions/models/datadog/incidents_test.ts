// Datadog Incidents — incident lifecycle, timelines, teams, and attachments — Tests
// Auto-generated by scripts/datadog-codegen — do not edit manually.
// SPDX-License-Identifier: Apache-2.0

import { assertEquals, assertExists } from "jsr:@std/assert@1";
import { createModelTestContext } from "@systeminit/swamp-testing";
import { model } from "./incidents.ts";

// ---------------------------------------------------------------------------
// Model Export Structure Tests
// ---------------------------------------------------------------------------

Deno.test("incidents model: has correct type", () => {
  assertEquals(model.type, "@webframp/datadog/incidents");
});

Deno.test("incidents model: has valid version format", () => {
  const versionPattern = /^\d{4}\.\d{2}\.\d{2}\.\d+$/;
  assertEquals(versionPattern.test(model.version), true);
});

Deno.test("incidents model: has globalArguments with apiKey and appKey", () => {
  assertExists(model.globalArguments);
  const shape = model.globalArguments.shape;
  assertExists(shape.apiKey);
  assertExists(shape.appKey);
  assertExists(shape.site);
});

Deno.test("incidents model: has expected methods", () => {
  assertExists(model.methods);
  assertExists(model.methods.list_incident_impacts);
  assertExists(model.methods.create_incident_impact);
  assertExists(model.methods.delete_incident_impact);
});

Deno.test("incidents model: has expected resources", () => {
  assertExists(model.resources);
  assertExists(model.resources["incident_impacts"]);
  assertExists(model.resources["incident_impact"]);
});

// ---------------------------------------------------------------------------
// Mock Datadog API Server
// ---------------------------------------------------------------------------

function startMockDdServer(
  responses: Record<string, { body: unknown; status?: number }>,
): { url: string; server: Deno.HttpServer } {
  const server = Deno.serve({ port: 0, onListen() {} }, (req) => {
    const url = new URL(req.url);
    const path = url.pathname;

    for (const [pattern, { body, status }] of Object.entries(responses)) {
      if (path.includes(pattern)) {
        return Response.json(body, { status: status ?? 200 });
      }
    }

    return Response.json(
      { errors: [{ status: "404", title: "Not found" }] },
      { status: 404 },
    );
  });

  const addr = server.addr as Deno.NetAddr;
  return { url: `http://localhost:${addr.port}`, server };
}

function installFetchMock(mockUrl: string): () => void {
  const originalFetch = globalThis.fetch;
  globalThis.fetch = (input, init) => {
    const reqUrl = typeof input === "string" ? input : input.toString();
    // Replace any DD site URL with the mock
    const newUrl = reqUrl
      .replace("https://api.datadoghq.com", mockUrl)
      .replace("https://us3.datadoghq.com", mockUrl)
      .replace("https://us5.datadoghq.com", mockUrl)
      .replace("https://api.datadoghq.eu", mockUrl)
      .replace("https://ap1.datadoghq.com", mockUrl)
      .replace("https://api.ddog-gov.com", mockUrl);
    return originalFetch(newUrl, init);
  };
  return () => {
    globalThis.fetch = originalFetch;
  };
}

Deno.test({
  name: "incidents model: list_incident_impacts fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockDdServer({
      "/test-id-123/impacts": {
        body: {
          "data": [{
            "id": "fixture-123",
            "type": "resource",
            "attributes": {
              "created": "2025-08-29T13:17:00Z",
              "description": "Service was unavailable for external users",
              "end_at": "2025-08-29T13:17:00Z",
              "fields": {
                "customers_impacted": "all",
                "products_impacted": ["shopping", "marketing"],
              },
              "impact_type": "customer",
              "modified": "2025-08-29T13:17:00Z",
              "start_at": "2025-08-28T13:17:00Z",
              "created_by_user_id": "test-value",
              "incident_id": "test-value",
              "last_modified_by_user_id": "test-value",
            },
          }],
          "meta": { "page": {} },
        },
      },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: {
          "apiKey": "test-api-key",
          "appKey": "test-app-key",
          "site": "us1",
        },
        definition: {
          id: "test-id",
          name: "test-incidents",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).list_incident_impacts.execute(
        { "incident_id": "test-id-123" },
        context,
      );
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "incidents model: create_incident_impact creates and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockDdServer({
      "/test-id-123/impacts": {
        body: {
          "data": {
            "id": "new-123",
            "type": "resource",
            "attributes": {
              "created": "2025-08-29T13:17:00Z",
              "description": "Service was unavailable for external users",
              "end_at": "2025-08-29T13:17:00Z",
              "fields": {
                "customers_impacted": "all",
                "products_impacted": ["shopping", "marketing"],
              },
              "impact_type": "customer",
              "modified": "2025-08-29T13:17:00Z",
              "start_at": "2025-08-28T13:17:00Z",
              "created_by_user_id": "test-value",
              "incident_id": "test-value",
              "last_modified_by_user_id": "test-value",
            },
          },
        },
      },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: {
          "apiKey": "test-api-key",
          "appKey": "test-app-key",
          "site": "us1",
        },
        definition: {
          id: "test-id",
          name: "test-incidents",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).create_incident_impact.execute({
        "incident_id": "test-id-123",
        "name": "test-resource",
      }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "incidents model: delete_incident_impact executes successfully",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockDdServer({
      "/impacts/test-id-123": { body: {}, status: 204 },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context } = createModelTestContext({
        globalArgs: {
          "apiKey": "test-api-key",
          "appKey": "test-app-key",
          "site": "us1",
        },
        definition: {
          id: "test-id",
          name: "test-incidents",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).delete_incident_impact.execute({
        "incident_id": "test-id-123",
        "impact_id": "test-id-123",
      }, context);
      assertEquals(result.dataHandles.length, 0);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});
