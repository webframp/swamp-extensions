/**
 * Datadog SLOs — service level objective definitions, status, and history
 *
 * Auto-generated by scripts/datadog-codegen — do not edit manually.
 *
 * @module
 */
// SPDX-License-Identifier: Apache-2.0

import { z } from "npm:zod@4.4.3";
import { ddApi, sanitizeInstanceName } from "./_lib/api.ts";

// =============================================================================
// Schemas
// =============================================================================

const GlobalArgsSchema = z.object({
  apiKey: z.string().meta({ sensitive: true }).describe(
    "Datadog API key (DD-API-KEY)",
  ),
  appKey: z.string().meta({ sensitive: true }).describe(
    "Datadog application key (DD-APPLICATION-KEY)",
  ),
  site: z.enum(["us1", "us3", "us5", "eu1", "ap1", "us1-fed"]).default("us1")
    .describe("Datadog site"),
});

const GetSloStatusSchema = z.object({
  id: z.string().describe("The ID of the SLO."),
  type: z.enum(["slo_status"]).optional().describe(
    "The type of the SLO status resource.",
  ),
  error_budget_remaining: z.number().describe(
    "The percentage of error budget remaining.",
  ),
  raw_error_budget_remaining: z.object({
    unit: z.string(),
    value: z.number(),
  }).describe("The raw error budget remaining for the SLO."),
  sli: z.number().describe(
    "The current Service Level Indicator (SLI) value as a percentage.",
  ),
  span_precision: z.number().int().describe(
    "The precision of the time span in seconds.",
  ),
  state: z.string().describe(
    "The current state of the SLO (for example, `breached`, `warning`, `ok`).",
  ),
}).passthrough();

// =============================================================================
// Model Definition
// =============================================================================

/** Datadog SLOs — service level objective definitions, status, and history */
export const model = {
  type: "@webframp/datadog/slos",
  version: "2026.08.28.2",
  globalArguments: GlobalArgsSchema,

  upgrades: [
    {
      toVersion: "2026.08.24.1",
      description:
        "Added optional durationMs, collectedBy, and fetchedAt output metadata fields",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.08.26.1",
      description:
        "No schema changes — restored inline npm:zod specifier for registry scoring; retained strict mode",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.08.26.2",
      description:
        "No schema changes — restored inline npm:zod specifier for registry scoring; retained strict mode",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.08.28.1",
      description:
        "No schema changes — normalized license to Apache-2.0 and corrected copyright holder to Sean Escriva",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.08.28.2",
      description: "Regenerated from updated API spec; no migration required",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
  ],

  resources: {
    "slo_status": {
      description: "Get SLO status",
      schema: GetSloStatusSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
  },

  methods: {
    get_slo_status: {
      description: "Get SLO status",
      arguments: z.object({
        slo_id: z.string().describe("The ID of the SLO."),
        from_ts: z.number().optional().describe(
          "The starting timestamp for the SLO status query in epoch seconds.",
        ),
        to_ts: z.number().optional().describe(
          "The ending timestamp for the SLO status query in epoch seconds.",
        ),
        disable_corrections: z.boolean().optional().describe(
          "Whether to exclude correction windows from the SLO status calculation. Defaul...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const queryParts: string[] = [];
        const excludeKeys = new Set<string>(["slo_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) {
            const apiName = k;
            queryParts.push(
              `${encodeURIComponent(apiName)}=${encodeURIComponent(String(v))}`,
            );
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "GET",
          `/api/v2/slo/${encodeURIComponent(String(args.slo_id))}/status${qs}`,
        );

        const handle = await context.writeResource(
          "slo_status",
          sanitizeInstanceName(String(args.slo_id)),
          result,
        );
        context.logger.info("Fetched slo_status", {});
        return { dataHandles: [handle] };
      },
    },
  },
};
