/**
 * Datadog Security Suppressions — suppression rule management
 *
 * Auto-generated by scripts/datadog-codegen — do not edit manually.
 *
 * @module
 */
// SPDX-License-Identifier: AGPL-3.0-or-later WITH Swamp-Extension-Exception

import { z } from "npm:zod@4.4.3";
import { ddApi, ddApiPaginated } from "./_lib/api.ts";

// =============================================================================
// Schemas
// =============================================================================

const GlobalArgsSchema = z.object({
  apiKey: z.string().meta({ sensitive: true }).describe(
    "Datadog API key (DD-API-KEY)",
  ),
  appKey: z.string().meta({ sensitive: true }).describe(
    "Datadog application key (DD-APPLICATION-KEY)",
  ),
  site: z.enum(["us1", "us3", "us5", "eu1", "ap1", "us1-fed"]).default("us1")
    .describe("Datadog site"),
});

const SecurityMonitoringSuppressionsItemSchema = z.object({
  id: z.string().describe("The ID of the suppression rule."),
  type: z.enum(["suppressions"]).optional().describe(
    "The type of the resource. The value should always be `suppressions`.",
  ),
  creation_date: z.number().int().optional().describe(
    "A Unix millisecond timestamp given the creation date of the suppression rule.",
  ),
  creator: z.object({
    handle: z.string().optional(),
    name: z.string().nullable().optional(),
  }).optional().describe("A user."),
  data_exclusion_query: z.string().optional().describe(
    "An exclusion query on the input data of the security rules, which could be logs, Agent events, or...",
  ),
  description: z.string().optional().describe(
    "A description for the suppression rule.",
  ),
  editable: z.boolean().optional().describe(
    "Whether the suppression rule is editable.",
  ),
  enabled: z.boolean().optional().describe(
    "Whether the suppression rule is enabled.",
  ),
  expiration_date: z.number().int().optional().describe(
    "A Unix millisecond timestamp giving an expiration date for the suppression rule. After this date,...",
  ),
  name: z.string().optional().describe("The name of the suppression rule."),
  rule_query: z.string().optional().describe(
    "The rule query of the suppression rule, with the same syntax as the search bar for detection rules.",
  ),
  start_date: z.number().int().optional().describe(
    "A Unix millisecond timestamp giving the start date for the suppression rule. After this date, it ...",
  ),
  suppression_query: z.string().optional().describe(
    "The suppression query of the suppression rule. If a signal matches this query, it is suppressed a...",
  ),
  tags: z.array(z.string()).optional().describe(
    "List of tags associated with the suppression rule.",
  ),
  update_date: z.number().int().optional().describe(
    "A Unix millisecond timestamp given the update date of the suppression rule.",
  ),
  updater: z.object({
    handle: z.string().optional(),
    name: z.string().nullable().optional(),
  }).optional().describe("A user."),
  version: z.number().int().max(2147483647).optional().describe(
    "The version of the suppression rule; it starts at 1, and is incremented at each update.",
  ),
});

const ListSecurityMonitoringSuppressionsSchema = z.object({
  items: z.array(SecurityMonitoringSuppressionsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateSecurityMonitoringSuppressionSchema = z.object({
  id: z.string().describe("The ID of the suppression rule."),
  type: z.enum(["suppressions"]).optional().describe(
    "The type of the resource. The value should always be `suppressions`.",
  ),
  creation_date: z.number().int().optional().describe(
    "A Unix millisecond timestamp given the creation date of the suppression rule.",
  ),
  creator: z.object({
    handle: z.string().optional(),
    name: z.string().nullable().optional(),
  }).optional().describe("A user."),
  data_exclusion_query: z.string().optional().describe(
    "An exclusion query on the input data of the security rules, which could be logs, Agent events, or...",
  ),
  description: z.string().optional().describe(
    "A description for the suppression rule.",
  ),
  editable: z.boolean().optional().describe(
    "Whether the suppression rule is editable.",
  ),
  enabled: z.boolean().optional().describe(
    "Whether the suppression rule is enabled.",
  ),
  expiration_date: z.number().int().optional().describe(
    "A Unix millisecond timestamp giving an expiration date for the suppression rule. After this date,...",
  ),
  name: z.string().optional().describe("The name of the suppression rule."),
  rule_query: z.string().optional().describe(
    "The rule query of the suppression rule, with the same syntax as the search bar for detection rules.",
  ),
  start_date: z.number().int().optional().describe(
    "A Unix millisecond timestamp giving the start date for the suppression rule. After this date, it ...",
  ),
  suppression_query: z.string().optional().describe(
    "The suppression query of the suppression rule. If a signal matches this query, it is suppressed a...",
  ),
  tags: z.array(z.string()).optional().describe(
    "List of tags associated with the suppression rule.",
  ),
  update_date: z.number().int().optional().describe(
    "A Unix millisecond timestamp given the update date of the suppression rule.",
  ),
  updater: z.object({
    handle: z.string().optional(),
    name: z.string().nullable().optional(),
  }).optional().describe("A user."),
  version: z.number().int().max(2147483647).optional().describe(
    "The version of the suppression rule; it starts at 1, and is incremented at each update.",
  ),
});

const GetSuppressionsAffectingFutureRuleSchema = z.object({
  id: z.string().describe("The ID of the suppression rule."),
  type: z.enum(["suppressions"]).optional().describe(
    "The type of the resource. The value should always be `suppressions`.",
  ),
  creation_date: z.number().int().optional().describe(
    "A Unix millisecond timestamp given the creation date of the suppression rule.",
  ),
  creator: z.object({
    handle: z.string().optional(),
    name: z.string().nullable().optional(),
  }).optional().describe("A user."),
  data_exclusion_query: z.string().optional().describe(
    "An exclusion query on the input data of the security rules, which could be logs, Agent events, or...",
  ),
  description: z.string().optional().describe(
    "A description for the suppression rule.",
  ),
  editable: z.boolean().optional().describe(
    "Whether the suppression rule is editable.",
  ),
  enabled: z.boolean().optional().describe(
    "Whether the suppression rule is enabled.",
  ),
  expiration_date: z.number().int().optional().describe(
    "A Unix millisecond timestamp giving an expiration date for the suppression rule. After this date,...",
  ),
  name: z.string().optional().describe("The name of the suppression rule."),
  rule_query: z.string().optional().describe(
    "The rule query of the suppression rule, with the same syntax as the search bar for detection rules.",
  ),
  start_date: z.number().int().optional().describe(
    "A Unix millisecond timestamp giving the start date for the suppression rule. After this date, it ...",
  ),
  suppression_query: z.string().optional().describe(
    "The suppression query of the suppression rule. If a signal matches this query, it is suppressed a...",
  ),
  tags: z.array(z.string()).optional().describe(
    "List of tags associated with the suppression rule.",
  ),
  update_date: z.number().int().optional().describe(
    "A Unix millisecond timestamp given the update date of the suppression rule.",
  ),
  updater: z.object({
    handle: z.string().optional(),
    name: z.string().nullable().optional(),
  }).optional().describe("A user."),
  version: z.number().int().max(2147483647).optional().describe(
    "The version of the suppression rule; it starts at 1, and is incremented at each update.",
  ),
});

const SuppressionsAffectingRuleItemSchema = z.object({
  id: z.string().describe("The ID of the suppression rule."),
  type: z.enum(["suppressions"]).optional().describe(
    "The type of the resource. The value should always be `suppressions`.",
  ),
  creation_date: z.number().int().optional().describe(
    "A Unix millisecond timestamp given the creation date of the suppression rule.",
  ),
  creator: z.object({
    handle: z.string().optional(),
    name: z.string().nullable().optional(),
  }).optional().describe("A user."),
  data_exclusion_query: z.string().optional().describe(
    "An exclusion query on the input data of the security rules, which could be logs, Agent events, or...",
  ),
  description: z.string().optional().describe(
    "A description for the suppression rule.",
  ),
  editable: z.boolean().optional().describe(
    "Whether the suppression rule is editable.",
  ),
  enabled: z.boolean().optional().describe(
    "Whether the suppression rule is enabled.",
  ),
  expiration_date: z.number().int().optional().describe(
    "A Unix millisecond timestamp giving an expiration date for the suppression rule. After this date,...",
  ),
  name: z.string().optional().describe("The name of the suppression rule."),
  rule_query: z.string().optional().describe(
    "The rule query of the suppression rule, with the same syntax as the search bar for detection rules.",
  ),
  start_date: z.number().int().optional().describe(
    "A Unix millisecond timestamp giving the start date for the suppression rule. After this date, it ...",
  ),
  suppression_query: z.string().optional().describe(
    "The suppression query of the suppression rule. If a signal matches this query, it is suppressed a...",
  ),
  tags: z.array(z.string()).optional().describe(
    "List of tags associated with the suppression rule.",
  ),
  update_date: z.number().int().optional().describe(
    "A Unix millisecond timestamp given the update date of the suppression rule.",
  ),
  updater: z.object({
    handle: z.string().optional(),
    name: z.string().nullable().optional(),
  }).optional().describe("A user."),
  version: z.number().int().max(2147483647).optional().describe(
    "The version of the suppression rule; it starts at 1, and is incremented at each update.",
  ),
});

const GetSuppressionsAffectingRuleSchema = z.object({
  items: z.array(SuppressionsAffectingRuleItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const SuppressionVersionHistoryItemSchema = z.object({
  id: z.string().describe("ID of the suppression."),
  type: z.enum(["suppression_version_history"]).optional().describe(
    "Type of data.",
  ),
  count: z.number().int().max(2147483647).optional().describe(
    "The number of suppression versions.",
  ),
  data: z.record(z.string(), z.unknown()).optional().describe(
    "The version history of a suppression.",
  ),
});

const GetSuppressionVersionHistorySchema = z.object({
  items: z.array(SuppressionVersionHistoryItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

// =============================================================================
// Model Definition
// =============================================================================

/** Datadog Security Suppressions — suppression rule management */
export const model = {
  type: "@webframp/datadog/security-suppressions",
  version: "2026.07.20.1",
  globalArguments: GlobalArgsSchema,

  upgrades: [],

  resources: {
    "security_monitoring_suppressions": {
      description: "Get all suppression rules",
      schema: ListSecurityMonitoringSuppressionsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "security_monitoring_suppression": {
      description: "Create a suppression rule",
      schema: CreateSecurityMonitoringSuppressionSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "suppressions_affecting_future_rule": {
      description: "Get suppressions affecting future rule",
      schema: GetSuppressionsAffectingFutureRuleSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "suppressions_affecting_rule": {
      description: "Get suppressions affecting a specific rule",
      schema: GetSuppressionsAffectingRuleSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "validate_security_monitoring_suppression": {
      description: "Validate a suppression rule",
      schema: z.object({}),
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "suppression_version_history": {
      description: "Get a suppression's version history",
      schema: GetSuppressionVersionHistorySchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
  },

  methods: {
    list_security_monitoring_suppressions: {
      description: "Get all suppression rules",
      arguments: z.object({
        query: z.string().optional().describe("Query string."),
        sort: z.string().optional().describe(
          "Attribute used to sort the list of suppression rules. Prefix with `-` to sort...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await ddApiPaginated(
          apiKey,
          appKey,
          site,
          `/api/v2/security_monitoring/configuration/suppressions`,
          {
            "style": "page_number",
            "limitParam": "page[size]",
            "limitDefault": 50,
            "pageParam": "page[number]",
          },
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "security_monitoring_suppressions",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} security_monitoring_suppressions", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_security_monitoring_suppression: {
      description: "Create a suppression rule",
      arguments: z.object({
        data_exclusion_query: z.string().optional().describe(
          "An exclusion query on the input data of the security rules, which could be lo...",
        ),
        description: z.string().optional().describe(
          "A description for the suppression rule.",
        ),
        enabled: z.boolean().describe(
          "Whether the suppression rule is enabled.",
        ),
        expiration_date: z.number().int().optional().describe(
          "A Unix millisecond timestamp giving an expiration date for the suppression ru...",
        ),
        name: z.string().describe("The name of the suppression rule."),
        rule_query: z.string().describe(
          "The rule query of the suppression rule, with the same syntax as the search ba...",
        ),
        start_date: z.number().int().optional().describe(
          "A Unix millisecond timestamp giving the start date for the suppression rule. ...",
        ),
        suppression_query: z.string().optional().describe(
          "The suppression query of the suppression rule. If a signal matches this query...",
        ),
        tags: z.array(z.string()).optional().describe(
          "List of tags associated with the suppression rule.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const attrs: Record<string, unknown> = {};
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) attrs[k] = v;
        }
        const body = { data: { type: "suppressions", attributes: attrs } };

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "POST",
          `/api/v2/security_monitoring/configuration/suppressions`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "security_monitoring_suppression",
          id,
          result,
        );
        context.logger.info("Created security_monitoring_suppression {id}", {
          id,
        });
        return { dataHandles: [handle] };
      },
    },
    get_suppressions_affecting_future_rule: {
      description: "Get suppressions affecting future rule",
      arguments: z.object({
        calculatedFields: z.array(z.unknown()).optional().describe(
          "Calculated fields. Only allowed for scheduled rules - in other words, when sc...",
        ),
        cases: z.array(z.unknown()).describe("Cases for generating signals."),
        filters: z.array(z.unknown()).optional().describe(
          "Additional queries to filter matched events before they are processed. This f...",
        ),
        groupSignalsBy: z.array(z.string()).optional().describe(
          "Additional grouping to perform on top of the existing groups in the query sec...",
        ),
        hasExtendedTitle: z.boolean().optional().describe(
          "Whether the notifications include the triggering group-by values in their title.",
        ),
        isEnabled: z.boolean().describe("Whether the rule is enabled."),
        message: z.string().describe("Message for generated signals."),
        name: z.string().describe("The name of the rule."),
        options: z.unknown(),
        queries: z.array(z.unknown()).describe(
          "Queries for selecting logs which are part of the rule.",
        ),
        referenceTables: z.array(z.unknown()).optional().describe(
          "Reference tables for the rule.",
        ),
        schedulingOptions: z.unknown().optional(),
        tags: z.array(z.string()).optional().describe(
          "Tags for generated signals.",
        ),
        thirdPartyCases: z.array(z.unknown()).optional().describe(
          "Cases for generating signals from third-party rules. Only available for third...",
        ),
        type: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "POST",
          `/api/v2/security_monitoring/configuration/suppressions/rules`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "get_suppressions_affecting_future_rule",
          id,
          result,
        );
        context.logger.info(
          "Created get_suppressions_affecting_future_rule {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    get_suppressions_affecting_rule: {
      description: "Get suppressions affecting a specific rule",
      arguments: z.object({
        rule_id: z.string().describe("The ID of the rule."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>(["rule_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await ddApiPaginated(
          apiKey,
          appKey,
          site,
          `/api/v2/security_monitoring/configuration/suppressions/rules/${
            encodeURIComponent(String(args.rule_id))
          }`,
          { "style": "none", "limitParam": "", "limitDefault": 0 },
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "suppressions_affecting_rule",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} suppressions_affecting_rule", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    validate_security_monitoring_suppression: {
      description: "Validate a suppression rule",
      arguments: z.object({
        data_exclusion_query: z.string().optional().describe(
          "An exclusion query on the input data of the security rules, which could be lo...",
        ),
        description: z.string().optional().describe(
          "A description for the suppression rule.",
        ),
        enabled: z.boolean().describe(
          "Whether the suppression rule is enabled.",
        ),
        expiration_date: z.number().int().optional().describe(
          "A Unix millisecond timestamp giving an expiration date for the suppression ru...",
        ),
        name: z.string().describe("The name of the suppression rule."),
        rule_query: z.string().describe(
          "The rule query of the suppression rule, with the same syntax as the search ba...",
        ),
        start_date: z.number().int().optional().describe(
          "A Unix millisecond timestamp giving the start date for the suppression rule. ...",
        ),
        suppression_query: z.string().optional().describe(
          "The suppression query of the suppression rule. If a signal matches this query...",
        ),
        tags: z.array(z.string()).optional().describe(
          "List of tags associated with the suppression rule.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const attrs: Record<string, unknown> = {};
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) attrs[k] = v;
        }
        const body = { data: { type: "suppressions", attributes: attrs } };

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "POST",
          `/api/v2/security_monitoring/configuration/suppressions/validation`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "validate_security_monitoring_suppression",
          id,
          result,
        );
        context.logger.info(
          "Created validate_security_monitoring_suppression {id}",
          { id },
        );
        return { dataHandles: [handle] };
      },
    },
    get_security_monitoring_suppression: {
      description: "Get a suppression rule",
      arguments: z.object({
        suppression_id: z.string().describe("The ID of the suppression rule"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "GET",
          `/api/v2/security_monitoring/configuration/suppressions/${
            encodeURIComponent(String(args.suppression_id))
          }`,
        );

        const handle = await context.writeResource(
          "security_monitoring_suppression",
          String(args.suppression_id),
          result,
        );
        context.logger.info("Fetched security_monitoring_suppression", {});
        return { dataHandles: [handle] };
      },
    },
    update_security_monitoring_suppression: {
      description: "Update a suppression rule",
      arguments: z.object({
        suppression_id: z.string().describe("The ID of the suppression rule"),
        data_exclusion_query: z.string().optional().describe(
          "An exclusion query on the input data of the security rules, which could be lo...",
        ),
        description: z.string().optional().describe(
          "A description for the suppression rule.",
        ),
        enabled: z.boolean().optional().describe(
          "Whether the suppression rule is enabled.",
        ),
        expiration_date: z.number().int().nullable().optional().describe(
          "A Unix millisecond timestamp giving an expiration date for the suppression ru...",
        ),
        name: z.string().optional().describe(
          "The name of the suppression rule.",
        ),
        rule_query: z.string().optional().describe(
          "The rule query of the suppression rule, with the same syntax as the search ba...",
        ),
        start_date: z.number().int().nullable().optional().describe(
          "A Unix millisecond timestamp giving the start date for the suppression rule. ...",
        ),
        suppression_query: z.string().optional().describe(
          "The suppression query of the suppression rule. If a signal matches this query...",
        ),
        tags: z.array(z.string()).optional().describe(
          "List of tags associated with the suppression rule.",
        ),
        version: z.number().int().max(2147483647).optional().describe(
          "The current version of the suppression. This is optional, but it can help pre...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const attrs: Record<string, unknown> = {};
        const excludeKeys = new Set<string>(["suppression_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) attrs[k] = v;
        }
        const body = { data: { type: "suppressions", attributes: attrs } };

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "PATCH",
          `/api/v2/security_monitoring/configuration/suppressions/${
            encodeURIComponent(String(args.suppression_id))
          }`,
          body,
        );

        const handle = await context.writeResource(
          "security_monitoring_suppression",
          String(args.suppression_id),
          result,
        );
        context.logger.info("Updated security_monitoring_suppression", {});
        return { dataHandles: [handle] };
      },
    },
    delete_security_monitoring_suppression: {
      description: "Delete a suppression rule",
      arguments: z.object({
        suppression_id: z.string().describe("The ID of the suppression rule"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        await ddApi(
          apiKey,
          appKey,
          site,
          "DELETE",
          `/api/v2/security_monitoring/configuration/suppressions/${
            encodeURIComponent(String(args.suppression_id))
          }`,
        );

        context.logger.info("Deleted resource {id}", {
          id: args.suppression_id,
        });
        return { dataHandles: [] };
      },
    },
    get_suppression_version_history: {
      description: "Get a suppression's version history",
      arguments: z.object({
        suppression_id: z.string().describe("The ID of the suppression rule"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>(["suppression_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await ddApiPaginated(
          apiKey,
          appKey,
          site,
          `/api/v2/security_monitoring/configuration/suppressions/${
            encodeURIComponent(String(args.suppression_id))
          }/version_history`,
          {
            "style": "page_number",
            "limitParam": "page[size]",
            "limitDefault": 50,
            "pageParam": "page[number]",
          },
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "suppression_version_history",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} suppression_version_history", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
  },
};
