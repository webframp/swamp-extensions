/**
 * Datadog Metrics — metric queries, submissions, tag configurations, and metadata
 *
 * Auto-generated by scripts/datadog-codegen — do not edit manually.
 *
 * @module
 */
// SPDX-License-Identifier: AGPL-3.0-or-later WITH Swamp-Extension-Exception

import { z } from "npm:zod@4.4.3";
import { ddApi, ddApiPaginated } from "./_lib/api.ts";

// =============================================================================
// Schemas
// =============================================================================

const GlobalArgsSchema = z.object({
  apiKey: z.string().meta({ sensitive: true }).describe(
    "Datadog API key (DD-API-KEY)",
  ),
  appKey: z.string().meta({ sensitive: true }).describe(
    "Datadog application key (DD-APPLICATION-KEY)",
  ),
  site: z.enum(["us1", "us3", "us5", "eu1", "ap1", "us1-fed"]).default("us1")
    .describe("Datadog site"),
});

const TagConfigurationsItemSchema = z.union([z.unknown(), z.unknown()]);

const ListTagConfigurationsSchema = z.object({
  items: z.array(TagConfigurationsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const ListActiveMetricConfigurationsSchema = z.object({
  id: z.string().describe("The metric name for this resource."),
  type: z.enum(["actively_queried_configurations"]).optional().describe(
    "The metric actively queried configuration resource type.",
  ),
  active_aggregations: z.array(z.unknown()).optional().describe(
    "List of aggregation combinations that have been actively queried.",
  ),
  active_tags: z.array(z.string()).optional().describe(
    "List of tag keys that have been actively queried.",
  ),
});

const TagsByMetricNameItemSchema = z.object({
  id: z.string().describe("The metric name for this resource."),
  type: z.enum(["metrics"]).optional().describe("The metric resource type."),
  ingested_tags: z.array(z.string()).optional().describe(
    "List of ingested tags that are not indexed.",
  ),
  tags: z.array(z.string()).optional().describe("List of indexed tags."),
});

const ListTagsByMetricNameSchema = z.object({
  items: z.array(TagsByMetricNameItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const ListMetricAssetsSchema = z.object({
  id: z.unknown(),
  relationships: z.unknown().optional(),
  type: z.unknown(),
});

const EstimateMetricsOutputSeriesSchema = z.object({
  id: z.string().describe("The metric name for this resource."),
  type: z.enum(["metric_cardinality_estimate"]).optional().describe(
    "The metric estimate resource type.",
  ),
  estimate_type: z.enum(["count_or_gauge", "distribution", "percentile"])
    .optional().describe(
      "Estimate type based on the queried configuration. By default, `count_or_gauge` is returned. `dist...",
    ),
  estimated_at: z.string().optional().describe(
    "Timestamp when the cardinality estimate was requested.",
  ),
  estimated_output_series: z.number().int().optional().describe(
    "Estimated cardinality of the metric based on the queried configuration.",
  ),
});

const MetricTagCardinalityDetailsItemSchema = z.object({
  id: z.string().describe("The name of the tag key."),
  type: z.string().optional().describe("This describes the endpoint action."),
  cardinality_delta: z.number().int().optional().describe(
    "This describes the recent change in the tag keys cardinality",
  ),
});

const GetMetricTagCardinalityDetailsSchema = z.object({
  items: z.array(MetricTagCardinalityDetailsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const ListTagConfigurationByNameSchema = z.object({
  id: z.string().describe("The metric name for this resource."),
  type: z.enum(["manage_tags"]).optional().describe(
    "The metric tag configuration resource type.",
  ),
  aggregations: z.array(z.unknown()).optional().describe(
    "Deprecated. You no longer need to configure specific time and space aggregations for Metrics With...",
  ),
  created_at: z.string().optional().describe(
    "Timestamp when the tag configuration was created.",
  ),
  exclude_tags_mode: z.boolean().optional().describe(
    "When set to true, the configuration will exclude the configured tags and include any other submit...",
  ),
  include_percentiles: z.boolean().optional().describe(
    "Toggle to include or exclude percentile aggregations for distribution metrics. Only present when ...",
  ),
  metric_type: z.enum(["gauge", "count", "rate", "distribution"]).optional()
    .describe("The metric's type."),
  modified_at: z.string().optional().describe(
    "Timestamp when the tag configuration was last modified.",
  ),
  tags: z.array(z.string()).optional().describe(
    "List of tag keys on which to group.",
  ),
  metric_volumes_id: z.string().optional().describe(
    "Related metric_volumes ID",
  ),
});

const CreateTagConfigurationSchema = z.object({
  id: z.string().describe("The metric name for this resource."),
  type: z.enum(["manage_tags"]).optional().describe(
    "The metric tag configuration resource type.",
  ),
  aggregations: z.array(z.unknown()).optional().describe(
    "Deprecated. You no longer need to configure specific time and space aggregations for Metrics With...",
  ),
  created_at: z.string().optional().describe(
    "Timestamp when the tag configuration was created.",
  ),
  exclude_tags_mode: z.boolean().optional().describe(
    "When set to true, the configuration will exclude the configured tags and include any other submit...",
  ),
  include_percentiles: z.boolean().optional().describe(
    "Toggle to include or exclude percentile aggregations for distribution metrics. Only present when ...",
  ),
  metric_type: z.enum(["gauge", "count", "rate", "distribution"]).optional()
    .describe("The metric's type."),
  modified_at: z.string().optional().describe(
    "Timestamp when the tag configuration was last modified.",
  ),
  tags: z.array(z.string()).optional().describe(
    "List of tag keys on which to group.",
  ),
  metric_volumes_id: z.string().optional().describe(
    "Related metric_volumes ID",
  ),
});

const ListVolumesByMetricNameSchema = z.object({
  id: z.string().describe("The metric name for this resource."),
  type: z.enum(["distinct_metric_volumes"]).optional().describe(
    "The metric distinct volume type.",
  ),
  distinct_volume: z.number().int().optional().describe(
    "Distinct volume for the given metric.",
  ),
});

const QueryScalarDataSchema = z.object({
  attributes: z.unknown().optional(),
  type: z.unknown().optional(),
});

const QueryTimeseriesDataSchema = z.object({
  attributes: z.unknown().optional(),
  type: z.unknown().optional(),
});

const SubmitMetricsSchema = z.object({
  errors: z.array(z.string()).optional().describe("A list of errors."),
});

// =============================================================================
// Model Definition
// =============================================================================

/** Datadog Metrics — metric queries, submissions, tag configurations, and metadata */
export const model = {
  type: "@webframp/datadog/metrics",
  version: "2026.07.20.3",
  globalArguments: GlobalArgsSchema,

  upgrades: [],

  resources: {
    "tag_configurations": {
      description: "Get a list of metrics",
      schema: ListTagConfigurationsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "list_active_metric_configurations": {
      description: "List active tags and aggregations",
      schema: ListActiveMetricConfigurationsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "tags_by_metric_name": {
      description: "List tags by metric name",
      schema: ListTagsByMetricNameSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "list_metric_assets": {
      description: "Related Assets to a Metric",
      schema: ListMetricAssetsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "estimate_metrics_output_series": {
      description: "Tag Configuration Cardinality Estimator",
      schema: EstimateMetricsOutputSeriesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "metric_tag_cardinality_details": {
      description: "Get tag key cardinality details",
      schema: GetMetricTagCardinalityDetailsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "list_tag_configuration_by_name": {
      description: "List tag configuration by name",
      schema: ListTagConfigurationByNameSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "tag_configuration": {
      description: "Create a tag configuration",
      schema: CreateTagConfigurationSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "list_volumes_by_metric_name": {
      description: "List distinct metric volumes by metric name",
      schema: ListVolumesByMetricNameSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "query_scalar_data": {
      description: "Query scalar data across multiple products",
      schema: QueryScalarDataSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "query_timeseries_data": {
      description: "Query timeseries data across multiple products",
      schema: QueryTimeseriesDataSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "submit_metrics": {
      description: "Submit metrics",
      schema: SubmitMetricsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
  },

  methods: {
    list_tag_configurations: {
      description: "Get a list of metrics",
      arguments: z.object({
        filter_configured: z.boolean().optional().describe(
          "Only return custom metrics that have been configured (`true`) or not configur...",
        ),
        filter_is_configurable: z.boolean().optional().describe(
          "Only return metrics that are eligible (`true`) or ineligible (`false`) for co...",
        ),
        filter_tags_configured: z.string().optional().describe(
          "Only return metrics that have the given tag key(s) in their Metrics Without L...",
        ),
        filter_metric_type: z.string().optional().describe(
          "Only return metrics of the given metric type.",
        ),
        filter_include_percentiles: z.boolean().optional().describe(
          "Only return distribution metrics that have percentile aggregations enabled (t...",
        ),
        filter_queried: z.boolean().optional().describe(
          "Only return metrics that have been queried (true) or not queried (false) in t...",
        ),
        filter_queried_window_seconds: z.number().optional().describe(
          "This parameter has no effect unless `filter[queried]` is also set. Only retur...",
        ),
        filter_tags: z.string().optional().describe(
          "Only return metrics that were submitted with tags matching this expression. Y...",
        ),
        filter_related_assets: z.boolean().optional().describe(
          "Only return metrics that are used in at least one dashboard, monitor, noteboo...",
        ),
        include: z.string().optional().describe(
          "Include related resources in the response. Set to `metric_volumes` to include...",
        ),
        sort: z.string().optional().describe(
          "Sort results by metric volume. Prefix a key with `-` for descending order. Su...",
        ),
        window_seconds: z.number().optional().describe(
          "Only return metrics that have been actively reporting in the specified window...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await ddApiPaginated(
          apiKey,
          appKey,
          site,
          `/api/v2/metrics`,
          {
            "style": "cursor",
            "limitParam": "page[size]",
            "limitDefault": 100,
            "cursorParam": "page[cursor]",
            "cursorResponsePath": "meta.page.after",
          },
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "tag_configurations",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} tag_configurations", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    list_active_metric_configurations: {
      description: "List active tags and aggregations",
      arguments: z.object({
        metric_name: z.string().describe("The name of the metric."),
        window_seconds: z.number().optional().describe(
          "The number of seconds of look back (from now). Default value is 604,800 (1 we...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const queryParts: string[] = [];
        const excludeKeys = new Set<string>(["metric_name"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) {
            queryParts.push(
              `${encodeURIComponent(k)}=${encodeURIComponent(String(v))}`,
            );
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "GET",
          `/api/v2/metrics/${
            encodeURIComponent(String(args.metric_name))
          }/active-configurations${qs}`,
        );

        const handle = await context.writeResource(
          "list_active_metric_configurations",
          String(args.metric_name),
          result,
        );
        context.logger.info("Fetched list_active_metric_configurations", {});
        return { dataHandles: [handle] };
      },
    },
    list_tags_by_metric_name: {
      description: "List tags by metric name",
      arguments: z.object({
        metric_name: z.string().describe("The name of the metric."),
        window_seconds: z.number().optional().describe(
          "The number of seconds of look back (from now) to query for tag data. Default ...",
        ),
        filter_tags: z.string().optional().describe(
          "Filter results to tags from data points that have the specified tags. For exa...",
        ),
        filter_match: z.string().optional().describe(
          "Filter returned tags to those matching a substring. For example, `filter[matc...",
        ),
        filter_include_tag_values: z.boolean().optional().describe(
          "Whether to include tag values in the response. Defaults to true.",
        ),
        filter_allow_partial: z.boolean().optional().describe(
          "Whether to allow partial results. Defaults to false.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>(["metric_name"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await ddApiPaginated(
          apiKey,
          appKey,
          site,
          `/api/v2/metrics/${
            encodeURIComponent(String(args.metric_name))
          }/all-tags`,
          {
            "style": "offset",
            "limitParam": "page[limit]",
            "limitDefault": 100,
            "offsetParam": "page[offset]",
          },
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "tags_by_metric_name",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} tags_by_metric_name", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    list_metric_assets: {
      description: "Related Assets to a Metric",
      arguments: z.object({
        metric_name: z.string().describe("The name of the metric."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "GET",
          `/api/v2/metrics/${
            encodeURIComponent(String(args.metric_name))
          }/assets`,
        );

        const handle = await context.writeResource(
          "list_metric_assets",
          String(args.metric_name),
          result,
        );
        context.logger.info("Fetched list_metric_assets", {});
        return { dataHandles: [handle] };
      },
    },
    estimate_metrics_output_series: {
      description: "Tag Configuration Cardinality Estimator",
      arguments: z.object({
        metric_name: z.string().describe("The name of the metric."),
        filter_groups: z.string().optional().describe(
          "Comma-separated list of tag keys that the metric is configured to query with....",
        ),
        filter_hours_ago: z.number().optional().describe(
          "The number of hours of look back (from now) to estimate cardinality with. If ...",
        ),
        filter_num_aggregations: z.number().optional().describe(
          "Deprecated. Number of aggregations has no impact on volume.",
        ),
        filter_pct: z.boolean().optional().describe(
          "A boolean, for distribution metrics only, to estimate cardinality if the metr...",
        ),
        filter_timespan_h: z.number().optional().describe(
          "A window, in hours, from the look back to estimate cardinality with. The mini...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const queryParts: string[] = [];
        const excludeKeys = new Set<string>(["metric_name"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) {
            queryParts.push(
              `${encodeURIComponent(k)}=${encodeURIComponent(String(v))}`,
            );
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "GET",
          `/api/v2/metrics/${
            encodeURIComponent(String(args.metric_name))
          }/estimate${qs}`,
        );

        const handle = await context.writeResource(
          "estimate_metrics_output_series",
          String(args.metric_name),
          result,
        );
        context.logger.info("Fetched estimate_metrics_output_series", {});
        return { dataHandles: [handle] };
      },
    },
    get_metric_tag_cardinality_details: {
      description: "Get tag key cardinality details",
      arguments: z.object({
        metric_name: z.string().describe("The name of the metric."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>(["metric_name"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await ddApiPaginated(
          apiKey,
          appKey,
          site,
          `/api/v2/metrics/${
            encodeURIComponent(String(args.metric_name))
          }/tag-cardinalities`,
          { "style": "none", "limitParam": "", "limitDefault": 0 },
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "metric_tag_cardinality_details",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} metric_tag_cardinality_details", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    list_tag_configuration_by_name: {
      description: "List tag configuration by name",
      arguments: z.object({
        metric_name: z.string().describe("The name of the metric."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "GET",
          `/api/v2/metrics/${
            encodeURIComponent(String(args.metric_name))
          }/tags`,
        );

        const handle = await context.writeResource(
          "list_tag_configuration_by_name",
          String(args.metric_name),
          result,
        );
        context.logger.info("Fetched list_tag_configuration_by_name", {});
        return { dataHandles: [handle] };
      },
    },
    create_tag_configuration: {
      description: "Create a tag configuration",
      arguments: z.object({
        metric_name: z.string().describe("The name of the metric."),
        aggregations: z.unknown().optional(),
        exclude_tags_mode: z.boolean().optional().describe(
          "When set to true, the configuration will exclude the configured tags and incl...",
        ),
        include_percentiles: z.boolean().optional().describe(
          "Toggle to include/exclude percentiles for a distribution metric. Defaults to ...",
        ),
        metric_type: z.unknown(),
        tags: z.array(z.string()).describe(
          "A list of tag keys that will be queryable for your metric.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const attrs: Record<string, unknown> = {};
        const excludeKeys = new Set<string>(["metric_name"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) attrs[k] = v;
        }
        const body = { data: { type: "manage_tags", attributes: attrs } };

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "POST",
          `/api/v2/metrics/${
            encodeURIComponent(String(args.metric_name))
          }/tags`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "tag_configuration",
          id,
          result,
        );
        context.logger.info("Created tag_configuration {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    update_tag_configuration: {
      description: "Update a tag configuration",
      arguments: z.object({
        metric_name: z.string().describe("The name of the metric."),
        aggregations: z.unknown().optional(),
        exclude_tags_mode: z.boolean().optional().describe(
          "When set to true, the configuration will exclude the configured tags and incl...",
        ),
        include_percentiles: z.boolean().optional().describe(
          "Toggle to include/exclude percentiles for a distribution metric. Defaults to ...",
        ),
        tags: z.array(z.string()).optional().describe(
          "A list of tag keys that will be queryable for your metric.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const attrs: Record<string, unknown> = {};
        const excludeKeys = new Set<string>(["metric_name"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) attrs[k] = v;
        }
        const body = { data: { type: "manage_tags", attributes: attrs } };

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "PATCH",
          `/api/v2/metrics/${
            encodeURIComponent(String(args.metric_name))
          }/tags`,
          body,
        );

        const handle = await context.writeResource(
          "tag_configuration",
          String(args.metric_name),
          result,
        );
        context.logger.info("Updated tag_configuration", {});
        return { dataHandles: [handle] };
      },
    },
    delete_tag_configuration: {
      description: "Delete a tag configuration",
      arguments: z.object({
        metric_name: z.string().describe("The name of the metric."),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        await ddApi(
          apiKey,
          appKey,
          site,
          "DELETE",
          `/api/v2/metrics/${
            encodeURIComponent(String(args.metric_name))
          }/tags`,
        );

        context.logger.info("Deleted resource {id}", { id: args.metric_name });
        return { dataHandles: [] };
      },
    },
    list_volumes_by_metric_name: {
      description: "List distinct metric volumes by metric name",
      arguments: z.object({
        metric_name: z.string().describe("The name of the metric."),
        window_seconds: z.number().optional().describe(
          "The number of seconds of look back (from now). Default value is 3,600 (1 hour...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const queryParts: string[] = [];
        const excludeKeys = new Set<string>(["metric_name"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) {
            queryParts.push(
              `${encodeURIComponent(k)}=${encodeURIComponent(String(v))}`,
            );
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "GET",
          `/api/v2/metrics/${
            encodeURIComponent(String(args.metric_name))
          }/volumes${qs}`,
        );

        const handle = await context.writeResource(
          "list_volumes_by_metric_name",
          String(args.metric_name),
          result,
        );
        context.logger.info("Fetched list_volumes_by_metric_name", {});
        return { dataHandles: [handle] };
      },
    },
    query_scalar_data: {
      description: "Query scalar data across multiple products",
      arguments: z.object({
        formulas: z.array(z.unknown()).optional().describe(
          "List of formulas to be calculated and returned as responses.",
        ),
        from: z.number().int().describe(
          "Start date (inclusive) of the query in milliseconds since the Unix epoch.",
        ),
        queries: z.unknown(),
        to: z.number().int().describe(
          "End date (exclusive) of the query in milliseconds since the Unix epoch.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const attrs: Record<string, unknown> = {};
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) attrs[k] = v;
        }
        const body = { data: { type: "scalar_request", attributes: attrs } };

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "POST",
          `/api/v2/query/scalar`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "query_scalar_data",
          id,
          result,
        );
        context.logger.info("Created query_scalar_data {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    query_timeseries_data: {
      description: "Query timeseries data across multiple products",
      arguments: z.object({
        formulas: z.array(z.unknown()).optional().describe(
          "List of formulas to be calculated and returned as responses.",
        ),
        from: z.number().int().describe(
          "Start date (inclusive) of the query in milliseconds since the Unix epoch.",
        ),
        interval: z.number().int().optional().describe(
          "A time interval in milliseconds. May be overridden by a larger interval if th...",
        ),
        queries: z.unknown(),
        to: z.number().int().describe(
          "End date (exclusive) of the query in milliseconds since the Unix epoch.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const attrs: Record<string, unknown> = {};
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) attrs[k] = v;
        }
        const body = {
          data: { type: "timeseries_request", attributes: attrs },
        };

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "POST",
          `/api/v2/query/timeseries`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "query_timeseries_data",
          id,
          result,
        );
        context.logger.info("Created query_timeseries_data {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    submit_metrics: {
      description: "Submit metrics",
      arguments: z.object({
        series: z.array(z.unknown()).describe(
          "A list of timeseries to submit to Datadog.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "POST",
          `/api/v2/series`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "submit_metrics",
          id,
          result,
        );
        context.logger.info("Created submit_metrics {id}", { id });
        return { dataHandles: [handle] };
      },
    },
  },
};
