/**
 * Datadog Monitors — monitor definitions, muting, status, and downtime management
 *
 * Auto-generated by scripts/datadog-codegen — do not edit manually.
 *
 * @module
 */
// SPDX-License-Identifier: AGPL-3.0-or-later WITH Swamp-Extension-Exception

import { z } from "npm:zod@4.4.3";
import { ddApi, ddApiPaginated } from "./_lib/api.ts";

// =============================================================================
// Schemas
// =============================================================================

const GlobalArgsSchema = z.object({
  apiKey: z.string().meta({ sensitive: true }).describe(
    "Datadog API key (DD-API-KEY)",
  ),
  appKey: z.string().meta({ sensitive: true }).describe(
    "Datadog application key (DD-APPLICATION-KEY)",
  ),
  site: z.enum(["us1", "us3", "us5", "eu1", "ap1", "us1-fed"]).default("us1")
    .describe("Datadog site"),
});

const MonitorNotificationRulesItemSchema = z.object({
  id: z.string().describe("The ID of the monitor notification rule."),
  type: z.enum(["monitor-notification-rule"]).optional().describe(
    "Monitor notification rule resource type.",
  ),
  conditional_recipients: z.object({
    conditions: z.array(z.unknown()),
    fallback_recipients: z.unknown().optional(),
  }).optional().describe(
    "Use conditional recipients to define different recipients for different situations. Cannot be use...",
  ),
  created: z.string().optional().describe(
    "Creation time of the monitor notification rule.",
  ),
  filter: z.union([z.unknown(), z.unknown()]).optional().describe(
    "Specifies the matching criteria for monitor notifications.",
  ),
  modified: z.string().optional().describe(
    "Time the monitor notification rule was last modified.",
  ),
  name: z.string().min(1).max(1000).optional().describe(
    "The name of the monitor notification rule.",
  ),
  recipients: z.array(z.string().max(255)).optional().describe(
    "A list of recipients to notify. Uses the same format as the monitor `message` field. Must not sta...",
  ),
  created_by_id: z.string().optional().describe("Related created_by ID"),
});

const GetMonitorNotificationRulesSchema = z.object({
  items: z.array(MonitorNotificationRulesItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateMonitorNotificationRuleSchema = z.object({
  id: z.string().describe("The ID of the monitor notification rule."),
  type: z.enum(["monitor-notification-rule"]).optional().describe(
    "Monitor notification rule resource type.",
  ),
  conditional_recipients: z.object({
    conditions: z.array(z.unknown()),
    fallback_recipients: z.unknown().optional(),
  }).optional().describe(
    "Use conditional recipients to define different recipients for different situations. Cannot be use...",
  ),
  created: z.string().optional().describe(
    "Creation time of the monitor notification rule.",
  ),
  filter: z.union([z.unknown(), z.unknown()]).optional().describe(
    "Specifies the matching criteria for monitor notifications.",
  ),
  modified: z.string().optional().describe(
    "Time the monitor notification rule was last modified.",
  ),
  name: z.string().min(1).max(1000).optional().describe(
    "The name of the monitor notification rule.",
  ),
  recipients: z.array(z.string().max(255)).optional().describe(
    "A list of recipients to notify. Uses the same format as the monitor `message` field. Must not sta...",
  ),
  created_by_id: z.string().optional().describe("Related created_by ID"),
});

const MonitorConfigPoliciesItemSchema = z.object({
  id: z.string().describe("ID of this monitor configuration policy."),
  type: z.enum(["monitor-config-policy"]).optional().describe(
    "Monitor configuration policy resource type.",
  ),
  policy: z.unknown().optional().describe("Configuration for the policy."),
  policy_type: z.enum(["tag"]).optional().describe(
    "The monitor configuration policy type.",
  ),
});

const ListMonitorConfigPoliciesSchema = z.object({
  items: z.array(MonitorConfigPoliciesItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
});

const CreateMonitorConfigPolicySchema = z.object({
  id: z.string().describe("ID of this monitor configuration policy."),
  type: z.enum(["monitor-config-policy"]).optional().describe(
    "Monitor configuration policy resource type.",
  ),
  policy: z.unknown().optional().describe("Configuration for the policy."),
  policy_type: z.enum(["tag"]).optional().describe(
    "The monitor configuration policy type.",
  ),
});

// =============================================================================
// Model Definition
// =============================================================================

/** Datadog Monitors — monitor definitions, muting, status, and downtime management */
export const model = {
  type: "@webframp/datadog/monitors",
  version: "2026.07.20.2",
  globalArguments: GlobalArgsSchema,

  upgrades: [],

  resources: {
    "monitor_notification_rules": {
      description: "Get all monitor notification rules",
      schema: GetMonitorNotificationRulesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "monitor_notification_rule": {
      description: "Create a monitor notification rule",
      schema: CreateMonitorNotificationRuleSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "monitor_config_policies": {
      description: "Get all monitor configuration policies",
      schema: ListMonitorConfigPoliciesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "monitor_config_policy": {
      description: "Create a monitor configuration policy",
      schema: CreateMonitorConfigPolicySchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
  },

  methods: {
    get_monitor_notification_rules: {
      description: "Get all monitor notification rules",
      arguments: z.object({
        sort: z.string().optional().describe(
          "String for sort order, composed of field and sort order separated by a colon,...",
        ),
        filters: z.string().optional().describe(
          "JSON-encoded filter object. Supported keys: * `text`: Free-text query matched...",
        ),
        include: z.string().optional().describe(
          "Comma-separated list of resource paths for related resources to include in th...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await ddApiPaginated(
          apiKey,
          appKey,
          site,
          `/api/v2/monitor/notification_rule`,
          {
            "style": "page_number",
            "limitParam": "per_page",
            "limitDefault": 50,
            "pageParam": "page",
          },
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "monitor_notification_rules",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} monitor_notification_rules", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_monitor_notification_rule: {
      description: "Create a monitor notification rule",
      arguments: z.object({
        conditional_recipients: z.unknown().optional(),
        filter: z.unknown().optional(),
        name: z.unknown(),
        recipients: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const attrs: Record<string, unknown> = {};
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) attrs[k] = v;
        }
        const body = {
          data: { type: "monitor-notification-rule", attributes: attrs },
        };

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "POST",
          `/api/v2/monitor/notification_rule`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "monitor_notification_rule",
          id,
          result,
        );
        context.logger.info("Created monitor_notification_rule {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_monitor_notification_rule: {
      description: "Get a monitor notification rule",
      arguments: z.object({
        rule_id: z.string().describe(
          "ID of the monitor notification rule to fetch.",
        ),
        include: z.string().optional().describe(
          "Comma-separated list of resource paths for related resources to include in th...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const queryParts: string[] = [];
        const excludeKeys = new Set<string>(["rule_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) {
            queryParts.push(
              `${encodeURIComponent(k)}=${encodeURIComponent(String(v))}`,
            );
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "GET",
          `/api/v2/monitor/notification_rule/${
            encodeURIComponent(String(args.rule_id))
          }${qs}`,
        );

        const handle = await context.writeResource(
          "monitor_notification_rule",
          String(args.rule_id),
          result,
        );
        context.logger.info("Fetched monitor_notification_rule", {});
        return { dataHandles: [handle] };
      },
    },
    update_monitor_notification_rule: {
      description: "Update a monitor notification rule",
      arguments: z.object({
        rule_id: z.string().describe(
          "ID of the monitor notification rule to update.",
        ),
        conditional_recipients: z.unknown().optional(),
        filter: z.unknown().optional(),
        name: z.unknown(),
        recipients: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const attrs: Record<string, unknown> = {};
        const excludeKeys = new Set<string>(["rule_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) attrs[k] = v;
        }
        const body = {
          data: { type: "monitor-notification-rule", attributes: attrs },
        };

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "PATCH",
          `/api/v2/monitor/notification_rule/${
            encodeURIComponent(String(args.rule_id))
          }`,
          body,
        );

        const handle = await context.writeResource(
          "monitor_notification_rule",
          String(args.rule_id),
          result,
        );
        context.logger.info("Updated monitor_notification_rule", {});
        return { dataHandles: [handle] };
      },
    },
    delete_monitor_notification_rule: {
      description: "Delete a monitor notification rule",
      arguments: z.object({
        rule_id: z.string().describe(
          "ID of the monitor notification rule to delete.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        await ddApi(
          apiKey,
          appKey,
          site,
          "DELETE",
          `/api/v2/monitor/notification_rule/${
            encodeURIComponent(String(args.rule_id))
          }`,
        );

        context.logger.info("Deleted resource {id}", { id: args.rule_id });
        return { dataHandles: [] };
      },
    },
    list_monitor_config_policies: {
      description: "Get all monitor configuration policies",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const params: Record<string, string> = {};
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await ddApiPaginated(
          apiKey,
          appKey,
          site,
          `/api/v2/monitor/policy`,
          { "style": "none", "limitParam": "", "limitDefault": 0 },
          params,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "monitor_config_policies",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
          },
        );

        context.logger.info("Found {count} monitor_config_policies", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_monitor_config_policy: {
      description: "Create a monitor configuration policy",
      arguments: z.object({
        policy: z.unknown(),
        policy_type: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const attrs: Record<string, unknown> = {};
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) attrs[k] = v;
        }
        const body = {
          data: { type: "monitor-config-policy", attributes: attrs },
        };

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "POST",
          `/api/v2/monitor/policy`,
          body,
        );

        const id = (result as { id?: string }).id ?? "created";
        const handle = await context.writeResource(
          "monitor_config_policy",
          id,
          result,
        );
        context.logger.info("Created monitor_config_policy {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_monitor_config_policy: {
      description: "Get a monitor configuration policy",
      arguments: z.object({
        policy_id: z.string().describe(
          "ID of the monitor configuration policy.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "GET",
          `/api/v2/monitor/policy/${
            encodeURIComponent(String(args.policy_id))
          }`,
        );

        const handle = await context.writeResource(
          "monitor_config_policy",
          String(args.policy_id),
          result,
        );
        context.logger.info("Fetched monitor_config_policy", {});
        return { dataHandles: [handle] };
      },
    },
    update_monitor_config_policy: {
      description: "Edit a monitor configuration policy",
      arguments: z.object({
        policy_id: z.string().describe(
          "ID of the monitor configuration policy.",
        ),
        policy: z.unknown(),
        policy_type: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        const attrs: Record<string, unknown> = {};
        const excludeKeys = new Set<string>(["policy_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) attrs[k] = v;
        }
        const body = {
          data: { type: "monitor-config-policy", attributes: attrs },
        };

        const result = await ddApi(
          apiKey,
          appKey,
          site,
          "PATCH",
          `/api/v2/monitor/policy/${
            encodeURIComponent(String(args.policy_id))
          }`,
          body,
        );

        const handle = await context.writeResource(
          "monitor_config_policy",
          String(args.policy_id),
          result,
        );
        context.logger.info("Updated monitor_config_policy", {});
        return { dataHandles: [handle] };
      },
    },
    delete_monitor_config_policy: {
      description: "Delete a monitor configuration policy",
      arguments: z.object({
        policy_id: z.string().describe(
          "ID of the monitor configuration policy.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, appKey, site } = context.globalArgs;
        await ddApi(
          apiKey,
          appKey,
          site,
          "DELETE",
          `/api/v2/monitor/policy/${
            encodeURIComponent(String(args.policy_id))
          }`,
        );

        context.logger.info("Deleted resource {id}", { id: args.policy_id });
        return { dataHandles: [] };
      },
    },
  },
};
