/**
 * fal.ai Assets — media library, characters, collections, tags, uploads, favorites
 *
 * Auto-generated by scripts/falai-codegen — do not edit manually.
 *
 * @module
 */
// SPDX-License-Identifier: Apache-2.0

import { z } from "npm:zod@4.6.5";
import {
  falApi,
  falApiPaginated,
  sanitizeInstanceName,
  shortHash,
} from "./_lib/api.ts";

const EXTENSION_NAME = "@webframp/falai/assets";

// =============================================================================
// Schemas
// =============================================================================

const GlobalArgsSchema = z.object({
  apiToken: z.string().meta({ sensitive: true }).describe(
    "fal.ai API key; overrides the FAL_KEY environment variable. Wire with a vault.get(...) expression to source it from a vault.",
  ).optional(),
});

const AssetsItemSchema = z.object({
  asset_id: z.string().nullable().optional().describe(
    "Asset ID (canonical catalog identity)",
  ),
  vector_id: z.string().optional().describe(
    "Vector ID. Prefer asset_id for addressing",
  ),
  request_id: z.string().nullable().optional().describe("Request ID"),
  url: z.string().nullable().optional().describe("Media URL"),
  type: z.enum(["image", "video", "audio", "3d"]).optional().describe(
    "Asset media type",
  ),
  title: z.string().optional().describe("Display title"),
  endpoint: z.string().nullable().optional().describe(
    "Model endpoint that produced the asset",
  ),
  created_at: z.string().nullable().optional().describe("Asset creation time"),
  source: z.string().nullable().optional().describe("Asset source"),
  prompt: z.string().nullable().optional().describe(
    "Saved prompt or description",
  ),
  width: z.number().nullable().optional().describe("Width in pixels"),
  height: z.number().nullable().optional().describe("Height in pixels"),
  content_type: z.string().nullable().optional().describe("Media content type"),
  is_favorited: z.boolean().optional().describe(
    "Whether the asset is favorited",
  ),
  collection_ids: z.array(z.string()).optional().describe(
    "Collections containing this asset",
  ),
  tags: z.array(z.object({
    id: z.string().optional(),
    name: z.string().optional(),
    created_at: z.string().nullable().optional(),
  })).optional().describe("Assigned tags"),
  similarity: z.number().nullable().optional().describe(
    "Semantic similarity score when applicable",
  ),
}).passthrough();

const ListAssetsSchema = z.object({
  items: z.array(AssetsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const AssetCollectionsItemSchema = z.object({
  id: z.string().optional().describe("Collection ID"),
  type: z.enum(["manual", "smart", "character"]).optional().describe(
    "Collection type",
  ),
  name: z.string().optional().describe("Collection display name"),
  description: z.string().nullable().optional().describe(
    "Collection description",
  ),
  icon: z.string().nullable().optional().describe("Optional collection icon"),
  color: z.string().nullable().optional().describe("Optional collection color"),
  cover_image_url: z.string().nullable().optional().describe(
    "Optional cover image URL for the collection",
  ),
  character_identifier: z.string().nullable().optional().describe(
    "Character @mention identifier for character collections",
  ),
  parent_collection_id: z.string().nullable().optional().describe(
    "Parent collection ID for a nested (manual) collection; null when top-level",
  ),
  is_favorited: z.boolean().optional().describe(
    "Whether the collection is favorited",
  ),
  filters: z.unknown().optional().describe(
    "Filter DSL backing smart and character collections",
  ),
  asset_count: z.number().nullable().optional().describe(
    "Exact asset count when available; null for smart/character collections",
  ),
  created_at: z.string().optional().describe("Collection creation time"),
  updated_at: z.string().optional().describe("Collection update time"),
}).passthrough();

const ListAssetCollectionsSchema = z.object({
  items: z.array(AssetCollectionsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const CreateAssetCollectionSchema = z.object({
  collection: z.object({
    id: z.string().optional(),
    type: z.enum(["manual", "smart", "character"]).optional(),
    name: z.string().optional(),
    description: z.string().nullable().optional(),
    icon: z.string().nullable().optional(),
    color: z.string().nullable().optional(),
    cover_image_url: z.string().nullable().optional(),
    character_identifier: z.string().nullable().optional(),
    parent_collection_id: z.string().nullable().optional(),
    is_favorited: z.boolean().optional(),
    filters: z.unknown().optional(),
    asset_count: z.number().nullable().optional(),
    created_at: z.string().optional(),
    updated_at: z.string().optional(),
  }).optional().describe("Asset collection"),
}).passthrough();

const GetAssetCollectionHierarchyItemSchema = z.object({
  id: z.string().optional().describe("Collection ID"),
  name: z.string().optional().describe("Collection display name"),
  type: z.enum(["manual", "smart", "character"]).optional().describe(
    "Collection type",
  ),
}).passthrough();

const GetAssetCollectionHierarchySchema = z.object({
  items: z.array(GetAssetCollectionHierarchyItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const FavoriteAssetCollectionSchema = z.object({
  collection: z.object({
    id: z.string().optional(),
    type: z.enum(["manual", "smart", "character"]).optional(),
    name: z.string().optional(),
    description: z.string().nullable().optional(),
    icon: z.string().nullable().optional(),
    color: z.string().nullable().optional(),
    cover_image_url: z.string().nullable().optional(),
    character_identifier: z.string().nullable().optional(),
    parent_collection_id: z.string().nullable().optional(),
    is_favorited: z.boolean().optional(),
    filters: z.unknown().optional(),
    asset_count: z.number().nullable().optional(),
    created_at: z.string().optional(),
    updated_at: z.string().optional(),
  }).optional().describe("Asset collection"),
}).passthrough();

const UnfavoriteAssetCollectionSchema = z.object({
  collection: z.object({
    id: z.string().optional(),
    type: z.enum(["manual", "smart", "character"]).optional(),
    name: z.string().optional(),
    description: z.string().nullable().optional(),
    icon: z.string().nullable().optional(),
    color: z.string().nullable().optional(),
    cover_image_url: z.string().nullable().optional(),
    character_identifier: z.string().nullable().optional(),
    parent_collection_id: z.string().nullable().optional(),
    is_favorited: z.boolean().optional(),
    filters: z.unknown().optional(),
    asset_count: z.number().nullable().optional(),
    created_at: z.string().optional(),
    updated_at: z.string().optional(),
  }).optional().describe("Asset collection"),
}).passthrough();

const MoveAssetCollectionSchema = z.object({
  collection: z.object({
    id: z.string().optional(),
    type: z.enum(["manual", "smart", "character"]).optional(),
    name: z.string().optional(),
    description: z.string().nullable().optional(),
    icon: z.string().nullable().optional(),
    color: z.string().nullable().optional(),
    cover_image_url: z.string().nullable().optional(),
    character_identifier: z.string().nullable().optional(),
    parent_collection_id: z.string().nullable().optional(),
    is_favorited: z.boolean().optional(),
    filters: z.unknown().optional(),
    asset_count: z.number().nullable().optional(),
    created_at: z.string().optional(),
    updated_at: z.string().optional(),
  }).optional().describe("Asset collection"),
}).passthrough();

const AssetCollectionAssetsItemSchema = z.object({
  asset_id: z.string().nullable().optional().describe(
    "Asset ID (canonical catalog identity)",
  ),
  vector_id: z.string().optional().describe(
    "Vector ID. Prefer asset_id for addressing",
  ),
  request_id: z.string().nullable().optional().describe("Request ID"),
  url: z.string().nullable().optional().describe("Media URL"),
  type: z.enum(["image", "video", "audio", "3d"]).optional().describe(
    "Asset media type",
  ),
  title: z.string().optional().describe("Display title"),
  endpoint: z.string().nullable().optional().describe(
    "Model endpoint that produced the asset",
  ),
  created_at: z.string().nullable().optional().describe("Asset creation time"),
  source: z.string().nullable().optional().describe("Asset source"),
  prompt: z.string().nullable().optional().describe(
    "Saved prompt or description",
  ),
  width: z.number().nullable().optional().describe("Width in pixels"),
  height: z.number().nullable().optional().describe("Height in pixels"),
  content_type: z.string().nullable().optional().describe("Media content type"),
  is_favorited: z.boolean().optional().describe(
    "Whether the asset is favorited",
  ),
  collection_ids: z.array(z.string()).optional().describe(
    "Collections containing this asset",
  ),
  tags: z.array(z.object({
    id: z.string().optional(),
    name: z.string().optional(),
    created_at: z.string().nullable().optional(),
  })).optional().describe("Assigned tags"),
  similarity: z.number().nullable().optional().describe(
    "Semantic similarity score when applicable",
  ),
}).passthrough();

const ListAssetCollectionAssetsSchema = z.object({
  items: z.array(AssetCollectionAssetsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const AddAssetToCollectionSchema = z.object({
  success: z.boolean().optional().describe("Whether the operation succeeded"),
}).passthrough();

const AssetCharactersItemSchema = z.object({
  id: z.string().optional().describe("Collection ID"),
  type: z.enum(["character"]).optional().describe("Collection type"),
  name: z.string().optional().describe("Collection display name"),
  description: z.string().nullable().optional().describe(
    "Collection description",
  ),
  icon: z.string().nullable().optional().describe("Optional collection icon"),
  color: z.string().nullable().optional().describe("Optional collection color"),
  cover_image_url: z.string().nullable().optional().describe(
    "Optional cover image URL for the collection",
  ),
  character_identifier: z.string().nullable().optional().describe(
    "Character @mention identifier for character collections",
  ),
  parent_collection_id: z.string().nullable().optional().describe(
    "Parent collection ID for a nested (manual) collection; null when top-level",
  ),
  is_favorited: z.boolean().optional().describe(
    "Whether the collection is favorited",
  ),
  asset_count: z.number().nullable().optional().describe(
    "Exact asset count when available; null for smart/character collections",
  ),
  created_at: z.string().optional().describe("Collection creation time"),
  updated_at: z.string().optional().describe("Collection update time"),
  reference_images: z.array(z.string()).optional().describe(
    "Reference images used for character matching",
  ),
}).passthrough();

const ListAssetCharactersSchema = z.object({
  items: z.array(AssetCharactersItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const CreateAssetCharacterSchema = z.object({
  character: z.object({
    id: z.string().optional(),
    type: z.enum(["character"]).optional(),
    name: z.string().optional(),
    description: z.string().nullable().optional(),
    icon: z.string().nullable().optional(),
    color: z.string().nullable().optional(),
    cover_image_url: z.string().nullable().optional(),
    character_identifier: z.string().nullable().optional(),
    parent_collection_id: z.string().nullable().optional(),
    is_favorited: z.boolean().optional(),
    asset_count: z.number().nullable().optional(),
    created_at: z.string().optional(),
    updated_at: z.string().optional(),
    reference_images: z.array(z.string()).optional(),
  }).optional().describe("Asset character"),
}).passthrough();

const FavoriteAssetCharacterSchema = z.object({
  character: z.object({
    id: z.string().optional(),
    type: z.enum(["character"]).optional(),
    name: z.string().optional(),
    description: z.string().nullable().optional(),
    icon: z.string().nullable().optional(),
    color: z.string().nullable().optional(),
    cover_image_url: z.string().nullable().optional(),
    character_identifier: z.string().nullable().optional(),
    parent_collection_id: z.string().nullable().optional(),
    is_favorited: z.boolean().optional(),
    asset_count: z.number().nullable().optional(),
    created_at: z.string().optional(),
    updated_at: z.string().optional(),
    reference_images: z.array(z.string()).optional(),
  }).optional().describe("Asset character"),
}).passthrough();

const UnfavoriteAssetCharacterSchema = z.object({
  character: z.object({
    id: z.string().optional(),
    type: z.enum(["character"]).optional(),
    name: z.string().optional(),
    description: z.string().nullable().optional(),
    icon: z.string().nullable().optional(),
    color: z.string().nullable().optional(),
    cover_image_url: z.string().nullable().optional(),
    character_identifier: z.string().nullable().optional(),
    parent_collection_id: z.string().nullable().optional(),
    is_favorited: z.boolean().optional(),
    asset_count: z.number().nullable().optional(),
    created_at: z.string().optional(),
    updated_at: z.string().optional(),
    reference_images: z.array(z.string()).optional(),
  }).optional().describe("Asset character"),
}).passthrough();

const AssetEntitiesItemSchema = z.object({
  id: z.string().optional().describe("Smart entity ID"),
  type: z.enum(["character", "prop", "environment", "style", "scene"])
    .optional().describe("Smart entity type"),
  name: z.string().min(1).max(255).optional().describe("Entity display name"),
  handle: z.string().optional().describe(
    "Resolved @mention handle, without the leading @",
  ),
  description: z.string().nullable().optional().describe(
    "Entity visual description",
  ),
  cover_image_url: z.string().nullable().optional().describe(
    "Entity cover image URL",
  ),
  reference_images: z.array(z.string()).optional().describe(
    "Defining reference image URLs",
  ),
  is_favorited: z.boolean().optional().describe(
    "Whether the entity is favorited",
  ),
  created_at: z.string().optional().describe("Creation time in UTC"),
  updated_at: z.string().optional().describe("Last update time in UTC"),
}).passthrough();

const ListAssetEntitiesSchema = z.object({
  items: z.array(AssetEntitiesItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const CreateAssetEntitySchema = z.object({
  entity: z.object({
    id: z.string().optional(),
    type: z.enum(["character", "prop", "environment", "style", "scene"])
      .optional(),
    name: z.string().min(1).max(255).optional(),
    handle: z.string().optional(),
    description: z.string().nullable().optional(),
    cover_image_url: z.string().nullable().optional(),
    reference_images: z.array(z.string()).optional(),
    is_favorited: z.boolean().optional(),
    created_at: z.string().optional(),
    updated_at: z.string().optional(),
  }).optional().describe("Saved smart entity"),
}).passthrough();

const AssetTagsItemSchema = z.object({
  id: z.string().optional().describe("Tag ID"),
  name: z.string().optional().describe("Tag name"),
  created_at: z.string().nullable().optional().describe("Tag creation time"),
}).passthrough();

const ListAssetTagsSchema = z.object({
  items: z.array(AssetTagsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const CreateAssetTagSchema = z.object({
  tag: z.object({
    id: z.string().optional(),
    name: z.string().optional(),
    created_at: z.string().nullable().optional(),
  }).optional().describe("Asset tag"),
}).passthrough();

const SetAssetTagsForAssetSchema = z.object({
  id: z.string().optional().describe("Tag ID"),
  name: z.string().optional().describe("Tag name"),
  created_at: z.string().nullable().optional().describe("Tag creation time"),
}).passthrough();

const UploadAssetSchema = z.object({
  asset: z.object({
    asset_id: z.string().nullable().optional(),
    vector_id: z.string().optional(),
    request_id: z.string().nullable().optional(),
    url: z.string().nullable().optional(),
    type: z.enum(["image", "video", "audio", "3d"]).optional(),
    title: z.string().optional(),
    endpoint: z.string().nullable().optional(),
    created_at: z.string().nullable().optional(),
    source: z.string().nullable().optional(),
    prompt: z.string().nullable().optional(),
    width: z.number().nullable().optional(),
    height: z.number().nullable().optional(),
    content_type: z.string().nullable().optional(),
    is_favorited: z.boolean().optional(),
    collection_ids: z.array(z.string()).optional(),
    tags: z.array(z.object({
      id: z.string().optional(),
      name: z.string().optional(),
      created_at: z.string().nullable().optional(),
    })).optional(),
    similarity: z.number().nullable().optional(),
  }).optional().describe("Asset details"),
}).passthrough();

const GetAssetSchema = z.object({
  asset: z.object({
    asset_id: z.string().nullable().optional(),
    vector_id: z.string().optional(),
    request_id: z.string().nullable().optional(),
    url: z.string().nullable().optional(),
    type: z.enum(["image", "video", "audio", "3d"]).optional(),
    title: z.string().optional(),
    endpoint: z.string().nullable().optional(),
    created_at: z.string().nullable().optional(),
    source: z.string().nullable().optional(),
    prompt: z.string().nullable().optional(),
    width: z.number().nullable().optional(),
    height: z.number().nullable().optional(),
    content_type: z.string().nullable().optional(),
    is_favorited: z.boolean().optional(),
    collection_ids: z.array(z.string()).optional(),
    tags: z.array(z.object({
      id: z.string().optional(),
      name: z.string().optional(),
      created_at: z.string().nullable().optional(),
    })).optional(),
    similarity: z.number().nullable().optional(),
  }).optional().describe("Asset details"),
}).passthrough();

const GetAssetLineageSchema = z.object({
  lineage: z.object({
    focus: z.string().optional(),
    nodes: z.array(z.union([
      z.object({
        kind: z.enum(["asset"]).optional(),
        id: z.string().optional(),
        output_key: z.string().optional(),
        url: z.string().nullable().optional(),
        type: z.enum(["image", "video", "audio", "text", "3d"]).optional(),
        request_id: z.string().optional(),
        asset_id: z.string().nullable().optional(),
        tombstone: z.boolean().optional(),
      }),
      z.object({
        kind: z.enum(["external_input"]).optional(),
        id: z.string().optional(),
        output_key: z.string().optional(),
        url: z.string().nullable().optional(),
      }),
      z.object({
        kind: z.enum(["request"]).optional(),
        id: z.string().optional(),
        request_id: z.string().optional(),
        endpoint: z.string().nullable().optional(),
        prompt: z.string().nullable().optional(),
        created_at: z.string().nullable().optional(),
      }),
      z.object({
        kind: z.enum(["entity"]).optional(),
        id: z.string().optional(),
        entity_id: z.string().optional(),
        entity_type: z.enum([
          "character",
          "prop",
          "environment",
          "style",
          "scene",
        ]).optional(),
        name: z.string().nullable().optional(),
        handle: z.string().nullable().optional(),
        tombstone: z.boolean().optional(),
      }),
    ])).optional(),
    edges: z.array(z.object({
      from: z.string().optional(),
      to: z.string().optional(),
      kind: z.enum(["input_to", "generated_by", "referenced_entity"])
        .optional(),
      role: z.union([
        z.enum(["source", "content_ref", "mask", "control"]),
        z.unknown(),
        z.unknown(),
      ]).optional(),
      entities: z.array(z.object({
        entity_id: z.string().optional(),
        entity_type: z.enum([
          "character",
          "prop",
          "environment",
          "style",
          "scene",
        ]).optional(),
        name: z.string().nullable().optional(),
        handle: z.string().nullable().optional(),
      })).optional(),
    })).optional(),
  }).optional().describe("Derivation lineage graph of the asset"),
}).passthrough();

const FavoriteAssetSchema = z.object({
  is_favorited: z.boolean().optional().describe(
    "Whether the asset is now favorited",
  ),
}).passthrough();

const UnfavoriteAssetSchema = z.object({
  is_favorited: z.boolean().optional().describe(
    "Whether the asset is now favorited",
  ),
}).passthrough();

const AssetTagsForAssetItemSchema = z.object({
  id: z.string().optional().describe("Tag ID"),
  name: z.string().optional().describe("Tag name"),
  created_at: z.string().nullable().optional().describe("Tag creation time"),
}).passthrough();

const ListAssetTagsForAssetSchema = z.object({
  items: z.array(AssetTagsForAssetItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const AssignAssetTagSchema = z.object({
  success: z.boolean().optional().describe("Whether the operation succeeded"),
}).passthrough();

// =============================================================================
// Model Definition
// =============================================================================

/** fal.ai Assets — media library, characters, collections, tags, uploads, favorites */
export const model = {
  type: "@webframp/falai/assets",
  version: "2026.09.18.1",
  globalArguments: GlobalArgsSchema,

  upgrades: [
    {
      toVersion: "2026.09.09.2",
      description: "Regenerated from updated API spec; no migration required",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.09.10.1",
      description: "Regenerated from updated API spec; no migration required",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.09.10.2",
      description: "Regenerated from updated API spec; no migration required",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.09.10.3",
      description: "Regenerated from updated API spec; no migration required",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.09.10.4",
      description: "Regenerated from updated API spec; no migration required",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.09.15.1",
      description: "No schema changes — dependency/license maintenance bump",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.09.17.1",
      description: "Regenerated from updated API spec; no migration required",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.09.17.2",
      description: "Regenerated from updated API spec; no migration required",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.09.18.1",
      description:
        "Normalized zod dependency version and applied pagination truncation fixes where applicable",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
  ],

  resources: {
    "assets": {
      description: "Browse assets",
      schema: ListAssetsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "asset_collections": {
      description: "List asset collections",
      schema: ListAssetCollectionsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "asset_collection": {
      description: "Create asset collection",
      schema: CreateAssetCollectionSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "get_asset_collection_hierarchy": {
      description: "Get asset collection hierarchy",
      schema: GetAssetCollectionHierarchySchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "favorite_asset_collection": {
      description: "Favorite asset collection",
      schema: FavoriteAssetCollectionSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "unfavorite_asset_collection": {
      description: "Unfavorite asset collection",
      schema: UnfavoriteAssetCollectionSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "move_asset_collection": {
      description: "Move asset collection",
      schema: MoveAssetCollectionSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "asset_collection_assets": {
      description: "Browse assets in a collection",
      schema: ListAssetCollectionAssetsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "add_asset_to_collection": {
      description: "Add asset to collection",
      schema: AddAssetToCollectionSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "asset_characters": {
      description: "List asset characters",
      schema: ListAssetCharactersSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "asset_character": {
      description: "Create asset character",
      schema: CreateAssetCharacterSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "favorite_asset_character": {
      description: "Favorite asset character",
      schema: FavoriteAssetCharacterSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "unfavorite_asset_character": {
      description: "Unfavorite asset character",
      schema: UnfavoriteAssetCharacterSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "asset_entities": {
      description: "List asset entities",
      schema: ListAssetEntitiesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "asset_entity": {
      description: "Create asset entity",
      schema: CreateAssetEntitySchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "asset_tags": {
      description: "List asset tags",
      schema: ListAssetTagsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "asset_tag": {
      description: "Create asset tag",
      schema: CreateAssetTagSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "set_asset_tags_for_asset": {
      description: "Set tags for an asset",
      schema: SetAssetTagsForAssetSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "upload_asset": {
      description: "Upload asset",
      schema: UploadAssetSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "asset": {
      description: "Get asset",
      schema: GetAssetSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "asset_lineage": {
      description: "Get asset lineage",
      schema: GetAssetLineageSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "favorite_asset": {
      description: "Favorite asset",
      schema: FavoriteAssetSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "unfavorite_asset": {
      description: "Unfavorite asset",
      schema: UnfavoriteAssetSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "asset_tags_for_asset": {
      description: "List tags for an asset",
      schema: ListAssetTagsForAssetSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "assign_asset_tag": {
      description: "Assign tag to asset",
      schema: AssignAssetTagSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
  },

  methods: {
    list_assets: {
      description: "Browse assets",
      arguments: z.object({
        limit: z.number().int().min(1).optional().describe(
          "Maximum number of items to return. Actual maximum depends on query type and expansion parameters.",
        ),
        cursor: z.string().optional().describe(
          "Pagination cursor from previous response. Encodes the page number.",
        ),
        q: z.string().optional().describe(
          "Text query for hybrid semantic search",
        ),
        search_image_url: z.string().optional().describe(
          "fal-hosted image URL to use for semantic image search",
        ),
        search_video_url: z.string().optional().describe(
          "fal-hosted video URL to use for semantic video search",
        ),
        media_type: z.array(z.enum(["image", "video", "audio", "3d"]))
          .nullable().optional().describe("Filter by one or more media types"),
        source: z.array(z.enum(["upload", "response"])).nullable().optional()
          .describe("Filter by one or more indexed sources"),
        section: z.enum(["all-media", "uploads", "favorites", "generated"])
          .optional().describe("Asset library section to browse"),
        collection_id: z.string().optional().describe(
          "Collection scope to browse",
        ),
        character_identifier: z.array(z.string()).nullable().optional()
          .describe(
            "Character identifiers to use as @mention semantic filters",
          ),
        tag_id: z.array(z.string()).nullable().optional().describe(
          "Tag IDs to filter by",
        ),
        tag_mode: z.enum(["any", "all"]).optional().describe(
          "Whether tag filters match any tag or all tags",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        const startMs = Date.now();
        const params: Record<string, string | string[]> = {};
        const excludeKeys = new Set<string>(["limit", "cursor"]);
        for (const [k, v] of Object.entries(args)) {
          if (v === undefined || v === null || excludeKeys.has(k)) continue;
          params[k] = Array.isArray(v) ? v.map(String) : String(v);
        }
        const requestedLimit = args.limit !== undefined
          ? Number(args.limit)
          : undefined;
        const requestedCursor = typeof args.cursor === "string"
          ? args.cursor
          : undefined;

        const { results, truncated } = await falApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/assets`,
          "assets",
          params,
          { limit: requestedLimit, cursor: requestedCursor },
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("assets", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
          durationMs: Date.now() - startMs,
          collectedBy: EXTENSION_NAME,
        });

        context.logger.info("Found {count} assets", { count: results.length });
        return { dataHandles: [handle] };
      },
    },
    list_asset_collections: {
      description: "List asset collections",
      arguments: z.object({
        limit: z.number().int().min(1).max(100).optional().describe(
          "Maximum number of collections to return",
        ),
        offset: z.number().int().min(0).nullable().optional().describe(
          "Number of collections to skip",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        const startMs = Date.now();
        const params = new URLSearchParams();
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (v === undefined || v === null || excludeKeys.has(k)) continue;
          if (Array.isArray(v)) {
            for (const item of v) params.append(k, String(item));
          } else {
            params.append(k, String(v));
          }
        }
        const qs = params.toString();
        const url = qs ? `/assets/collections?${qs}` : `/assets/collections`;

        const result = await falApi<Record<string, unknown>>(
          apiToken,
          "GET",
          url,
        );
        const items = ((result as Record<string, unknown>)["collections"] ??
          []) as unknown[];
        // No cursor/offset in this response: a full page equal to the
        // effective limit (explicit or the API's default) means more results
        // may exist that we didn't fetch.
        const limit = args.limit !== undefined ? Number(args.limit) : 50;
        const truncated = limit !== undefined && items.length === limit;

        const handle = await context.writeResource(
          "asset_collections",
          "main",
          {
            items,
            truncated,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );

        context.logger.info("Found {count} asset_collections", {
          count: items.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_asset_collection: {
      description: "Create asset collection",
      arguments: z.object({
        name: z.string().min(1).max(255).describe("Collection display name"),
        description: z.string().nullable().optional().describe(
          "Optional collection description",
        ),
        icon: z.string().nullable().optional().describe(
          "Optional collection icon",
        ),
        color: z.string().nullable().optional().describe(
          "Optional collection color",
        ),
        cover_image_url: z.string().nullable().optional().describe(
          "Optional fal-hosted cover image URL for the collection",
        ),
        parent_collection_id: z.string().min(1).nullable().optional().describe(
          "Optional parent collection ID to nest this collection under (manual collectio...",
        ),
        filters: z.unknown().optional().describe("Assets filter DSL"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;

        const result = await falApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/assets/collections`,
          args,
        );

        const id = sanitizeInstanceName(
          String(
            ((result as Record<string, unknown>)["collection"] as
              | Record<string, unknown>
              | undefined)?.["id"] ?? "created",
          ),
        );
        const handle = await context.writeResource(
          "asset_collection",
          id,
          result,
        );
        context.logger.info("Created asset_collection {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_asset_collection: {
      description: "Get asset collection",
      arguments: z.object({
        collection_id: z.string().describe("Collection ID"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;

        const result = await falApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/assets/collections/${
            encodeURIComponent(String(args.collection_id))
          }`,
        );

        const handle = await context.writeResource(
          "asset_collection",
          sanitizeInstanceName(String(args.collection_id)),
          result,
        );
        context.logger.info("Fetched asset_collection", {});
        return { dataHandles: [handle] };
      },
    },
    update_asset_collection: {
      description: "Update asset collection",
      arguments: z.object({
        collection_id: z.string().describe("Collection ID"),
        name: z.string().min(1).max(255).optional().describe(
          "Collection display name",
        ),
        description: z.string().nullable().optional().describe(
          "Optional collection description",
        ),
        icon: z.string().nullable().optional().describe(
          "Optional collection icon",
        ),
        color: z.string().nullable().optional().describe(
          "Optional collection color",
        ),
        cover_image_url: z.string().nullable().optional().describe(
          "Optional fal-hosted cover image URL for the collection",
        ),
        filters: z.unknown().optional().describe("Assets filter DSL"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["collection_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await falApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/assets/collections/${
            encodeURIComponent(String(args.collection_id))
          }`,
          body,
        );

        const handle = await context.writeResource(
          "asset_collection",
          sanitizeInstanceName(String(args.collection_id)),
          result,
        );
        context.logger.info("Updated asset_collection", {});
        return { dataHandles: [handle] };
      },
    },
    delete_asset_collection: {
      description: "Delete asset collection",
      arguments: z.object({
        collection_id: z.string().describe("Collection ID"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;

        await falApi(
          apiToken,
          "DELETE",
          `/assets/collections/${
            encodeURIComponent(String(args.collection_id))
          }`,
        );

        context.logger.info("Deleted resource {id}", {
          id: args.collection_id,
        });
        return { dataHandles: [] };
      },
    },
    get_asset_collection_hierarchy: {
      description: "Get asset collection hierarchy",
      arguments: z.object({
        collection_id: z.string().describe("Collection ID"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        const startMs = Date.now();
        const params = new URLSearchParams();
        const excludeKeys = new Set<string>(["collection_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v === undefined || v === null || excludeKeys.has(k)) continue;
          if (Array.isArray(v)) {
            for (const item of v) params.append(k, String(item));
          } else {
            params.append(k, String(v));
          }
        }
        const qs = params.toString();
        const url = qs
          ? `/assets/collections/${
            encodeURIComponent(String(args.collection_id))
          }/hierarchy?${qs}`
          : `/assets/collections/${
            encodeURIComponent(String(args.collection_id))
          }/hierarchy`;

        const result = await falApi<Record<string, unknown>>(
          apiToken,
          "GET",
          url,
        );
        const items =
          ((result as Record<string, unknown>)["ancestors"] ?? []) as unknown[];
        const truncated = false;

        const handle = await context.writeResource(
          "get_asset_collection_hierarchy",
          "main",
          {
            items,
            truncated,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );

        context.logger.info("Found {count} get_asset_collection_hierarchy", {
          count: items.length,
        });
        return { dataHandles: [handle] };
      },
    },
    favorite_asset_collection: {
      description: "Favorite asset collection",
      arguments: z.object({
        collection_id: z.string().describe("Collection ID"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;

        const result = await falApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/assets/collections/${
            encodeURIComponent(String(args.collection_id))
          }/favorite`,
        );

        const handle = await context.writeResource(
          "favorite_asset_collection",
          "latest",
          result ?? {},
        );
        context.logger.info("Executed favorite_asset_collection", {});
        return { dataHandles: [handle] };
      },
    },
    unfavorite_asset_collection: {
      description: "Unfavorite asset collection",
      arguments: z.object({
        collection_id: z.string().describe("Collection ID"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;

        const result = await falApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/assets/collections/${
            encodeURIComponent(String(args.collection_id))
          }/unfavorite`,
        );

        const handle = await context.writeResource(
          "unfavorite_asset_collection",
          "latest",
          result ?? {},
        );
        context.logger.info("Executed unfavorite_asset_collection", {});
        return { dataHandles: [handle] };
      },
    },
    move_asset_collection: {
      description: "Move asset collection",
      arguments: z.object({
        collection_id: z.string().describe("Collection ID"),
        parent_collection_id: z.string().min(1).nullable().describe(
          "Parent collection ID to move this collection under, or null to move it to the...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["collection_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await falApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/assets/collections/${
            encodeURIComponent(String(args.collection_id))
          }/move`,
          body,
        );

        const id = sanitizeInstanceName(
          String(
            ((result as Record<string, unknown>)["collection"] as
              | Record<string, unknown>
              | undefined)?.["id"] ?? "created",
          ),
        );
        const handle = await context.writeResource(
          "move_asset_collection",
          id,
          result,
        );
        context.logger.info("Created move_asset_collection {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    list_asset_collection_assets: {
      description: "Browse assets in a collection",
      arguments: z.object({
        collection_id: z.string().describe("Collection ID"),
        limit: z.number().int().min(1).optional().describe(
          "Maximum number of items to return. Actual maximum depends on query type and expansion parameters.",
        ),
        cursor: z.string().optional().describe(
          "Pagination cursor from previous response. Encodes the page number.",
        ),
        q: z.string().optional().describe(
          "Text query for hybrid semantic search",
        ),
        search_image_url: z.string().optional().describe(
          "fal-hosted image URL to use for semantic image search",
        ),
        search_video_url: z.string().optional().describe(
          "fal-hosted video URL to use for semantic video search",
        ),
        media_type: z.array(z.enum(["image", "video", "audio", "3d"]))
          .nullable().optional().describe("Filter by one or more media types"),
        source: z.array(z.enum(["upload", "response"])).nullable().optional()
          .describe("Filter by one or more indexed sources"),
        section: z.enum(["all-media", "uploads", "favorites", "generated"])
          .optional().describe("Asset library section to browse"),
        character_identifier: z.array(z.string()).nullable().optional()
          .describe(
            "Character identifiers to use as @mention semantic filters",
          ),
        tag_id: z.array(z.string()).nullable().optional().describe(
          "Tag IDs to filter by",
        ),
        tag_mode: z.enum(["any", "all"]).optional().describe(
          "Whether tag filters match any tag or all tags",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        const startMs = Date.now();
        const params: Record<string, string | string[]> = {};
        const excludeKeys = new Set<string>([
          "collection_id",
          "limit",
          "cursor",
        ]);
        for (const [k, v] of Object.entries(args)) {
          if (v === undefined || v === null || excludeKeys.has(k)) continue;
          params[k] = Array.isArray(v) ? v.map(String) : String(v);
        }
        const requestedLimit = args.limit !== undefined
          ? Number(args.limit)
          : undefined;
        const requestedCursor = typeof args.cursor === "string"
          ? args.cursor
          : undefined;

        const { results, truncated } = await falApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/assets/collections/${
            encodeURIComponent(String(args.collection_id))
          }/assets`,
          "assets",
          params,
          { limit: requestedLimit, cursor: requestedCursor },
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "asset_collection_assets",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );

        context.logger.info("Found {count} asset_collection_assets", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    add_asset_to_collection: {
      description: "Add asset to collection",
      arguments: z.object({
        collection_id: z.string().describe("Collection ID"),
        asset_id: z.string().min(1).optional().describe(
          "Asset ID (canonical catalog identity)",
        ),
        request_id: z.string().min(1).optional().describe(
          "Request ID to save as an asset before mutating",
        ),
        vector_id: z.string().min(1).optional().describe(
          "Vector ID to save as an asset before mutating. Prefer asset_id",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["collection_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await falApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/assets/collections/${
            encodeURIComponent(String(args.collection_id))
          }/assets`,
          body,
        );

        // No id- or name-shaped field anywhere in this response —
        // derive a deterministic, collision-resistant instance name
        // from the request instead of colliding every call onto "created".
        const id = sanitizeInstanceName(await shortHash(JSON.stringify(args)));
        const handle = await context.writeResource(
          "add_asset_to_collection",
          id,
          result,
        );
        context.logger.info("Created add_asset_to_collection {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    remove_asset_from_collection: {
      description: "Remove asset from collection",
      arguments: z.object({
        collection_id: z.string().describe("Collection ID"),
        asset_id: z.string().min(1).optional().describe(
          "Asset ID (canonical catalog identity)",
        ),
        request_id: z.string().min(1).optional().describe(
          "Request ID to save as an asset before mutating",
        ),
        vector_id: z.string().min(1).optional().describe(
          "Vector ID to save as an asset before mutating. Prefer asset_id",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["collection_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        await falApi(
          apiToken,
          "DELETE",
          `/assets/collections/${
            encodeURIComponent(String(args.collection_id))
          }/assets`,
          body,
        );

        context.logger.info("Deleted resource {id}", {
          id: args.collection_id,
        });
        return { dataHandles: [] };
      },
    },
    list_asset_characters: {
      description: "List asset characters",
      arguments: z.object({
        limit: z.number().int().min(1).max(100).optional().describe(
          "Maximum number of collections to return",
        ),
        offset: z.number().int().min(0).nullable().optional().describe(
          "Number of collections to skip",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        const startMs = Date.now();
        const params = new URLSearchParams();
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (v === undefined || v === null || excludeKeys.has(k)) continue;
          if (Array.isArray(v)) {
            for (const item of v) params.append(k, String(item));
          } else {
            params.append(k, String(v));
          }
        }
        const qs = params.toString();
        const url = qs ? `/assets/characters?${qs}` : `/assets/characters`;

        const result = await falApi<Record<string, unknown>>(
          apiToken,
          "GET",
          url,
        );
        const items = ((result as Record<string, unknown>)["characters"] ??
          []) as unknown[];
        // No cursor/offset in this response: a full page equal to the
        // effective limit (explicit or the API's default) means more results
        // may exist that we didn't fetch.
        const limit = args.limit !== undefined ? Number(args.limit) : 50;
        const truncated = limit !== undefined && items.length === limit;

        const handle = await context.writeResource("asset_characters", "main", {
          items,
          truncated,
          fetchedAt: new Date().toISOString(),
          durationMs: Date.now() - startMs,
          collectedBy: EXTENSION_NAME,
        });

        context.logger.info("Found {count} asset_characters", {
          count: items.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_asset_character: {
      description: "Create asset character",
      arguments: z.object({
        name: z.string().min(1).max(255).describe("Character display name"),
        identifier: z.string().max(64).nullable().optional().describe(
          "Optional @mention identifier for the character",
        ),
        description: z.string().min(1).max(2000).describe(
          "Text description used for character semantic matching",
        ),
        reference_images: z.array(z.string().min(1)).describe(
          "Reference images for the character. Prefer vector IDs or request IDs for exis...",
        ),
        cover_image_url: z.string().nullable().optional().describe(
          "Optional fal-hosted cover image URL for the character",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;

        const result = await falApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/assets/characters`,
          args,
        );

        const id = sanitizeInstanceName(
          String(
            ((result as Record<string, unknown>)["character"] as
              | Record<string, unknown>
              | undefined)?.["id"] ?? "created",
          ),
        );
        const handle = await context.writeResource(
          "asset_character",
          id,
          result,
        );
        context.logger.info("Created asset_character {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_asset_character: {
      description: "Get asset character",
      arguments: z.object({
        character_id: z.string().describe("Character collection ID"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;

        const result = await falApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/assets/characters/${encodeURIComponent(String(args.character_id))}`,
        );

        const handle = await context.writeResource(
          "asset_character",
          sanitizeInstanceName(String(args.character_id)),
          result,
        );
        context.logger.info("Fetched asset_character", {});
        return { dataHandles: [handle] };
      },
    },
    update_asset_character: {
      description: "Update asset character",
      arguments: z.object({
        character_id: z.string().describe("Character collection ID"),
        name: z.string().min(1).max(255).optional().describe(
          "Character display name",
        ),
        description: z.string().min(1).max(2000).optional().describe(
          "Text description used for character semantic matching",
        ),
        reference_images: z.array(z.string().min(1)).optional().describe(
          "Reference images for the character. Prefer vector IDs or request IDs for exis...",
        ),
        cover_image_url: z.string().nullable().optional().describe(
          "Optional fal-hosted cover image URL for the character",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["character_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await falApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/assets/characters/${encodeURIComponent(String(args.character_id))}`,
          body,
        );

        const handle = await context.writeResource(
          "asset_character",
          sanitizeInstanceName(String(args.character_id)),
          result,
        );
        context.logger.info("Updated asset_character", {});
        return { dataHandles: [handle] };
      },
    },
    delete_asset_character: {
      description: "Delete asset character",
      arguments: z.object({
        character_id: z.string().describe("Character collection ID"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;

        await falApi(
          apiToken,
          "DELETE",
          `/assets/characters/${encodeURIComponent(String(args.character_id))}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.character_id });
        return { dataHandles: [] };
      },
    },
    favorite_asset_character: {
      description: "Favorite asset character",
      arguments: z.object({
        character_id: z.string().describe("Character collection ID"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;

        const result = await falApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/assets/characters/${
            encodeURIComponent(String(args.character_id))
          }/favorite`,
        );

        const handle = await context.writeResource(
          "favorite_asset_character",
          "latest",
          result ?? {},
        );
        context.logger.info("Executed favorite_asset_character", {});
        return { dataHandles: [handle] };
      },
    },
    unfavorite_asset_character: {
      description: "Unfavorite asset character",
      arguments: z.object({
        character_id: z.string().describe("Character collection ID"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;

        const result = await falApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/assets/characters/${
            encodeURIComponent(String(args.character_id))
          }/unfavorite`,
        );

        const handle = await context.writeResource(
          "unfavorite_asset_character",
          "latest",
          result ?? {},
        );
        context.logger.info("Executed unfavorite_asset_character", {});
        return { dataHandles: [handle] };
      },
    },
    list_asset_entities: {
      description: "List asset entities",
      arguments: z.object({
        limit: z.number().int().min(1).max(100).optional().describe(
          "Maximum number of entities to return",
        ),
        offset: z.number().int().min(0).nullable().optional().describe(
          "Number of entities to skip",
        ),
        types: z.array(
          z.enum(["character", "prop", "environment", "style", "scene"]),
        ).nullable().optional().describe(
          "Entity types to include, comma-separated. Omit to list all types.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        const startMs = Date.now();
        const params = new URLSearchParams();
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (v === undefined || v === null || excludeKeys.has(k)) continue;
          if (Array.isArray(v)) {
            for (const item of v) params.append(k, String(item));
          } else {
            params.append(k, String(v));
          }
        }
        const qs = params.toString();
        const url = qs ? `/assets/entities?${qs}` : `/assets/entities`;

        const result = await falApi<Record<string, unknown>>(
          apiToken,
          "GET",
          url,
        );
        const items =
          ((result as Record<string, unknown>)["entities"] ?? []) as unknown[];
        // No cursor/offset in this response: a full page equal to the
        // effective limit (explicit or the API's default) means more results
        // may exist that we didn't fetch.
        const limit = args.limit !== undefined ? Number(args.limit) : 50;
        const truncated = limit !== undefined && items.length === limit;

        const handle = await context.writeResource("asset_entities", "main", {
          items,
          truncated,
          fetchedAt: new Date().toISOString(),
          durationMs: Date.now() - startMs,
          collectedBy: EXTENSION_NAME,
        });

        context.logger.info("Found {count} asset_entities", {
          count: items.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_asset_entity: {
      description: "Create asset entity",
      arguments: z.object({
        type: z.enum(["character", "prop", "environment", "style", "scene"])
          .describe("Smart entity type"),
        name: z.string().min(1).max(255).describe("Entity display name"),
        handle: z.string().min(1).max(64).optional().describe(
          "Optional @mention handle. Defaults from the name; must be unique across all e...",
        ),
        description: z.string().min(1).max(2000).nullable().optional().describe(
          "Visual description. Required and non-empty for characters; optional for other...",
        ),
        reference_images: z.array(z.string().min(1)).describe(
          "Defining reference images. Use saved asset IDs, request IDs, vector IDs, or f...",
        ),
        cover_image_url: z.string().nullable().optional().describe(
          "Cover image URL. Must be one of the entity's reference images.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;

        const result = await falApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/assets/entities`,
          args,
        );

        const id = sanitizeInstanceName(
          String(
            ((result as Record<string, unknown>)["entity"] as
              | Record<string, unknown>
              | undefined)?.["id"] ?? "created",
          ),
        );
        const handle = await context.writeResource("asset_entity", id, result);
        context.logger.info("Created asset_entity {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_asset_entity: {
      description: "Get asset entity",
      arguments: z.object({
        entity_id: z.string().describe("Smart entity ID"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;

        const result = await falApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/assets/entities/${encodeURIComponent(String(args.entity_id))}`,
        );

        const handle = await context.writeResource(
          "asset_entity",
          sanitizeInstanceName(String(args.entity_id)),
          result,
        );
        context.logger.info("Fetched asset_entity", {});
        return { dataHandles: [handle] };
      },
    },
    update_asset_entity: {
      description: "Update asset entity",
      arguments: z.object({
        entity_id: z.string().describe("Smart entity ID"),
        name: z.string().min(1).max(255).optional().describe(
          "Entity display name",
        ),
        handle: z.string().min(1).max(64).optional().describe(
          "Replacement @mention handle. Character handles cannot be changed.",
        ),
        description: z.string().min(1).max(2000).nullable().optional().describe(
          "Replacement visual description; null clears it for non-character entities.",
        ),
        reference_images: z.array(z.string().min(1)).optional().describe(
          "Replace the defining references with these saved asset IDs, request IDs, vect...",
        ),
        cover_image_url: z.string().nullable().optional().describe(
          "Replacement cover image URL from the entity's reference images; null clears t...",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["entity_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await falApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/assets/entities/${encodeURIComponent(String(args.entity_id))}`,
          body,
        );

        const handle = await context.writeResource(
          "asset_entity",
          sanitizeInstanceName(String(args.entity_id)),
          result,
        );
        context.logger.info("Updated asset_entity", {});
        return { dataHandles: [handle] };
      },
    },
    delete_asset_entity: {
      description: "Delete asset entity",
      arguments: z.object({
        entity_id: z.string().describe("Smart entity ID"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;

        await falApi(
          apiToken,
          "DELETE",
          `/assets/entities/${encodeURIComponent(String(args.entity_id))}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.entity_id });
        return { dataHandles: [] };
      },
    },
    list_asset_tags: {
      description: "List asset tags",
      arguments: z.object({}),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        const startMs = Date.now();
        const params = new URLSearchParams();
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (v === undefined || v === null || excludeKeys.has(k)) continue;
          if (Array.isArray(v)) {
            for (const item of v) params.append(k, String(item));
          } else {
            params.append(k, String(v));
          }
        }
        const qs = params.toString();
        const url = qs ? `/assets/tags?${qs}` : `/assets/tags`;

        const result = await falApi<Record<string, unknown>>(
          apiToken,
          "GET",
          url,
        );
        const items =
          ((result as Record<string, unknown>)["tags"] ?? []) as unknown[];
        const truncated = false;

        const handle = await context.writeResource("asset_tags", "main", {
          items,
          truncated,
          fetchedAt: new Date().toISOString(),
          durationMs: Date.now() - startMs,
          collectedBy: EXTENSION_NAME,
        });

        context.logger.info("Found {count} asset_tags", {
          count: items.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_asset_tag: {
      description: "Create asset tag",
      arguments: z.object({
        name: z.string().min(1).max(50).describe("Tag name"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;

        const result = await falApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/assets/tags`,
          args,
        );

        const id = sanitizeInstanceName(
          String(
            ((result as Record<string, unknown>)["tag"] as
              | Record<string, unknown>
              | undefined)?.["id"] ?? "created",
          ),
        );
        const handle = await context.writeResource("asset_tag", id, result);
        context.logger.info("Created asset_tag {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    set_asset_tags_for_asset: {
      description: "Set tags for an asset",
      arguments: z.object({
        asset_id: z.string().min(1).optional().describe(
          "Asset ID (canonical catalog identity)",
        ),
        request_id: z.string().min(1).optional().describe(
          "Request ID to save as an asset before mutating",
        ),
        vector_id: z.string().min(1).optional().describe(
          "Vector ID to save as an asset before mutating. Prefer asset_id",
        ),
        tag_ids: z.array(z.string()).describe(
          "Full replacement set of tag IDs",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;

        const result = await falApi<Record<string, unknown>>(
          apiToken,
          "PUT",
          `/assets/tags`,
          args,
        );

        const handle = await context.writeResource(
          "set_asset_tags_for_asset",
          "updated",
          result,
        );
        context.logger.info("Updated set_asset_tags_for_asset", {});
        return { dataHandles: [handle] };
      },
    },
    update_asset_tag: {
      description: "Update asset tag",
      arguments: z.object({
        tag_id: z.string().describe("Tag ID"),
        name: z.string().min(1).max(50).optional().describe("Tag name"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["tag_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await falApi<Record<string, unknown>>(
          apiToken,
          "PATCH",
          `/assets/tags/${encodeURIComponent(String(args.tag_id))}`,
          body,
        );

        const handle = await context.writeResource(
          "asset_tag",
          sanitizeInstanceName(String(args.tag_id)),
          result,
        );
        context.logger.info("Updated asset_tag", {});
        return { dataHandles: [handle] };
      },
    },
    delete_asset_tag: {
      description: "Delete asset tag",
      arguments: z.object({
        tag_id: z.string().describe("Tag ID"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;

        await falApi(
          apiToken,
          "DELETE",
          `/assets/tags/${encodeURIComponent(String(args.tag_id))}`,
        );

        context.logger.info("Deleted resource {id}", { id: args.tag_id });
        return { dataHandles: [] };
      },
    },
    upload_asset: {
      description: "Upload asset",
      arguments: z.object({
        url: z.string().describe(
          "fal-hosted media URL to ingest into the asset library",
        ),
        type: z.enum(["image", "video", "audio", "3d"]).describe(
          "Media type for the uploaded asset",
        ),
        prompt: z.string().min(1).max(2000).nullable().optional().describe(
          "Optional caller-provided caption or description to index with the uploaded asset",
        ),
        collection_id: z.string().nullable().optional().describe(
          "Optional manual collection ID to add the uploaded asset to",
        ),
        favorite: z.boolean().optional().describe(
          "Whether to favorite the uploaded asset immediately",
        ),
        tag_ids: z.array(z.string()).optional().describe(
          "Tag IDs to assign to the uploaded asset",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;

        const result = await falApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/assets/uploads`,
          args,
        );

        const id = sanitizeInstanceName(
          String(
            ((result as Record<string, unknown>)["asset"] as
              | Record<string, unknown>
              | undefined)?.["vector_id"] ?? "created",
          ),
        );
        const handle = await context.writeResource("upload_asset", id, result);
        context.logger.info("Created upload_asset {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_asset: {
      description: "Get asset",
      arguments: z.object({
        asset_id: z.string().describe("Asset ID. A vector ID is also accepted"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;

        const result = await falApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/assets/${encodeURIComponent(String(args.asset_id))}`,
        );

        const handle = await context.writeResource(
          "asset",
          sanitizeInstanceName(String(args.asset_id)),
          result,
        );
        context.logger.info("Fetched asset", {});
        return { dataHandles: [handle] };
      },
    },
    get_asset_lineage: {
      description: "Get asset lineage",
      arguments: z.object({
        asset_id: z.string().describe("Asset ID"),
        depth: z.number().int().min(1).max(5).optional().describe(
          "Maximum traversal depth (levels of derivation edges)",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;

        const queryParts: string[] = [];
        const queryKeys = new Set(["depth"]);
        for (const [k, v] of Object.entries(args)) {
          if (v === undefined || v === null || !queryKeys.has(k)) continue;
          if (Array.isArray(v)) {
            for (const item of v) {
              queryParts.push(`${k}=${encodeURIComponent(String(item))}`);
            }
          } else {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await falApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/assets/${encodeURIComponent(String(args.asset_id))}/lineage${qs}`,
        );

        const handle = await context.writeResource(
          "asset_lineage",
          sanitizeInstanceName(String(args.asset_id)),
          result,
        );
        context.logger.info("Fetched asset_lineage", {});
        return { dataHandles: [handle] };
      },
    },
    favorite_asset: {
      description: "Favorite asset",
      arguments: z.object({
        asset_id: z.string().min(1).optional().describe(
          "Asset ID (canonical catalog identity)",
        ),
        request_id: z.string().min(1).optional().describe(
          "Request ID to save as an asset before mutating",
        ),
        vector_id: z.string().min(1).optional().describe(
          "Vector ID to save as an asset before mutating. Prefer asset_id",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;

        const result = await falApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/assets/favorite`,
          args,
        );

        // No id- or name-shaped field anywhere in this response —
        // derive a deterministic, collision-resistant instance name
        // from the request instead of colliding every call onto "created".
        const id = sanitizeInstanceName(await shortHash(JSON.stringify(args)));
        const handle = await context.writeResource(
          "favorite_asset",
          id,
          result,
        );
        context.logger.info("Created favorite_asset {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    unfavorite_asset: {
      description: "Unfavorite asset",
      arguments: z.object({
        asset_id: z.string().min(1).optional().describe(
          "Asset ID (canonical catalog identity)",
        ),
        request_id: z.string().min(1).optional().describe(
          "Request ID to save as an asset before mutating",
        ),
        vector_id: z.string().min(1).optional().describe(
          "Vector ID to save as an asset before mutating. Prefer asset_id",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;

        const result = await falApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/assets/unfavorite`,
          args,
        );

        // No id- or name-shaped field anywhere in this response —
        // derive a deterministic, collision-resistant instance name
        // from the request instead of colliding every call onto "created".
        const id = sanitizeInstanceName(await shortHash(JSON.stringify(args)));
        const handle = await context.writeResource(
          "unfavorite_asset",
          id,
          result,
        );
        context.logger.info("Created unfavorite_asset {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    list_asset_tags_for_asset: {
      description: "List tags for an asset",
      arguments: z.object({
        asset_id: z.string().describe("Asset ID. A vector ID is also accepted"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        const startMs = Date.now();
        const params = new URLSearchParams();
        const excludeKeys = new Set<string>(["asset_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (v === undefined || v === null || excludeKeys.has(k)) continue;
          if (Array.isArray(v)) {
            for (const item of v) params.append(k, String(item));
          } else {
            params.append(k, String(v));
          }
        }
        const qs = params.toString();
        const url = qs
          ? `/assets/${encodeURIComponent(String(args.asset_id))}/tags?${qs}`
          : `/assets/${encodeURIComponent(String(args.asset_id))}/tags`;

        const result = await falApi<Record<string, unknown>>(
          apiToken,
          "GET",
          url,
        );
        const items =
          ((result as Record<string, unknown>)["tags"] ?? []) as unknown[];
        const truncated = false;

        const handle = await context.writeResource(
          "asset_tags_for_asset",
          "main",
          {
            items,
            truncated,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );

        context.logger.info("Found {count} asset_tags_for_asset", {
          count: items.length,
        });
        return { dataHandles: [handle] };
      },
    },
    assign_asset_tag: {
      description: "Assign tag to asset",
      arguments: z.object({
        tag_id: z.string().describe("Tag ID"),
        asset_id: z.string().min(1).optional().describe(
          "Asset ID (canonical catalog identity)",
        ),
        request_id: z.string().min(1).optional().describe(
          "Request ID to save as an asset before mutating",
        ),
        vector_id: z.string().min(1).optional().describe(
          "Vector ID to save as an asset before mutating. Prefer asset_id",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["tag_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await falApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/assets/tags/${encodeURIComponent(String(args.tag_id))}/assign`,
          body,
        );

        // No id- or name-shaped field anywhere in this response —
        // derive a deterministic, collision-resistant instance name
        // from the request instead of colliding every call onto "created".
        const id = sanitizeInstanceName(await shortHash(JSON.stringify(args)));
        const handle = await context.writeResource(
          "assign_asset_tag",
          id,
          result,
        );
        context.logger.info("Created assign_asset_tag {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    unassign_asset_tag: {
      description: "Unassign tag from asset",
      arguments: z.object({
        tag_id: z.string().describe("Tag ID"),
        asset_id: z.string().min(1).optional().describe(
          "Asset ID (canonical catalog identity)",
        ),
        request_id: z.string().min(1).optional().describe(
          "Request ID to save as an asset before mutating",
        ),
        vector_id: z.string().min(1).optional().describe(
          "Vector ID to save as an asset before mutating. Prefer asset_id",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["tag_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        await falApi(
          apiToken,
          "DELETE",
          `/assets/tags/${encodeURIComponent(String(args.tag_id))}/assign`,
          body,
        );

        context.logger.info("Deleted resource {id}", { id: args.tag_id });
        return { dataHandles: [] };
      },
    },
  },
};
