// fal.ai Serverless — app deployments, queue, revisions, files, logs, metrics, requests, usage — Tests
// Auto-generated by scripts/falai-codegen — do not edit manually.
// SPDX-License-Identifier: Apache-2.0

import { assertEquals, assertExists } from "jsr:@std/assert@1.0.19";
import { createModelTestContext } from "@systeminit/swamp-testing";
import { model } from "./serverless.ts";

// ---------------------------------------------------------------------------
// Model Export Structure Tests
// ---------------------------------------------------------------------------

Deno.test("serverless model: has correct type", () => {
  assertEquals(model.type, "@webframp/falai/serverless");
});

Deno.test("serverless model: has valid version format", () => {
  const versionPattern = /^\d{4}\.\d{2}\.\d{2}\.\d+$/;
  assertEquals(versionPattern.test(model.version), true);
});

Deno.test("serverless model: has globalArguments with apiToken", () => {
  assertExists(model.globalArguments);
  const shape = model.globalArguments.shape;
  assertExists(shape.apiToken);
});

Deno.test("serverless model: has expected methods", () => {
  assertExists(model.methods);
  assertExists(model.methods.get_analytics);
  assertExists(model.methods.list_apps);
  assertExists(model.methods.get_app_queue_info);
  assertExists(model.methods.delete_serverless_flush_app_queue);
  assertExists(model.methods.get_runner_history);
  assertExists(model.methods.list_app_events);
  assertExists(model.methods.list_app_revisions);
  assertExists(model.methods.list_root);
  assertExists(model.methods.list_directory);
  assertExists(model.methods.upload_from_url);
  assertExists(model.methods.serverless_logs_history);
  assertExists(model.methods.list_requests_by_endpoint);
  assertExists(model.methods.get_usage);
});

Deno.test("serverless model: has expected resources", () => {
  assertExists(model.resources);
  assertExists(model.resources["get_analytics"]);
  assertExists(model.resources["apps"]);
  assertExists(model.resources["app_queue_info"]);
  assertExists(model.resources["get_runner_history"]);
  assertExists(model.resources["app_events"]);
  assertExists(model.resources["app_revisions"]);
  assertExists(model.resources["list_root"]);
  assertExists(model.resources["list_directory"]);
  assertExists(model.resources["upload_from_url"]);
  assertExists(model.resources["serverless_logs_history"]);
  assertExists(model.resources["requests_by_endpoint"]);
  assertExists(model.resources["get_usage"]);
});

// ---------------------------------------------------------------------------
// Mock fal.ai API Server
// ---------------------------------------------------------------------------

function startMockFalServer(
  responses: Record<string, { body: unknown }>,
): { url: string; server: Deno.HttpServer } {
  const server = Deno.serve({ port: 0, onListen() {} }, (req) => {
    const url = new URL(req.url);
    const path = url.pathname;

    for (const [pattern, { body }] of Object.entries(responses)) {
      if (path.includes(pattern)) {
        return Response.json(body);
      }
    }

    return Response.json(
      { error: { type: "not_found", message: "Not found" } },
      { status: 404 },
    );
  });

  const addr = server.addr as Deno.NetAddr;
  return { url: `http://localhost:${addr.port}`, server };
}

function installFetchMock(mockUrl: string): () => void {
  const originalFetch = globalThis.fetch;
  globalThis.fetch = (input, init) => {
    const reqUrl = typeof input === "string" ? input : input.toString();
    const newUrl = reqUrl.replace("https://api.fal.ai/v1", mockUrl);
    return originalFetch(newUrl, init);
  };
  return () => {
    globalThis.fetch = originalFetch;
  };
}

Deno.test({
  name: "serverless model: get_analytics fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockBody = {
      "time_series": [{
        "bucket": "test-value",
        "results": [{
          "endpoint_id": "test-value",
          "request_count": 0,
          "success_count": 0,
          "user_error_count": 0,
          "error_count": 0,
          "p50_prepare_duration": 0,
          "p75_prepare_duration": 0,
          "p90_prepare_duration": 0,
          "p95_prepare_duration": 0,
          "p99_prepare_duration": 0,
          "p50_duration": 0,
          "p75_duration": 0,
          "p90_duration": 0,
          "p25_duration": 0,
          "p95_duration": 0,
          "p99_duration": 0,
          "startup_error_count": 0,
          "connection_error_count": 0,
          "timeout_error_count": 0,
          "runtime_error_count": 0,
          "cold_boot_count": 0,
          "p50_cold_boot_duration": 0,
          "p75_cold_boot_duration": 0,
          "p90_cold_boot_duration": 0,
          "total_billable_duration": 0,
        }],
      }],
    };
    const { url, server } = startMockFalServer({
      "/serverless/analytics": { body: mockBody },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token" },
        definition: {
          id: "test-id",
          name: "test-serverless",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).get_analytics.execute({}, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "serverless model: get_app_queue_info fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockBody = { "queue_size": 5 };
    const { url, server } = startMockFalServer({
      "/serverless/apps/test-id-123/test-id-123/queue": { body: mockBody },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token" },
        definition: {
          id: "test-id",
          name: "test-serverless",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).get_app_queue_info.execute({
        "owner": "test-id-123",
        "name": "test-id-123",
      }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name:
    "serverless model: delete_serverless_flush_app_queue executes successfully",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockFalServer({
      "/serverless/apps/test-id-123/test-id-123/queue": {
        body: { id: "test-id-123" },
      },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context } = createModelTestContext({
        globalArgs: { "apiToken": "test-token" },
        definition: {
          id: "test-id",
          name: "test-serverless",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).delete_serverless_flush_app_queue.execute({
        "owner": "test-id-123",
        "name": "test-id-123",
      }, context);
      assertEquals(result.dataHandles.length, 0);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "serverless model: upload_from_url executes and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockBody = true;
    const { url, server } = startMockFalServer({
      "/serverless/files/file/url/test-id-123": { body: mockBody },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token" },
        definition: {
          id: "test-id",
          name: "test-serverless",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).upload_from_url.execute({ "file": "test-id-123" }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});
