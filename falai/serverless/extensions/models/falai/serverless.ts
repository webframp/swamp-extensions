/**
 * fal.ai Serverless — app deployments, queue, revisions, files, logs, metrics, requests, usage
 *
 * Auto-generated by scripts/falai-codegen — do not edit manually.
 *
 * @module
 */
// SPDX-License-Identifier: Apache-2.0

import { z } from "npm:zod@4.4.3";
import { falApi, falApiPaginated, sanitizeInstanceName } from "./_lib/api.ts";

const EXTENSION_NAME = "@webframp/falai/serverless";

// =============================================================================
// Schemas
// =============================================================================

const GlobalArgsSchema = z.object({
  apiToken: z.string().meta({ sensitive: true }).describe(
    "fal.ai API key; overrides the FAL_KEY environment variable. Wire with a vault.get(...) expression to source it from a vault.",
  ).optional(),
});

const GetAnalyticsItemSchema = z.object({
  bucket: z.string().describe(
    "Time bucket timestamp in user's timezone with offset (ISO8601 datetime)",
  ),
  results: z.array(z.object({
    endpoint_id: z.string(),
    request_count: z.number().int().min(0).optional(),
    success_count: z.number().int().min(0).optional(),
    user_error_count: z.number().int().min(0).optional(),
    error_count: z.number().int().min(0).optional(),
    p50_prepare_duration: z.number().min(0).optional(),
    p75_prepare_duration: z.number().min(0).optional(),
    p90_prepare_duration: z.number().min(0).optional(),
    p95_prepare_duration: z.number().min(0).optional(),
    p99_prepare_duration: z.number().min(0).optional(),
    p50_duration: z.number().min(0).optional(),
    p75_duration: z.number().min(0).optional(),
    p90_duration: z.number().min(0).optional(),
    p25_duration: z.number().min(0).optional(),
    p95_duration: z.number().min(0).optional(),
    p99_duration: z.number().min(0).optional(),
    startup_error_count: z.number().int().min(0).optional(),
    connection_error_count: z.number().int().min(0).optional(),
    timeout_error_count: z.number().int().min(0).optional(),
    runtime_error_count: z.number().int().min(0).optional(),
    cold_boot_count: z.number().int().min(0).optional(),
    p50_cold_boot_duration: z.number().min(0).optional(),
    p75_cold_boot_duration: z.number().min(0).optional(),
    p90_cold_boot_duration: z.number().min(0).optional(),
    total_billable_duration: z.number().min(0).optional(),
  })).describe("Analytics records for this time bucket"),
}).passthrough();

const GetAnalyticsSchema = z.object({
  items: z.array(GetAnalyticsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const AppsItemSchema = z.object({
  endpoint_id: z.string().describe(
    "Canonical endpoint identifier ('<owner>/<name>') accepted by the analytics, requests, logs, and q...",
  ),
  name: z.string().describe("Application name (alias)"),
  owner: z.string().describe("Nickname of the app owner"),
  environment: z.string().describe("Environment the app is deployed in"),
  machine_type: z.string().describe("Machine type the app runs on"),
  auth_mode: z.enum(["private", "public", "shared"]).describe(
    "Endpoint authentication mode",
  ),
  keep_alive: z.number().describe(
    "Seconds a runner stays warm after its last request",
  ),
  min_concurrency: z.number().describe("Minimum number of runners kept alive"),
  max_concurrency: z.number().describe("Maximum number of concurrent runners"),
  request_timeout: z.number().describe("Request timeout in seconds"),
  startup_timeout: z.number().describe("Runner startup timeout in seconds"),
  valid_regions: z.array(z.string()).describe("Regions the app may run in"),
  updated_at: z.string().describe("Last update timestamp (UTC ISO8601)"),
  endpoints: z.array(z.string()).optional().describe(
    "Registered route-level endpoint ids for this app (only present when expand=endpoints). Multi-rout...",
  ),
}).passthrough();

const ListAppsSchema = z.object({
  items: z.array(AppsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const GetAppQueueInfoSchema = z.object({
  queue_size: z.number().int().min(0).describe(
    "Current number of requests in the queue",
  ),
}).passthrough();

const GetRunnerHistoryItemSchema = z.object({
  timestamp: z.string().describe("Bucket start timestamp (UTC ISO8601)"),
  running: z.number().describe("Runners actively processing requests"),
  idle: z.number().describe(
    "Warm runners waiting for requests (billed but unused)",
  ),
  pending: z.number().describe(
    "Runners waiting to be scheduled or starting up",
  ),
  draining: z.number().describe(
    "Runners finishing in-flight work before shutting down",
  ),
}).passthrough();

const GetRunnerHistorySchema = z.object({
  items: z.array(GetRunnerHistoryItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const AppEventsItemSchema = z.object({
  event_id: z.string().describe("Unique event identifier"),
  category: z.enum([
    "runner_started",
    "runner_failed",
    "runner_finished",
    "runner_pending",
    "runner_startup_failure",
    "runner_docker_pull",
    "runner_setup",
    "runner_draining",
    "runner_stopping",
    "deployment_started",
    "deployment_failed",
    "deployment_ended",
    "deployment_rolling_started",
    "deployment_rolling_failed",
    "deployment_rolling_ended",
    "deployment_recreate_applied",
    "config_changed",
  ]).describe("Event category"),
  created_at: z.string().describe("Event timestamp (UTC ISO8601)"),
  payload: z.object({
    job_id: z.string().optional(),
    machine_type: z.string().optional(),
    reason: z.string().optional(),
    state: z.string().optional(),
    new_application_id: z.string().optional(),
    old_application_id: z.string().optional(),
    old_config: z.record(z.string(), z.unknown()).optional(),
    new_config: z.record(z.string(), z.unknown()).optional(),
    old_app_auth_mode: z.string().optional(),
    new_app_auth_mode: z.string().optional(),
    actor: z.object({
      nickname: z.string().optional(),
      full_name: z.string().optional(),
    }).optional(),
  }).describe(
    "Event details; populated fields depend on the event category (runner_*, deployment_*, config_chan...",
  ),
}).passthrough();

const ListAppEventsSchema = z.object({
  items: z.array(AppEventsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const AppRevisionsItemSchema = z.object({
  revision_id: z.string().describe("Unique revision identifier"),
  created_at: z.string().describe("Revision creation timestamp (UTC ISO8601)"),
  is_current: z.boolean().describe(
    "Whether this revision is the one currently serving traffic",
  ),
  message: z.string().nullable().describe(
    "Freeform message attached to this revision at deploy time (fal deploy --message). Null when the d...",
  ),
  annotations: z.record(z.string(), z.string()).nullable().describe(
    "Custom key/value annotations attached to this revision at deploy time (fal deploy --annotation KE...",
  ),
  status: z.enum(["deployed", "failed", "deploying"]).nullable().describe(
    "Deployment status derived from recent deployment events. Null when no deployment events are avail...",
  ),
  deployed_by: z.string().nullable().describe(
    "Nickname or display name of the user who deployed this revision, when known from recent deploymen...",
  ),
}).passthrough();

const ListAppRevisionsSchema = z.object({
  items: z.array(AppRevisionsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const ListRootSchema = z.array(z.object({
  path: z.string(),
  name: z.string(),
  created_time: z.string(),
  updated_time: z.string(),
  is_file: z.boolean(),
  size: z.number(),
  checksum_sha256: z.string().optional(),
  checksum_md5: z.string().optional(),
}));

const ListDirectorySchema = z.array(z.object({
  path: z.string(),
  name: z.string(),
  created_time: z.string(),
  updated_time: z.string(),
  is_file: z.boolean(),
  size: z.number(),
  checksum_sha256: z.string().optional(),
  checksum_md5: z.string().optional(),
}));

const UploadFromUrlSchema = z.boolean();

const ServerlessLogsHistorySchema = z.object({
  timestamp: z.string().describe("ISO timestamp of the log line"),
  level: z.string().describe("Log level"),
  message: z.string().describe("Log message"),
  app: z.string().describe("App identifier"),
  revision: z.string().describe("Revision identifier"),
  labels: z.record(z.string(), z.string()).optional().describe(
    "Additional labels",
  ),
}).passthrough();

const RequestsByEndpointItemSchema = z.object({
  request_id: z.string().describe("Unique identifier for the request"),
  endpoint_id: z.string().describe(
    "Endpoint that was executed for this request",
  ),
  started_at: z.string().describe("Time when request processing started"),
  sent_at: z.string().describe("Time when request was sent to the backend"),
  ended_at: z.string().nullable().optional().describe(
    "Time when request finished processing",
  ),
  status_code: z.number().int().nullable().optional().describe(
    "HTTP status code returned by the request",
  ),
  duration: z.number().nullable().optional().describe(
    "Total request duration in seconds",
  ),
  json_input: z.unknown().optional().describe("Input payload for the request"),
  json_output: z.unknown().optional().describe(
    "Output payload for the request",
  ),
  runner_id: z.string().nullable().describe(
    "Unique identifier for the runner execution instance. Null if no runner was assigned (e.g. the req...",
  ),
  billable_units: z.number().nullable().optional().describe(
    "Billable units for this request (only present when expand=billing). Null if a billing event has n...",
  ),
}).passthrough();

const ListRequestsByEndpointSchema = z.object({
  items: z.array(RequestsByEndpointItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const GetUsageItemSchema = z.object({
  bucket: z.string().describe(
    "Time bucket timestamp in user's timezone with offset (ISO8601 datetime)",
  ),
  results: z.array(z.object({
    app: z.string().nullable(),
    environment: z.string().nullable(),
    machine_type: z.string(),
    unit: z.string(),
    quantity: z.number().min(0),
    unit_price: z.number().min(0),
    percent_discount: z.number().min(0).max(100).nullable(),
    cost_subtotal: z.number().min(0),
    cost_discount: z.number().min(0),
    cost_total: z.number().min(0),
    cost: z.number().min(0),
    currency: z.string().min(3).max(3),
    is_surge: z.boolean(),
  })).describe("Usage records for this time bucket"),
}).passthrough();

const GetUsageSchema = z.object({
  items: z.array(GetUsageItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

// =============================================================================
// Model Definition
// =============================================================================

/** fal.ai Serverless — app deployments, queue, revisions, files, logs, metrics, requests, usage */
export const model = {
  type: "@webframp/falai/serverless",
  version: "2026.09.10.3",
  globalArguments: GlobalArgsSchema,

  upgrades: [
    {
      toVersion: "2026.09.09.2",
      description: "Regenerated from updated API spec; no migration required",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.09.10.1",
      description: "Regenerated from updated API spec; no migration required",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.09.10.2",
      description: "Regenerated from updated API spec; no migration required",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
    {
      toVersion: "2026.09.10.3",
      description: "Regenerated from updated API spec; no migration required",
      upgradeAttributes: (old: Record<string, unknown>) => old,
    },
  ],

  resources: {
    "get_analytics": {
      description: "Analytics",
      schema: GetAnalyticsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "apps": {
      description: "List Applications",
      schema: ListAppsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "app_queue_info": {
      description: "Queue Size",
      schema: GetAppQueueInfoSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "get_runner_history": {
      description: "Runner State History",
      schema: GetRunnerHistorySchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "app_events": {
      description: "Application Events",
      schema: ListAppEventsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "app_revisions": {
      description: "Application Revisions",
      schema: ListAppRevisionsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "list_root": {
      description: "List files (root)",
      schema: ListRootSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "list_directory": {
      description: "List files (directory)",
      schema: ListDirectorySchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "upload_from_url": {
      description: "Upload file from URL",
      schema: UploadFromUrlSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "serverless_logs_history": {
      description: "Logs history (paginated)",
      schema: ServerlessLogsHistorySchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "requests_by_endpoint": {
      description: "List requests by endpoint(s)",
      schema: ListRequestsByEndpointSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "get_usage": {
      description: "Usage",
      schema: GetUsageSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
  },

  methods: {
    get_analytics: {
      description: "Analytics",
      arguments: z.object({
        limit: z.number().int().min(1).optional().describe(
          "Maximum number of items to return. Actual maximum depends on query type and expansion parameters.",
        ),
        cursor: z.string().optional().describe(
          "Pagination cursor from previous response. Encodes the page number.",
        ),
        start: z.union([
          z.string(),
          z.string().regex(new RegExp("^\\d{4}-\\d{2}-\\d{2}$")),
        ]).optional().describe(
          "Start date in ISO8601 format (e.g., '2025-01-01T00:00:00Z' or '2025-01-01'). Defaults to 24 hours ago.",
        ),
        end: z.union([
          z.string(),
          z.string().regex(new RegExp("^\\d{4}-\\d{2}-\\d{2}$")),
        ]).optional().describe(
          "End date in ISO8601 format, exclusive (e.g., '2025-02-01T00:00:00Z' or '2025-02-01'). Data up to but not including this timestamp is returned. Defaults to current time.",
        ),
        timezone: z.string().optional().describe(
          "Timezone for date aggregation and boundaries. All timestamps in responses are in UTC, but this controls how dates are bucketed.",
        ),
        timeframe: z.enum(["minute", "hour", "day", "week", "month"]).optional()
          .describe(
            "Aggregation timeframe for timeseries data (auto-detected from date range if not specified). Auto-detection uses: minute (<2h), hour (<2d), day (<64d), week (<183d), month (>=183d).",
          ),
        bound_to_timeframe: z.enum(["true", "false"]).optional().describe(
          "Whether to adjust start/end dates to align with timeframe boundaries and use exclusive end. Defaults to true. When true, dates are aligned to the start of the timeframe period (e.g., start of day) and end is made exclusive (e.g., start of next day). When false, uses exact dates provided.",
        ),
        endpoint_id: z.union([z.string(), z.array(z.string())]).optional()
          .describe(
            "Filter by specific endpoint ID(s). Accepts 1-50 endpoint IDs. Supports comma-separated values: ?endpoint_id=model1,model2 or array syntax: ?endpoint_id=model1&endpoint_id=model2",
          ),
        expand: z.union([z.string(), z.array(z.string())]).optional().describe(
          "Data and metrics to include in the response. Use 'time_series' for time-bucketed data, metric names for specific metrics in time series, and 'summary' for aggregate statistics. At least one of 'time_series' or 'summary' and at least one metric are required.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        const startMs = Date.now();
        const params: Record<string, string | string[]> = {};
        const excludeKeys = new Set<string>(["limit", "cursor"]);
        for (const [k, v] of Object.entries(args)) {
          if (v === undefined || v === null || excludeKeys.has(k)) continue;
          params[k] = Array.isArray(v) ? v.map(String) : String(v);
        }
        const requestedLimit = args.limit !== undefined
          ? Number(args.limit)
          : undefined;
        const requestedCursor = typeof args.cursor === "string"
          ? args.cursor
          : undefined;

        const { results, truncated } = await falApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/serverless/analytics`,
          "time_series",
          params,
          { limit: requestedLimit, cursor: requestedCursor },
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("get_analytics", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
          durationMs: Date.now() - startMs,
          collectedBy: EXTENSION_NAME,
        });

        context.logger.info("Found {count} get_analytics", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    list_apps: {
      description: "List Applications",
      arguments: z.object({
        environment: z.string().optional().describe(
          "Environment to list apps from. Defaults to the main environment.",
        ),
        search: z.string().optional().describe(
          "Case-insensitive substring match on the app name",
        ),
        expand: z.union([z.string(), z.array(z.string())]).optional().describe(
          "Fields to expand in the response. Use endpoints to include each app's registered route-level endpoint ids.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        const startMs = Date.now();
        const params = new URLSearchParams();
        const excludeKeys = new Set<string>([]);
        for (const [k, v] of Object.entries(args)) {
          if (v === undefined || v === null || excludeKeys.has(k)) continue;
          if (Array.isArray(v)) {
            for (const item of v) params.append(k, String(item));
          } else {
            params.append(k, String(v));
          }
        }
        const qs = params.toString();
        const url = qs ? `/serverless/apps?${qs}` : `/serverless/apps`;

        const result = await falApi<Record<string, unknown>>(
          apiToken,
          "GET",
          url,
        );
        const items =
          ((result as Record<string, unknown>)["apps"] ?? []) as unknown[];
        const truncated = false;

        const handle = await context.writeResource("apps", "main", {
          items,
          truncated,
          fetchedAt: new Date().toISOString(),
          durationMs: Date.now() - startMs,
          collectedBy: EXTENSION_NAME,
        });

        context.logger.info("Found {count} apps", { count: items.length });
        return { dataHandles: [handle] };
      },
    },
    get_app_queue_info: {
      description: "Queue Size",
      arguments: z.object({
        owner: z.string().describe("Username of the app owner"),
        name: z.string().describe("Application name"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;

        const result = await falApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/serverless/apps/${encodeURIComponent(String(args.owner))}/${
            encodeURIComponent(String(args.name))
          }/queue`,
        );

        const handle = await context.writeResource(
          "app_queue_info",
          sanitizeInstanceName(
            [String(args.owner), String(args.name)].join("_"),
          ),
          result,
        );
        context.logger.info("Fetched app_queue_info", {});
        return { dataHandles: [handle] };
      },
    },
    delete_serverless_flush_app_queue: {
      description: "Flush Application Queue",
      arguments: z.object({
        owner: z.string().describe("Username of the app owner"),
        name: z.string().describe("Application name"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;

        await falApi(
          apiToken,
          "DELETE",
          `/serverless/apps/${encodeURIComponent(String(args.owner))}/${
            encodeURIComponent(String(args.name))
          }/queue`,
        );

        context.logger.info("Deleted resource {id}", { id: args.name });
        return { dataHandles: [] };
      },
    },
    get_runner_history: {
      description: "Runner State History",
      arguments: z.object({
        owner: z.string().describe("Username of the app owner"),
        name: z.string().describe("Application name"),
        start: z.union([
          z.string(),
          z.string().regex(new RegExp("^\\d{4}-\\d{2}-\\d{2}$")),
        ]).optional().describe(
          "Start date in ISO8601 format (e.g., '2025-01-01T00:00:00Z' or '2025-01-01'). Defaults to 24 hours ago.",
        ),
        end: z.union([
          z.string(),
          z.string().regex(new RegExp("^\\d{4}-\\d{2}-\\d{2}$")),
        ]).optional().describe(
          "End date in ISO8601 format, exclusive (e.g., '2025-02-01T00:00:00Z' or '2025-02-01'). Data up to but not including this timestamp is returned. Defaults to current time.",
        ),
        timeframe: z.enum(["minute", "hour", "day", "week", "month"]).optional()
          .describe(
            "Bucket size for the time series (auto-detected from the date range if not specified). Auto-detection uses: minute (<2h), hour (<2d), day (<64d), week (<183d), month (>=183d).",
          ),
        aggregation: z.enum(["max", "avg"]).optional().describe(
          "How runner counts are aggregated within each bucket: 'max' shows peak concurrency (capacity analysis), 'avg' shows mean concurrency (utilization analysis).",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        const startMs = Date.now();
        const params = new URLSearchParams();
        const excludeKeys = new Set<string>(["owner", "name"]);
        for (const [k, v] of Object.entries(args)) {
          if (v === undefined || v === null || excludeKeys.has(k)) continue;
          if (Array.isArray(v)) {
            for (const item of v) params.append(k, String(item));
          } else {
            params.append(k, String(v));
          }
        }
        const qs = params.toString();
        const url = qs
          ? `/serverless/apps/${encodeURIComponent(String(args.owner))}/${
            encodeURIComponent(String(args.name))
          }/runners/history?${qs}`
          : `/serverless/apps/${encodeURIComponent(String(args.owner))}/${
            encodeURIComponent(String(args.name))
          }/runners/history`;

        const result = await falApi<Record<string, unknown>>(
          apiToken,
          "GET",
          url,
        );
        const items =
          ((result as Record<string, unknown>)["history"] ?? []) as unknown[];
        const truncated = false;

        const handle = await context.writeResource(
          "get_runner_history",
          "main",
          {
            items,
            truncated,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );

        context.logger.info("Found {count} get_runner_history", {
          count: items.length,
        });
        return { dataHandles: [handle] };
      },
    },
    list_app_events: {
      description: "Application Events",
      arguments: z.object({
        owner: z.string().describe("Username of the app owner"),
        name: z.string().describe("Application name"),
        start: z.union([
          z.string(),
          z.string().regex(new RegExp("^\\d{4}-\\d{2}-\\d{2}$")),
        ]).optional().describe(
          "Start date in ISO8601 format (e.g., '2025-01-01T00:00:00Z' or '2025-01-01'). Defaults to 24 hours ago.",
        ),
        end: z.union([
          z.string(),
          z.string().regex(new RegExp("^\\d{4}-\\d{2}-\\d{2}$")),
        ]).optional().describe(
          "End date in ISO8601 format, exclusive (e.g., '2025-02-01T00:00:00Z' or '2025-02-01'). Data up to but not including this timestamp is returned. Defaults to current time.",
        ),
        category: z.union([
          z.enum([
            "runner_started",
            "runner_failed",
            "runner_finished",
            "runner_pending",
            "runner_startup_failure",
            "runner_docker_pull",
            "runner_setup",
            "runner_draining",
            "runner_stopping",
            "deployment_started",
            "deployment_failed",
            "deployment_ended",
            "deployment_rolling_started",
            "deployment_rolling_failed",
            "deployment_rolling_ended",
            "deployment_recreate_applied",
            "config_changed",
          ]),
          z.array(
            z.enum([
              "runner_started",
              "runner_failed",
              "runner_finished",
              "runner_pending",
              "runner_startup_failure",
              "runner_docker_pull",
              "runner_setup",
              "runner_draining",
              "runner_stopping",
              "deployment_started",
              "deployment_failed",
              "deployment_ended",
              "deployment_rolling_started",
              "deployment_rolling_failed",
              "deployment_rolling_ended",
              "deployment_recreate_applied",
              "config_changed",
            ]),
          ),
        ]).optional().describe(
          "Filter by event category. Repeat the parameter to include multiple categories: ?category=deployment_started&category=config_changed",
        ),
        limit: z.number().int().min(1).max(1000).optional().describe(
          "Maximum number of events to return per page (1-1000)",
        ),
        cursor: z.string().optional().describe(
          "Pagination cursor from previous response",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        const startMs = Date.now();
        const params: Record<string, string | string[]> = {};
        const excludeKeys = new Set<string>([
          "owner",
          "name",
          "limit",
          "cursor",
        ]);
        for (const [k, v] of Object.entries(args)) {
          if (v === undefined || v === null || excludeKeys.has(k)) continue;
          params[k] = Array.isArray(v) ? v.map(String) : String(v);
        }
        const requestedLimit = args.limit !== undefined
          ? Number(args.limit)
          : undefined;
        const requestedCursor = typeof args.cursor === "string"
          ? args.cursor
          : undefined;

        const { results, truncated } = await falApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/serverless/apps/${encodeURIComponent(String(args.owner))}/${
            encodeURIComponent(String(args.name))
          }/events`,
          "events",
          params,
          { limit: requestedLimit, cursor: requestedCursor },
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("app_events", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
          durationMs: Date.now() - startMs,
          collectedBy: EXTENSION_NAME,
        });

        context.logger.info("Found {count} app_events", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    list_app_revisions: {
      description: "Application Revisions",
      arguments: z.object({
        owner: z.string().describe("Username of the app owner"),
        name: z.string().describe("Application name"),
        limit: z.number().int().min(1).max(100).optional().describe(
          "Maximum number of revisions to return per page (1-100)",
        ),
        cursor: z.string().optional().describe(
          "Pagination cursor from previous response",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        const startMs = Date.now();
        const params: Record<string, string | string[]> = {};
        const excludeKeys = new Set<string>([
          "owner",
          "name",
          "limit",
          "cursor",
        ]);
        for (const [k, v] of Object.entries(args)) {
          if (v === undefined || v === null || excludeKeys.has(k)) continue;
          params[k] = Array.isArray(v) ? v.map(String) : String(v);
        }
        const requestedLimit = args.limit !== undefined
          ? Number(args.limit)
          : undefined;
        const requestedCursor = typeof args.cursor === "string"
          ? args.cursor
          : undefined;

        const { results, truncated } = await falApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/serverless/apps/${encodeURIComponent(String(args.owner))}/${
            encodeURIComponent(String(args.name))
          }/revisions`,
          "revisions",
          params,
          { limit: requestedLimit, cursor: requestedCursor },
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("app_revisions", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
          durationMs: Date.now() - startMs,
          collectedBy: EXTENSION_NAME,
        });

        context.logger.info("Found {count} app_revisions", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    list_root: {
      description: "List files (root)",
      arguments: z.object({}),
      execute: async (
        _args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;

        const result = await falApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/serverless/files/list`,
        );

        const handle = await context.writeResource(
          "list_root",
          "latest",
          result,
        );
        context.logger.info("Fetched list_root", {});
        return { dataHandles: [handle] };
      },
    },
    list_directory: {
      description: "List files (directory)",
      arguments: z.object({
        dir: z.string().describe("Directory path to list"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;

        const result = await falApi<Record<string, unknown>>(
          apiToken,
          "GET",
          `/serverless/files/list/${encodeURIComponent(String(args.dir))}`,
        );

        const handle = await context.writeResource(
          "list_directory",
          sanitizeInstanceName(String(args.dir)),
          result,
        );
        context.logger.info("Fetched list_directory", {});
        return { dataHandles: [handle] };
      },
    },
    upload_from_url: {
      description: "Upload file from URL",
      arguments: z.object({
        file: z.string().describe("Target file path (including filename)"),
        url: z.string().describe("Publicly accessible URL for the file"),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;

        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["file"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await falApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/serverless/files/file/url/${encodeURIComponent(String(args.file))}`,
          body,
        );

        const handle = await context.writeResource(
          "upload_from_url",
          "latest",
          result ?? {},
        );
        context.logger.info("Executed upload_from_url", {});
        return { dataHandles: [handle] };
      },
    },
    serverless_logs_history: {
      description: "Logs history (paginated)",
      arguments: z.object({
        limit: z.number().int().min(1).max(1000).optional().describe(
          "Number of results per page",
        ),
        cursor: z.string().optional().describe(
          "Pagination cursor from previous response (timestamp-based). Use as 'until' parameter for next page.",
        ),
        start: z.union([
          z.string(),
          z.string().regex(new RegExp("^\\d{4}-\\d{2}-\\d{2}$")),
        ]).optional().describe(
          "Start date in ISO8601 format (e.g., '2025-01-01T00:00:00Z' or '2025-01-01'). Defaults to 24 hours ago.",
        ),
        end: z.union([
          z.string(),
          z.string().regex(new RegExp("^\\d{4}-\\d{2}-\\d{2}$")),
        ]).optional().describe(
          "End date in ISO8601 format, exclusive (e.g., '2025-02-01T00:00:00Z' or '2025-02-01'). Data up to but not including this timestamp is returned. Defaults to current time.",
        ),
        app_id: z.union([z.string(), z.array(z.string())]).optional().describe(
          "Filter by specific app ID(s) in '<owner>/<name>' format (e.g. 'fal-ai/my-app'). Accepts 1-50 app IDs. Supports comma-separated values: ?app_id=fal-ai/foo,fal-ai/bar or array syntax: ?app_id=fal-ai/foo&app_id=fal-ai/bar",
        ),
        revision: z.string().optional().describe("Filter by revision"),
        run_source: z.enum(["grpc-run", "grpc-register", "gateway", "cron"])
          .optional().describe("Filter by run source"),
        traceback: z.boolean().nullable().optional().describe(
          "Include tracebacks",
        ),
        search: z.string().optional().describe("Free-text search"),
        level: z.string().optional().describe("Minimum log level"),
        job_id: z.string().optional().describe("Filter by job id"),
        request_id: z.string().optional().describe("Filter by request id"),
        items: z.array(z.object({
          key: z.string(),
          value: z.union([z.string(), z.array(z.string())]),
          condition_type: z.enum(["equals", "in", "not_equals", "not_in"])
            .optional(),
        })),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;

        const body = args.items;

        const queryParts: string[] = [];
        const queryKeys = new Set([
          "limit",
          "cursor",
          "start",
          "end",
          "app_id",
          "revision",
          "run_source",
          "traceback",
          "search",
          "level",
          "job_id",
          "request_id",
        ]);
        for (const [k, v] of Object.entries(args)) {
          if (v === undefined || v === null || !queryKeys.has(k)) continue;
          if (Array.isArray(v)) {
            for (const item of v) {
              queryParts.push(`${k}=${encodeURIComponent(String(item))}`);
            }
          } else {
            queryParts.push(`${k}=${encodeURIComponent(String(v))}`);
          }
        }
        const qs = queryParts.length > 0 ? `?${queryParts.join("&")}` : "";
        const result = await falApi<Record<string, unknown>>(
          apiToken,
          "POST",
          `/serverless/logs/history${qs}`,
          body,
        );

        const handle = await context.writeResource(
          "serverless_logs_history",
          "latest",
          result ?? {},
        );
        context.logger.info("Executed serverless_logs_history", {});
        return { dataHandles: [handle] };
      },
    },
    list_requests_by_endpoint: {
      description: "List requests by endpoint(s)",
      arguments: z.object({
        limit: z.number().int().min(1).max(100).optional().describe(
          "Number of items to return per page (max 100)",
        ),
        cursor: z.string().optional().describe(
          "Pagination cursor encoding the page number",
        ),
        endpoint_id: z.union([z.string(), z.array(z.string())]).optional()
          .describe(
            "Filter by specific endpoint ID(s). Accepts 1-50 endpoint IDs. Supports comma-separated values: ?endpoint_id=model1,model2 or array syntax: ?endpoint_id=model1&endpoint_id=model2",
          ),
        start: z.union([
          z.string(),
          z.string().regex(new RegExp("^\\d{4}-\\d{2}-\\d{2}$")),
        ]).optional().describe(
          "Start date in ISO8601 format (e.g., '2025-01-01T00:00:00Z' or '2025-01-01'). Defaults to 24 hours ago.",
        ),
        end: z.union([
          z.string(),
          z.string().regex(new RegExp("^\\d{4}-\\d{2}-\\d{2}$")),
        ]).optional().describe(
          "End date in ISO8601 format, exclusive (e.g., '2025-02-01T00:00:00Z' or '2025-02-01'). Data up to but not including this timestamp is returned. Defaults to current time.",
        ),
        status: z.enum(["success", "error", "user_error"]).optional().describe(
          "Filter by request status",
        ),
        request_id: z.string().optional().describe(
          "Filter by specific request ID",
        ),
        expand: z.union([z.string(), z.array(z.string())]).optional().describe(
          "Fields to expand in the response. Use payloads to include input and output payloads. Use billing to include billable_units per request.",
        ),
        sort_by: z.enum(["ended_at", "duration"]).optional().describe(
          "Sort results by end time or duration",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        const startMs = Date.now();
        const params: Record<string, string | string[]> = {};
        const excludeKeys = new Set<string>(["limit", "cursor"]);
        for (const [k, v] of Object.entries(args)) {
          if (v === undefined || v === null || excludeKeys.has(k)) continue;
          params[k] = Array.isArray(v) ? v.map(String) : String(v);
        }
        const requestedLimit = args.limit !== undefined
          ? Number(args.limit)
          : undefined;
        const requestedCursor = typeof args.cursor === "string"
          ? args.cursor
          : undefined;

        const { results, truncated } = await falApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/serverless/requests/by-endpoint`,
          "items",
          params,
          { limit: requestedLimit, cursor: requestedCursor },
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "requests_by_endpoint",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );

        context.logger.info("Found {count} requests_by_endpoint", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    get_usage: {
      description: "Usage",
      arguments: z.object({
        limit: z.number().int().min(1).optional().describe(
          "Maximum number of items to return. Actual maximum depends on query type and expansion parameters.",
        ),
        cursor: z.string().optional().describe(
          "Pagination cursor from previous response. Encodes the page number.",
        ),
        start: z.union([
          z.string(),
          z.string().regex(new RegExp("^\\d{4}-\\d{2}-\\d{2}$")),
        ]).optional().describe(
          "Start date in ISO8601 format (e.g., '2025-01-01T00:00:00Z' or '2025-01-01'). Defaults to 24 hours ago.",
        ),
        end: z.union([
          z.string(),
          z.string().regex(new RegExp("^\\d{4}-\\d{2}-\\d{2}$")),
        ]).optional().describe(
          "End date in ISO8601 format, exclusive (e.g., '2025-02-01T00:00:00Z' or '2025-02-01'). Data up to but not including this timestamp is returned. Defaults to current time.",
        ),
        timezone: z.string().optional().describe(
          "Timezone for date aggregation and boundaries. All timestamps in responses are in UTC, but this controls how dates are bucketed.",
        ),
        timeframe: z.enum(["minute", "hour", "day", "week", "month"]).optional()
          .describe(
            "Aggregation timeframe for timeseries data (auto-detected from date range if not specified). Auto-detection uses: minute (<2h), hour (<2d), day (<64d), week (<183d), month (>=183d).",
          ),
        bound_to_timeframe: z.enum(["true", "false"]).optional().describe(
          "Whether to adjust start/end dates to align with timeframe boundaries and use exclusive end. Defaults to true. When true, dates are aligned to the start of the timeframe period (e.g., start of day) and end is made exclusive (e.g., start of next day). When false, uses exact dates provided.",
        ),
        app: z.union([z.string(), z.array(z.string())]).optional().describe(
          "Filter to one or more serverless apps, matched exactly against the `app` value in the response (deployed name, owner prefix stripped). Accepts a comma-separated list or repeated parameter (1-50). For partial/name-only matching use `search`.",
        ),
        search: z.string().optional().describe(
          "Case-insensitive substring match on the app name — returns every app whose name contains this term (e.g. `search=autohdr` matches all `autohdr-*` apps across environments). Combined with `app` via AND when both are given.",
        ),
        expand: z.union([z.string(), z.array(z.string())]).optional().describe(
          "Data to include in the response. Use 'time_series' for time-bucketed data and 'summary' for aggregate statistics across the entire window. At least one is required.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiToken } = context.globalArgs;
        const startMs = Date.now();
        const params: Record<string, string | string[]> = {};
        const excludeKeys = new Set<string>(["limit", "cursor"]);
        for (const [k, v] of Object.entries(args)) {
          if (v === undefined || v === null || excludeKeys.has(k)) continue;
          params[k] = Array.isArray(v) ? v.map(String) : String(v);
        }
        const requestedLimit = args.limit !== undefined
          ? Number(args.limit)
          : undefined;
        const requestedCursor = typeof args.cursor === "string"
          ? args.cursor
          : undefined;

        const { results, truncated } = await falApiPaginated<
          Record<string, unknown>
        >(
          apiToken,
          `/serverless/usage`,
          "time_series",
          params,
          { limit: requestedLimit, cursor: requestedCursor },
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("get_usage", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
          durationMs: Date.now() - startMs,
          collectedBy: EXTENSION_NAME,
        });

        context.logger.info("Found {count} get_usage", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
  },
};
