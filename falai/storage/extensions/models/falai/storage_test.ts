// fal.ai Storage — file ACLs, signed URLs, storage settings — Tests
// Auto-generated by scripts/falai-codegen — do not edit manually.
// SPDX-License-Identifier: Apache-2.0

import { assertEquals, assertExists } from "jsr:@std/assert@1.0.19";
import { createModelTestContext } from "@systeminit/swamp-testing";
import { model } from "./storage.ts";

// ---------------------------------------------------------------------------
// Model Export Structure Tests
// ---------------------------------------------------------------------------

Deno.test("storage model: has correct type", () => {
  assertEquals(model.type, "@webframp/falai/storage");
});

Deno.test("storage model: has valid version format", () => {
  const versionPattern = /^\d{4}\.\d{2}\.\d{2}\.\d+$/;
  assertEquals(versionPattern.test(model.version), true);
});

Deno.test("storage model: has globalArguments with apiToken", () => {
  assertExists(model.globalArguments);
  const shape = model.globalArguments.shape;
  assertExists(shape.apiToken);
});

Deno.test("storage model: has expected methods", () => {
  assertExists(model.methods);
  assertExists(model.methods.get_storage_file_acl);
  assertExists(model.methods.set_storage_file_acl);
  assertExists(model.methods.sign_storage_file_url);
  assertExists(model.methods.get_storage_settings);
  assertExists(model.methods.update_storage_settings);
});

Deno.test("storage model: has expected resources", () => {
  assertExists(model.resources);
  assertExists(model.resources["get_storage_file_acl"]);
  assertExists(model.resources["set_storage_file_acl"]);
  assertExists(model.resources["sign_storage_file_url"]);
  assertExists(model.resources["storage_settings"]);
});

// ---------------------------------------------------------------------------
// Mock fal.ai API Server
// ---------------------------------------------------------------------------

function startMockFalServer(
  responses: Record<string, { body: unknown }>,
): { url: string; server: Deno.HttpServer } {
  const server = Deno.serve({ port: 0, onListen() {} }, (req) => {
    const url = new URL(req.url);
    const path = url.pathname;

    for (const [pattern, { body }] of Object.entries(responses)) {
      if (path.includes(pattern)) {
        return Response.json(body);
      }
    }

    return Response.json(
      { error: { type: "not_found", message: "Not found" } },
      { status: 404 },
    );
  });

  const addr = server.addr as Deno.NetAddr;
  return { url: `http://localhost:${addr.port}`, server };
}

function installFetchMock(mockUrl: string): () => void {
  const originalFetch = globalThis.fetch;
  globalThis.fetch = (input, init) => {
    const reqUrl = typeof input === "string" ? input : input.toString();
    const newUrl = reqUrl.replace("https://api.fal.ai/v1", mockUrl);
    return originalFetch(newUrl, init);
  };
  return () => {
    globalThis.fetch = originalFetch;
  };
}

Deno.test({
  name: "storage model: get_storage_file_acl fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockBody = {
      "rules": [{ "user": "some-user", "decision": "allow" }],
    };
    const { url, server } = startMockFalServer({
      "/storage/files/acl": { body: mockBody },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token" },
        definition: {
          id: "test-id",
          name: "test-storage",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).get_storage_file_acl.execute({}, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "storage model: set_storage_file_acl executes and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockBody = { "user": "some-user", "decision": "allow" };
    const { url, server } = startMockFalServer({
      "/storage/files/acl": { body: mockBody },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token" },
        definition: {
          id: "test-id",
          name: "test-storage",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).set_storage_file_acl.execute({
        "default": "allow",
        "rules": [{ "user": "some-user", "decision": "allow" }],
      }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "storage model: sign_storage_file_url creates and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockBody = {
      "id": "new-123",
      "signed_url":
        "https://v3.fal.media/files/b/0a1b2c3d/output.png?identity=...",
    };
    const { url, server } = startMockFalServer({
      "/storage/files/sign": { body: mockBody },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token" },
        definition: {
          id: "test-id",
          name: "test-storage",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).sign_storage_file_url.execute(
        { "expiration_seconds": 3600 },
        context,
      );
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "storage model: get_storage_settings fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockBody = {
      "expiration_duration_seconds": 86400,
      "initial_acl": {
        "default": "allow",
        "rules": [{ "user": "some-user", "decision": "allow" }],
      },
    };
    const { url, server } = startMockFalServer({
      "/storage/settings": { body: mockBody },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token" },
        definition: {
          id: "test-id",
          name: "test-storage",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).get_storage_settings.execute({}, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});
