// fal.ai Compute — dedicated GPU compute instances — Tests
// Auto-generated by scripts/falai-codegen — do not edit manually.
// SPDX-License-Identifier: Apache-2.0

import { assertEquals, assertExists } from "jsr:@std/assert@1.0.19";
import { createModelTestContext } from "@systeminit/swamp-testing";
import { model } from "./compute.ts";

// ---------------------------------------------------------------------------
// Model Export Structure Tests
// ---------------------------------------------------------------------------

Deno.test("compute model: has correct type", () => {
  assertEquals(model.type, "@webframp/falai/compute");
});

Deno.test("compute model: has valid version format", () => {
  const versionPattern = /^\d{4}\.\d{2}\.\d{2}\.\d+$/;
  assertEquals(versionPattern.test(model.version), true);
});

Deno.test("compute model: has globalArguments with apiToken", () => {
  assertExists(model.globalArguments);
  const shape = model.globalArguments.shape;
  assertExists(shape.apiToken);
});

Deno.test("compute model: has expected methods", () => {
  assertExists(model.methods);
  assertExists(model.methods.list_compute_instances);
  assertExists(model.methods.create_compute_instance);
  assertExists(model.methods.get_compute_instance);
  assertExists(model.methods.delete_compute_instance);
});

Deno.test("compute model: has expected resources", () => {
  assertExists(model.resources);
  assertExists(model.resources["compute_instances"]);
  assertExists(model.resources["compute_instance"]);
});

// ---------------------------------------------------------------------------
// Mock fal.ai API Server
// ---------------------------------------------------------------------------

function startMockFalServer(
  responses: Record<string, { body: unknown }>,
): { url: string; server: Deno.HttpServer } {
  const server = Deno.serve({ port: 0, onListen() {} }, (req) => {
    const url = new URL(req.url);
    const path = url.pathname;

    for (const [pattern, { body }] of Object.entries(responses)) {
      if (path.includes(pattern)) {
        return Response.json(body);
      }
    }

    return Response.json(
      { error: { type: "not_found", message: "Not found" } },
      { status: 404 },
    );
  });

  const addr = server.addr as Deno.NetAddr;
  return { url: `http://localhost:${addr.port}`, server };
}

function installFetchMock(mockUrl: string): () => void {
  const originalFetch = globalThis.fetch;
  globalThis.fetch = (input, init) => {
    const reqUrl = typeof input === "string" ? input : input.toString();
    const newUrl = reqUrl.replace("https://api.fal.ai/v1", mockUrl);
    return originalFetch(newUrl, init);
  };
  return () => {
    globalThis.fetch = originalFetch;
  };
}

Deno.test({
  name: "compute model: list_compute_instances fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockBody = {
      "instances": [{
        "id": "inst_abc123xyz",
        "instance_type": "gpu_1x_h100_sxm5",
        "region": "us-west",
        "sector": "sector_1",
        "ip": "203.0.113.42",
        "status": "ready",
        "creator_user_nickname": "developer",
      }],
    };
    const { url, server } = startMockFalServer({
      "/compute/instances": { body: mockBody },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token" },
        definition: {
          id: "test-id",
          name: "test-compute",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).list_compute_instances.execute({}, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "compute model: create_compute_instance creates and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockBody = {
      "id": "inst_abc123xyz",
      "instance_type": "gpu_1x_h100_sxm5",
      "region": "us-west",
      "sector": "sector_1",
      "ip": "203.0.113.42",
      "status": "ready",
      "creator_user_nickname": "developer",
    };
    const { url, server } = startMockFalServer({
      "/compute/instances": { body: mockBody },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token" },
        definition: {
          id: "test-id",
          name: "test-compute",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).create_compute_instance.execute({
        "id": "inst_abc123xyz",
        "instance_type": "gpu_1x_h100_sxm5",
        "region": "us-west",
        "sector": "sector_1",
        "ip": "203.0.113.42",
        "status": "ready",
        "creator_user_nickname": "developer",
      }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "compute model: get_compute_instance fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockBody = {
      "id": "inst_abc123xyz",
      "instance_type": "gpu_1x_h100_sxm5",
      "region": "us-west",
      "sector": "sector_1",
      "ip": "203.0.113.42",
      "status": "ready",
      "creator_user_nickname": "developer",
    };
    const { url, server } = startMockFalServer({
      "/compute/instances/test-id-123": { body: mockBody },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token" },
        definition: {
          id: "test-id",
          name: "test-compute",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).get_compute_instance.execute({ "id": "test-id-123" }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "compute model: delete_compute_instance executes successfully",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockFalServer({
      "/compute/instances/test-id-123": { body: { id: "test-id-123" } },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context } = createModelTestContext({
        globalArgs: { "apiToken": "test-token" },
        definition: {
          id: "test-id",
          name: "test-compute",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).delete_compute_instance.execute({ "id": "test-id-123" }, context);
      assertEquals(result.dataHandles.length, 0);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});
