// fal.ai Models — model catalog, pricing, analytics, usage, billing events, request search — Tests
// Auto-generated by scripts/falai-codegen — do not edit manually.
// SPDX-License-Identifier: Apache-2.0

import { assertEquals, assertExists } from "jsr:@std/assert@1.0.19";
import { createModelTestContext } from "@systeminit/swamp-testing";
import { model } from "./models.ts";

// ---------------------------------------------------------------------------
// Model Export Structure Tests
// ---------------------------------------------------------------------------

Deno.test("models model: has correct type", () => {
  assertEquals(model.type, "@webframp/falai/models");
});

Deno.test("models model: has valid version format", () => {
  const versionPattern = /^\d{4}\.\d{2}\.\d{2}\.\d+$/;
  assertEquals(versionPattern.test(model.version), true);
});

Deno.test("models model: has globalArguments with apiToken", () => {
  assertExists(model.globalArguments);
  const shape = model.globalArguments.shape;
  assertExists(shape.apiToken);
});

Deno.test("models model: has expected methods", () => {
  assertExists(model.methods);
  assertExists(model.methods.get_models);
  assertExists(model.methods.get_pricing);
  assertExists(model.methods.estimate_pricing);
  assertExists(model.methods.get_usage);
  assertExists(model.methods.get_analytics);
  assertExists(model.methods.get_billing_events);
  assertExists(model.methods.delete_request_payloads);
  assertExists(model.methods.list_requests_by_endpoint);
  assertExists(model.methods.search_requests);
});

Deno.test("models model: has expected resources", () => {
  assertExists(model.resources);
  assertExists(model.resources["get_models"]);
  assertExists(model.resources["get_pricing"]);
  assertExists(model.resources["estimate_pricing"]);
  assertExists(model.resources["get_usage"]);
  assertExists(model.resources["get_analytics"]);
  assertExists(model.resources["get_billing_events"]);
  assertExists(model.resources["requests_by_endpoint"]);
  assertExists(model.resources["search_requests"]);
});

// ---------------------------------------------------------------------------
// Mock fal.ai API Server
// ---------------------------------------------------------------------------

function startMockFalServer(
  responses: Record<string, { body: unknown }>,
): { url: string; server: Deno.HttpServer } {
  const server = Deno.serve({ port: 0, onListen() {} }, (req) => {
    const url = new URL(req.url);
    const path = url.pathname;

    for (const [pattern, { body }] of Object.entries(responses)) {
      if (path.includes(pattern)) {
        return Response.json(body);
      }
    }

    return Response.json(
      { error: { type: "not_found", message: "Not found" } },
      { status: 404 },
    );
  });

  const addr = server.addr as Deno.NetAddr;
  return { url: `http://localhost:${addr.port}`, server };
}

function installFetchMock(mockUrl: string): () => void {
  const originalFetch = globalThis.fetch;
  globalThis.fetch = (input, init) => {
    const reqUrl = typeof input === "string" ? input : input.toString();
    const newUrl = reqUrl.replace("https://api.fal.ai/v1", mockUrl);
    return originalFetch(newUrl, init);
  };
  return () => {
    globalThis.fetch = originalFetch;
  };
}

Deno.test({
  name: "models model: get_models fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockBody = {
      "models": [{
        "endpoint_id": "test-value",
        "metadata": {
          "display_name": "test-value",
          "category": "test-value",
          "description": "test-value",
          "status": "active",
          "tags": ["test-value"],
          "updated_at": "test-value",
          "is_favorited": true,
          "thumbnail_url": "test-value",
          "thumbnail_animated_url": "test-value",
          "model_url": "test-value",
          "github_url": "test-value",
          "license_type": "commercial",
          "date": "test-value",
          "group": { "key": "test-value", "label": "test-value" },
          "highlighted": false,
          "kind": "inference",
          "training_endpoint_ids": ["test-value"],
          "inference_endpoint_ids": ["test-value"],
          "stream_url": "test-value",
          "duration_estimate": 1,
          "pinned": false,
        },
        "openapi": { "openapi": "test-value" },
        "enterprise_status": "ready",
      }],
    };
    const { url, server } = startMockFalServer({
      "/models": { body: mockBody },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token" },
        definition: {
          id: "test-id",
          name: "test-models",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).get_models.execute({}, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "models model: estimate_pricing executes and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const mockBody = {
      "estimate_type": "historical_api_price",
      "total_cost": 3.75,
      "currency": "USD",
    };
    const { url, server } = startMockFalServer({
      "/models/pricing/estimate": { body: mockBody },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiToken": "test-token" },
        definition: {
          id: "test-id",
          name: "test-models",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).estimate_pricing.execute({
        "estimate_type": "historical_api_price",
        "total_cost": 3.75,
        "currency": "USD",
      }, context);
      assertEquals(result.dataHandles.length, 1);

      const resources = getWrittenResources();
      assertEquals(resources.length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "models model: delete_request_payloads executes successfully",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockFalServer({
      "/models/requests/test-id-123/payloads": { body: { id: "test-id-123" } },
    });
    const uninstall = installFetchMock(url);

    try {
      const { context } = createModelTestContext({
        globalArgs: { "apiToken": "test-token" },
        definition: {
          id: "test-id",
          name: "test-models",
          version: 1,
          tags: {},
        },
      });

      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).delete_request_payloads.execute(
        { "request_id": "test-id-123" },
        context,
      );
      assertEquals(result.dataHandles.length, 0);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});
