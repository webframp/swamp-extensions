/**
 * Griptape Cloud Models — model configurations and provider auth configs
 *
 * Auto-generated by scripts/griptape-codegen — do not edit manually.
 *
 * @module
 */
// SPDX-License-Identifier: Apache-2.0

import { z } from "npm:zod@4.4.3";
import {
  griptapeApi,
  griptapeApiPaginated,
  sanitizeInstanceName,
} from "./_lib/api.ts";

const EXTENSION_NAME = "@webframp/griptape/models";

// =============================================================================
// Schemas
// =============================================================================

const GlobalArgsSchema = z.object({
  apiKey: z.string().meta({ sensitive: true }).describe(
    "Griptape Cloud API key; overrides the GT_CLOUD_API_KEY environment variable. Wire with a vault.get(...) expression to source it from a vault.",
  ).optional(),
  baseUrl: z.string().describe(
    "Griptape Cloud API base URL; overrides the GT_CLOUD_BASE_URL environment variable and the built-in default.",
  ).optional(),
});

const ModelsItemSchema = z.looseObject({
  active: z.boolean().optional(),
  auth_config: z.unknown().optional(),
  created_at: z.string(),
  default: z.boolean(),
  description: z.string().optional(),
  editable: z.boolean().optional(),
  kwargs: z.unknown().optional(),
  model_config_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ).optional(),
  model_name: z.string(),
  model_type: z.unknown(),
  source: z.string().optional(),
  updated_at: z.string(),
});

const ListModelsSchema = z.object({
  items: z.array(ModelsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const CreateModelSchema = z.looseObject({
  active: z.boolean().optional(),
  auth_config: z.unknown().optional(),
  created_at: z.string(),
  default: z.boolean(),
  description: z.string().optional(),
  editable: z.boolean().optional(),
  kwargs: z.unknown().optional(),
  model_config_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ).optional(),
  model_name: z.string(),
  model_type: z.unknown(),
  source: z.string().optional(),
  updated_at: z.string(),
});

const AuthConfigsItemSchema = z.looseObject({
  api_key_secret_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  auth_config_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  base_url: z.string(),
  created_at: z.string(),
  kwargs: z.unknown().optional(),
  name: z.string().min(1).max(200),
  updated_at: z.string(),
});

const ListAuthConfigsSchema = z.object({
  items: z.array(AuthConfigsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const CreateAuthConfigSchema = z.looseObject({
  api_key_secret_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  auth_config_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  base_url: z.string(),
  created_at: z.string(),
  kwargs: z.unknown().optional(),
  name: z.string().min(1).max(200),
  updated_at: z.string(),
});

// =============================================================================
// Model Definition
// =============================================================================

/** Griptape Cloud Models — model configurations and provider auth configs */
export const model = {
  type: "@webframp/griptape/models",
  version: "2026.08.28.1",
  globalArguments: GlobalArgsSchema,

  upgrades: [],

  resources: {
    "models": {
      description: "list operation",
      schema: ListModelsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "model": {
      description: "create operation",
      schema: CreateModelSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "auth_configs": {
      description: "list operation",
      schema: ListAuthConfigsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "auth_config": {
      description: "create operation",
      schema: CreateAuthConfigSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
  },

  methods: {
    list_models: {
      description: "list operation",
      arguments: z.object({
        page: z.number().optional(),
        page_size: z.number().optional(),
        model_type: z.string().optional(),
        default: z.boolean().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const startMs = Date.now();
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "page_size"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await griptapeApiPaginated<
          Record<string, unknown>
        >(
          apiKey,
          `/api/models`,
          "models",
          params,
          baseUrl,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("models", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
          durationMs: Date.now() - startMs,
          collectedBy: EXTENSION_NAME,
        });

        context.logger.info("Found {count} models", { count: results.length });
        return { dataHandles: [handle] };
      },
    },
    create_model: {
      description: "create operation",
      arguments: z.object({
        active: z.boolean().optional(),
        auth_config_id: z.string().regex(
          new RegExp(
            "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
          ),
        ),
        description: z.string().optional(),
        kwargs: z.unknown().optional(),
        model_name: z.string().min(1).max(1000),
        model_type: z.unknown(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const body = args;

        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "POST",
          `/api/models`,
          body,
          baseUrl,
        );

        const record = result as Record<string, unknown>;
        const pathParamValues = new Set<string>([]);
        const idCandidates = ["model_id", "id"];
        let rawId = idCandidates.map((k) => record[k]).find((v) =>
          v !== undefined && v !== null && v !== ""
        );
        if (rawId === undefined) {
          // No named id candidate matched. Scan for a single id-shaped key that is
          // not organization_id and whose value is not a path param (the parent
          // addressing context, e.g. bucket_id on an asset — would collide).
          for (const [k, v] of Object.entries(record)) {
            if (
              /(^|_)id$/.test(k) && k !== "organization_id" &&
              typeof v === "string" && v !== "" && !pathParamValues.has(v)
            ) {
              rawId = v;
              break;
            }
          }
        }
        if (
          rawId === undefined && typeof record.name === "string" &&
          record.name !== ""
        ) {
          rawId = record.name;
        }
        const fallbackId =
          [].filter((s: string) => s && s !== "undefined").join("_") ||
          "created";
        const id = sanitizeInstanceName(String(rawId ?? fallbackId));
        const handle = await context.writeResource("model", id, result);
        context.logger.info("Created model {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    list_auth_configs: {
      description: "list operation",
      arguments: z.object({
        page: z.number().optional(),
        page_size: z.number().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const startMs = Date.now();
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "page_size"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await griptapeApiPaginated<
          Record<string, unknown>
        >(
          apiKey,
          `/api/models/auth-configs`,
          "auth_configs",
          params,
          baseUrl,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("auth_configs", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
          durationMs: Date.now() - startMs,
          collectedBy: EXTENSION_NAME,
        });

        context.logger.info("Found {count} auth_configs", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_auth_config: {
      description: "create operation",
      arguments: z.object({
        api_key_secret_id: z.string().regex(
          new RegExp(
            "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
          ),
        ),
        base_url: z.string(),
        kwargs: z.unknown().optional(),
        name: z.string().min(1).max(200),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const body = args;

        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "POST",
          `/api/models/auth-configs`,
          body,
          baseUrl,
        );

        const record = result as Record<string, unknown>;
        const pathParamValues = new Set<string>([]);
        const idCandidates = ["auth_config_id", "id"];
        let rawId = idCandidates.map((k) => record[k]).find((v) =>
          v !== undefined && v !== null && v !== ""
        );
        if (rawId === undefined) {
          // No named id candidate matched. Scan for a single id-shaped key that is
          // not organization_id and whose value is not a path param (the parent
          // addressing context, e.g. bucket_id on an asset — would collide).
          for (const [k, v] of Object.entries(record)) {
            if (
              /(^|_)id$/.test(k) && k !== "organization_id" &&
              typeof v === "string" && v !== "" && !pathParamValues.has(v)
            ) {
              rawId = v;
              break;
            }
          }
        }
        if (
          rawId === undefined && typeof record.name === "string" &&
          record.name !== ""
        ) {
          rawId = record.name;
        }
        const fallbackId =
          [].filter((s: string) => s && s !== "undefined").join("_") ||
          "created";
        const id = sanitizeInstanceName(String(rawId ?? fallbackId));
        const handle = await context.writeResource("auth_config", id, result);
        context.logger.info("Created auth_config {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_auth_config: {
      description: "get operation",
      arguments: z.object({
        auth_config_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "GET",
          `/api/models/auth-configs/${
            encodeURIComponent(String(args.auth_config_id))
          }`,
          undefined,
          baseUrl,
        );

        const handle = await context.writeResource(
          "auth_config",
          sanitizeInstanceName(String(args.auth_config_id)),
          result,
        );
        context.logger.info("Fetched auth_config", {});
        return { dataHandles: [handle] };
      },
    },
    update_auth_config: {
      description: "update operation",
      arguments: z.object({
        auth_config_id: z.string(),
        api_key_secret_id: z.string().regex(
          new RegExp(
            "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
          ),
        ).optional(),
        base_url: z.string().optional(),
        kwargs: z.unknown().optional(),
        name: z.string().min(1).max(200).optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["auth_config_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "PATCH",
          `/api/models/auth-configs/${
            encodeURIComponent(String(args.auth_config_id))
          }`,
          body,
          baseUrl,
        );

        const handle = await context.writeResource(
          "auth_config",
          sanitizeInstanceName(String(args.auth_config_id)),
          result,
        );
        context.logger.info("Updated auth_config", {});
        return { dataHandles: [handle] };
      },
    },
    delete_auth_config: {
      description: "delete operation",
      arguments: z.object({
        auth_config_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        await griptapeApi<Record<string, unknown>>(
          apiKey,
          "DELETE",
          `/api/models/auth-configs/${
            encodeURIComponent(String(args.auth_config_id))
          }`,
          undefined,
          baseUrl,
        );

        context.logger.info("Deleted resource {id}", {
          id: args.auth_config_id,
        });
        return { dataHandles: [] };
      },
    },
    get_model: {
      description: "get operation",
      arguments: z.object({
        model_config_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "GET",
          `/api/models/${encodeURIComponent(String(args.model_config_id))}`,
          undefined,
          baseUrl,
        );

        const handle = await context.writeResource(
          "model",
          sanitizeInstanceName(String(args.model_config_id)),
          result,
        );
        context.logger.info("Fetched model", {});
        return { dataHandles: [handle] };
      },
    },
    update_model: {
      description: "update operation",
      arguments: z.object({
        model_config_id: z.string(),
        active: z.boolean().optional(),
        auth_config_id: z.string().regex(
          new RegExp(
            "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
          ),
        ).optional(),
        description: z.string().optional(),
        kwargs: z.unknown().optional(),
        model_name: z.string().min(1).max(1000).optional(),
        model_type: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["model_config_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "PATCH",
          `/api/models/${encodeURIComponent(String(args.model_config_id))}`,
          body,
          baseUrl,
        );

        const handle = await context.writeResource(
          "model",
          sanitizeInstanceName(String(args.model_config_id)),
          result,
        );
        context.logger.info("Updated model", {});
        return { dataHandles: [handle] };
      },
    },
    delete_model: {
      description: "delete operation",
      arguments: z.object({
        model_config_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        await griptapeApi<Record<string, unknown>>(
          apiKey,
          "DELETE",
          `/api/models/${encodeURIComponent(String(args.model_config_id))}`,
          undefined,
          baseUrl,
        );

        context.logger.info("Deleted resource {id}", {
          id: args.model_config_id,
        });
        return { dataHandles: [] };
      },
    },
  },
};
