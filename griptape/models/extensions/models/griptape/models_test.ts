// Griptape Cloud Models — model configurations and provider auth configs — Tests
// Auto-generated by scripts/griptape-codegen — do not edit manually.
// SPDX-License-Identifier: Apache-2.0

import { assertEquals, assertExists } from "jsr:@std/assert@1.0.19";
import { createModelTestContext } from "@systeminit/swamp-testing";
import { model } from "./models.ts";

// ---------------------------------------------------------------------------
// Model Export Structure Tests
// ---------------------------------------------------------------------------

Deno.test("models model: has correct type", () => {
  assertEquals(model.type, "@webframp/griptape/models");
});

Deno.test("models model: has valid version format", () => {
  const versionPattern = /^\d{4}\.\d{2}\.\d{2}\.\d+$/;
  assertEquals(versionPattern.test(model.version), true);
});

Deno.test("models model: has globalArguments with apiKey", () => {
  assertExists(model.globalArguments);
  const shape = model.globalArguments.shape;
  assertExists(shape.apiKey);
});

Deno.test("models model: has expected methods", () => {
  assertExists(model.methods);
  assertExists(model.methods.list_models);
  assertExists(model.methods.create_model);
  assertExists(model.methods.list_auth_configs);
  assertExists(model.methods.create_auth_config);
  assertExists(model.methods.get_auth_config);
  assertExists(model.methods.update_auth_config);
  assertExists(model.methods.delete_auth_config);
  assertExists(model.methods.get_model);
  assertExists(model.methods.update_model);
  assertExists(model.methods.delete_model);
});

Deno.test("models model: has expected resources", () => {
  assertExists(model.resources);
  assertExists(model.resources["models"]);
  assertExists(model.resources["model"]);
  assertExists(model.resources["auth_configs"]);
  assertExists(model.resources["auth_config"]);
});

// ---------------------------------------------------------------------------
// Mock Griptape API Server
// ---------------------------------------------------------------------------

interface MockResponse {
  body: unknown;
  itemsKey?: string;
  status?: number;
}

function startMockGtServer(
  responses: Record<string, MockResponse>,
): { url: string; server: Deno.HttpServer } {
  const server = Deno.serve({ port: 0, onListen() {} }, (req) => {
    const url = new URL(req.url);
    const path = url.pathname;

    // Guard against a doubled "/api" prefix (base URL + already-/api-prefixed
    // path). This is invisible to path.includes() matching, so assert it here:
    // a regression that reintroduces it fails the test loudly instead of 404ing
    // only in production.
    if (path.includes("/api/api/")) {
      return Response.json(
        { message: `doubled /api prefix in request path: ${path}` },
        { status: 500 },
      );
    }

    for (const [pattern, spec] of Object.entries(responses)) {
      if (path.includes(pattern)) {
        const status = spec.status ?? 200;
        // 204/205 must have a null body.
        if (status === 204 || status === 205) {
          return new Response(null, { status });
        }
        if (spec.itemsKey) {
          return Response.json({
            [spec.itemsKey]: spec.body,
            pagination: {
              page_number: 1,
              page_size: 50,
              total_count: Array.isArray(spec.body) ? spec.body.length : 0,
              total_pages: 1,
            },
          }, { status });
        }
        return Response.json(spec.body, { status });
      }
    }

    return Response.json({ message: "Not found" }, { status: 404 });
  });

  const addr = server.addr as Deno.NetAddr;
  return { url: `http://localhost:${addr.port}`, server };
}

function installFetchMock(mockUrl: string): () => void {
  const originalFetch = globalThis.fetch;
  globalThis.fetch = (input, init) => {
    const reqUrl = typeof input === "string" ? input : input.toString();
    const newUrl = reqUrl.replace("https://cloud.griptape.ai", mockUrl);
    return originalFetch(newUrl, init);
  };
  return () => {
    globalThis.fetch = originalFetch;
  };
}

Deno.test({
  name: "models model: list_models fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockGtServer({
      "/api/models": {
        body: [{
          "active": true,
          "auth_config": null,
          "created_at": "2024-01-01T00:00:00Z",
          "default": true,
          "description": "test-value",
          "editable": true,
          "kwargs": null,
          "model_config_id": "test-value",
          "model_name": "test-value",
          "model_type": null,
          "source": "test-value",
          "updated_at": "2024-01-01T00:00:00Z",
        }],
        itemsKey: "models",
      },
    });
    const uninstall = installFetchMock(url);
    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiKey": "test-key" },
        definition: {
          id: "test-id",
          name: "test-models",
          version: 1,
          tags: {},
        },
      });
      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).list_models.execute({}, context);
      assertEquals(result.dataHandles.length, 1);
      assertEquals(getWrittenResources().length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "models model: create_model creates and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockGtServer({
      "/api/models": {
        body: {
          "model_config_id": "test-value",
          "active": true,
          "auth_config": null,
          "created_at": "2024-01-01T00:00:00Z",
          "default": true,
          "description": "test-value",
          "editable": true,
          "kwargs": null,
          "model_name": "test-value",
          "model_type": null,
          "source": "test-value",
          "updated_at": "2024-01-01T00:00:00Z",
        },
      },
    });
    const uninstall = installFetchMock(url);
    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiKey": "test-key" },
        definition: {
          id: "test-id",
          name: "test-models",
          version: 1,
          tags: {},
        },
      });
      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).create_model.execute({
        "active": true,
        "auth_config": null,
        "created_at": "2024-01-01T00:00:00Z",
        "default": true,
        "description": "test-value",
        "editable": true,
        "kwargs": null,
        "model_config_id": "test-value",
        "model_name": "test-value",
        "model_type": null,
        "source": "test-value",
        "updated_at": "2024-01-01T00:00:00Z",
      }, context);
      assertEquals(result.dataHandles.length, 1);
      assertEquals(getWrittenResources().length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "models model: get_auth_config fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockGtServer({
      "/api/models/auth-configs/test-id-123": {
        body: {
          "api_key_secret_id": "test-value",
          "auth_config_id": "test-value",
          "base_url": "test-value",
          "created_at": "2024-01-01T00:00:00Z",
          "kwargs": null,
          "name": "test-value",
          "updated_at": "2024-01-01T00:00:00Z",
        },
      },
    });
    const uninstall = installFetchMock(url);
    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiKey": "test-key" },
        definition: {
          id: "test-id",
          name: "test-models",
          version: 1,
          tags: {},
        },
      });
      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).get_auth_config.execute(
        { "auth_config_id": "test-id-123" },
        context,
      );
      assertEquals(result.dataHandles.length, 1);
      assertEquals(getWrittenResources().length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "models model: update_auth_config executes and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockGtServer({
      "/api/models/auth-configs/test-id-123": {
        body: {
          "api_key_secret_id": "test-value",
          "auth_config_id": "test-value",
          "base_url": "test-value",
          "created_at": "2024-01-01T00:00:00Z",
          "kwargs": null,
          "name": "test-value",
          "updated_at": "2024-01-01T00:00:00Z",
        },
      },
    });
    const uninstall = installFetchMock(url);
    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiKey": "test-key" },
        definition: {
          id: "test-id",
          name: "test-models",
          version: 1,
          tags: {},
        },
      });
      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).update_auth_config.execute({
        "api_key_secret_id": "test-value",
        "auth_config_id": "test-id-123",
        "base_url": "test-value",
        "created_at": "2024-01-01T00:00:00Z",
        "kwargs": null,
        "name": "test-value",
        "updated_at": "2024-01-01T00:00:00Z",
      }, context);
      assertEquals(result.dataHandles.length, 1);
      assertEquals(getWrittenResources().length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "models model: delete_auth_config executes successfully",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockGtServer({
      "/api/models/auth-configs/test-id-123": { body: {}, status: 204 },
    });
    const uninstall = installFetchMock(url);
    try {
      const { context } = createModelTestContext({
        globalArgs: { "apiKey": "test-key" },
        definition: {
          id: "test-id",
          name: "test-models",
          version: 1,
          tags: {},
        },
      });
      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).delete_auth_config.execute(
        { "auth_config_id": "test-id-123" },
        context,
      );
      assertEquals(result.dataHandles.length, 0);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});
