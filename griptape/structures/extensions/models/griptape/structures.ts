/**
 * Griptape Cloud Structures — deployed structures, runs, logs, and spans
 *
 * Auto-generated by scripts/griptape-codegen — do not edit manually.
 *
 * @module
 */
// SPDX-License-Identifier: Apache-2.0

import { z } from "npm:zod@4.4.3";
import {
  griptapeApi,
  griptapeApiPaginated,
  sanitizeInstanceName,
} from "./_lib/api.ts";

const EXTENSION_NAME = "@webframp/griptape/structures";

// =============================================================================
// Schemas
// =============================================================================

const GlobalArgsSchema = z.object({
  apiKey: z.string().meta({ sensitive: true }).describe(
    "Griptape Cloud API key; overrides the GT_CLOUD_API_KEY environment variable. Wire with a vault.get(...) expression to source it from a vault.",
  ).optional(),
  baseUrl: z.string().describe(
    "Griptape Cloud API base URL; overrides the GT_CLOUD_BASE_URL environment variable and the built-in default.",
  ).optional(),
});

const GetStructureRunSchema = z.looseObject({
  args: z.array(z.string()),
  completed_at: z.string().nullable().default(null),
  created_at: z.string(),
  created_by: z.string(),
  deployment_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ).optional(),
  env_vars: z.array(z.unknown()).optional(),
  output: z.unknown().optional(),
  output_timestamp: z.number().optional(),
  started_at: z.string().nullable().default(null),
  status: z.unknown(),
  status_detail: z.unknown().optional(),
  structure_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  structure_run_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  updated_at: z.string(),
});

const EventsItemSchema = z.looseObject({
  created_at: z.string(),
  event_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  origin: z.string(),
  payload: z.unknown(),
  structure_run_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  timestamp: z.number(),
  type: z.string(),
});

const ListEventsSchema = z.object({
  items: z.array(EventsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const StructureRunLogsItemSchema = z.string();

const ListStructureRunLogsSchema = z.object({
  items: z.array(StructureRunLogsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const SpansItemSchema = z.looseObject({
  attributes: z.unknown(),
  end_time: z.string(),
  events: z.unknown(),
  name: z.string(),
  parent_id: z.string().optional(),
  span_id: z.string(),
  start_time: z.string(),
  status: z.unknown(),
  trace_id: z.string(),
});

const ListSpansSchema = z.object({
  items: z.array(SpansItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const StructuresItemSchema = z.looseObject({
  code: z.unknown(),
  created_at: z.string(),
  created_by: z.string(),
  description: z.string(),
  env_vars: z.array(z.unknown()),
  latest_deployment_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  name: z.string(),
  organization_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  structure_config_file: z.string().optional(),
  structure_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  updated_at: z.string(),
  webhook_enabled: z.boolean(),
});

const ListStructuresSchema = z.object({
  items: z.array(StructuresItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const CreateStructureSchema = z.looseObject({
  code: z.unknown(),
  created_at: z.string(),
  created_by: z.string(),
  description: z.string(),
  env_vars: z.array(z.unknown()),
  latest_deployment_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  name: z.string(),
  organization_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  structure_config_file: z.string().optional(),
  structure_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  updated_at: z.string(),
  webhook_enabled: z.boolean(),
});

const StructureDeploymentsItemSchema = z.looseObject({
  code_source: z.unknown(),
  completed_at: z.string().nullable().optional().default(null),
  created_at: z.string(),
  created_by: z.string(),
  deployment_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  status: z.unknown(),
  status_detail: z.unknown().optional(),
  structure_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
});

const ListStructureDeploymentsSchema = z.object({
  items: z.array(StructureDeploymentsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const CreateStructureDeploymentSchema = z.looseObject({
  code_source: z.unknown(),
  completed_at: z.string().nullable().optional().default(null),
  created_at: z.string(),
  created_by: z.string(),
  deployment_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  status: z.unknown(),
  status_detail: z.unknown().optional(),
  structure_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
});

const StructureRunsItemSchema = z.looseObject({
  args: z.array(z.string()),
  completed_at: z.string().nullable().default(null),
  created_at: z.string(),
  created_by: z.string(),
  deployment_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ).optional(),
  env_vars: z.array(z.unknown()).optional(),
  output: z.unknown().optional(),
  output_timestamp: z.number().optional(),
  started_at: z.string().nullable().default(null),
  status: z.unknown(),
  status_detail: z.unknown().optional(),
  structure_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  structure_run_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  updated_at: z.string(),
});

const ListStructureRunsSchema = z.object({
  items: z.array(StructureRunsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const GetInvokeStructureWebhookGetSchema = z.looseObject({
  code: z.unknown(),
  created_at: z.string(),
  created_by: z.string(),
  description: z.string(),
  env_vars: z.array(z.unknown()),
  latest_deployment_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  name: z.string(),
  organization_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  structure_config_file: z.string().optional(),
  structure_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  updated_at: z.string(),
  webhook_enabled: z.boolean(),
});

const InvokeStructureWebhookPostSchema = z.looseObject({
  code: z.unknown(),
  created_at: z.string(),
  created_by: z.string(),
  description: z.string(),
  env_vars: z.array(z.unknown()),
  latest_deployment_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  name: z.string(),
  organization_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  structure_config_file: z.string().optional(),
  structure_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  updated_at: z.string(),
  webhook_enabled: z.boolean(),
});

// =============================================================================
// Model Definition
// =============================================================================

/** Griptape Cloud Structures — deployed structures, runs, logs, and spans */
export const model = {
  type: "@webframp/griptape/structures",
  version: "2026.08.28.1",
  globalArguments: GlobalArgsSchema,

  upgrades: [],

  resources: {
    "structure_run": {
      description: "get operation",
      schema: GetStructureRunSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "events": {
      description: "list operation",
      schema: ListEventsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "structure_run_logs": {
      description: "list operation",
      schema: ListStructureRunLogsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "spans": {
      description: "list operation",
      schema: ListSpansSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "structures": {
      description: "list operation",
      schema: ListStructuresSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "structure": {
      description: "create operation",
      schema: CreateStructureSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "structure_deployments": {
      description: "list operation",
      schema: ListStructureDeploymentsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "structure_deployment": {
      description: "create operation",
      schema: CreateStructureDeploymentSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "structure_runs": {
      description: "list operation",
      schema: ListStructureRunsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "invoke_structure_webhook_get": {
      description:
        "Invoke a webhook for a structure. Must have the `webhook_enabled` flag set to `true`.",
      schema: GetInvokeStructureWebhookGetSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "invoke_structure_webhook_post": {
      description:
        "Invoke a webhook for a structure, sending the POST body as the first Structure arg. Must have the `webhook_enabled` flag set to `true`.",
      schema: InvokeStructureWebhookPostSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
  },

  methods: {
    get_structure_run: {
      description: "get operation",
      arguments: z.object({
        structure_run_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "GET",
          `/api/structure-runs/${
            encodeURIComponent(String(args.structure_run_id))
          }`,
          undefined,
          baseUrl,
        );

        const handle = await context.writeResource(
          "structure_run",
          sanitizeInstanceName(String(args.structure_run_id)),
          result,
        );
        context.logger.info("Fetched structure_run", {});
        return { dataHandles: [handle] };
      },
    },
    cancel_structure_run: {
      description: "action operation",
      arguments: z.object({
        structure_run_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["structure_run_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }
        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "POST",
          `/api/structure-runs/${
            encodeURIComponent(String(args.structure_run_id))
          }/cancel`,
          body,
          baseUrl,
        );

        const handle = await context.writeResource(
          "structure_run",
          sanitizeInstanceName(String(args.structure_run_id)),
          result,
        );
        context.logger.info("Ran cancel_structure_run", {});
        return { dataHandles: [handle] };
      },
    },
    list_events: {
      description: "list operation",
      arguments: z.object({
        structure_run_id: z.string(),
        limit: z.string().optional(),
        offset: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const startMs = Date.now();
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["structure_run_id", "page", "page_size"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await griptapeApiPaginated<
          Record<string, unknown>
        >(
          apiKey,
          `/api/structure-runs/${
            encodeURIComponent(String(args.structure_run_id))
          }/events`,
          "events",
          params,
          baseUrl,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("events", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
          durationMs: Date.now() - startMs,
          collectedBy: EXTENSION_NAME,
        });

        context.logger.info("Found {count} events", { count: results.length });
        return { dataHandles: [handle] };
      },
    },
    create_events: {
      description: "create operation",
      arguments: z.object({
        structure_run_id: z.string(),
        events: z.array(z.unknown()).optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["structure_run_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "POST",
          `/api/structure-runs/${
            encodeURIComponent(String(args.structure_run_id))
          }/events`,
          body,
          baseUrl,
        );

        const record = result as Record<string, unknown>;
        const pathParamValues = new Set<string>([
          String(args.structure_run_id),
        ]);
        const idCandidates = ["event_id", "id"];
        let rawId = idCandidates.map((k) => record[k]).find((v) =>
          v !== undefined && v !== null && v !== ""
        );
        if (rawId === undefined) {
          // No named id candidate matched. Scan for a single id-shaped key that is
          // not organization_id and whose value is not a path param (the parent
          // addressing context, e.g. bucket_id on an asset — would collide).
          for (const [k, v] of Object.entries(record)) {
            if (
              /(^|_)id$/.test(k) && k !== "organization_id" &&
              typeof v === "string" && v !== "" && !pathParamValues.has(v)
            ) {
              rawId = v;
              break;
            }
          }
        }
        if (
          rawId === undefined && typeof record.name === "string" &&
          record.name !== ""
        ) {
          rawId = record.name;
        }
        const fallbackId =
          [String(args.structure_run_id)].filter((s: string) =>
            s && s !== "undefined"
          ).join("_") || "created";
        const id = sanitizeInstanceName(String(rawId ?? fallbackId));
        const handle = await context.writeResource("events", id, result);
        context.logger.info("Created events {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    list_structure_run_logs: {
      description: "list operation",
      arguments: z.object({
        structure_run_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const startMs = Date.now();
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["structure_run_id", "page", "page_size"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await griptapeApiPaginated<
          Record<string, unknown>
        >(
          apiKey,
          `/api/structure-runs/${
            encodeURIComponent(String(args.structure_run_id))
          }/logs`,
          "logs",
          params,
          baseUrl,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "structure_run_logs",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );

        context.logger.info("Found {count} structure_run_logs", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    list_spans: {
      description: "list operation",
      arguments: z.object({
        structure_run_id: z.string(),
        page: z.number().optional(),
        page_size: z.number().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const startMs = Date.now();
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["structure_run_id", "page", "page_size"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await griptapeApiPaginated<
          Record<string, unknown>
        >(
          apiKey,
          `/api/structure-runs/${
            encodeURIComponent(String(args.structure_run_id))
          }/spans`,
          "spans",
          params,
          baseUrl,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("spans", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
          durationMs: Date.now() - startMs,
          collectedBy: EXTENSION_NAME,
        });

        context.logger.info("Found {count} spans", { count: results.length });
        return { dataHandles: [handle] };
      },
    },
    list_structures: {
      description: "list operation",
      arguments: z.object({
        page: z.number().optional(),
        page_size: z.number().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const startMs = Date.now();
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "page_size"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await griptapeApiPaginated<
          Record<string, unknown>
        >(
          apiKey,
          `/api/structures`,
          "structures",
          params,
          baseUrl,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("structures", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
          durationMs: Date.now() - startMs,
          collectedBy: EXTENSION_NAME,
        });

        context.logger.info("Found {count} structures", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_structure: {
      description: "create operation",
      arguments: z.object({
        code: z.unknown().optional(),
        description: z.string().min(1).max(200).optional(),
        env_vars: z.array(z.unknown()).optional(),
        name: z.string().min(1).max(200),
        structure_config_file: z.string().min(1).max(200).optional(),
        webhook_enabled: z.boolean().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const body = args;

        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "POST",
          `/api/structures`,
          body,
          baseUrl,
        );

        const record = result as Record<string, unknown>;
        const pathParamValues = new Set<string>([]);
        const idCandidates = ["structure_id", "id"];
        let rawId = idCandidates.map((k) => record[k]).find((v) =>
          v !== undefined && v !== null && v !== ""
        );
        if (rawId === undefined) {
          // No named id candidate matched. Scan for a single id-shaped key that is
          // not organization_id and whose value is not a path param (the parent
          // addressing context, e.g. bucket_id on an asset — would collide).
          for (const [k, v] of Object.entries(record)) {
            if (
              /(^|_)id$/.test(k) && k !== "organization_id" &&
              typeof v === "string" && v !== "" && !pathParamValues.has(v)
            ) {
              rawId = v;
              break;
            }
          }
        }
        if (
          rawId === undefined && typeof record.name === "string" &&
          record.name !== ""
        ) {
          rawId = record.name;
        }
        const fallbackId =
          [].filter((s: string) => s && s !== "undefined").join("_") ||
          "created";
        const id = sanitizeInstanceName(String(rawId ?? fallbackId));
        const handle = await context.writeResource("structure", id, result);
        context.logger.info("Created structure {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_structure: {
      description: "get operation",
      arguments: z.object({
        structure_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "GET",
          `/api/structures/${encodeURIComponent(String(args.structure_id))}`,
          undefined,
          baseUrl,
        );

        const handle = await context.writeResource(
          "structure",
          sanitizeInstanceName(String(args.structure_id)),
          result,
        );
        context.logger.info("Fetched structure", {});
        return { dataHandles: [handle] };
      },
    },
    update_structure: {
      description: "update operation",
      arguments: z.object({
        structure_id: z.string(),
        code: z.unknown().optional(),
        description: z.string().min(1).max(200).optional(),
        env_vars: z.array(z.unknown()).optional(),
        name: z.string().min(1).max(200).optional(),
        structure_config_file: z.string().min(1).max(200).optional(),
        webhook_enabled: z.boolean().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["structure_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "PATCH",
          `/api/structures/${encodeURIComponent(String(args.structure_id))}`,
          body,
          baseUrl,
        );

        const handle = await context.writeResource(
          "structure",
          sanitizeInstanceName(String(args.structure_id)),
          result,
        );
        context.logger.info("Updated structure", {});
        return { dataHandles: [handle] };
      },
    },
    delete_structure: {
      description: "delete operation",
      arguments: z.object({
        structure_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        await griptapeApi<Record<string, unknown>>(
          apiKey,
          "DELETE",
          `/api/structures/${encodeURIComponent(String(args.structure_id))}`,
          undefined,
          baseUrl,
        );

        context.logger.info("Deleted resource {id}", { id: args.structure_id });
        return { dataHandles: [] };
      },
    },
    list_structure_deployments: {
      description: "list operation",
      arguments: z.object({
        structure_id: z.string(),
        page: z.number().optional(),
        page_size: z.number().optional(),
        status: z.string().optional().describe(
          "Comma-separated list of deployment statuses to filter by.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const startMs = Date.now();
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["structure_id", "page", "page_size"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await griptapeApiPaginated<
          Record<string, unknown>
        >(
          apiKey,
          `/api/structures/${
            encodeURIComponent(String(args.structure_id))
          }/deployments`,
          "deployments",
          params,
          baseUrl,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "structure_deployments",
          "main",
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );

        context.logger.info("Found {count} structure_deployments", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_structure_deployment: {
      description: "create operation",
      arguments: z.object({
        structure_id: z.string(),
        code_source: z.unknown().optional(),
        force: z.boolean().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["structure_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "POST",
          `/api/structures/${
            encodeURIComponent(String(args.structure_id))
          }/deployments`,
          body,
          baseUrl,
        );

        const record = result as Record<string, unknown>;
        const pathParamValues = new Set<string>([String(args.structure_id)]);
        const idCandidates = ["structure_deployment_id", "id"];
        let rawId = idCandidates.map((k) => record[k]).find((v) =>
          v !== undefined && v !== null && v !== ""
        );
        if (rawId === undefined) {
          // No named id candidate matched. Scan for a single id-shaped key that is
          // not organization_id and whose value is not a path param (the parent
          // addressing context, e.g. bucket_id on an asset — would collide).
          for (const [k, v] of Object.entries(record)) {
            if (
              /(^|_)id$/.test(k) && k !== "organization_id" &&
              typeof v === "string" && v !== "" && !pathParamValues.has(v)
            ) {
              rawId = v;
              break;
            }
          }
        }
        if (
          rawId === undefined && typeof record.name === "string" &&
          record.name !== ""
        ) {
          rawId = record.name;
        }
        const fallbackId =
          [String(args.structure_id)].filter((s: string) =>
            s && s !== "undefined"
          ).join("_") || "created";
        const id = sanitizeInstanceName(String(rawId ?? fallbackId));
        const handle = await context.writeResource(
          "structure_deployment",
          id,
          result,
        );
        context.logger.info("Created structure_deployment {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    list_structure_runs: {
      description: "list operation",
      arguments: z.object({
        structure_id: z.string(),
        page: z.number().optional(),
        page_size: z.number().optional(),
        status: z.string().optional().describe(
          "Comma-separated list of run statuses to filter by.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const startMs = Date.now();
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["structure_id", "page", "page_size"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await griptapeApiPaginated<
          Record<string, unknown>
        >(
          apiKey,
          `/api/structures/${
            encodeURIComponent(String(args.structure_id))
          }/runs`,
          "structure_runs",
          params,
          baseUrl,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("structure_runs", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
          durationMs: Date.now() - startMs,
          collectedBy: EXTENSION_NAME,
        });

        context.logger.info("Found {count} structure_runs", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_structure_run: {
      description: "create operation",
      arguments: z.object({
        structure_id: z.string(),
        args: z.array(z.string()),
        env_vars: z.array(z.unknown()).optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["structure_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "POST",
          `/api/structures/${
            encodeURIComponent(String(args.structure_id))
          }/runs`,
          body,
          baseUrl,
        );

        const record = result as Record<string, unknown>;
        const pathParamValues = new Set<string>([String(args.structure_id)]);
        const idCandidates = ["structure_run_id", "id"];
        let rawId = idCandidates.map((k) => record[k]).find((v) =>
          v !== undefined && v !== null && v !== ""
        );
        if (rawId === undefined) {
          // No named id candidate matched. Scan for a single id-shaped key that is
          // not organization_id and whose value is not a path param (the parent
          // addressing context, e.g. bucket_id on an asset — would collide).
          for (const [k, v] of Object.entries(record)) {
            if (
              /(^|_)id$/.test(k) && k !== "organization_id" &&
              typeof v === "string" && v !== "" && !pathParamValues.has(v)
            ) {
              rawId = v;
              break;
            }
          }
        }
        if (
          rawId === undefined && typeof record.name === "string" &&
          record.name !== ""
        ) {
          rawId = record.name;
        }
        const fallbackId =
          [String(args.structure_id)].filter((s: string) =>
            s && s !== "undefined"
          ).join("_") || "created";
        const id = sanitizeInstanceName(String(rawId ?? fallbackId));
        const handle = await context.writeResource("structure_run", id, result);
        context.logger.info("Created structure_run {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_invoke_structure_webhook_get: {
      description:
        "Invoke a webhook for a structure. Must have the `webhook_enabled` flag set to `true`.",
      arguments: z.object({
        structure_id: z.string(),
        api_key: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "GET",
          `/api/structures/${
            encodeURIComponent(String(args.structure_id))
          }/webhook`,
          undefined,
          baseUrl,
        );

        const handle = await context.writeResource(
          "invoke_structure_webhook_get",
          sanitizeInstanceName(String(args.structure_id)),
          result,
        );
        context.logger.info("Fetched invoke_structure_webhook_get", {});
        return { dataHandles: [handle] };
      },
    },
    invoke_structure_webhook_post: {
      description:
        "Invoke a webhook for a structure, sending the POST body as the first Structure arg. Must have the `webhook_enabled` flag set to `true`.",
      arguments: z.object({
        structure_id: z.string(),
        api_key: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["structure_id", "api_key"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }
        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "POST",
          `/api/structures/${
            encodeURIComponent(String(args.structure_id))
          }/webhook`,
          body,
          baseUrl,
        );

        const handle = await context.writeResource(
          "invoke_structure_webhook_post",
          sanitizeInstanceName(String(args.structure_id)),
          result,
        );
        context.logger.info("Ran invoke_structure_webhook_post", {});
        return { dataHandles: [handle] };
      },
    },
  },
};
