# @webframp/griptape/rulesets

Griptape Cloud Rulesets — behavioral rulesets and their rules

Auto-generated by `scripts/griptape-codegen` from the Griptape Cloud OpenAPI
specification.

## Usage

```bash
swamp extension pull @webframp/griptape/rulesets

swamp model create @webframp/griptape/rulesets gt-rulesets \
  --global-arg apiKey=GRIPTAPE_CLOUD_API_KEY
```

The `apiKey` global argument overrides the `GT_CLOUD_API_KEY` environment
variable and is wireable with a `vault.get(...)` expression. An optional
`baseUrl` argument overrides `GT_CLOUD_BASE_URL` and the built-in default
(`https://cloud.griptape.ai/api`).

## Methods

| Method           | Description      |
| ---------------- | ---------------- |
| `list_rules`     | list operation   |
| `create_rule`    | create operation |
| `get_rule`       | get operation    |
| `update_rule`    | update operation |
| `delete_rule`    | delete operation |
| `list_rulesets`  | list operation   |
| `create_ruleset` | create operation |
| `get_ruleset`    | get operation    |
| `update_ruleset` | update operation |
| `delete_ruleset` | delete operation |

## Development

```bash
cd ../../griptape/rulesets
deno task check
deno task lint
deno task fmt
deno task test
```

## License

Apache-2.0 — see [LICENSE.md](LICENSE.md).
