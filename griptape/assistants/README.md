# @webframp/griptape/assistants

Griptape Cloud Assistants — assistant definitions and assistant runs

Auto-generated by `scripts/griptape-codegen` from the Griptape Cloud OpenAPI
specification.

## Usage

```bash
swamp extension pull @webframp/griptape/assistants

swamp model create @webframp/griptape/assistants gt-assistants \
  --global-arg apiKey=GRIPTAPE_CLOUD_API_KEY
```

The `apiKey` global argument overrides the `GT_CLOUD_API_KEY` environment
variable and is wireable with a `vault.get(...)` expression. An optional
`baseUrl` argument overrides `GT_CLOUD_BASE_URL` and the built-in default
(`https://cloud.griptape.ai`).

## Methods

| Method                  | Description      |
| ----------------------- | ---------------- |
| `get_assistant_run`     | get operation    |
| `cancel_assistant_run`  | action operation |
| `list_assistant_events` | list operation   |
| `list_assistants`       | list operation   |
| `create_assistant`      | create operation |
| `get_assistant`         | get operation    |
| `update_assistant`      | update operation |
| `delete_assistant`      | delete operation |
| `list_assistant_runs`   | list operation   |
| `create_assistant_run`  | create operation |

## Development

```bash
cd ../../griptape/assistants
deno task check
deno task lint
deno task fmt
deno task test
```

## License

Apache-2.0 — see [LICENSE.md](LICENSE.md).
