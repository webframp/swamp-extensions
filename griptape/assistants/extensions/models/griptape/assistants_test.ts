// Griptape Cloud Assistants — assistant definitions and assistant runs — Tests
// Auto-generated by scripts/griptape-codegen — do not edit manually.
// SPDX-License-Identifier: Apache-2.0

import { assertEquals, assertExists } from "jsr:@std/assert@1.0.19";
import { createModelTestContext } from "@systeminit/swamp-testing";
import { model } from "./assistants.ts";

// ---------------------------------------------------------------------------
// Model Export Structure Tests
// ---------------------------------------------------------------------------

Deno.test("assistants model: has correct type", () => {
  assertEquals(model.type, "@webframp/griptape/assistants");
});

Deno.test("assistants model: has valid version format", () => {
  const versionPattern = /^\d{4}\.\d{2}\.\d{2}\.\d+$/;
  assertEquals(versionPattern.test(model.version), true);
});

Deno.test("assistants model: has globalArguments with apiKey", () => {
  assertExists(model.globalArguments);
  const shape = model.globalArguments.shape;
  assertExists(shape.apiKey);
});

Deno.test("assistants model: has expected methods", () => {
  assertExists(model.methods);
  assertExists(model.methods.get_assistant_run);
  assertExists(model.methods.cancel_assistant_run);
  assertExists(model.methods.list_assistant_events);
  assertExists(model.methods.list_assistants);
  assertExists(model.methods.create_assistant);
  assertExists(model.methods.get_assistant);
  assertExists(model.methods.update_assistant);
  assertExists(model.methods.delete_assistant);
  assertExists(model.methods.list_assistant_runs);
  assertExists(model.methods.create_assistant_run);
});

Deno.test("assistants model: has expected resources", () => {
  assertExists(model.resources);
  assertExists(model.resources["assistant_run"]);
  assertExists(model.resources["assistant_events"]);
  assertExists(model.resources["assistants"]);
  assertExists(model.resources["assistant"]);
  assertExists(model.resources["assistant_runs"]);
});

// ---------------------------------------------------------------------------
// Mock Griptape API Server
// ---------------------------------------------------------------------------

interface MockResponse {
  body: unknown;
  itemsKey?: string;
  status?: number;
}

function startMockGtServer(
  responses: Record<string, MockResponse>,
): { url: string; server: Deno.HttpServer } {
  const server = Deno.serve({ port: 0, onListen() {} }, (req) => {
    const url = new URL(req.url);
    const path = url.pathname;

    for (const [pattern, spec] of Object.entries(responses)) {
      if (path.includes(pattern)) {
        const status = spec.status ?? 200;
        // 204/205 must have a null body.
        if (status === 204 || status === 205) {
          return new Response(null, { status });
        }
        if (spec.itemsKey) {
          return Response.json({
            [spec.itemsKey]: spec.body,
            pagination: {
              page_number: 1,
              page_size: 50,
              total_count: Array.isArray(spec.body) ? spec.body.length : 0,
              total_pages: 1,
            },
          }, { status });
        }
        return Response.json(spec.body, { status });
      }
    }

    return Response.json({ message: "Not found" }, { status: 404 });
  });

  const addr = server.addr as Deno.NetAddr;
  return { url: `http://localhost:${addr.port}`, server };
}

function installFetchMock(mockUrl: string): () => void {
  const originalFetch = globalThis.fetch;
  globalThis.fetch = (input, init) => {
    const reqUrl = typeof input === "string" ? input : input.toString();
    const newUrl = reqUrl.replace("https://cloud.griptape.ai/api", mockUrl);
    return originalFetch(newUrl, init);
  };
  return () => {
    globalThis.fetch = originalFetch;
  };
}

Deno.test({
  name: "assistants model: get_assistant_run fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockGtServer({
      "/api/assistant-runs/test-id-123": {
        body: {
          "args": ["test-value"],
          "assistant_id": "test-value",
          "assistant_run_id": "test-value",
          "completed_at": "2024-01-01T00:00:00Z",
          "created_at": "2024-01-01T00:00:00Z",
          "created_by": "test-value",
          "input": "test-value",
          "knowledge_base_ids": ["test-value"],
          "model": "test-value",
          "output": null,
          "retriever_ids": ["test-value"],
          "ruleset_ids": ["test-value"],
          "status": null,
          "status_detail": null,
          "stream": true,
          "structure_ids": ["test-value"],
          "thread_id": "test-value",
          "tool_ids": ["test-value"],
          "updated_at": "2024-01-01T00:00:00Z",
        },
      },
    });
    const uninstall = installFetchMock(url);
    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiKey": "test-key" },
        definition: {
          id: "test-id",
          name: "test-assistants",
          version: 1,
          tags: {},
        },
      });
      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).get_assistant_run.execute(
        { "assistant_run_id": "test-id-123" },
        context,
      );
      assertEquals(result.dataHandles.length, 1);
      assertEquals(getWrittenResources().length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "assistants model: cancel_assistant_run executes and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockGtServer({
      "/api/assistant-runs/test-id-123/cancel": {
        body: {
          "args": ["test-value"],
          "assistant_id": "test-value",
          "assistant_run_id": "test-value",
          "completed_at": "2024-01-01T00:00:00Z",
          "created_at": "2024-01-01T00:00:00Z",
          "created_by": "test-value",
          "input": "test-value",
          "knowledge_base_ids": ["test-value"],
          "model": "test-value",
          "output": null,
          "retriever_ids": ["test-value"],
          "ruleset_ids": ["test-value"],
          "status": null,
          "status_detail": null,
          "stream": true,
          "structure_ids": ["test-value"],
          "thread_id": "test-value",
          "tool_ids": ["test-value"],
          "updated_at": "2024-01-01T00:00:00Z",
        },
      },
    });
    const uninstall = installFetchMock(url);
    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiKey": "test-key" },
        definition: {
          id: "test-id",
          name: "test-assistants",
          version: 1,
          tags: {},
        },
      });
      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).cancel_assistant_run.execute({
        "args": ["test-value"],
        "assistant_id": "test-value",
        "assistant_run_id": "test-id-123",
        "completed_at": "2024-01-01T00:00:00Z",
        "created_at": "2024-01-01T00:00:00Z",
        "created_by": "test-value",
        "input": "test-value",
        "knowledge_base_ids": ["test-value"],
        "model": "test-value",
        "output": null,
        "retriever_ids": ["test-value"],
        "ruleset_ids": ["test-value"],
        "status": null,
        "status_detail": null,
        "stream": true,
        "structure_ids": ["test-value"],
        "thread_id": "test-value",
        "tool_ids": ["test-value"],
        "updated_at": "2024-01-01T00:00:00Z",
      }, context);
      assertEquals(result.dataHandles.length, 1);
      assertEquals(getWrittenResources().length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "assistants model: list_assistant_events fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockGtServer({
      "/api/assistant-runs/test-id-123/events": {
        body: [{
          "assistant_run_id": "test-value",
          "created_at": "2024-01-01T00:00:00Z",
          "event_id": "test-value",
          "origin": "test-value",
          "payload": null,
          "timestamp": 1,
          "type": "test-value",
        }],
        itemsKey: "events",
      },
    });
    const uninstall = installFetchMock(url);
    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiKey": "test-key" },
        definition: {
          id: "test-id",
          name: "test-assistants",
          version: 1,
          tags: {},
        },
      });
      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).list_assistant_events.execute(
        { "assistant_run_id": "test-id-123" },
        context,
      );
      assertEquals(result.dataHandles.length, 1);
      assertEquals(getWrittenResources().length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "assistants model: create_assistant creates and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockGtServer({
      "/api/assistants": {
        body: {
          "assistant_id": "test-value",
          "created_at": "2024-01-01T00:00:00Z",
          "created_by": "test-value",
          "description": "test-value",
          "input": "test-value",
          "knowledge_base_ids": ["test-value"],
          "model": "test-value",
          "name": "test-value",
          "organization_id": "test-value",
          "retriever_ids": ["test-value"],
          "ruleset_ids": ["test-value"],
          "structure_ids": ["test-value"],
          "tool_ids": ["test-value"],
          "updated_at": "2024-01-01T00:00:00Z",
        },
      },
    });
    const uninstall = installFetchMock(url);
    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiKey": "test-key" },
        definition: {
          id: "test-id",
          name: "test-assistants",
          version: 1,
          tags: {},
        },
      });
      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).create_assistant.execute({
        "assistant_id": "test-value",
        "created_at": "2024-01-01T00:00:00Z",
        "created_by": "test-value",
        "description": "test-value",
        "input": "test-value",
        "knowledge_base_ids": ["test-value"],
        "model": "test-value",
        "name": "test-value",
        "organization_id": "test-value",
        "retriever_ids": ["test-value"],
        "ruleset_ids": ["test-value"],
        "structure_ids": ["test-value"],
        "tool_ids": ["test-value"],
        "updated_at": "2024-01-01T00:00:00Z",
      }, context);
      assertEquals(result.dataHandles.length, 1);
      assertEquals(getWrittenResources().length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "assistants model: update_assistant executes and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockGtServer({
      "/api/assistants/test-id-123": {
        body: {
          "assistant_id": "test-value",
          "created_at": "2024-01-01T00:00:00Z",
          "created_by": "test-value",
          "description": "test-value",
          "input": "test-value",
          "knowledge_base_ids": ["test-value"],
          "model": "test-value",
          "name": "test-value",
          "organization_id": "test-value",
          "retriever_ids": ["test-value"],
          "ruleset_ids": ["test-value"],
          "structure_ids": ["test-value"],
          "tool_ids": ["test-value"],
          "updated_at": "2024-01-01T00:00:00Z",
        },
      },
    });
    const uninstall = installFetchMock(url);
    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiKey": "test-key" },
        definition: {
          id: "test-id",
          name: "test-assistants",
          version: 1,
          tags: {},
        },
      });
      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).update_assistant.execute({
        "assistant_id": "test-id-123",
        "created_at": "2024-01-01T00:00:00Z",
        "created_by": "test-value",
        "description": "test-value",
        "input": "test-value",
        "knowledge_base_ids": ["test-value"],
        "model": "test-value",
        "name": "test-value",
        "organization_id": "test-value",
        "retriever_ids": ["test-value"],
        "ruleset_ids": ["test-value"],
        "structure_ids": ["test-value"],
        "tool_ids": ["test-value"],
        "updated_at": "2024-01-01T00:00:00Z",
      }, context);
      assertEquals(result.dataHandles.length, 1);
      assertEquals(getWrittenResources().length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "assistants model: delete_assistant executes successfully",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockGtServer({
      "/api/assistants/test-id-123": { body: {}, status: 204 },
    });
    const uninstall = installFetchMock(url);
    try {
      const { context } = createModelTestContext({
        globalArgs: { "apiKey": "test-key" },
        definition: {
          id: "test-id",
          name: "test-assistants",
          version: 1,
          tags: {},
        },
      });
      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).delete_assistant.execute({ "assistant_id": "test-id-123" }, context);
      assertEquals(result.dataHandles.length, 0);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});
