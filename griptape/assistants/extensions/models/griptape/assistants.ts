/**
 * Griptape Cloud Assistants — assistant definitions and assistant runs
 *
 * Auto-generated by scripts/griptape-codegen — do not edit manually.
 *
 * @module
 */
// SPDX-License-Identifier: Apache-2.0

import { z } from "npm:zod@4.4.3";
import {
  griptapeApi,
  griptapeApiPaginated,
  sanitizeInstanceName,
} from "./_lib/api.ts";

const EXTENSION_NAME = "@webframp/griptape/assistants";

// =============================================================================
// Schemas
// =============================================================================

const GlobalArgsSchema = z.object({
  apiKey: z.string().meta({ sensitive: true }).describe(
    "Griptape Cloud API key; overrides the GT_CLOUD_API_KEY environment variable. Wire with a vault.get(...) expression to source it from a vault.",
  ).optional(),
  baseUrl: z.string().describe(
    "Griptape Cloud API base URL; overrides the GT_CLOUD_BASE_URL environment variable and the built-in default.",
  ).optional(),
});

const GetAssistantRunSchema = z.looseObject({
  args: z.array(z.string()),
  assistant_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  assistant_run_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  completed_at: z.string().nullable().default(null),
  created_at: z.string(),
  created_by: z.string(),
  input: z.string().optional(),
  knowledge_base_ids: z.array(
    z.string().regex(
      new RegExp(
        "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
      ),
    ),
  ),
  model: z.string().optional(),
  output: z.unknown().optional(),
  retriever_ids: z.array(
    z.string().regex(
      new RegExp(
        "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
      ),
    ),
  ),
  ruleset_ids: z.array(
    z.string().regex(
      new RegExp(
        "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
      ),
    ),
  ),
  status: z.unknown(),
  status_detail: z.unknown().optional(),
  stream: z.boolean(),
  structure_ids: z.array(
    z.string().regex(
      new RegExp(
        "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
      ),
    ),
  ),
  thread_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ).optional(),
  tool_ids: z.array(
    z.string().regex(
      new RegExp(
        "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
      ),
    ),
  ),
  updated_at: z.string(),
});

const AssistantEventsItemSchema = z.looseObject({
  assistant_run_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  created_at: z.string(),
  event_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  origin: z.string(),
  payload: z.unknown(),
  timestamp: z.number(),
  type: z.string(),
});

const ListAssistantEventsSchema = z.object({
  items: z.array(AssistantEventsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const AssistantsItemSchema = z.looseObject({
  assistant_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  created_at: z.string(),
  created_by: z.string(),
  description: z.string(),
  input: z.string().optional(),
  knowledge_base_ids: z.array(
    z.string().regex(
      new RegExp(
        "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
      ),
    ),
  ),
  model: z.string().optional(),
  name: z.string(),
  organization_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  retriever_ids: z.array(
    z.string().regex(
      new RegExp(
        "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
      ),
    ),
  ),
  ruleset_ids: z.array(
    z.string().regex(
      new RegExp(
        "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
      ),
    ),
  ),
  structure_ids: z.array(
    z.string().regex(
      new RegExp(
        "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
      ),
    ),
  ),
  tool_ids: z.array(
    z.string().regex(
      new RegExp(
        "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
      ),
    ),
  ),
  updated_at: z.string(),
});

const ListAssistantsSchema = z.object({
  items: z.array(AssistantsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const CreateAssistantSchema = z.looseObject({
  assistant_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  created_at: z.string(),
  created_by: z.string(),
  description: z.string(),
  input: z.string().optional(),
  knowledge_base_ids: z.array(
    z.string().regex(
      new RegExp(
        "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
      ),
    ),
  ),
  model: z.string().optional(),
  name: z.string(),
  organization_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  retriever_ids: z.array(
    z.string().regex(
      new RegExp(
        "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
      ),
    ),
  ),
  ruleset_ids: z.array(
    z.string().regex(
      new RegExp(
        "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
      ),
    ),
  ),
  structure_ids: z.array(
    z.string().regex(
      new RegExp(
        "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
      ),
    ),
  ),
  tool_ids: z.array(
    z.string().regex(
      new RegExp(
        "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
      ),
    ),
  ),
  updated_at: z.string(),
});

const AssistantRunsItemSchema = z.looseObject({
  args: z.array(z.string()),
  assistant_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  assistant_run_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  completed_at: z.string().nullable().default(null),
  created_at: z.string(),
  created_by: z.string(),
  input: z.string().optional(),
  knowledge_base_ids: z.array(
    z.string().regex(
      new RegExp(
        "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
      ),
    ),
  ),
  model: z.string().optional(),
  output: z.unknown().optional(),
  retriever_ids: z.array(
    z.string().regex(
      new RegExp(
        "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
      ),
    ),
  ),
  ruleset_ids: z.array(
    z.string().regex(
      new RegExp(
        "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
      ),
    ),
  ),
  status: z.unknown(),
  status_detail: z.unknown().optional(),
  stream: z.boolean(),
  structure_ids: z.array(
    z.string().regex(
      new RegExp(
        "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
      ),
    ),
  ),
  thread_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ).optional(),
  tool_ids: z.array(
    z.string().regex(
      new RegExp(
        "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
      ),
    ),
  ),
  updated_at: z.string(),
});

const ListAssistantRunsSchema = z.object({
  items: z.array(AssistantRunsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

// =============================================================================
// Model Definition
// =============================================================================

/** Griptape Cloud Assistants — assistant definitions and assistant runs */
export const model = {
  type: "@webframp/griptape/assistants",
  version: "2026.08.28.1",
  globalArguments: GlobalArgsSchema,

  upgrades: [],

  resources: {
    "assistant_run": {
      description: "get operation",
      schema: GetAssistantRunSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "assistant_events": {
      description: "list operation",
      schema: ListAssistantEventsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "assistants": {
      description: "list operation",
      schema: ListAssistantsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "assistant": {
      description: "create operation",
      schema: CreateAssistantSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "assistant_runs": {
      description: "list operation",
      schema: ListAssistantRunsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
  },

  methods: {
    get_assistant_run: {
      description: "get operation",
      arguments: z.object({
        assistant_run_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "GET",
          `/api/assistant-runs/${
            encodeURIComponent(String(args.assistant_run_id))
          }`,
          undefined,
          baseUrl,
        );

        const handle = await context.writeResource(
          "assistant_run",
          sanitizeInstanceName(String(args.assistant_run_id)),
          result,
        );
        context.logger.info("Fetched assistant_run", {});
        return { dataHandles: [handle] };
      },
    },
    cancel_assistant_run: {
      description: "action operation",
      arguments: z.object({
        assistant_run_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["assistant_run_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }
        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "POST",
          `/api/assistant-runs/${
            encodeURIComponent(String(args.assistant_run_id))
          }/cancel`,
          body,
          baseUrl,
        );

        const handle = await context.writeResource(
          "assistant_run",
          sanitizeInstanceName(String(args.assistant_run_id)),
          result,
        );
        context.logger.info("Ran cancel_assistant_run", {});
        return { dataHandles: [handle] };
      },
    },
    list_assistant_events: {
      description: "list operation",
      arguments: z.object({
        assistant_run_id: z.string(),
        limit: z.string().optional(),
        offset: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const startMs = Date.now();
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["assistant_run_id", "page", "page_size"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await griptapeApiPaginated<
          Record<string, unknown>
        >(
          apiKey,
          `/api/assistant-runs/${
            encodeURIComponent(String(args.assistant_run_id))
          }/events`,
          "events",
          params,
          baseUrl,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("assistant_events", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
          durationMs: Date.now() - startMs,
          collectedBy: EXTENSION_NAME,
        });

        context.logger.info("Found {count} assistant_events", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    list_assistants: {
      description: "list operation",
      arguments: z.object({
        page: z.number().optional(),
        page_size: z.number().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const startMs = Date.now();
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "page_size"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await griptapeApiPaginated<
          Record<string, unknown>
        >(
          apiKey,
          `/api/assistants`,
          "assistants",
          params,
          baseUrl,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("assistants", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
          durationMs: Date.now() - startMs,
          collectedBy: EXTENSION_NAME,
        });

        context.logger.info("Found {count} assistants", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_assistant: {
      description: "create operation",
      arguments: z.object({
        description: z.string().min(1).max(200).optional(),
        input: z.string().optional(),
        knowledge_base_ids: z.array(
          z.string().regex(
            new RegExp(
              "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
            ),
          ),
        ).optional(),
        model: z.string().optional(),
        name: z.string().min(1).max(200),
        retriever_ids: z.array(
          z.string().regex(
            new RegExp(
              "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
            ),
          ),
        ).optional(),
        ruleset_ids: z.array(
          z.string().regex(
            new RegExp(
              "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
            ),
          ),
        ).optional(),
        structure_ids: z.array(
          z.string().regex(
            new RegExp(
              "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
            ),
          ),
        ).optional(),
        tool_ids: z.array(
          z.string().regex(
            new RegExp(
              "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
            ),
          ),
        ).optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const body = args;

        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "POST",
          `/api/assistants`,
          body,
          baseUrl,
        );

        const record = result as Record<string, unknown>;
        const pathParamValues = new Set<string>([]);
        const idCandidates = ["assistant_id", "id"];
        let rawId = idCandidates.map((k) => record[k]).find((v) =>
          v !== undefined && v !== null && v !== ""
        );
        if (rawId === undefined) {
          // No named id candidate matched. Scan for a single id-shaped key that is
          // not organization_id and whose value is not a path param (the parent
          // addressing context, e.g. bucket_id on an asset — would collide).
          for (const [k, v] of Object.entries(record)) {
            if (
              /(^|_)id$/.test(k) && k !== "organization_id" &&
              typeof v === "string" && v !== "" && !pathParamValues.has(v)
            ) {
              rawId = v;
              break;
            }
          }
        }
        if (
          rawId === undefined && typeof record.name === "string" &&
          record.name !== ""
        ) {
          rawId = record.name;
        }
        const fallbackId =
          [].filter((s: string) => s && s !== "undefined").join("_") ||
          "created";
        const id = sanitizeInstanceName(String(rawId ?? fallbackId));
        const handle = await context.writeResource("assistant", id, result);
        context.logger.info("Created assistant {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_assistant: {
      description: "get operation",
      arguments: z.object({
        assistant_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "GET",
          `/api/assistants/${encodeURIComponent(String(args.assistant_id))}`,
          undefined,
          baseUrl,
        );

        const handle = await context.writeResource(
          "assistant",
          sanitizeInstanceName(String(args.assistant_id)),
          result,
        );
        context.logger.info("Fetched assistant", {});
        return { dataHandles: [handle] };
      },
    },
    update_assistant: {
      description: "update operation",
      arguments: z.object({
        assistant_id: z.string(),
        description: z.string().min(1).max(200).optional(),
        input: z.string().optional(),
        knowledge_base_ids: z.array(
          z.string().regex(
            new RegExp(
              "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
            ),
          ),
        ).optional(),
        model: z.string().optional(),
        name: z.string().min(1).max(200).optional(),
        ruleset_ids: z.array(
          z.string().regex(
            new RegExp(
              "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
            ),
          ),
        ).optional(),
        structure_ids: z.array(
          z.string().regex(
            new RegExp(
              "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
            ),
          ),
        ).optional(),
        tool_ids: z.array(
          z.string().regex(
            new RegExp(
              "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
            ),
          ),
        ).optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["assistant_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "PATCH",
          `/api/assistants/${encodeURIComponent(String(args.assistant_id))}`,
          body,
          baseUrl,
        );

        const handle = await context.writeResource(
          "assistant",
          sanitizeInstanceName(String(args.assistant_id)),
          result,
        );
        context.logger.info("Updated assistant", {});
        return { dataHandles: [handle] };
      },
    },
    delete_assistant: {
      description: "delete operation",
      arguments: z.object({
        assistant_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        await griptapeApi<Record<string, unknown>>(
          apiKey,
          "DELETE",
          `/api/assistants/${encodeURIComponent(String(args.assistant_id))}`,
          undefined,
          baseUrl,
        );

        context.logger.info("Deleted resource {id}", { id: args.assistant_id });
        return { dataHandles: [] };
      },
    },
    list_assistant_runs: {
      description: "list operation",
      arguments: z.object({
        assistant_id: z.string(),
        page: z.number().optional(),
        page_size: z.number().optional(),
        status: z.string().optional().describe(
          "Comma-separated list of run statuses to filter by.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const startMs = Date.now();
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["assistant_id", "page", "page_size"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await griptapeApiPaginated<
          Record<string, unknown>
        >(
          apiKey,
          `/api/assistants/${
            encodeURIComponent(String(args.assistant_id))
          }/runs`,
          "assistant_runs",
          params,
          baseUrl,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("assistant_runs", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
          durationMs: Date.now() - startMs,
          collectedBy: EXTENSION_NAME,
        });

        context.logger.info("Found {count} assistant_runs", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_assistant_run: {
      description: "create operation",
      arguments: z.object({
        assistant_id: z.string(),
        additional_knowledge_base_ids: z.array(
          z.string().regex(
            new RegExp(
              "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
            ),
          ),
        ).optional(),
        additional_retriever_ids: z.array(
          z.string().regex(
            new RegExp(
              "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
            ),
          ),
        ).optional(),
        additional_ruleset_ids: z.array(
          z.string().regex(
            new RegExp(
              "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
            ),
          ),
        ).optional(),
        additional_structure_ids: z.array(
          z.string().regex(
            new RegExp(
              "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
            ),
          ),
        ).optional(),
        additional_tool_ids: z.array(
          z.string().regex(
            new RegExp(
              "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
            ),
          ),
        ).optional(),
        args: z.array(z.string()).optional(),
        input: z.string().optional(),
        knowledge_base_ids: z.array(
          z.string().regex(
            new RegExp(
              "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
            ),
          ),
        ).optional(),
        model: z.string().optional(),
        new_thread: z.boolean().optional().describe(
          "If true, create a new thread for this run to be returned in the response thread_id.",
        ),
        retriever_ids: z.array(
          z.string().regex(
            new RegExp(
              "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
            ),
          ),
        ).optional(),
        ruleset_ids: z.array(
          z.string().regex(
            new RegExp(
              "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
            ),
          ),
        ).optional(),
        stream: z.boolean().optional(),
        structure_ids: z.array(
          z.string().regex(
            new RegExp(
              "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
            ),
          ),
        ).optional(),
        thread_id: z.string().regex(
          new RegExp(
            "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
          ),
        ).optional().describe(
          "If provided, the run will be associated with the given thread. This takes precedence over new_thr...",
        ),
        tool_ids: z.array(
          z.string().regex(
            new RegExp(
              "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
            ),
          ),
        ).optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["assistant_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "POST",
          `/api/assistants/${
            encodeURIComponent(String(args.assistant_id))
          }/runs`,
          body,
          baseUrl,
        );

        const record = result as Record<string, unknown>;
        const pathParamValues = new Set<string>([String(args.assistant_id)]);
        const idCandidates = ["assistant_run_id", "id"];
        let rawId = idCandidates.map((k) => record[k]).find((v) =>
          v !== undefined && v !== null && v !== ""
        );
        if (rawId === undefined) {
          // No named id candidate matched. Scan for a single id-shaped key that is
          // not organization_id and whose value is not a path param (the parent
          // addressing context, e.g. bucket_id on an asset — would collide).
          for (const [k, v] of Object.entries(record)) {
            if (
              /(^|_)id$/.test(k) && k !== "organization_id" &&
              typeof v === "string" && v !== "" && !pathParamValues.has(v)
            ) {
              rawId = v;
              break;
            }
          }
        }
        if (
          rawId === undefined && typeof record.name === "string" &&
          record.name !== ""
        ) {
          rawId = record.name;
        }
        const fallbackId =
          [String(args.assistant_id)].filter((s: string) =>
            s && s !== "undefined"
          ).join("_") || "created";
        const id = sanitizeInstanceName(String(rawId ?? fallbackId));
        const handle = await context.writeResource("assistant_run", id, result);
        context.logger.info("Created assistant_run {id}", { id });
        return { dataHandles: [handle] };
      },
    },
  },
};
