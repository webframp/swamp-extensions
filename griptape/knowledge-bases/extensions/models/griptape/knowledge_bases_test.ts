// Griptape Cloud Knowledge Bases — RAG knowledge bases, queries, searches, and index jobs — Tests
// Auto-generated by scripts/griptape-codegen — do not edit manually.
// SPDX-License-Identifier: Apache-2.0

import { assertEquals, assertExists } from "jsr:@std/assert@1.0.19";
import { createModelTestContext } from "@systeminit/swamp-testing";
import { model } from "./knowledge_bases.ts";

// ---------------------------------------------------------------------------
// Model Export Structure Tests
// ---------------------------------------------------------------------------

Deno.test("knowledge-bases model: has correct type", () => {
  assertEquals(model.type, "@webframp/griptape/knowledge-bases");
});

Deno.test("knowledge-bases model: has valid version format", () => {
  const versionPattern = /^\d{4}\.\d{2}\.\d{2}\.\d+$/;
  assertEquals(versionPattern.test(model.version), true);
});

Deno.test("knowledge-bases model: has globalArguments with apiKey", () => {
  assertExists(model.globalArguments);
  const shape = model.globalArguments.shape;
  assertExists(shape.apiKey);
});

Deno.test("knowledge-bases model: has expected methods", () => {
  assertExists(model.methods);
  assertExists(model.methods.get_knowledge_base_job);
  assertExists(model.methods.cancel_knowledge_base_job);
  assertExists(model.methods.get_knowledge_base_query);
  assertExists(model.methods.get_knowledge_base_search);
  assertExists(model.methods.list_knowledge_bases);
  assertExists(model.methods.create_knowledge_base);
  assertExists(model.methods.get_knowledge_base);
  assertExists(model.methods.update_knowledge_base);
  assertExists(model.methods.delete_knowledge_base);
  assertExists(model.methods.list_knowledge_base_jobs);
  assertExists(model.methods.create_knowledge_base_job);
  assertExists(model.methods.list_knowledge_base_queries);
  assertExists(model.methods.query_knowledge_base);
  assertExists(model.methods.search_knowledge_base);
  assertExists(model.methods.list_knowledge_base_searches);
});

Deno.test("knowledge-bases model: has expected resources", () => {
  assertExists(model.resources);
  assertExists(model.resources["knowledge_base_job"]);
  assertExists(model.resources["knowledge_base_query"]);
  assertExists(model.resources["knowledge_base_search"]);
  assertExists(model.resources["knowledge_bases"]);
  assertExists(model.resources["knowledge_base"]);
  assertExists(model.resources["knowledge_base_jobs"]);
  assertExists(model.resources["knowledge_base_queries"]);
  assertExists(model.resources["knowledge_base_searches"]);
});

// ---------------------------------------------------------------------------
// Mock Griptape API Server
// ---------------------------------------------------------------------------

interface MockResponse {
  body: unknown;
  itemsKey?: string;
  status?: number;
}

function startMockGtServer(
  responses: Record<string, MockResponse>,
): { url: string; server: Deno.HttpServer } {
  const server = Deno.serve({ port: 0, onListen() {} }, (req) => {
    const url = new URL(req.url);
    const path = url.pathname;

    // Guard against a doubled "/api" prefix (base URL + already-/api-prefixed
    // path). This is invisible to path.includes() matching, so assert it here:
    // a regression that reintroduces it fails the test loudly instead of 404ing
    // only in production.
    if (path.includes("/api/api/")) {
      return Response.json(
        { message: `doubled /api prefix in request path: ${path}` },
        { status: 500 },
      );
    }

    for (const [pattern, spec] of Object.entries(responses)) {
      if (path.includes(pattern)) {
        const status = spec.status ?? 200;
        // 204/205 must have a null body.
        if (status === 204 || status === 205) {
          return new Response(null, { status });
        }
        if (spec.itemsKey) {
          return Response.json({
            [spec.itemsKey]: spec.body,
            pagination: {
              page_number: 1,
              page_size: 50,
              total_count: Array.isArray(spec.body) ? spec.body.length : 0,
              total_pages: 1,
            },
          }, { status });
        }
        return Response.json(spec.body, { status });
      }
    }

    return Response.json({ message: "Not found" }, { status: 404 });
  });

  const addr = server.addr as Deno.NetAddr;
  return { url: `http://localhost:${addr.port}`, server };
}

function installFetchMock(mockUrl: string): () => void {
  const originalFetch = globalThis.fetch;
  globalThis.fetch = (input, init) => {
    const reqUrl = typeof input === "string" ? input : input.toString();
    const newUrl = reqUrl.replace("https://cloud.griptape.ai", mockUrl);
    return originalFetch(newUrl, init);
  };
  return () => {
    globalThis.fetch = originalFetch;
  };
}

Deno.test({
  name:
    "knowledge-bases model: get_knowledge_base_job fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockGtServer({
      "/api/knowledge-base-jobs/test-id-123": {
        body: {
          "bytes_ingested": 1,
          "completed_at": "2024-01-01T00:00:00Z",
          "created_at": "2024-01-01T00:00:00Z",
          "created_by": "test-value",
          "errors": [null],
          "knowledge_base_id": "test-value",
          "knowledge_base_job_id": "test-value",
          "status": null,
          "status_detail": null,
        },
      },
    });
    const uninstall = installFetchMock(url);
    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiKey": "test-key" },
        definition: {
          id: "test-id",
          name: "test-knowledge-bases",
          version: 1,
          tags: {},
        },
      });
      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).get_knowledge_base_job.execute({
        "knowledge_base_job_id": "test-id-123",
      }, context);
      assertEquals(result.dataHandles.length, 1);
      assertEquals(getWrittenResources().length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name:
    "knowledge-bases model: cancel_knowledge_base_job executes and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockGtServer({
      "/api/knowledge-base-jobs/test-id-123/cancel": {
        body: {
          "bytes_ingested": 1,
          "completed_at": "2024-01-01T00:00:00Z",
          "created_at": "2024-01-01T00:00:00Z",
          "created_by": "test-value",
          "errors": [null],
          "knowledge_base_id": "test-value",
          "knowledge_base_job_id": "test-value",
          "status": null,
          "status_detail": null,
        },
      },
    });
    const uninstall = installFetchMock(url);
    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiKey": "test-key" },
        definition: {
          id: "test-id",
          name: "test-knowledge-bases",
          version: 1,
          tags: {},
        },
      });
      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).cancel_knowledge_base_job.execute({
        "bytes_ingested": 1,
        "completed_at": "2024-01-01T00:00:00Z",
        "created_at": "2024-01-01T00:00:00Z",
        "created_by": "test-value",
        "errors": [null],
        "knowledge_base_id": "test-value",
        "knowledge_base_job_id": "test-id-123",
        "status": null,
        "status_detail": null,
      }, context);
      assertEquals(result.dataHandles.length, 1);
      assertEquals(getWrittenResources().length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name:
    "knowledge-bases model: list_knowledge_bases fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockGtServer({
      "/api/knowledge-bases": {
        body: [{
          "asset_paths": ["test-value"],
          "config": null,
          "created_at": "2024-01-01T00:00:00Z",
          "created_by": "test-value",
          "description": "test-value",
          "embedding_model": null,
          "knowledge_base_id": "test-value",
          "name": "test-value",
          "organization_id": "test-value",
          "schedule_expression": "test-value",
          "transforms": [null],
          "type": "test-value",
          "updated_at": "2024-01-01T00:00:00Z",
          "use_default_embedding_model": true,
        }],
        itemsKey: "knowledge_bases",
      },
    });
    const uninstall = installFetchMock(url);
    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiKey": "test-key" },
        definition: {
          id: "test-id",
          name: "test-knowledge-bases",
          version: 1,
          tags: {},
        },
      });
      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).list_knowledge_bases.execute({}, context);
      assertEquals(result.dataHandles.length, 1);
      assertEquals(getWrittenResources().length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name:
    "knowledge-bases model: create_knowledge_base creates and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockGtServer({
      "/api/knowledge-bases": {
        body: {
          "knowledge_base_id": "test-value",
          "asset_paths": ["test-value"],
          "config": null,
          "created_at": "2024-01-01T00:00:00Z",
          "created_by": "test-value",
          "description": "test-value",
          "embedding_model": null,
          "name": "test-value",
          "organization_id": "test-value",
          "schedule_expression": "test-value",
          "transforms": [null],
          "type": "test-value",
          "updated_at": "2024-01-01T00:00:00Z",
          "use_default_embedding_model": true,
        },
      },
    });
    const uninstall = installFetchMock(url);
    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiKey": "test-key" },
        definition: {
          id: "test-id",
          name: "test-knowledge-bases",
          version: 1,
          tags: {},
        },
      });
      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).create_knowledge_base.execute({
        "asset_paths": ["test-value"],
        "config": null,
        "created_at": "2024-01-01T00:00:00Z",
        "created_by": "test-value",
        "description": "test-value",
        "embedding_model": null,
        "knowledge_base_id": "test-value",
        "name": "test-value",
        "organization_id": "test-value",
        "schedule_expression": "test-value",
        "transforms": [null],
        "type": "test-value",
        "updated_at": "2024-01-01T00:00:00Z",
        "use_default_embedding_model": true,
      }, context);
      assertEquals(result.dataHandles.length, 1);
      assertEquals(getWrittenResources().length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name:
    "knowledge-bases model: update_knowledge_base executes and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockGtServer({
      "/api/knowledge-bases/test-id-123": {
        body: {
          "asset_paths": ["test-value"],
          "config": null,
          "created_at": "2024-01-01T00:00:00Z",
          "created_by": "test-value",
          "description": "test-value",
          "embedding_model": null,
          "knowledge_base_id": "test-value",
          "name": "test-value",
          "organization_id": "test-value",
          "schedule_expression": "test-value",
          "transforms": [null],
          "type": "test-value",
          "updated_at": "2024-01-01T00:00:00Z",
          "use_default_embedding_model": true,
        },
      },
    });
    const uninstall = installFetchMock(url);
    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiKey": "test-key" },
        definition: {
          id: "test-id",
          name: "test-knowledge-bases",
          version: 1,
          tags: {},
        },
      });
      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).update_knowledge_base.execute({
        "asset_paths": ["test-value"],
        "config": null,
        "created_at": "2024-01-01T00:00:00Z",
        "created_by": "test-value",
        "description": "test-value",
        "embedding_model": null,
        "knowledge_base_id": "test-id-123",
        "name": "test-value",
        "organization_id": "test-value",
        "schedule_expression": "test-value",
        "transforms": [null],
        "type": "test-value",
        "updated_at": "2024-01-01T00:00:00Z",
        "use_default_embedding_model": true,
      }, context);
      assertEquals(result.dataHandles.length, 1);
      assertEquals(getWrittenResources().length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "knowledge-bases model: delete_knowledge_base executes successfully",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockGtServer({
      "/api/knowledge-bases/test-id-123": { body: {}, status: 204 },
    });
    const uninstall = installFetchMock(url);
    try {
      const { context } = createModelTestContext({
        globalArgs: { "apiKey": "test-key" },
        definition: {
          id: "test-id",
          name: "test-knowledge-bases",
          version: 1,
          tags: {},
        },
      });
      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).delete_knowledge_base.execute(
        { "knowledge_base_id": "test-id-123" },
        context,
      );
      assertEquals(result.dataHandles.length, 0);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});
