/**
 * Griptape Cloud Knowledge Bases — RAG knowledge bases, queries, searches, and index jobs
 *
 * Auto-generated by scripts/griptape-codegen — do not edit manually.
 *
 * @module
 */
// SPDX-License-Identifier: Apache-2.0

import { z } from "npm:zod@4.4.3";
import {
  griptapeApi,
  griptapeApiPaginated,
  sanitizeInstanceName,
} from "./_lib/api.ts";

const EXTENSION_NAME = "@webframp/griptape/knowledge-bases";

// =============================================================================
// Schemas
// =============================================================================

const GlobalArgsSchema = z.object({
  apiKey: z.string().meta({ sensitive: true }).describe(
    "Griptape Cloud API key; overrides the GT_CLOUD_API_KEY environment variable. Wire with a vault.get(...) expression to source it from a vault.",
  ).optional(),
  baseUrl: z.string().describe(
    "Griptape Cloud API base URL; overrides the GT_CLOUD_BASE_URL environment variable and the built-in default.",
  ).optional(),
});

const GetKnowledgeBaseJobSchema = z.looseObject({
  bytes_ingested: z.number().optional(),
  completed_at: z.string().nullable().optional().default(null),
  created_at: z.string(),
  created_by: z.string(),
  errors: z.array(z.unknown()).optional(),
  knowledge_base_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  knowledge_base_job_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  status: z.unknown(),
  status_detail: z.unknown().optional(),
});

const GetKnowledgeBaseQuerySchema = z.looseObject({
  created_at: z.string(),
  created_by: z.string(),
  entries: z.array(z.unknown()),
  knowledge_base_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  knowledge_base_query_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  query: z.string(),
});

const GetKnowledgeBaseSearchSchema = z.looseObject({
  created_at: z.string(),
  created_by: z.string(),
  knowledge_base_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  knowledge_base_search_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  query: z.string(),
  result: z.string(),
});

const KnowledgeBasesItemSchema = z.looseObject({
  asset_paths: z.array(z.string()),
  config: z.unknown(),
  created_at: z.string(),
  created_by: z.string(),
  description: z.string().optional(),
  embedding_model: z.unknown().optional(),
  knowledge_base_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  name: z.string(),
  organization_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  schedule_expression: z.string().min(1).max(200).optional(),
  transforms: z.array(z.unknown()).optional(),
  type: z.string().min(1).max(200),
  updated_at: z.string(),
  use_default_embedding_model: z.boolean().optional(),
});

const ListKnowledgeBasesSchema = z.object({
  items: z.array(KnowledgeBasesItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const CreateKnowledgeBaseSchema = z.looseObject({
  asset_paths: z.array(z.string()),
  config: z.unknown(),
  created_at: z.string(),
  created_by: z.string(),
  description: z.string().optional(),
  embedding_model: z.unknown().optional(),
  knowledge_base_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  name: z.string(),
  organization_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  schedule_expression: z.string().min(1).max(200).optional(),
  transforms: z.array(z.unknown()).optional(),
  type: z.string().min(1).max(200),
  updated_at: z.string(),
  use_default_embedding_model: z.boolean().optional(),
});

const KnowledgeBaseJobsItemSchema = z.looseObject({
  bytes_ingested: z.number().optional(),
  completed_at: z.string().nullable().optional().default(null),
  created_at: z.string(),
  created_by: z.string(),
  errors: z.array(z.unknown()).optional(),
  knowledge_base_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  knowledge_base_job_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  status: z.unknown(),
  status_detail: z.unknown().optional(),
});

const ListKnowledgeBaseJobsSchema = z.object({
  items: z.array(KnowledgeBaseJobsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const KnowledgeBaseQueriesItemSchema = z.looseObject({
  created_at: z.string(),
  created_by: z.string(),
  entries: z.array(z.unknown()),
  knowledge_base_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  knowledge_base_query_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  query: z.string(),
});

const ListKnowledgeBaseQueriesSchema = z.object({
  items: z.array(KnowledgeBaseQueriesItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const KnowledgeBaseSearchesItemSchema = z.looseObject({
  created_at: z.string(),
  created_by: z.string(),
  knowledge_base_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  knowledge_base_search_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  query: z.string(),
  result: z.string(),
});

const ListKnowledgeBaseSearchesSchema = z.object({
  items: z.array(KnowledgeBaseSearchesItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

// =============================================================================
// Model Definition
// =============================================================================

/** Griptape Cloud Knowledge Bases — RAG knowledge bases, queries, searches, and index jobs */
export const model = {
  type: "@webframp/griptape/knowledge-bases",
  version: "2026.08.28.1",
  globalArguments: GlobalArgsSchema,

  upgrades: [],

  resources: {
    "knowledge_base_job": {
      description: "get operation",
      schema: GetKnowledgeBaseJobSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "knowledge_base_query": {
      description: "get operation",
      schema: GetKnowledgeBaseQuerySchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "knowledge_base_search": {
      description: "get operation",
      schema: GetKnowledgeBaseSearchSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "knowledge_bases": {
      description: "list operation",
      schema: ListKnowledgeBasesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "knowledge_base": {
      description: "create operation",
      schema: CreateKnowledgeBaseSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "knowledge_base_jobs": {
      description: "list operation",
      schema: ListKnowledgeBaseJobsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "knowledge_base_queries": {
      description: "list operation",
      schema: ListKnowledgeBaseQueriesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "knowledge_base_searches": {
      description: "list operation",
      schema: ListKnowledgeBaseSearchesSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
  },

  methods: {
    get_knowledge_base_job: {
      description: "get operation",
      arguments: z.object({
        knowledge_base_job_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "GET",
          `/api/knowledge-base-jobs/${
            encodeURIComponent(String(args.knowledge_base_job_id))
          }`,
          undefined,
          baseUrl,
        );

        const handle = await context.writeResource(
          "knowledge_base_job",
          sanitizeInstanceName(String(args.knowledge_base_job_id)),
          result,
        );
        context.logger.info("Fetched knowledge_base_job", {});
        return { dataHandles: [handle] };
      },
    },
    cancel_knowledge_base_job: {
      description: "action operation",
      arguments: z.object({
        knowledge_base_job_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["knowledge_base_job_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }
        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "POST",
          `/api/knowledge-base-jobs/${
            encodeURIComponent(String(args.knowledge_base_job_id))
          }/cancel`,
          body,
          baseUrl,
        );

        const handle = await context.writeResource(
          "knowledge_base_job",
          sanitizeInstanceName(String(args.knowledge_base_job_id)),
          result,
        );
        context.logger.info("Ran cancel_knowledge_base_job", {});
        return { dataHandles: [handle] };
      },
    },
    get_knowledge_base_query: {
      description: "get operation",
      arguments: z.object({
        knowledge_base_query_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "GET",
          `/api/knowledge-base-queries/${
            encodeURIComponent(String(args.knowledge_base_query_id))
          }`,
          undefined,
          baseUrl,
        );

        const handle = await context.writeResource(
          "knowledge_base_query",
          sanitizeInstanceName(String(args.knowledge_base_query_id)),
          result,
        );
        context.logger.info("Fetched knowledge_base_query", {});
        return { dataHandles: [handle] };
      },
    },
    get_knowledge_base_search: {
      description: "get operation",
      arguments: z.object({
        knowledge_base_search_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "GET",
          `/api/knowledge-base-searches/${
            encodeURIComponent(String(args.knowledge_base_search_id))
          }`,
          undefined,
          baseUrl,
        );

        const handle = await context.writeResource(
          "knowledge_base_search",
          sanitizeInstanceName(String(args.knowledge_base_search_id)),
          result,
        );
        context.logger.info("Fetched knowledge_base_search", {});
        return { dataHandles: [handle] };
      },
    },
    list_knowledge_bases: {
      description: "list operation",
      arguments: z.object({
        page: z.number().optional(),
        page_size: z.number().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const startMs = Date.now();
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "page_size"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await griptapeApiPaginated<
          Record<string, unknown>
        >(
          apiKey,
          `/api/knowledge-bases`,
          "knowledge_bases",
          params,
          baseUrl,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("knowledge_bases", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
          durationMs: Date.now() - startMs,
          collectedBy: EXTENSION_NAME,
        });

        context.logger.info("Found {count} knowledge_bases", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_knowledge_base: {
      description: "create operation",
      arguments: z.object({
        asset_paths: z.array(z.string()).optional(),
        config: z.unknown(),
        description: z.string().min(1).max(200).optional(),
        embedding_model: z.unknown().optional(),
        name: z.string().min(1).max(200),
        transforms: z.array(z.unknown()).optional(),
        type: z.string().min(1).max(200),
        use_default_embedding_model: z.boolean().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const body = args;

        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "POST",
          `/api/knowledge-bases`,
          body,
          baseUrl,
        );

        const record = result as Record<string, unknown>;
        const pathParamValues = new Set<string>([]);
        const idCandidates = ["knowledge_base_id", "id"];
        let rawId = idCandidates.map((k) => record[k]).find((v) =>
          v !== undefined && v !== null && v !== ""
        );
        if (rawId === undefined) {
          // No named id candidate matched. Scan for a single id-shaped key that is
          // not organization_id and whose value is not a path param (the parent
          // addressing context, e.g. bucket_id on an asset — would collide).
          for (const [k, v] of Object.entries(record)) {
            if (
              /(^|_)id$/.test(k) && k !== "organization_id" &&
              typeof v === "string" && v !== "" && !pathParamValues.has(v)
            ) {
              rawId = v;
              break;
            }
          }
        }
        if (
          rawId === undefined && typeof record.name === "string" &&
          record.name !== ""
        ) {
          rawId = record.name;
        }
        const fallbackId =
          [].filter((s: string) => s && s !== "undefined").join("_") ||
          "created";
        const id = sanitizeInstanceName(String(rawId ?? fallbackId));
        const handle = await context.writeResource(
          "knowledge_base",
          id,
          result,
        );
        context.logger.info("Created knowledge_base {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_knowledge_base: {
      description: "get operation",
      arguments: z.object({
        knowledge_base_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "GET",
          `/api/knowledge-bases/${
            encodeURIComponent(String(args.knowledge_base_id))
          }`,
          undefined,
          baseUrl,
        );

        const handle = await context.writeResource(
          "knowledge_base",
          sanitizeInstanceName(String(args.knowledge_base_id)),
          result,
        );
        context.logger.info("Fetched knowledge_base", {});
        return { dataHandles: [handle] };
      },
    },
    update_knowledge_base: {
      description: "update operation",
      arguments: z.object({
        knowledge_base_id: z.string(),
        asset_paths: z.array(z.string()).optional(),
        config: z.unknown(),
        description: z.string().min(1).max(200).optional(),
        embedding_model: z.unknown().optional(),
        name: z.string().min(1).max(200).optional(),
        schedule_expression: z.string().min(1).max(200).optional(),
        transforms: z.array(z.unknown()).optional(),
        type: z.string().min(1).max(200),
        use_default_embedding_model: z.boolean().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["knowledge_base_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "PATCH",
          `/api/knowledge-bases/${
            encodeURIComponent(String(args.knowledge_base_id))
          }`,
          body,
          baseUrl,
        );

        const handle = await context.writeResource(
          "knowledge_base",
          sanitizeInstanceName(String(args.knowledge_base_id)),
          result,
        );
        context.logger.info("Updated knowledge_base", {});
        return { dataHandles: [handle] };
      },
    },
    delete_knowledge_base: {
      description: "delete operation",
      arguments: z.object({
        knowledge_base_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        await griptapeApi<Record<string, unknown>>(
          apiKey,
          "DELETE",
          `/api/knowledge-bases/${
            encodeURIComponent(String(args.knowledge_base_id))
          }`,
          undefined,
          baseUrl,
        );

        context.logger.info("Deleted resource {id}", {
          id: args.knowledge_base_id,
        });
        return { dataHandles: [] };
      },
    },
    list_knowledge_base_jobs: {
      description: "list operation",
      arguments: z.object({
        knowledge_base_id: z.string(),
        page: z.number().optional(),
        page_size: z.number().optional(),
        status: z.string().optional().describe(
          "Comma-separated list of statuses to filter by.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const startMs = Date.now();
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["knowledge_base_id", "page", "page_size"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await griptapeApiPaginated<
          Record<string, unknown>
        >(
          apiKey,
          `/api/knowledge-bases/${
            encodeURIComponent(String(args.knowledge_base_id))
          }/knowledge-base-jobs`,
          "knowledge_base_jobs",
          params,
          baseUrl,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "knowledge_base_jobs",
          sanitizeInstanceName(String(args.knowledge_base_id)),
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );

        context.logger.info("Found {count} knowledge_base_jobs", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_knowledge_base_job: {
      description: "create operation",
      arguments: z.object({
        knowledge_base_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["knowledge_base_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "POST",
          `/api/knowledge-bases/${
            encodeURIComponent(String(args.knowledge_base_id))
          }/knowledge-base-jobs`,
          body,
          baseUrl,
        );

        const record = result as Record<string, unknown>;
        const pathParamValues = new Set<string>([
          String(args.knowledge_base_id),
        ]);
        const idCandidates = ["knowledge_base_job_id", "id"];
        let rawId = idCandidates.map((k) => record[k]).find((v) =>
          v !== undefined && v !== null && v !== ""
        );
        if (rawId === undefined) {
          // No named id candidate matched. Scan for a single id-shaped key that is
          // not organization_id and whose value is not a path param (the parent
          // addressing context, e.g. bucket_id on an asset — would collide).
          for (const [k, v] of Object.entries(record)) {
            if (
              /(^|_)id$/.test(k) && k !== "organization_id" &&
              typeof v === "string" && v !== "" && !pathParamValues.has(v)
            ) {
              rawId = v;
              break;
            }
          }
        }
        if (
          rawId === undefined && typeof record.name === "string" &&
          record.name !== ""
        ) {
          rawId = record.name;
        }
        const fallbackId =
          [String(args.knowledge_base_id)].filter((s: string) =>
            s && s !== "undefined"
          ).join("_") || "created";
        const id = sanitizeInstanceName(String(rawId ?? fallbackId));
        const handle = await context.writeResource(
          "knowledge_base_job",
          id,
          result,
        );
        context.logger.info("Created knowledge_base_job {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    list_knowledge_base_queries: {
      description: "list operation",
      arguments: z.object({
        knowledge_base_id: z.string(),
        page: z.number().optional(),
        page_size: z.number().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const startMs = Date.now();
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["knowledge_base_id", "page", "page_size"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await griptapeApiPaginated<
          Record<string, unknown>
        >(
          apiKey,
          `/api/knowledge-bases/${
            encodeURIComponent(String(args.knowledge_base_id))
          }/queries`,
          "knowledge_base_queries",
          params,
          baseUrl,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "knowledge_base_queries",
          sanitizeInstanceName(String(args.knowledge_base_id)),
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );

        context.logger.info("Found {count} knowledge_base_queries", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    query_knowledge_base: {
      description: "action operation",
      arguments: z.object({
        knowledge_base_id: z.string(),
        query: z.string().optional(),
        query_args: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["knowledge_base_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }
        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "POST",
          `/api/knowledge-bases/${
            encodeURIComponent(String(args.knowledge_base_id))
          }/query`,
          body,
          baseUrl,
        );

        const handle = await context.writeResource(
          "knowledge_base_query",
          sanitizeInstanceName(String(args.knowledge_base_id)),
          result,
        );
        context.logger.info("Ran query_knowledge_base", {});
        return { dataHandles: [handle] };
      },
    },
    search_knowledge_base: {
      description: "action operation",
      arguments: z.object({
        knowledge_base_id: z.string(),
        query: z.string(),
        query_args: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["knowledge_base_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }
        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "POST",
          `/api/knowledge-bases/${
            encodeURIComponent(String(args.knowledge_base_id))
          }/search`,
          body,
          baseUrl,
        );

        const handle = await context.writeResource(
          "knowledge_base_search",
          sanitizeInstanceName(String(args.knowledge_base_id)),
          result,
        );
        context.logger.info("Ran search_knowledge_base", {});
        return { dataHandles: [handle] };
      },
    },
    list_knowledge_base_searches: {
      description: "list operation",
      arguments: z.object({
        knowledge_base_id: z.string(),
        page: z.number().optional(),
        page_size: z.number().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const startMs = Date.now();
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["knowledge_base_id", "page", "page_size"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await griptapeApiPaginated<
          Record<string, unknown>
        >(
          apiKey,
          `/api/knowledge-bases/${
            encodeURIComponent(String(args.knowledge_base_id))
          }/searches`,
          "knowledge_base_searches",
          params,
          baseUrl,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "knowledge_base_searches",
          sanitizeInstanceName(String(args.knowledge_base_id)),
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );

        context.logger.info("Found {count} knowledge_base_searches", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
  },
};
