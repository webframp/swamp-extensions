// Griptape Cloud Secrets — organization secret management — Tests
// Auto-generated by scripts/griptape-codegen — do not edit manually.
// SPDX-License-Identifier: Apache-2.0

import { assertEquals, assertExists } from "jsr:@std/assert@1.0.19";
import { createModelTestContext } from "@systeminit/swamp-testing";
import { model } from "./secrets.ts";

// ---------------------------------------------------------------------------
// Model Export Structure Tests
// ---------------------------------------------------------------------------

Deno.test("secrets model: has correct type", () => {
  assertEquals(model.type, "@webframp/griptape/secrets");
});

Deno.test("secrets model: has valid version format", () => {
  const versionPattern = /^\d{4}\.\d{2}\.\d{2}\.\d+$/;
  assertEquals(versionPattern.test(model.version), true);
});

Deno.test("secrets model: has globalArguments with apiKey", () => {
  assertExists(model.globalArguments);
  const shape = model.globalArguments.shape;
  assertExists(shape.apiKey);
});

Deno.test("secrets model: has expected methods", () => {
  assertExists(model.methods);
  assertExists(model.methods.list_secrets);
  assertExists(model.methods.create_secret);
  assertExists(model.methods.get_secret);
  assertExists(model.methods.update_secret);
  assertExists(model.methods.delete_secret);
});

Deno.test("secrets model: has expected resources", () => {
  assertExists(model.resources);
  assertExists(model.resources["secrets"]);
  assertExists(model.resources["secret"]);
});

// ---------------------------------------------------------------------------
// Mock Griptape API Server
// ---------------------------------------------------------------------------

interface MockResponse {
  body: unknown;
  itemsKey?: string;
  status?: number;
}

function startMockGtServer(
  responses: Record<string, MockResponse>,
): { url: string; server: Deno.HttpServer } {
  const server = Deno.serve({ port: 0, onListen() {} }, (req) => {
    const url = new URL(req.url);
    const path = url.pathname;

    for (const [pattern, spec] of Object.entries(responses)) {
      if (path.includes(pattern)) {
        const status = spec.status ?? 200;
        // 204/205 must have a null body.
        if (status === 204 || status === 205) {
          return new Response(null, { status });
        }
        if (spec.itemsKey) {
          return Response.json({
            [spec.itemsKey]: spec.body,
            pagination: {
              page_number: 1,
              page_size: 50,
              total_count: Array.isArray(spec.body) ? spec.body.length : 0,
              total_pages: 1,
            },
          }, { status });
        }
        return Response.json(spec.body, { status });
      }
    }

    return Response.json({ message: "Not found" }, { status: 404 });
  });

  const addr = server.addr as Deno.NetAddr;
  return { url: `http://localhost:${addr.port}`, server };
}

function installFetchMock(mockUrl: string): () => void {
  const originalFetch = globalThis.fetch;
  globalThis.fetch = (input, init) => {
    const reqUrl = typeof input === "string" ? input : input.toString();
    const newUrl = reqUrl.replace("https://cloud.griptape.ai/api", mockUrl);
    return originalFetch(newUrl, init);
  };
  return () => {
    globalThis.fetch = originalFetch;
  };
}

Deno.test({
  name: "secrets model: list_secrets fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockGtServer({
      "/api/secrets": {
        body: [{
          "created_at": "2024-01-01T00:00:00Z",
          "last_used": "2024-01-01T00:00:00Z",
          "name": "test-value",
          "organization_id": "test-value",
          "secret_id": "test-value",
          "updated_at": "2024-01-01T00:00:00Z",
        }],
        itemsKey: "secrets",
      },
    });
    const uninstall = installFetchMock(url);
    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiKey": "test-key" },
        definition: {
          id: "test-id",
          name: "test-secrets",
          version: 1,
          tags: {},
        },
      });
      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).list_secrets.execute({}, context);
      assertEquals(result.dataHandles.length, 1);
      assertEquals(getWrittenResources().length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "secrets model: create_secret creates and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockGtServer({
      "/api/secrets": {
        body: {
          "secret_id": "test-value",
          "created_at": "2024-01-01T00:00:00Z",
          "last_used": "2024-01-01T00:00:00Z",
          "name": "test-value",
          "organization_id": "test-value",
          "updated_at": "2024-01-01T00:00:00Z",
        },
      },
    });
    const uninstall = installFetchMock(url);
    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiKey": "test-key" },
        definition: {
          id: "test-id",
          name: "test-secrets",
          version: 1,
          tags: {},
        },
      });
      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).create_secret.execute({
        "created_at": "2024-01-01T00:00:00Z",
        "last_used": "2024-01-01T00:00:00Z",
        "name": "test-value",
        "organization_id": "test-value",
        "secret_id": "test-value",
        "updated_at": "2024-01-01T00:00:00Z",
      }, context);
      assertEquals(result.dataHandles.length, 1);
      assertEquals(getWrittenResources().length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "secrets model: get_secret fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockGtServer({
      "/api/secrets/test-id-123": {
        body: {
          "created_at": "2024-01-01T00:00:00Z",
          "last_used": "2024-01-01T00:00:00Z",
          "name": "test-value",
          "organization_id": "test-value",
          "secret_id": "test-value",
          "updated_at": "2024-01-01T00:00:00Z",
        },
      },
    });
    const uninstall = installFetchMock(url);
    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiKey": "test-key" },
        definition: {
          id: "test-id",
          name: "test-secrets",
          version: 1,
          tags: {},
        },
      });
      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).get_secret.execute({ "secret_id": "test-id-123" }, context);
      assertEquals(result.dataHandles.length, 1);
      assertEquals(getWrittenResources().length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "secrets model: update_secret executes and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockGtServer({
      "/api/secrets/test-id-123": {
        body: {
          "created_at": "2024-01-01T00:00:00Z",
          "last_used": "2024-01-01T00:00:00Z",
          "name": "test-value",
          "organization_id": "test-value",
          "secret_id": "test-value",
          "updated_at": "2024-01-01T00:00:00Z",
        },
      },
    });
    const uninstall = installFetchMock(url);
    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiKey": "test-key" },
        definition: {
          id: "test-id",
          name: "test-secrets",
          version: 1,
          tags: {},
        },
      });
      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).update_secret.execute({
        "created_at": "2024-01-01T00:00:00Z",
        "last_used": "2024-01-01T00:00:00Z",
        "name": "test-value",
        "organization_id": "test-value",
        "secret_id": "test-id-123",
        "updated_at": "2024-01-01T00:00:00Z",
      }, context);
      assertEquals(result.dataHandles.length, 1);
      assertEquals(getWrittenResources().length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "secrets model: delete_secret executes successfully",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockGtServer({
      "/api/secrets/test-id-123": { body: {}, status: 204 },
    });
    const uninstall = installFetchMock(url);
    try {
      const { context } = createModelTestContext({
        globalArgs: { "apiKey": "test-key" },
        definition: {
          id: "test-id",
          name: "test-secrets",
          version: 1,
          tags: {},
        },
      });
      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).delete_secret.execute({ "secret_id": "test-id-123" }, context);
      assertEquals(result.dataHandles.length, 0);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});
