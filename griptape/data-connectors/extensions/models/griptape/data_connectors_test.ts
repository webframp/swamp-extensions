// Griptape Cloud Data Connectors — data source connectors and ingest jobs — Tests
// Auto-generated by scripts/griptape-codegen — do not edit manually.
// SPDX-License-Identifier: Apache-2.0

import { assertEquals, assertExists } from "jsr:@std/assert@1.0.19";
import { createModelTestContext } from "@systeminit/swamp-testing";
import { model } from "./data_connectors.ts";

// ---------------------------------------------------------------------------
// Model Export Structure Tests
// ---------------------------------------------------------------------------

Deno.test("data-connectors model: has correct type", () => {
  assertEquals(model.type, "@webframp/griptape/data-connectors");
});

Deno.test("data-connectors model: has valid version format", () => {
  const versionPattern = /^\d{4}\.\d{2}\.\d{2}\.\d+$/;
  assertEquals(versionPattern.test(model.version), true);
});

Deno.test("data-connectors model: has globalArguments with apiKey", () => {
  assertExists(model.globalArguments);
  const shape = model.globalArguments.shape;
  assertExists(shape.apiKey);
});

Deno.test("data-connectors model: has expected methods", () => {
  assertExists(model.methods);
  assertExists(model.methods.list_data_connectors);
  assertExists(model.methods.create_data_connector);
  assertExists(model.methods.get_data_connector);
  assertExists(model.methods.update_data_connector);
  assertExists(model.methods.delete_data_connector);
  assertExists(model.methods.list_data_jobs);
  assertExists(model.methods.create_data_job);
  assertExists(model.methods.get_data_job);
  assertExists(model.methods.cancel_data_job);
});

Deno.test("data-connectors model: has expected resources", () => {
  assertExists(model.resources);
  assertExists(model.resources["data_connectors"]);
  assertExists(model.resources["data_connector"]);
  assertExists(model.resources["data_jobs"]);
  assertExists(model.resources["data_job"]);
});

// ---------------------------------------------------------------------------
// Mock Griptape API Server
// ---------------------------------------------------------------------------

interface MockResponse {
  body: unknown;
  itemsKey?: string;
  status?: number;
}

function startMockGtServer(
  responses: Record<string, MockResponse>,
): { url: string; server: Deno.HttpServer } {
  const server = Deno.serve({ port: 0, onListen() {} }, (req) => {
    const url = new URL(req.url);
    const path = url.pathname;

    for (const [pattern, spec] of Object.entries(responses)) {
      if (path.includes(pattern)) {
        const status = spec.status ?? 200;
        // 204/205 must have a null body.
        if (status === 204 || status === 205) {
          return new Response(null, { status });
        }
        if (spec.itemsKey) {
          return Response.json({
            [spec.itemsKey]: spec.body,
            pagination: {
              page_number: 1,
              page_size: 50,
              total_count: Array.isArray(spec.body) ? spec.body.length : 0,
              total_pages: 1,
            },
          }, { status });
        }
        return Response.json(spec.body, { status });
      }
    }

    return Response.json({ message: "Not found" }, { status: 404 });
  });

  const addr = server.addr as Deno.NetAddr;
  return { url: `http://localhost:${addr.port}`, server };
}

function installFetchMock(mockUrl: string): () => void {
  const originalFetch = globalThis.fetch;
  globalThis.fetch = (input, init) => {
    const reqUrl = typeof input === "string" ? input : input.toString();
    const newUrl = reqUrl.replace("https://cloud.griptape.ai/api", mockUrl);
    return originalFetch(newUrl, init);
  };
  return () => {
    globalThis.fetch = originalFetch;
  };
}

Deno.test({
  name:
    "data-connectors model: list_data_connectors fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockGtServer({
      "/api/data-connectors": {
        body: [{
          "bucket_id": "test-value",
          "config": null,
          "created_at": "2024-01-01T00:00:00Z",
          "created_by": "test-value",
          "data_connector_id": "test-value",
          "description": "test-value",
          "name": "test-value",
          "organization_id": "test-value",
          "schedule_expression": "test-value",
          "transforms": [null],
          "type": "test-value",
          "updated_at": "2024-01-01T00:00:00Z",
        }],
        itemsKey: "data_connectors",
      },
    });
    const uninstall = installFetchMock(url);
    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiKey": "test-key" },
        definition: {
          id: "test-id",
          name: "test-data-connectors",
          version: 1,
          tags: {},
        },
      });
      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).list_data_connectors.execute({}, context);
      assertEquals(result.dataHandles.length, 1);
      assertEquals(getWrittenResources().length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name:
    "data-connectors model: create_data_connector creates and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockGtServer({
      "/api/data-connectors": {
        body: {
          "data_connector_id": "test-value",
          "config": null,
          "created_at": "2024-01-01T00:00:00Z",
          "created_by": "test-value",
          "data_job_id": "test-value",
          "description": "test-value",
          "name": "test-value",
          "schedule_expression": "test-value",
          "type": "test-value",
          "updated_at": "2024-01-01T00:00:00Z",
        },
      },
    });
    const uninstall = installFetchMock(url);
    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiKey": "test-key" },
        definition: {
          id: "test-id",
          name: "test-data-connectors",
          version: 1,
          tags: {},
        },
      });
      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).create_data_connector.execute({
        "config": null,
        "created_at": "2024-01-01T00:00:00Z",
        "created_by": "test-value",
        "data_connector_id": "test-value",
        "data_job_id": "test-value",
        "description": "test-value",
        "name": "test-value",
        "schedule_expression": "test-value",
        "type": "test-value",
        "updated_at": "2024-01-01T00:00:00Z",
      }, context);
      assertEquals(result.dataHandles.length, 1);
      assertEquals(getWrittenResources().length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "data-connectors model: get_data_connector fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockGtServer({
      "/api/data-connectors/test-id-123": {
        body: {
          "bucket_id": "test-value",
          "config": null,
          "created_at": "2024-01-01T00:00:00Z",
          "created_by": "test-value",
          "data_connector_id": "test-value",
          "description": "test-value",
          "name": "test-value",
          "organization_id": "test-value",
          "schedule_expression": "test-value",
          "transforms": [null],
          "type": "test-value",
          "updated_at": "2024-01-01T00:00:00Z",
        },
      },
    });
    const uninstall = installFetchMock(url);
    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiKey": "test-key" },
        definition: {
          id: "test-id",
          name: "test-data-connectors",
          version: 1,
          tags: {},
        },
      });
      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).get_data_connector.execute(
        { "data_connector_id": "test-id-123" },
        context,
      );
      assertEquals(result.dataHandles.length, 1);
      assertEquals(getWrittenResources().length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name:
    "data-connectors model: update_data_connector executes and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockGtServer({
      "/api/data-connectors/test-id-123": {
        body: {
          "config": null,
          "created_at": "2024-01-01T00:00:00Z",
          "created_by": "test-value",
          "data_connector_id": "test-value",
          "data_job_id": "test-value",
          "description": "test-value",
          "name": "test-value",
          "organization_id": "test-value",
          "schedule_expression": "test-value",
          "transforms": [null],
          "type": "test-value",
          "updated_at": "2024-01-01T00:00:00Z",
        },
      },
    });
    const uninstall = installFetchMock(url);
    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiKey": "test-key" },
        definition: {
          id: "test-id",
          name: "test-data-connectors",
          version: 1,
          tags: {},
        },
      });
      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).update_data_connector.execute({
        "config": null,
        "created_at": "2024-01-01T00:00:00Z",
        "created_by": "test-value",
        "data_connector_id": "test-id-123",
        "data_job_id": "test-value",
        "description": "test-value",
        "name": "test-value",
        "organization_id": "test-value",
        "schedule_expression": "test-value",
        "transforms": [null],
        "type": "test-value",
        "updated_at": "2024-01-01T00:00:00Z",
      }, context);
      assertEquals(result.dataHandles.length, 1);
      assertEquals(getWrittenResources().length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "data-connectors model: delete_data_connector executes successfully",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockGtServer({
      "/api/data-connectors/test-id-123": { body: {}, status: 204 },
    });
    const uninstall = installFetchMock(url);
    try {
      const { context } = createModelTestContext({
        globalArgs: { "apiKey": "test-key" },
        definition: {
          id: "test-id",
          name: "test-data-connectors",
          version: 1,
          tags: {},
        },
      });
      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).delete_data_connector.execute(
        { "data_connector_id": "test-id-123" },
        context,
      );
      assertEquals(result.dataHandles.length, 0);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "data-connectors model: cancel_data_job executes and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockGtServer({
      "/api/data-jobs/test-id-123/cancel": {
        body: {
          "bytes_ingested": 1,
          "completed_at": "2024-01-01T00:00:00Z",
          "created_at": "2024-01-01T00:00:00Z",
          "created_by": "test-value",
          "data_connector_id": "test-value",
          "data_job_id": "test-value",
          "errors": [null],
          "status": null,
          "status_detail": null,
        },
      },
    });
    const uninstall = installFetchMock(url);
    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiKey": "test-key" },
        definition: {
          id: "test-id",
          name: "test-data-connectors",
          version: 1,
          tags: {},
        },
      });
      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).cancel_data_job.execute({
        "bytes_ingested": 1,
        "completed_at": "2024-01-01T00:00:00Z",
        "created_at": "2024-01-01T00:00:00Z",
        "created_by": "test-value",
        "data_connector_id": "test-value",
        "data_job_id": "test-id-123",
        "errors": [null],
        "status": null,
        "status_detail": null,
      }, context);
      assertEquals(result.dataHandles.length, 1);
      assertEquals(getWrittenResources().length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});
