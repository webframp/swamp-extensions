/**
 * Griptape Cloud Data Connectors — data source connectors and ingest jobs
 *
 * Auto-generated by scripts/griptape-codegen — do not edit manually.
 *
 * @module
 */
// SPDX-License-Identifier: Apache-2.0

import { z } from "npm:zod@4.4.3";
import {
  griptapeApi,
  griptapeApiPaginated,
  sanitizeInstanceName,
} from "./_lib/api.ts";

const EXTENSION_NAME = "@webframp/griptape/data-connectors";

// =============================================================================
// Schemas
// =============================================================================

const GlobalArgsSchema = z.object({
  apiKey: z.string().meta({ sensitive: true }).describe(
    "Griptape Cloud API key; overrides the GT_CLOUD_API_KEY environment variable. Wire with a vault.get(...) expression to source it from a vault.",
  ).optional(),
  baseUrl: z.string().describe(
    "Griptape Cloud API base URL; overrides the GT_CLOUD_BASE_URL environment variable and the built-in default.",
  ).optional(),
});

const DataConnectorsItemSchema = z.looseObject({
  bucket_id: z.string().optional(),
  config: z.unknown(),
  created_at: z.string(),
  created_by: z.string(),
  data_connector_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  description: z.string().optional(),
  name: z.string(),
  organization_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  schedule_expression: z.string().optional(),
  transforms: z.array(z.unknown()).optional(),
  type: z.string(),
  updated_at: z.string(),
});

const ListDataConnectorsSchema = z.object({
  items: z.array(DataConnectorsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const CreateDataConnectorSchema = z.looseObject({
  config: z.unknown(),
  created_at: z.string(),
  created_by: z.string(),
  data_connector_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  data_job_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ).optional(),
  description: z.string().optional(),
  name: z.string(),
  schedule_expression: z.string().optional(),
  type: z.string(),
  updated_at: z.string(),
});

const DataJobsItemSchema = z.looseObject({
  bytes_ingested: z.number().optional(),
  completed_at: z.string().nullable().optional().default(null),
  created_at: z.string(),
  created_by: z.string(),
  data_connector_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  data_job_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  errors: z.array(z.unknown()).optional(),
  status: z.unknown(),
  status_detail: z.unknown().optional(),
});

const ListDataJobsSchema = z.object({
  items: z.array(DataJobsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const CreateDataJobSchema = z.looseObject({
  bytes_ingested: z.number().optional(),
  completed_at: z.string().nullable().optional().default(null),
  created_at: z.string(),
  created_by: z.string(),
  data_connector_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  data_job_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  errors: z.array(z.unknown()).optional(),
  status: z.unknown(),
  status_detail: z.unknown().optional(),
});

// =============================================================================
// Model Definition
// =============================================================================

/** Griptape Cloud Data Connectors — data source connectors and ingest jobs */
export const model = {
  type: "@webframp/griptape/data-connectors",
  version: "2026.08.28.1",
  globalArguments: GlobalArgsSchema,

  upgrades: [],

  resources: {
    "data_connectors": {
      description: "list operation",
      schema: ListDataConnectorsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "data_connector": {
      description: "create operation",
      schema: CreateDataConnectorSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "data_jobs": {
      description: "list operation",
      schema: ListDataJobsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "data_job": {
      description: "create operation",
      schema: CreateDataJobSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
  },

  methods: {
    list_data_connectors: {
      description: "list operation",
      arguments: z.object({
        page: z.number().optional(),
        page_size: z.number().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const startMs = Date.now();
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "page_size"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await griptapeApiPaginated<
          Record<string, unknown>
        >(
          apiKey,
          `/api/data-connectors`,
          "data_connectors",
          params,
          baseUrl,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("data_connectors", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
          durationMs: Date.now() - startMs,
          collectedBy: EXTENSION_NAME,
        });

        context.logger.info("Found {count} data_connectors", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_data_connector: {
      description: "create operation",
      arguments: z.object({
        config: z.unknown(),
        description: z.string().min(1).max(200).optional(),
        name: z.string().min(1).max(200),
        schedule_expression: z.string().min(1).max(200).optional(),
        transforms: z.array(z.unknown()).optional(),
        type: z.string().min(1).max(200),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const body = args;

        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "POST",
          `/api/data-connectors`,
          body,
          baseUrl,
        );

        const record = result as Record<string, unknown>;
        const pathParamValues = new Set<string>([]);
        const idCandidates = ["data_connector_id", "id"];
        let rawId = idCandidates.map((k) => record[k]).find((v) =>
          v !== undefined && v !== null && v !== ""
        );
        if (rawId === undefined) {
          // No named id candidate matched. Scan for a single id-shaped key that is
          // not organization_id and whose value is not a path param (the parent
          // addressing context, e.g. bucket_id on an asset — would collide).
          for (const [k, v] of Object.entries(record)) {
            if (
              /(^|_)id$/.test(k) && k !== "organization_id" &&
              typeof v === "string" && v !== "" && !pathParamValues.has(v)
            ) {
              rawId = v;
              break;
            }
          }
        }
        if (
          rawId === undefined && typeof record.name === "string" &&
          record.name !== ""
        ) {
          rawId = record.name;
        }
        const fallbackId =
          [].filter((s: string) => s && s !== "undefined").join("_") ||
          "created";
        const id = sanitizeInstanceName(String(rawId ?? fallbackId));
        const handle = await context.writeResource(
          "data_connector",
          id,
          result,
        );
        context.logger.info("Created data_connector {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_data_connector: {
      description: "get operation",
      arguments: z.object({
        data_connector_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "GET",
          `/api/data-connectors/${
            encodeURIComponent(String(args.data_connector_id))
          }`,
          undefined,
          baseUrl,
        );

        const handle = await context.writeResource(
          "data_connector",
          sanitizeInstanceName(String(args.data_connector_id)),
          result,
        );
        context.logger.info("Fetched data_connector", {});
        return { dataHandles: [handle] };
      },
    },
    update_data_connector: {
      description: "update operation",
      arguments: z.object({
        data_connector_id: z.string(),
        config: z.unknown().optional(),
        description: z.string().min(1).max(200).optional(),
        name: z.string().min(1).max(200).optional(),
        schedule_expression: z.string().min(1).max(200).optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["data_connector_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "PATCH",
          `/api/data-connectors/${
            encodeURIComponent(String(args.data_connector_id))
          }`,
          body,
          baseUrl,
        );

        const handle = await context.writeResource(
          "data_connector",
          sanitizeInstanceName(String(args.data_connector_id)),
          result,
        );
        context.logger.info("Updated data_connector", {});
        return { dataHandles: [handle] };
      },
    },
    delete_data_connector: {
      description: "delete operation",
      arguments: z.object({
        data_connector_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        await griptapeApi<Record<string, unknown>>(
          apiKey,
          "DELETE",
          `/api/data-connectors/${
            encodeURIComponent(String(args.data_connector_id))
          }`,
          undefined,
          baseUrl,
        );

        context.logger.info("Deleted resource {id}", {
          id: args.data_connector_id,
        });
        return { dataHandles: [] };
      },
    },
    list_data_jobs: {
      description: "list operation",
      arguments: z.object({
        data_connector_id: z.string(),
        page: z.number().optional(),
        page_size: z.number().optional(),
        status: z.string().optional().describe(
          "Comma-separated list of statuses to filter by.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const startMs = Date.now();
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["data_connector_id", "page", "page_size"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await griptapeApiPaginated<
          Record<string, unknown>
        >(
          apiKey,
          `/api/data-connectors/${
            encodeURIComponent(String(args.data_connector_id))
          }/data-jobs`,
          "data_jobs",
          params,
          baseUrl,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource(
          "data_jobs",
          sanitizeInstanceName(String(args.data_connector_id)),
          {
            items: results,
            truncated,
            fetchedAt: new Date().toISOString(),
            durationMs: Date.now() - startMs,
            collectedBy: EXTENSION_NAME,
          },
        );

        context.logger.info("Found {count} data_jobs", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_data_job: {
      description: "create operation",
      arguments: z.object({
        data_connector_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["data_connector_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "POST",
          `/api/data-connectors/${
            encodeURIComponent(String(args.data_connector_id))
          }/data-jobs`,
          body,
          baseUrl,
        );

        const record = result as Record<string, unknown>;
        const pathParamValues = new Set<string>([
          String(args.data_connector_id),
        ]);
        const idCandidates = ["data_job_id", "id"];
        let rawId = idCandidates.map((k) => record[k]).find((v) =>
          v !== undefined && v !== null && v !== ""
        );
        if (rawId === undefined) {
          // No named id candidate matched. Scan for a single id-shaped key that is
          // not organization_id and whose value is not a path param (the parent
          // addressing context, e.g. bucket_id on an asset — would collide).
          for (const [k, v] of Object.entries(record)) {
            if (
              /(^|_)id$/.test(k) && k !== "organization_id" &&
              typeof v === "string" && v !== "" && !pathParamValues.has(v)
            ) {
              rawId = v;
              break;
            }
          }
        }
        if (
          rawId === undefined && typeof record.name === "string" &&
          record.name !== ""
        ) {
          rawId = record.name;
        }
        const fallbackId =
          [String(args.data_connector_id)].filter((s: string) =>
            s && s !== "undefined"
          ).join("_") || "created";
        const id = sanitizeInstanceName(String(rawId ?? fallbackId));
        const handle = await context.writeResource("data_job", id, result);
        context.logger.info("Created data_job {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_data_job: {
      description: "get operation",
      arguments: z.object({
        data_job_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "GET",
          `/api/data-jobs/${encodeURIComponent(String(args.data_job_id))}`,
          undefined,
          baseUrl,
        );

        const handle = await context.writeResource(
          "data_job",
          sanitizeInstanceName(String(args.data_job_id)),
          result,
        );
        context.logger.info("Fetched data_job", {});
        return { dataHandles: [handle] };
      },
    },
    cancel_data_job: {
      description: "action operation",
      arguments: z.object({
        data_job_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["data_job_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }
        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "POST",
          `/api/data-jobs/${
            encodeURIComponent(String(args.data_job_id))
          }/cancel`,
          body,
          baseUrl,
        );

        const handle = await context.writeResource(
          "data_job",
          sanitizeInstanceName(String(args.data_job_id)),
          result,
        );
        context.logger.info("Ran cancel_data_job", {});
        return { dataHandles: [handle] };
      },
    },
  },
};
