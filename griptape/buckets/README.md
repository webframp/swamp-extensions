# @webframp/griptape/buckets

Griptape Cloud Buckets — asset buckets and their stored assets

Auto-generated by `scripts/griptape-codegen` from the Griptape Cloud OpenAPI
specification.

## Usage

```bash
swamp extension pull @webframp/griptape/buckets

swamp model create @webframp/griptape/buckets gt-buckets \
  --global-arg apiKey=GRIPTAPE_CLOUD_API_KEY
```

The `apiKey` global argument overrides the `GT_CLOUD_API_KEY` environment
variable and is wireable with a `vault.get(...)` expression. An optional
`baseUrl` argument overrides `GT_CLOUD_BASE_URL` and the built-in default
(`https://cloud.griptape.ai/api`).

## Methods

| Method             | Description      |
| ------------------ | ---------------- |
| `list_buckets`     | list operation   |
| `create_bucket`    | create operation |
| `get_bucket`       | get operation    |
| `update_bucket`    | update operation |
| `delete_bucket`    | delete operation |
| `create_asset_url` | create operation |
| `list_assets`      | list operation   |
| `create_asset`     | create operation |
| `get_asset`        | get operation    |
| `delete_asset`     | delete operation |

## Development

```bash
cd ../../griptape/buckets
deno task check
deno task lint
deno task fmt
deno task test
```

## License

Apache-2.0 — see [LICENSE.md](LICENSE.md).
