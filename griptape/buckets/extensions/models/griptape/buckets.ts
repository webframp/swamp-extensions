/**
 * Griptape Cloud Buckets — asset buckets and their stored assets
 *
 * Auto-generated by scripts/griptape-codegen — do not edit manually.
 *
 * @module
 */
// SPDX-License-Identifier: Apache-2.0

import { z } from "npm:zod@4.4.3";
import {
  griptapeApi,
  griptapeApiPaginated,
  sanitizeInstanceName,
} from "./_lib/api.ts";

const EXTENSION_NAME = "@webframp/griptape/buckets";

// =============================================================================
// Schemas
// =============================================================================

const GlobalArgsSchema = z.object({
  apiKey: z.string().meta({ sensitive: true }).describe(
    "Griptape Cloud API key; overrides the GT_CLOUD_API_KEY environment variable. Wire with a vault.get(...) expression to source it from a vault.",
  ).optional(),
  baseUrl: z.string().describe(
    "Griptape Cloud API base URL; overrides the GT_CLOUD_BASE_URL environment variable and the built-in default.",
  ).optional(),
});

const BucketsItemSchema = z.looseObject({
  bucket_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  created_at: z.string(),
  created_by: z.string(),
  name: z.string(),
  organization_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  updated_at: z.string(),
});

const ListBucketsSchema = z.object({
  items: z.array(BucketsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const CreateBucketSchema = z.looseObject({
  bucket_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  created_at: z.string(),
  created_by: z.string(),
  name: z.string(),
  organization_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  updated_at: z.string(),
});

const CreateAssetUrlSchema = z.looseObject({
  headers: z.unknown(),
  url: z.string(),
});

const AssetsItemSchema = z.looseObject({
  bucket_id: z.string().optional(),
  contents: z.unknown().optional(),
  created_at: z.string(),
  created_by: z.string(),
  name: z.string(),
  organization_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  size: z.number().optional(),
  updated_at: z.string(),
});

const ListAssetsSchema = z.object({
  items: z.array(AssetsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const CreateAssetSchema = z.looseObject({
  bucket_id: z.string().optional(),
  contents: z.unknown().optional(),
  created_at: z.string(),
  created_by: z.string(),
  name: z.string(),
  organization_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  size: z.number().optional(),
  updated_at: z.string(),
});

// =============================================================================
// Model Definition
// =============================================================================

/** Griptape Cloud Buckets — asset buckets and their stored assets */
export const model = {
  type: "@webframp/griptape/buckets",
  version: "2026.08.28.1",
  globalArguments: GlobalArgsSchema,

  upgrades: [],

  resources: {
    "buckets": {
      description: "list operation",
      schema: ListBucketsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "bucket": {
      description: "create operation",
      schema: CreateBucketSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "asset_url": {
      description: "create operation",
      schema: CreateAssetUrlSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "assets": {
      description: "list operation",
      schema: ListAssetsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "asset": {
      description: "create operation",
      schema: CreateAssetSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
  },

  methods: {
    list_buckets: {
      description: "list operation",
      arguments: z.object({
        page: z.number().optional(),
        page_size: z.number().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const startMs = Date.now();
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "page_size"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await griptapeApiPaginated<
          Record<string, unknown>
        >(
          apiKey,
          `/api/buckets`,
          "buckets",
          params,
          baseUrl,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("buckets", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
          durationMs: Date.now() - startMs,
          collectedBy: EXTENSION_NAME,
        });

        context.logger.info("Found {count} buckets", { count: results.length });
        return { dataHandles: [handle] };
      },
    },
    create_bucket: {
      description: "create operation",
      arguments: z.object({
        name: z.string().min(1).max(256),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const body = args;

        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "POST",
          `/api/buckets`,
          body,
          baseUrl,
        );

        const record = result as Record<string, unknown>;
        const pathParamValues = new Set<string>([]);
        const idCandidates = ["bucket_id", "id"];
        let rawId = idCandidates.map((k) => record[k]).find((v) =>
          v !== undefined && v !== null && v !== ""
        );
        if (rawId === undefined) {
          // No named id candidate matched. Scan for a single id-shaped key that is
          // not organization_id and whose value is not a path param (the parent
          // addressing context, e.g. bucket_id on an asset — would collide).
          for (const [k, v] of Object.entries(record)) {
            if (
              /(^|_)id$/.test(k) && k !== "organization_id" &&
              typeof v === "string" && v !== "" && !pathParamValues.has(v)
            ) {
              rawId = v;
              break;
            }
          }
        }
        if (
          rawId === undefined && typeof record.name === "string" &&
          record.name !== ""
        ) {
          rawId = record.name;
        }
        const fallbackId =
          [].filter((s: string) => s && s !== "undefined").join("_") ||
          "created";
        const id = sanitizeInstanceName(String(rawId ?? fallbackId));
        const handle = await context.writeResource("bucket", id, result);
        context.logger.info("Created bucket {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_bucket: {
      description: "get operation",
      arguments: z.object({
        bucket_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "GET",
          `/api/buckets/${encodeURIComponent(String(args.bucket_id))}`,
          undefined,
          baseUrl,
        );

        const handle = await context.writeResource(
          "bucket",
          sanitizeInstanceName(String(args.bucket_id)),
          result,
        );
        context.logger.info("Fetched bucket", {});
        return { dataHandles: [handle] };
      },
    },
    update_bucket: {
      description: "update operation",
      arguments: z.object({
        bucket_id: z.string(),
        name: z.string().min(1).max(256).optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["bucket_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "PATCH",
          `/api/buckets/${encodeURIComponent(String(args.bucket_id))}`,
          body,
          baseUrl,
        );

        const handle = await context.writeResource(
          "bucket",
          sanitizeInstanceName(String(args.bucket_id)),
          result,
        );
        context.logger.info("Updated bucket", {});
        return { dataHandles: [handle] };
      },
    },
    delete_bucket: {
      description: "delete operation",
      arguments: z.object({
        bucket_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        await griptapeApi<Record<string, unknown>>(
          apiKey,
          "DELETE",
          `/api/buckets/${encodeURIComponent(String(args.bucket_id))}`,
          undefined,
          baseUrl,
        );

        context.logger.info("Deleted resource {id}", { id: args.bucket_id });
        return { dataHandles: [] };
      },
    },
    create_asset_url: {
      description: "create operation",
      arguments: z.object({
        name: z.string(),
        bucket_id: z.string(),
        content_type: z.string().min(1).max(255).regex(
          new RegExp("^[A-Za-z0-9!#$&^_.+-]+/[A-Za-z0-9!#$&^_.+-]+$"),
        ).optional().describe(
          "Optional MIME type to apply to the presigned URL. When provided, overrides the type guessed from ...",
        ),
        operation: z.unknown().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["name", "bucket_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "POST",
          `/api/buckets/${
            encodeURIComponent(String(args.bucket_id))
          }/asset-urls/${encodeURIComponent(String(args.name))}`,
          body,
          baseUrl,
        );

        const record = result as Record<string, unknown>;
        const pathParamValues = new Set<string>([
          String(args.name),
          String(args.bucket_id),
        ]);
        const idCandidates = ["asset_url_id", "id"];
        let rawId = idCandidates.map((k) => record[k]).find((v) =>
          v !== undefined && v !== null && v !== ""
        );
        if (rawId === undefined) {
          // No named id candidate matched. Scan for a single id-shaped key that is
          // not organization_id and whose value is not a path param (the parent
          // addressing context, e.g. bucket_id on an asset — would collide).
          for (const [k, v] of Object.entries(record)) {
            if (
              /(^|_)id$/.test(k) && k !== "organization_id" &&
              typeof v === "string" && v !== "" && !pathParamValues.has(v)
            ) {
              rawId = v;
              break;
            }
          }
        }
        if (
          rawId === undefined && typeof record.name === "string" &&
          record.name !== ""
        ) {
          rawId = record.name;
        }
        const fallbackId =
          [String(args.name), String(args.bucket_id)].filter((s: string) =>
            s && s !== "undefined"
          ).join("_") || "created";
        const id = sanitizeInstanceName(String(rawId ?? fallbackId));
        const handle = await context.writeResource("asset_url", id, result);
        context.logger.info("Created asset_url {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    list_assets: {
      description: "list operation",
      arguments: z.object({
        bucket_id: z.string(),
        page: z.number().optional(),
        page_size: z.number().optional(),
        prefix: z.string().optional(),
        postfix: z.string().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const startMs = Date.now();
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["bucket_id", "page", "page_size"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await griptapeApiPaginated<
          Record<string, unknown>
        >(
          apiKey,
          `/api/buckets/${encodeURIComponent(String(args.bucket_id))}/assets`,
          "assets",
          params,
          baseUrl,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("assets", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
          durationMs: Date.now() - startMs,
          collectedBy: EXTENSION_NAME,
        });

        context.logger.info("Found {count} assets", { count: results.length });
        return { dataHandles: [handle] };
      },
    },
    create_asset: {
      description: "create operation",
      arguments: z.object({
        bucket_id: z.string(),
        name: z.string().min(1).max(256),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["bucket_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "PUT",
          `/api/buckets/${encodeURIComponent(String(args.bucket_id))}/assets`,
          body,
          baseUrl,
        );

        const record = result as Record<string, unknown>;
        const pathParamValues = new Set<string>([String(args.bucket_id)]);
        const idCandidates = ["asset_id", "id"];
        let rawId = idCandidates.map((k) => record[k]).find((v) =>
          v !== undefined && v !== null && v !== ""
        );
        if (rawId === undefined) {
          // No named id candidate matched. Scan for a single id-shaped key that is
          // not organization_id and whose value is not a path param (the parent
          // addressing context, e.g. bucket_id on an asset — would collide).
          for (const [k, v] of Object.entries(record)) {
            if (
              /(^|_)id$/.test(k) && k !== "organization_id" &&
              typeof v === "string" && v !== "" && !pathParamValues.has(v)
            ) {
              rawId = v;
              break;
            }
          }
        }
        if (
          rawId === undefined && typeof record.name === "string" &&
          record.name !== ""
        ) {
          rawId = record.name;
        }
        const fallbackId =
          [String(args.bucket_id)].filter((s: string) => s && s !== "undefined")
            .join("_") || "created";
        const id = sanitizeInstanceName(String(rawId ?? fallbackId));
        const handle = await context.writeResource("asset", id, result);
        context.logger.info("Created asset {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_asset: {
      description: "get operation",
      arguments: z.object({
        name: z.string(),
        bucket_id: z.string(),
        include_contents: z.boolean().optional().describe(
          "true/false to include contents for the asset.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "GET",
          `/api/buckets/${encodeURIComponent(String(args.bucket_id))}/assets/${
            encodeURIComponent(String(args.name))
          }`,
          undefined,
          baseUrl,
        );

        const handle = await context.writeResource(
          "asset",
          sanitizeInstanceName(String(args.name)),
          result,
        );
        context.logger.info("Fetched asset", {});
        return { dataHandles: [handle] };
      },
    },
    delete_asset: {
      description: "delete operation",
      arguments: z.object({
        name: z.string(),
        bucket_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        await griptapeApi<Record<string, unknown>>(
          apiKey,
          "DELETE",
          `/api/buckets/${encodeURIComponent(String(args.bucket_id))}/assets/${
            encodeURIComponent(String(args.name))
          }`,
          undefined,
          baseUrl,
        );

        context.logger.info("Deleted resource {id}", { id: args.name });
        return { dataHandles: [] };
      },
    },
  },
};
