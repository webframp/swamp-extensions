/**
 * Griptape Cloud Tools — hosted tools, activities, deployments, and tool runs
 *
 * Auto-generated by scripts/griptape-codegen — do not edit manually.
 *
 * @module
 */
// SPDX-License-Identifier: Apache-2.0

import { z } from "npm:zod@4.4.3";
import {
  griptapeApi,
  griptapeApiPaginated,
  sanitizeInstanceName,
} from "./_lib/api.ts";

const EXTENSION_NAME = "@webframp/griptape/tools";

// =============================================================================
// Schemas
// =============================================================================

const GlobalArgsSchema = z.object({
  apiKey: z.string().meta({ sensitive: true }).describe(
    "Griptape Cloud API key; overrides the GT_CLOUD_API_KEY environment variable. Wire with a vault.get(...) expression to source it from a vault.",
  ).optional(),
  baseUrl: z.string().describe(
    "Griptape Cloud API base URL; overrides the GT_CLOUD_BASE_URL environment variable and the built-in default.",
  ).optional(),
});

const GetToolRunSchema = z.looseObject({
  completed_at: z.string().nullable().default(null),
  created_at: z.string(),
  created_by: z.string(),
  deployment_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ).optional(),
  env_vars: z.array(z.unknown()).optional(),
  input: z.unknown(),
  output: z.unknown().optional(),
  output_timestamp: z.number().optional(),
  runtime_path: z.string(),
  started_at: z.string().nullable().default(null),
  status: z.unknown(),
  status_detail: z.unknown().optional(),
  tool_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  tool_run_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  updated_at: z.string(),
});

const ToolRunLogsItemSchema = z.string();

const ListToolRunLogsSchema = z.object({
  items: z.array(ToolRunLogsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const ToolsItemSchema = z.looseObject({
  code: z.unknown(),
  created_at: z.string(),
  created_by: z.string(),
  description: z.string(),
  env_vars: z.array(z.unknown()),
  latest_deployment_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  name: z.string(),
  organization_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  tool_config_file: z.string().optional(),
  tool_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  updated_at: z.string(),
});

const ListToolsSchema = z.object({
  items: z.array(ToolsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const CreateToolSchema = z.looseObject({
  code: z.unknown(),
  created_at: z.string(),
  created_by: z.string(),
  description: z.string(),
  env_vars: z.array(z.unknown()),
  latest_deployment_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  name: z.string(),
  organization_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  tool_config_file: z.string().optional(),
  tool_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  updated_at: z.string(),
});

const CreateToolActivityRunSchema = z.unknown();

const ToolDeploymentsItemSchema = z.looseObject({
  code_source: z.unknown(),
  completed_at: z.string().nullable().optional().default(null),
  created_at: z.string(),
  created_by: z.string(),
  deployment_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  status: z.unknown(),
  status_detail: z.unknown().optional(),
  tool_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
});

const ListToolDeploymentsSchema = z.object({
  items: z.array(ToolDeploymentsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

const CreateToolDeploymentSchema = z.looseObject({
  code_source: z.unknown(),
  completed_at: z.string().nullable().optional().default(null),
  created_at: z.string(),
  created_by: z.string(),
  deployment_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  status: z.unknown(),
  status_detail: z.unknown().optional(),
  tool_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
});

const GetToolOpenApiRunSchema = z.unknown();

const ToolRunsItemSchema = z.looseObject({
  completed_at: z.string().nullable().default(null),
  created_at: z.string(),
  created_by: z.string(),
  deployment_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ).optional(),
  env_vars: z.array(z.unknown()).optional(),
  input: z.unknown(),
  output: z.unknown().optional(),
  output_timestamp: z.number().optional(),
  runtime_path: z.string(),
  started_at: z.string().nullable().default(null),
  status: z.unknown(),
  status_detail: z.unknown().optional(),
  tool_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  tool_run_id: z.string().regex(
    new RegExp(
      "^[0-9(a-f|A-F)]{8}-[0-9(a-f|A-F)]{4}-4[0-9(a-f|A-F)]{3}-[89ab][0-9(a-f|A-F)]{3}-[0-9(a-f|A-F)]{12}$",
    ),
  ),
  updated_at: z.string(),
});

const ListToolRunsSchema = z.object({
  items: z.array(ToolRunsItemSchema),
  truncated: z.boolean(),
  fetchedAt: z.string(),
  durationMs: z.number().optional().describe(
    "Method execution duration in milliseconds",
  ),
  collectedBy: z.string().optional().describe(
    "Extension that collected this data",
  ),
});

// =============================================================================
// Model Definition
// =============================================================================

/** Griptape Cloud Tools — hosted tools, activities, deployments, and tool runs */
export const model = {
  type: "@webframp/griptape/tools",
  version: "2026.08.28.1",
  globalArguments: GlobalArgsSchema,

  upgrades: [],

  resources: {
    "tool_run": {
      description: "get operation",
      schema: GetToolRunSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "tool_run_logs": {
      description: "list operation",
      schema: ListToolRunLogsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "tools": {
      description: "list operation",
      schema: ListToolsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "tool": {
      description: "create operation",
      schema: CreateToolSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "tool_activity_run": {
      description: "create operation",
      schema: CreateToolActivityRunSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "tool_deployments": {
      description: "list operation",
      schema: ListToolDeploymentsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
    "tool_deployment": {
      description: "create operation",
      schema: CreateToolDeploymentSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "tool_open_api_run": {
      description: "get operation",
      schema: GetToolOpenApiRunSchema,
      lifetime: "infinite" as const,
      garbageCollection: 20,
    },
    "tool_runs": {
      description: "list operation",
      schema: ListToolRunsSchema,
      lifetime: "infinite" as const,
      garbageCollection: 10,
    },
  },

  methods: {
    get_tool_run: {
      description: "get operation",
      arguments: z.object({
        tool_run_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "GET",
          `/api/tool-runs/${encodeURIComponent(String(args.tool_run_id))}`,
          undefined,
          baseUrl,
        );

        const handle = await context.writeResource(
          "tool_run",
          sanitizeInstanceName(String(args.tool_run_id)),
          result,
        );
        context.logger.info("Fetched tool_run", {});
        return { dataHandles: [handle] };
      },
    },
    list_tool_run_logs: {
      description: "list operation",
      arguments: z.object({
        tool_run_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const startMs = Date.now();
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["tool_run_id", "page", "page_size"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await griptapeApiPaginated<
          Record<string, unknown>
        >(
          apiKey,
          `/api/tool-runs/${encodeURIComponent(String(args.tool_run_id))}/logs`,
          "logs",
          params,
          baseUrl,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("tool_run_logs", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
          durationMs: Date.now() - startMs,
          collectedBy: EXTENSION_NAME,
        });

        context.logger.info("Found {count} tool_run_logs", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    list_tools: {
      description: "list operation",
      arguments: z.object({
        page: z.number().optional(),
        page_size: z.number().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const startMs = Date.now();
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["page", "page_size"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await griptapeApiPaginated<
          Record<string, unknown>
        >(
          apiKey,
          `/api/tools`,
          "tools",
          params,
          baseUrl,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("tools", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
          durationMs: Date.now() - startMs,
          collectedBy: EXTENSION_NAME,
        });

        context.logger.info("Found {count} tools", { count: results.length });
        return { dataHandles: [handle] };
      },
    },
    create_tool: {
      description: "create operation",
      arguments: z.object({
        code: z.unknown().optional(),
        description: z.string().min(1).max(200).optional(),
        env_vars: z.array(z.unknown()).optional(),
        name: z.string().min(1).max(200),
        tool_config_file: z.string().min(1).max(200).optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const body = args;

        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "POST",
          `/api/tools`,
          body,
          baseUrl,
        );

        const record = result as Record<string, unknown>;
        const pathParamValues = new Set<string>([]);
        const idCandidates = ["tool_id", "id"];
        let rawId = idCandidates.map((k) => record[k]).find((v) =>
          v !== undefined && v !== null && v !== ""
        );
        if (rawId === undefined) {
          // No named id candidate matched. Scan for a single id-shaped key that is
          // not organization_id and whose value is not a path param (the parent
          // addressing context, e.g. bucket_id on an asset — would collide).
          for (const [k, v] of Object.entries(record)) {
            if (
              /(^|_)id$/.test(k) && k !== "organization_id" &&
              typeof v === "string" && v !== "" && !pathParamValues.has(v)
            ) {
              rawId = v;
              break;
            }
          }
        }
        if (
          rawId === undefined && typeof record.name === "string" &&
          record.name !== ""
        ) {
          rawId = record.name;
        }
        const fallbackId =
          [].filter((s: string) => s && s !== "undefined").join("_") ||
          "created";
        const id = sanitizeInstanceName(String(rawId ?? fallbackId));
        const handle = await context.writeResource("tool", id, result);
        context.logger.info("Created tool {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_tool: {
      description: "get operation",
      arguments: z.object({
        tool_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "GET",
          `/api/tools/${encodeURIComponent(String(args.tool_id))}`,
          undefined,
          baseUrl,
        );

        const handle = await context.writeResource(
          "tool",
          sanitizeInstanceName(String(args.tool_id)),
          result,
        );
        context.logger.info("Fetched tool", {});
        return { dataHandles: [handle] };
      },
    },
    update_tool: {
      description: "update operation",
      arguments: z.object({
        tool_id: z.string(),
        code: z.unknown().optional(),
        description: z.string().min(1).max(200).optional(),
        env_vars: z.array(z.unknown()).optional(),
        name: z.string().min(1).max(200).optional(),
        tool_config_file: z.string().min(1).max(200).optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["tool_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "PATCH",
          `/api/tools/${encodeURIComponent(String(args.tool_id))}`,
          body,
          baseUrl,
        );

        const handle = await context.writeResource(
          "tool",
          sanitizeInstanceName(String(args.tool_id)),
          result,
        );
        context.logger.info("Updated tool", {});
        return { dataHandles: [handle] };
      },
    },
    delete_tool: {
      description: "delete operation",
      arguments: z.object({
        tool_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        await griptapeApi<Record<string, unknown>>(
          apiKey,
          "DELETE",
          `/api/tools/${encodeURIComponent(String(args.tool_id))}`,
          undefined,
          baseUrl,
        );

        context.logger.info("Deleted resource {id}", { id: args.tool_id });
        return { dataHandles: [] };
      },
    },
    create_tool_activity_run: {
      description: "create operation",
      arguments: z.object({
        tool_id: z.string(),
        activity_path: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["tool_id", "activity_path"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "POST",
          `/api/tools/${encodeURIComponent(String(args.tool_id))}/activities/${
            encodeURIComponent(String(args.activity_path))
          }`,
          body,
          baseUrl,
        );

        const record = result as Record<string, unknown>;
        const pathParamValues = new Set<string>([
          String(args.tool_id),
          String(args.activity_path),
        ]);
        const idCandidates = ["tool_activity_run_id", "id"];
        let rawId = idCandidates.map((k) => record[k]).find((v) =>
          v !== undefined && v !== null && v !== ""
        );
        if (rawId === undefined) {
          // No named id candidate matched. Scan for a single id-shaped key that is
          // not organization_id and whose value is not a path param (the parent
          // addressing context, e.g. bucket_id on an asset — would collide).
          for (const [k, v] of Object.entries(record)) {
            if (
              /(^|_)id$/.test(k) && k !== "organization_id" &&
              typeof v === "string" && v !== "" && !pathParamValues.has(v)
            ) {
              rawId = v;
              break;
            }
          }
        }
        if (
          rawId === undefined && typeof record.name === "string" &&
          record.name !== ""
        ) {
          rawId = record.name;
        }
        const fallbackId =
          [String(args.tool_id), String(args.activity_path)].filter((
            s: string,
          ) => s && s !== "undefined").join("_") || "created";
        const id = sanitizeInstanceName(String(rawId ?? fallbackId));
        const handle = await context.writeResource(
          "tool_activity_run",
          id,
          result,
        );
        context.logger.info("Created tool_activity_run {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    list_tool_deployments: {
      description: "list operation",
      arguments: z.object({
        tool_id: z.string(),
        page: z.number().optional(),
        page_size: z.number().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const startMs = Date.now();
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["tool_id", "page", "page_size"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await griptapeApiPaginated<
          Record<string, unknown>
        >(
          apiKey,
          `/api/tools/${encodeURIComponent(String(args.tool_id))}/deployments`,
          "deployments",
          params,
          baseUrl,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("tool_deployments", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
          durationMs: Date.now() - startMs,
          collectedBy: EXTENSION_NAME,
        });

        context.logger.info("Found {count} tool_deployments", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
    create_tool_deployment: {
      description: "create operation",
      arguments: z.object({
        tool_id: z.string(),
        code_source: z.unknown().optional(),
        force: z.boolean().optional(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const body: Record<string, unknown> = {};
        const excludeKeys = new Set(["tool_id"]);
        for (const [k, v] of Object.entries(args)) {
          if (!excludeKeys.has(k)) body[k] = v;
        }

        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "POST",
          `/api/tools/${encodeURIComponent(String(args.tool_id))}/deployments`,
          body,
          baseUrl,
        );

        const record = result as Record<string, unknown>;
        const pathParamValues = new Set<string>([String(args.tool_id)]);
        const idCandidates = ["tool_deployment_id", "id"];
        let rawId = idCandidates.map((k) => record[k]).find((v) =>
          v !== undefined && v !== null && v !== ""
        );
        if (rawId === undefined) {
          // No named id candidate matched. Scan for a single id-shaped key that is
          // not organization_id and whose value is not a path param (the parent
          // addressing context, e.g. bucket_id on an asset — would collide).
          for (const [k, v] of Object.entries(record)) {
            if (
              /(^|_)id$/.test(k) && k !== "organization_id" &&
              typeof v === "string" && v !== "" && !pathParamValues.has(v)
            ) {
              rawId = v;
              break;
            }
          }
        }
        if (
          rawId === undefined && typeof record.name === "string" &&
          record.name !== ""
        ) {
          rawId = record.name;
        }
        const fallbackId =
          [String(args.tool_id)].filter((s: string) => s && s !== "undefined")
            .join("_") || "created";
        const id = sanitizeInstanceName(String(rawId ?? fallbackId));
        const handle = await context.writeResource(
          "tool_deployment",
          id,
          result,
        );
        context.logger.info("Created tool_deployment {id}", { id });
        return { dataHandles: [handle] };
      },
    },
    get_tool_open_api_run: {
      description: "get operation",
      arguments: z.object({
        tool_id: z.string(),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const result = await griptapeApi<Record<string, unknown>>(
          apiKey,
          "GET",
          `/api/tools/${encodeURIComponent(String(args.tool_id))}/openapi`,
          undefined,
          baseUrl,
        );

        const handle = await context.writeResource(
          "tool_open_api_run",
          sanitizeInstanceName(String(args.tool_id)),
          result,
        );
        context.logger.info("Fetched tool_open_api_run", {});
        return { dataHandles: [handle] };
      },
    },
    list_tool_runs: {
      description: "list operation",
      arguments: z.object({
        tool_id: z.string(),
        page: z.number().optional(),
        page_size: z.number().optional(),
        status: z.string().optional().describe(
          "Comma-separated list of run statuses to filter by.",
        ),
      }),
      execute: async (
        args: Record<string, unknown>,
        context: {
          globalArgs: Record<string, string>;
          writeResource: (
            spec: string,
            instance: string,
            data: unknown,
          ) => Promise<{ name: string }>;
          logger: {
            info: (msg: string, props: Record<string, unknown>) => void;
          };
        },
      ) => {
        const { apiKey, baseUrl } = context.globalArgs;
        const startMs = Date.now();
        const params: Record<string, string> = {};
        const excludeKeys = new Set(["tool_id", "page", "page_size"]);
        for (const [k, v] of Object.entries(args)) {
          if (v !== undefined && !excludeKeys.has(k)) params[k] = String(v);
        }

        const { results, truncated } = await griptapeApiPaginated<
          Record<string, unknown>
        >(
          apiKey,
          `/api/tools/${encodeURIComponent(String(args.tool_id))}/runs`,
          "tool_runs",
          params,
          baseUrl,
        );

        if (truncated) {
          context.logger.info(
            "WARNING: results truncated at {count} (pagination cap)",
            { count: results.length },
          );
        }

        const handle = await context.writeResource("tool_runs", "main", {
          items: results,
          truncated,
          fetchedAt: new Date().toISOString(),
          durationMs: Date.now() - startMs,
          collectedBy: EXTENSION_NAME,
        });

        context.logger.info("Found {count} tool_runs", {
          count: results.length,
        });
        return { dataHandles: [handle] };
      },
    },
  },
};
