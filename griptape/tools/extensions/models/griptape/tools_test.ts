// Griptape Cloud Tools — hosted tools, activities, deployments, and tool runs — Tests
// Auto-generated by scripts/griptape-codegen — do not edit manually.
// SPDX-License-Identifier: Apache-2.0

import { assertEquals, assertExists } from "jsr:@std/assert@1.0.19";
import { createModelTestContext } from "@systeminit/swamp-testing";
import { model } from "./tools.ts";

// ---------------------------------------------------------------------------
// Model Export Structure Tests
// ---------------------------------------------------------------------------

Deno.test("tools model: has correct type", () => {
  assertEquals(model.type, "@webframp/griptape/tools");
});

Deno.test("tools model: has valid version format", () => {
  const versionPattern = /^\d{4}\.\d{2}\.\d{2}\.\d+$/;
  assertEquals(versionPattern.test(model.version), true);
});

Deno.test("tools model: has globalArguments with apiKey", () => {
  assertExists(model.globalArguments);
  const shape = model.globalArguments.shape;
  assertExists(shape.apiKey);
});

Deno.test("tools model: has expected methods", () => {
  assertExists(model.methods);
  assertExists(model.methods.get_tool_run);
  assertExists(model.methods.list_tool_run_logs);
  assertExists(model.methods.list_tools);
  assertExists(model.methods.create_tool);
  assertExists(model.methods.get_tool);
  assertExists(model.methods.update_tool);
  assertExists(model.methods.delete_tool);
  assertExists(model.methods.create_tool_activity_run);
  assertExists(model.methods.list_tool_deployments);
  assertExists(model.methods.create_tool_deployment);
  assertExists(model.methods.get_tool_open_api_run);
  assertExists(model.methods.list_tool_runs);
});

Deno.test("tools model: has expected resources", () => {
  assertExists(model.resources);
  assertExists(model.resources["tool_run"]);
  assertExists(model.resources["tool_run_logs"]);
  assertExists(model.resources["tools"]);
  assertExists(model.resources["tool"]);
  assertExists(model.resources["tool_activity_run"]);
  assertExists(model.resources["tool_deployments"]);
  assertExists(model.resources["tool_deployment"]);
  assertExists(model.resources["tool_open_api_run"]);
  assertExists(model.resources["tool_runs"]);
});

// ---------------------------------------------------------------------------
// Mock Griptape API Server
// ---------------------------------------------------------------------------

interface MockResponse {
  body: unknown;
  itemsKey?: string;
  status?: number;
}

function startMockGtServer(
  responses: Record<string, MockResponse>,
): { url: string; server: Deno.HttpServer } {
  const server = Deno.serve({ port: 0, onListen() {} }, (req) => {
    const url = new URL(req.url);
    const path = url.pathname;

    // Guard against a doubled "/api" prefix (base URL + already-/api-prefixed
    // path). This is invisible to path.includes() matching, so assert it here:
    // a regression that reintroduces it fails the test loudly instead of 404ing
    // only in production.
    if (path.includes("/api/api/")) {
      return Response.json(
        { message: `doubled /api prefix in request path: ${path}` },
        { status: 500 },
      );
    }

    for (const [pattern, spec] of Object.entries(responses)) {
      if (path.includes(pattern)) {
        const status = spec.status ?? 200;
        // 204/205 must have a null body.
        if (status === 204 || status === 205) {
          return new Response(null, { status });
        }
        if (spec.itemsKey) {
          return Response.json({
            [spec.itemsKey]: spec.body,
            pagination: {
              page_number: 1,
              page_size: 50,
              total_count: Array.isArray(spec.body) ? spec.body.length : 0,
              total_pages: 1,
            },
          }, { status });
        }
        return Response.json(spec.body, { status });
      }
    }

    return Response.json({ message: "Not found" }, { status: 404 });
  });

  const addr = server.addr as Deno.NetAddr;
  return { url: `http://localhost:${addr.port}`, server };
}

function installFetchMock(mockUrl: string): () => void {
  const originalFetch = globalThis.fetch;
  globalThis.fetch = (input, init) => {
    const reqUrl = typeof input === "string" ? input : input.toString();
    const newUrl = reqUrl.replace("https://cloud.griptape.ai", mockUrl);
    return originalFetch(newUrl, init);
  };
  return () => {
    globalThis.fetch = originalFetch;
  };
}

Deno.test({
  name: "tools model: get_tool_run fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockGtServer({
      "/api/tool-runs/test-id-123": {
        body: {
          "completed_at": "2024-01-01T00:00:00Z",
          "created_at": "2024-01-01T00:00:00Z",
          "created_by": "test-value",
          "deployment_id": "test-value",
          "env_vars": [null],
          "input": null,
          "output": null,
          "output_timestamp": 1,
          "runtime_path": "test-value",
          "started_at": "2024-01-01T00:00:00Z",
          "status": null,
          "status_detail": null,
          "tool_id": "test-value",
          "tool_run_id": "test-value",
          "updated_at": "2024-01-01T00:00:00Z",
        },
      },
    });
    const uninstall = installFetchMock(url);
    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiKey": "test-key" },
        definition: { id: "test-id", name: "test-tools", version: 1, tags: {} },
      });
      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).get_tool_run.execute({ "tool_run_id": "test-id-123" }, context);
      assertEquals(result.dataHandles.length, 1);
      assertEquals(getWrittenResources().length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "tools model: list_tool_run_logs fetches and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockGtServer({
      "/api/tool-runs/test-id-123/logs": {
        body: [{ "value": "test-value" }],
        itemsKey: "logs",
      },
    });
    const uninstall = installFetchMock(url);
    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiKey": "test-key" },
        definition: { id: "test-id", name: "test-tools", version: 1, tags: {} },
      });
      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).list_tool_run_logs.execute(
        { "tool_run_id": "test-id-123" },
        context,
      );
      assertEquals(result.dataHandles.length, 1);
      assertEquals(getWrittenResources().length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "tools model: create_tool creates and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockGtServer({
      "/api/tools": {
        body: {
          "tool_id": "test-value",
          "code": null,
          "created_at": "2024-01-01T00:00:00Z",
          "created_by": "test-value",
          "description": "test-value",
          "env_vars": [null],
          "latest_deployment_id": "test-value",
          "name": "test-value",
          "organization_id": "test-value",
          "tool_config_file": "test-value",
          "updated_at": "2024-01-01T00:00:00Z",
        },
      },
    });
    const uninstall = installFetchMock(url);
    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiKey": "test-key" },
        definition: { id: "test-id", name: "test-tools", version: 1, tags: {} },
      });
      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).create_tool.execute({
        "code": null,
        "created_at": "2024-01-01T00:00:00Z",
        "created_by": "test-value",
        "description": "test-value",
        "env_vars": [null],
        "latest_deployment_id": "test-value",
        "name": "test-value",
        "organization_id": "test-value",
        "tool_config_file": "test-value",
        "tool_id": "test-value",
        "updated_at": "2024-01-01T00:00:00Z",
      }, context);
      assertEquals(result.dataHandles.length, 1);
      assertEquals(getWrittenResources().length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "tools model: update_tool executes and writes resource",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockGtServer({
      "/api/tools/test-id-123": {
        body: {
          "code": null,
          "created_at": "2024-01-01T00:00:00Z",
          "created_by": "test-value",
          "description": "test-value",
          "env_vars": [null],
          "latest_deployment_id": "test-value",
          "name": "test-value",
          "organization_id": "test-value",
          "tool_config_file": "test-value",
          "tool_id": "test-value",
          "updated_at": "2024-01-01T00:00:00Z",
        },
      },
    });
    const uninstall = installFetchMock(url);
    try {
      const { context, getWrittenResources } = createModelTestContext({
        globalArgs: { "apiKey": "test-key" },
        definition: { id: "test-id", name: "test-tools", version: 1, tags: {} },
      });
      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).update_tool.execute({
        "code": null,
        "created_at": "2024-01-01T00:00:00Z",
        "created_by": "test-value",
        "description": "test-value",
        "env_vars": [null],
        "latest_deployment_id": "test-value",
        "name": "test-value",
        "organization_id": "test-value",
        "tool_config_file": "test-value",
        "tool_id": "test-id-123",
        "updated_at": "2024-01-01T00:00:00Z",
      }, context);
      assertEquals(result.dataHandles.length, 1);
      assertEquals(getWrittenResources().length, 1);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});

Deno.test({
  name: "tools model: delete_tool executes successfully",
  sanitizeResources: false,
  fn: async () => {
    const { url, server } = startMockGtServer({
      "/api/tools/test-id-123": { body: {}, status: 204 },
    });
    const uninstall = installFetchMock(url);
    try {
      const { context } = createModelTestContext({
        globalArgs: { "apiKey": "test-key" },
        definition: { id: "test-id", name: "test-tools", version: 1, tags: {} },
      });
      const result = await (model.methods as Record<
        string,
        {
          execute: (
            args: Record<string, unknown>,
            ctx: unknown,
          ) => Promise<{ dataHandles: unknown[] }>;
        }
      >).delete_tool.execute({ "tool_id": "test-id-123" }, context);
      assertEquals(result.dataHandles.length, 0);
    } finally {
      uninstall();
      await server.shutdown();
    }
  },
});
