## 2026.07.18.2

**Added:** An `upgrades` array entry (no-op) to `networking.ts` for proper `typeVersion` tracking on existing instances. No schema or behavior changes.

## 2026.07.18.1

**Changed:** Bumped `@aws-sdk/client-cloudwatch`, `@aws-sdk/client-ec2`, and
`@aws-sdk/client-elastic-load-balancing-v2` from `3.1069.0` to `3.1090.0` for
dependency freshness. No behavior change.
