## 2026.08.24.2

**Fixed:** Corrected installation command (`swamp extension install` →
`swamp extension pull`).

**Added:** Troubleshooting section documenting the `MAX_PAGES = 10` cap in
`get_summary`, the silent `parseRelativeTime` fallback, single-page history
limitation, and region-scope guidance.
